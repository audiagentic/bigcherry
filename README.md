# bigcherry
A dynamically tunable LLamacpp derivative. Creates tuned kernal maps for every signature encountered for specific models optimizing the live run rather than relying on hardcoded kernal selection heuristics.
