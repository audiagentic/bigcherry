# HI168: production-shaped stock/native baseline

## Balanced smoke capture

Run `native-stock-smoke-003`, orchestration revision `34aa876f798e32ded84f1cc8c9764648927d6578`.
Four real server cells completed through the maintained
`bench/run_bench.py --bench-type server-bench` endpoint runner.
No llama-bench, injected observer, or ABI probe was used.

The configured build server was idle before launch. Both arms used the 27B
Q8_0 model, dual gfx1100, tensor split, context 4096, batch 2048, ubatch 512,
eight CPU threads, one slot, flash attention, default logging and no
speculative decoding. Compiler observations confirmed diagnostics OFF.
The same requested runtime settings and common CMake settings were checked.

| Round | First arm | Stock pp512 | BC-native pp512 | Stock tg128 | BC-native tg128 |
| --- | --- | ---: | ---: | ---: | ---: |
| 1 | stock | 928.90 | 930.92 | 34.19 | 34.34 |
| 2 | native | 929.21 | 930.17 | 34.32 | 34.22 |

Units: tokens/second. Observed paired geometric differences were +0.160% pp512
and +0.073% tg128. Two pairs are a smoke baseline, **not a parity verdict**.
Every cell shut down cleanly: stock via SIGINT, BC via HTTP; no forced kill.

Raw harness timings report identical basic work counts in all cells:
pp512: 524 evaluated prompt tokens and 128 generated tokens;
tg128: 9 evaluated prompt tokens and 128 generated tokens; cache_n=0.
These counts do not establish output equivalence or production MTP behavior.

## Provenance and limitations

- Stock source slice: `e77c7964e5dc0ba3bf8bddc644b3cd78`; build plan: `cdb680dd53f7e17b2d1ec7e679279112`.
- BC-native source slice: `343f189d403698ef41d7d62248a2ca82`; build plan: `e0153a726f9b446d5e9c1b197dfb752c`.
- Live source attestations and runtime-library hashes are retained in the run manifest.
- Local retained manifest: `artifacts/hi168-runs/native-stock-smoke-003-run.json`.
- Manifest SHA256: `5f19a291372a8bcee05033c167d481cc35b60581061918c24e954fcd0d9a2b53`.
- Machine paths remain in local artifacts/environment configuration, not this procedure record.
- Explicit `execution_evidence=observe`: default server logs lack physical-device evidence. Every cell records that limitation and `performance_admitted=false`.
- Stock versus framework-native describes the whole composition, not causal attribution to one patch. No replay winner or MTP qualification follows.

## Completed six-pair comparison

Predeclared `native-stock-balanced-001`: six paired rounds, seed 168,
20-second inter-cell settling, pp512/pp2048/tg512, one repetition per cell.
Used the same completed binaries and production-shaped non-speculative
runtime settings. All 12 cells completed, with matching basic work counts
and clean shutdowns. No optional attestation probe was used.

| Metric | BC native versus stock | Paired bootstrap 95% interval |
| --- | ---: | ---: |
| pp512 | -0.213% | -0.827% to +0.421% |
| pp2048 | +1.343% | -0.092% to +3.979% |
| tg512 | -0.415% | -0.778% to +0.029% |

These exploratory within-run estimates do not establish parity. The pp2048
average is dominated by round 2's slow stock cell (1192.97 tokens/s versus
roughly 1284 in the other stock cells); it is retained, not discarded.
The pp512 and tg512 point estimates are negative and merit follow-up.
The basic work counts were 524/128 for pp512, 2091/128 for pp2048 and
9/512 for tg512 (evaluated prompt/generated tokens), with cache_n=0.

Retained manifest: `artifacts/hi168-runs/native-stock-balanced-001-run.json`.
SHA256: `1b93fc97dbb1c82f6e61601a3afccff639c8aeacc02d44b14caf20f94abddec7`.
All cells remain `performance_admitted=false`; the physical-device evidence
and output-equivalence limitations above remain applicable.

## Requested single-GPU 9B follow-up

The canonical `tierB-qwen9b-q6k` model is present on the configured build
server. Run each XTX independently, then gfx1201 and gfx1030, using the same
9B Q6_K model, context 8192, flash attention, batch 2048, ubatch 512,
eight CPU threads, one slot and no speculation. This is a single-device
comparison, not the dual-device 27B topology above.

Each row compares stock llama.cpp native, framework BC native and production
BC replay through the maintained endpoint runner, with pp512/pp2048/tg512
and six complete three-arm permutations. These are whole-composition
comparisons; framework-native versus release replay alone does not isolate
the causal effect of dispatch tuning.

Use `tune-campaign --runtime-profile production-safe-single` separately for
each physical device. Require its current behavioral gate and exported cache
before filling the replay arm. Historical 27B or ungated 9B winners do not
satisfy this requirement. Diagnostic companion results remain separate from
production timings. Resolve host/model/cache locations from environment
configuration; keep physical locators in local run configurations.

Single-device benchmark configurations use ROCR to select the physical
device and HIP index zero within that filtered list. The tuning command's
`--devices` uses the physical HIP index with ambient ROCR selection unset.
Do not apply both conventions at once.

Four local comparison configurations exist. After the 27B capture completed
and an idle-device/process check, the first XTX campaign
`hi168-9b-gpu0-20260906` started using orchestration revision `34aa876f`.
Its build/tuning/validation results are pending; no 9B parity result is
claimed yet. Runner/evidence regression checks passed: 60 tests and 4 subtests.
