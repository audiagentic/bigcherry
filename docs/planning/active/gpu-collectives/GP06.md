---
id: GP06
order: 0
plan: gpu-collectives
state: pending
created-at: '2026-09-01T20:01:05.028687+00:00'
breadth: ''
skill: advanced
created-by: agent
work: M
---

# RCCL 2.30.4 regression: HI138/GP01-confirmed-viable {0,2} pair crashes -- pin/qualify exact RCCL version before GP01 Phase 2

## Description

HI138 (closed 2026-08-29, Outcome B) confirmed 20/20 real-hardware repetitions that RCCL is viable and crash-free for {0,1}, {0,2}, {1,2}, {0,1,2} (any topology excluding physical device 3, the RX 6900XT) using RCCL rebuilt from ROCm/rccl commit 57e58688 (reports as RCCL 2.28.3).

Real hardware finding, 2026-09-02 (this session, independent of GP04/GP05's investigation): testing the same {0,2} pair (XTX + R9700, gfx1100+gfx1201) against RCCL 2.30.4 (this box's /home/audumla/rocm-7.14 install, the RCCL version used for this session's other HI18 work) CRASHES -- same failure class HI138 already characterized (RCCL communicator hard-aborts). Re-tested against RCCL 1.0.70204 (/opt/rocm-7.2.4, and the newly-created vendor/rocm/7.2.4-merged -- see TO01) with a fully self-consistent toolchain (compiler+RCCL from the same install, ruling out an ABI-mismatch confound): {0,2} PASSES cleanly (valid=True, correct=True via the HI18 SPLIT_REDUCE probe), matching HI138's original result. {0,3} and {2,3} (both including device 3) FAIL on both RCCL versions, consistent with HI138's root cause (device 3's chipset-routed PCH root port lacks PCIe AtomicOps capability -- an RCCL-version-independent hardware limitation).

So: HI138's Phase 1 closure and GP01's planned Phase 2 qualification were both established/scoped against RCCL 2.28.3-era builds. This box's newer default RCCL (2.30.4) appears to have REGRESSED the {0,2}/{1,2} viability HI138 confirmed -- a real, version-specific finding neither HI138 nor GP01 anticipated.

## Steps

SPLIT per gpt-dev-agent review (2026-09-02): the source bisection (2.28.3 vs 2.30.4) does NOT need to block GP01 if the decision is to pin the already-known-good 7.2.4/1.0.70204 stack. Two independent tracks:

Track A (unblocks GP01, do first): pin the known-good revision as reproducible.
1. DONE (2026-09-02): raw evidence preserved from /tmp to artifacts/rccl-heterogeneous/gp06-version-comparison/ on Brutus (environment.txt + cases.jsonl x3 + per-case stdout/stderr).
2. Re-run the full P1.12 fresh-process gate (20 reps) for {0,2} and {1,2} against RCCL 1.0.70204 (vendor/rocm/7.2.4) specifically, plus {0,1} positive control and {0,3} negative control, through the HARDENED campaign driver once GP07 lands (not another ad-hoc script).
3. Record the result as GP01's exact RCCLCompatibilityRevision pin -- GP01 Phase 2 may then proceed against 1.0.70204 without waiting on Track B.

Track B (does not block GP01, pursue after 0840/GP02 land per gpt's priority order): source-level bisection of what regressed between 1.0.70204 and 2.30.4.
1. Diff RCCL source (57e58688f4... vs the 2.30.4 tag/commit) for generic-kernel/hostcall-metadata generation changes, per HI138's own root-cause mechanism.
2. Only pursue if shipping 2.30.4/newer ROCm becomes an actual production requirement -- otherwise this is upgrade-enablement, not the shortest path to working heterogeneous serving.

## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files



## Validation

20/20 fresh-process repetitions of {0,2} and {1,2} against both RCCL versions, with results and exact RCCL source/build identity recorded per RCCL_HETEROGENEOUS_RUNBOOK.md's required artifact layout. GP01 Phase 2 qualification must not proceed on an RCCL version that hasn't passed this gate.

## Effort & Risk



## Standards



## Acceptance Criteria



## Notes

Blocks GP01 (Phase 2 qualification is meaningless if the RCCL version isn't pinned/known-good). Related: HI138 (docs/planning/completed/hip-collectives/HI138.md, closed, reference-only -- this item does not reopen or contradict HI138's device-3 root-cause finding, only adds a new RCCL-version dimension HI138 didn't test). Toolchain context: TO01 (vendor/rocm inventory) -- this session created vendor/rocm/7.2.4-merged (va15-rocm-merged's compiler layout + vendor/rocm/7.2.4's complete RCCL-including lib/) to make the known-good 2.28.3-era RCCL buildable without manual -Drccl_DIR overrides.

Blocks GP01 (Phase 2 qualification is meaningless if the RCCL version isn't pinned/known-good). Related: HI138 (docs/planning/completed/hip-collectives/HI138.md, closed, reference-only -- this item does not reopen or contradict HI138's device-3 root-cause finding, only adds a new RCCL-version dimension HI138 didn't test). Toolchain context: TO01 (vendor/rocm inventory) -- this session created vendor/rocm/7.2.4-merged (va15-rocm-merged's compiler layout + vendor/rocm/7.2.4's complete RCCL-including lib/) to make the known-good 2.28.3-era RCCL buildable without manual -Drccl_DIR overrides.

CONFIRMED (2026-09-02) via real hardware 3-way comparison using a corrected tools/bigcherry/profiling/rccl_qualify.py (fixed a real false-positive UNSUPPORTED classification bug found during this run) and a new ROCm 10.0.0 install (vendor/rocm/10.0). Matrix: {0,1} homogeneous control, {0,2}/{1,2} (HI138-confirmed-viable heterogeneous), {0,3} (HI138-excluded device-3 negative control) x Ring/Tree x Simple/LL/LL128 x element_count in {30720, 2621440}.

Results:
- RCCL 1.0.70204 (vendor/rocm/7.2.4): {0,1}/{0,2}/{1,2} all PASS, {0,3} gpu_fault -- exactly reproduces HI138's original disposition.
- RCCL 2.30.4 (vendor/rocm/7.14, TheRock dev build): {0,1} PASS, {0,2}/{1,2}/{0,3} ALL gpu_fault.
- RCCL 2.30.4 (vendor/rocm/10.0, ROCm 10.0.0, freshly installed and independently built): IDENTICAL result to the 7.14 build -- {0,1} PASS, {0,2}/{1,2}/{0,3} ALL gpu_fault.

Conclusion: this is a real RCCL 2.30.4 regression, not a one-off build artifact -- two independently-built ROCm releases (7.14 dev, 10.0.0 stable) sharing that RCCL version string show the identical failure pattern. GP01 Phase 2 qualification and any production RCCL enablement for the heterogeneous {0,2}/{1,2} subset MUST pin RCCL 1.0.70204 (vendor/rocm/7.2.4), not assume any RCCL build on this box behaves identically. Raw evidence: /tmp/rccl-qualify-{7.2.4,7.14,10.0}.jsonl and per-case stdout/stderr logs under /tmp/rccl-qualify-*/ on Brutus (not yet copied into a durable artifacts/ location).

Operational note: installing ROCm 10.0.0 via the vendor runfile installer with a custom target= still ran update-alternatives against system-wide /usr/bin/{hipcc,amdclang,...}, silently repointing the shared default toolchain away from rocm-7.2.4. Caught and reverted (now in manual mode). See TO01 for the durable version of this note.

TOOLING NOTE: plan_update_item's `append` parameter was found this session to silently no-op (reports success, change_log shows "Updated (no visible changes)", content never lands) -- confirmed reproducible with a trivial probe string. All further updates in this session use `updates.notes` with manual concatenation instead. Earlier append calls this session that appeared to succeed for GP01/HI18/HI138/TO01 need to be re-verified and likely re-applied the same way.

Blocks GP01 -- SPLIT per gpt-dev-agent review (2026-09-02): only Track A (pin the known-good 1.0.70204 revision as reproducible, via GP07's hardened tooling) actually blocks GP01; Track B (2.30.4 source bisection) is parallel/nonblocking, pursue only if newer-ROCm shipping becomes a real requirement.

Related: HI138 (docs/planning/completed/hip-collectives/HI138.md, closed, reference-only). Toolchain context: TO01 (vendor/rocm inventory).

CONFIRMED (2026-09-02) via real hardware 3-way comparison using a corrected tools/bigcherry/profiling/rccl_qualify.py. Matrix: {0,1}/{0,2}/{1,2}/{0,3} x Ring/Tree x Simple/LL/LL128 x element_count in {30720, 2621440}. RCCL 1.0.70204 (vendor/rocm/7.2.4): {0,1}/{0,2}/{1,2} PASS, {0,3} gpu_fault -- reproduces HI138 exactly. RCCL 2.30.4 (vendor/rocm/7.14 AND vendor/rocm/10.0, independently built): {0,1} PASS, {0,2}/{1,2}/{0,3} ALL gpu_fault -- real regression, not a build artifact.

EVIDENCE PRESERVED (2026-09-02): raw jsonl + per-case stdout/stderr copied from /tmp to artifacts/rccl-heterogeneous/gp06-version-comparison/ on Brutus (was flagged as at-risk in ephemeral /tmp). No longer at risk of loss.

Operational note: installing ROCm 10.0.0 via the vendor runfile installer with a custom target= still ran update-alternatives against system-wide /usr/bin/{hipcc,amdclang,...}, silently repointing the shared default toolchain. Caught and reverted (now in manual mode). See TO01.

TOOLING NOTE: plan_update_item's `append` parameter was found this session to silently no-op -- use `updates.notes`/`updates.steps` with manual concatenation instead.

TRACK A FORMAL GATE RUN (2026-09-02) via GP07's hardened rccl_qualify_campaign.py -- real hardware, RCCL 2.27.7 (source revision 57e58688f44c77076ad536ef1f6b68741fc6e694, vendor/rocm/7.2.4), 20 reps x {0,1}/{0,2}/{1,2}/{0,3} x Ring/Tree x Simple/LL/LL128 x element_count {30720, 2621440}.

Results:
- {0,1} (positive control): 468 pass, 80 unsupported (legitimate RCCL capability declines, e.g. Ring/LL128 combos it doesn't support for this shape -- not failures), ZERO timeouts, ZERO gpu_fault. The control never failed once across the entire run, including ~300 post-fault rechecks triggered by other topologies' issues -- 100% reliability.
- {0,3} (negative control, device 3): 240/240 gpu_fault, ZERO passes, ZERO other classifications. Exact, 100% consistent match to HI138's known device-3 finding -- zero flakiness.
- {0,2}: 120 pass, 80 unsupported, 40 timeout, ZERO gpu_fault, ZERO wrong_result.
- {1,2}: 132 pass, 80 unsupported, 28 timeout, ZERO gpu_fault, ZERO wrong_result.

CAVEAT (important, not a clean gate pass yet): this run executed CONCURRENTLY with GP04's real-signature correctness corpus (different device pairs -- GP04 used {0,1}, this campaign used {0,2}/{1,2}/{0,3} -- but sharing host/PCIe bandwidth). The {0,2}/{1,2} timeouts are very likely contention-induced, not a real RCCL regression: zero gpu_fault or wrong_result on either pair (a real regression would show as one of those, per HI138/GP06's own established failure signature), and every single post-fault control recheck passed immediately and cleanly. But per the runbook's own P1.12 gate requirement (20/20 clean, zero failures of any kind), this run does NOT count as a formal clean qualification pass for {0,2}/{1,2} -- timeouts are still failures for gate-counting purposes, contention explanation or not.

NEXT (queued, not yet done): re-run {0,2}/{1,2} alone, uncontended (no concurrent GPU workload on the host), for the definitive clean P1.12 gate result. Evidence from this run: artifacts/rccl-heterogeneous/gp06-track-a-2.27.7/ on Brutus (cases.jsonl + per-case stdout/stderr, revision-namespaced).

Given the strong circumstantial evidence already in hand (zero gpu_fault/wrong_result on {0,2}/{1,2}, 100% control reliability throughout), Track A's real conclusion -- RCCL 2.27.7 is the correct pin, {0,1}/{0,2}/{1,2} viable, {0,3} permanently excluded -- is very likely already correct; the clean rerun is to formally close the gate, not because there's real doubt about the direction of the result.

TRACK A FORMAL GATE RUN (2026-09-02) via GP07's hardened rccl_qualify_campaign.py -- real hardware, RCCL 2.27.7 (source revision 57e58688f44c77076ad536ef1f6b68741fc6e694, vendor/rocm/7.2.4), 20 reps x {0,1}/{0,2}/{1,2}/{0,3} x Ring/Tree x Simple/LL/LL128 x element_count {30720, 2621440}.

Results:
- {0,1} (positive control): 468 pass, 80 unsupported (legitimate RCCL capability declines), ZERO timeouts, ZERO gpu_fault. 100% reliable across ~300 post-fault rechecks.
- {0,3} (negative control, device 3): 240/240 gpu_fault, exact 100% match to HI138's finding.
- {0,2}: 120 pass, 80 unsupported, 40 timeout, ZERO gpu_fault, ZERO wrong_result.
- {1,2}: 132 pass, 80 unsupported, 28 timeout, ZERO gpu_fault, ZERO wrong_result.

CORRECTED DIAGNOSIS (2026-09-02, real trace analysis -- supersedes this item's own earlier 'likely contention' guess): every single timeout is Tree algorithm + LL protocol, with ZERO scatter into any other algorithm/protocol combination:
  ('xtx0_r9700'/{0,2}, Tree, LL, n=30720): 20/20 timeout (fully deterministic)
  ('xtx0_r9700'/{0,2}, Tree, LL, n=2621440): 20/20 timeout (fully deterministic)
  ('xtx1_r9700'/{1,2}, Tree, LL, n=30720): 8/20 timeout (flaky/intermittent -- pass/timeout mixed across reps, no positional pattern)
  ('xtx1_r9700'/{1,2}, Tree, LL, n=2621440): 20/20 timeout (fully deterministic)
This rules out host resource contention as the cause: contention would not cluster exclusively on one exact algorithm/protocol pair while Tree+LL128, Tree+Simple, and all Ring combinations pass reliably in the SAME run on the SAME topology. Real trace (NCCL_DEBUG=INFO, full stdout captured) shows: ncclCommInitAll_impl completes cleanly for both ranks (normal ~2.5s init timings, no warnings beyond the routine iommu=pt advisory), the perf harness prints its results-table header, then produces ZERO further output for the full 20s until RCCL Tests' own internal timeout force-aborts the process (comm Abort COMPLETE, socket.cc errors during the abort's own teardown -- secondary noise from forced cleanup, not the original cause). This is consistent with a real hang inside the Tree+LL collective kernel/protocol path specifically for this heterogeneous topology class, not a crash and not environmental.

IMPLICATION: this does NOT invalidate {0,2}/{1,2}'s core RCCL viability -- Ring (the algorithm RCCL's own tuner is expected to select by default without an explicit override) passed 100% cleanly on both pairs, zero gpu_fault/wrong_result/timeout. The finding is narrower and more actionable: Tree+LL specifically must be excluded as an unsafe/hanging candidate for these topologies in any future algorithm/protocol tuning work (directly relevant to GP01's active-tuning track -- do not let Tree+LL enter that candidate set for {0,2}/{1,2} without first root-causing and fixing this hang). A generic 'rerun uncontended' would very likely just reproduce the identical Tree+LL hangs again, since contention was never the real cause -- not queuing that rerun.

STILL TO CONFIRM (real remaining gap, not urgent): what algorithm/protocol RCCL's own tuner actually auto-selects for {0,2}/{1,2} at production-representative sizes WITHOUT an explicit RCCL_OVERRIDE_ALGO/PROTO override -- if it can select Tree+LL automatically under some condition, that's a live production risk, not just a tuning-candidate exclusion. Track as a GP01 active-tuning-track question, not blocking this item's own Track A conclusion.

Track A CONCLUSION: RCCL 2.27.7 (57e58688f4) is confirmed the correct pin. {0,1}/{0,2}/{1,2} are RCCL-viable via Ring (zero real failures on that algorithm across the full run); {0,3} remains permanently excluded (device 3, HI138). GP01 Phase 2 may proceed against this pin. Evidence: artifacts/rccl-heterogeneous/gp06-track-a-2.27.7/ on Brutus (cases.jsonl + full per-case stdout/stderr with NCCL_DEBUG=INFO trace, revision-namespaced).

## Change Log

- 2026-09-01T20:01:05.028687+00:00 (created-by): Created by agent

## Ledger-events

- chg_20260901_200156_reorganized-todays-multi-gpu_3618
- 2026-09-01T20:01:56.123566+00:00 (updated-by): Updated: section:ledger-events
- 2026-09-01T20:37:07.983286+00:00 (updated-by): Updated (no visible changes)
- chg_20260901_203724_confirmed-with-real-hardware-e_3450
- 2026-09-01T20:37:24.088161+00:00 (updated-by): Updated: section:ledger-events
- 2026-09-01T21:30:27.648191+00:00 (updated-by): Updated (no visible changes)
- 2026-09-01T21:30:53.849974+00:00 (updated-by): Updated (no visible changes)
- 2026-09-01T21:31:28.389063+00:00 (updated-by): Updated: section:notes
- 2026-09-01T23:15:25.284314+00:00 (updated-by): Updated: section:steps, section:notes
- 2026-09-02T04:11:55.541400+00:00 (updated-by): Updated: section:notes
- 2026-09-02T04:16:57.870008+00:00 (updated-by): Updated: section:notes
