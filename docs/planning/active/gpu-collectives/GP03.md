---
id: GP03
order: 0
plan: gpu-collectives
state: pending
created-at: '2026-09-01T12:06:44.735660+00:00'
breadth: ''
skill: advanced
created-by: agent
work: M
---

# Consolidate 1243/0840 into ONE production AllReduce dispatch implementation

## Description

RESCOPED 2026-09-02 per explicit operator direction: this plan group's aim is to improve RCCL where it is currently supported, allow RCCL to operate over heterogeneous topologies where the underlying PCIe features it needs are actually available, and provide a safe AND performant fallback where they are not -- not just qualification/documentation of the boundary. Two independent patches (1243, this item's own original implementation, and 0840_hybrid_allreduce_dispatch, written independently) currently exist for the size-adaptive dispatch problem. THIS ITEM'S JOB IS TO END WITH EXACTLY ONE SHIPPED IMPLEMENTATION, not two competing patches.

Direction from gpt-dev-agent's comparative review (2026-09-02): 0840 is the sounder design (correct per-call provider_name/telemetry, threshold derived from the internal pipeline's own real value, forces exact F32 automatically) but has 4 real P1 bugs (missing 0830 dependency; have_internal && !have_nccl large calls wrongly fall to META instead of internal; explicit-override telemetry mislabel; no RCCL admission guard). 1243 is architecturally weaker (independent threshold knob that can drift, manual BF16 handling) but contributed the only real hardware pp/tg evidence so far. Consolidation path: fix 0840's bugs, wire in GP02's admission predicate once it lands, reproduce 1243's real hardware numbers through corrected 0840, THEN retire/delete 1243 entirely -- do not leave both in the tree once 0840 is validated.

EXPLICIT SUCCESS/FAILURE BAR (operator's own words, 2026-09-02): 'if its slower than layer split or meta then we have failed in that scenario.' This applies per-topology, not as one blanket verdict:
- RCCL-viable topologies (PCIe AtomicOps available): dispatch must beat both pure META (-sm layer / butterfly) AND pure internal-only across the full pp/tg range. Already shown directionally true for {0,1} via 1243's numbers; must be reproduced through corrected 0840 with an exact recorded RCCLCompatibilityRevision (1243's numbers have no revision recorded, per GP06 this makes them provisional, not final evidence).
- Device-3 / PCI-atomics-incapable topologies: whatever fallback mechanism ships (see GP09) must be measured faster than META/layer-split for that exact topology, or it must NOT ship -- keep using META there instead. A safe-but-slower special case is itself a failure of this item's aim, not an acceptable compromise.

BASELINE REQUIREMENT (operator's own words): 'baseline is native llamacpp rccl' -- every performance claim in this consolidation must be benchmarked against stock/vanilla llama.cpp's own unpatched RCCL path (not merely against BigCherry's own prior internal-only or META numbers), so it's possible to tell whether BigCherry's dispatch layer is actually adding value over doing nothing.

## Steps

REWRITTEN 2026-09-02 per gpt-dev-agent review (prior Steps here were stale, still describing the old independent-threshold-knob design this rescope explicitly retires):
1. Fix 0840's 4 known P1 bugs (see this item's notes and patches/0840_hybrid_allreduce_dispatch's own docstring): missing requires=["0830_split_reduce_telemetry"] in patch.toml; have_internal && !have_nccl large calls must route to internal, not fall through to META; explicit GGML_HIP_REDUCE_PLAN=rccl telemetry must report the actual servicing provider, not a stale provider_name; GGML_CUDA_AR_COPY_THRESHOLD=0 sentinel semantics must match the internal pipeline's own interpretation (0 = never use copy-engine), not 0840's inverted reading.
2. Wire GP02's shared admission predicate into 0840's ggml_backend_cuda_comm_init_hybrid() before its ncclCommInitAll() call -- do not ship without this, it is the identical device-3 crash gap 1243 had.
3. Validate corrected 0840 via GP08's OPTIMIZE tool once built, with all required baseline arms present: native llama.cpp RCCL (source.llama-native/build.stock, GGML_HIP_RCCL=ON -- already exists in the repo per campaign.standard's stock native lane, no new build prerequisite needed), meta-tensor (-sm tensor META/butterfly) and layer-split (-sm layer) as DISTINCT arms (do not conflate them), and BigCherry's own rccl/internal providers.
4. Reproduce 1243's real hardware numbers (pp512=1502.49 etc. on {0,1}) through corrected 0840, with an exact RCCLCompatibilityRevision now recorded (GP06/GP07).
5. Once corrected 0840 reproduces or beats 1243's numbers under the required baselines: delete patches/1243_gp03_size_adaptive_allreduce_dispatch entirely. One implementation only -- do not leave both in the tree.

## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files



## Validation

REWRITTEN 2026-09-02: corrected 0840 passes GP08's full baseline comparison (native llama.cpp RCCL, meta-tensor, layer-split, BigCherry internal-only) for {0,1} at minimum, matching or beating 1243's already-recorded real hardware numbers (pp512=1502.49, pp2048=1451.17, pp4096=1435.27, tg128=37.23) under an exact recorded RCCLCompatibilityRevision. {0,3} (or any device-3-inclusive set) fails closed via GP02's predicate before ever calling ncclCommInitAll a second time -- not a crash, a clean refusal. 1243 is deleted from the tree once the above passes, not left as a parallel implementation.

## Effort & Risk



## Standards



## Acceptance Criteria



## Notes

Original: docs/planning/active/hip-collectives/HI155.md (now superseded, pointing here). Real evidence this item depends on: project memory project_dual_xtx_baseline.md (2026-08-31 3-arm decomposition and the pp/tg crossover sweep) and patches/1001_hip_internal_allreduce/SUMMARY.md. Do not implement size-adaptive dispatch as a first resort without first investigating whether the regression itself is fixable -- per this item's 2026-09-02 refinement.

Original: docs/planning/active/hip-collectives/HI155.md (now superseded, pointing here). Real evidence this item depends on: project memory project_dual_xtx_baseline.md (2026-08-31 3-arm decomposition and the pp/tg crossover sweep) and patches/1001_hip_internal_allreduce/SUMMARY.md.

2026-09-02 source archaeology found the dispatch mechanism already exists and is already compiled into production (GGML_HIP_DISPATCH is auto-enabled whenever GGML_HIP_AUTOTUNE is on) -- this significantly de-risks and shrinks the implementation to a small, surgical change in one function (ggml_backend_cuda_comm_reduce_plan, ggml-cuda.cu ~line 1316), not new architecture. NOT YET IMPLEMENTED OR BUILT -- this is the concrete next-session starting point.

IMPLEMENTED AND VALIDATED on Brutus (2026-09-02).

Root cause chain found during implementation:
1. Wrote the size-adaptive per-call dispatch (ggml_backend_cuda_comm_reduce_plan(nbytes) threshold on GGML_HIP_REDUCE_RCCL_THRESHOLD, default 256KB; ggml_backend_cuda_comm_try_reduce_plan() rccl branch uses a secondary ncclCommInitAll() brought up alongside the internal ar_pipeline in ggml_backend_cuda_comm_init_internal()) in ggml-cuda.cu -- this logic was correct from the start (confirmed via debug fprintf: nbytes/threshold/decision computed correctly, rccl branch reached correctly).
2. First real validation showed the rccl branch's guard always failing: comms.size()=0 despite backends.size()=2, so every "rccl" decision silently fell through and produced no measurable change.
3. Root cause: the private scratch source tree used for this work never had patch 1001_hip_internal_allreduce applied. ggml_cuda_ar_pipeline_init() has two implementations in allreduce.cu -- a real CUDA one and a stub gated by `#if !defined(GGML_USE_HIP) && !defined(GGML_USE_MUSA)` that unconditionally returns nullptr on HIP. Patch 1001 flips that guard to `#if !defined(GGML_USE_MUSA)` plus maps pinned-host-memory APIs and swaps __nanosleep for __builtin_amdgcn_s_sleep(4) in vendors/hip.h. Without it, GGML_CUDA_ALLREDUCE=internal was silently falling back to the meta-backend butterfly path the whole session -- so my added secondary-NCCL-init code (inside the `if (ret->ar_pipeline)` branch) never executed at all.
4. Applied patch 1001's exact edits to the scratch tree, rebuilt, reran. Fixed immediately: ar_pipeline_init now returns non-null, secondary ncclCommInitAll succeeds (rc=0, comms.size()=2), rccl branch's guard passes.

Validated real numbers (2x RX 7900 XTX, HIP_VISIBLE_DEVICES=0,1, GGML_CUDA_ALLREDUCE=internal, GGML_CUDA_AR_BF16_THRESHOLD=0, -sm tensor -fa on -b 2048 -ub 512, r=3) vs. patch 1001's own SUMMARY.md baselines:

| metric | RCCL baseline | internal-only baseline | GP03 dispatch |
|---|---|---|---|
| pp512 | 1504.67 | 998.21 (-33.7%) | 1502.49 (matches RCCL) |
| pp2048 | 1447.58 | 980.64 (-32.3%) | 1451.17 (matches RCCL) |
| pp4096 | 1431.77 | 972.08 (-32.1%) | 1435.27 (matches RCCL) |
| tg128 | 33.69 | 36.01 (+6.9%) | 37.23 (+10.5%, decode win preserved and slightly improved) |

pp regression eliminated; tg decode win retained. Goal achieved exactly as scoped (per-call provider dispatch, not an internal-algorithm rewrite).

Remaining work before this ships as a real BigCherry patch (still open):
- Package as a new numbered patch near 0830_split_reduce_telemetry/1001_hip_internal_allreduce, built ON TOP of 1001 (this patch cannot function without 1001 applied first -- must declare that dependency explicitly in patch.toml/GROUP ordering).
- Only the debug fprintf instrumentation should be stripped before packaging; the functional logic (reduce_plan/try_reduce_plan/secondary NCCL init) is unchanged from what was validated.
- Still needs: correctness/behavioral-gate pass under the tune-campaign, and validation on the 3-device {0,1,2} RCCL-safe subset per GP01 (this run was 2-device {0,1} only).
- GGML_HIP_REDUCE_RCCL_THRESHOLD default (256KB) not yet tuned/swept -- current value is an initial guess, not validated against a threshold sweep.

STATUS UPDATE (2026-09-02) per gpt-dev-agent deep review: canonical target implementation is now patches/0840_hybrid_allreduce_dispatch (pre-existing, independently written, materially sounder design -- correct per-call provider_name/telemetry on the normal auto path, threshold derived from the internal pipeline's own real copy-engine threshold rather than an independent knob, forces exact F32 automatically). Patch 1243_gp03_size_adaptive_allreduce_dispatch (this item's own implementation) is retained as VALIDATED EXPERIMENTAL EVIDENCE/REFERENCE only -- do not treat it as the implementation path forward. Supersede 1243 only after a corrected 0840 reproduces its real hardware pp/tg numbers.

IMPORTANT CORRECTION: this item's {0,1}/{0,2}/{1,2} pp/tg numbers (pp512=1502.49 etc.) were captured WITHOUT recording an exact RCCLCompatibilityRevision (RCCL source/library identity) -- given GP06's finding that RCCL 2.30.4 regresses the exact {0,2}/{1,2} topology class these numbers were measured on, these results can no longer be treated as portable 'RCCL-safe' qualification evidence until the exact RCCL revision actually linked at benchmark time is recovered from logs/build artifacts (if it can be). Keep the numbers as PERFORMANCE evidence (a real optimization target to reproduce); do NOT use them as admission/qualification evidence -- that determination now belongs solely to GP01/GP06's revision-pinned qualification process.

0840 itself is NOT ready to ship either -- see its own patch.py/SUMMARY.md and this session's gpt-dev-agent review for its 4 P1 blockers (undeclared 0830 dependency; have_internal && !have_nccl large calls wrongly fall to META instead of internal; explicit GGML_HIP_REDUCE_PLAN=rccl telemetry mislabels the effective provider; no RCCL admission guard at all -- same device-3 gap 1243 has, now GP02's rewritten scope). Fix 0840, then hardware-validate it against 1243's numbers under an exact recorded RCCL revision, per gpt's recommended priority order (this is rated the highest-value next runtime-implementation step -- it is what actually turns a qualified topology into a usable mixed pp/tg serving config).

Consolidation target: patches/0840_hybrid_allreduce_dispatch (fix its 4 P1 bugs, wire GP02's admission predicate), reproduce patches/1243_gp03_size_adaptive_allreduce_dispatch's real hardware numbers through it under a recorded RCCLCompatibilityRevision, then delete 1243 -- one implementation only, per explicit operator direction (2026-09-02: 'lets consolidate our patches so we have one implementation'). Depends on GP02 (admission predicate, hard blocker -- 0840 has the identical device-3 crash gap 1243 had) and benefits from GP06 Track A (pinned known-good RCCL revision) for the baseline-comparison requirement above. See GP09 for the separate device-3-specific fallback investigation and GP10 for the base-level testing harness this consolidation should validate through before any full llama.cpp/BigCherry integration test.

## Change Log

- 2026-09-01T12:06:44.735660+00:00 (created-by): Created by agent
- 2026-09-01T12:10:19.830658+00:00 (updated-by): Updated: section:description, section:steps, section:notes
- 2026-09-01T19:31:13.055264+00:00 (updated-by): Updated: section:notes

## Append

- Heterogeneous-topology validation (2026-09-02): tested beyond the {0,1} pair the fix was validated on.

- {0,2} (7900 XTX + R9700, gfx1100+gfx1201, RCCL-safe per HI138): clean, no crash. pp512=1205.82, tg32=29.58.
- {1,2} (7900 XTX + R9700): clean, no crash. pp512=1283.78, tg32=30.46.
- {0,3} (7900 XTX + 6900XT, device 3 = the HI138-excluded chipset-routed GPU): CRASHES. GP03's secondary ncclCommInitAll() succeeds at init time (rc=0, comms.size()=2) -- it has no HI138/device-3 awareness -- so the rccl-branch guard passes, and the first pp-sized reduction hard-aborts inside ggml_backend_cuda_comm_allreduce_nccl (GGML_ASSERT/ggml_cuda_error), identical failure mode to raw GGML_CUDA_ALLREDUCE=nccl on this topology. This is NOT a safe fallback -- it's a hard crash, worse than doing nothing, on any topology that includes device 3.

Implication: GP03's patch MUST NOT ship without wiring in the device-3 guard from patch 1225 (GP02's scope) first, or GP03 needs to consume GP01's qualified-topology list directly and refuse to bring up the secondary NCCL communicator (fall back to internal/butterfly instead) whenever device 3 is in the active device set. Recorded here as a hard blocking dependency: GP03 must not be packaged/shipped independently of GP02's guard landing, or without GP03 itself checking topology before calling ncclCommInitAll a second time.
- 2026-09-01T19:41:49.203082+00:00 (updated-by): Updated: section:append
- 2026-09-01T23:15:51.475002+00:00 (updated-by): Updated: section:notes
- 2026-09-02T00:09:13.983495+00:00 (updated-by): Updated: section:title, section:description, section:notes
- 2026-09-02T00:16:53.796701+00:00 (updated-by): Updated: section:steps, section:validation
