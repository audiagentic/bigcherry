---
id: GP11
order: 0
plan: gpu-collectives
state: pending
created-at: '2026-09-02T02:08:00.312931+00:00'
breadth: ''
skill: advanced
created-by: agent
work: L
---

# Generalize the internal AllReduce pipeline beyond n_devices==2 (N-way ring/tree)

## Description

Patch 1001_hip_internal_allreduce's pinned-host-memory AllReduce pipeline (`ggml_cuda_ar_pipeline_init` in allreduce.cu) hard-rejects anything but exactly 2 devices:

```cpp
if (n_devices != 2) {
    GGML_LOG_DEBUG("internal AllReduce only supports n_devices=2 (got %zu); falling back\n", n_devices);
    return nullptr;
}
```

This is a real algorithmic constraint, not an oversight: the pipeline is a direct pairwise scheme -- two GPUs ping-pong through a pinned host-mapped buffer with a device-side spin-wait/signal handshake between exactly rank 0 and rank 1. It was originally an upstream PR (#27825) designed specifically as a low-latency 2-GPU optimization, not a general N-way ring or tree reduction.

Consequence, confirmed on real hardware (2026-09-02, GP03 3-GPU validation): GGML_CUDA_ALLREDUCE=hybrid (patch 0840, GP03's consolidated dispatch) correctly falls through to pure RCCL for any 3+ device topology, since `have_internal` is always false there -- {0,1,2} numbers on real hardware tracked native RCCL almost exactly (pp512 1322.92 vs 1327.99, tg128 35.91 vs 35.87), confirming the fallback itself is correct and safe, but it also means hybrid's whole per-call decode-side win (the +3.9% to +51% improvement validated on {0,1}) is currently UNAVAILABLE for any qualified 3+ device topology (GP01's {0,1,2} subset, once GP06/GP02 unblock it for production use). The decode-side benefit that makes hybrid worth shipping on 2 GPUs is simply not present at all above 2 GPUs today.

This item is to design and implement a real N-way variant of the internal pipeline's pairwise scheme (ring topology with N pinned buffers and a matching handshake protocol, or a tree reduction), so hybrid's decode-side win can extend to GP01's qualified 3-GPU subset -- not a dispatch fix, real algorithm work.

## Steps

GP10 harness result (2026-09-02): the naive star-pattern N-way extension of patch 1001's pairwise mechanism scales roughly linearly in N-1 (real hardware, {0,1}->{0,1,2}: +68% to +79% latency) -- a structural property of every-rank-reads-every-peer, not an artifact of the harness's minimal (1-block, unvectorized) kernel. Do not implement the star extension.

Candidate designs to try next, cheapest/most-informative first (2026-09-02, expanded per operator direction after reviewing alternatives):
1. Reduce-to-root + broadcast: designate one rank as root; each of the other N-1 ranks does exactly one pairwise handshake with the root only (not with each other) -- reuses 1001's proven pairwise mechanism almost unchanged, just applied N-1 times from a common root. Root's handshakes with different peers can run on independent per-peer streams concurrently rather than ring's forced sequential N-1 stages, so this may beat ring's latency for small N (3-4 devices) despite moving the same total data. Cheapest next experiment -- try this BEFORE ring since it reuses more of the already-proven code.
2. Ring: each rank passes its partial sum to exactly one neighbor per step, N-1 steps, O(1) peer-interaction cost per rank per step. Try if (1) doesn't show a good result, or as a direct comparison once (1) has real numbers.
3. Binary/binomial tree: log2(N) levels instead of N-1 steps -- doesn't really differ from (1)/(2) until N > ~4-5, which is outside this box's real topology space (GP01 qualifies up to {0,1,2} only). Low priority, do only if N grows in a future topology.
4. Diagnostic prerequisite for interpreting any of the above: isolate WHERE 1001's real +17% tg win over RCCL at N=2 actually comes from -- avoiding RCCL's per-call launch/setup overhead on small latency-sensitive messages (a fixed cost, which any low-hop-count N-way design should preserve), vs more efficient PCIe path use (a bandwidth-scaling cost, where RCCL's own ring may already be close to optimal at N=3). Cheap to check in the harness (compare per-call fixed overhead component directly) and materially changes which of 1-3 is worth pursuing.
5. Legitimate negative-result exit: if the root-reduce experiment also shows unfavorable scaling once the diagnostic in (4) is understood, close this item with a documented real-numbers negative result rather than forcing a design to work -- same discipline GP09/GP01's active-tuning track already applied this session.

2026-09-02: sending this candidate-design list to gpt-dev-agent (dev-gpt-agent) for a solution-level design pass (real algorithm/protocol detail, not just review) before implementing option 1 in the GP10 harness -- see append/notes for gpt's response once it lands.

## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files



## Validation

Real hardware {0,1,2} MTP completion-bench shows the new N-way internal path (via hybrid) beating both native RCCL and layer-split for decode throughput, the same bar GP03 applied at N=2 -- not just crash-free, not just correctness-preserving.

## Effort & Risk



## Standards



## Acceptance Criteria



## Notes

Not currently blocking anything -- GP01's {0,1,2} qualification and GP03's consolidated dispatch both already degrade safely to pure RCCL for 3+ devices today (confirmed on real hardware, no bug). This item exists to capture a real, quantified opportunity cost (hybrid's proven decode win is unavailable above 2 GPUs), not to fix a defect. Depends on GP01 (3-GPU subset must be qualified/production-safe first) and benefits from GP10 (base-level harness) for cheap go/no-go before committing to the full implementation -- same discipline GP09 applies to its own candidate mechanisms.

GP10's harness (tools/lab/gp10-collective-harness/nway_star_allreduce.cpp) produced a real, cheap first signal (2026-09-02) that changes this item's recommended design BEFORE any full implementation: a direct star-pattern generalization of patch 1001's pairwise mechanism (every rank spins on and reads all N-1 peers) scales roughly linearly in N-1 (real hardware: +68% to +79% going from N=2 to N=3 at production-realistic sizes, on {0,1}/{0,1,2}). Patch 1001's real production win over RCCL at N=2 was already a modest +17% tg -- a mechanism whose overhead grows linearly in N-1 is likely to lose that margin by N=3 regardless of kernel-level optimization (multi-block vectorization, matching production's real 1001 kernel), since this is a structural property of the star topology, not an implementation gap this harness's simplicity introduced.

Recommendation before committing to a full ggml-cuda.cu implementation: do NOT implement the direct star/all-to-all N-way extension. Next real step is to add a RING-topology variant to the same GP10 harness (each rank passes its partial sum to exactly one neighbor per step, N-1 steps total, O(1) peer-interaction cost per rank per step rather than O(N-1)) and get a real go/no-go signal on THAT design before writing any production patch. This is the same cheap-iteration discipline GP10 exists for -- do not skip straight to a full implementation on the strength of the star result alone (it was never the recommended design, just the simplest one to test first).

2026-09-02 round 2 (post gpt review): fixed two real bugs reviewer-gpt-agent found in the star harness (arrival slots weren't actually cache-line-separated -- ARRIVAL_STRIDE_INTS was defined but unused; correctness was only checked on rank 0's warmup rep). Added --blocks (production-shaped 8-block grid, matching GGML_CUDA_AR_KERNEL_BLOCKS) and float4 vectorized copies. Implemented dev-gpt-agent's recommended fused reduce-to-root+broadcast design (one kernel launch per rank, root reduces and republishes for leaves to read -- no independent concurrent root streams, since root's reads from every leaf cross its own single PCIe endpoint regardless of stream count, per gpt's PCIe-topology correction).

CORRECTED star results (8-block, vectorized, {0,1}->{0,1,2}, real hardware): far better than the original naive-kernel numbers suggested. 30720 elem: N=2 62.44us (RCCL: 61.9us, essentially TIED), N=3 101.20us (RCCL: 87.8us, only +15%). 2621440 elem: N=2 3592.75us (RCCL: 1432.0us, 2.5x), N=3 7036.81us (RCCL: 3671.0us, 1.9x). The N=2->N=3 relative scaling is still present (+62% small, +96% large) but the absolute picture is now much more competitive than the naive kernel implied -- gpt's caution that 'do not conclude star is unfixably bad from a naive kernel' was correct and material.

ROOT mode results (8-block, vectorized, N=3, real hardware): 30720 elem, root=0(XTX): 114.08us; root=2(R9700): 100.52us. 2621440 elem, root=2: 5253.49us (beats star's 7036.81us, still behind RCCL's 3671.0us). root=0 large-size: 8438us AND one correctness failure (max_abs_err=2.78) on the very first run out of ~270 total reps observed across repeated stress runs at that exact config (0 failures in the following ~265 reps) -- a real, rare, intermittent race, same category/severity class as GP06's Tree+LL finding (passes almost always, fails occasionally under real repeated use). NOT yet root-caused. Root choice matters: R9700-as-root (root=2) is faster than XTX-as-root (root=0) at large size, plausibly because concentrating reads on the R9700's PCIe link vs the busier XTX0 link differs -- worth the --root sweep gpt recommended.

Open items before this could ever be considered for production: (1) root-cause and fix the intermittent correctness race (rate ~1/270 reps at N=3/root=0/large-size) -- do not productionize on top of an unexplained rare correctness fault, full stop; (2) run gpt's recommended dense size-sweep (4B..1MB) with alpha/beta (fixed-overhead vs bandwidth) decomposition plus a signal-only floor measurement, to determine whether 1001's real N=2 production win is a fixed-overhead story (favors root, which this data already looks consistent with at small sizes) or a bandwidth story (favors segmented multi-root, per gpt's fallback design); (3) segmented multi-root candidate (gpt's stronger bandwidth-oriented fallback, described in GP11's steps) not yet implemented -- try only if root shows a large-size bandwidth ceiling after (2); (4) real production-realism check: this harness's numbers are raw collective time only, not the real end-to-end call overhead RCCL pays in the actual llama.cpp dispatch path (patch 1001's own +17% N=2 win included avoiding that overhead) -- GP08's full-stack OPTIMIZE comparison is still the only tier that can confirm a real win, this harness stays encouraging-not-sufficient evidence per its own charter.

Given the still-open correctness question, do NOT implement this as a production patch yet. Next real step is root-causing the intermittent race before any further latency optimization work.

RACE FIXED AND CONFIRMED (2026-09-02, round 3). gpt-dev-agent root-caused the intermittent root_kernel correctness failure to a real, concrete bug in the harness's mapped-pointer setup (not a fence/ordering race): hipHostGetDevicePointer() was called ONCE for the peer-pointer table and arrival array, with whichever device happened to be current at that point in main() (the last device from the earlier per-device alloc loop) -- that single alias was then shared across every rank's kernel launch on every device. HIP does not guarantee a device-pointer alias resolved under one device's context is valid when dereferenced by a DIFFERENT device's kernel. This exactly matched the observed pattern: aliases resolved under device 2's context -> root=2 always clean; root=0 (furthest from device 2) showed the rare failures.

Fix applied: resolve a SEPARATE alias table per CONSUMING device (loop over every device, hipSetDevice to it, resolve hipHostGetDevicePointer for the arrival array AND for every peer's stage buffer under THAT device's context, store in a real device-side array via hipMalloc+hipMemcpy). Also hardened both leaf and root signal paths with a second __threadfence_system() immediately after ar_signal_set() (matching production allreduce.cu's exact protocol), per gpt's recommendation, though gpt assessed this as unlikely to have been the actual cause.

Stress validation post-fix: 3000/3000 reps clean (500 reps x 2 batches x 3 roots, {0,1,2}, 2621440 F32 elements, the exact config that previously failed ~1/270). Zero failures. The fused reduce-to-root+broadcast design is now CORRECTNESS-CONFIRMED and can move to optimization/production consideration.

Current best numbers (8-block, vectorized, real hardware): root=2(R9700) large-size 5269-5280us (vs RCCL's 3671us, 1.4x behind); root=0/1(XTX) large-size ~8400-8740us (worse than root=2 -- R9700-as-root benefits from being the least loaded PCIe endpoint). Small/decode-realistic size (30720 elem) not yet re-measured against the fixed+hardened kernel -- do that next, it's the size that matters most for the real production use case.

gpt also provided a concrete N=3 optimization (independent float4 loads of both leaf buffers before summing, rather than the current serial dependent-add loop, to let both PCIe reads be outstanding simultaneously) and cautioned against chasing large-size (10MB) bandwidth as the primary success criterion -- production's own real pinned-host AllReduce already loses to RCCL by ~32-34% at large/prefill sizes and is meant to be size-dispatched back to RCCL there (same as patch 1001's real design); the decode-realistic small-size numbers are what should decide GP11's actual go/no-go.

Next steps: (1) re-run the small-size (30720 elem) sweep against the fixed+hardened root_kernel and star_kernel to get the real decision-relevant numbers; (2) apply gpt's N=3 independent-load optimization; (3) only then move toward GP08's full-stack OPTIMIZE validation if the small-size numbers look competitive.

MAJOR RESULT (2026-09-02, round 4): applied gpt-dev-agent's N=3 optimization (independent float4 loads of both leaf buffers before summing, instead of a serial dependent-add loop) to root_kernel. Real hardware result at the decode-realistic size (30720 F32 elements, the size that matters for GP11's actual production motivation):

  root=0 (XTX0 as root): 68.3-68.8us median across 800 stress reps (4x200), 100% correct, ZERO failures.
  RCCL N=3 (Ring, same config): 87.8us.

  ROOT BEATS RCCL BY ~22% at the decode-realistic size. This is the first N=3 result that actually clears GP11's real bar (beat RCCL, not just match it) at the size that matters most.

Full picture across sizes (post pointer-alias fix + fence hardening + N=3 optimization):
  30720 elem: root=0 68.3-68.8us (BEATS RCCL's 87.8us by ~22%), root=2 82.56us (beats RCCL by ~6%), star 99.00us (13% behind RCCL) -- root=0 is now the clear winner at small size, having flipped from WORST (pre-optimization) to BEST.
  2621440 elem: root=0 4586.73us (25% behind RCCL's 3671.0us, but improved 45% from pre-optimization's 8438us), root=2 5174.34us (41% behind, improved from 5253-5292us) -- both still behind RCCL at large size, consistent with gpt's guidance that large/prefill sizes should stay dispatched to RCCL (matching production's own real size-based dispatch design already in patch 1001/0840).

Stress-validated: root=0 at the winning small-size config ran 800/800 reps clean (4 batches of 200), zero correctness failures, stable timing (68.28-68.76us range) -- this specific winning config is not the same one that had the earlier race in its NON-optimized/pre-fix form, and has now been independently re-validated post-fix at high rep count.

This changes GP11's overall disposition from 'leaning negative' to 'a real, viable candidate exists': a fused reduce-to-root design, sized-dispatched (route small/decode-sized reductions through this pinned-host N-way root mechanism, large/prefill-sized reductions through RCCL -- exactly mirroring patch 1001's existing 2-device size-dispatch logic), could plausibly deliver a real N=3 decode-time win analogous to patch 1001's proven N=2 win.

Open items before any production patch work: (1) this harness's numbers are still raw collective time only, not the real end-to-end call-path overhead RCCL pays in llama.cpp's actual dispatch (patch 1001's own +17% N=2 production win included avoiding that overhead, which this harness cannot measure) -- GP08's full-stack OPTIMIZE comparison via real MTP completion-bench is the only tier that can confirm this translates to a real decode-throughput win, same caveat as before, now more worth actually running given a real positive signal exists; (2) gpt's recommended dense size-sweep (alpha/beta fixed-overhead-vs-bandwidth decomposition) still not done -- would clarify exactly where the size-dispatch threshold should sit for N=3 (patch 1001's existing GGML_CUDA_AR_COPY_THRESHOLD may not be the right threshold for N=3); (3) root=0 specifically won here -- worth confirming root=1 (the other XTX) shows a similar win before assuming 'any XTX-as-root' generalizes, since root choice mattered a lot pre-optimization too; (4) this is still just the pinned-host mechanism's raw speed -- has not yet been implemented as an actual ggml-cuda.cu N-way pipeline variant, that's real patch work, appropriately gated behind confirming (1)-(3) first per this item's own discipline.

Follow-up (same session): root=1 confirmed -- 200 reps (2x100) at the winning small-size config, 68.32-68.48us, matching root=0's 68.3-68.8us almost exactly. This is a real 'either RX 7900 XTX as root' win, not specific to device ordinal 0 -- item (3) from the prior note is resolved. Both XTX devices as root beat RCCL's 87.8us by ~22% at the decode-realistic size, with the R9700 as root beating it by a smaller ~6% margin (82.56us). Root choice matters (XTX clearly better than R9700 here, plausibly reflecting the R9700's different PCIe generation/bandwidth characteristics as Navi 48 vs the XTXs' Navi 31), but any working topology beats RCCL now post-optimization.

PRELIMINARY PRODUCTION PATCH BUILT AND INTEGRATION-TESTED (2026-09-02): patch 1244_gp11_internal_allreduce_nway_root wires the harness's validated fused-root design into real ggml-cuda.cu/allreduce.cu (root=devices[0], F32-only, chunked-kernel-eligible sizes only, BF16 wire compression forced off for n_devices==3). Two real integration gaps found and fixed beyond what the harness alone could catch: (1) a SEPARATE hardcoded n_backends==2 assert in ggml-cuda.cu's own comm dispatcher wrapper (ggml_backend_cuda_comm_allreduce_internal), not just allreduce.cu's pipeline -- missed on the first pass since the harness never exercised that call path; (2) a real build-system gotcha -- `git checkout -- allreduce.cu` to revert a broken test attempt accidentally reverted patch 1001's own essential HIP-enable guard too, since 1001 was applied as an uncommitted working-tree modification, not a git commit; produced a silently near-empty (1.5KB) compiled object with no compiler errors, since the whole file body compiled out under the original (un-1001-patched) #if guard. Real lesson: always verify a suspiciously small/unchanged object size after any patch-application troubleshooting step, not just absence of compiler errors.

After both fixes: real hardware smoke test on {0,1,2}, GGML_CUDA_ALLREDUCE=internal, confirmed 'initialized AllReduce pipeline: 3 GPUs' (no fallback), no crash, tg32 throughput jumped from 20.36-20.46 t/s (broken/META-fallback baseline) to 38.26-38.30 t/s -- a plausible, encouraging number in the range of prior real {0,1,2} measurements (~35.87-35.91 from GP03's own hybrid numbers).

HOWEVER: real completion request through llama-server with this patch produced GARBAGE TEXT ('' isf w نافwai-wjwvwmim  0ches jcorindinggmyvg,urif '. and0gh::oneon.i.k'') for the prompt 'The capital of France is' at temperature=0 -- a genuine, serious correctness bug in the real llama.cpp integration, NOT caught by the harness's own validated correctness (which used synthetic buffers, not real ggml_tensor call patterns from an actual model forward pass). Throughput and crash-freedom are NOT sufficient evidence of correctness -- this is exactly the kind of result that must not be treated as a win without real output verification, consistent with this project's own standing discipline (HI138/GP04/GP05's own history of catching exactly this class of false-positive). Root cause not yet found -- candidates to investigate: possible issue with tensors[i]->data pointer arithmetic/strides differing from the harness's synthetic-buffer assumptions, GGML_TENSOR_FLAG_COMPUTE semantics at N=3 tensor-split, or a genuine kernel bug that only manifests at real (possibly much smaller, e.g. per-token hidden-dim-sized) decode reduction sizes not covered by the harness's own tested sizes (30720/2621440). Sent to gpt-dev-agent (same session, ses_26897cca8a0d4e67) for review of the actual patch diff against this real symptom.

DO NOT treat the plausible tg32 throughput number as a real result until this is fixed -- it is currently a plausible-looking but WRONG-OUTPUT number, the most dangerous kind of false positive.

--- gpt-dev-agent's optimization roadmap (received 2026-09-02, BLOCKED on the correctness bug above -- do not start until it's fixed) ---
Also confirmed via hipDeviceCanAccessPeer(): 0 for every {0,1,2} pair on this box -- no direct P2P VRAM access, closing off that avenue permanently; the host-mediated mechanism (already winning) is the right one to keep optimizing, not a distraction.
Prioritized harness-level (tools/lab/gp10-collective-harness/) experiment order once correctness is fixed:
P1 root3-specialized kernels (remove generic is_root/n_devices/rank branching from the hot kernel; separate root3_root_kernel/root3_leaf_kernel with fixed float4 signatures)
P2 parallel token waits (root's two leaf-token polls done by threadIdx 0 and 1 concurrently instead of serially)
P3 UNROLL={1,2,4} template on the root reduce loop (more outstanding mapped-host reads in flight; watch VGPR pressure via hipcc --resource-usage)
P4 blocks/threads/poll-sleep geometry sweep (sequence: UNROLL first at 8x256, then blocks, then threads, then poll sleep -- not a full brute-force grid)
P5 ISA inspection (llvm-objdump) to confirm float4 mapped-host reads actually emit as wide loads, not scalarized -- fix this before further micro-tuning if it's scalarized
STOP-and-validate gate: full-stack test root3 via GP08 if it reaches <=~65us reliably
P6 seg3: three-way segmented reduction (each rank owns a contiguous 1/3 of the vector, publishes only its two NON-owned segments -- reduces max per-GPU PCIe traffic from 3.0M to 2.333M bytes) -- NOT expected to beat root3 at 30720 elements (extra sync phase), targeted at the crossover region (120KiB-10MiB) where root3 currently still loses to RCCL. Real production dispatch could end up 3-tier: tiny/small->root3, middle->whichever wins, large->RCCL.
P7 size sweep root3 vs seg3 vs RCCL to find real crossover points
P8 weighted seg3 (unequal segment sizes based on each GPU's measured isolated throughput -- XTX/XTX/R9700 are heterogeneous) -- only if seg3 shows a real win
Explicitly deprioritized/ruled out: direct P2P (impossible, canAccessPeer=0 confirmed), standard ring (extra sync stages with no P2P transport benefit to justify them), binary tree (N=3 too small to benefit), more fence removal (correctness-sensitive, low expected value), LDS staging (no data reuse), BF16 compression (already established as negative for decode-realistic sizes), further large-message-only root tuning (RCCL remains the correct provider there).

CORRECTNESS BUG FIXED AND VERIFIED (2026-09-02): gpt-dev-agent found two real bugs in patch 1244 (req_1f050a8abef749a2). (1) PRIMARY, definite root cause: ggml_cuda_ar_kernel3 signalled/polled the BASE (slot,rank) arrival pointer directly instead of adding the per-block offset (blockIdx.x * ARRIVAL_INTS) that the working N=2 kernel and ggml_cuda_ar_arrival_ptr's own contract require -- all 8 blocks shared one token, so any block finishing first released every other block's wait, which then read stripes other blocks hadn't published yet. Exactly explains the observed symptom: fast, no crash, garbage values. (2) A real cross-device pointer-alias bug -- host_buf[owner].dev/arrival.dev aliases resolved under whichever device was current in pipeline_init were reused across every consuming device's kernel launch, the SAME class of bug already fixed in the harness but reintroduced in the production integration.

Fixed both: kernel3 now computes my_slot/a_slot/b_slot = base + blockIdx.x*ARRIVAL_INTS internally (matching production's own per-block scheme exactly); added per-consuming-device alias tables (host_buf_dev3[3][3], arrival_dev3[3]) resolved once at pipeline_init with each consumer device current in turn, plus a new ggml_cuda_ar_arrival_ptr3 helper mirroring the existing ggml_cuda_ar_arrival_ptr but indexed by consumer.

RE-VALIDATED on real hardware after the fix: llama-bench confirms 'initialized AllReduce pipeline: 3 GPUs' (real engagement, not fallback), tg32=38.03-38.30 t/s (consistent, stable). CRITICALLY, real llama-server completion requests across 8 diverse prompts (factual Q&A, arithmetic, code generation, creative writing) at temperature=0 ALL produced fully coherent, correct output -- 'The capital of France is' -> ' Paris.\nThe capital of Germany is Berlin...'; 'def fibonacci(n):' -> correct base-case code; etc. This is real, diverse-content correctness evidence, not a single lucky prompt.

GP11's preliminary production patch (1244) is now CORRECTNESS-VALIDATED for the F32/small-message/root=devices[0] regime it targets. Still labeled 'untested' pending: broader stress testing (this session only ran a handful of completion requests, not the harness's earlier 3000+-rep discipline -- worth a longer soak before calling it validated), root=1/root=2 real-integration testing (only root=0/devices[0] tested in the real llama.cpp path so far), and GP08's full-stack OPTIMIZE comparison against required baselines before any real production promotion decision.

Next: apply gpt's fence-lesson-informed optimization roadmap (root3-specialized kernels, ILP unroll, geometry sweep, seg3) to the HARNESS first (cheap iteration, per GP10's own discipline) before further production-patch changes -- now unblocked since the correctness foundation is solid.

P1 COMPLETE (2026-09-02): implemented gpt-dev-agent's root3-specialized design in the harness (--mode root3) -- fixed float4 signatures, no generic is_root/n_devices/root branching in the hot kernel, root's two leaf-token waits polled by threadIdx 0 and 1 concurrently (P2 folded in since it was cheap). gpt confirmed beforehand the harness's existing arrival-slot indexing and per-consumer pointer aliases were already correct (no latent version of either production bug carried over).

Real hardware result at the decode-realistic size (30720 F32 elements, {0,1,2}): root3 root=0/root=1: 64.1-65.0us median, STRESS-VALIDATED 1500+ reps clean (3x500 at root=0), zero correctness failures. This BEATS the already-good generic root kernel's 68.3-68.8us by ~5%, and beats RCCL's 87.8us by ~27% (up from ~22% with the generic kernel). root=2 (R9700): 79.96us, smaller improvement over generic root's 82.56us but still real.

Next: P3 (UNROLL={1,2,4} template on root3_root_kernel's reduce loop, more outstanding mapped-host reads in flight) then P4 (blocks/threads/poll-sleep geometry sweep) then P5 (ISA inspection via llvm-objdump to confirm float4 loads aren't scalarized) per gpt's prioritized order. STOP-and-validate gate once <=~65us is reliably beaten further: full-stack test via GP08. Then P6-P8 (seg3 segmented reduction) for the large-message crossover region.

REAL FULL-STACK MTP VALIDATION (2026-09-02): ran patch 1244 (generic root_kernel, not yet the root3-specialized version) through the actual production MTP speculative-decode serving path -- bigcherry.bench.server_completion against the real mtp-27b-v1 corpus (48 real prompts: code, logic puzzles, factual Q&A, creative writing), {0,1,2}, GGML_CUDA_ALLREDUCE=internal vs GGML_CUDA_ALLREDUCE=nccl (RCCL), same topology, same everything else.

Result: internal-root tps_median=64.96/mean=65.47 vs RCCL tps_median=59.09/mean=60.05 -- a REAL +9.0% mean throughput win through the full production serving path (not just the raw collective microbenchmark). draft_acceptance comparable (0.5855 vs 0.5455, if anything slightly better for internal-root, most likely sampling noise across the corpus rather than a systematic effect since AllReduce provider choice shouldn't affect model predictions). MTP compatibility CONFIRMED -- the patch works correctly with real speculative decoding, no special MTP-specific wiring was needed since MTP's draft context shares the main model's device/tensor-split config and goes through the same comm dispatch mechanism (see GP12's earlier finding on how MTP loading works).

This satisfies the 'STOP and full-stack test' gate from gpt's roadmap and directly validates GP11's original motivation (recovering hybrid's decode-time win at N=3) with real numbers through the real serving path -- the first evidence in this whole investigation that isn't just a raw collective-time comparison. And this used the GENERIC root kernel, not yet the root3-specialized version validated in the harness (~5% additional raw speedup) -- upgrading the production patch to root3 is a real next step that could grow this further.

Evidence: /home/audumla/bigcherry/artifacts/gp11-mtp-n3-internal/run.jsonl and gp11-mtp-n3-rccl/run.jsonl on Brutus.

Still open before any real production promotion: broader stress/soak testing of the full-stack path (this was one 48-prompt run each, not the harness's earlier 3000+-rep discipline), root=1/root=2 real-integration testing, and upgrading the patch to the root3-specialized kernel design before calling this GP11's final result.

## Change Log

- 2026-09-02T02:08:00.312931+00:00 (created-by): Created by agent
- 2026-09-02T05:37:32.243033+00:00 (updated-by): Updated: section:notes

## Ledger-events


- chg_20260902_053739_built-a-fast-test-harness-for_9608
- 2026-09-02T05:37:40.005534+00:00 (updated-by): Updated: section:ledger-events
- 2026-09-02T05:45:46.653428+00:00 (updated-by): Updated: section:steps
- 2026-09-02T05:56:04.759535+00:00 (updated-by): Updated: section:notes
- 2026-09-02T06:22:30.574428+00:00 (updated-by): Updated: section:notes
- 2026-09-02T06:25:24.763325+00:00 (updated-by): Updated: section:notes
- 2026-09-02T06:25:36.885213+00:00 (updated-by): Updated: section:notes
- 2026-09-02T06:45:03.524861+00:00 (updated-by): Updated: section:notes
- 2026-09-02T07:15:44.844686+00:00 (updated-by): Updated: section:notes
- 2026-09-02T07:31:19.070178+00:00 (updated-by): Updated: section:notes
- 2026-09-02T07:40:19.709431+00:00 (updated-by): Updated: section:notes
- chg_20260902_074027_confirmed-on-real-hardware-th_4049
- 2026-09-02T07:40:28.015758+00:00 (updated-by): Updated: section:ledger-events
