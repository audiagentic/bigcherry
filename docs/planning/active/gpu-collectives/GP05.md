---
id: GP05
order: 0
plan: gpu-collectives
state: pending
created-at: '2026-09-01T20:00:43.695832+00:00'
breadth: ''
skill: advanced
created-by: agent
work: M
---

# Extend HI18 correctness-harness analytical bound to cover the BF16-compression regime (ne>=32768, <=2 devices)

## Description

Found 2026-09-02 while running GP04's real-signature pre-flight: HI18's reduce_correctness.py analytical F32 summation error bound (gamma=(D-1)*u/(1-(D-1)*u), u=2^-24) assumes pure FP32 precision throughout, but upstream llama.cpp's own ggml_backend_cuda_comm_allreduce_nccl() (ggml-cuda.cu ~line 1075) intentionally compresses BOTH ranks' F32 data to BF16 before the NCCL collective, then converts back, whenever element_count crosses a hardcoded per-device-count threshold (ne<32768 for n_backends<=2; ne<131072 for 3; ne<262144 for >=4) -- a deliberate bandwidth/precision tradeoff, not a bug.

Real hardware evidence (2026-09-02): the two large real-production stress signatures (element_count=2621440/[5120,512,1,1] and 2606080/[5120,509,1,1], from artifacts/hardware/20260820-qwen38-mtp-dual-xtx/reduction.jsonl) both fail HI18's current FP32-only bound by ~30,000x (measured abs error ~0.00095 vs bound ~3.4e-08) on the RCCL provider specifically, while META (a different, non-BF16-compressing reduction implementation in this codebase) computes the exact same input correctly. Root cause confirmed via source read (ggml-cuda.cu:1053-1128) plus exact threshold bisection on real hardware (PASS at ne=30720, FAIL at ne=35840, bracketing the real ne<32768 constant precisely) plus two independent RCCL-bypass confirmations (bare rccl-tests all_reduce_perf, and a standalone hipMalloc+ncclAllReduce reproducer with no ggml/bigcherry code at all) both passing cleanly at the same element count -- proving RCCL itself is correct and the harness's tolerance model is the actual gap.

No env var currently exists to force the FP32-only NCCL path for correctness testing (checked: ggml-cuda.cu's only related knobs are GGML_CUDA_ALLREDUCE for provider selection and GGML_CUDA_AR_BF16_THRESHOLD for the SEPARATE, currently-unwired 'internal' provider from patch 1001 -- see GP03's own notes on that mechanism).

## Steps

1. Ask GPT (or decide directly) which of three options to take: (a) add a size-aware BF16-appropriate tolerance model to reduce_correctness.py's analytical bound for ne>=32768/<=2-device (and the 3/4+ device thresholds) cases; (b) exclude ne>=32768 cases from HI18's strict FP32-analytical-bound claim and document them as a separate, intentionally-lossy regime; (c) find/add a way to force the FP32-only NCCL path during correctness testing specifically (e.g. a new env override in the NCCL allreduce function) so correctness evidence is always apples-to-apples regardless of production's real BF16 behavior.
2. Implement the chosen fix in tools/bigcherry/tuning/reduction.py (the REAL HI18 module -- NOT tools/bigcherry/reduce_correctness.py, a retired root facade that was mistakenly used for an earlier, incorrect version of this fix this session and has since been deleted).
3. Re-run the full real-signature corpus (GP04) with the fix in place and confirm the large-s1 cases now pass (or are correctly excluded/labeled) rather than spuriously failing.

## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files



## Validation

The two real stress signatures (s1=512, s1=509) either pass under a BF16-appropriate bound, or are explicitly and correctly excluded from the strict-FP32 promotion claim with a clear documented reason -- never silently mislabeled as a correctness failure of RCCL or of BigCherry's own dispatch code.

## Effort & Risk



## Standards



## Acceptance Criteria



## Notes

Blocks GP04 (real-signature promotion evidence). Root-cause investigation and real hardware confirmation done 2026-09-02 (this session) -- see this item's description for the exact evidence chain. Related: tools/tests/tuning/test_reduce_correctness.py (the real test file for this module) already has the 4-real-production-signature well-formedness tests added this session (commit 591eba0).

## Change Log

- 2026-09-01T20:00:43.695832+00:00 (created-by): Created by agent

## Ledger-events

- chg_20260901_200156_reorganized-todays-multi-gpu_3618
- 2026-09-01T20:01:56.112703+00:00 (updated-by): Updated: section:ledger-events
