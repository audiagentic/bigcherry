---
id: TO01
order: 0
plan: toolchain-deps
state: completed
created-at: '2026-08-23T03:36:14.929002+00:00'
breadth: ''
skill: intermediate
created-by: agent
work: M
---

# Toolchain/dependency acquisition and version inventory (ROCm toolchains, llama.cpp pin, Vulkan backend)

## Description

Current-source review at BigCherry `7e690088df137321b00d92e7f1b2bb6142081133` confirms this item is only toolchain/dependency inventory. TO02 owns non-patch hardware evidence; TO03 owns shared-checkout coordination.

The original problem is now addressed by a dedicated durable reference, `docs/reference/build/DEPENDENCIES.md`, layered on the existing authorities rather than a new inventory engine:

- `config/recipes.toml`: llama.cpp pin plus source/build/platform/backend identities.
- `config/environment.toml`: build-server host/toolchain/device facts.
- `tools/bigcherry/build/toolchain.py`: per-build compiler/toolchain capture.
- `docs/reference/build/DEPENDENCIES.md`: observed/configured inventory, acquisition recipes, freshness rules, and known gaps.

No `bigcherry doctor` extension is needed for this acceptance criterion. Doctor is currently a migration/pin diagnostic, and turning it into a second host inventory would duplicate configuration/docs without adding evidence.

## Current-source disposition

1. **Inventory:** implemented in `docs/reference/build/DEPENDENCIES.md`. It distinguishes configured paths from historical observations and per-run verification, so gitignored `vendor/` state is not presented as checkout-guaranteed fact.
2. **Documentation/tool shape:** resolved in favor of one dedicated reference doc, reusing existing config and `build.toolchain` capture rather than adding a manifest/validator solely for TO01.
3. **Vulkan:** the old question is stale. `config/recipes.toml` already defines `source.vulkan-stock`, `build.vulkan-stock`, `platform.vulkan-linux`, and RADV/AMDVLK stack identities. Vulkan is a real configured backend. Real device/ICD identity remains explicitly unproven in the Vulkan platform and belongs to future run evidence, not this inventory item.
4. **ROCm x llama pin:** documented explicitly as independent identity axes. There is no enforced compatibility matrix; a successful combination must be recorded with the run rather than inferred from a pin or stack name.
5. **RD87:** the old blocker is stale. RD87 built and ran `hipblaslt-bench` on Brutus on 2026-08-23. Its monorepo/invoke/GTest/Tensile/libomp acquisition path is now summarized in the dependency reference; RD87 remains separately responsible for its actual offline-tuning hypothesis.

## Stale/duplicated requirements removed by this review

- TO01 validation no longer asks where to record hardware verification; that is TO02.
- The repeated 2026-08-23 owner-history blocks were historical duplication from the known concurrent-write race; git history preserves them and they are not active requirements.
- "Establish whether Vulkan is real" is superseded by the configured Vulkan lane.
- "Close RD87's client-build gap once it settles" is superseded by RD87's successful client build/run.
- A new doctor/manifest implementation is not required merely because it was listed as one of several possible shapes.

## Validation

Review from a fresh checkout:

```bash
grep '^pinned' config/recipes.toml
grep -n 'vulkan-stock\|vulkan-linux\|stack.vulkan' config/recipes.toml
sed -n '1,240p' docs/reference/build/DEPENDENCIES.md
```

On a host, `tools/rocm-env.sh --list` and the version-qualified checks documented in `DEPENDENCIES.md` establish current availability. Those host checks are intentionally not claimed by this GitHub-only completion review.

## Acceptance Criteria

- [x] One durable reference identifies the known/configured toolchain/backend inventory and distinguishes configuration from observed host state.
- [x] Missing/unusual acquisition paths are documented, including the resolved RD87 `hipblaslt-bench` path and the ROCm installer shared-`update-alternatives` hazard.
- [x] Vulkan is classified from current source as configured but still hardware-unqualified.
- [x] ROCm version and llama.cpp pin composition is documented without inventing a compatibility relation the code does not enforce.
- [x] TO01 no longer duplicates TO02 evidence-capture or TO03 coordination scope.

## Evidence / limits

Completion is for the inventory/documentation requirement, not a fresh assertion that every gitignored SDK path still exists. Source evidence already in the repository includes TO01's 2026-09-02 direct Brutus observations, RD87's 2026-08-23 successful client build/run and reproducible recipe, current `recipes.toml`, current `environment.toml`, and the existing BUILD reference. Any result depending on present host state still needs per-run verification/evidence.

## Change Log

- 2026-08-23T03:36:14.929002+00:00 (created-by): Created by agent
- 2026-09-08: current-source adversarial review removed split-out/stale requirements, added durable dependency inventory, and completed the bounded documentation scope from existing repository evidence.

## Ledger-events



- chg_20260823_034049_turned-this-sessions-real-har_1769
- chg_20260823_041145_rd87s-real-blocker-turned-out_4350
- chg_20260823_043725_folded-the-concurrent-agent-co_9443
- chg_20260823_070037_resolved-the-four-this-item-i_5123
- chg_20260823_074644_an-external-gpt-review-of-the_3267
- chg_20260901_200156_reorganized-todays-multi-gpu_3618
- chg_20260901_203724_confirmed-with-real-hardware-e_3450
- chg_20260908_093642_assessed-to01to02to03-agains_8836
- 2026-09-08T09:36:42.330410+00:00 (updated-by): Updated: section:ledger-events
- chg_20260910_001436_completed-the-planning-rebasel_5794
- 2026-09-10T00:14:38.343049+00:00 (updated-by): Updated: section:ledger-events
