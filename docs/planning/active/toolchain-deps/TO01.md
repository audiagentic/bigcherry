---
id: TO01
order: 0
plan: toolchain-deps
state: pending
created-at: '2026-08-23T03:36:14.929002+00:00'
breadth: ''
skill: intermediate
created-by: agent
work: M
---

# Formalize external dependency/toolchain management, ad hoc test/evidence capture, and concurrent-agent build coordination

## Description

This session found real gaps in how BigCherry tracks and documents its external dependencies AND how it captures the results of ad hoc real-hardware verification runs. (1) a working vendored ROCm 7.1 HIP toolchain plus a real local GPU were available on this workstation the whole time, but that wasn't obvious/discoverable without already knowing to check vendor/rocm/ and BUILD.md's Windows section -- an agent (or a new contributor) starting cold could easily conclude 'no compiler/hardware available' incorrectly, as happened this session. (2) hipBLASLt's offline-tuning client binaries are a real, confirmed, currently-unfilled dependency gap (RD87) with no documented acquisition path until this session -- and RD87's own investigation escalated well past a simple missing-package problem into a structural one (the ROCm/hipBLASLt tag needs AMD's much larger rocm-libraries monorepo, not a standalone clone), which is itself evidence that dependency acquisition paths need to be recorded BEFORE they're needed, not rediscovered live each time. (3) More broadly: the project already vendors multiple ROCm versions side by side (vendor/rocm/7.1, 7.2.4, 7.14, 7.14-multiarch across Windows/Brutus per BUILD.md), tracks the llama.cpp upstream pin via recipes.toml's tree_state_key/repin mechanism, and may need to support multiple GPU backends (Vulkan is named by the owner as a concern, alongside HIP) -- but there is no single place that inventories 'what toolchains/backends/library versions does this project depend on, which are vendored where, which are missing, and how do you get the missing ones.' (4) Separately, this session ran several real-hardware verification passes (HI16's forced-native-parity check, HI24's hot-list smoke test, HI37's cross-language digest check) whose results were captured only as prose in each plan item's own notes field -- discoverable if you know which item to check, but not indexed, not structured, and not reusable the way docs/reference/patch-validation-evidence/*.json + patch_validation_campaign.py already is for PATCH acceptance specifically. There is no equivalent structured evidence store for feature-level real-hardware verification runs that aren't patch-acceptance campaigns.

## Steps

### Toolchain/dependency half
1. Audit what's actually vendored/available today across both known build hosts (this Windows workstation's vendor/rocm/<version>/, Brutus's vendor/rocm/<version>/ plus its system /opt/rocm*) and record it somewhere durable -- not just discovered ad hoc per session.
2. Decide the doc/tooling shape: extend BUILD.md's 'Known dependency/toolchain gaps' section (2026-08-23) into a fuller dependency inventory, or a dedicated DEPENDENCIES.md, or a machine-readable manifest tools/bigcherry could read/validate against (e.g. a `bigcherry doctor`-style command that reports what's vendored/missing) -- pick one rather than letting documentation and reality drift independently.
3. Establish whether Vulkan is a real near-term backend concern for this project (grep for existing Vulkan references/plans) or a hypothetical the owner wants tracked preemptively -- scope this item's Vulkan half accordingly rather than assuming.
4. Cover multi-version handling explicitly: the project already has two versioning concerns that interact (ROCm toolchain version vendored under vendor/rocm/<version>/, and the llama.cpp upstream pin via recipes.toml) -- document how they compose (e.g. does a given recipe/pin combination require a specific ROCm version, and is that recorded/checked anywhere).
5. Close RD87's concrete sub-gap (hipBLASLt client build) as part of or alongside this, once scoped -- including recording its real acquisition path (ROCm/rocm-libraries monorepo, sparse-checked-out into vendor/rocm-libraries on Brutus, per RD87's 2026-08-23 notes) once RD87 settles.

### Test/evidence-capture half (owner request, 2026-08-23, folded in)
6. Decide whether ad hoc real-hardware feature-verification runs (distinct from patch-acceptance campaigns, which already have patch_validation_campaign.py) deserve their own structured evidence store, or whether extending the existing patch-validation-evidence mechanism to cover non-patch verification runs is the better fit -- don't invent a third parallel mechanism without checking whether the existing one can just be generalized.
7. If a new/extended mechanism is built, retrofit this session's own real-hardware runs (HI16 forced-native parity, HI24 hot-list, HI37 workload digest cross-check, RD87's build attempts) as its first real entries, so the new mechanism is validated against real prior evidence rather than only future runs.

### Concurrent-agent build/checkout coordination half (owner request, 2026-08-23, folded in)
8. Brutus (and potentially other shared hosts) run multiple agent sessions against the SAME ~/bigcherry git checkout concurrently, with no lock file or coordination mechanism -- confirmed directly this session: two agents' uncommitted work landed in the same working tree at different points (HI64/HI67/HI89/HI90 from a concurrent session; this session's own HI80/HI24/HI37/HI16/RD87 work), and `releases/*.json` bookkeeping files were independently regenerated by at least two different agents' `bigcherry apply`/`audit` runs on the same checkout without any coordination, each session just reverting the other's incidental side effects rather than anything being actually locked.
9. Decide what coordination is actually needed: at minimum, document the informal protocol every session in this backlog has been improvising (check `git status` before committing, stage only your own files, never touch a file another session has uncommitted changes in) as an explicit written convention rather than leaving each session to rediscover it; at most, a real lock file or session-registration mechanism (e.g. a `.agentic/active-sessions/<pid>.json` heartbeat) that lets a session see what another is currently touching before it starts.
10. Separately note: build OUTPUT directories (like this session's `vendor/rocm-libraries/` on Brutus, or `~/bc-build-*` trees) are generally NOT a collision risk since each is self-contained and gitignored -- the real risk is entirely in the shared git checkout and its generated-but-tracked bookkeeping files (`releases/*.json`). Scope this half of the item accordingly: it is a source-control coordination problem, not a build-isolation problem.

## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files



## Validation

A new contributor or a fresh agent session can determine, without prior session context: (a) which toolchains/backends are vendored where, which are missing, and how to obtain a missing one; (b) where to look for (and where to record) the results of a real-hardware verification run for a specific plan item -- ideally by running one command or reading one doc section, not by spelunking vendor/, BUILD.md, and individual plan items' notes fields.

## Effort & Risk



## Standards



## Acceptance Criteria



## Notes

Raised by the owner (2026-08-23) mid-session, immediately after this session discovered (a) a real vendored ROCm 7.1 toolchain + local GPU were available and had been wrongly assumed absent, and (b) hipBLASLt's client/tuning binaries are a real, confirmed gap with a real but substantial fix (from-source build with Tensile, see RD87). BUILD.md already picked up a first-pass 'Known dependency/toolchain gaps' section and a git-bash/cmake build-gotcha section this same session -- this item is about generalizing that into a durable, discoverable practice rather than another one-off note that only helps until the next session forgets to check it.

Raised by the owner (2026-08-23) mid-session, immediately after this session discovered (a) a real vendored ROCm 7.1 toolchain + local GPU were available and had been wrongly assumed absent, and (b) hipBLASLt's client/tuning binaries are a real, confirmed gap whose real fix escalated from 'install a package' to 'clone a different, much larger repo' over the course of RD87's investigation. BUILD.md already picked up a first-pass 'Known dependency/toolchain gaps' section and a git-bash/cmake build-gotcha section this same session. The test/evidence-capture half was raised separately by the owner later the same session, after noticing this session's real-hardware verification results (HI16/HI24/HI37) were only ever written as prose into each item's own notes field, with no structured or indexed capture -- explicitly folded into this item rather than filed separately, per the owner's direction.

Raised by the owner (2026-08-23) mid-session, immediately after this session discovered (a) a real vendored ROCm 7.1 toolchain + local GPU were available and had been wrongly assumed absent, and (b) hipBLASLt's client/tuning binaries are a real, confirmed gap whose real fix escalated from 'install a package' to 'clone a different, much larger monorepo' over the course of RD87's investigation. BUILD.md already picked up a first-pass 'Known dependency/toolchain gaps' section and a git-bash/cmake build-gotcha section this same session. The test/evidence-capture half was raised separately by the owner later the same session, after noticing this session's real-hardware verification results (HI16/HI24/HI37) were only ever written as prose into each item's own notes field, with no structured or indexed capture. The concurrent-agent coordination half was raised last, after the owner asked directly whether this session's hipBLASLt build was clashing with another agent's concurrent build activity on Brutus -- checked directly (ps aux, git status) and found no active build-process collision, but did find both the same-checkout uncommitted-work pattern (already being handled ad hoc, session to session, via git status checks) and the releases/*.json regeneration-noise pattern (also already being handled ad hoc, via reverting it before every commit) -- both real, both currently informal, both explicitly folded into this item per the owner's direction rather than filed separately.

## Split (2026-08-23, plan-hygiene, audit §7.2): TO01 now scoped to toolchain acquisition/version inventory only

This item was a mega-item folding in three independent concerns. Two have been split out and TO01 is now narrowed to concern A:

SPLIT OUT:
- Concern B (structured evidence capture for ad-hoc real-hardware verification runs) -> TO02.
- Concern C (concurrent-agent shared-checkout coordination -- a source-control/operational concern, not toolchain) -> TO03.

TO01 REMAINING SCOPE (concern A -- toolchain/dependency acquisition + version inventory):
1. Audit what's actually vendored/available today across both build hosts (this Windows workstation's vendor/rocm/<version>/, Brutus's vendor/rocm/<version>/ plus system /opt/rocm*) and record it durably -- not discovered ad hoc per session.
2. Decide the doc/tooling shape: extend BUILD.md's 'Known dependency/toolchain gaps' section, a dedicated DEPENDENCIES.md, or a machine-readable manifest + `bigcherry doctor`-style command. Pick one.
3. Establish whether Vulkan is a real near-term backend concern or a hypothetical to track preemptively -- scope accordingly.
4. Cover multi-version handling explicitly: ROCm toolchain version (vendor/rocm/<version>/) x llama.cpp upstream pin (recipes.toml) -- document how they compose and whether a recipe/pin combo requires a specific ROCm version.
5. Close RD87's concrete sub-gap (hipBLASLt client build) alongside this once scoped, recording its real acquisition path (ROCm/rocm-libraries monorepo, sparse-checked-out into vendor/rocm-libraries on Brutus).

Suggested reframed title: 'Toolchain/dependency acquisition and version inventory (ROCm toolchains, llama.cpp pin, Vulkan backend).'

NOTE: the three duplicated 'Raised by the owner (2026-08-23)' blocks earlier in this note are historical duplication from the concurrent-write race; this block supersedes them for scoping purposes.

## Change Log

- 2026-08-23T03:36:14.929002+00:00 (created-by): Created by agent

## Ledger-events


- chg_20260823_034049_turned-this-sessions-real-har_1769
- 2026-08-23T03:40:49.766948+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-23T04:11:29.019059+00:00 (updated-by): Updated: section:title, section:description, section:steps, section:validation, section:notes
- chg_20260823_041145_rd87s-real-blocker-turned-out_4350
- 2026-08-23T04:11:45.111046+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-23T04:37:11.654658+00:00 (updated-by): Updated: section:title, section:steps, section:notes
- chg_20260823_043725_folded-the-concurrent-agent-co_9443
- 2026-08-23T04:37:25.528956+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-23T06:58:59.566913+00:00 (updated-by): Updated: section:notes
- chg_20260823_070037_resolved-the-four-this-item-i_5123
- 2026-08-23T07:00:37.401463+00:00 (updated-by): Updated: section:ledger-events
