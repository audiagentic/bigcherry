---
id: HI90
order: 0
plan: hip-autotune
state: pending
created-at: '2026-08-23T03:10:00.627711+00:00'
breadth: ''
skill: basic
created-by: agent
work: S
---

# Graph-lifecycle trace markers (patch 1231) invisible below llama-server -lv 4

## Description

Real-hardware finding from HI14's 2026-08-23 capture-lifecycle closure run, split out so the fix doesn't get lost inside HI14's own long history.

patches/1231_hi14_graph_capture_lifecycle_evidence.py emits its BIGCHERRY_GRAPH_LIFECYCLE stage=... markers via raw GGML_LOG_INFO(...) calls in ggml-cuda.cu. Traced through vendor/llama.cpp/common/log.cpp: llama-server's common_get_verbosity() maps a raw ggml GGML_LOG_LEVEL_INFO message to LOG_LEVEL_TRACE (4), and common_log_default_callback() drops it unless the server's verbosity threshold is >= 4 (i.e. `-lv 4` or higher). Default server verbosity is 3. So at any normal production verbosity, these markers are silently invisible -- confirmed on real hardware: a genuine graph-capturing run at default verbosity produced zero BIGCHERRY_GRAPH_LIFECYCLE log lines even though the server's own 'graphs reused = N' counters proved graphs were actually captured and replayed; the identical run at `-lv 4` produced all four expected markers cleanly.

This is not a functional bug (graphs work fine; the counters prove it) -- it's an observability gap in evidence-collection tooling that was written to be checked, and would otherwise require anyone reproducing this evidence to already know to pass -lv 4, which is undocumented anywhere in HI14's design notes or the patch itself.

## Steps

- Decide the right fix: switch the markers to GGML_LOG_WARN (visible at default verbosity, same as the pre-existing NCCL heterogeneous-arch guard's warnings) even though these aren't warnings semantically, OR keep GGML_LOG_INFO but document -lv 4 as a hard requirement everywhere this evidence-collection procedure is described (this item's own description, HI14's notes, any operator runbook), OR add a llama-server-independent sink (e.g. a distinct env-gated file writer) that doesn't route through llama-server's verbosity-filtered logger at all.
- Whichever fix is chosen, re-verify on real hardware that markers appear reliably without needing to raise verbosity, or that the -lv 4 requirement is clearly documented at every place this procedure could be repeated.

## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files



## Validation



## Effort & Risk



## Standards



## Acceptance Criteria



## Notes

Filed alongside HI14's 2026-08-23 real capture-lifecycle closure (see that item's notes for the full reproduction: build, flags, model, and the debugging trail that found this). Low severity -- purely an observability/documentation gap, not a correctness issue -- but worth fixing before anyone else tries to reproduce this evidence and gets a false negative.

FIXED (2026-08-23): patches/1231_hi14_graph_capture_lifecycle_evidence.py's four markers switched from GGML_LOG_INFO to GGML_LOG_WARN. Root cause (see HI14's notes for the full trace): vendor/llama.cpp/common/log.cpp maps a raw ggml GGML_LOG_LEVEL_INFO message to LOG_LEVEL_TRACE (4) via common_get_verbosity(), and common_log_default_callback() drops it below that threshold -- default server verbosity is 3, so these markers were invisible without `-lv 4`. GGML_LOG_LEVEL_WARN is not filtered by this mechanism (log.cpp's print() only gates DEBUG-level messages on the verbosity threshold), so it is visible at any normal server verbosity. Matches the existing precedent of HI85/patches/1225's heterogeneous-arch guard, which also uses WARN for an opt-in evidence marker an operator must be able to see without special flags.

Updated the patch's own docstring to record this finding for future readers. Anchors/guards unchanged (only the log macro name inside the replacement text changed), so the patch still applies cleanly -- confirmed via the existing patch-contract test suite (252 patch-related tests, all passing) and the full offline suite (1727 passed, 3 skipped, 61 subtests).

NOT YET re-verified on real hardware specifically for this change (the original bug WAS found and fixed on real Brutus hardware as part of HI14's 2026-08-23 closure run; this specific WARN-vs-INFO fix itself has only been offline-verified against the patch/test contract, not re-run against a live server to directly observe the markers now appearing at default verbosity). A follow-up real run (same dual-XTX recipe as HI14's closure notes, but WITHOUT -lv 4) would be the concrete confirmation step, low-cost since the build/run recipe is already fully documented in HI14's notes.

FIXED (2026-08-23): patches/1231_hi14_graph_capture_lifecycle_evidence.py's four markers switched from GGML_LOG_INFO to GGML_LOG_WARN. Root cause (see HI14's notes for the full trace): vendor/llama.cpp/common/log.cpp maps a raw ggml GGML_LOG_LEVEL_INFO message to LOG_LEVEL_TRACE (4) via common_get_verbosity(), and common_log_default_callback() drops it below that threshold -- default server verbosity is 3, so these markers were invisible without `-lv 4`. GGML_LOG_LEVEL_WARN is not filtered by this mechanism (log.cpp's print() only gates DEBUG-level messages on the verbosity threshold), so it is visible at any normal server verbosity. Matches the existing precedent of HI85/patches/1225's heterogeneous-arch guard, which also uses WARN for an opt-in evidence marker an operator must be able to see without special flags.

Updated the patch's own docstring to record this finding for future readers. Anchors/guards unchanged (only the log macro name inside the replacement text changed), so the patch still applies cleanly -- confirmed via the existing patch-contract test suite (252 patch-related tests, all passing) and the full offline suite (1727 passed, 3 skipped, 61 subtests).

REAL-HARDWARE RE-VERIFICATION ATTEMPTED, DEFERRED: copied the updated patch to Brutus and ran `bigcherry apply --dry-run` to check it would apply cleanly before rebuilding -- found the overlay apply pipeline currently reports overall FAIL on Brutus due to an UNRELATED patch (rd17-struct-host/rd17-struct-device, ggml_cuda_mm_fusion_args_host/device anchors, 0/1 matches) -- clear evidence of another concurrent session's in-progress, uncommitted patch work on the same shared Brutus checkout. Did not proceed with a real apply+rebuild to avoid colliding with that work. patch 1231's own edits were not implicated in the failure. Real hardware confirmation (same dual-XTX recipe as HI14's closure notes, but WITHOUT -lv 4) remains the concrete next step once Brutus's patch-application state is otherwise clean.

## Change Log

- 2026-08-23T03:10:00.627711+00:00 (created-by): Created by agent

## Ledger-events


- chg_20260823_031008_got-real-hardware-proof-that-h_7622
- 2026-08-23T03:10:08.879960+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-23T03:25:01.872177+00:00 (updated-by): Updated: section:notes
- 2026-08-23T03:25:54.602795+00:00 (updated-by): Updated: section:notes
- chg_20260823_032603_fixed-the-graph-capture-eviden_9752
- 2026-08-23T03:26:03.128785+00:00 (updated-by): Updated: section:ledger-events
