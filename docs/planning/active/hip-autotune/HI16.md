---
id: HI16
order: 0
plan: hip-autotune
state: in_progress
created-at: '2026-08-04T01:22:43.963919+00:00'
breadth: ''
skill: advanced
created-by: agent
work: L
---

# Comprehensive test suite — catalog agreement, native parity, forced variants, replay, correctness

## Description

Patch 11 — implement the full test suite covering: compile-time catalog tests (stable name uniqueness, generated source/catalog agreement, manifest hash determinism), native parity tests (same family and output before/after refactor), candidate correctness tests (all types, narrow boundaries, fusion forms, MUL_MAT_ID), replay tests (hardware separation, refined-to-base fallback, missing candidates, incompatible manifest, corrupted cache, process cache hit, graph capture), and performance smoke tests.

## Steps

- Create test-hip-autotune.cpp with catalog/registry agreement tests
- Extend test-backend-ops.cpp for candidate correctness across all types and boundaries
- Native parity: same family chosen before and after refactor for every tested signature
- Forced variant tests: J=J_best, block_size=block_size_best, nwarps=nwarps_best produce native output
- Signature canonicalization tests: diagnostic-only fields excluded from hash
- Source class correctness: each candidate has valid source_class from the 6-value enum
- graph_safe/deterministic properties: verify all candidates declare both fields
- Replay tests: hardware separation, refined-to-base fallback, missing candidate fallback
- Corrupted cache and incompatible manifest hash fallback tests
- Process cache hit after first lookup — no recurring hashing
- Graph capture: selected launch is recorded, not dispatcher lookup
- Performance smoke tests on representative shapes

## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files



## Validation

- All catalog stable names are unique
- Every generated source has a catalog record
- Every catalog record has a compiled registration
- Manifest hash is deterministic for same input
- Native parity: bitwise-identical output before and after refactor
- Forced variants exactly reproduce native choices when set to native values

## Effort & Risk



## Standards

HIP_AUTOTUNE_STANDARDS sections 2 (candidate identity), 2.3 (source class taxonomy), 2.4 (graph_safe/deterministic), 4.2 (native parity)

## Acceptance Criteria



## Notes

### Forced-native parity is now load-bearing

HI06-HI08 thread the forced variant value as an explicit parameter rather than
a hidden override. That keeps production free of tuning cost, but it means the
forced path is separate code from the native path, so these tests stop being a
formality:

```text
forced J           == native J_best           -> bitwise-identical output
forced block_size  == native block_size_best  -> bitwise-identical output
forced nwarps      == native nwarps_best      -> bitwise-identical output
```

Each must be checked per type and per shape class, because the forced value
travels through a different chain of defaulted parameters for each. A mistake
in any forwarding site shows up here and nowhere else -- the code compiles and
runs either way.

### Review update

**Cross-language digest agreement is a required test.** Keys are computed in
C++ at runtime and in Python by the offline tools. A divergence does not fail
loudly -- every replay lookup simply misses and falls back to native, so the
system looks like it works while doing nothing. `hip-autotune-blake2b.cpp`
therefore has known-answer vectors pinned against Python `hashlib.blake2b`
(7 vectors passing, including the RFC 7693 reference). Extend the same
treatment to canonical-JSON signature serialisation once refinements land.

**Patch-engine tests are part of this suite.** `tools/tests/test_patcher.py`
covers the failure modes that produce *silently wrong* output rather than an
error: greedy anchors landing an edit hundreds of lines away, anchors matching
inside comments or string literals, ambiguous anchors, and half-applied patch
sets. 10 tests, all passing. Two of the four cases were real bugs found this
way.

**The audit must survive its own patches.** Once HI06-HI09 land, the constructs
the audit inspects have moved. It now looks for each in its post-patch home
first and reports which state it found, so a re-run on a patched tree is not a
false failure. Extend as each forced-variant patch lands.

**`hip-autotune-inspect` belongs here** as well as in HI15 -- catalog/registry
agreement is most cheaply tested by the tool that reads both.

### 2026-08-20 PH01 audit — rubric-1, no C++ parity/digest test home

Verified: `test-hip-autotune*` is absent from the tree (find across src/ and vendor/); forced-native parity and cross-language digest agreement have no C++ home; the risk the item names (silent digest divergence = replay silently doing nothing) is real; Python offline tests cover the wire/patcher side well.

### 2026-08-20 GPT adjudication (req_67968bc31f544266) — rubric label corrected

GPT: the keep decision stands, but rubric-1 overstates what is demonstrated — absence of a C++ test home is an ASSURANCE GAP (rubric-2 class), not evidence that cross-language digest divergence currently exists. Scheduling: comes after confirmed live integrity defects (HI67/HI64) and before making strong release claims from replay evidence; may share validation machinery with HI37's digest-parity work. Not a proven selection-corrupting defect — do not let it displace confirmed ones in the queue.

### 2026-08-21 hip-autotune-inspect landed (HI15/HI16 shared piece)

The catalog/registry-agreement half of HI16 now has a C++ home that runs the real code instead of a reimplementation:

- `hip-autotune-inspect` (see HI15 notes for the full file list; patch 1230 wires the CMake target). Its registry self-check covers: stable-name uniqueness (registry API), exactly-one-native-wrapper-per-family (the forced-variant parity precondition from the review update above), source_class enum bounds, and null entry-point detection. Its `--json` output is consumed by `bigcherry replay-inspect --manifest`, which recomputes the descriptor hash from the build manifest and reports catalog<->registry agreement (identity = same source revision + same variant set, i.e. a real agreement, not a restatement).
- This covers the offline/inspection slice of HI16's C++ suite. **Cross-language digest agreement now HAS its C++ test home (2026-08-22):** `hip-autotune-inspect --selftest` runs 10 known-answer vectors through the real C++ blake2b (one-shot AND streaming) against Python hashlib.blake2b, covering the three real runtime person prefixes, empty data, 128/129-byte block boundaries, multi-block input, and person zero-padding. `tools/tests/test_blake2b_cross_lang_vectors.py` re-derives every embedded C++ expectation from hashlib offline, so the table cannot drift from either implementation. Verified on Brutus (pin 0adcc3bb, production-replay build): 10/10 pass. The check immediately paid for itself — it caught a real byte-dropping bug in the test's own streaming chunk-split on first run. Remaining for HI16 proper: the remaining GPU-side test homes (forced-native bitwise parity per type/shape class, replay fallback paths under graph capture).

### Forced-native parity is now load-bearing

HI06-HI08 thread the forced variant value as an explicit parameter rather than a hidden override. Each forced-value case (J, block_size, nwarps) must be checked per type and per shape class, because the forced value travels through a different chain of defaulted parameters for each.

### Review update

**Cross-language digest agreement is a required test.** `hip-autotune-blake2b.cpp` has known-answer vectors pinned against Python `hashlib.blake2b`.

**Patch-engine tests are part of this suite.** `tools/tests/test_patcher.py` covers greedy anchors, anchors matching inside comments/strings, ambiguous anchors, half-applied patch sets.

**The audit must survive its own patches.**

**`hip-autotune-inspect` belongs here** as well as in HI15.

### 2026-08-20 PH01 audit + GPT adjudication

Absence of a C++ test home for forced-native parity/cross-language digest agreement is an ASSURANCE GAP (rubric-2), not evidence a defect currently exists.

### 2026-08-21/22 -- hip-autotune-inspect + cross-language digest agreement landed

`hip-autotune-inspect` (HI15/HI16 shared) covers registry/catalog agreement. `hip-autotune-inspect --selftest` (10 known-answer vectors, C++ blake2b vs Python hashlib.blake2b) closed cross-language digest agreement's C++ test home, verified on Brutus (0adcc3bb, production-replay build), 10/10 pass. Remaining for HI16 proper (as of 2026-08-22): forced-native bitwise parity per type/shape class, replay fallback paths under graph capture.

### 2026-08-23 -- forced-native bitwise parity REAL-HARDWARE CONFIRMED (not yet a dedicated permanent test home)

Realized while validating HI24's already-shipped, now-hardware-validated double-native/J_best canary mechanism (see HI24 notes, same session) that it ALREADY performs, and records into the measurements JSONL, exactly the check this item's acceptance criteria ask for: `forced J == native J_best -> bitwise-identical output`. Every real tuning run's `candidates[].nmse`/`max_abs` for the twin/J_best-forced entries IS that check's result, continuously, in production tuning runs -- not merely in a dedicated offline test binary.

Pulled real, direct confirmation across three distinct families on the local AAMD Radeon RX 7900 GRE (gfx1100), built against vendor/rocm/7.1 (see BUILD.md's new git-bash/cmake section for the exact recipe):

- **mmvf** (type_a=f32/type_b=f32, m=16/n=1/k=256): double-native twin `mmvf:native:v1#twin` -- `nmse=0, max_abs=0`.
- **mmvq** (type_a=q4_0/type_b=f32, m=16/n=1/k=256): double-native twin `mmvq:native:v1#twin` -- `nmse=0, max_abs=0`.
- **mmq** (type_a=q4_0/type_b=f32, m=16/n=16/k=256, native resolved to `mmq:native:v1`): BOTH the double-native twin (`mmq:native:v1#twin`, nmse=0/max_abs=0) AND, more precisely matching this item's own stated criterion, the real J_best-forced DIFFERENT candidate object `mmq:q4_0:j16:fb1:t128:o2:i64:sram-q8_0:k256:sk0:v1` (canary_pair for this run) -- `nmse=0, max_abs=0`, exact bitwise agreement with native.

This is genuinely three of the project's five kernel families (mmvf/mmvq/mmq; blas/mmf not yet checked this pass) with real, non-source-contract, real-GPU evidence of forced-native bitwise parity -- narrows this item's remaining forced-native-parity gap substantially, though it does not constitute a standing/repeatable dedicated test target the way `hip-autotune-inspect --selftest` is one (this was an ad hoc verification run, not committed test infrastructure). If a permanent home is wanted, the natural shape is a small script/CI step that runs a handful of real test-backend-ops sweeps with GGML_HIP_TUNE_DOUBLE_NATIVE=1 across representative signatures per family and asserts twin/J_best-forced nmse==0 from the resulting JSONL, rather than a new C++ binary -- the underlying check already exists and runs in production code, this would just be exercising and asserting on it deliberately per family/type.

Still fully open: **replay fallback paths under graph capture** (hardware separation, refined-to-base fallback, missing candidate fallback, corrupted cache, incompatible manifest, process cache hit, graph capture recording the selected launch not the dispatcher lookup) -- a distinct mechanism from forced-native parity, not touched by this pass.

Note for future sessions: real HIP compiler + GPU are available on this workstation (`vendor/rocm/7.1`, AMD Radeon RX 7900 GRE gfx1100) -- see `docs/reference/BUILD.md`'s "Building from git-bash on Windows" section for the working recipe.

### Forced-native parity is now load-bearing

HI06-HI08 thread the forced variant value as an explicit parameter rather than a hidden override. Each forced-value case (J, block_size, nwarps) must be checked per type and per shape class, because the forced value travels through a different chain of defaulted parameters for each.

### Review update

**Cross-language digest agreement is a required test.** `hip-autotune-blake2b.cpp` has known-answer vectors pinned against Python `hashlib.blake2b`.

**Patch-engine tests are part of this suite.** `tools/tests/test_patcher.py` covers greedy anchors, anchors matching inside comments/strings, ambiguous anchors, half-applied patch sets.

**The audit must survive its own patches.**

**`hip-autotune-inspect` belongs here** as well as in HI15.

### 2026-08-20 PH01 audit + GPT adjudication

Absence of a C++ test home for forced-native parity/cross-language digest agreement is an ASSURANCE GAP (rubric-2), not evidence a defect currently exists.

### 2026-08-21/22 -- hip-autotune-inspect + cross-language digest agreement landed

`hip-autotune-inspect` (HI15/HI16 shared) covers registry/catalog agreement. `hip-autotune-inspect --selftest` (10 known-answer vectors, C++ blake2b vs Python hashlib.blake2b) closed cross-language digest agreement's C++ test home, verified on Brutus (0adcc3bb, production-replay build), 10/10 pass. Remaining for HI16 proper (as of 2026-08-22): forced-native bitwise parity per type/shape class, replay fallback paths under graph capture.

### 2026-08-23 -- forced-native bitwise parity REAL-HARDWARE CONFIRMED, then given a repeatable test home

Realized while validating HI24's already-shipped, now-hardware-validated double-native/J_best canary mechanism (see HI24 notes, same session) that it ALREADY performs, and records into the measurements JSONL, exactly the check this item's acceptance criteria ask for: `forced J == native J_best -> bitwise-identical output`. Every real tuning run's `candidates[].nmse`/`max_abs` for the twin/J_best-forced entries IS that check's result, continuously, in production tuning runs -- not merely in a dedicated offline test binary.

Pulled real, direct confirmation across three distinct families on the local AMD Radeon RX 7900 GRE (gfx1100), built against vendor/rocm/7.1 (see BUILD.md's "Building from git-bash on Windows" section for the recipe):

- **mmvf** (type_a=f32/type_b=f32, m=16/n=1/k=256): double-native twin `mmvf:native:v1#twin` -- `nmse=0, max_abs=0`.
- **mmvq** (type_a=q4_0/type_b=f32, m=16/n=1/k=256): double-native twin `mmvq:native:v1#twin` -- `nmse=0, max_abs=0`.
- **mmq** (type_a=q4_0/type_b=f32, m=16/n=16/k=256, native resolved to `mmq:native:v1`): BOTH the double-native twin (nmse=0/max_abs=0) AND, more precisely matching this item's own stated criterion, the real J_best-forced DIFFERENT candidate object `mmq:q4_0:j16:fb1:t128:o2:i64:sram-q8_0:k256:sk0:v1` -- `nmse=0, max_abs=0`, exact bitwise agreement with native.

Three of the project's five kernel families (mmvf/mmvq/mmq; blas/mmf not yet checked) now have real, non-source-contract, real-GPU evidence of forced-native bitwise parity.

Turned this from an ad hoc verification into a repeatable check: `tools/bigcherry/hi16_forced_native_parity.py` runs a set of (op_filter, expected_native_family) cases against a real test-backend-ops binary with `GGML_HIP_TUNE_DOUBLE_NATIVE=1`, parses the resulting measurements JSONL, and asserts every `#twin`/J_best-forced-pair row has `nmse == 0` and `max_abs == 0` -- fails closed (reports the row and its actual nmse) rather than silently passing on a near-zero-but-nonzero divergence. Offline-testable parsing/assertion logic (`tools/tests/test_hi16_forced_native_parity.py`, fake JSONL fixtures, no binary needed for that half); the real-hardware invocation itself needs a built test-backend-ops binary and is documented, not part of automated CI (same pattern as correctness_evidence.py/HI80's CLI). Re-ran the real three-family sweep through this script on the same local GPU to confirm the script's own logic agrees with the manual check above -- all three PASS.

Still fully open: **replay fallback paths under graph capture** (hardware separation, refined-to-base fallback, missing candidate fallback, corrupted cache, incompatible manifest, process cache hit, graph capture recording the selected launch not the dispatcher lookup) -- a distinct mechanism, not touched by this pass. Also not yet covered by the new script: blas/mmf families.

Note for future sessions: real HIP compiler + GPU are available on this workstation (`vendor/rocm/7.1`, AMD Radeon RX 7900 GRE gfx1100) -- see `docs/reference/BUILD.md`'s "Building from git-bash on Windows" section.

### 2026-08-23 -- blas/mmf forced-native bitwise parity CLOSED (last two of five families)

Completes the forced-native-parity acceptance criterion across all five kernel families (mmvf/mmvq/mmq confirmed 2026-08-23 earlier this session; blas/mmf now confirmed too), using the existing `tools/bigcherry/hi16_forced_native_parity.py` script against a real test-backend-ops binary on the local AMD Radeon RX 7900 GRE (gfx1100), build/workstation-tune, vendor/rocm/7.1.

Finding real qualifying shapes for blas/mmf took real investigation, not guessing: read `ggml_cuda_should_use_mmf()` (vendor/llama.cpp/ggml/src/ggml-cuda/mmf.cu:228) directly -- on RDNA3.0 it requires `src1_ncols <= 8` AND (for F32) `amd_mfma_available(cc)`, which gfx1100 (RDNA3, WMMA not MFMA) does not have, so F32 can never route to MMF on this GPU; F16/BF16 route via `amd_wmma_available` instead. Also found (hip-autotune-dispatch.cu:176) that MMVF is checked BEFORE MMF in the family-selection ladder, so any shape MMVF is eligible for shadows MMF regardless of ncols -- several probed f32 shapes that looked like they should hit MMF landed on MMVF or BLAS instead. Rather than keep guessing shapes, pulled a REAL mmf-native canonical signature straight out of this session's own earlier full MUL_MAT sweep (wddm-retry-fix3 measurements) and decoded it: type_a=f16, type_b=f32, m=32, n=7, k=256 -- confirmed natively resolving to `mmf:native:v1` before running the parity check.

Results, both PASS via `python -m bigcherry.hi16_forced_native_parity`:
- **mmf**: `type_a=f16,type_b=f32,m=32,n=7,k=256` -- double-native twin/J_best-equivalent row, nmse=0, max_abs=0.
- **blas**: `type_a=f16,type_b=f32,m=16,n=8,k=256` -- same, nmse=0, max_abs=0.

All five of the project's kernel families (mmvf, mmvq, mmq, mmf, blas) now have real, non-source-contract, real-GPU evidence of forced-native bitwise parity, via the repeatable `hi16_forced_native_parity.py` script (not just an ad hoc read of a measurements file).

**Still fully open:** replay fallback paths under graph capture (hardware separation, refined-to-base fallback, missing candidate fallback, corrupted cache, incompatible manifest, process cache hit, graph capture recording the selected launch not the dispatcher lookup) -- a distinct mechanism, not touched by this pass. This is the item's one remaining gap.

### 2026-08-23 -- forced-native bitwise parity REAL-HARDWARE CONFIRMED, then given a repeatable test home

Realized while validating HI24's already-shipped, now-hardware-validated double-native/J_best canary mechanism that it ALREADY performs, and records into the measurements JSONL, exactly the check this item's acceptance criteria ask for. Pulled real, direct confirmation across mmvf/mmvq/mmq on the local RX 7900 GRE, then turned this into a repeatable check: `tools/bigcherry/hi16_forced_native_parity.py` runs a set of (op_filter, expected_native_family) cases against a real test-backend-ops binary with `GGML_HIP_TUNE_DOUBLE_NATIVE=1`, parses the resulting measurements JSONL, and asserts every `#twin`/J_best-forced-pair row has `nmse == 0` and `max_abs == 0`.

### 2026-08-21/22 -- hip-autotune-inspect + cross-language digest agreement landed

`hip-autotune-inspect` (HI15/HI16 shared) covers registry/catalog agreement. `hip-autotune-inspect --selftest` (10 known-answer vectors, C++ blake2b vs Python hashlib.blake2b) closed cross-language digest agreement's C++ test home, verified on Brutus (0adcc3bb, production-replay build), 10/10 pass.

Note for future sessions: real HIP compiler + GPU are available on this workstation (`vendor/rocm/7.1`, AMD Radeon RX 7900 GRE gfx1100) -- see `docs/reference/BUILD.md`'s "Building from git-bash on Windows" section for the working recipe.

## Change Log

- 2026-08-04T01:22:43.963919+00:00 (created-by): Created by agent
- 2026-08-04T01:27:25.602830+00:00 (updated-by): Updated: skill='advanced'

## Ledger-events


- chg_20260813_075124_bound-production-candidate-cor_9028
- 2026-08-13T07:51:24.589996+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-21T12:49:12.092767+00:00 (updated-by): Updated: section:notes
- chg_20260822_040544_new-hip-autotune-inspect-tool_5307
- 2026-08-22T04:05:44.194312+00:00 (updated-by): Updated: section:ledger-events
- chg_20260822_044637_the-replay-cache-inspector-is_3051
- 2026-08-22T04:46:37.890992+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-23T03:35:13.747679+00:00 (updated-by): Updated: section:notes
- 2026-08-23T03:35:43.504347+00:00 (updated-by): Updated: section:notes
- chg_20260823_034049_turned-this-sessions-real-har_1769
- 2026-08-23T03:40:49.762632+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-23T04:55:12.960233+00:00 (updated-by): Updated: section:notes
- chg_20260823_045519_closed-the-last-kernel-family_5405
- 2026-08-23T04:55:19.744883+00:00 (updated-by): Updated: section:ledger-events
