---
id: HI149
order: 0
plan: hip-autotune
state: pending
created-at: '2026-08-29T13:07:38.038754+00:00'
breadth: ''
skill: intermediate
created-by: agent
work: S
---

# CORRECTED -- no real regression: RD13 test assumed an out-of-band local mutation that pull() legitimately resets

## Description

CORRECTION (2026-08-29): the original title/description for this item was wrong. Verified directly: patch 1206_rd13_mul_mat_add_view_fusion's exact `_OLD` anchor block matches byte-for-byte in the real b10680 vendor/llama.cpp/ggml-cuda.cu, and a real dry-run `apply_patch(mod.PATCH, vendor_root, dry_run=True)` succeeds cleanly. There is NO upstream shape change and NO patch incompatibility.

The actual cause: tools/tests/patch/test_patch_1206_rd13_mul_mat_add_view_fusion.py::Rd13PatchApplicationTests::test_current_pinned_source_has_the_expected_fusion_site reads vendor/llama.cpp/ggml/src/ggml-cuda/ggml-cuda.cu DIRECTLY (not through any fixture) and asserts it already contains the patch's POST-APPLY text (`has_view`, etc). That assumption only holds if patch 1206 (group 'rdna-boosts', state 'untested', NOT part of the 'bigcherry' recipe) had previously been applied out-of-band to the local vendor tree, e.g. by an earlier standalone RD13 benchmarking session. `bigcherry pull`'s `git checkout --force` to the new tag legitimately resets every tracked file, including ggml-cuda.cu, back to pristine upstream -- correctly erasing that out-of-band mutation, since it was never committed or recipe-tracked. The test then failed for the right structural reason (the text really is gone from the live tree) but the wrong actual concern (nothing is broken; the patch just isn't currently applied there).

Still worth a small process fix even though nothing is broken: this test reads a live, mutable path (vendor/llama.cpp) rather than an isolated fixture, so its pass/fail depends on incidental prior out-of-band state in whoever's working tree runs it -- it will spuriously fail after every pull unless 1206 happens to be re-applied first, and spuriously pass if it happens to still be sitting there. Consider either (a) having the test apply the patch to a temp copy of a real snapshot and assert against that, matching test_patch_dry_run_rewrites_only_the_exact_pre_change_block's own pattern in the same file, or (b) just deleting this one test since the dry-run apply test already covers real compatibility.

The hi104_nested_runtime failures (missing hip-autotune-build-hash.h) are unrelated and separately expected -- stale build artifacts invalidated by the bump per PIN_BUMP.md's own invalidation list, resolves after a real rebuild.

## Steps

1. Decide whether to fix or delete test_current_pinned_source_has_the_expected_fusion_site per the two options above.
2. No patch content changes needed -- 1206 is fine as-is against b10680.

## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files



## Validation

tools/tests/patch/test_patch_1206_rd13_mul_mat_add_view_fusion.py passes against the vendor tree at b10680.

## Effort & Risk



## Standards



## Acceptance Criteria



## Notes



## Change Log

- 2026-08-29T13:07:38.038754+00:00 (created-by): Created by agent
- 2026-08-29T13:11:38.329619+00:00 (updated-by): Updated: section:title, section:description, section:steps
