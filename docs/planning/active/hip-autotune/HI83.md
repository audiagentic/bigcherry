---
id: HI83
order: 0
plan: hip-autotune
state: pending
created-at: '2026-08-22T02:17:46.938754+00:00'
breadth: ''
skill: advanced
created-by: agent
work: L
---

# Validated-state evidence contract: tie STATE="validated" to real HI82 evidence

## Description

GPT review (gpt-auto-agent, req_6b1466ee8369406c) of the completed HI82 body of work (patch source isolation, activation evidence, build-configuration identity, campaign-identity-gated resume, per-bench-arm build identity -- all implemented and verified on real gfx1100 hardware this session) found the most consequential remaining gap: BigCherry can now produce strong, real evidence that a patch was genuinely built, activated, and tested -- but the repository's authoritative patch lifecycle state (a patch module's own `STATE = "validated" | "untested" | "rejected"`, cross-checked against the TOML catalog by patch_catalog.py) is NOT mechanically tied to that evidence. A developer can still hand-edit a patch module's STATE to "validated" with no machine check that matching evidence exists, or that it still matches the CURRENT patch implementation/source/build. Editing a validated patch today does not automatically invalidate its validated status.

GPT's recommended contract (a validation record binding, at minimum): patch ID; patch_implementation_digest; base/source revision or patched source-tree identity; relevant runtime GPU architecture; activation disposition (executed, or legitimately gate-verified-blocked); correctness disposition (passed, or an explicitly admissible N/A with eligibility-gate proof); campaign identity digest; the relevant completed-build identities (compile_verification_id, runtime_bundle_hash); evidence/artifact references. For performance patches, GPT recommends also binding benchmark evidence, but keeping "performance claim proven" conceptually distinct from "correctness/activation validated".

Governance check to add (conceptually): STATE != validated -> no evidence requirement. STATE == validated -> current patch digest must have matching evidence, current source/base identity must match, required architecture/gate conditions must be covered, activation must be proven or gate-verified-blocked, correctness must be proven. The key property this closes: staleness -- editing patches/1204_rd08_q6k_mmvq_vdr2.py (or any validated patch) after a successful validation run should invalidate STATE="validated" until revalidated, since we already have the exact patch/source/build identities needed to detect that.

GPT explicitly ranked this as the single highest-value next piece of work in this whole area (above the deferred campaign_workers.py production-worker compile-verification migration, and above running a validation sweep over the untested rdna-boosts patch backlog) -- "the repository does not yet require that evidence before allowing the authoritative word validated to be asserted... closing that boundary is what I would do next."

## Steps



## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files



## Validation



## Effort & Risk



## Standards



## Acceptance Criteria



## Notes

Filed 2026-08-22 directly from GPT's holistic HI82 review (req_6b1466ee8369406c), rather than implemented immediately, since it's a substantial new governance feature (touches patch_catalog.py, patch lifecycle reporting, and the whole patches/ authoring workflow) deserving its own dedicated scoping rather than being folded onto the tail of the already-very-long HI82 session arc. HI82 itself (source isolation, activation evidence, build identity, campaign-identity resume, per-bench-arm identity) is complete and closed; this item is deliberately new/separate. GPT's secondary recommendation, once this is done: stop building infrastructure and run the untested rdna-boosts patch backlog through the now-solid pipeline -- that is where the investment should start paying off directly.

Filed 2026-08-22 from GPT's holistic HI82 review (req_6b1466ee8369406c). Full design + code from GPT (req_487497b28d444d50), reviewed against the real repo at commit 7648207 before implementing -- found and fixed one real signature error in GPT's own code (psi.materialize_source()'s actual keyword arguments don't match what GPT reconstructed from memory).

DONE (safe, additive, no production-path enforcement): new tools/bigcherry/patch_validation_evidence.py -- the tracked JSON evidence contract itself (one file per patch under docs/reference/patch-validation-evidence/, git-tracked since the authority must be resolvable from the repo alone, not artifacts/ (gitignored) or the external ledger (not resolvable from a fresh checkout/CI)). Key design point verified as correct and necessary: patch_validation_subject_digest() normalizes only the literal STATE=... assignment, so the validate-then-flip-STATE-to-"validated" sequence doesn't immediately self-invalidate the evidence that authorized it, while any other byte change (edit, guard, anchor, GROUP, provenance) does invalidate it. 20 tests, all passing.

patch_catalog.py extended: new validation_architectures CatalogEntry field (a distinct policy field from the pre-existing free-form `hardware` metadata), validation_evidence_statuses()/require_validation_evidence() (pure diagnostic functions), and cross_check() extended to check evidence for the real repository catalog by default (synthetic temp-catalog unit tests keep pre-HI83 narrow semantics unless they opt in). Generated the required one-time legacy-baseline.json grandfather snapshot (15 currently-validated patches' exact content hashes) -- without it, landing this would have immediately broken every existing STATE="validated" patch's cross-check with zero migration path, exactly as GPT warned.

New `patch-verify-evidence` CLI command (observational only). patch_validation_campaign.py now writes a real HI83 record after every successful campaign run (hoisted patch_digest/patched_source_tree locals, fixed activation_evidence/activation_verdict's undefined-outside-the-trace-probe-block lifetime, added --correctness-evidence input). Verified for real on gfx1100: reran RD08+Qwen3.5-2B, got a real, honest record (validation_disposition=incomplete, eligible_for_validated_state=False, since RD08 currently has no activation probe registered and no correctness evidence supplied) -- exactly the intended behavior, not a false positive.

DELIBERATELY NOT DONE (per my own judgment call, not GPT's recommendation, after finding a real conflict): hard enforcement wiring into campaign_lane.py's _execute_materialize_phase() or __main__.py's _apply_selection(). Reason: tools/bigcherry/patch_catalog.py's own module docstring explicitly states it is "read-only, additive metadata... does not change what bigcherry apply/bigcherry build actually select" -- wiring hard enforcement through it would directly conflict with that stated, deliberate architectural invariant. Combined with GPT's own two flagged open policy questions (a correctness-evidence producer per patch class -- no universal one exists today; and a per-patch validation-architectures obligation -- no authoritative source exists to infer it from), and the very real risk that flipping a hard gate on in a shared, actively-developed repo (a concurrent session is independently working in this same working tree) with zero currently-passing real HI83 records could break `bigcherry build`/`apply` for everyone, this is exactly the kind of high-blast-radius change that needs explicit user sign-off first, not just GPT's say-so. Flagging this tension explicitly rather than silently either implementing or ignoring GPT's suggestion.

Full offline pytest suite green throughout (1521 passed, 3 skipped, up from 1501 pre-HI83).

## Change Log

- 2026-08-22T02:17:46.938754+00:00 (created-by): Created by agent
- 2026-08-22T02:52:35.415149+00:00 (updated-by): Updated: section:notes

## Ledger-events

- chg_20260822_025254_built-the-core-of-the-validate_9940
- 2026-08-22T02:52:54.662531+00:00 (updated-by): Updated: section:ledger-events
