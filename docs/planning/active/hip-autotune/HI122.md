---
id: HI122
order: 0
plan: hip-autotune
state: pending
created-at: '2026-08-26T09:32:55.506612+00:00'
breadth: ''
skill: advanced
created-by: agent
work: M
---

# HI121 close-out step 1: exact per-row DB winner/hardware/native binding (RV84 P0-1)

## Description

RV84 P0-1, confirmed real by dev-gpt-agent's independent investigation (2026-08-26) against current source: replay_projection._require_row_belongs_to_build() only proves a measurement EXISTS for (source_build_id, dispatch_digest, signature_digest) -- it does not bind the artifact row's own winner/native/hardware/candidate identity to that DB measurement/winner. Concrete attack (from RV84): real DB row has winner=generated-X/native=native-X; edited artifact row claims winner=native-Y/native=native-Y (native-Y exists in both manifests) -- row-membership and candidate-equivalence both pass, and replay's promotion gate treats winner==native as safe, skipping non-native gates.

## Steps

1. Read tools/bigcherry/tuning/replay_projection.py's _require_row_belongs_to_build() and the DB schema (build_capability, measurement, winner, hardware, signature, candidate tables in sql/dispatch-db.sql).
2. Replace the membership-only check with authoritative DB linkage: JOIN winner (or measurement) against hardware, signature, candidate to prove the artifact row's dispatch + signature + hardware + winner stable_name + native stable_name ALL match what the DB actually recorded for source_build_id -- not just that SOME measurement exists for that (build,dispatch,signature) triple.
3. Add adversarial tests: edited winner/native/hardware in the artifact row (each independently and combined) must be rejected even when dispatch/signature membership alone would pass.

## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files

tools/bigcherry/tuning/replay_projection.py, tools/tests/tuning/test_replay_projection.py

## Validation

New adversarial tests pass; existing replay_projection test suite unaffected; full offline suite green.

## Effort & Risk

M, correctness-critical but localized to replay_projection.py and its DB queries -- no hardware required per dev-gpt-agent's investigation.

## Standards



## Acceptance Criteria



## Notes

Per dev-gpt-agent's recommended closure order (2026-08-26): do this and step 2 (HI122) FIRST -- they are bounded trust-boundary fixes that clarify the data model before the larger candidate-implementation-identity work (step 3). Better still per RV84: persist an ingest-time artifact digest binding the whole measurements artifact to its DB ingestion -- consider if the JOIN-based fix alone proves insufficient.

## Change Log

- 2026-08-26T09:32:55.506612+00:00 (created-by): Created by agent
