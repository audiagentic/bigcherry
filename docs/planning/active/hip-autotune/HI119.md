---
id: HI119
order: 0
plan: hip-autotune
state: pending
created-at: '2026-08-25T06:40:18.995422+00:00'
breadth: ''
skill: advanced
created-by: agent
work: M
---

# Fused MUL_MAT(_ID)+GLU correctness-evidence harness (real two-op graph, not the single-op --test-file mapper)

## Description

Follow-up to HI118 (which captures real gate-tensor geometry in the signature schema -- a hard prerequisite for this item). test-backend-ops' --test-file/test_generic_op escape hatch (the mechanism HI80/HI105's mapper pattern relies on) can only build ONE op per test case (confirmed via make_test_cases_from_file(), vendor/llama.cpp/tests/test-backend-ops.cpp ~line 10528) -- it cannot represent the real fused MUL_MAT(_ID)+GLU compound HI108's investigation found every recorded GLU signature actually is. This item builds a bespoke small harness that constructs the REAL two-tensor-input fused computation (the real x/gate mul_mat_id calls with HI118's captured real gate geometry, followed by the real GLU op) via the ggml C API directly, runs native vs candidate, and produces evidence in the same digest/metric format tools/bigcherry/correctness_evidence.py's write_correctness_evidence() already expects -- so it plugs into the existing hi80_generate_correctness_evidence.py CLI / e2e_smoke_campaign.py S3b stage without changing their own orchestration logic, only the evidence-GENERATION step for this one op shape.

## Steps

MAJOR PROGRESS 2026-08-25 (continued): steps 7, 8, 9, 10-11, 13 are now also DONE, real-hardware validated end-to-end. Session total: patches 1238 (deterministic init_mul_mat_id_tensors, closing the correctness-integrity gap the design review flagged), 1239 (test_bigcherry_moe_glu_fusion registered test_case, with a broadcast/non-broadcast `b` parameter added after a real shape-mismatch bug was found and fixed), 1240 (--moe-glu-file CLI hook, 8-field line format after the broadcast fix) -- all three committed, in patches/catalog.toml (49 patches) and config/recipes.toml's hi105-correctness set.

Python side (this session, committed d3fc54d): tools/bigcherry/tuning/signature_mapping.py gained signature_to_moe_glu_file_line() -- fails closed on unknown/non-GLU op, missing HAS_IDS, any HI118 bias/scale bit set (covers step 13), non-GATE fusion kind, unfusable glu_op, malformed ne0/ne1/ned/n_expert(_used), invalid ne1[1] (covers step 8's validation requirement). tools/bigcherry/tuning/correctness_evidence.py's run_test_backend_ops/collect_seed_evidence/generate_correctness_evidence gained an additive moe_glu_file parameter (step 10-11's plumbing). tools/bigcherry/hi80_generate_correctness_evidence.py's generate_for_row() gained a real GLU branch: routes GLU-op rows through the new mapper + --moe-glu-file instead of the single-op --test-file path, and -- the review's 'most important new requirement' -- added _observed_signature_hex(), the mandatory observed-vs-requested dispatch-signature-digest gate (step 9): runs the same --moe-glu-file line once under GGML_HIP_DISPATCH_MODE=record and refuses to certify correctness (raises CliError) if the real observed signature hex doesn't match the row's own requested signature_hex, closing the fusion-detection-regression gap the review identified.

Step 7 reassessed as DONE via a different (better) mechanism than originally envisioned: rather than registering more fixed instances, patch 1240's --moe-glu-file takes a runtime `type glu_op k n m n_mats n_used broadcast` line (mirroring --test-file's own convention), so any k/n/m/n_expert/n_expert_used/type/glu_op/broadcast combination can be evidenced without recompiling test-backend-ops -- confirmed on real hardware to reproduce the EXACT SAME recorded signature as the statically-registered instance.

Real-hardware end-to-end pipeline validation (throwaway script, not committed -- see HI119's own real-hardware trail in test_signature_to_moe_glu_file_line.py and test_hi80_generate_correctness_evidence.py's docstrings): built a real schema-6 dispatch_db seeded with HI108's exact real blocked routed dispatch shape, ran the REAL generate_for_row() against the REAL patched test-backend-ops binary. Confirmed (a) a deliberately-wrong signature_hex correctly triggers the observed-digest CliError; (b) using the real observed hash as signature_hex produces real written evidence (evidence_id=1, e_c=6.59967e-14) and a real promotion_correctness_gate evaluation. One more real bug found+fixed along the way: an earlier rebuild cycle had dropped patch 1223 (machine-readable correctness metrics) from the chain, which looked like a deep C++ BIGCHERRY_CORRECTNESS_METRIC-missing bug until `grep -c` on the built binary's own test-backend-ops.cpp showed 0 occurrences -- root cause was the build chain, not the harness.

Offline coverage added and green: tools/tests/test_signature_to_moe_glu_file_line.py (9 tests, using the real signature captured via record-mode on live hardware) and 4 new tests in tools/tests/test_hi80_generate_correctness_evidence.py's new GluGenerateForRowTests class (14 tests total in that file) covering the GLU branch's happy path, the real promotion gate pass, and BOTH outcomes of the observed-digest gate (mismatch raises CliError with no evidence written; missing observation row raises CliError). Full offline suite: 2094 passed, 3 skipped, only the known pre-existing Windows symlink-privilege test failing (unrelated).

Still genuinely OPEN (steps 14-16, not attempted this session):
14-15. Real-hardware validation against the LITERAL two HI108 dispatch IDs (7ef2471585a5aa6fbb49384efe566ac5 and d816ffce280de2a7ce2292c6406da23d) through the actual production campaign pipeline and tune-promote -- this session validated the MECHANISM/SHAPE with a synthetic fixture matching HI108's shape, not the literal dispatch rows from a real campaign run. If promoted, a real end-to-end A/B is also still required before claiming any production win.
16. HI108's SECOND blocked dispatch (d816ffce280de2a7ce2292c6406da23d, has_ids=false, dense/non-routed shared-expert GLU fusion) needs a SIBLING test_case using plain ggml_mul_mat instead of ggml_mul_mat_id (no ids/n_mats/n_used) -- test_bigcherry_moe_glu_fusion as built only covers the has_ids=true/MUL_MAT_ID-routed case. Not started.

Also still open from the review, not yet done: a behavioral signature/digest test (toggling each HI118 flag bit must change the produced dispatch signature -- not yet written); explicit fail-closed handling for legacy v1 fusion records that predate the HI118 flag bits (generate_for_row's GLU branch does not special-case these yet).

## Detailed Solution & Technical Design

dev-gpt-agent deep design review (2026-08-25) -- GO verdict with two major corrections, both independently verified against real source before being recorded here:

VERIFIED CLAIM 1 -- test_mul_mat_vec_fusion already exists (vendor/llama.cpp/tests/test-backend-ops.cpp, struct at line 6389, use_id=true graph-building branch at line 6507-6554): confirmed real. Its use_id path builds `ggml_mul_mat_id(ctx, ups, cur, ids)` and `ggml_mul_mat_id(ctx, gates, cur, ids)` sharing the exact same `cur`/`ids` tensor objects, then combines via build_gate() (ggml_glu_split or ggml_swiglu_oai for the SWIGLU_OAI case) -- almost exactly HI119's needed shape. BUT confirmed real gap: line 6548-6550 shows an unconditional `ggml_mul(ctx, out, scale)` AFTER build_gate()'s GLU output, so the GLU there is never the terminal graph output -- must be fixed (make GLU terminal) for HI119's own use, not reused unmodified.

VERIFIED CLAIM 2 -- init_mul_mat_id_tensors() (test-backend-ops.cpp line 4717) is genuinely non-deterministic: confirmed real, exact code is `std::random_device rd; std::default_random_engine rng(rd());` seeding the expert-ID shuffle. Confirmed patch 1222 (patches/1222_hi67_deterministic_test_backend_ops_seed.py) only covers float tensor init. Confirmed patch 1236's own docstring (patches/1236_hi105_deterministic_mul_mat_id_ids.py) explicitly scopes itself ONLY to test_generic_op's own SEPARATE std::random_device, and explicitly distinguishes 'the registered test_mul_mat_id class's own init_mul_mat_id_tensors()' as out of scope for that patch. So ANY registered test_case using init_mul_mat_id_tensors() (test_mul_mat_id, test_mul_mat_vec_fusion, and HI119's planned new class if it reused this initializer unmodified) gets non-deterministic expert routing today -- native and candidate forced-runs could route to different experts despite identical deterministic float inputs, silently invalidating any correctness comparison. This is a real, load-bearing blocker for HI119's evidence integrity, not a style nit -- must be fixed (extend deterministic-seed coverage to this initializer, or an equivalent) before HI119's evidence can be trusted.

Other source-verified corrections from the review (see revised Steps for the full list): ggml_cuda_should_fuse_mul_mat/ggml_can_fuse_subgraph require gate/up to share the exact same activation/ids TENSOR POINTERS (not merely same-shape separate tensors) and each MM result to have exactly one consumer; supported glu_op is SWIGLU/GEGLU/SWIGLU_OAI only (swapped rejected); the real dispatch construction site's tensor identities (up_n->src[0/1/2] as src0/src1/ids, gate_n->src[0] as fusion.gate, the GLU node itself as dst) match what HI108/HI118 already found from the consumer side.

MOST IMPORTANT NEW REQUIREMENT (not something I had planned for): correctness PASS must require proof the fused signature was ACTUALLY executed (observed dispatch signature digest == requested digest, observed candidate resolution == expected), not just that CPU-vs-GPU numerics matched -- otherwise a future fusion-detection regression (CUDA silently falling back to unfused execution) could make CPU comparison still pass while HI119 wrongly certifies correctness for a candidate that was never actually exercised as fused. This closes a real gap in what 'correctness evidence' would otherwise mean for this specific harness.

## Code Samples & Guidance



## Files

- tools/bigcherry/ (new fused-GLU evidence producer module)
- tools/bigcherry/hi80_generate_correctness_evidence.py
- tools/bigcherry/correctness_evidence.py (only if a genuinely shared hook is needed -- prefer additive, do not weaken the existing MUL_MAT/MUL_MAT_ID paths)

## Validation



## Effort & Risk



## Standards



## Acceptance Criteria



## Notes

Split from HI108 (2026-08-25). HARD DEPENDENCY on HI118 (gate-tensor geometry must be captured before this can be built correctly -- dev-gpt-agent's explicit warning: do not infer the gate shape from the x side even where models happen to make them equal). Real, well-scoped, but materially bigger than HI105's single-op mapper pattern -- this is a new evidence-generation code path, not a new branch in an existing dispatcher function. Cross-reference: HI108 (closed, investigation only), HI118 (prerequisite), HI105 (the pattern this cannot directly reuse and why).

Split from HI108 (2026-08-25). UNBLOCKED (2026-08-25): HI118 (gate-tensor geometry prerequisite) is now DONE -- turned out to need only presence flags (GGML_HIP_SIG_FUSION_X_BIAS/GATE_BIAS/X_SCALE/GATE_SCALE), not new ne/type fields or a schema bump, since every fusion tensor's real geometry is already algebraically derivable from existing signature fields (gate shares src0's type/stride; biases are F32 sized by dst.ne[0]/n_expert -- all confirmed against mmvq.cu's own GGML_ASSERT calls). This item can proceed directly against the current schema: read sig.fusion/sig.glu_op/ne0/ned/n_expert plus the new HI118 presence flags to determine exactly which synthetic gate/bias tensor(s) to construct. Real, well-scoped, but materially bigger than HI105's single-op mapper pattern -- this is a new evidence-generation code path, not a new branch in an existing dispatcher function. Cross-reference: HI108 (closed, investigation only), HI118 (prerequisite, completed), HI105 (the pattern this cannot directly reuse and why).

## Change Log

- 2026-08-25T06:40:18.995422+00:00 (created-by): Created by agent

## Ledger-events


- chg_20260825_064046_investigated-extending-correct_1939
- 2026-08-25T06:40:46.432794+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-25T07:41:16.388636+00:00 (updated-by): Updated: section:notes
- chg_20260825_074124_extended-the-tuning-dispatch-s_4182
- 2026-08-25T07:41:24.213308+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-25T08:11:43.186565+00:00 (updated-by): Updated: section:steps
- chg_20260825_081152_independent-review-caught-a-re_7832
- 2026-08-25T08:11:52.181621+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-25T09:29:28.279564+00:00 (updated-by): Updated: section:detailed_solution
- 2026-08-25T09:30:28.073018+00:00 (updated-by): Updated: section:detailed_solution
- chg_20260825_093038_made-concrete-design-progress_4093
- 2026-08-25T09:30:38.863708+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-25T09:39:40.521747+00:00 (updated-by): Updated: section:detailed_solution
- chg_20260825_094016_confirmed-with-real-data-not_5189
- 2026-08-25T09:40:16.908717+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-25T10:22:50.552338+00:00 (updated-by): Updated: section:steps
- 2026-08-25T10:23:07.684957+00:00 (updated-by): Updated: section:detailed_solution
- chg_20260825_102315_a-second-deep-review-caught-a_3927
- 2026-08-25T10:23:15.511130+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-25T11:27:41.307008+00:00 (updated-by): Updated: section:steps
- chg_20260825_112748_fixed-a-real-correctness-integ_2095
- 2026-08-25T11:27:48.428799+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-25T11:47:48.392496+00:00 (updated-by): Updated: section:steps
- chg_20260825_114755_built-and-hardware-validated-t_5202
- 2026-08-25T11:47:55.692019+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-25T12:42:32.169583+00:00 (updated-by): Updated: section:steps
- chg_20260825_124242_finished-the-fused-glu-moe-ga_8822
- 2026-08-25T12:42:42.174533+00:00 (updated-by): Updated: section:ledger-events
