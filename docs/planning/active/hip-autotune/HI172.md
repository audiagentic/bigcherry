---
id: HI172
order: 0
plan: hip-autotune
state: pending
created-at: '2026-09-08T17:26:42.486119+00:00'
breadth: ''
skill: advanced
created-by: agent
priority: P0
work: M
---

# Critical-path/slack vs contention hypothesis for isolated-vs-e2e tuning divergence

## Description

HI171's real e2e matrix (2026-09-08, current source, all 5 HI168 caches) found that isolated per-dispatch-key tuner speedups (5.7%-35% across the five caches, all execution-audit-confirmed as genuinely launching from cache) do NOT reliably translate into real end-to-end serving performance on ANY topology tested: GPU0 shows a real regression (-1.3% pp512, -1.8% pp2048 replay vs native), GPU1 is mixed, GPU2/GPU3/27B are flat/no-measurable-effect despite real isolated wins.

REVISED TAXONOMY (dev-gpt-agent review, 2026-09-08, req_714d201cc4b64f87 -- corrects the original 3-way + inverse framing):

1. DILUTION -- isolated win real, tuned dispatch is a small fraction of total GPU time; nets out to well under 1% overall, invisible against noise. kernel-fraction's family share/wall-clock ceiling tests this directly.

2. OFF-CRITICAL-PATH -- kernel genuinely faster but doesn't gate the next pipeline step (hidden behind/overlapping other work), so wall-clock is unaffected. IMPORTANT CORRECTION (gpt): like dilution, this alone gives ~0 wall benefit, NOT a negative result -- it CANNOT by itself explain GPU0's real regression. GPU0 needs hypothesis 3 or 4 below, not 2.

3. IN-CONTEXT KERNEL REGRESSION (broadened from the original 'contention-dependent' framing -- gpt: proving the replay kernel is genuinely slower in real pipelined context proves isolated-to-in-context reversal, but does NOT by itself prove occupancy/register/LDS contention specifically; other real causes include cache/memory-state effects, DVFS/power-state differences, launch/queue differences, or changed surrounding activity. Must not claim 'contention' as the mechanism without direct concurrency evidence.)

4. SYSTEM/NEIGHBOR EXTERNALITY (NEW, added per gpt review -- the missing mechanism that can produce a negative e2e result while the tuned kernel ITSELF remains faster in context): the tuned kernel stays faster in-context, but its presence makes SURROUNDING kernels slower -- concurrent resource pressure, sequential cache pollution, memory-bandwidth contention, host launch/sync overhead shifts, etc. This is now believed to be the most likely explanation for a genuine negative wall-clock result where the kernel itself measures fine.

INVERSE CASE (kept, but gpt notes traces alone cannot establish it, since an isolated LOSER candidate was never selected/promoted and so never appears in a replay trace to compare): a candidate that measures worse in isolation but is better in real serving by exploiting GPU slack differently. Testing this requires an INTERVENTIONAL counterfactual -- force an isolated runner-up/loser candidate into the cache (GGML_HIP_FORCE_CANDIDATE) and measure real e2e, not passive tracing of what's already promoted. This is exactly the winner-vs-runner-up counterfactual design already discussed for the GPU0/GPU1 cross-swap test earlier in this session -- same mechanism, now formally the way to test hypothesis 4's inverse.

EXCLUDED as a tuning mechanism, kept as a null/confound: measurement/environment drift. HI132 already has interleaved unprofiled controls specifically to detect and flag this (environment_stable=false gate) rather than mistake it for a real effect.

METHOD, PER GPT'S REVIEW: reuse HI132 (bigcherry profile-campaign), which is FULLY IMPLEMENTED and already validated on real hardware (not merely 'pending' as its own plan-item state currently says -- see its own notes: full offline test suite green, a real dual-GPU rocprofv3 run succeeded 2026-08-27 with real kernel data, RCCL capture, and environment-drift detection all confirmed working). Do NOT build an independent profiler.

HOWEVER, gpt identified real gaps between HI132's current top-level `profile-campaign` orchestrator and HI171's exact setup that must be bridged: HI132 launches with runtime_profile.tune_context (not production context), uses its own fixed prompt/96-token completion (not HI171's server-bench pp512/pp2048/tg512 workload), builds its OWN lane rather than directly profiling HI171's already-proven native/replay arms, does not currently inject replay mode/cache into the profiled server, and sets only HIP_VISIBLE_DEVICES (not the newer canonical ROCR/HIP visibility pair, VA22's two-selector-trap fix HI171 itself had to work around earlier this session). CORRECT APPROACH: make HI172 a narrow extension/consumer of HI132's PRIMITIVES (ServerRunner.command_prefix, rocprofv3_command_prefix builder, capture validation, the raw CSV parser, interleaved controls) -- driving the EXACT HI171 native/replay binary+cache+environment+server-bench workload, not HI132's own top-level campaign shape.

GRANULARITY (gpt, corrects the original kernel-fraction-only proposal): family aggregation (kernel_fraction.py's own output) is sufficient ONLY for the dilution test. For causal diagnosis (hypotheses 2/3/4) it is insufficient -- need (a) exact-kernel-symbol statistics (calls/mean/p95/total duration, native vs replay, per exact kernel name -- HI132's own rocprof.py parser ALREADY aggregates per exact Kernel_Name including VGPR/SGPR/scratch, unlike kernel_fraction.py which collapses to family), and (b) raw dispatch rows/timestamps (Queue_Id, Stream_Id, Dispatch_Id, Correlation_Id, grid/workgroup) for overlap/queue/stream/critical-path analysis -- HI132's raw CSV has these columns but its current summary parser discards most of them; may need a thin additional aggregation step reading the raw per-kernel rows HI132's parser already retains.

IMPORTANT CAVEAT (gpt): do not assume a promoted candidate's stable name (e.g. 'mmvq:q8_0:w4:nw8:rpb1:sk0:v1') literally appears in rocprof's device-kernel symbol -- rocprof records the LAUNCHED device kernel, not necessarily the host wrapper name. Must build a VERIFIED promoted-candidate/config -> observed-kernel-symbol(s) mapping from each real trace, not assume a naming match. Also profile the COMPLETE candidate path (e.g. kernel_fraction deliberately includes MMQ activation quantization since the tuned matmul path includes it) -- comparing only one kernel could miss where cost actually moved.

PRIORITIZATION (gpt): profile GPU0's pp2048 workload first -- the largest, cleanest real regression (-1.81%), best signal-per-GPU-hour of anywhere in the whole matrix.

DISCRIMINATOR TABLE (gpt, final):
Exact candidate faster in replay + tiny wall share -> dilution
Exact candidate faster in replay + adequate share, but hidden behind independent gating work -> off-critical-path
Exact candidate SLOWER in replay in-context -> in-context regression
Exact candidate faster in replay but neighbors/system become slower -> externality
No stable repeatable trace/control difference -> unresolved/confounded

## Steps



## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files



## Validation



## Effort & Risk



## Standards



## Acceptance Criteria



## Notes

Session context (2026-09-08): raised directly by the owner after HI171's real e2e matrix found isolated tuner wins not translating to (and in GPU0's case, actively reversing into) real end-to-end performance across all five HI168 caches. Owner's explicit instruction: fold this hypothesis into a plan item, consult dev-gpt-agent on it, then run the trace.

Session context (2026-09-08): raised directly by the owner after HI171's real e2e matrix found isolated tuner wins not translating to (and in GPU0's case, actively reversing into) real end-to-end performance across all five HI168 caches. Owner's explicit instruction: fold this hypothesis into a plan item, consult dev-gpt-agent on it, then run the trace.

dev-gpt-agent review complete (req_714d201cc4b64f87) -- taxonomy corrected/broadened (see description), method confirmed as extend-HI132-don't-rebuild, granularity requirement raised from family-level to exact-kernel-symbol-level, GPU0 pp2048 identified as the priority target. NEXT: build the narrow HI132-primitive-based trace script for GPU0 native vs replay under pp2048, using the ALREADY-BUILT production binaries (b86b1e97d91605ce764e48f59342a962 native, GPU0's replay build+cache from the hi171-9b-gpu0-reexport-20260908 campaign) rather than HI132's own lane-building path.

Session context (2026-09-08): raised directly by the owner after HI171's real e2e matrix found isolated tuner wins not translating to (and in GPU0's case, actively reversing into) real end-to-end performance across all five HI168 caches. Owner's explicit instruction: fold this hypothesis into a plan item, consult dev-gpt-agent on it, then run the trace.

dev-gpt-agent review complete (req_714d201cc4b64f87) -- taxonomy corrected/broadened, method confirmed as extend-HI132-don't-rebuild, granularity requirement raised from family-level to exact-kernel-symbol-level, GPU0 pp2048 identified as the priority target.

FIRST REAL TRACE RUN 2026-09-08 (single-shot, GPU0, ~3521-token prompt via the production-safe-single-shaped native/replay binaries and GPU0's real re-exported cache, wrapped with rocprof.rocprofv3_command_prefix and parsed via rocprof.parse_kernel_trace -- reused HI132's exact primitives per gpt's instruction, no new profiler built):

TOTAL GPU kernel time: native 1,389,920.6us vs replay 1,390,057.3us -- delta +0.0098%, essentially noise-level. Matmul-family kernels (mul_mat_q/mul_mat_vec_q/quantize_mmq_q8_1) sum to roughly 133k of the 1.39M total us for one arm, ~9-10% share -- a real, non-trivial fraction, not dilution-scale small.

Confirmed rocprof DOES record the exact tuned template instantiation in the kernel symbol (contrary to gpt's caveat that this needed verification, not assumption) -- e.g. native's untuned mul_mat_vec_q<(ggml_type)8, 1, false,false,false,0,0> vs replay's tuned mul_mat_vec_q<(ggml_type)8, 1, false,false,false,1,2> (differing rows_per_block/small_k template params), so a verified candidate<->kernel-symbol mapping is directly readable from the trace, no inference needed.

For the swapped kernel pairs (native's untuned variant vs replay's tuned variant at the SAME call count -- e.g. 168 vs 168, 48 vs 48, 24 vs 24 calls, confirming these are literally the same real dispatch sites just resolving to a different compiled kernel): total-time deltas are small and mixed-direction (one pair +1.8% slower in replay, others roughly flat within a few percent). No single dramatic villain kernel visible the way HI141 found one for the MTP defect -- this single trace does NOT reproduce a clear regression signal at the kernel level.

HONEST LIMITATION, explicitly not overclaimed: this is a SINGLE, UNREPLICATED trace (n=1) with a short 8-token generation, chosen to keep the diagnostic bounded rather than repeat the full statistical rigor of HI171's own 6-round e2e comparison. It is consistent with (a) the regression being a genuinely small, aggregate, hard-to-see-in-one-shot effect requiring HI132's proper multi-pass/interleaved-control methodology to resolve reliably, or (b) this particular single sample simply not having hit the regression (GPU0's real e2e regression was itself only ~1.3-1.8%, comparable in scale to normal single-shot kernel-timing noise -- a single trace cannot reliably distinguish a real ~1.5% aggregate effect from noise at this granularity).

NEXT (not done this pass, real further GPU time): repeat this same trace 3-6 times with interleaved unprofiled controls (HI132's own established pattern) to get a statistically meaningful native-vs-replay kernel-level comparison, rather than trusting a single sample either way. Given the scope already covered this session (5 full campaigns, 5 e2e matrices, this diagnostic trace), stopping here as a reasonable checkpoint -- the first real trace neither confirms nor rules out the hypothesis, and getting a decisive answer needs a properly repeated pass, which is real additional GPU-hours.

Session context (2026-09-08): raised by the owner after HI171's real e2e matrix; dev-gpt-agent review (req_714d201cc4b64f87) corrected the taxonomy and confirmed extending HI132's primitives rather than building a new profiler. First single-shot GPU0 trace was inconclusive (n=1).

BUILT THE REPEATABLE VERSION 2026-09-08: tools/lab/hi172-kernel-trace/ (run.py + README + config-27b-dual-xtx.json), committed to the repo per this project's lab-experiment convention. 5 profiled passes per arm (native/replay) with an unprofiled control BEFORE AND AFTER every profiled pair, matching HI132's own >5% environment-drift-detection discipline. Reuses HI132's exact primitives (rocprof.rocprofv3_command_prefix, rocprof.parse_kernel_trace, ServerRunner.command_prefix) -- no new profiler, per dev-gpt-agent's explicit instruction.

RAN ON 27B DUAL-XTX 2026-09-08 (the most common real deployment topology, per owner's explicit request to prioritize it) -- real production native/replay binaries + the current-source-fresh 27B cache from hi171-27b-dual-reexport-20260908, both already validated in HI171's own e2e matrix.

environment_stable flag reported False, but investigated and explained: driven by a SINGLE transient outlier in native's 10 control samples (2871.1ms vs a tight 2617-2635ms cluster for the other 9) -- replay's own controls were extremely tight (0.21% spread). Not systemic drift; a one-off spike. Excluding it, environment was genuinely stable -- treating this as a usable, trustworthy 5-pass comparison, same judgment HI132 itself applies (data usable diagnostically; the automated flag is conservative by design).

RESULT, aggregated across all 5 passes (calls x pooled-mean per exact kernel symbol): TOTAL kernel time across the whole request is 0.72% LOWER for replay than native (26,242,275.8us vs 26,054,436.7us) -- a real, small, NET POSITIVE effect, not a regression. Every shared kernel's per-call mean time is flat-to-faster in replay: mul_mat_q<8,128,false> -0.6%, mul_mat_q<8,128,true> -9.1%, mul_mat_vec_q<8,1,...,0,0> -6.5%, mul_mat_vec_q<8,2,...,0,0> -37.0%, quantize_mmq_q8_1 -3.1%, two others near-flat (+0.1%, -0.3%). No regression found anywhere in the matmul family for this topology.

This is CONSISTENT with (not contradicting) step 3's e2e finding of 'clean parity' for 27B dual-XTX -- here the kernel-level total comes out slightly positive rather than exactly flat, still small and undramatic, but genuinely different in direction from GPU0's real regression. Confirms, again, that results are topology-specific and no single verdict applies project-wide.

STRUCTURAL FINDING (unexpected, worth noting): replay does not simply swap one native kernel 1:1 for one tuned kernel of the same shape. It REDIRECTS a large share of real dispatch VOLUME away from native's default-width kernels into 9 additional tuned-specific kernel variants that do not exist in native's own kernel set at all (call counts in the shared kernels drop substantially in replay -- e.g. one shared kernel: native calls=15528 vs replay calls=10752; another: native calls=9920 vs replay calls=1280 -- with that missing volume appearing in the 9 replay-only kernels instead, all present in 3-5 of 5 passes, real and repeatable, not transient). This means the promoted candidate set for 27B genuinely changes WHICH kernel handles a given real dispatch shape more broadly than a simple same-shape substitution, which the isolated per-signature tuner view doesn't surface as clearly as this aggregated real-trace view does.

CONCLUSION FOR 27B DUAL-XTX SPECIFICALLY: no evidence of the dilution/off-critical-path/in-context-regression/externality failure modes that would explain a NEGATIVE result, because there IS no negative result here to explain -- the kernel-level data is coherent with a small real win, matching the e2e finding. HI172's taxonomy remains needed for GPU0 (which DOES have a real regression to explain) but does not apply to 27B dual-XTX, which shows no divergence between isolated and real-world direction (both mildly positive/neutral), only a magnitude gap (isolated candidate claims up to 23%, real aggregate kernel-level effect ~0.7%) -- consistent with plain DILUTION for this topology specifically (many kernels, the tuned ones are a fraction of the total, so even a a real per-kernel win nets out small in aggregate), not a sign-flipping mechanism.

Artifacts: /home/audumla/hi172-artifacts/27b-dual-xtx-20260908/ on Brutus (summary.json + full rocprofv3 CSVs per pass, not committed to the repo -- generated diagnostic data per tools/lab/'s own convention, artifacts/lab/ pattern).

GPU0 REPEATABLE TRACE RESULT 2026-09-09 (config-gpu0.json, same 5-pass interleaved-control methodology as 27B, real production GPU0 native/replay binaries + hi171-9b-gpu0-reexport-20260908 cache): environment_stable=TRUE this time (native control spread 4.2%, replay 1.1%, both under the 5% threshold -- clean data, no drift issue like the 27B run had).

RESULT: total kernel time across the whole request is +1.168% HIGHER for replay than native (7,035,636.0us vs 6,954,420.6us) -- a REAL, small, controlled REGRESSION, directionally consistent with HI171's own e2e finding (pp512 -1.30%, pp2048 -1.81% replay vs native). This is the first properly-controlled (not single-shot) confirmation of GPU0's regression at the kernel level -- resolves the earlier single-shot trace's inconclusive n=1 limitation.

BREAKDOWN: three matched candidate swaps (native untuned kernel vs replay's tuned replacement, same call count each -- 840/240/120 calls): w1-family +2.05% slower in replay, w2-family +0.49% slower, w4-family -3.66% faster -- individually small and mixed, roughly a wash (+1.03% net across the matched set alone).

THE REAL STORY: a FOURTH kernel appears ONLY in replay with NO native counterpart at all -- mul_mat_vec_q<(ggml_type)14, 4, false,true,false,4,4>, 160 calls, present in ALL 5 passes (real and repeatable, not a fluke), adding 6,831.4us of genuinely NEW GPU work that native never performs at all. (ggml_type=14 is a different quantization format than the type=8 seen in the other three swaps -- likely Q6_K, this being a Q6_K model -- so this is a distinct dispatch signature/shape, not a duplicate of the other three.) This additive extra kernel, not present in native's execution path at all, is real overhead the isolated per-dispatch-key tuner view cannot see (each candidate is scored independently against its OWN native baseline in isolation; nothing in that view represents 'this promotion also adds a wholly new kernel invocation elsewhere in the real request').

TAXONOMY VERDICT FOR GPU0: closest fit is a mix of IN-CONTEXT REGRESSION (small, real, per-swap slowdowns for w1/w2, not reproduced in isolated tuner numbers which claimed all three were wins) and something not cleanly in the original 4-way taxonomy -- an ADDITIVE cost from a signature that isn't a 1:1 swap of an existing native dispatch at all, but a wholly new promoted candidate invocation. This is closer to SYSTEM/NEIGHBOR EXTERNALITY in spirit (extra real cost the isolated per-signature view doesn't attribute to the swap being evaluated) but is more precisely its own category: PROMOTED-CANDIDATE ADDITIVE OVERHEAD -- a promoted dispatch key that, in real serving, requires a kernel launch with no native equivalent at all (as opposed to a same-shape substitution), so its full real cost is pure addition, not a substitution delta, and the isolated tuner's own per-signature comparison (candidate vs THAT signature's own native) cannot represent an added dispatch that native's execution path never reaches in the first place.

CONCLUSION: GPU0's real, small (1-2%) regression is now confirmed with proper statistical controls, not just a directional match to noisy e2e numbers. Root cause is a combination of small genuine in-context slowdowns on 2 of 3 matched swaps AND one real additive kernel invocation with no native equivalent -- not a dramatic single villain, but real, measured, repeatable, and now explained at the mechanism level for the first time.

NEXT (not done this pass): (a) trace back which specific promoted signature the additive ggml_type=14 kernel belongs to and check its own isolated tune-time improvement_pct claim against this real evidence: (b) consider whether HI171's own execution-audit tooling should be extended to flag 'promoted candidates with no native-kernel-symbol equivalent in a real trace' as a distinct evidence category, since it represents a real cost class the current DIFFERENT_FASTER/SAME_NATIVE/FALLBACK vocabulary doesn't capture.

## Change Log

- 2026-09-08T17:26:42.486119+00:00 (created-by): Created by agent
- 2026-09-08T17:27:08.863449+00:00 (updated-by): Updated: section:description, section:notes

## Ledger-events


- chg_20260908_172719_wrote-up-a-formal-hypothesis-f_7881
- 2026-09-08T17:27:19.726020+00:00 (updated-by): Updated: section:ledger-events
- 2026-09-08T17:33:02.698629+00:00 (updated-by): Updated: section:description, section:notes
- chg_20260908_173310_got-expert-review-on-the-hypot_4565
- 2026-09-08T17:33:11.004976+00:00 (updated-by): Updated: section:ledger-events
- 2026-09-08T17:36:01.143967+00:00 (updated-by): Updated: section:notes
- chg_20260908_173608_ran-the-first-real-kernel-leve_3014
- 2026-09-08T17:36:08.068982+00:00 (updated-by): Updated: section:ledger-events
- 2026-09-08T17:50:03.669572+00:00 (updated-by): Updated: section:notes
- chg_20260908_175011_built-the-proper-repeatable-ve_3367
- 2026-09-08T17:50:11.977009+00:00 (updated-by): Updated: section:ledger-events
- 2026-09-08T21:01:53.021657+00:00 (updated-by): Updated: section:notes
- chg_20260908_210201_confirmed-with-proper-controll_8306
- 2026-09-08T21:02:01.806644+00:00 (updated-by): Updated: section:ledger-events
