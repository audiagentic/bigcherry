---
id: HI102
order: 0
plan: hip-autotune
state: in_progress
created-at: '2026-08-23T06:59:58.959563+00:00'
breadth: ''
skill: advanced
created-by: agent
work: M
---

# Production patch admission gate for validated-state evidence (HI83 admission phase)

## Description

Split out of HI83 (2026-08-23, plan-hygiene, audit §5.7 Option A). HI83 has accumulated several generations of design; the EVIDENCE-CONTRACT + verifier phase is shipped and hardware-verified (now closed as HI83). This item is the remaining POLICY-OPEN phase: the production admission gate for validated-state evidence.

Already shipped (in HI83, do NOT redone here): tracked validation-evidence format; state-normalized subject digest; validation_architectures metadata field; evidence status/verification helpers (validation_evidence_statuses()/require_validation_evidence() in patch_catalog.py); legacy grandfather baseline (docs/reference/patch-validation-evidence/legacy-baseline.json, 15 patches); patch-verify-evidence CLI; campaign record production; RD08 activation marker; RD08 exact-output-digest correctness machinery; same-base isolated subject/control source construction; real hardware validation.

NOTE: patch_admission.py does NOT exist yet -- that layer is the core deliverable of this item.

## Steps

- Decide the authoritative validation_architectures obligation (gfx1100-only vs RDNA3+RDNA4).
- Wire live resolved_base_revision checking into the real pinned_ref resolution path (mechanism exists in patch_catalog.py, not wired).
- Formalize the composition-vs-admission distinction into a small patch_admission.py layer so patch_catalog.py stops being parser + cross-checker + backend-policy + evidence-verifier + admission-authority.
- Decide whether direct `bigcherry apply` hard-fails, warns, or allows an explicit --allow-stale-validation-evidence escape hatch (a SEPARATE decision from the production campaign gate).
- Define when a STATE=validated assertion becomes mechanically inadmissible.
- Only hard-gate the production campaign/build path AFTER one real non-grandfathered eligible_for_validated_state=true record passes.

## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files



## Validation

patch_admission.py exists and the production campaign/build path is gated on it; a real non-grandfathered eligible_for_validated_state=true record passes the gate; the direct-apply behavior (hard-fail / warn / escape-hatch) is decided and enforced consistently; the resolved_base_revision check is wired into the real pinned_ref resolution path.

## Effort & Risk



## Standards



## Acceptance Criteria



## Notes

Split from HI83 (2026-08-23, plan-hygiene, audit §5.7 Option A). HI83 now represents the shipped evidence-contract + verifier only. The shipped machinery (patch_validation_evidence.py, catalog extensions, legacy-baseline.json, patch-verify-evidence CLI, RD08 record) is the basis this policy builds on.

Infrastructure implemented (2026-08-26, luna-lite-agent, reviewed and merged): patch_admission.py exists as the post-selection admission-policy layer, wired into patch/catalog.py's resolve_for_context() at the real production seam. Policy decisions made: gfx1100-only default validation_architectures (RDNA3+RDNA4 expansion explicitly flagged as needing human confirmation, not decided here); live resolved_base_revision checking reuses source/identity.py's git_revision() (PA08/PA09's primitive); direct apply hard-fails on stale evidence by default with an allow_stale_validation_evidence escape hatch (CLI wiring for the escape hatch flag itself is a separate follow-up -- __main__.py/CLI files were out of this pass's scope).

NOT YET fully satisfying this item's own validation criterion: 'a real non-grandfathered eligible_for_validated_state=true record passes the gate.' Currently all 15 legacy evidence records are grandfather-only -- the gate is correctly bootstrap-aware (reports not-ready/non-blocking until a real qualifying record exists) so it does not regress existing patches, but the item should stay in_progress rather than completed until: (1) a real non-grandfathered validated-state record actually exists and passes, and (2) the CLI escape-hatch flag is wired into bigcherry apply.

Infrastructure implemented (2026-08-26, luna-lite-agent, reviewed and merged): patch_admission.py exists as the post-selection admission-policy layer, wired into patch/catalog.py's resolve_for_context() at the real production seam. Policy: gfx1100-only default validation_architectures (RDNA3+RDNA4 expansion explicitly flagged as needing human confirmation, not decided here); direct apply hard-fails on stale evidence by default with an allow_stale_validation_evidence escape hatch (CLI wiring for the escape hatch flag itself is a separate follow-up -- __main__.py/CLI files were out of this pass's scope).

CORRECTION (2026-08-26, reviewer-gpt-agent adversarial follow-up): the original notes overclaimed that live resolved_base_revision checking was wired -- it was not. resolve_for_context() never accepted or forwarded a live revision at all; verify_live_revision() was dead code from the production path's perspective, exercised only by its own direct unit test. Fixed for real: resolve_for_context() now accepts resolved_base_revision and forwards it to require_admission(); campaign/lane.py's resolve_lane() call site (the one place in this chain with real live-revision knowledge) now supplies materialized.resolved_revision. Verified with a real regression test (test_resolved_base_revision_is_forwarded_to_the_admission_gate) proving the value actually reaches require_admission(), not just that the parameter exists.

STILL NOT fully satisfying this item's own validation criterion: 'a real non-grandfathered eligible_for_validated_state=true record passes the gate.' Currently all 15 legacy evidence records are grandfather-only -- the gate is correctly bootstrap-aware (reports not-ready/non-blocking until a real qualifying record exists) so it does not regress existing patches, but the item should stay in_progress rather than completed until: (1) a real non-grandfathered validated-state record actually exists and passes, and (2) the CLI escape-hatch flag is wired into bigcherry apply.

## Change Log

- 2026-08-23T06:59:58.959563+00:00 (created-by): Created by agent

## Ledger-events

- chg_20260823_070037_resolved-the-four-this-item-i_5123
- 2026-08-23T07:00:37.484536+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-26T12:35:48.092634+00:00 (state-transition): State: pending → completed
- 2026-08-26T12:35:56.318019+00:00 (state-transition): State: completed → in_progress
- 2026-08-26T12:36:05.822242+00:00 (updated-by): Updated: section:notes
- chg_20260826_123617_a-production-admission-gate-fo_3714
- 2026-08-26T12:36:17.156151+00:00 (updated-by): Updated: section:ledger-events
- chg_20260826_200528_the-patch-admission-gates-liv_6881
- 2026-08-26T20:05:28.404481+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-26T20:05:39.827082+00:00 (updated-by): Updated: section:notes
