---
id: HI102
order: 0
plan: hip-autotune
state: pending
created-at: '2026-08-23T06:59:58.959563+00:00'
breadth: ''
skill: advanced
created-by: agent
work: M
---

# Production patch admission gate for validated-state evidence (HI83 admission phase)

## Description

Split out of HI83 (2026-08-23, plan-hygiene, audit §5.7 Option A). HI83 has accumulated several generations of design; the EVIDENCE-CONTRACT + verifier phase is shipped and hardware-verified (now closed as HI83). This item is the remaining POLICY-OPEN phase: the production admission gate for validated-state evidence.

Already shipped (in HI83, do NOT redone here): tracked validation-evidence format; state-normalized subject digest; validation_architectures metadata field; evidence status/verification helpers (validation_evidence_statuses()/require_validation_evidence() in patch_catalog.py); legacy grandfather baseline (docs/reference/patch-validation-evidence/legacy-baseline.json, 15 patches); patch-verify-evidence CLI; campaign record production; RD08 activation marker; RD08 exact-output-digest correctness machinery; same-base isolated subject/control source construction; real hardware validation.

NOTE: patch_admission.py does NOT exist yet -- that layer is the core deliverable of this item.

## Steps

- Decide the authoritative validation_architectures obligation (gfx1100-only vs RDNA3+RDNA4).
- Wire live resolved_base_revision checking into the real pinned_ref resolution path (mechanism exists in patch_catalog.py, not wired).
- Formalize the composition-vs-admission distinction into a small patch_admission.py layer so patch_catalog.py stops being parser + cross-checker + backend-policy + evidence-verifier + admission-authority.
- Decide whether direct `bigcherry apply` hard-fails, warns, or allows an explicit --allow-stale-validation-evidence escape hatch (a SEPARATE decision from the production campaign gate).
- Define when a STATE=validated assertion becomes mechanically inadmissible.
- Only hard-gate the production campaign/build path AFTER one real non-grandfathered eligible_for_validated_state=true record passes.

## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files



## Validation

patch_admission.py exists and the production campaign/build path is gated on it; a real non-grandfathered eligible_for_validated_state=true record passes the gate; the direct-apply behavior (hard-fail / warn / escape-hatch) is decided and enforced consistently; the resolved_base_revision check is wired into the real pinned_ref resolution path.

## Effort & Risk



## Standards



## Acceptance Criteria



## Notes

Split from HI83 (2026-08-23, plan-hygiene, audit §5.7 Option A). HI83 now represents the shipped evidence-contract + verifier only. The shipped machinery (patch_validation_evidence.py, catalog extensions, legacy-baseline.json, patch-verify-evidence CLI, RD08 record) is the basis this policy builds on.

## Change Log

- 2026-08-23T06:59:58.959563+00:00 (created-by): Created by agent

## Ledger-events

- chg_20260823_070037_resolved-the-four-this-item-i_5123
- 2026-08-23T07:00:37.484536+00:00 (updated-by): Updated: section:ledger-events
