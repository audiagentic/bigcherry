---
id: HI146
order: 0
plan: hip-autotune
state: pending
created-at: '2026-08-29T12:18:48.665764+00:00'
breadth: ''
skill: advanced
created-by: agent
work: M
---

# RCCL admission gate: implement level-1 comm-init topology admission

## Description

Implementation slice split from HI145 (design item). Replaces patch 1225's blunt raw-architecture-mismatch check at ggml_backend_cuda_comm_init_nccl() with a real topology-admission check: qualified topologies ({0,2},{1,2},{0,1,2} per HI142's real qualification+correctness evidence) pass through to ncclCommInitAll; any topology including physical device 3 (or any other unqualified topology) still hard-aborts with a clear message. Depends on HI145's design decision being settled first (TopologyIdentityV1 schema, admission-mechanism choice) -- do not start until that's done.

## Steps

1. Implement TopologyIdentityV1 construction at the real guard call site per HI145's settled design.
2. Implement the qualified-topology admission check (allowlist or manifest-driven, per HI145's decision) -- must fail closed: an unrecognized/unqualified topology is blocked, never silently admitted.
3. Update patch 1225's tests (or its replacement patch) to cover: a qualified topology passes through to ncclCommInitAll; an unqualified topology (including any device-3-inclusive set) still aborts with a clear message; a topology with no matching qualification record fails closed.
4. Run the full patch test suite -- must stay green (7/7 for this patch specifically at minimum, full suite must not regress).
5. This item's own hardware validation: confirm on real Brutus hardware that {0,2}/{1,2}/{0,1,2} now pass through this new gate (no abort) while a device-3-inclusive topology still aborts cleanly.

## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files



## Validation



## Effort & Risk



## Standards



## Acceptance Criteria



## Notes

Testable/completable independently: this item's own acceptance is 'the gate correctly discriminates qualified vs unqualified topologies, with tests and real hardware confirmation' -- it does NOT need level-2 (per-call candidate binding) or promotion to be done first. Do not start until HI145's design is settled (no open gpt disagreement).

## Change Log

- 2026-08-29T12:18:48.665764+00:00 (created-by): Created by agent
