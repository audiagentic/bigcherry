---
id: HI15
order: 0
plan: hip-autotune
state: in_progress
created-at: '2026-08-04T01:22:32.731893+00:00'
breadth: ''
skill: advanced
created-by: agent
work: L
---

# Production hardening — replay-full binary, cache export, and corruption handling

## Description

Phase 9 — production hardening. Export validated winners to compact immutable cache. Build replay-full binary with no SQLite/tuner. Implement candidate blacklist, bounded miss logs, metrics/coverage reporting, and replay-slim generator.

## Steps

- Export validated SQLite winners to compact immutable binary cache
- Build replay-full production binary — confirm no SQLite/tuner linked
- Implement corruption handling: incompatible/corrupted cache falls back safely
- Unknown signatures use native and record misses via `GGML_HIP_DISPATCH_MISS=native-record`
- Cache loaded from `GGML_HIP_DISPATCH_CACHE=path/to/replay.bin`
- Candidate blacklist mechanism for invalid/high-spill geometries
- Bounded miss logging in production
- Metrics and coverage reporting
- Replay-slim generator: compile only selected winners plus native fallbacks
- C++ hip-autotune-inspect (shared with HI16) — [IMPLEMENTED + BRUTUS-VALIDATED 2026-08-22, pin 0adcc3bb, production-replay config (REPLAY=ON, ROUTING=OFF) in ~/bc-build-replay: full CMake compile+link PASS; `--selftest` 10/10 cross-language digest vectors PASS (C++ one-shot AND streaming vs Python hashlib.blake2b, incl. the three real runtime person prefixes, block boundaries, short-person zero-padding); registry self-check 88 candidates 0 anomalies exit 0; real dispatch-27b-v5.cache (sha256 1ed60d4d…): 59/59 winner slots loaded through the production loader, all usable on this build, exit 0. Patch 1230 STATE stays 'untested' ONLY because the HI83 contract ties 'validated' to an HI82 campaign record that does not yet exist for this patch — the STATE comment in the module documents the evidence. The selftest also caught and fixed one real bug in its own streaming chunk-split (up to 2 bytes dropped for some lengths) — exactly the class of bug the check exists to catch. Remaining for full HI15: runtime corruption-fallback evidence point (separate scope).]

## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files



## Validation

- Offline replay source-partition auditor proves replay retains dispatch, transform, signature/hash, coverage, and strict replay-loader sources.
- Offline audit rejects tuner, journal/SMI, record, and SQLite link inputs from production replay.
- Full offline pytest suite passes; GPU build/runtime validation remains for a later bounded slice.

## Effort & Risk



## Standards

HIP_AUTOTUNE_STANDARDS sections 6.4 (runtime env vars), 9 (replay and production)

## Acceptance Criteria



## Notes

### Review update

**`GGML_HIP_EXPORT_METRICS` already exists upstream** (`ggml/CMakeLists.txt`, and it already sets `-Rpass-analysis=kernel-resource-usage --save-temps`). No patch needed — consume its output rather than adding the flag. (A4)

**The blacklist belongs before tuning, not in production hardening.** Plan 17.2 is explicit: compile full-max as an audit, inspect compiler resource output, and blacklist high-spill geometries *before* runtime tuning. A spilling MMVQ geometry that reaches the tuner wastes measurement time on a candidate that was never viable. Moved to a new item after HI09 (PACK_REVIEW B6); what remains here is the runtime blacklist consumption.

**Two tools the plans dropped** (PATCH_MAP patches 10 and 11, PACK_REVIEW B7):

- `hip-autotune-export` — SQLite winners to compact binary cache. Not optional: it is the only bridge from tune builds to production. May be Python, since it runs offline.
- `hip-autotune-inspect` — inspect a cache or registry. Should be **C++**, so it links the real registry and validates with the same loader production uses. A Python re-implementation could disagree with the loader, which is the one thing it exists to check.

**Restart-only cache reload is a deliberate non-goal** (plan 12.5, PACK_REVIEW B8). Captured HIP graphs hold the resolved candidate, so live reload requires graph and binding invalidation that has not been proven.

### 2026-08-20 PH01 completion scan — the deferred GPU slice has been realized

Re-verified each step against the current tree. The Validation line "GPU build/runtime validation remains for a later bounded slice" is now STALE: RE15 closed (e94ade7) with the full A–L acceptance sequence on real hardware, which includes stage F (replay-full generate), the balanced comparison, coverage writer (v2 five-way classification, RE27), tamper-rejection (J), legacy-absence (K), and the Windows + Brutus suites (L). HI59 additionally proved the exported cache loads in the production replay build per architecture (1 winner, 0 misses, correctness 1/1). Current per-step state:

- Export to immutable binary cache — done (`tools/bigcherry/replay_cache.py`, v4 wire format, fail-closed; chg_20260813_082118, HI56 work).
- Replay-full binary without SQLite/tuner — done (RE15 stage F on real hardware; `tools/bigcherry/replay_build_audit.py` source-partition audit + tests).
- Corruption / incompatible-cache fallback — done offline (`hip-autotune-replay.cpp` + `test_replay_cache_wire.py`); runtime corruption demo on hardware not separately evidenced (RE15's tamper-rejection stage J covers campaign artifacts, not the cache loader specifically).
- `GGML_HIP_DISPATCH_CACHE` / `GGML_HIP_DISPATCH_MISS` — done (`hip-autotune-replay.cpp:560,591`).
- Bounded miss logging — done (hip-autotune-replay.cpp:23+ cap; `GGML_HIP_DISPATCH_MISS_LOG`).
- Runtime blacklist consumption — present in `hip-autotune-dispatch.cu`.
- Replay-slim / patch selection — present (`autotune_catalog.py`, `campaign_workers.py`, `test_patch_selection.py`).
- C++ `hip-autotune-inspect` — **not found in tree**; the Notes assign it to HI16 as well. This is the main remaining gap.

Item stays in_progress pending the inspect tool (shared with HI16) and an explicit runtime corruption-fallback evidence point.

### 2026-08-21 GPT adjudication (req_09656b782a5d449a) — harden v5, not v4

Do not spend production-hardening effort polishing the v4 reader/exporter: HI30/31's route contract forces a replay v5 (uint16 transform_id, 0=none; v4 → rerun_required). HI15's final acceptance should cover **v5 export → load → corruption handling → deterministic rejection → provenance**. Historical v4 campaign artifacts may keep a Python/offline legacy reader, but production replay should not accept v4 for analytical convenience. Depends on the new Replay v5 foundation item (HI74).

### 2026-08-21 hip-autotune-inspect tool landed (HI15/HI16 shared gap)

The last missing tool is now implemented, pending real-HIP compile validation on Brutus (GPU2 free at time of writing):

- `src/ggml/src/ggml-cuda/hip-autotune-inspect.cpp` — host executable linking ggml-hip: registry self-check (stable-name uniqueness, exactly-one-native-wrapper-per-family via the registry API, source_class enum bounds, null entry-point detection) + cache inspection through the real loader (its own rejection classification, per-entry registered/fresh/stale-impl/match_kind state via new loader exports). Exit codes 0/1/2/3/4 make it composable as a gate. `--json` for the Python wrapper.
- `hip-autotune-replay.{h,cpp}` — two additive loader exports: `ggml_hip_replay_load_failure()` (the loader's own whole-cache rejection classification) and `ggml_hip_replay_foreach_winner()` (per-slot state from the loader's tables; Winner gained a `stored_name` copy because the file's string table does not outlive init — load-time cost only, the lookup hot path never reads it).
- `patches/1230_hip_autotune_inspect.py` + catalog entry — CMake target in ggml-hip/CMakeLists.txt, built when `GGML_HIP_AUTOTUNE OR GGML_HIP_DISPATCH_REPLAY`, binary at `<build>/bin/hip-autotune-inspect`. Dry-run apply: PASS against the real current CMakeLists.
- `tools/bigcherry/replay_inspect.py` + `replay-inspect` CLI subcommand — runs the binary, reconciles its report against the build manifest (descriptor-hash identity = catalog<->registry agreement, recomputed not trusted), human + JSON output. 17 new tests (scripted stand-in for the binary; the C++ contract is pinned by the real build).
- Full offline suite: 1462 passed.

Remaining: (1) real HIP compile + run on Brutus against dispatch-27b-v5.cache and a tampered variant — this doubles as the GPT-adjudicated 'v5 export → load → corruption handling → deterministic rejection' acceptance point and the 'runtime corruption-fallback evidence point' the PH01 scan flagged; (2) then flip patch 1230 to validated.

## Change Log

- 2026-08-04T01:22:32.731893+00:00 (created-by): Created by agent
- 2026-08-04T01:27:23.097965+00:00 (updated-by): Updated: skill='advanced'
- 2026-08-05T04:23:34.502309+00:00 (updated-by): Updated: section:notes

## Ledger-events


- chg_20260805_131114_measured-the-finished-system-e_8962
- 2026-08-05T13:11:14.745623+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-13T07:24:07.594582+00:00 (state-transition): State: pending → in_progress
- 2026-08-13T07:24:07.620278+00:00 (updated-by): Updated: section:validation
- chg_20260813_072413_added-fail-closed-offline-chec_7124
- 2026-08-13T07:24:13.144627+00:00 (updated-by): Updated: section:ledger-events
- chg_20260813_082118_replay-cache-exports-now-fail_4428
- 2026-08-13T08:21:18.608468+00:00 (updated-by): Updated: section:ledger-events
- chg_20260820_090432_closed-hi12-tuning-engine-af_5550
- 2026-08-20T09:04:32.667663+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-21T04:51:13.136535+00:00 (updated-by): Updated: section:notes
- 2026-08-21T12:43:20.765434+00:00 (updated-by): Updated: section:notes, section:steps
- chg_20260822_040544_new-hip-autotune-inspect-tool_5307
- 2026-08-22T04:05:44.189836+00:00 (updated-by): Updated: section:ledger-events
- chg_20260822_044637_the-replay-cache-inspector-is_3051
- 2026-08-22T04:46:37.885571+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-22T15:02:01.993173+00:00 (updated-by): Updated (no visible changes)
- chg_20260822_150211_ran-real-hardware-checks-on-th_9533
- 2026-08-22T15:02:11.946892+00:00 (updated-by): Updated: section:ledger-events
