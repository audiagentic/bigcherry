---
id: HI171
order: 0
plan: hip-autotune
state: pending
created-at: '2026-09-08T01:12:49.367049+00:00'
breadth: ''
skill: advanced
created-by: claude-sonnet-5
priority: P0
work: L
---

# Existing-cache execution audit is P0, before any further tuning campaign

## Description

Owner-directed correction to the tuning runbook (C:\Users\mgs\Downloads\BigCherry_HIP_Tuning_Improvement_and_Candidate_Completeness_Runbook.md) after reviewing HI168's production-shaped baseline (docs/evidence/hi168-baseline-20260906.md). The runbook's original sequencing put HI166 (ordered speculative trace equality) and candidate-completeness auditing first, then execution/accounting. That is right for NEW campaigns, but it skips a cheaper, higher-value question for the FIVE caches HI168 already produced: did the winners we already promoted even launch as intended, and if so, did they help? Answering that needs no new GPU tuning time, only joining artifacts already on disk.

HI168's concrete findings that motivate this: GPU0, GPU1 and gfx1030 replay generally failed to improve on BC native end-to-end (dominant metrics near parity or negative). The dual-XTX 27B and gfx1201 results remain exploratory -- every row across all five caches is performance_admitted=false. The gfx1201 prompt-processing deltas (+15-24%) are an EXECUTION-IDENTITY WARNING, not a performance claim: their magnitude is inconsistent with the XTX/gfx1030 results and must not be treated as real until physical-device evidence rules out CPU fallback or a topology mismatch.

Given that, widening the candidate search (more MMQ variants, BLAS completion, etc.) before knowing whether existing promoted keys even execute as tuned risks the same failure class HI166 exists to prevent: a larger search space finds more apparent winners for reasons unrelated to being faster. Poor replay results must not be read as 'we need more candidates' until execution is audited -- they may equally mean the winners we have never launch, or launch and lose.

## Steps

1. DONE: tools/bigcherry/tuning/execution_audit.py -- joins promoted.jsonl (native/winner identity, promotion_status, the tuner's own isolated improvement_pct) with the GGML_HIP_DISPATCH_HIT_LOG replay hit log (real launch evidence, now recorded only at the true final post-revalidation binding decision -- see 4a) into hip-tuning-execution-audit.jsonl.
2. DONE: classifications SAME_NATIVE, DIFFERENT_FASTER, DIFFERENT_SLOWER, FALLBACK, NOT_EXECUTED, matching the runbook's section 7/13 vocabulary exactly.
3. DONE: `bigcherry execution-audit --promoted <path> --hit-log <path> --e2e-verdicts <path> --output hip-tuning-execution-audit.jsonl` CLI command.
4. DONE: wired into tune-campaign's end-of-run advisories as a STOP-severity advisory (EXECUTION_AUDIT_MISSING) that fires whenever a campaign promoted winners with no execution-audit artifact alongside it.
4a. DONE (2026-09-08): self-review + a parallel session's concurrent fix (commit 7c0f74fa) closed test-bootstrap and stale-audit-file gaps. That same session's commit also fixed a real bug this review found independently via dev-gpt-agent consultation (req_7794101acc0d4b7f, session ses_5de61f4891da44dc): ggml_hip_replay_record_hit() was called at a PROVISIONAL binding point in hip-autotune-dispatch.cu, before the arch/can_execute recheck that can still downgrade to native, so a hit could be recorded against a winner that never actually launched. Fixed by moving the call to the true final-decision point in ggml_hip_dispatch_resolve() (guarded on mode==REPLAY), and extending the Hit record + JSONL with from_cache so a logged entry distinguishes a genuinely-launched tuned winner from a fallback-to-native (commit 1291bea7, ledger chg_20260908_050027). Known follow-up, not yet done: warm thread-local L1 cache hits return before this point and are not recorded, so a long-running server under-counts launches relative to true frequency -- every recorded entry is trustworthy, but coverage is incomplete for warm servers. Verified: syntax-checked both edited translation units (hip-autotune-dispatch.cu, hip-autotune-replay.cpp) against the real replay-diagnostic build flags (GGML_HIP_DISPATCH_REPLAY, GGML_HIP_REPLAY_DIAGNOSTICS, --offload-arch gfx1100/gfx1201/gfx1030) via clang++ -fsyntax-only on Brutus -- both clean. grep confirmed exactly one live call site for ggml_hip_replay_record_hit plus its definition.
4b. DONE (2026-09-08): checked manifest-hash compatibility across HI168's five caches before deciding how many diagnostic-companion builds are needed (dev-gpt-agent's explicit instruction: dedupe by manifest hash, not topology count). Read each cache's binary header directly via replay_cache.read_cache() on Brutus. Result: all five caches share one source_revision_digest (fc29c60a180c4fe75b71c91b84728dc1) -- same source tree, as expected. But manifest_hash splits into exactly two groups: GPU0/GPU1/GPU2(gfx1201)/GPU3(gfx1030) -- the four 9B campaigns -- share manifest_hash 442be8a2f901ef07ef59236d84146adc; the dual-XTX 27B campaign has its own manifest_hash 92ddd46fa1792421638e6cad027c2cfc (different model/workload, so a different generated candidate inventory, as expected). Confirmed against each campaign's tune-campaign-receipt.json: same replay.source_root/source_slice_id across all five, distinct replay.build_plan_id per topology (device-set-specific), consistent with one shared source producing per-topology builds. Conclusion: exactly TWO manifest-compatible diagnostic-companion builds are required, not five and not one -- one covering the four 9B/GPU0-3 caches, one covering the 27B dual-XTX cache.
5. TODO: build the two replay-diagnostic companion binaries identified in 4b (9B-manifest build, 27B-manifest build) from the current fixed source (commit 1291bea7 or later), each with GGML_HIP_DISPATCH_REPLAY_REVISION_MATCH left at its default (never bypassed) so the preflight below is meaningful.
5a. TODO: before trusting any capture, preflight each cache against its rebuilt binary -- require stale=false, rerun_required=0, incompatible=0, and nonzero replay.exact in the coverage JSON.
5b. TODO: capture real GGML_HIP_DISPATCH_HIT_LOG evidence per cache under the same production-shaped workload HI168 used (GGML_HIP_DISPATCH_MODE=replay, per-cache GGML_HIP_DISPATCH_CACHE/COVERAGE paths, GGML_HIP_DISPATCH_COUNTERS=1). Suggested order (dev-gpt-agent): gfx1201 short smoke first (resolves step 6's identity warning early), then GPU1, GPU3/gfx1030, GPU0, then dual-XTX 27B last.
5c. TODO: run execution-audit against each cache's promoted.jsonl with its real hit-log. Note the L1-hit-coverage gap from 4a when interpreting NOT_EXECUTED rows from a warm server.
6. TODO: for GPU2/gfx1201 specifically, resolve the execution-identity warning BEFORE trusting any of its numbers -- confirm physical-device targeting (no CPU fallback, no topology mismatch) using real attestation, not inference from the numbers alone (coverage JSON's total_executed>0, replay.exact>0 against gfx1201's cache, and HI160's final_tuned_launches>0 are the concrete checks; full BDF/UUID attestation is HI169/Codex's scope, don't duplicate it). Coordinate with HI169.
7. TODO: only after step 5/6, decide per-cache whether to keep, retune, or discard the cache -- through bigcherry-patch-lifecycle if any promotion/demotion decision follows, never automatically from the audit alone.

4b. DONE (2026-09-08): replay-diagnostic validation now sets GGML_HIP_DISPATCH_HIT_LOG to a fresh per-run sibling artifact, unsets ambient values, clears stale logs, and records bounded hit-log metadata; 60 workflow/audit/advisory tests pass. This closes the pipeline wiring gap that previously left all five HI168 runs without hit-log artifacts.

## Detailed Solution & Technical Design

Deliberately NOT attempted: per-key production GPU-time. Nothing in this project's current artifacts measures how long an individual promoted candidate runs inside the real serving graph -- that needs rocprofv3 kernel-fraction data joined per dispatch digest (runbook section 16), which is a separate, larger piece of work. execution_audit.py's DIFFERENT_FASTER/DIFFERENT_SLOWER split is therefore driven by an optional, CALLER-SUPPLIED --e2e-verdicts mapping (dispatch digest -> 'improved'|'regressed'), never invented from the isolated tuner measurement. Absent that input, a genuinely-different, genuinely-launched candidate defaults to DIFFERENT_FASTER with e2e_verdict='not_measured' recorded explicitly in the row -- an isolated win, not a production claim.

## Code Samples & Guidance



## Files



## Validation

14 unit tests in tools/tests/tuning/test_execution_audit.py, covering: native-only and rejected rows excluded from the audit entirely; no-hit-log => NOT_EXECUTED; hit confirms winner => DIFFERENT_FASTER by default; hit-log candidate mismatch (post-revalidation substitution) => FALLBACK; winner==native => SAME_NATIVE regardless of hits; a supplied 'regressed' e2e verdict overrides the isolated win to DIFFERENT_SLOWER; malformed e2e verdict values raise; missing hit-log PATH (not just empty) is not an error; summary unproven_fraction counts only NOT_EXECUTED+FALLBACK. All green. `bigcherry check` passes except the pre-existing, unrelated overlay.vendor_sync finding (another agent's in-flight C++ edits).

## Effort & Risk



## Standards



## Acceptance Criteria



## Notes

STOP CONDITION (runbook amendment, also enforced automatically): no large retuning campaign, and no candidate-set expansion, until every currently-promoted cache has an execution-audit artifact and its classification distribution has been reviewed. This is now enforced structurally, not just documented: tune-campaign emits the EXECUTION_AUDIT_MISSING stop advisory automatically whenever promoted_after_evidence > 0 and no hip-tuning-execution-audit.jsonl sits beside the receipt.

Runbook doc itself (outside the repo, in Downloads) was updated in place with: this audit's priority as P0 before further GPU tuning; the required per-promoted-key join (native candidate/config/kernel -> replay candidate/config/kernel -> launch counts -> fallback, GPU time deliberately flagged as NOT YET AVAILABLE from current artifacts rather than fabricated); the five classifications; the explicit statement that exact replay cache hits prove compatibility, not tuned execution; HI168's concrete GPU0/GPU1/gfx1030/27B findings; that candidate expansion is conditional on this audit and poor replay must not be assumed to mean missing candidates; hip-tuning-execution-audit.jsonl as a mandatory artifact; the stop condition; and the preserved gfx1201 execution-identity warning + performance_admitted=false status.

2026-09-08 current-source replay-diagnostic companion 7b81cf41 built after refreshing 0100/0700 evidence. Re-exported dispatch cache against companion manifest from accepted promoted.jsonl and tune.sqlite. Stale cache produced 57/57 native. Identity-matched dual-XTX server-bench proof produced 57 final-decision rows: 30 from_cache=true, 27 native/fallback; execution-audit classified 14 DIFFERENT_FASTER and 4 NOT_EXECUTED, with no exercised promoted key classified FALLBACK. Four promoted keys were not exercised by this server-bench workload and are not claimed executed. Artifacts are under artifacts/hi171-runs/27b-dual-current. Activation evidence only; production timing remains pending.

STOP CONDITION (runbook amendment, also enforced automatically): no large retuning campaign, and no candidate-set expansion, until every currently-promoted cache has an execution-audit artifact and its classification distribution has been reviewed. This is now enforced structurally, not just documented: tune-campaign emits the EXECUTION_AUDIT_MISSING stop advisory automatically whenever promoted_after_evidence > 0 and no hip-tuning-execution-audit.jsonl sits beside the receipt.

Runbook doc itself (outside the repo, in Downloads) was updated in place with: this audit's priority as P0 before further GPU tuning; the required per-promoted-key join (native candidate/config/kernel -> replay candidate/config/kernel -> launch counts -> fallback, GPU time deliberately flagged as NOT YET AVAILABLE from current artifacts rather than fabricated); the five classifications; the explicit statement that exact replay cache hits prove compatibility, not tuned execution; HI168's concrete GPU0/GPU1/gfx1030/27B findings; that candidate expansion is conditional on this audit and poor replay must not be assumed to mean missing candidates; hip-tuning-execution-audit.jsonl as a mandatory artifact; the stop condition; and the preserved gfx1201 execution-identity warning + performance_admitted=false status.

OWNER COMPLETION ORDER (2026-09-08, recorded verbatim -- this is now the authoritative sequencing for HI171+HI168+CO01 jointly, and is a MULTI-SESSION, real-GPU-hours plan; do not attempt to fake completion of any step without real hardware evidence):

1. Finish HI171 cache validation: re-export all four 9B caches against the current binary/manifest; preflight each (stale=false, rerun_required=0, incompatible=0); run the same server-bench workload per physical GPU (XTX GPU0, XTX GPU1, gfx1201, gfx1030); capture unique hit logs and execution audits; resolve the gfx1201 physical-device identity warning explicitly; require every exercised winner to be cache-backed, with no exercised fallback or unexplained NOT_EXECUTED.
2. Complete HI168's 9B matrix: same Q6_K model, prompt set, runner, batch settings, repetition policy; run each GPU serially for llama.cpp native/stock, BC native, validated BC replay; measure pp512/pp2048/tg512 separately; balanced ordering, enough repetitions for confidence intervals; diagnostic and production runs kept separate (diagnostics are activation evidence, not performance evidence).
3. Repeat the 27B dual-XTX run: after 9B is complete, run fresh current-source stock/BC-native/validated-replay, Q8_0 27B on both XTX cards, same balanced server-bench protocol -- fresh stock arm and complete provenance required before calling parity (the existing native/replay pair alone is only encouraging, not sufficient).
4. Add the MTP equivalence gate: compare native and replay using the production MTP profile; record request count, generated tokens, speculative acceptance, layer/device placement, work totals; do not admit MTP performance unless those are equivalent.
5. Exercise the complete build matrix (stock, BC native, control, record, tune, replay, replay-diagnostic) -- only stock/native/replay are ever used for performance claims; the rest are diagnostics/cache-generation evidence only.
6. Make the process reusable: extend the existing campaign runner (do not introduce another harness -- this is HI170's territory, coordinate rather than duplicate). Each run emits a machine-readable manifest: source commit + build-plan IDs, binary hash, model path+digest, cache/manifest hash, GPU UUID/BDF/ISA, environment selectors, workload+repetition config, diagnostics mode, result status+provenance. One preflight/admission command rejects mismatched binary/cache/model/device/workload combinations before execution.
7. Close CO01: run the FULL tools/tests discovery suite (not just targeted tests), fix any CO01-specific failures, record full-suite evidence, close the associated review, mark CO01 complete only after that evidence is committed. NOTE: CO01 is a separate plan item outside hip-autotune -- this step belongs there, not in HI171/HI168's own ledger, when actually executed.

Definition of done (owner's own words): all four 9B caches pass current-source identity and execution audits; gfx1201 has explicit physical-device attestation; 9B has complete stock/native/replay per-GPU results; 27B has fresh stock/native/replay dual-XTX results; MTP work equivalence is proven; all build roles have been exercised and documented; logs/manifests are sufficient for UI progress monitoring; ledger entries, plan updates, commits, and push are complete.

Owner's explicit guidance on scope discipline: do NOT do a broad dispatch or tuning refactor yet. If an identity-matched cache still produces all-fallback or all-unexecuted results after this procedure, THAT is the point to inspect dispatch-key generation and cache projection -- not before. The existing 27B result suggests the runtime path is functioning; the remaining risk is incomplete cache provenance and missing comparison arms, not an obvious new regression.

BLOCKED as of 2026-09-08T06:xx: the real Brutus build checkout (~/bigcherry) is several commits behind this session's pushed HEAD (at 52ab88be vs. our current tuning-code-rebase tip) AND has another concurrent agent's uncommitted work in it (patches/0840_hybrid_allreduce_dispatch, patches/1225_hi85_..., releases/*.json, a new tools/bigcherry/campaign/collective_benchmark.py) -- none of it touching the tuning/replay path, but a git pull/checkout there risks colliding with that in-progress work. This must be resolved (coordinate with that session, or use an isolated worktree) before step 1's re-export can safely run against current-source.

STOP CONDITION (runbook amendment, also enforced automatically): no large retuning campaign, and no candidate-set expansion, until every currently-promoted cache has an execution-audit artifact and its classification distribution has been reviewed. This is now enforced structurally, not just documented: tune-campaign emits the EXECUTION_AUDIT_MISSING stop advisory automatically whenever promoted_after_evidence > 0 and no hip-tuning-execution-audit.jsonl sits beside the receipt.

OWNER COMPLETION ORDER (2026-09-08, recorded verbatim -- this is now the authoritative sequencing for HI171+HI168+CO01 jointly, and is a MULTI-SESSION, real-GPU-hours plan; do not attempt to fake completion of any step without real hardware evidence):

1. Finish HI171 cache validation: re-export all four 9B caches against the current binary/manifest; preflight each (stale=false, rerun_required=0, incompatible=0); run the same server-bench workload per physical GPU (XTX GPU0, XTX GPU1, gfx1201, gfx1030); capture unique hit logs and execution audits; resolve the gfx1201 physical-device identity warning explicitly; require every exercised winner to be cache-backed, with no exercised fallback or unexplained NOT_EXECUTED.
2. Complete HI168's 9B matrix: same Q6_K model, prompt set, runner, batch settings, repetition policy; run each GPU serially for llama.cpp native/stock, BC native, validated BC replay; measure pp512/pp2048/tg512 separately; balanced ordering, enough repetitions for confidence intervals; diagnostic and production runs kept separate (diagnostics are activation evidence, not performance evidence).
3. Repeat the 27B dual-XTX run: after 9B is complete, run fresh current-source stock/BC-native/validated-replay, Q8_0 27B on both XTX cards, same balanced server-bench protocol -- fresh stock arm and complete provenance required before calling parity.
4. Add the MTP equivalence gate: compare native and replay using the production MTP profile; record request count, generated tokens, speculative acceptance, layer/device placement, work totals; do not admit MTP performance unless those are equivalent.
5. Exercise the complete build matrix (stock, BC native, control, record, tune, replay, replay-diagnostic) -- only stock/native/replay are ever used for performance claims.
6. Make the process reusable: extend the existing campaign runner (HI170's territory -- coordinate, do not duplicate). Each run emits a machine-readable manifest (source commit + build-plan IDs, binary hash, model path+digest, cache/manifest hash, GPU UUID/BDF/ISA, environment selectors, workload+repetition config, diagnostics mode, result status+provenance). One preflight/admission command rejects mismatched combinations before execution.
7. Close CO01 (separate plan item, outside hip-autotune): run the FULL tools/tests discovery suite, fix CO01-specific failures, record full-suite evidence, close the review, mark complete only after that evidence is committed.

Definition of done (owner's own words): all four 9B caches pass current-source identity and execution audits; gfx1201 has explicit physical-device attestation; 9B has complete stock/native/replay per-GPU results; 27B has fresh stock/native/replay dual-XTX results; MTP work equivalence is proven; all build roles have been exercised and documented; logs/manifests sufficient for UI progress monitoring; ledger/plan/commits/push complete.

Owner's explicit scope discipline: do NOT do a broad dispatch or tuning refactor yet. If an identity-matched cache still produces all-fallback/all-unexecuted after this procedure, THAT is the point to inspect dispatch-key generation/cache projection -- not before.

PROGRESS 2026-09-08: unblocked the checkout conflict by cloning a fresh, isolated tree at ~/bigcherry-hi171-tuning on Brutus (git clone --branch tuning-code-rebase from origin) instead of touching the shared, dirty ~/bigcherry checkout -- avoids colliding with another concurrent agent's uncommitted work there (patches/0840_..., patches/1225_..., releases/*.json, a new campaign/collective_benchmark.py, none touching the tuning/replay path, but a pull/checkout in that tree would still have been unsafe). Applied bigcherry patch-set cleanly (RESULT: PASS, patch_set_id=1394ec2a17bd2e0c3bdd2aa2ec532261) against upstream b10705.

Ran `generate --variant-set workload-max --arch gfx1100,gfx1201,gfx1030 --inventory <GPU0's real inventory.json>` against this current-source tree: manifest hash = d7e6c85392aabc6d2d91b381543a777d. THIS CONFIRMS dev-gpt-agent's 2026-09-08 warning: it does NOT match the four old 9B caches' manifest hash (442be8a2f901ef07ef59236d84146adc) -- my hip-autotune-dispatch.cu fix (1291bea7) changed candidate/build identity as expected. The four existing 9B dispatch.cache files are stale against current source and CANNOT be reused as-is; step 1's "re-export all four 9B caches against the current binary/manifest" is confirmed mandatory, not optional, and must start from a full record+tune+replay-export cycle on current source, not merely a rebuild pointed at the old caches. Same expected for the 27B-manifest group but not yet independently re-generated/confirmed. NEXT: build the actual replay-diagnostic binary from this manifest (not just generate), which needs a real GPU build (~minutes) -- that is the next real, boundable action in this tree, still short of a full record+tune+replay cycle (hours of GPU time), which is the true next multi-hour step per the owner's completion order.

STOP CONDITION (runbook amendment, also enforced automatically): no large retuning campaign, and no candidate-set expansion, until every currently-promoted cache has an execution-audit artifact and its classification distribution has been reviewed.

OWNER COMPLETION ORDER (2026-09-08, recorded verbatim, see prior notes entries for full text) -- step 1 (re-export all four 9B caches against current source) is IN PROGRESS.

PROGRESS 2026-09-08, real GPU campaign results (owner authorized "all four 9B caches, one after another"), all via a fresh isolated clone at ~/bigcherry-hi171-tuning on Brutus (git clone from origin, patches applied cleanly, RESULT: PASS):

- GPU0 (hi171-9b-gpu0-reexport-20260908): re-export SUCCEEDED. 4 promoted keys, execution-audit against the REAL hit-log (auto-captured by the concurrent session's workflow.py wiring, commit 23a6e2a5): 4/4 DIFFERENT_FASTER, 0% unproven fraction. coverage.json: total_executed==total_dispatched (5461/5461), replay.exact=20, stale=false, rerun_required=0, incompatible=0. First-ever real launch evidence for a promoted HI168 winner.
- GPU1 (hi171-9b-gpu1-reexport-20260908): re-export SUCCEEDED. 7 promoted keys, execution-audit: 7/7 DIFFERENT_FASTER, 0% unproven.
- GPU2/gfx1201 (hi171-9b-gpu2-reexport-20260908): re-export SUCCEEDED. 9 promoted keys, execution-audit: 9/9 DIFFERENT_FASTER, 0% unproven. Execution-identity signals checked: total_executed==total_dispatched (5461/5461, rules out CPU-only fallback), replay.exact=20 matches this fresh cache, stale=false/rerun_required=0/incompatible=0. This satisfies the coverage-based checks HI171 owns; full physical HIP-ordinal->BDF/UUID attestation remains HI169's explicit separate scope, not fabricated here. The gfx1201 execution-identity WARNING (anomalous prompt-processing deltas from HI168's baseline) is NOT YET independently resolved -- that requires the actual pp512/pp2048 remeasurement in step 2 of the owner's completion order, not just this execution-audit pass; do not treat this GPU2 result as having cleared the warning, only as having confirmed real GPU launch (not CPU fallback) for its promoted winners.
- GPU3/gfx1030 (hi171-9b-gpu3-reexport-20260908): re-export FAILED at the behavioral gate, before any promotion/execution-audit was possible. Error: "behavioral gate failed and recovery search could not produce a publishable assignment: failed to capture native traces for recovery: vector 'single-card-dense-128-v1': 'timings' is missing draft_n/draft_n_accepted -- MTP did not engage or the response schema changed; refusing to silently treat this as draft_n=0 (fail-open was a real gap in an earlier version)". Gate report: both selected vectors (single-card-dense-128-v1, single-card-dense-512-v1) hard_fail=true, native_draft=[0,0], candidate_draft=[0,0] on both.

  ROOT-CAUSE INVESTIGATION (done, not yet resolved): compared against the ORIGINAL hi168-9b-gpu3-20260907 campaign's own behavioral-gate.json -- it hit the IDENTICAL condition (native_draft=[0,0], candidate_draft=[0,0] for the same vector) but recorded hard_fail=false and promoted anyway. The behavioral gate's own pass/fail logic has since been hardened by a concurrent session (this exact error message describes it: "fail-open was a real gap in an earlier version") to correctly distinguish "MTP genuinely produced 0 draft tokens" from "the timings response is MISSING the draft_n/draft_n_accepted fields entirely" (a broken/absent telemetry signal, not a valid zero). The OLD gate silently conflated these and let GPU3's original cache get promoted on invalid gate evidence. GPU0/GPU1/GPU2 all passed the SAME hardened check cleanly on the SAME production-safe-single runtime profile against the SAME model just now -- this is NOT a new regression from my source changes, and NOT random flakiness. It reproduces deterministically and is specific to GPU3/gfx1030 (RX 6900 XT, the oldest/only RDNA2 card in the fleet).

  WORKING HYPOTHESIS (not yet confirmed against source): gfx1030/RDNA2 may not support or report MTP speculative-decode telemetry the same way RDNA3/RDNA3.5/gfx1201 do -- i.e. this may be a genuine hardware/architecture limitation being correctly surfaced for the first time, not a bug to fix. Needs verification: check llama-server's MTP-eligibility logic for an explicit RDNA2/gfx1030 exclusion, or run a standalone completion against GPU3 with MTP enabled and inspect the raw /completion timings response to see whether draft_n/draft_n_accepted keys are present at all for this architecture.

  IMPLICATION FOR HI168's ORIGINAL GPU3/gfx1030 RESULT: its promotion evidence was gated by the buggy fail-open logic and should be treated as UNVERIFIED for MTP-path correctness, not merely stale -- this is a stronger finding than the manifest-hash staleness already recorded, and should be weighed when this cache's disposition is eventually decided via bigcherry-patch-lifecycle. Do NOT silently re-run with a workaround (e.g. disabling MTP for this vector) to force a green result -- the owner's own guidance is explicit: "If an identity-matched cache still produces all-fallback or all-unexecuted results after this procedure, that is the point to inspect dispatch-key generation and cache projection" -- this is that point, for GPU3 specifically, and needs a real decision (fix MTP support detection, exclude gfx1030 from the MTP vector, or determine this profile is genuinely inapplicable to this card) before a fourth 9B cache can be produced.

NEXT: do not retry GPU3 blindly. Determine root cause first (see working hypothesis above), then either fix the gate/profile mismatch or make an explicit, recorded decision to exclude gfx1030 from MTP-vector behavioral gating. Steps 2-7 of the owner's completion order remain not started.

STOP CONDITION (runbook amendment, also enforced automatically): no large retuning campaign, and no candidate-set expansion, until every currently-promoted cache has an execution-audit artifact and its classification distribution has been reviewed.

OWNER COMPLETION ORDER (2026-09-08, recorded verbatim, see prior notes entries for full text) -- step 1 (re-export all four 9B caches against current source) is COMPLETE.

FINAL RESULT 2026-09-08, real GPU campaign (owner authorized "all four 9B caches, one after another"), all via a fresh isolated clone at ~/bigcherry-hi171-tuning on Brutus:

- GPU0 (hi171-9b-gpu0-reexport-20260908): 4 promoted keys, 4/4 DIFFERENT_FASTER, 0% unproven. coverage.json: total_executed==total_dispatched (5461/5461), replay.exact=20, stale=false, rerun_required=0, incompatible=0.
- GPU1 (hi171-9b-gpu1-reexport-20260908): 7 promoted keys, 7/7 DIFFERENT_FASTER, 0% unproven.
- GPU2/gfx1201 (hi171-9b-gpu2-reexport-20260908): 9 promoted keys, 9/9 DIFFERENT_FASTER, 0% unproven. total_executed==total_dispatched (5461/5461, rules out CPU-only fallback), replay.exact=20 matches. This satisfies the coverage-based execution-identity checks HI171 owns; the gfx1201 PERFORMANCE identity warning (anomalous pp512/pp2048 deltas from HI168's baseline) is NOT resolved by this alone -- still needs step 2's real remeasurement. Full physical HIP-ordinal->BDF/UUID attestation remains HI169's separate scope.
- GPU3/gfx1030: FIRST ATTEMPT (hi171-9b-gpu3-reexport-20260908) FAILED at the behavioral gate: 'timings' response missing draft_n/draft_n_accepted entirely (not just zero). CORRECTED FINDING after investigation and a retry (do not trust the initial 'reproduces deterministically' claim from the first pass -- it was WRONG, based on a single sample): compared behavioral-native.log and behavioral-candidate.log between GPU0 and GPU3's first attempt -- both show IDENTICAL 'model has unused tensor blk.32.nextn.*' ignoring warnings on BOTH native and candidate legs for BOTH GPUs, meaning MTP was NOT engaging on GPU0 either (draft_n=0 there, just present in the response rather than missing). This ruled out the RDNA2/gfx1030-architecture-limitation hypothesis -- MTP wasn't active on GPU0 (which passed) either, so architecture cannot be the differentiator. Retried GPU3 once more (hi171-9b-gpu3-reexport-retry-20260908): SUCCEEDED cleanly. 8 promoted keys, 8/8 DIFFERENT_FASTER, 0% unproven via execution-audit. CONCLUSION: the first attempt's missing draft_n/draft_n_accepted fields were a one-off transient capture/response glitch (e.g. a dropped or malformed HTTP response during the behavioral-gate probe), NOT a deterministic gfx1030 hardware/architecture limitation and NOT a regression from this session's source changes. The hardened fail-open fix (refusing to silently treat a missing field as draft_n=0) worked exactly as intended: it caught a real, if transient, capture defect instead of silently accepting corrupt evidence -- which is precisely why the ORIGINAL hi168-9b-gpu3-20260907 campaign's promotion (made under the old buggy fail-open gate, on the identical native_draft=[0,0]/candidate_draft=[0,0] condition) should still be treated as UNVERIFIED for MTP-path correctness pending its own resolution, even though current-source GPU3 now re-exports cleanly.

ALL FOUR 9B CACHES NOW HAVE REAL, CLEAN EXECUTION-AUDIT EVIDENCE: GPU0 4/4, GPU1 7/7, GPU2/gfx1201 9/9, GPU3/gfx1030 8/8, all DIFFERENT_FASTER, 0% unproven fraction across the board. Step 1 of the owner's completion order is DONE. gfx1201's PERFORMANCE execution-identity warning is the one item from step 1's spirit not yet resolved (needs step 2's real pp512/pp2048 remeasurement, not just this execution audit).

NEXT: step 2 (complete HI168's 9B matrix -- llama.cpp native/stock, BC native, validated BC replay per GPU, pp512/pp2048/tg512 with confidence intervals) has not started. Steps 3-7 also not started. This is still real, multi-hour, multi-session GPU work.

CRITICAL FINDING 2026-09-08, GPU0 real e2e (owner correction of my own incomplete analysis): ab-benchmark's exploratory_comparisons only reports vs-STOCK (native_vs_stock, replay_vs_stock), which masked the comparison that actually matters -- replay (tuned) vs native (untuned BC) -- since both showed small, statistically-insignificant effects vs stock individually. Owner caught this. Computed replay/native directly from the 6 paired rounds in run.json's raw runs[] (matched by pair number):

  pp512_tps:  geomean effect -1.30% (ratios: 1.0101, 0.9802, 0.9485, 0.9978, 0.9792, 1.0075 -- 4/6 rounds below 1.0)
  pp2048_tps: geomean effect -1.81% (ratios: 1.0036, 0.9024, 1.0010, 0.9978, 0.9971, 0.9940 -- 5/6 rounds below 1.0)
  tg512_tps:  geomean effect +0.22% (roughly flat, no clear direction)

THIS IS A REAL REGRESSION, not merely 'no improvement': GPU0's tuned replay cache measurably SLOWS DOWN prompt processing (pp512, pp2048) relative to untuned BC native, despite the isolated per-dispatch-key tuner claiming 5.68-12.60% speedups for the exact same 4 promoted winners (execution-audit already confirmed all 4 genuinely launch from cache, from_cache=true, DIFFERENT_FASTER). The isolated microbenchmark gain is not just failing to survive into end-to-end serving -- it inverts into a measurable loss on the two metrics that matter most for prompt-heavy workloads.

Sample size caution: only 6 paired rounds (n=6), no formal CI computed for this specific replay-vs-native contrast (the tool's own paired-bootstrap CI machinery was only run for the vs-stock contrasts). The direction is fairly consistent (4/6 and 5/6 rounds respectively below 1.0) but this should be treated as a strong exploratory signal, not a decision-grade verdict, until either a larger paired-round count or the tool's own CI machinery is applied to this exact contrast.

IMPLICATION: this is the reason HI171 exists. GPU0's current promoted 9B replay cache should NOT be treated as a production win on current evidence -- it is execution-clean (launches correctly) but performance-negative for prompt processing. This is a bigcherry-patch-lifecycle-scope question (demote/quarantine this cache's promoted keys, or treat as needing re-tuning against a corpus that better reflects real prompt-processing costs) -- NOT something to silently patch around in the tuner. Do not read this as 'more candidates needed' per the runbook's own stop condition; read it as 'the tuner's isolated measurement does not predict real pp512/pp2048 cost for these particular MMVQ/MMQ candidates on GPU0'.

MUST REPEAT this exact replay-vs-native computation for GPU1, GPU2/gfx1201, GPU3/gfx1030 (GPU1's e2e run is in progress as of this note) before drawing any topology-general conclusion. GPU2/gfx1201's isolated tuner claims are much larger (up to 35.08%), making its real replay-vs-native result the single most important number left to obtain in step 2.

[... existing GPU0/1/2/3 step-1 execution-audit results and owner completion order preserved above this entry, see prior notes ...]

CONFIG BUG FOUND AND FIXED 2026-09-08: hand-wrote GPU1/2/3's server-config JSON environment blocks with HIP_VISIBLE_DEVICES == ROCR_VISIBLE_DEVICES (e.g. both '1' for GPU1) -- this is exactly the VA22 'two-selector trap' HI170 just fixed elsewhere: ROCR_VISIBLE_DEVICES filters physical devices first, then HIP_VISIBLE_DEVICES indexes by POSITION into that already-filtered list. ROCR=1 correctly selects physical GPU1, but HIP=1 then tries to index position 1 into a list of size 1 -- out of range, silently produces 'failed to initialize ROCm: no ROCm-capable device is detected' and the server exits near-instantly, which surfaced as ab-benchmark dying after printing only 'round 1/6 position 1' with no visible traceback (took several failed background-launch attempts and one foreground unbuffered run reading server.log directly to find the real error). GPU0 happened to work because 0==0 regardless of interpretation. FIXED: HIP_VISIBLE_DEVICES=0 (always, since ROCR already filtered to exactly one device) with ROCR_VISIBLE_DEVICES=<physical index> for GPU1/2/3's configs.

GPU1 real e2e result (hi171-9b-gpu1-e2e-20260908, 6 paired rounds, replay vs native computed directly from raw runs[]):
  pp512_tps:  geomean effect +2.63% (ratios: 1.0682, 0.9925, 0.9576, 1.0585, 1.0952, 0.9931 -- noisy but net positive)
  pp2048_tps: geomean effect +0.04% (flat/noise)
  tg512_tps:  geomean effect -0.81% (ratios mostly <1.0, 5/6 rounds -- small but fairly consistent regression)

DIFFERENT PATTERN FROM GPU0: GPU0 regressed on pp512/pp2048 and was flat on tg512; GPU1 improves on pp512, is flat on pp2048, and regresses slightly on tg512. This is NOT a uniform topology-wide verdict -- each GPU's promoted cache needs its own lifecycle decision, not a blanket 'tuning is failing' or 'tuning works' conclusion. Reinforces that per-cache, per-metric evidence is required (exactly HI171's premise), not a single project-wide verdict.

NEXT: GPU2/gfx1201 e2e run launching now with the corrected visibility config -- this is the highest-value remaining result since its isolated tuner claims are the largest (up to 35.08%) and it carries the still-unresolved execution-identity warning from HI168's original baseline. GPU3/gfx1030 e2e after that.

RECONCILIATION 2026-09-08: another concurrent session pushed docs/evidence/2026-09-08-HI168-e2e/ (commit da5da684, README/MANIFEST/RESULT) reporting a full 9B(x4)+27B e2e matrix with numbers that initially looked like they conflicted with this session's GPU0 finding (their replay-vs-native at pp512/pp2048 reads near-parity, -0.38%/+0.10%, vs this session's measured -1.30%/-1.81% regression). Investigated by reading their raw run.json (found at ~/hi168-baseline-34aa876f/artifacts/hi168-runs/9b-gpu0-three-arm-001/run.json on Brutus): their replay arm binary is build_plan_id a8cbfbb8d40684fbf657b059c0d3d881 -- the SAME original, PRE-FIX build from before this session's hip-autotune-dispatch.cu fix (1291bea7) that made replay hit-log evidence trustworthy in the first place. Their stock/native build_plan_ids also differ from this session's current-source builds. CONCLUSION: their numbers are a reformatted writeup of the ORIGINAL/historical HI168 baseline data (same underlying measurement point in time as the pre-existing hi168-baseline-20260906.md doc), not an independent re-verification against current source. Not in conflict with this session's finding -- they are measuring different code. This session's GPU0/GPU1 e2e results (using current-source builds b86b1e97d91605ce764e48f59342a962/native and freshly re-exported replay caches from the hi171-9b-gpu*-reexport-20260908 campaigns) are the current-source-accurate measurements and should be treated as superseding, not merely alongside, the historical numbers for any current-source decision. Their doc remains valid as a record of the ORIGINAL baseline; it should not be read as having re-validated or contradicted current source.

Their doc's own useful contribution, independent of the version-mismatch: GPU2's STOCK arm result was flagged there as anomalously low relative to native/replay ("must be treated as a stock execution/build anomaly, not a tuning gain") -- this reframes part of the historical gfx1201 execution-identity warning as possibly a stock-build-side anomaly rather than (or in addition to) a BC-native/replay-side one. Worth checking against this session's own GPU2 e2e run (in progress) once it completes -- compare this session's stock arm numbers for consistency.

GPU2/gfx1201 REAL e2e RESULT 2026-09-08 (hi171-9b-gpu2-e2e-20260908, current source, corrected visibility config): the gfx1201 execution-identity anomaly from HI168's original baseline REPRODUCES on current source, with real evidence for the first time about its likely nature.

replay vs native (the tuning question): pp512 -1.10%, pp2048 +0.15%, tg512 -0.03% -- essentially flat. Tuning itself does NOT explain the large deltas; replay tracks native closely.

native vs stock (the BC-framework question): pp512 +10.25%, pp2048 +25.05%, tg512 +0.57% -- THIS reproduces the magnitude of HI168's original +15-24% execution-identity warning almost exactly, on fresh current-source builds. The anomaly is in native-vs-stock (the BC dispatch framework itself, independent of tuning), not replay-vs-native.

Raw pp2048_tps values checked directly (not just ratios) to rule out a single-outlier artifact: stock = [2277.8, 2257.7, 2150.4, 2250.9, 2243.6, 2243.1] (tight, ~2200-2280 range, 1-2% spread); native = [2809.8, 2807.3, 2792.9, 2794.6, 2792.4, 2786.5] (tight, ~2790-2840, similarly low spread). Both clusters are internally consistent across all 6 balanced-order rounds -- this does NOT look like a one-off measurement glitch or an intermittent CPU-fallback artifact (which would typically show much larger variance or occasional extreme outliers, not two tight, well-separated, reproducible clusters).

Combined with step 1's already-confirmed coverage.json evidence for this exact GPU2 cache (total_executed==total_dispatched=5461/5461, ruling out CPU-only execution; replay.exact=20 matching a fresh, non-stale cache), the evidence increasingly points toward a REAL, reproducible BC-framework architectural difference specific to gfx1201 -- not an execution-identity/CPU-fallback artifact. This is a MEANINGFUL SHIFT from treating this purely as a 'warning to distrust': it may be that BC's dispatch/native candidate selection genuinely outperforms stock llama.cpp's default kernel path on gfx1201 by ~10-25% for prompt processing, for reasons unrelated to the promoted tuning winners specifically (since replay barely differs from native).

HOWEVER: this is NOT yet a resolved conclusion. What remains missing, per the owner's own completion-order requirement, is physical HIP-ordinal->BDF/UUID attestation (HI169's explicit scope) proving the 'stock' and 'native/replay' server processes both ran on the SAME physical GPU2 device and not e.g. a topology/device-binding difference between the stock build (no BC dispatch/device-selection code) and BC builds (with BC's own device-selection logic) that could route to different physical hardware despite identical ROCR_VISIBLE_DEVICES/HIP_VISIBLE_DEVICES env vars. Do not treat this as fully resolved until that attestation exists. Recorded here as the strongest evidence yet gathered, not as a final verdict.

Cross-check against the concurrent session's docs/evidence/2026-09-08-HI168-e2e/ doc (old/pre-fix build, see prior reconciliation note): their doc flagged GPU2's STOCK numbers as the anomalous side ('anomalously low relative to native/replay... a stock execution/build anomaly'). This session's fresh data is CONSISTENT with that framing -- stock is the low outlier relative to a tight, well-separated native/replay cluster, in both the old and new builds independently. Two independent measurements (different builds, different points in time) agree on the SHAPE of the anomaly (stock low, BC high, large tg512 stays near parity) even though they disagree on absolute numbers -- this consistency across independent runs further supports 'real effect' over 'random glitch'.

GPU3/gfx1030 e2e run is the last one remaining for the 9B matrix -- launching next with the corrected visibility config.

STEP 2 (9B matrix) COMPLETE 2026-09-08. Consolidated real current-source e2e results, replay vs native (the tuning-payoff question), all 4 GPUs, computed directly from raw paired rounds (6 balanced-order rounds each):

| GPU | pp512 | pp2048 | tg512 | verdict |
|---|---|---|---|---|
| GPU0 (gfx1100) | -1.30% | -1.81% | +0.22% | REGRESSION on prompt processing |
| GPU1 (gfx1100) | +2.63% | +0.04% | -0.81% | MIXED -- real pp512 win, small tg512 loss |
| GPU2 (gfx1201) | -1.10% | +0.15% | -0.03% | FLAT -- tuning itself not the story here (see native-vs-stock anomaly below) |
| GPU3 (gfx1030) | -0.53% | -0.12% | +0.45% | FLAT/near-parity, no clear signal either way |

CONCLUSION: there is NO uniform verdict across the four 9B caches. Only GPU0 shows a clear, real regression. GPU1 shows a real but small win on one metric and a small loss on another. GPU2 and GPU3 are essentially flat for the tuning-specific comparison. This directly disproves any blanket claim that 'tuning is failing' or 'tuning works' project-wide -- each cache needs its own lifecycle decision (bigcherry-patch-lifecycle scope), based on its own metric-by-metric evidence, not a single verdict extrapolated from one GPU (as nearly happened when GPU0's regression was found first).

Separately, GPU2/gfx1201's native-vs-stock comparison reproduces HI168's original execution-identity anomaly almost exactly (+10.25% pp512, +25.05% pp2048) -- see the dedicated GPU2 note entry above for full analysis. That anomaly is orthogonal to the tuning verdict above (replay tracks native closely on GPU2), and remains not-fully-resolved pending HI169's physical attestation.

All four GPUs' isolated per-dispatch-key tuner claims (recorded earlier, execution_audit.py step 1): GPU0 mean 9.32% (range 5.68-12.60%), GPU1 mean 9.96% (3.87-13.93%), GPU2 mean 17.88% (7.98-35.08%), GPU3 mean 9.55% (4.23-22.35%) -- NONE of these isolated claims are corroborated by the real e2e replay-vs-native numbers above. This is the core finding HI171 was built to surface: isolated per-kernel microbenchmark speedup does not reliably predict real end-to-end serving performance for these promoted 9B candidates, on any of the four topologies tested.

NEXT per the owner's completion order: step 3 (27B dual-XTX rerun, fresh stock/native/replay -- NOT the historical/pre-fix data from the concurrent session's evidence doc), step 4 (MTP equivalence gate), steps 5-7 (build-matrix exercise, reusable manifest via HI170 coordination, CO01 closure). All still real, multi-hour GPU work not yet started this pass.

STEP 3 (27B dual-XTX rerun) COMPLETE 2026-09-08. Fresh current-source re-export (hi171-27b-dual-reexport-20260908): 18 promoted keys, execution-audit 17/18 DIFFERENT_FASTER, 1 NOT_EXECUTED (5.56% unproven -- a legitimate, honestly-reported gap, not a failure; that one promoted key simply wasn't exercised by this workload). Real e2e (27b-dual-e2e-20260908, current-source stock/native/replay, 6 balanced rounds, dual GPU0+1):

replay vs native: pp512 -0.02%, pp2048 +0.09%, tg512 +0.57% -- essentially flat, clean parity.
native vs stock: pp512 +0.41%, pp2048 -0.002%, tg512 -0.39% -- also flat.

NO regression, no anomaly, no dramatic win anywhere for 27B dual-XTX. This matches HI168's ORIGINAL baseline framing for this topology ('near parity... no dramatic win') and is now independently confirmed on fresh current-source builds with the fixed instrumentation. Contrast with the concurrent session's evidence doc for this topology (old/pre-fix build): their numbers also showed near-parity (native within ~0.1% of stock, replay within ~0.3% of native) -- this is a case where BOTH old and new builds agree closely, unlike GPU0's case. 27B dual-XTX is the one topology in this whole matrix with a genuinely clean, uncomplicated, reproduced-across-builds result.

FULL 4x9B + 1x27B MATRIX NOW COMPLETE (replay vs native, current source):
| topology | pp512 | pp2048 | tg512 | verdict |
|---|---|---|---|---|
| GPU0 9B | -1.30% | -1.81% | +0.22% | regression |
| GPU1 9B | +2.63% | +0.04% | -0.81% | mixed |
| GPU2/gfx1201 9B | -1.10% | +0.15% | -0.03% | flat (separate native-vs-stock anomaly, likely real, see dedicated note) |
| GPU3/gfx1030 9B | -0.53% | -0.12% | +0.45% | flat |
| 27B dual-XTX | -0.02% | +0.09% | +0.57% | flat, clean parity |

Steps 1-3 of the owner's completion order are DONE (all real GPU work, current source, fresh builds). Remaining: step 4 (MTP equivalence gate -- draft_n/draft_n_accepted/acceptance comparison between native and replay under the production MTP profile; note the transient MTP-telemetry-missing glitch found and resolved for GPU3 in step 1 is a relevant precedent to watch for here), step 5 (exercise stock/native/control/record/tune/replay/replay-diagnostic -- control/record/tune/replay-diagnostic roles not yet separately exercised this pass, though tune-campaign's own pipeline exercises record/tune/replay internally), step 6 (reusable manifest -- HI170's territory, coordinate not duplicate), step 7 (CO01 closure -- separate item, 16 real pre-existing test failures still unresolved as of last check).

STEP 4 (MTP equivalence gate) STARTED, CRITICAL CORRECTION to step 3's earlier 'clean parity' framing 2026-09-08: while investigating step 4, found that the SAME hi171-27b-dual-reexport-20260908 campaign's own behavioral-gate.json (produced automatically as part of the campaign's replay-validate/diagnostic-companion pipeline, comparing native-dispatch-mode vs replay-dispatch-mode on the SAME diagnostic binary via GGML_HIP_DISPATCH_MODE toggle, per workflow.py's _stage_replay_validate) reports hard_fail=true with a REAL, substantial, and consistent MTP speculative-decode acceptance-rate divergence across all three corpus vectors:

  hi141-mtp-4096-v1 (128 tok):     native 93.5% accept (107/100... i.e. 100/107) vs candidate 62.1% accept (90/145)
  hi141-mtp-4096-tg512-v1 (512 tok): native 98.3% accept (407/414) vs candidate 72.3% accept (379/524)
  hi141-mtp-4096-tg2048-v1 (2048 tok): native 99.6% accept (1636/1643) vs candidate 86.7% accept (1589/1832)

first_output_divergence=1 on all three (token output diverges almost immediately). This means the tuned 27B replay cache's promoted winners cause MEANINGFULLY MORE speculative-decode rejections than native -- a real behavioral/quality-relevant regression, not a throughput-only concern.

IMPORTANT NUANCE, not yet fully resolved: the campaign STILL PROMOTED 18 keys despite this hard_fail (confirmed: campaign log shows no failure message, receipt shows finished_at/promoted_after_evidence=18 normally -- contrast with GPU3's first attempt, which genuinely ABORTED the whole campaign on its own behavioral hard_fail). This raises a real open question about whether hard_fail actually gates promotion for this campaign/profile shape, or only for certain profiles/stages -- needs source investigation (tune-campaign's promotion-gating logic vs this specific 'diagnostic-companion' observation_role) before concluding whether this is a policy gap or working as designed (e.g. hard_fail may correctly gate the MAIN replay cache export but this specific behavioral-gate.json artifact may be from a LATER, separate diagnostic-companion re-check using the replay-diagnostic binary specifically, which is allowed to diverge from the production replay binary due to DIAGNOSTICS=ON instrumentation changing MTP timing -- this was my initial hypothesis, NOT YET CONFIRMED OR REFUTED).

RECONCILE WITH STEP 3's 'clean parity' FINDING: step 3's real e2e ab-benchmark (stock/native/replay THROUGHPUT only: pp512/pp2048/tg512, using the separate PRODUCTION native/replay binaries, not this diagnostic toggle) showed tg512 +0.57% -- roughly flat, NOT the large slowdown a 62-87% MTP accept rate (vs 93-99% native) would naively predict if throughput were driven proportionally by accept rate. This inconsistency is NOT YET EXPLAINED -- possible causes: (a) the ab-benchmark's server-bench workload/prompts differ from the behavioral-gate's own HI141/GP11 corpus vectors and don't exercise the same divergence, (b) the production replay binary used for step 3's e2e (build 5ffe0ebb35a044d0397aa986d53da12d, no diagnostics) may behave differently from the diagnostic-companion evaluation captured here, (c) MTP acceptance rate and raw tg tokens/sec are not as tightly coupled as assumed (a rejected draft still costs verification time but the net throughput effect may be smaller than the accept-rate delta suggests). Do NOT resolve this by assumption -- needs a dedicated real MTP equivalence run against the PRODUCTION (non-diagnostic) native and replay binaries specifically, using behavioral_gate.run_gate() directly (see tools/bigcherry/tuning/behavioral_gate.py's run_gate()/run_vector() -- these are already-existing, reusable, not tied to tune-campaign's own diagnostic-companion invocation) with the corpus_edition 'qwen38-production-v2' resolved via behavioral_corpus.py, per HI141/GP11's own established methodology.

STOP: do not treat 27B as production-ready pending this reconciliation, and revise the earlier 'clean parity, no dramatic win' framing to 'clean parity on measured THROUGHPUT metrics; UNRESOLVED and possibly serious divergence on MTP behavioral/acceptance equivalence, which is a materially different and arguably more important axis for a speculative-decode-enabled production profile.' This is exactly why the owner's completion order lists step 4 as a SEPARATE, required step from step 2/3's throughput comparison, not a redundant check -- this finding proves why.

STEP 4 RESOLUTION, PART 1 (context): read HI141's own plan item (completed) -- this MTP acceptance-divergence pattern is FULLY ROOT-CAUSED, historical, and NOT a new defect. Summary: mmvq:q8_0:w4:nw8:rpb1:sk0:v1 (dispatch cd3b5f5bd371babe46338f367530f68f) has a legitimate FP-reassociation numeric difference (nwarps=8 changes summation order vs native/nw1) that is within HI67's correctness contract (epsilon-level NMSE) but occasionally flips a close top-1/top-2 MTP argmax decision -- a real, root-caused, GPT-confirmed (A2, high confidence) defect CLASS, not a kernel bug. Its manifestation is CANDIDATE-MIX-DEPENDENT (a fresh campaign can re-promote this exact candidate and NOT reproduce the regression, confirmed in HI141's own investigation). HI143's real-generation behavioral gate is the adopted, validated, durable defense against this class -- it correctly caught this campaign's reproduction (hard_fail=true).

STEP 4 RESOLUTION, PART 2 (mechanism, resolves 'why did it still promote'): read workflow.py's _stage_replay_validate. hard_fail triggers HTR01's bounded recovery search (recovery_mod.run_recovery), which tries alternative candidates for the offending signature and re-validates against the full corpus; only if recovery EXHAUSTS its budget without a publishable assignment does the campaign abort (exactly what happened to GPU3's FIRST attempt in step 1). For this 27B campaign, recovery-result.json shows: published=true, stop_reason='full-corpus validation passed', final_overrides={'cd3b5f5bd371babe46338f367530f68f': 'mmvq:native:v1'} -- i.e. recovery correctly overrode the guilty dispatch to NATIVE (all 3 tuned alternatives tried and exhausted) and the recovered cache passed full-corpus behavioral validation. Code confirms provisional_cache.replace(final_cache_path) promotes the recovered cache to the final artifact.

STEP 4 RESOLUTION, PART 3 -- UNRESOLVED, REAL DISCREPANCY (does not fit the above cleanly, flagging rather than resolving further given scope): the hit-log/execution-audit evidence for this EXACT dispatch (cd3b5f5bd371babe46338f367530f68f) shows the GUILTY candidate genuinely launching from cache (actual_from_cache=true, actual_launched_candidate='mmvq:q8_0:w4:nw8:rpb1:sk0:v1', classified DIFFERENT_FASTER) -- directly contradicting recovery-result.json's claim that this exact dispatch was overridden to native in the final cache. promoted.jsonl also still shows this row as promotion_status='promoted', winner=the guilty candidate (promoted.jsonl appears to be a pre-recovery measurement record that is never rewritten post-recovery, which may be expected/by-design -- but the HIT-LOG capturing the guilty candidate as genuinely launched is NOT expected if the final cache truly contains the native override).

Possible explanations, NOT YET DISTINGUISHED: (a) the diagnostic-companion/replay_coverage capture stage (observation_role='diagnostic-companion' in the receipt) may independently re-export or re-use a DIFFERENT cache than the main campaign's post-recovery final_cache_path -- e.g. if its own hit-log capture launches against dispatch_cache_path taken from a point in the pipeline before the recovery replace() landed; (b) a real bug where the recovery override does not propagate into whatever artifact the diagnostic-companion/hit-log stage actually loads. This casts real doubt specifically on execution-audit's DIFFERENT_FASTER classification for THIS ONE dispatch (not the other 17 promoted keys, which have no recovery history) -- and by extension raises a question about whether HI171's step-1 hit-log-based execution-audit evidence is reliably reading the SAME final cache that ships to production for any campaign that went through HTR01 recovery.

SCOPE DECISION: this is a genuinely separate, substantial investigation (needs source-level tracing of exactly which cache path the diagnostic-companion stage's ServerRunner points at relative to the main campaign's recovered final_cache_path) -- explicitly NOT resolved further in this pass given the scope already covered this session. Flagging as the SINGLE highest-priority open question for whoever continues HI171/HI143 work: confirm whether recovery overrides survive into the artifact execution-audit's hit-log evidence is captured against, for ANY campaign that triggers HTR01 recovery (not just this one). If they do not survive, execution-audit's 'DIFFERENT_FASTER' claims for any recovered dispatch key are unreliable and need re-verification against the TRUE final production cache specifically (not the diagnostic-companion build's own re-derived one).

Does NOT invalidate step 2/3's real e2e ab-benchmark THROUGHPUT results (those used the actual final dispatch.cache file directly via GGML_HIP_DISPATCH_CACHE, the same path recovery wrote to) -- those measurements remain valid regardless of this hit-log-capture-path question. It specifically undermines confidence in the DIAGNOSTIC/hit-log-based execution-audit evidence (step 1) for any recovered signature, not the e2e throughput evidence (steps 2-3).

## Change Log

- 2026-09-08T01:12:49.367049+00:00 (created-by): Created by claude-sonnet-5

## Ledger-events


- chg_20260908_045332_make-tuned-launch-evidence-tru_6169
- 2026-09-08T04:53:32.952381+00:00 (updated-by): Updated: section:ledger-events
- 2026-09-08T04:56:09.843063+00:00 (updated-by): Updated: section:steps
- chg_20260908_045618_closed-real-gaps-in-the-execut_8438
- 2026-09-08T04:56:18.629201+00:00 (updated-by): Updated: section:ledger-events
- 2026-09-08T04:58:28.256758+00:00 (updated-by): Updated: section:steps
- chg_20260908_050027_make-replay-hit-logs-distingui_8127
- 2026-09-08T05:00:27.255893+00:00 (updated-by): Updated: section:ledger-events
- 2026-09-08T05:04:01.761131+00:00 (updated-by): Updated: section:steps
- chg_20260908_050408_verified-the-replay-hit-log-fi_2193
- 2026-09-08T05:04:09.005276+00:00 (updated-by): Updated: section:ledger-events
- chg_20260908_050508_confirmed-the-five-diagnostic_4179
- 2026-09-08T05:05:08.608703+00:00 (updated-by): Updated: section:ledger-events
- chg_20260908_050943_fix-the-missing-diagnostic-evi_7743
- 2026-09-08T05:09:43.603621+00:00 (updated-by): Updated: section:ledger-events
- 2026-09-08T05:10:11.433946+00:00 (updated-by): Updated: section:steps
- chg_20260908_051021_hi171s-diagnostic-pipeline-no_4282
- 2026-09-08T05:10:21.449300+00:00 (updated-by): Updated: section:ledger-events
- chg_20260908_052324_refreshed-current-source-frame_3261
- 2026-09-08T05:23:24.715642+00:00 (updated-by): Updated: section:ledger-events
- chg_20260908_052402_fixed-a-real-bug-in-the-tuning_9969
- 2026-09-08T05:24:02.131459+00:00 (updated-by): Updated: section:ledger-events
- chg_20260908_055842_added-a-consolidated-tuning-re_8283
- 2026-09-08T05:58:42.084400+00:00 (updated-by): Updated: section:ledger-events
- 2026-09-08T06:39:25.290135+00:00 (updated-by): Updated: section:notes
- chg_20260908_063946_proved-the-current-source-repl_3960
- 2026-09-08T06:39:46.334000+00:00 (updated-by): Updated: section:ledger-events
- chg_20260908_064919_fresh-current-source-productio_7217
- 2026-09-08T06:49:19.441464+00:00 (updated-by): Updated: section:ledger-events
- 2026-09-08T07:01:49.193617+00:00 (updated-by): Updated: section:notes
- 2026-09-08T07:03:57.453095+00:00 (updated-by): Updated: section:notes
- chg_20260908_070404_confirmed-the-four-existing-9b_8758
- 2026-09-08T07:04:04.393479+00:00 (updated-by): Updated: section:ledger-events
- 2026-09-08T09:24:02.176900+00:00 (updated-by): Updated: section:notes
- chg_20260908_092410_found-that-gpu3s-original-tun_3878
- 2026-09-08T09:24:10.124946+00:00 (updated-by): Updated: section:ledger-events
- 2026-09-08T11:37:00.062491+00:00 (updated-by): Updated: section:notes
- chg_20260908_113705_all-four-gpu-tuning-caches-for_4632
- 2026-09-08T11:37:06.008767+00:00 (updated-by): Updated: section:ledger-events
- 2026-09-08T12:26:32.050615+00:00 (updated-by): Updated: section:notes
- chg_20260908_122646_found-that-gpu0s-tuned-kernel_1178
- 2026-09-08T12:26:46.481200+00:00 (updated-by): Updated: section:ledger-events
- 2026-09-08T12:44:32.843341+00:00 (updated-by): Updated: section:notes
- chg_20260908_124708_found-and-fixed-a-gpu-selectio_6469
- 2026-09-08T12:47:08.647702+00:00 (updated-by): Updated: section:ledger-events
- 2026-09-08T12:49:01.627197+00:00 (updated-by): Updated: section:notes
- chg_20260908_124907_reconciled-two-different-looki_1665
- 2026-09-08T12:49:07.883482+00:00 (updated-by): Updated: section:ledger-events
- 2026-09-08T13:00:39.419574+00:00 (updated-by): Updated: section:notes
- chg_20260908_130046_the-unresolved-gfx1201-anomaly_5864
- 2026-09-08T13:00:46.798068+00:00 (updated-by): Updated: section:ledger-events
- 2026-09-08T13:18:11.273507+00:00 (updated-by): Updated: section:notes
- chg_20260908_131818_finished-real-end-to-end-perfo_5792
- 2026-09-08T13:18:18.423798+00:00 (updated-by): Updated: section:ledger-events
- 2026-09-08T14:14:09.932139+00:00 (updated-by): Updated: section:notes
- chg_20260908_141416_finished-the-full-end-to-end-p_7539
- 2026-09-08T14:14:16.184143+00:00 (updated-by): Updated: section:ledger-events
- 2026-09-08T16:33:32.273417+00:00 (updated-by): Updated: section:notes
- chg_20260908_163339_found-a-real-unresolved-discr_2316
- 2026-09-08T16:33:39.435052+00:00 (updated-by): Updated: section:ledger-events
- 2026-09-08T16:36:35.898082+00:00 (updated-by): Updated: section:notes
- chg_20260908_163644_traced-the-27b-tuning-caches_7565
- 2026-09-08T16:36:44.566683+00:00 (updated-by): Updated: section:ledger-events
