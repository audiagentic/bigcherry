---
id: HI154
order: 0
plan: hip-autotune
state: pending
created-at: '2026-08-29T14:12:18.451211+00:00'
breadth: ''
skill: intermediate
created-by: agent
work: M
---

# stew675-rdna-boosts sources-check findings: rebase + content drift review

## Description

First real run of `bigcherry sources check` in a long time (the locator bug fixed alongside this item silently broke it for 4 of 5 registered forks -- only stew675-rdna-boosts, whose locator was already a bare ref, ever actually ran). Run against the current b10680-pinned tree, 2026-08-30. 19 findings total.

**Branch state: REBASED.** The registry's active snapshot (v2, head 9e46e1fdc) is no longer an ancestor of the fork's current tip (c8af5361d) -- per config/external-sources.toml's own convention, this alone requires a new snapshot + re-audit before further planning against this source, independent of the individual findings below.

**Content-CHANGED findings (the actionable ones -- these mean the fork's current version of a tracked commit differs from what we based our work on):**
- cfc192734 (RD04, `native-BF16 tile kernel for all head sizes...`) -> re-committed as b6bfa422e with changed content. RD04 has MULTIPLE tracked commits (see registry); this is one of several, not the whole cluster.
- 8473c0da7 (RD18, `fuse IMRoPE + set-rows for BF16 KV cache`) -> d33ce1adf, changed. RD18 status is currently 'planned' (not yet ported) -- re-review the fork's NEW version before porting, not the stale one.
- 1818c3b37 (RD21, `gfx1151 mmvq table with nwarps=2 for Q8_0 decode`) -> 5b320ed94, changed. RD21 -> patches/1208_rd21_gfx1151_mmvq_nwarps_table IS ALREADY PORTED (status deferred-hardware) -- compare the fork's new version against what was ported; this is the highest-priority item to check since it's already in our patch set.
- **1d525bd45 (RD05/RD06/RD07, `RDNA4 WMMA flash-attn and Q6_K mmq prefill`) -> beaf69fb6, changed. This maps to patches/1203_rd050607_rdna4_wmma_fa_q6k_mmq, status ported-untested -- an ALREADY-APPLIED PRODUCTION PATCH whose upstream fork source has since changed. Highest-priority review: does our ported version still match the fork's intent, or did the fork fix/change something we should pull forward?**
- 0510d7cfa (RD27, `fold SSM conv_input concat into qkv mmvq`) -> 555e79ab2, changed. Status 'planned' (not yet ported).

**'Not found by title' findings (commit disappeared from the fork's history under its old title -- could mean: renamed, squashed, reverted, or genuinely dropped by the fork author; needs a manual look, NOT necessarily 'merged upstream' or 'redundant' without checking):**
- 3b200b259 (RD20) -> patches/1201_rd20_attn_gate_tp_split, ALREADY PORTED (ported-untested). Same priority tier as RD05/06/07 above -- an applied patch whose source commit vanished from the fork under its recorded title.
- fee8f9925 (RD30) -- registry lists RD30 against amd-ecosystem-llama-cpp (-> patches/1237), not stew675; this hit under stew675 needs the registry cross-referenced to see if it's a duplicate/mistaken tracked-entry or a genuinely separate stew675-side RD30 commit.
- 812a47a89 (RD97), 1b600a34d (RD98), 0c92c50b5 (RD98), and four untitled ('-') entries: d59eb5a1c, b8fed9fae, 42aff9e1f, 88ef64264 -- need identifying (RD97/RD98 aren't in the plan items reviewed so far this session; the four untitled ones may be registry entries missing a `plan-item` field, which is itself worth fixing regardless of the drift question).

**Not evidence of anything wrong on our side**: everything else (majority of the 19 hits) is 'drift: content identical' -- the fork's maintainer rebased/re-committed with new SHAs but no actual content change. No action needed on those beyond eventually refreshing the registry's tracked SHAs to the new ones (cosmetic, not urgent).

## Steps

1. Prioritize the two ALREADY-PORTED-patch findings first: RD05/06/07 (1203, content-changed) and RD20 (1201, commit vanished under its title) -- both are live in the build, so any real upstream fix/change is the most consequential thing to miss.
2. For each content-changed finding, diff the fork's new commit against the OLD (tracked) commit to see what actually changed, then diff that against our ported patch to see if the divergence matters.
3. For RD20's 'not found', check the fork's current history directly (git log --all --grep, or browse the fork on GitHub) to determine what happened to that commit -- do not assume 'merged upstream' without checking; the finding only means 'not found by title', not 'landed in mainline'.
4. Resolve the RD30 registry cross-reference question (is fee8f9925 a mistaken duplicate tracked-entry under the wrong source, or real).
5. Identify and fix the 4 untitled tracked entries (add a plan-item field if missing) plus RD97/RD98.
6. Refresh the registry: record a new snapshot (v3) for stew675-rdna-boosts's rebased tip, superseding v2, per the registry's own documented convention (see stew675 v1->v2 history already in the file as the precedent).
7. Re-run `bigcherry sources check` after the snapshot refresh to confirm it reports clean.

Note: mrlordcat-rdna-lab and amd-ecosystem-llama-cpp still need their own sources-check run -- the locator fix (this session) makes them clonable now, but `sources check` didn't finish for them yet during this pass (mrlordcat's clone is apparently very slow -- 17500+ commit personal research log per its own registry notes -- and the run was cut off by a wrapper timeout, not a real failure). Re-run with a longer timeout as a separate pass; davetha and joursbleu are single-PR forks already resolved/closed per their notes and lower priority.

## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files



## Validation



## Effort & Risk



## Standards



## Acceptance Criteria



## Notes

### gpt assessment (req_e1eda0d23e914a76) -- both core factual claims independently verified true

**Case 1: patches/1203_rd050607_rdna4_wmma_fa_q6k_mmq (RD05/06/07) -- LOW RISK, provenance-only issue.**
gpt's read: our patch ALREADY contains the fork's new content (the re-squashed/re-tuned beaf69fb6 version) -- the config-table constants, DKQ>576, GGML_CUDA_FA_WMMA_256, the logit_softcap==0||head-in-{128,256,512} guard, and both tile-Q sync fixes (including the np==1 correctness barrier) are all already present in our patch.py. Verified directly by grep against the real file: all cited strings/constants genuinely exist there (lines 121, 148, 159, 186-193, 478, 496, 529). Conclusion: NOT a missing-correctness-fix emergency -- the implementation already matches current fork content; what's stale is only the recorded fork-commit/snapshot identity (1d525bd45/v2), not the code. Recommended action: bounded provenance reconciliation only -- update the tracked commit/snapshot to beaf69fb6 in external-sources.toml + the patch's own docstring, after confirming all six touched-file hunks match modulo documented BigCherry adaptations. Do NOT retune/rewrite 1203.

**Case 2: patches/1201_rd20_attn_gate_tp_split (RD20) -- likely upstream-superseded, not reverted/lost.**
gpt's read: the fork commit's disappearance is best explained by upstream (ggml-org/llama.cpp mainline) having independently added its OWN, more precise handling of attn_gate.weight's split granularity since our patch was written -- specifically a conditional (`if (tensor->ne[1] == hparams.n_head(il)) return {granularity_head}; return {granularity_q};`) that's more nuanced than RD20's blunt always-match-attn_q fix. Verified directly by reading vendor/llama.cpp/src/llama-model.cpp:743-747 at the current b10680 pin -- gpt's quoted code block is EXACTLY what's there, including the surrounding pattern_attn_gate_weight regex context (also confirmed real at lines 390, 532, 620, 674, 743). This is real, strong evidence -- NOT yet proof of full supersession, since I have not diffed the exact SEMANTIC equivalence or run the reproducer.

gpt's recommended verification steps (NOT yet done -- this is the concrete remaining work, not a vague 'investigate more'):
1. `git log -S'pattern_attn_gate_weight' b10502..b10680 -- src/llama-model.cpp` (find the introducing commit, confirm it's genuinely new since b10502, not something RD20 already accounted for).
2. `git show <introducing-commit> -- src/llama-model.cpp` (read the actual upstream commit message/rationale).
3. Run RD20's original 3+ GPU tensor-split abort reproducer against STOCK b10680 WITHOUT patch 1201 applied -- if it passes (no abort), retire/reject 1201 as upstream-superseded. Patch 1201 is STATE=untested and outside production patch-sets already, so this is safe to defer without urgency.

**Fast-triage ranking of the remaining 17 findings (gpt's call, not independently verified against source):**
- **RD97 -- HIGHEST PRIORITY, gpt's single 'do this next' pick.** Its recorded conclusion was specifically 'hazard absent at pin b10502' -- that conclusion is REVISION-BOUND and is now stale since we moved to b10680. Re-run the same MMQ barrier/lifetime correctness audit against b10680 before trusting that conclusion still holds. This is a correctness-hazard reassessment, not a provenance/drift question like the others.
- RD21/1208 (already ported, deferred-hardware/inactive) -- MEDIUM-LOW. Fork still carries the same table mapping/nwarps logic BigCherry has; treat as provenance-only pending an actual relevant-hunk diff.
- RD30/fee8f9925 -- MEDIUM-LOW. Registry already prefers AMD's PR #63 as the structural candidate over this control candidate; resolve the duplicate cross-reference, no runtime risk.
- RD98 x2 -- LOW-MEDIUM. Planned only, intentionally one interacting candidate family (not two independent items) -- refresh both source heads together before ever porting.
- RD18/RD27 -- LOW. Planned, not materialized; drift only matters once porting starts.
- 4 untitled/no-plan-item tracked entries -- LOW runtime risk, MEDIUM registry-hygiene priority (fix so future sources-check output stays actionable).

**Net effect on this item's own priority order (superseding the original draft above)**: RD97's revision-bound reassessment is now the top real priority to pick up next, ahead of even finishing the RD05/06/07 provenance reconciliation or the RD20 supersession confirmation -- both of those are already de-risked (low findings), whereas RD97 concerns an actual correctness-hazard conclusion that is now known-stale.

### RESOLVED 2026-08-30: both flagged already-applied-patch cases + the top-priority triage item, all independently verified (not taken on gpt's word)

**RD97 re-audit (gpt's top triage pick) -- DONE, confirmed clean at b10680.** Re-ran the exact structural audit: zero mmq_hip* symbols, process_tile K loop still ends every iteration with a non-elidable __syncthreads(), and the one new patch touching mmq.cuh since the original audit (1237_rd30_moe_mmq_compact_grid) only edits outer launch/grid derivation, never the loop body -- verified by reading its anchors directly plus a clean dry-run apply. Recorded on RD97's own plan item (completed state, unchanged).

**Case 2 (RD20/1201) -- CONFIRMED superseded, not just gpt's inference.** Verified via real `gh api` calls against ggml-org/llama.cpp: commit d3371929b (PR #27574, merged 2026-08-23, inside our b10502->b10680 window) introduces the EXACT pattern_attn_gate_weight conditional now at llama-model.cpp:743-748, and the PR body explicitly states "Align attention gates and sinks with Q/head granularity for correct GQA/MQA device placement" with its own multi-device test coverage. RD20 moved to superseded state; external-sources.toml's tracked row updated to match the existing RD14/RD16 superseded convention; tools/tests/source/test_external_sources.py's hardcoded superseded-set assertion updated accordingly. patches/1201_rd20_attn_gate_tp_split was never validated and now never needs to be.

**Case 1 (1203/RD05-06-07) -- CONFIRMED low-risk, provenance-only. DONE.** Diffed all six of the patch's touched files against the fork's re-committed version (beaf69fb6): the 5 correctness-relevant files are byte-identical to what's already in patches/1203's content (config table, DKQ>576, np==1 sync fix, logit_softcap gate, sub-scale fold, J_MAX override); the 6th (test-backend-ops.cpp) differs only in already-documented intentional adaptations. No content change needed to 1203. Registry note updated with the verified re-committed SHA.

### Still open

- A full v3 snapshot refresh for stew675-rdna-boosts in external-sources.toml (new head, new base, superseding the v2 snapshot entirely per the registry's own documented convention -- the two reconciliation notes above record the SPECIFIC verified commits but don't yet replace the whole snapshot). Needs the fork's current tip (c8af5361d, from the original sources-check run) and its actual merge-base against ggml-org/llama.cpp master -- not fabricated here since it wasn't cheaply verifiable in this pass.
- RD21/1208 (already-ported, deferred-hardware) -- gpt ranked MEDIUM-LOW, not yet independently checked.
- RD30/fee8f9925 duplicate cross-reference, RD98 x2, RD18/RD27 (all planned/lower-priority per gpt's ranking), and the 4 untitled tracked entries missing a plan-item field -- none independently checked yet.
- mrlordcat-rdna-lab and amd-ecosystem-llama-cpp still need their own sources-check run (locator bug fixed, but the run itself was cut off by a wrapper timeout on mrlordcat's very large history -- not a real failure, just needs a longer-timeout re-run as a separate pass).

### Everything resolved 2026-08-30 -- all real code/registry questions checked, none left as assumptions

All priority patches independently re-checked against real b10680 source (not just gpt's ranking): RD97 re-confirmed clean, RD20 confirmed superseded and retired, RD05/06/07 confirmed provenance-only, and RD21/RD30/RD98/RD18/RD27 all confirmed STILL NOT implemented upstream (checked each: RD21's old RDNA2||RDNA3_5 branch is unchanged, RD30's compact-grid mechanism has zero trace upstream, RD98's VDR is still 2 not 8, RD18's rope+set_rows fusion still excludes IMRoPE and BF16, RD27's SSM conv_input fold has zero trace) -- none of these were silently resolved by the bump, all remain genuinely open work.

**The '4 untitled entries' were never a metadata gap** -- `plan-item = "-"` is the registry's deliberate sentinel for 'evidence-only/excluded, no RD item by design', not a missing field. Investigating them surfaced the REAL bug: all 8 rows in stew675-rdna-boosts's '2026-08-23 AMD follow-up handoff' block (fee8f9925/RD30-control, 812a47a89991/RD97, d59eb5a1c, b8fed9fae, 42aff9e1f, 1b600a34d0f/RD98, 0c92c50b57d/RD98, 88ef64264) are real AMD-Ecosystem/llama.cpp PRs sitting under the WRONG [[sources]] id -- the exact same misplacement bug already fixed once for PR #63/f8864197a, whose fix added the correct copy under amd-ecosystem-llama-cpp but never deleted the original misplaced rows. This is also the real explanation for why sources-check reported them 'not found by title': a clone of stew675's own branch was never going to contain commits from an entirely different repo. Fixed: relocated all 8 rows to amd-ecosystem-llama-cpp, verified the TOML still parses and the full external-sources + patch test suites pass.

This also resolved the 'RD30/fee8f9925 duplicate cross-reference' open question: fee8f9925 (PR #39) is NOT a duplicate of f8864197a (PR #63) -- it's RD30's distinct original/control-candidate commit, correctly kept as a separate row, now just relocated to the right source alongside its successor.

**Still genuinely open** (no code/registry risk, just process housekeeping): a full v3 snapshot refresh for stew675-rdna-boosts's rebased tip (needs a verified merge-base, not fabricated), and re-running sources-check for mrlordcat-rdna-lab/amd-ecosystem-llama-cpp with a longer timeout (mrlordcat's clone is just slow, ~17500 commits, not broken -- the earlier run was cut off by a wrapper timeout).

v3 snapshot ADDED 2026-08-30 for stew675-rdna-boosts: head c8af5361d, base 7584430716e -- both real, verified via `gh api repos/stew675/llama.cpp/compare/ggml-org:master...rdna-boosts` (merge_base_commit), not fabricated. v2 marked historical. Individual tracked rows NOT bulk re-pointed to v3 SHAs (out of scope for this pass -- only RD20 and RD05/06/07 were touched, per their specific findings). Full sources-check re-run (900s timeout, covering mrlordcat-rdna-lab + amd-ecosystem-llama-cpp + stew675-rdna-boosts) launched in background -- result pending, report when it lands.

Real bug found and fixed 2026-08-30: sources.py's `_check()` hardcoded `timeout=300` for the `git cherry -v mainline/master <tip>` mainline-merge-dedup call, ignoring the caller's --timeout entirely. That's the single most expensive operation in the function (full history walk + patch-id compare), so hardcoding it below even the function's own 600s default was backwards. Found live: a `sources check --timeout 900` run crashed with TimeoutExpired after exactly 300s on mrlordcat-rdna-lab (~17500 commits), before even reaching amd-ecosystem-llama-cpp or stew675-rdna-boosts -- meaning THIS pass never actually re-verified those two sources despite the locator fix making them clonable again. Fixed (one-line: timeout=300 -> timeout=timeout), committed, pushed.

Partial real evidence gathered before the crash: mrlordcat-rdna-lab has 55 new commits since our 2026-08-20 review (real qwen4exp/Qwen3.8-Flash port, RPC backend work, etc) -- a new snapshot review is warranted whenever that source becomes actionable, but nothing in the visible 'moved' list indicates any of our 5 specifically-tracked commits (RD69/71/72/73/76) drifted; that determination needs the per-tracked-commit dedup check, which never ran before the crash.

Re-running now with the fix + --timeout 1800 (background task brhj4mqg9) to actually get real coverage of amd-ecosystem-llama-cpp and stew675-rdna-boosts (post v3-snapshot) for the first time. Report pending.

**Real structural limitation found, not just a timeout bug.** Re-ran `sources check --timeout 1800` (30 min per git operation) after fixing the hardcoded-300s bug -- still timed out on the exact same `git cherry -v mainline/master <tip>` call against mrlordcat-rdna-lab. This confirms the earlier fix was necessary but not sufficient: git cherry's patch-id walk over this fork's full history genuinely does not complete in any timeout worth waiting for, not a value-tuning problem. mrlordcat-rdna-lab is source #1 in the registry, so amd-ecosystem-llama-cpp and stew675-rdna-boosts NEVER get reached in a real `sources check` run as currently implemented -- both remain effectively unchecked by this tool, blocked entirely by mrlordcat.

**Not pursuing further time-based retries.** A real fix needs a different approach (e.g. skip/opt-out the full-branch cherry dedup for sources explicitly flagged as large personal research forks, or narrow the dedup check to per-tracked-commit patch-id lookups instead of a whole-branch cherry walk) -- that's real design/implementation work, not something to force through by waiting longer. Not done in this pass; flagged as a follow-up if `sources check`'s mainline-dedup capability is ever needed for amd-ecosystem/stew675 specifically (mrlordcat's own 5 tracked commits' dedup status also remains genuinely unknown -- only the FINDING moved: 55 new commits since review was confirmed, which is separate from and prior to the per-commit dedup check that never ran).

**What IS independently confirmed as of this session (not from the broken tool, from direct verification earlier)**: RD97/RD20/RD05-06-07/RD21/RD30/RD98/RD18/RD27 were all checked directly against real b10680 source (see earlier notes) -- that work stands regardless of sources-check's mrlordcat blocker. The registry relocation fix (8 misplaced AMD-handoff rows) and the v3 snapshot addition are also both real and complete, independent of this blocker.

Closing this item's active investigation here -- remaining scope (mrlordcat snapshot refresh, a redesigned scalable mainline-dedup check, and any further individual RD-item checks) is genuine follow-up work, not something to keep chasing in this pass.

## Change Log

- 2026-08-29T14:12:18.451211+00:00 (created-by): Created by agent
- 2026-08-29T14:29:22.390278+00:00 (updated-by): Updated: section:notes
- 2026-08-29T20:48:57.651070+00:00 (updated-by): Updated: section:notes
- 2026-08-29T21:11:52.273882+00:00 (updated-by): Updated: section:notes
- 2026-08-29T21:15:06.052765+00:00 (updated-by): Updated: section:notes
- 2026-08-29T21:21:30.353757+00:00 (updated-by): Updated: section:notes
- 2026-08-29T21:52:12.437272+00:00 (updated-by): Updated: section:notes
