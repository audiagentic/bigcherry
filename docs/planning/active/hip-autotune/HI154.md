---
id: HI154
order: 0
plan: hip-autotune
state: pending
created-at: '2026-08-29T14:12:18.451211+00:00'
breadth: ''
skill: intermediate
created-by: agent
work: M
---

# stew675-rdna-boosts sources-check findings: rebase + content drift review

## Description

First real run of `bigcherry sources check` in a long time (the locator bug fixed alongside this item silently broke it for 4 of 5 registered forks -- only stew675-rdna-boosts, whose locator was already a bare ref, ever actually ran). Run against the current b10680-pinned tree, 2026-08-30. 19 findings total.

**Branch state: REBASED.** The registry's active snapshot (v2, head 9e46e1fdc) is no longer an ancestor of the fork's current tip (c8af5361d) -- per config/external-sources.toml's own convention, this alone requires a new snapshot + re-audit before further planning against this source, independent of the individual findings below.

**Content-CHANGED findings (the actionable ones -- these mean the fork's current version of a tracked commit differs from what we based our work on):**
- cfc192734 (RD04, `native-BF16 tile kernel for all head sizes...`) -> re-committed as b6bfa422e with changed content. RD04 has MULTIPLE tracked commits (see registry); this is one of several, not the whole cluster.
- 8473c0da7 (RD18, `fuse IMRoPE + set-rows for BF16 KV cache`) -> d33ce1adf, changed. RD18 status is currently 'planned' (not yet ported) -- re-review the fork's NEW version before porting, not the stale one.
- 1818c3b37 (RD21, `gfx1151 mmvq table with nwarps=2 for Q8_0 decode`) -> 5b320ed94, changed. RD21 -> patches/1208_rd21_gfx1151_mmvq_nwarps_table IS ALREADY PORTED (status deferred-hardware) -- compare the fork's new version against what was ported; this is the highest-priority item to check since it's already in our patch set.
- **1d525bd45 (RD05/RD06/RD07, `RDNA4 WMMA flash-attn and Q6_K mmq prefill`) -> beaf69fb6, changed. This maps to patches/1203_rd050607_rdna4_wmma_fa_q6k_mmq, status ported-untested -- an ALREADY-APPLIED PRODUCTION PATCH whose upstream fork source has since changed. Highest-priority review: does our ported version still match the fork's intent, or did the fork fix/change something we should pull forward?**
- 0510d7cfa (RD27, `fold SSM conv_input concat into qkv mmvq`) -> 555e79ab2, changed. Status 'planned' (not yet ported).

**'Not found by title' findings (commit disappeared from the fork's history under its old title -- could mean: renamed, squashed, reverted, or genuinely dropped by the fork author; needs a manual look, NOT necessarily 'merged upstream' or 'redundant' without checking):**
- 3b200b259 (RD20) -> patches/1201_rd20_attn_gate_tp_split, ALREADY PORTED (ported-untested). Same priority tier as RD05/06/07 above -- an applied patch whose source commit vanished from the fork under its recorded title.
- fee8f9925 (RD30) -- registry lists RD30 against amd-ecosystem-llama-cpp (-> patches/1237), not stew675; this hit under stew675 needs the registry cross-referenced to see if it's a duplicate/mistaken tracked-entry or a genuinely separate stew675-side RD30 commit.
- 812a47a89 (RD97), 1b600a34d (RD98), 0c92c50b5 (RD98), and four untitled ('-') entries: d59eb5a1c, b8fed9fae, 42aff9e1f, 88ef64264 -- need identifying (RD97/RD98 aren't in the plan items reviewed so far this session; the four untitled ones may be registry entries missing a `plan-item` field, which is itself worth fixing regardless of the drift question).

**Not evidence of anything wrong on our side**: everything else (majority of the 19 hits) is 'drift: content identical' -- the fork's maintainer rebased/re-committed with new SHAs but no actual content change. No action needed on those beyond eventually refreshing the registry's tracked SHAs to the new ones (cosmetic, not urgent).

## Steps

1. Prioritize the two ALREADY-PORTED-patch findings first: RD05/06/07 (1203, content-changed) and RD20 (1201, commit vanished under its title) -- both are live in the build, so any real upstream fix/change is the most consequential thing to miss.
2. For each content-changed finding, diff the fork's new commit against the OLD (tracked) commit to see what actually changed, then diff that against our ported patch to see if the divergence matters.
3. For RD20's 'not found', check the fork's current history directly (git log --all --grep, or browse the fork on GitHub) to determine what happened to that commit -- do not assume 'merged upstream' without checking; the finding only means 'not found by title', not 'landed in mainline'.
4. Resolve the RD30 registry cross-reference question (is fee8f9925 a mistaken duplicate tracked-entry under the wrong source, or real).
5. Identify and fix the 4 untitled tracked entries (add a plan-item field if missing) plus RD97/RD98.
6. Refresh the registry: record a new snapshot (v3) for stew675-rdna-boosts's rebased tip, superseding v2, per the registry's own documented convention (see stew675 v1->v2 history already in the file as the precedent).
7. Re-run `bigcherry sources check` after the snapshot refresh to confirm it reports clean.

Note: mrlordcat-rdna-lab and amd-ecosystem-llama-cpp still need their own sources-check run -- the locator fix (this session) makes them clonable now, but `sources check` didn't finish for them yet during this pass (mrlordcat's clone is apparently very slow -- 17500+ commit personal research log per its own registry notes -- and the run was cut off by a wrapper timeout, not a real failure). Re-run with a longer timeout as a separate pass; davetha and joursbleu are single-PR forks already resolved/closed per their notes and lower priority.

## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files



## Validation



## Effort & Risk



## Standards



## Acceptance Criteria



## Notes

### gpt assessment (req_e1eda0d23e914a76) -- both core factual claims independently verified true

**Case 1: patches/1203_rd050607_rdna4_wmma_fa_q6k_mmq (RD05/06/07) -- LOW RISK, provenance-only issue.**
gpt's read: our patch ALREADY contains the fork's new content (the re-squashed/re-tuned beaf69fb6 version) -- the config-table constants, DKQ>576, GGML_CUDA_FA_WMMA_256, the logit_softcap==0||head-in-{128,256,512} guard, and both tile-Q sync fixes (including the np==1 correctness barrier) are all already present in our patch.py. Verified directly by grep against the real file: all cited strings/constants genuinely exist there (lines 121, 148, 159, 186-193, 478, 496, 529). Conclusion: NOT a missing-correctness-fix emergency -- the implementation already matches current fork content; what's stale is only the recorded fork-commit/snapshot identity (1d525bd45/v2), not the code. Recommended action: bounded provenance reconciliation only -- update the tracked commit/snapshot to beaf69fb6 in external-sources.toml + the patch's own docstring, after confirming all six touched-file hunks match modulo documented BigCherry adaptations. Do NOT retune/rewrite 1203.

**Case 2: patches/1201_rd20_attn_gate_tp_split (RD20) -- likely upstream-superseded, not reverted/lost.**
gpt's read: the fork commit's disappearance is best explained by upstream (ggml-org/llama.cpp mainline) having independently added its OWN, more precise handling of attn_gate.weight's split granularity since our patch was written -- specifically a conditional (`if (tensor->ne[1] == hparams.n_head(il)) return {granularity_head}; return {granularity_q};`) that's more nuanced than RD20's blunt always-match-attn_q fix. Verified directly by reading vendor/llama.cpp/src/llama-model.cpp:743-747 at the current b10680 pin -- gpt's quoted code block is EXACTLY what's there, including the surrounding pattern_attn_gate_weight regex context (also confirmed real at lines 390, 532, 620, 674, 743). This is real, strong evidence -- NOT yet proof of full supersession, since I have not diffed the exact SEMANTIC equivalence or run the reproducer.

gpt's recommended verification steps (NOT yet done -- this is the concrete remaining work, not a vague 'investigate more'):
1. `git log -S'pattern_attn_gate_weight' b10502..b10680 -- src/llama-model.cpp` (find the introducing commit, confirm it's genuinely new since b10502, not something RD20 already accounted for).
2. `git show <introducing-commit> -- src/llama-model.cpp` (read the actual upstream commit message/rationale).
3. Run RD20's original 3+ GPU tensor-split abort reproducer against STOCK b10680 WITHOUT patch 1201 applied -- if it passes (no abort), retire/reject 1201 as upstream-superseded. Patch 1201 is STATE=untested and outside production patch-sets already, so this is safe to defer without urgency.

**Fast-triage ranking of the remaining 17 findings (gpt's call, not independently verified against source):**
- **RD97 -- HIGHEST PRIORITY, gpt's single 'do this next' pick.** Its recorded conclusion was specifically 'hazard absent at pin b10502' -- that conclusion is REVISION-BOUND and is now stale since we moved to b10680. Re-run the same MMQ barrier/lifetime correctness audit against b10680 before trusting that conclusion still holds. This is a correctness-hazard reassessment, not a provenance/drift question like the others.
- RD21/1208 (already ported, deferred-hardware/inactive) -- MEDIUM-LOW. Fork still carries the same table mapping/nwarps logic BigCherry has; treat as provenance-only pending an actual relevant-hunk diff.
- RD30/fee8f9925 -- MEDIUM-LOW. Registry already prefers AMD's PR #63 as the structural candidate over this control candidate; resolve the duplicate cross-reference, no runtime risk.
- RD98 x2 -- LOW-MEDIUM. Planned only, intentionally one interacting candidate family (not two independent items) -- refresh both source heads together before ever porting.
- RD18/RD27 -- LOW. Planned, not materialized; drift only matters once porting starts.
- 4 untitled/no-plan-item tracked entries -- LOW runtime risk, MEDIUM registry-hygiene priority (fix so future sources-check output stays actionable).

**Net effect on this item's own priority order (superseding the original draft above)**: RD97's revision-bound reassessment is now the top real priority to pick up next, ahead of even finishing the RD05/06/07 provenance reconciliation or the RD20 supersession confirmation -- both of those are already de-risked (low findings), whereas RD97 concerns an actual correctness-hazard conclusion that is now known-stale.

## Change Log

- 2026-08-29T14:12:18.451211+00:00 (created-by): Created by agent
- 2026-08-29T14:29:22.390278+00:00 (updated-by): Updated: section:notes
