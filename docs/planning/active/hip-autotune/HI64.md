---
id: HI64
order: 40
plan: hip-autotune
state: pending
created-at: '2026-08-16T11:49:36.576356+00:00'
breadth: ''
skill: intermediate
created-by: agent
work: S
---

# Windows/WDDM measurement hardening — unblocked sweeps poison on transient fatal failures

## Description

On the local Windows workstation (WDDM), full unblocked HIP tuning sweeps hit transient fatal measurement failures that poison the whole experiment: both an ON and a matched OFF run of Slice A died at different random stages with identical failure modes, proving the flake is environmental, not caused by the double-native twin. Evidence collection therefore had to use HIP_LAUNCH_BLOCKING=1, which serializes launches and makes blocked-run timings non-authoritative (accepted for Slice A's local gate; production-architecture validation on Brutus/Linux remains the unblocked evidence gate).

The failure is a transient WDDM/TDR-class measurement error inside hipEvent timing, not a kernel bug. The current policy treats any launch/timing failure as global poison, which is correct in principle but means one transient driver hiccup discards an entire multi-hour sweep with no retry or quarantine of the affected signature.

## Steps

- Reproduce unblocked: run a full sweep without HIP_LAUNCH_BLOCKING=1 on the workstation and capture the fatal-failure class (TDR/event error code) from the log.
- Classify transient vs permanent failure modes at the hipEvent/launch layer.
- Add bounded retry with backoff for the transient class only; permanent failures keep poisoning (standards: a wrong measurement is worse than no measurement).
- Record the retry/recovery in the journal JSONL so poisoned-vs-recovered runs are distinguishable in artifacts.
- Verify: an unblocked sweep completes on the workstation, or fails with a classified permanent error, and the OFF/ON parity still holds under verify_slice_a.py.

## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files

- src/ggml/src/ggml-cuda/hip-autotune-tuner.cu
- tools/verify_slice_a.py
- docs/reference/HANDOFF.md

## Validation

- Unblocked targeted sweep (MUL_MAT families x n in {1,4,9}) completes 268/268 on the workstation without HIP_LAUNCH_BLOCKING=1, or each failure is classified permanent with driver evidence.
- A synthetic transient-failure injection recovers and the run completes; a permanent failure still poisons.
- verify_slice_a.py passes on both ON and OFF artifacts produced unblocked.
- Journal rows for retried signatures are present and parseable by inventory.

## Effort & Risk

S. Risk is low if the failure classifies cleanly as transient (TDR recovery); if the driver error is not retryable in-process, the fix may degrade to documented use of HIP_LAUNCH_BLOCKING=1 on WDDM plus a pre-flight stability probe.

## Standards



## Acceptance Criteria



## Notes

Filed from the Slice A close-out (2026-08-13, GPT verdict Q3): the WDDM flake is a separate hardening item, not part of Slice A's scope. Local evidence with HIP_LAUNCH_BLOCKING=1 was accepted for the local gate; this item covers making unblocked runs reliable on Windows so workstation sweeps stop needing the serialization workaround.

## Change Log

- 2026-08-16T11:49:36.576356+00:00 (created-by): Created by agent

## Ledger-events

- chg_20260816_114950_closed-slice-a-of-the-hip-auto_6176
- 2026-08-16T11:49:50.345590+00:00 (updated-by): Updated: section:ledger-events
