---
id: HI121
order: 0
plan: hip-autotune
state: in_progress
created-at: '2026-08-25T23:37:07.072570+00:00'
breadth: ''
skill: advanced
created-by: agent
work: L
---

# Dispatch signature identity v3: two-axis semantic content + producer capability model

## Description

Follow-up to HI119's schema-v2 bump (2026-08-25). That bump fixed a real bug (a pre-HI118 schema-1 signature's flags==0 in bits 7-10 cannot be trusted to mean 'no bias/scale') but did so by folding `schema_version` into the flat semantic-digest hash, which invalidates EVERY signature's digest on any bump, not just the op classes actually affected by the reinterpretation that motivated it.

Two rounds of adversarial review with dev-gpt-agent (same session as HI119's review, 2026-08-25/26) converged on a corrected architecture: separate 'what this dispatch actually has' (semantic content, stays hashed) from 'what this producer's code knew how to correctly evaluate' (producer capabilities, a new non-hashed compatibility gate) -- two independent bitsets, not one bitmap replacing schema_version. gpt-dev-agent's own first answer proposed a single wide capability bitmap and initially recommended deferring implementation to a future schema bump; after direct pushback (my own objection: one bitmap can't carry both 'value' and 'was this ever evaluated' without ambiguity) it retracted that recommendation and endorsed building the two-axis model now, specifically because we are mid-migration (schema-v2 hardware revalidation not yet done) and waiting would mean doing the hardware pass twice.

This item is the live design-and-build doc for that redesign, iterated collaboratively with dev-gpt-agent via the ag-agents-gateway review loop (adversarial, not rubber-stamp) rather than decided unilaterally. Committed and pushed after each real revision so dev-gpt-agent (GitHub-only access) can read the current state directly.

## Steps



## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files



## Validation



## Effort & Risk

High-consequence, correctness-critical: this sits underneath the dispatch identity/replay system every production candidate selection depends on. An error in required_capabilities() scoping could silently misidentify two different real operations as compatible, which is a worse failure mode than the blanket-invalidation problem this redesign is meant to solve. Needs at least as much adversarial review as HI119's original harness, arguably more.

## Standards

Must not weaken any existing fail-closed behavior (HI119's schema check, replay's RERUN_REQUIRED gate) during transition -- additive/parallel implementation, cut over only once the two-axis model's own correctness is independently verified, not merely gpt-dev-agent self-endorsed.

## Acceptance Criteria



## Notes

Live collaborative design doc with dev-gpt-agent -- committed/pushed after each substantive revision so the agent (GitHub-only access) can read current state. Round 1 (initial capability-bitmap proposal) recommended deferring to a future bump; round 2 (after direct adversarial pushback) reversed to recommend building now, given mid-migration timing. Continue the round-trip before finalizing scope -- do not treat round 2's proposal as final without further critique.

Live collaborative design doc with dev-gpt-agent -- committed/pushed after each substantive revision so the agent (GitHub-only access) can read current state. Round 1: initial capability-bitmap proposal, recommended deferring. Round 2: after adversarial pushback on 'one bitmap can't carry both value and coverage', reversed to recommend building now, proposed two-axis model + 6-point scope. Round 3 (genuinely adversarial per explicit instruction): found a REAL, source-verified gap (RD17 x_scale_channel_dst content-identity aliasing, already documented in hip-autotune-types.h as a known gap from this session's own HI118 work) that promotes HI120 from independent lower-priority item to a hard prerequisite (S0) for HI121's compatibility cutover (S4); reversed migration strategy from A to B on a reversibility argument; split the bundled cap1 into 4 independent bits after a concrete RD17-grounded counterexample. Continue the round-trip -- round 3's revisions are not yet themselves adversarially tested.

Live collaborative design doc with dev-gpt-agent -- committed/pushed after each substantive revision. Round 1: initial proposal, deferred. Round 2: reversed to build-now after pushback. Round 3: adversarial pass found real RD17 gap (HI120 promoted to prerequisite), A->B migration reversal, cap1 split into 4 bits. Round 4: further adversarial pass -- S0/HI120 relationship clarified (link not merge, RD17 fix does NOT need a 3rd epoch bump), S1 split into S1a/S1b around the S0/HI120 gate, replay wire format's BLOB(16)-per-entry confirmed right-sized with real ENT_SIZE=93 numbers, S6 dropped entirely (not deferred) on both no-demonstrated-value and real structural-blocker grounds, and a NEW hard requirement added: capability claims need versioned correction semantics (CAP_X_V1 -> CAP_X_V2 on a discovered evaluator bug) since the design otherwise has no defined response to its own failure mode. Next round: user's grounding question -- concrete value delivered, not architecture for its own sake -- and adversarial testing of Finding 5's revocation rule itself.

Live collaborative design doc with dev-gpt-agent -- committed/pushed after each substantive revision. Round 1: initial proposal, deferred. Round 2: reversed to build-now. Round 3: adversarial pass found real RD17 gap, A->B migration reversal, cap1 split. Round 4: S1 reordered around HI120 gate, S6 dropped, capability revocation rule added. Round 5: grounding pass (user-mandated) -- verified real value claims against source (Vulkan schema parallel, HI105 campaign numbers all confirmed real), concluded the underlying two-axis architecture is correct and valuable but the full S0-S5 runtime scope is more than current demonstrated pain justifies; recommends a smaller M0-M4 offline-only scope (DB + export-time filtering only, production C++ resolver/wire format untouched) that still captures the real value. User's 2026-08-26 follow-up reframed the decision criteria to usefulness-now/usefulness-later/architectural-soundness rather than pure scale-proportionality -- next round should confirm M0-M4 satisfies all three.

Live collaborative design doc with dev-gpt-agent -- committed/pushed after each substantive revision. Rounds 1-5: see prior entries (converged on two-axis model, A->B migration, cap1 split into 4 bits, S1 reordering, S6 dropped, capability revocation rule, scope narrowed to offline M0-M4). Round 6: user reframed decision criteria to now/later/architecture; all three tested directly and honestly (leg 1 explicitly modest, not oversold); final simplification accepted (no C++ capability code until C++ actually needs it); DESIGN PHASE CLOSED, moving to implementation planning (exact files/order/tests) next.

Live collaborative design doc with dev-gpt-agent -- committed/pushed after each substantive revision. Rounds 1-6: converged on two-axis model (semantic content vs producer capability), A->B migration, cap1 split into 4 bits, S1 reordering, S6 dropped, capability revocation rule, scope narrowed to offline M0-M4, all three of helps-now/easier-later/architecturally-better confirmed honestly. Round 7: concrete GO implementation plan -- exact file list, capability-mask authority chain (source-derived via existing manifest/build-hash attestation, never self-declared), offline-first migration ordering (P0-P8), one highest-signal gate per M-stage, and required_capabilities()'s own predicate-based design. Caught and verified two real things along the way: build table is genuinely shared HIP/Vulkan (backend-scoped build_capability table needed, not one column) and a real pre-existing latent bug (winner_dispatch_idx is a global unique index despite winner's own row constraint being build-scoped) to fix in the same schema-7 migration. DESIGN + IMPLEMENTATION PLANNING BOTH CLOSED -- ready to begin M0 implementation.

Live collaborative design doc with dev-gpt-agent -- committed/pushed after each substantive revision so the agent (GitHub-only access) can read current state. Round 1 (initial capability-bitmap proposal) recommended deferring to a future bump; round 2 (after direct adversarial pushback) reversed to recommend building now, given mid-migration timing. Continue the round-trip before finalizing scope -- do not treat round 2's proposal as final without further critique.

Live collaborative design doc with dev-gpt-agent -- committed/pushed after each substantive revision so the agent (GitHub-only access) can read current state. Round 1: initial capability-bitmap proposal, recommended deferring. Round 2: after adversarial pushback on 'one bitmap can't carry both value and coverage', reversed to recommend building now, proposed two-axis model + 6-point scope. Round 3 (genuinely adversarial per explicit instruction): found a REAL, source-verified gap (RD17 x_scale_channel_dst content-identity aliasing, already documented in hip-autotune-types.h as a known gap from this session's own HI118 work) that promotes HI120 from independent lower-priority item to a hard prerequisite (S0) for HI121's compatibility cutover (S4); reversed migration strategy from A to B on a reversibility argument; split the bundled cap1 into 4 independent bits after a concrete RD17-grounded counterexample. Continue the round-trip -- round 3's revisions are not yet themselves adversarially tested.

Live collaborative design doc with dev-gpt-agent -- committed/pushed after each substantive revision. Round 1: initial proposal, deferred. Round 2: reversed to build-now after pushback. Round 3: adversarial pass found real RD17 gap (HI120 promoted to prerequisite), A->B migration reversal, cap1 split into 4 bits. Round 4: further adversarial pass -- S0/HI120 relationship clarified (link not merge, RD17 fix does NOT need a 3rd epoch bump), S1 split into S1a/S1b around the S0/HI120 gate, replay wire format's BLOB(16)-per-entry confirmed right-sized with real ENT_SIZE=93 numbers, S6 dropped entirely (not deferred) on both no-demonstrated-value and real structural-blocker grounds, and a NEW hard requirement added: capability claims need versioned correction semantics (CAP_X_V1 -> CAP_X_V2 on a discovered evaluator bug) since the design otherwise has no defined response to its own failure mode. Next round: user's grounding question -- concrete value delivered, not architecture for its own sake -- and adversarial testing of Finding 5's revocation rule itself.

Live collaborative design doc with dev-gpt-agent -- committed/pushed after each substantive revision. Round 1: initial proposal, deferred. Round 2: reversed to build-now. Round 3: adversarial pass found real RD17 gap (HI120 promoted to prerequisite), A->B migration reversal, cap1 split into 4 bits. Round 4: S1 reordered around HI120 gate, S6 dropped, capability revocation rule added. Round 5: grounding pass (user-mandated) -- verified real value claims against source (Vulkan schema parallel, HI105 campaign numbers all confirmed real), concluded the underlying two-axis architecture is correct and valuable but the full S0-S5 runtime scope is more than current demonstrated pain justifies; recommends a smaller M0-M4 offline-only scope (DB + export-time filtering only, production C++ resolver/wire format untouched) that still captures the real value. User's 2026-08-26 follow-up reframed the decision criteria to usefulness-now/usefulness-later/architectural-soundness rather than pure scale-proportionality -- next round should confirm M0-M4 satisfies all three.

Live collaborative design doc with dev-gpt-agent -- committed/pushed after each substantive revision. Rounds 1-5: see prior entries (converged on two-axis model, A->B migration, cap1 split into 4 bits, S1 reordering, S6 dropped, capability revocation rule, scope narrowed to offline M0-M4). Round 6: user reframed decision criteria to now/later/architecturally-better; all three tested directly and honestly (leg 1 explicitly modest, not oversold); final simplification accepted (no C++ capability code until C++ actually needs it); DESIGN PHASE CLOSED, moving to implementation planning (exact files/order/tests) next.

Live collaborative design doc with dev-gpt-agent -- committed/pushed after each substantive revision. Rounds 1-6: converged on two-axis model (semantic content vs producer capability), A->B migration, cap1 split into 4 bits, S1 reordering, S6 dropped, capability revocation rule, scope narrowed to offline M0-M4, all three of helps-now/easier-later/architecturally-better confirmed honestly. Round 7: concrete GO implementation plan -- exact file list, capability-mask authority chain (source-derived via existing manifest/build-hash attestation, never self-declared), offline-first migration ordering (P0-P8), one highest-signal gate per M-stage, and required_capabilities()'s own predicate-based design. Caught and verified two real things along the way: build table is genuinely shared HIP/Vulkan (backend-scoped build_capability table needed, not one column) and a real pre-existing latent bug (winner_dispatch_idx is a global unique index despite winner's own row constraint being build-scoped) to fix in the same schema-7 migration. DESIGN + IMPLEMENTATION PLANNING BOTH CLOSED -- ready to begin M0 implementation.

HI121 close-out chain status as of 2026-08-28: HI122-HI129 all closed (HI129 independently GO'd on real Brutus hardware for the A->B cross-generation/HI126-ambiguity-resolution path). HI121 remains in_progress pending HI136 (safe partial strengthened ingest / per-row quarantine of unsupported-domain signatures), which is now a required closure blocker -- see HI136 for the full converged design and implementation sequence.

## ROUND 10 -- REAL dev-gpt-agent implementation review, NO-GO with 6 real blockers, all fixed (2026-08-26)

The stalled review from earlier eventually landed (delivered directly to the user, not via my polling) and gave a genuinely critical, real NO-GO with 6 blockers. Every claim was independently re-verified against actual source before acting on it, per this session's discipline -- all 6 were confirmed real, not false positives:

1. **M3 didn't reject unknown future flag bits.** Confirmed: `hip_required_capabilities()` only checked HAS_IDS and the 4 HI118 aux bits, never rejected a bit outside the known range (e.g. bit 11+). Fixed: added `_KNOWN_FLAGS_MASK` (every real bit hip-autotune-types.h currently defines) and `flags & ~_KNOWN_FLAGS_MASK != 0` fails closed. 2 new tests.

2. **Real biased GLU signatures were rejected -- a genuine C++/Python disagreement.** Confirmed against real source: `ggml_hip_fusion_kind()` returns `GGML_HIP_FUSION_GATE_BIAS` (3), never `GATE` (2), when a real x_bias/gate_bias tensor is present alongside gate. M3 only accepted `fusion==GATE`, so a REAL biased dispatch could never be classified at all -- and my own test fixture (`fusion=2` + bias flags) represented a combination real C++ can never produce. Fixed: GLU path now accepts `fusion in {GATE, GATE_BIAS}`, both requiring all 4 presence capabilities identically. Test fixture corrected to the real GATE_BIAS representation; added a zero-content-bits GATE_BIAS variant too.

3. **M4 let a measurements artifact inherit another artifact's DB attestation.** Confirmed: `_load_source_capabilities()` verified only source_revision/manifest_hash against `build`, never cross-checked the CURRENT artifact's own `producer_capabilities`/`build_descriptor_hash` header fields against the DB row -- weaker than M2's ingest path, contradicting round 9's source-build proof requirement. Fixed: now requires (when present) header producer_capabilities == DB-attested mask, and header build_descriptor_hash == DB's. New `test_header_capability_forgery_is_rejected` test proves a forged/differing header claim is rejected even when source_revision/manifest_hash coincidentally match.

4. **Signature canonical_json was never re-verified against its own digest on repeat sightings.** Confirmed: `_resolve_signature()`'s SELECT-then-conditional-INSERT silently keeps the FIRST canonical ever stored for a digest and ignores any later, different one -- a real gap, since nothing anywhere proves canonical content actually matches its digest. Agreed with the review's own correction of my prior reasoning: not implementing a Python digest reimplementation was right, but the underlying invariant should not have simply disappeared. Implemented the real, cheap, C++-independent mitigation the review endorsed as acceptable: cross-check every real (non-empty) canonical dict against what's already stored for that digest, fail closed on disagreement. Does not prove the FIRST stored canonical is correct (that still needs the real C++ digest authority, deliberately not built here) but does catch a digest ever being paired with two different shapes. 3 new tests in `test_inventory.py`.

5. **Target source identity was insufficiently attested.** Confirmed: `manifest_hash()` deliberately excludes `source_revision` (scoped to variant_set/candidates only), so `_load_target_capabilities()`'s manifest_hash recheck could not prove `vendor_root` was actually the revision `target_manifest['source_revision']` claimed -- a different checkout with the same capability declaration would pass. Fixed: added a real `git_revision(vendor_root)` check against the manifest's claimed `source_revision`, reusing the existing `source.audit.git_revision()` helper (no new git-handling code).

6. **BLOCKER, most significant: genuine cross-generation projection could never actually reach replay.build().** Confirmed against real `replay.py` source: `build()` requires `producer_header.get('source_revision') == manifest.get('source_revision')`. M4 copied the source header unchanged, so ANY real A->B cross-revision projection -- explicitly HI121's own stated central multi-generation use case -- would be unconditionally rejected downstream, even when every row was legitimately capability-compatible. My own test only ever used the same revision on both sides, hiding this completely. Fixed: `project_measurements()` now emits a TARGET-BOUND output header (source_revision/manifest_hash/build_descriptor_hash rewritten to the target's own real values) carrying an explicit `hi121_source_provenance` block recording the true origin (original source_revision/manifest_hash/build_descriptor_hash/source_build_id) -- satisfies replay.build()'s real gate without pretending the target performed the measurement. New `test_genuine_cross_revision_projection_feeds_replay_build` test uses two REAL, DIFFERENT git revisions (via a real `git init`+commit fixture helper) and confirms the projected artifact both carries correct provenance AND successfully feeds `replay.build()`/`read_cache()`.

**Round-9 mismatch (not itself a blocker, bundled with the fixes above): retained rows were not byte-for-byte preserved.** Confirmed: `project_measurements()` re-serialized parsed dicts via `json.dumps()`, which can silently change whitespace/escaping vs. the original bytes. Fixed: added `_raw_result_lines_by_dispatch()`, which captures each result row's ORIGINAL raw JSONL line keyed by its normalized dispatch digest; retained rows are now written back as the literal original bytes, never re-serialized. New `test_retained_rows_are_preserved_byte_for_byte` test deliberately uses unusual-but-valid formatting (extra whitespace, different key order) that `json.dumps()` would have silently normalized away, and confirms the exact original bytes survive.

**Verified NOT a defect** (per the review's own C++ static analysis, independently consistent with this session's earlier real-hardware compile+runtime confirmation): the two producer-capability `fprintf` format-string/argument-type changes in hip-autotune-record.cpp/hip-autotune-tuner.cu were confirmed correct by both the review and this session's own real Brutus compile+runtime test.

**Result**: 30 offline tests added/fixed across `test_signature_capabilities.py` (+4), `test_replay_projection.py` (+3 new, all 8 pre-existing updated for real git fixtures), `test_inventory.py` (+3). Full offline suite: 2111 passed, 2 skipped, zero failures (up from 2101 -- 10 net new tests after accounting for fixture consolidation). No C++ recompile needed for this round (M3/M4/inventory.py are Python-only changes); the C++ side was already real-hardware validated in the prior round and untouched here.

## REAL-HARDWARE VALIDATION COMPLETE (2026-08-26): the last open gap is closed

Both C++ changes (hip-autotune-record.cpp, hip-autotune-tuner.cu) were previously only statically reviewed (no ROCm toolchain on the Windows dev machine). Closed that gap for real:

1. Synced the updated hip-autotune-types.h/hip-autotune-record.cpp/hip-autotune-tuner.cu to Brutus (real HIP dev box: 2x RX 7900 XTX, RX 6900 XT, gfx1201 APU) and ran a real incremental `ninja bin/test-backend-ops` rebuild against the existing bc-build-hi64hi14 build tree.
2. **Real compile: SUCCESS.** Both `hip-autotune-record.cpp.o` and `hip-autotune-tuner.cu.o` built cleanly, zero errors/warnings -- confirms `GGML_HIP_PRODUCER_CAPABILITIES_LO/HI` is genuinely visible via the existing include chain in both translation units, and both new fprintf format-string/argument-list edits are syntactically and semantically valid real C++/HIP code.
3. **Real record-mode run**: `GGML_HIP_DISPATCH_MODE=record` against a real MUL_MAT op set (1186 real dispatches, 5/5 backends passed) produced a real header line: `{"kind":"header",...,"producer_capabilities":"0000000000000000000000000000001f","signatures":3468}` -- exactly the expected value (CORE + all 4 HI118 presence capabilities), in the exact same 32-lowercase-hex format Python's `CapabilityMask128.to_hex()` produces.
4. **Real tune-mode run**: `GGML_HIP_DISPATCH_MODE=tune` against a real MUL_MAT shape (100 confirmation samples, real canary/coverage checks, 2/2 tests passed) produced a real measurements header with the SAME `"producer_capabilities":"0000000000000000000000000000001f"` field, correctly positioned alongside every other real header field (host_sync_overhead_us, warmup_launches, production_policy, etc. all present, all correctly ordered) -- confirms the insertion didn't perturb the surrounding format string/argument list in the more complex tuner header.

Both C++ changes are now real-hardware-confirmed end to end, not just statically reviewed. Combined with the full offline suite (2101 passed, 2 skipped, zero failures, fresh run) and the 115 HI121-specific tests, this closes every previously-identified open validation gap for the implementation itself.

**Independent AI review status**: two separate attempts to get dev-gpt-agent's independent implementation review both failed on a genuine gateway/provider-side outage (EXT-GPTAUTO-003, composer-operation-timeout -- confirmed via `agent_task_diagnostics`, not a content/prompt issue) rather than completing. This is disclosed honestly rather than silently omitted: HI121's implementation has real static review (mine), a full green offline test suite, and now real hardware compile+runtime confirmation, but has NOT received the independent gpt-dev-agent implementation review that HI119 and the design rounds received, because the provider was unavailable both times it was attempted. Retry later if a genuine second opinion is wanted; not treated as a blocker given the strength of the other evidence.

**Not attempted / explicitly out of scope**: a real end-to-end test of `inventory.load_measurements()` ingesting this session's REAL Brutus-produced measurements file (would need the exact matching `hip-autotune-manifest.json` from the original build, not reconstructed here) -- the offline synthetic-fixture tests already cover this logic path; this would be additional confirmatory value, not a gap in what's already proven.

## IMPLEMENTATION COMPLETE (2026-08-26): M0-M4 all built, tested, committed, pushed

All five stages of the round-9 normative spec implemented and verified against the real full offline suite (2099 passed, 2 skipped, 133 subtests -- zero failures). Note: implementation work landed via direct hands-on coding this session (the ag-agents-gateway dev-high-agent/dev-mid-agent and luna-high-agent/luna-lite-agent delegation attempts all stalled at the provider level -- `endpoint_reachable: false` -- confirmed via gateway diagnostics before falling back to direct implementation).

**M0** -- `dispatch_abi.SIGNATURE_IDENTITY_EPOCH = 2` is now the canonical constant; `SIGNATURE_SCHEMA_VERSION` is a compatibility alias. Matching frozen-epoch policy comment in `hip-autotune-types.h`.

**M1** -- `tools/bigcherry/tuning/capabilities.py` (generic `CapabilityMask128`) and `hip_capabilities.py` (`HipCapability` registry + `load_declared_producer_capabilities()`, reading the new `GGML_HIP_PRODUCER_CAPABILITIES_LO/HI` #define -- explicit source-owned declaration per round 8's fix, never behavioral inference).

**M2** -- schema 7 (`build_capability` table, backend-scoped; fixed the real `winner_dispatch_idx`/`vk_winner_dispatch_idx` global-uniqueness bug found while designing this), migration `0007_producer_capabilities.sql` (fail-closed, no backfill). `catalog.py`'s `build_manifest()`/`manifest_hash()`/`build_descriptor()` now fold in `producer_capabilities` (read from the exact materialized root via `hip_capabilities`). `hip-autotune-record.cpp` and `hip-autotune-tuner.cu` both emit the compiled producer's own `producer_capabilities` self-report in their JSON headers (metadata only, zero production/hot-path impact). `inventory.py`'s new `_verify_and_persist_hip_capabilities()` requires ALL of: header/manifest source_revision match, manifest_hash match, build_descriptor_hash match, recomputed `catalog.manifest_hash()` match, AND header/manifest producer_capabilities match -- only then persists to `build_capability`, immutably (a later mismatch on the same build_id raises rather than overwriting). No manifest supplied, or an older header/manifest predating the field, persists nothing (capability-unknown by design, never inferred).

**M3** -- `tools/bigcherry/tuning/signature_capabilities.py`'s `hip_required_capabilities()`: explicit per-op-class allowlist (MUL_MAT/MUL_MAT_ID -> CORE only, with the round-8 defensive `glu_op==0` check confirmed structurally unreachable today but kept for future-refactor safety; GLU -> all four HI118 presence capabilities regardless of current content-flag values -- the central `zero-and-known != zero-and-unknown` distinction; RD17/RD12's unrepresented domains fail closed pending HI120; everything else `UnsupportedSignatureDomain`). Deliberate, flagged scope decision: does NOT reimplement the real C++ canonical-signature blake2b digest in Python (round 9's `canonical_signature_digest()` suggestion) -- this session's own established practice (never trust a Python-reimplemented digest; always defer to real C++ output, per `hi80_generate_correctness_evidence.py`'s `_observed_signature_hex()` pattern) was judged to make that specific piece of round 9 more risk than safety.

**M4** -- `tools/bigcherry/tuning/replay_projection.py`'s `project_measurements()`: verifies source build's DB-attested capability row against the measurements header, verifies the target manifest's `producer_capabilities` against the target vendor_root's own live declaration (catching a generated-from-wrong-source-root manifest), and requires BOTH source AND target to hold each row's `hip_required_capabilities()` result (round 9's self-review strengthening) before retaining a row unmodified. Output feeds directly into the completely UNCHANGED `replay.build()`/`read_cache()` -- real end-to-end test confirms `REPLAY_VERSION`/`ENT_SIZE` untouched. New CLI command `bigcherry project-replay` (`cli/tuning.py` + `cli/main.py`).

**Tests**: `test_capability_mask.py` (27), `test_hip_producer_capabilities.py` (9), `test_dispatch_db_capabilities.py` (10), `test_signature_capabilities.py` (16), `test_replay_projection.py` (8), plus real fixes/extensions to `test_dispatch_abi_agreement.py`, `test_hi67_correctness_evidence_schema.py`, `test_vk_autotune_types.py`, `test_hi99_tuner_config_macro.py`, `test_inventory.py` (7 new capability-persistence tests), `test_replay_inspect.py`.

**Committed**: `0ed55e3` (M0/M1/M2-SQL) and folded into the account owner's own `0d99330` ("tooling cleanup", concurrent with this session's live test/package reorganization) for M2-Python/M3/M4/CLI -- both on `tuning-code-rebase`, both pushed to origin. Verified post-commit: full offline suite green (2099 passed, 2 skipped, 133 subtests, zero failures).

**Deliberately out of scope, unchanged from round 9**: `replay.py`, `signature_mapping.py`'s existing schema-2 guard, the production C++ replay/dispatch/resolver hot path, replay v5/`ENT_SIZE=93` -- all confirmed untouched. HI120 (RD17/RD12 semantic-content fixes) remains the real prerequisite before GLU-domain rows can ever pass M3 for those specific sub-cases; not attempted this round.

## ROUND 9 (final consolidated design, 2026-08-26): NORMATIVE SPEC -- READY TO IMPLEMENT

dev-gpt-agent produced the complete, amended, self-validated implementation document (folding round 8's authority fix directly into round 7 rather than leaving two deltas to merge by hand) and then independently re-reviewed its own output against rounds 1-8 before handing it back. I independently re-verified its single most safety-critical new claim (the 3rd `ggml_hip_make_signature()` call site's real eligibility gating) against actual source before accepting it -- CONFIRMED exactly as claimed: `hip-autotune-transform.cu`'s `signature_from_ctx()` passes `ids=nullptr, fusion=nullptr` (line 54-56) with an explicit comment 'a fused pattern is never eligible for rewriting in the first place'; `ggml_hip_transform_signature_is_eligible()` (line 341-353) rejects both `sig.fusion != GGML_HIP_FUSION_NONE` and `sig.flags & GGML_HIP_SIG_HAS_IDS` before any transform occurs. So the three-case allowlist (MUL_MAT/MUL_MAT_ID/GLU) is genuinely complete and fail-closed -- no 4th rule needed for this call site.

**THIS DOCUMENT (the full text below, round 9) IS THE NORMATIVE M0-M4 SPECIFICATION. Rounds 1-8 remain as decision history/rationale only, non-normative wherever this document conflicts with them.**

### Locked architecture (unchanged from rounds 1-8, restated as the final frame)
Semantic content (hashed) vs. producer capabilities (backend-scoped 128-bit compatibility metadata, NOT hashed) vs. measurement DB (durable evidence + capability provenance) vs. M4 (offline target-specific projection) vs. production (existing replay v5/ENT_SIZE=93/resolver, completely unchanged). `SIGNATURE_IDENTITY_EPOCH=2` frozen; never bumped for additive/scoped semantics, new capabilities, or V1->V2 corrections. Capability IDs 0-127 per backend, append-only, never reused; a buggy evaluator gets a new `*_V2` id, `*_V1` is never redefined. HIP and Vulkan share the `CapabilityMask128` mechanism but have fully independent bit namespaces. Missing capability provenance = UNKNOWN, never inferred from schema_version or any other proxy, and not M4-reusable.

### Exact file list (round 8's authority fix now folded directly in, not left as a separate delta)
| File | Action | Stage |
|---|---|---|
| tools/bigcherry/tuning/dispatch_abi.py | `SIGNATURE_IDENTITY_EPOCH=2`; `SIGNATURE_SCHEMA_VERSION` becomes a compatibility alias; frozen-epoch policy | M0 |
| src/ggml/src/ggml-cuda/hip-autotune-types.h | Epoch policy comments; add `GGML_HIP_PRODUCER_CAPABILITIES_LO/HI` explicit source-owned declaration | M0/M1 |
| tools/tests/test_dispatch_abi_agreement.py | Keep C++/Python/SQL epoch=2 agreement; assert the compatibility alias | M0 |
| tools/bigcherry/tuning/capabilities.py | NEW: generic backend-neutral `CapabilityMask128` | M1 |
| tools/bigcherry/tuning/hip_capabilities.py | NEW: immutable `HipCapability` IDs + explicit declaration PARSER (reads the #define, does NOT infer from evaluator behavior) | M1 |
| tools/bigcherry/tuning/catalog.py | Read the declared mask from the exact materialized root; fold into manifest, `manifest_hash()`, `build_descriptor()` | M1/M2 |
| src/ggml/src/ggml-cuda/hip-autotune-record.cpp | Emit compiled `producer_capabilities` in the JSON header (metadata only) | M2 |
| src/ggml/src/ggml-cuda/hip-autotune-tuner.cu | Emit compiled `producer_capabilities` in the measurements JSON header (metadata only) | M2 |
| sql/dispatch-db.sql | Schema 7: add backend-scoped `build_capability`; fix the accidentally-global winner/vk_winner indexes | M2 |
| sql/migrations/0007_producer_capabilities.sql | NEW explicit, transactional, schema-guarded v6->v7 migration | M2 |
| tools/bigcherry/tuning/inventory.py | `CURRENT_DB_SCHEMA_VERSION="7"`; verify full provenance+capability attestation before persisting; never accept a caller-supplied mask | M2 |
| tools/bigcherry/tuning/signature_capabilities.py | NEW: `canonical_signature_digest()` + fail-closed `hip_required_capabilities()` | M3 |
| tools/bigcherry/tuning/replay_projection.py | NEW: `project_measurements()` -- filters an existing measurements JSONL, hands off to UNCHANGED `replay.build()` | M4 |
| tools/bigcherry/cli/tuning.py + cli/main.py | Add/register the M4 `project-replay` CLI command | M4 |
| tools/tests/test_capability_mask.py, test_hip_producer_capabilities.py, test_dispatch_db_capabilities.py, test_signature_capabilities.py, test_replay_projection.py | NEW, one per stage | M1-M4 |

**Explicit non-changes** (only 3 small C++ METADATA edits total, none touching production hot paths): `tools/bigcherry/tuning/replay.py`, `signature_mapping.py` behavior, `hip-autotune-replay.cpp/.h`, replay v5, `ENT_SIZE=93`, production resolver/kernel-launch/dispatch hot path all UNCHANGED.

### Capability-mask authority chain (round 8's fix, now the only mechanism -- no behavioral source inspection anywhere)
```
GGML_HIP_PRODUCER_CAPABILITIES_LO/HI  (explicit #define in hip-autotune-types.h, source-owned)
  -> hip_capabilities.load_declared_producer_capabilities(root)  [reads the DECLARATION, never infers from behavior]
  -> catalog.py folds it into the manifest + manifest_hash() + build_descriptor()
  -> the SAME compiled binary emits the SAME declaration in its own JSON header (record + tuner)
  -> inventory.load_measurements() requires header.producer_capabilities == manifest.producer_capabilities
     (alongside the pre-existing source_revision/manifest_hash/build_descriptor_hash checks)
  -> only then: INSERT INTO build_capability(build_id, 'hip', mask.to_bytes())
```
No `load_measurements(..., producer_capabilities=...)` parameter, no update-capabilities CLI, no inference from schema_version/commit-date/anything else. Missing/unverified manifest binding = no `build_capability` row = simply not M4-reusable (fails closed, not defaulted).

### Exact schema 7 (real SQL)
```sql
CREATE TABLE build_capability (
    build_id              INTEGER NOT NULL REFERENCES build(build_id),
    backend               TEXT    NOT NULL,
    producer_capabilities BLOB    NOT NULL,
    PRIMARY KEY (build_id, backend),
    CHECK (length(producer_capabilities) = 16)
);
```
Bundled real bug fix (verified: `winner`'s own row constraint is build-scoped `UNIQUE(build_id, hardware_id, objective, dispatch_digest)`, but `winner_dispatch_idx`/`vk_winner_dispatch_idx` are separately, incorrectly GLOBAL `UNIQUE(dispatch_digest, objective)` indexes -- replaced with ordinary non-unique lookup indexes). `sql/migrations/0007_producer_capabilities.sql` is transactional, schema-guarded (fails closed unless exactly v6), drops+recreates both indexes, bumps `schema_meta.schema_version` 6->7, ends with `PRAGMA foreign_key_check`. No capability backfill for historical rows -- absence = UNKNOWN, by design (round 4's dropped-S6 finding).

### hip_required_capabilities() -- complete final rule table
Preconditions (any failure -> `UnsupportedSignatureDomain`): `schema_version==2`, well-formed types, op resolves via `signature_mapping.load_ggml_op_names()`, no unknown future flag bits.
- **MUL_MAT**: requires `fusion==NONE, glu_op==0, HAS_IDS==0`, all 4 HI118 aux flags `==0` -> `CORE_SIGNATURE_V1`. The `glu_op==0` check is explicit defense-in-depth (VERIFIED structurally unreachable today, kept for future-refactor safety per round 8).
- **MUL_MAT_ID**: same but `HAS_IDS==1` -> `CORE_SIGNATURE_V1`.
- **GLU**: requires ALL FOUR HI118 presence capabilities (`X_BIAS/GATE_BIAS/X_SCALE/GATE_SCALE_PRESENCE_V1`) REGARDLESS of current content-flag values (the central distinction: `flag=0 + capability present -> known absent`; `flag=0 + capability absent -> unknown`, never inferred as compatible). Until HI120: X_SCALE/GATE_SCALE-actually-present and RD12's dst_gate domain both fail closed as `UnsupportedSignatureDomain`.
- **Every other op**: `UnsupportedSignatureDomain`, no fallback.
- **3rd call site (hip-autotune-transform.cu)**: VERIFIED needs no 4th rule -- its eligibility gate (`ggml_hip_transform_signature_is_eligible`) already rejects `fusion!=NONE` and `HAS_IDS` before any transform runs, so it can only ever produce signatures the MUL_MAT/CORE rule already covers.

### M4 -- `project_measurements()`, exact algorithm
Validates source build's measurement-header provenance against its DB row, requires exactly one `build_capability` row, requires the artifact's own reported mask matches the DB-attested mask, does the same for the target manifest/declaration. For every result row: recomputes `canonical_signature_digest()` and requires it matches the row's own stored digest (round-9's first added strengthening -- prevents a row's canonical content from silently disagreeing with its persisted digest), computes `required = hip_required_capabilities(...)`, and requires BOTH `source_caps.contains(required)` AND `target_caps.contains(required)` (round-9's second added strengthening -- a target that itself can't distinguish the relevant semantics must not receive the row either, even if the source could). Retained rows are copied byte-for-byte unmodified. Output feeds directly into the existing, unchanged `replay.build()` -- production v5/ENT_SIZE=93/resolver untouched.

### Four stage gates (final, round 8's M2 replacement included)
- **M1**: declared mask round-trips exactly (`0000000000000000000000000000001f` for today's real HEAD); an unknown/undeclared bit raises `HipCapabilityError`.
- **M2 (strengthened per round 8)**: source root A declares mask A, root B declares mask B; generate the manifest from B; have the measurement artifact report compiled mask A (otherwise internally consistent) -- ingest MUST reject and roll back, no `build_capability` row written. This is the case round 7's original gate (pure post-hoc tampering) could not catch.
- **M3**: plain MUL_MAT/CORE-only -> ACCEPT; GLU/all-HI118-flags-zero/CORE-only -> REJECT; same GLU with all 4 HI118 caps -> ACCEPT; MUL_MAT with glu_op!=0 -> `UnsupportedSignatureDomain`.
- **M4**: source caps=CORE only, measurements has a plain MUL_MAT (S1) and a zero-flag GLU (S2), target caps=CORE+all 4 HI118 -- projection retains S1 byte-for-byte, omits S2 with an explicit reason, and the projected file feeds cleanly into unchanged `replay.build()` producing valid v5 output.

### Implementation order (P0-P11, offline-first until P9 passes)
```
P0 normalize this doc as normative  P1 M0  P2 M1 (mask+registry+declaration)  P3 catalog binding
P4 compiled producer attestation (record+tuner headers)  P5 schema-7+migration+index fix
P6 M2 ingest (all provenance checks, idempotent persistence, no backfill)  P7 M3
P8 M4 library  P9 M4 CLI  P10 full offline regression  P11 real HIP smoke test end-to-end
```
Do not touch the real working dispatch_db until P1-P9 pass against throwaway fixtures.

### Self-validation (dev-gpt-agent's own re-review of this consolidated version, independently spot-checked)
Confirmed clean: round 8's authority fix fully merged (no behavioral regex remains anywhere); round 6's 'M0-M4 is C++-free' claim explicitly corrected to 'three small C++ METADATA edits, zero production hot-path impact'; round 8's M2 gate replaces round 7's weaker one; the 3rd call site is explicitly handled (independently VERIFIED above, not just trusted); HI120 correctly stays de-escalated (fails closed in M3, doesn't block M0-M4 infrastructure); no S6/backfill accidentally reintroduced; `signature_mapping.py`/replay v5 confirmed untouched; the winner-index fix wording corrected to match the real schema. Two new strengthenings added during self-review, both verified non-conflicting with rounds 1-8: canonical-digest recheck before trusting M3, and requiring the TARGET (not just source) to hold `required` capabilities in M4.

**FINAL VERDICT: CLEAN / READY TO IMPLEMENT, P0 through P11.**

## ROUND 8 (final critical review/validation, 2026-08-26): CHANGES-REQUIRED -- one real blocker, one amendment, otherwise READY

dev-gpt-agent's honest final-gate review of the complete 7-round document (all decisions verified, not rubber-stamped). Two of its claims independently re-verified against real source before recording:

**Verified, with a nuance**: the `fusion==NONE` + `glu_op!=NONE` combination it flagged as a required defensive check IS structurally unreachable in the current real code -- confirmed against `ggml_hip_fusion_kind()` (hip-autotune-signature.cpp:224-240) and `sig.glu_op`'s own assignment (lines 285-287): `glu_op` is only ever set nonzero when `fusion->gate != nullptr`, and that same condition always makes `ggml_hip_fusion_kind()` return GATE or GATE_BIAS, never NONE. So this is NOT a live bug today -- it's a defense-in-depth assertion worth keeping in `required_capabilities()` against a FUTURE refactor that decouples glu_op/fusion-kind assignment, not a currently-exploitable gap. Recorded accurately as defensive, not corrective.

**Verified**: `ggml_hip_make_signature()` call sites -- confirmed 2 in hip-autotune-dispatch.cu (lines 1127, 1214) as gpt stated, PLUS a third in hip-autotune-transform.cu:54 gpt did not mention (scoped its claim correctly to 'in hip-autotune-dispatch.cu', but M3 implementation should explicitly check this third site's dst->op range too before treating the 3-case allowlist as complete).

### Consistency check: PASS
All 7 rounds' reversals confirmed explicitly reconciled (cap1 split, A->B migration, S0-S5-runtime->M0-M4-offline, HI120 hard-prerequisite->unsupported-domain-fail-close, unqualified column->backend-scoped table, S6 dropped, signature_mapping.py untouched). Non-blocking cleanup accepted: add a note that **Round 7 is the authoritative implementation contract; rounds 1-6 are decision history, non-normative where superseded** -- prevents a future implementer from following stale S1/S4/replay-v6 material by accident.

### required_capabilities() strengthened (accepted, minor addition to round 7's rule set)
```
MUL_MAT / MUL_MAT_ID:
    require fusion == NONE
    require glu_op == NONE          <- NEW explicit defensive check (see nuance above)
    require HI118 aux flags == 0
    otherwise UnsupportedSignatureDomain
GLU:
    validate known real fused combination
    require all four HI118 presence caps
    unsupported HI120 semantic modes -> UnsupportedSignatureDomain
EVERY OTHER op (including anything from the 3rd call site not yet covered):
    UnsupportedSignatureDomain
```
Correctly biased: an unsupported real path gets unnecessarily rerun (safe, annoying) rather than incorrectly reused (unsafe) -- explicitly confirmed as the right first-release bias.

### THE BLOCKER: source-pattern-matching is not strong enough capability authority -- amend before M0
Round 7's `derive_producer_capabilities(root)` (grant a capability when Python's source-text inspection sees both the enum bit and the matching evaluator assignment) has a real false-positive failure mode, distinct from the safe false-negative case:
- Refactor moves/renames the assignment -> parser misses it -> false NEGATIVE -> unnecessary rerun -> SAFE.
- Assignment's exact text still matches, but the surrounding predicate became wrong / the assignment became unreachable / the value is later overwritten / a helper's semantics changed / the wrong fusion object is inspected -> parser still grants the capability -> false POSITIVE -> **unsafe measurement reuse**. Unacceptable, since a capability bit IS a permission grant.

**Required amendment**: replace behavioral source-text inspection with an explicit, source-OWNED, self-declared capability constant -- NOT full C++ runtime producer_capabilities[128] machinery, just a small compile-time declaration:
```c
// hip-autotune-types.h -- amends round 6's 'no C++ code' claim with this one small addition
#define GGML_HIP_PRODUCER_CAPABILITIES_LO UINT64_C(0x000000000000001f)
#define GGML_HIP_PRODUCER_CAPABILITIES_HI UINT64_C(0x0000000000000000)
```
Python parses this DECLARATION (not behavior) from the materialized source root. The compiled record/tuner producer ALSO emits these same two values in its JSON header. New authority chain:
```
source-owned capability declaration (the #define above)
  -> Python manifest generation reads the declaration (not source behavior)
  -> manifest/hash/descriptor bind it
  -> compiled producer emits the SAME declaration in its own header
  -> ingest requires: producer header mask == manifest mask (closes a real second gap too --
     see M2 gate below)
```
Why this is strictly better: a semantically-neutral refactor no longer changes capability detection at all (the declaration doesn't move just because code around it does); when evaluator semantics genuinely change or a bug is found, the declaration itself changes from CAP_X_V1 to CAP_X_V2, exactly matching the already-designed governance model (round 4's Finding 5). Focused signature-construction tests then validate the declaration is TRUTHFUL (does the compiled behavior really match what's declared) -- regex is no longer being asked to prove semantic correctness by itself, only to read an explicit assertion and the tests separately verify that assertion holds.

This does not touch production dispatch, replay, resolver, kernel launch, or any hot path -- it is a metadata-only compile-time constant, consistent with the spirit (if not the letter) of 'no C++ runtime capability code'.

### M2 gate strengthened (closes a second real gap the original gate missed)
Original round-7 gate (tamper the manifest's capability mask post hoc, expect rejection) only proves post-generation tampering resistance. It does NOT catch: manifest generated from source root B, but the actual compiled binary was built from source root A, while manifest B remains internally self-consistent -- with source-inspection-only authority, ingest cannot detect this mismatch at all. The compile-time self-report above closes this directly. **New highest-signal M2 gate**: root A declares capability mask A, root B declares mask B; generate the manifest from root B; have the (real or simulated) producer artifact/header report compiled mask A; attempt ingest -> must REJECT, no `build_capability` row written. Also require ALL of: `header.producer_capabilities == manifest.producer_capabilities`, `header.manifest_hash == manifest.manifest_hash`, `header.build_descriptor_hash == manifest.build_descriptor.descriptor_hash`.

### Final sign-off
**NOT ready to implement exactly as round 7 specified.** Single blocker: replace regex/behavioral source inspection as capability authority with the explicit source-owned declaration above, cross-checked against the manifest at ingest. Once amended, dev-gpt-agent would sign off. Everything else confirmed READY as-is: M0 frozen identity epoch, 128-bit/backend-separated model, schema-7 `build_capability` storage, M3's fail-closed allowlist (with the glu_op==NONE addition), M4's offline projection into unchanged replay v5, production/replay hot path completely unchanged. Recommendation: fix the authority contract in the document BEFORE starting M0, even though M0 itself is technically independent of it, so implementation begins from one genuinely frozen specification rather than changing M1/M2 out from under it partway through.

## ROUND 7 (implementation plan, 2026-08-26): concrete, file-level, GO

One correction accepted before the file list: `build` is a single table SHARED by both HIP (`winner`) and Vulkan (`vk_winner`) -- confirmed real (sql/dispatch-db.sql: both winner and vk_winner reference build_id/hardware_id against the same `build` table). An unqualified `build.producer_capabilities` column would be ambiguous between HIP/Vulkan interpretations -- use a backend-scoped `build_capability(build_id, backend, producer_capabilities)` table instead.

**Bonus real finding, verified**: `winner_dispatch_idx` (sql/dispatch-db.sql:300) is `UNIQUE(dispatch_digest, objective)` -- a GLOBAL index, NOT build-scoped -- while `winner`'s own table-level constraint (line 251) IS build-scoped (`UNIQUE(build_id, hardware_id, candidate_id, objective, stage, dispatch_digest)`). This means two different builds can never both hold a winner row for the same dispatch today -- a genuine pre-existing latent bug, unrelated to capabilities but caught incidentally while designing schema 7. Fix bundled into the same migration (see below). Same issue confirmed on `vk_winner_dispatch_idx`.

### Exact file list
| File | Action | Stage |
|---|---|---|
| tools/bigcherry/tuning/dispatch_abi.py | Edit: add `SIGNATURE_IDENTITY_EPOCH = 2`, keep `SIGNATURE_SCHEMA_VERSION` as a compatibility alias, rewrite bump policy comment | M0 |
| src/ggml/src/ggml-cuda/hip-autotune-types.h | Edit comments ONLY: schema 2 is a frozen identity epoch, additive/scoped semantics never bump it. No compiled behavior change. | M0 |
| tools/tests/test_dispatch_abi_agreement.py | Edit: assert identity epoch stays C++/Python/SQL == 2 | M0 |
| tools/bigcherry/tuning/capabilities.py | NEW: backend-neutral `CapabilityMask128` (`to_bytes`/`from_bytes`/`contains`) | M1 |
| tools/bigcherry/tuning/hip_capabilities.py | NEW: `HipCapability` IntEnum (0-4), `derive_producer_capabilities(root)` -- source-derived, immutable/V2 governance | M1 |
| tools/bigcherry/tuning/catalog.py | Edit: derive HIP caps from the exact materialized source root, embed the 32-hex mask in the HIP manifest, fold into `manifest_hash()`/`build_descriptor()` (catalog.py already generates `hip-autotune-build-hash.h` embedding manifest_hash -- confirmed real at catalog.py:1591/1389 -- this reuses that existing attestation mechanism, no new one invented) | M1/M2 |
| sql/dispatch-db.sql | Edit: schema 7; add `build_capability`; fix the global-vs-build-scoped winner index bug above as directly-related cleanup | M2 |
| sql/migrations/0007_producer_capabilities.sql | NEW: v6->v7 additive migration (transactional, foreign_key_check, matches existing migration file pattern) | M2 |
| tools/bigcherry/tuning/inventory.py | Edit: schema 7; persist ONLY the manifest-attested HIP capability mask; `load_measurements()` never accepts a caller-supplied mask | M2 |
| tools/bigcherry/tuning/signature_capabilities.py | NEW: `hip_required_capabilities()`, fail-closed domain validation, predicate-based (not a giant op/fusion/glu_op Cartesian dict) | M3 |
| tools/bigcherry/tuning/replay_projection.py | NEW: `project_measurements()` -- filters an existing measurements JSONL by source/target capability compatibility, feeds the result into the EXISTING unchanged `replay.build()` | M4 |
| tools/bigcherry/cli/tuning.py + cli/main.py | Edit only at M4 completion, to expose the projection command | M4 |
| tools/tests/test_capability_mask.py, test_hip_producer_capabilities.py, test_dispatch_db_capabilities.py, test_signature_capabilities.py, test_replay_projection.py | NEW, one per stage | M1-M4 |

**Explicit non-changes** (confirms M0-M4 really is C++/runtime-free): `tools/bigcherry/tuning/replay.py` UNCHANGED, `tools/bigcherry/tuning/signature_mapping.py` UNCHANGED (keeps its current schema-2 guard exactly as-is), production C++ replay/dispatch path UNCHANGED, replay v5/`ENT_SIZE=93` UNCHANGED.

### M2's capability-mask authority chain (the one hard requirement, now concretely answered)
Option (c) confirmed, source-derived pre-build manifest attestation -- NOT keyed on `source_revision` (that's upstream llama.cpp's own SHA, doesn't describe BigCherry's overlay/patch semantics) and NOT a new C++ field:
```
exact materialized source root
  -> hip_capabilities.derive_producer_capabilities(root)   [inspects real source: for each HI118 bit,
                                                             requires BOTH the enum definition AND the
                                                             corresponding ggml_hip_make_signature()
                                                             evaluator assignment to actually exist]
  -> hip-autotune-manifest.json producer_capabilities
  -> manifest_hash() + build_descriptor() include the capability mask
  -> existing generated hip-autotune-build-hash.h embeds manifest_hash + descriptor_hash (real,
     existing mechanism, confirmed at catalog.py:1591)
  -> compiled tuning binary
  -> existing measurements header emits those compiled identities
  -> inventory.load_measurements() VERIFIES manifest/header binding before trusting anything
  -> build_capability(build_id, "hip", mask)
```
Hard API rule for `load_measurements()`: NO `producer_capabilities=` parameter, NO DB update-capabilities CLI, NO deriving caps from `signature_schema`, NO deriving from commit date. Verification requires ALL of: `header.source_revision == manifest.source_revision`, `header.manifest_hash == manifest.manifest_hash`, `header.build_descriptor_hash == manifest.build_descriptor.descriptor_hash`, recomputed `manifest_hash == manifest.manifest_hash`. Missing/unverified manifest => no `build_capability` row => that build's measurements simply cannot be M4-projected (fails closed, not defaulted).

### Migration ordering (offline-first, same discipline as HI119's own harness)
```
P0  M0 epoch policy/comments/tests
P1  CapabilityMask128 + HIP registry/source derivation -- pure Python, no artifacts/DB touched
P2  Add producer_capabilities to generated manifest/manifest_hash/build_descriptor -- still synthetic
P3  Schema-7 + inventory persistence -- tested exclusively on temporary/throwaway DBs
P4  hip_required_capabilities() -- pure Python + synthetic canonical signatures
P5  replay_projection.py -- synthetic old/new producer fixtures, feed into UNCHANGED replay.build()
P6  Full offline suite
P7  Only now migrate/recreate the real working dispatch_db
P8  Next real tune automatically creates capability-attested data
```
`build_capability` migration ships with explicit NO BACKFILL: existing build rows deliberately get no capability row (old row + no attestation = unknown = not M4-reusable) -- do NOT infer schema1->CORE or schema2->CORE+HI118 retroactively; preserving today's data was never a stated requirement (matches round 4's dropped-S6 finding).

### Highest-signal gate per stage (one each, not exhaustive suites)
- **M1**: take a source fixture matching current HEAD, assert caps 0-4 present; then delete JUST the real `if (fusion->x_scale != nullptr) flags |= GGML_HIP_SIG_FUSION_X_SCALE;` line and re-derive -- `FUSION_X_SCALE_PRESENCE_V1` must disappear while the other bits remain. Proves the registry is genuinely source-derived, not ceremonial.
- **M2**: generate manifest A + measurement header A, load (stores exact BLOB16); then modify the manifest's capability mask post hoc while keeping header A -- load must FAIL and roll back with no changed capability row. Proves Python cannot re-label historical measurements after the fact.
- **M3**: one table-driven test -- plain MUL_MAT/CORE-only -> ACCEPT; GLU with all HI118 flags zero/CORE-only producer -> REJECT; same GLU with producer holding all 4 HI118 caps -> ACCEPT. Directly proves the central distinction: zero-and-known != zero-and-unknown.
- **M4**: source measurements has a plain MUL_MAT (S1) and a fused GLU (S2), source caps=CORE only, target caps=CORE+HI118 -- projection must retain S1 byte-semantically, omit S2 with an explicit missing-capability reason, and the projected file must feed cleanly into existing `replay.build()` producing valid v5 output.

### required_capabilities() location and shape
Standalone module `tools/bigcherry/tuning/signature_capabilities.py`, NOT added to signature_mapping.py (reuses `signature_mapping.load_ggml_op_names()` for op resolution rather than duplicating vendor-enum parsing, but keeps its own applicability logic separate). Predicate-based, not a Cartesian (op,fusion,glu_op)->mask dict -- e.g. for GLU: require X_BIAS/GATE_BIAS/X_SCALE/GATE_SCALE presence-capability REGARDLESS of whether those content bits are currently 0 or 1 (a zero content bit still needs authoritative knowledge that the producer evaluated it -- `if flags & X_BIAS: require X_BIAS_CAP` is explicitly called out as WRONG; correct form is `if x_bias_semantics_are_applicable_to_this_signature_class: require X_BIAS_CAP`). Until HI120 lands: X_SCALE/GATE_SCALE actually-present and RD12's fusion=GATE+glu_op=NONE domain both return `UnsupportedSignatureDomain` conservatively.

**Implementation order to actually start with**: M0 -> M1 (source-derived mask) -> catalog binding -> M2 (schema/persistence) -> M3 (applicability) -> M4 (measurements projection) -> existing replay.py (untouched). No runtime cutover, no C++ changes beyond M0's comment edit.

## ROUND 6 (final convergence, 2026-08-26): all three legs (now/later/architecture) tested directly -- M0-M4 confirmed, design phase closed

User reframed decision criteria: usefulness now, usefulness later, architectural soundness -- not pure scale-proportionality. Answered directly, honestly (not oversold):

### Leg 1 -- Helps now: YES, but modestly, and NOT what it doesn't do
Explicitly does NOT: make schema-1 rows exact-match schema-2 digests, avoid HI119's already-required schema-2 hardware rerun, improve current kernel performance, or by itself justify replacing HI119's `schema_version == 2` guard (that guard stays as-is; migrating it is separate future work only if a concrete reason emerges). Real day-1 value: the schema-2 hardware measurements HI119 still needs to regenerate (see open Brutus rebuild item) become, once M0-M4 exists, selectively-reusable assets instead of being one future semantic change away from ANOTHER blanket invalidation. Day-1 utility = protecting the value of the NEXT real campaign, not recovering the current one.

### Leg 2 -- Easier later: YES, materially, worked end-to-end against this session's own real experience
Concrete comparison using HI120's real RD17 fix as the worked example:
**Without M0-M4 (what this session actually did for v1->v2):** discover ambiguity -> bump SIGNATURE_SCHEMA_VERSION globally -> update C++ constant + Python constants (4 sites, now 1 via dispatch_abi) + SQL bootstrap + replay/cache expectations + fixtures/tests + historical readers -> EVERY signature digest changes -> existing replay caches RERUN_REQUIRED -> re-measure every relevant workload signature INCLUDING operations completely unrelated to the change -> regenerate correctness/promotion/replay artifacts. (This session's own real commit f23c99c is the concrete evidence for this cost.)
**With M0-M4 (RD17 fix under the new model):** add real semantic content bit (`GGML_HIP_SIG_X_SCALE_CHANNEL_DST`) -> do NOT bump the now-frozen identity_epoch=2 -> allocate producer capability `X_SCALE_MODE_V1` -> new producer declares it -> update `required_capabilities()` for signatures where scale mode could matter (unrelated signatures unaffected) -> RD17's own new signatures get new digests, unrelated MUL_MAT signatures keep byte-identical canonical identity -> record/tune only what actually changed -> export-time filter (M4) keeps compatible old measurements, omits/reruns only ambiguous ones -> emit an ORDINARY unchanged v5 cache, production resolver untouched.
The irreducible complexity (thinking carefully about semantic applicability) doesn't disappear -- what disappears is the unrelated migration/remeasurement work. This is a genuine change in the INVALIDATION UNIT: from 'whole signature universe' to 'semantic domain actually affected', not just complexity relocated elsewhere.

### Leg 3 -- Architecturally better: YES, strongly, and it's doing real explanatory work (not a hedge)
Four genuinely different facts were being conflated under one `schema_version` field: semantic content ('x_scale_channel_dst IS true'), producer knowledge ('this producer knew how to distinguish scale modes'), physical replay encoding (v5 layout), and DB structure (schema 6). The two-axis model also represents a real state the current model structurally CANNOT: `flag=0 + producer knows the evaluator -> known absent` vs `flag=0 + producer lacks the evaluator -> unknown` -- exactly the real bug HI118/HI119 exposed this session. This distinction is what makes leg 2's safer workflow possible -- 'architecturally better' cashes out concretely, it isn't decorative.

### One final simplification accepted: keep capabilities OUT of C++/runtime entirely for now
Since M0-M4 explicitly never touches replay/runtime, a C++ `producer_capabilities[128]` abstraction would be premature -- add C++ capability declarations/parity only when C++ actually consumes or emits them (i.e. only if/when the deferred full S0-S5 runtime scope is ever triggered). Final locked scope:
```
M0 -- Identity policy: freeze semantic identity epoch=2; update comments/tests so additive/scoped
      semantic content changes do NOT imply an epoch bump (only genuine canonicalization-algorithm
      breaks do).
M1 -- Python/offline capability model: append-only 128-bit registry, cap0 (CORE_SIGNATURE_V1) +
      the 4 split HI118 bits, immutable CAP_X_V1->CAP_X_V2 correction rule, mask helpers.
M2 -- Producer persistence: build.producer_capabilities BLOB(16), bound deterministically to the
      EXACT source/build identity that produced the measurements (Python must not be able to
      arbitrarily self-declare capabilities post hoc -- this is M2's one hard requirement).
M3 -- Offline required_capabilities(canonical_signature): fail-closed on unsupported/incomplete
      semantic domains, exhaustive truth-table tests, pure Python, no runtime lookup involvement.
M4 -- Export-time compatibility projection: for each existing measurement, exact digest match AND
      producer_caps superset required_caps, else omit/rerun; output is an ORDINARY unchanged v5
      replay cache -- production C++ resolver/loader/wire format completely untouched.
```
HI120's RD17/RD12 relationship unchanged from round 5: required_capabilities() simply fail-closes (returns unsupported) for those semantic domains until HI120 lands; does not block M0-M4's own construction.

**Final verdict, explicit: build M0-M4.** All three legs pass (now: modest but real; later: material, worked end-to-end; architecture: strong, non-decorative). Design phase is CLOSED as of this round -- dev-gpt-agent's own assessment: 'sufficiently converged... next round should be the concrete implementation plan: exact files, migration ordering, authority for deriving a build's capability mask, and tests/fail-closed cut points.'

## ROUND 5 (grounding, 2026-08-26): real value confirmed, verified against source; scope narrowed to offline-only (M0-M4)

User explicitly required this stay grounded in demonstrable value, not architecture for its own sake. Both load-bearing factual claims below independently VERIFIED against real source before being recorded:
- `GGML_VK_SIGNATURE_SCHEMA_VERSION 1` confirmed real at src/ggml/src/ggml-vulkan/bigcherry/vk-autotune-types.h:62 (with the same 'bump on hashed-field change' comment pattern HIP's version had before this session).
- HI105's real campaign numbers confirmed exactly as claimed in docs/planning/completed/hip-autotune/HI105.md: 29 real signatures, 95 candidates measured, 3 correctness-promoted MUL_MAT_ID candidates, 8-round interleaved real A/B: pp512 native 1974.32 vs replay 1972.99 t/s (-0.07%), tg128 72.620 vs 72.620 t/s (+0.0008%) -- both indistinguishable from measurement noise (~0.2-0.25% round-to-round stdev).

### Real, concrete value (not manufactured) -- what becomes possible that isn't safe today
1. **Selective measurement reuse**: today, ANY schema-adjacent change rejects every measurement globally. With capabilities: `required_caps(plain MUL_MAT) = CORE` means an old plain-MUL_MAT measurement stays usable across a bump that only adds fusion-presence knowledge; only rows whose op-class actually needed the new knowledge (e.g. fused GLU) get correctly rejected. This is the real fix for the exact thing HI119's v1->v2 bump had to do bluntly.
2. **Targeted evaluator revocation**: if a capability's detection logic (e.g. HI118's own X_BIAS/GATE_BIAS/X_SCALE/GATE_SCALE checks) is later found buggy, `CAP_X_V1 -> CAP_X_V2` invalidates only the dependent measurements -- today's only tool for this is another blanket schema bump.
3. **Durable multi-generation measurement store**: the DB can hold measurements from producers at different capability/knowledge levels simultaneously and let each consumer (a specific target build) select only what's semantically valid for it -- real reuse across sessions/builds, not just within one.
4. **Vulkan backend (strongest future-feature case)**: vk-autotune-types.h already mirrors HIP's OLD linear-schema-version design and is still unintegrated scaffolding (not yet operational). Adopting the two-axis model for HIP now, before Vulkan's runtime work goes live, avoids Vulkan accumulating the exact same debt HIP just had to pay down mid-session.

### What it does NOT do / is orthogonal to (confirmed explicitly, not just implied)
- Kernel A/B or canary rollout: BigCherry already has candidate identity, generations, per-entry provenance -- capabilities only matter there if the rollout ALSO changes signature interpretation, which most rollouts won't.
- Cross-hardware portability: capabilities describe producer CODE differences, not hardware differences -- an RX 7900 XTX result still can't be ported to a different hardware class via this mechanism; the existing hardware digest remains the real gate for that.
- Mixed-fleet single-shared-cache serving: possible, but only with the heavier per-entry runtime design (S3/S4) -- not needed if each target build gets its own target-specific replay export instead (see M4 below).

### Real cost being avoided, honestly bounded
Could NOT find a trustworthy committed GPU-hour/wall-clock figure for a full re-tune -- explicitly not claimed. What IS real: a blanket invalidation today costs re-measuring on the order of tens of signatures / low hundreds of candidate measurements per model campaign (HI105's real 29/95 numbers), not tens of thousands -- a real but bounded cost, not a catastrophic one at BigCherry's current scale. Also material to the honest cost/benefit: HI105's own 3 real promoted candidates moved production throughput by an amount indistinguishable from noise -- BigCherry has not yet demonstrated a case where preserving a large tuning estate was itself high-value; the redesign's value is more about correctness/architecture hygiene and unblocking future work cheaply than rescuing large sunk tuning investment today.

### Scope recommendation revised: implement M0-M4 (offline-only), defer replay-v6/runtime capability gating
dev-gpt-agent's round-5 conclusion: the full S0-S5 runtime redesign (replay v6, per-entry 128-bit masks, C++ loader changes, broad consumer migration) is more correctness-critical surface than current demonstrated pain justifies -- NOT a reversal on whether the underlying two-axis design is right (it isn't), but a scope call on WHERE to draw the implementation boundary right now. Revised smallest-useful-implementation, capturing the real value above without touching the production C++ resolver at all:
```
M0 -- Freeze signature identity epoch at 2. New policy: additive/scoped semantic fields do NOT
      bump epoch; epoch changes only for genuine incompatible canonicalization-algorithm changes.
M1 -- Capability vocabulary: append-only 128-bit registry, producer capability mask,
      immutable/versioned correction semantics (CAP_X_V1 -> CAP_X_V2 rule from round 4).
M2 -- Persist producer capabilities in DB/record metadata (build.producer_capabilities BLOB(16)).
M3 -- OFFLINE required_capabilities(canonical_signature): fail-closed, operates on DB/record
      canonical signature fields directly -- no runtime lookup involvement at all.
M4 -- Compatibility-FILTERING replay EXPORT: when producing a cache for a specific target build,
      keep only rows where (exact semantic digest) AND (producer_caps superset required_caps);
      omit/rerun everything else. The resulting cache is an ORDINARY v5 replay cache -- the
      production C++ resolver, wire format, and loader are UNCHANGED.
```
Key framing: **measurement DB = durable multi-generation knowledge store; replay cache = a cheap, target-specific PROJECTION of only the measurements safe for that one binary.** This captures the real value (selective reuse, clean revocation, multi-generation storage, Vulkan groundwork) while keeping the production resolver exactly as simple and fail-closed as it is today. Capability revocation works cleanly under this scope too: bump required_capabilities() to require V2, regenerate the export from the same DB, done -- no C++/wire format change needed.

Also revises HI120's relationship: de-escalate from 'global HI121 prerequisite' to 'must be fixed before RD12/RD17's specific semantic domains are allowed to EXPORT via M4's filter' -- required_capabilities() can simply return unsupported/fail-closed for those domains until HI120 lands, without blocking M0-M4's own construction.

**S1a/S2/S3/S4/S5 (the full runtime redesign) reclassified as a DEFERRED future item**, not dropped -- build only if a real requirement emerges for one shared replay cache to safely serve binaries at heterogeneous capability levels simultaneously.

## Still open for the next round

- Whether S0's RD17/RD12 audit should literally BECOME HI120 (rename/absorb) or stay a separate item HI121 depends on (dependency link, not scope merge) -- not yet decided.
- Exact BLOB(16) vs wider encoding for producer_capabilities at the DB/wire level given 128 bits is the target width but only 5 bits are meaningful today.
- Full S5 consumer inventory (which real call sites beyond HI119's own guard need migrating).
- Whether S6's rekey importer is worth building at all vs simply accepting v1 measurements as permanently non-reusable (low real cost today since HI108's actual production dispatches are schema-2-required anyway per Finding 1's own GLU case).

## ROUND 4 (adversarial, 2026-08-26): round 3 revised further -- S1 reordered, S6 dropped, capability revocation added as a real requirement

### Finding 1: S0/HI120 relationship clarified -- link, don't merge; HI121 only partially blocked

HI120 keeps ownership of the actual RD17/RD12 semantic-content fixes (its existing real scope), but is promoted from optional/if-promoted housekeeping to an explicit prerequisite gating HI121's S4 (compatibility cutover) specifically -- not all of HI121. Real dependency chain:
```
HI121 S0 (audit/spec) -> identifies the semantic gaps
HI120 -> implements the RD17/RD12 semantic-CONTENT fixes (not a capability-layer patch)
HI121 S1b (required_capabilities()) -> finalized only against the corrected content model
HI121 S4 (compatibility cutover) -> BLOCKED until HI120 + S0 both complete
```
HI121's registry/types/storage/wire plumbing (S1a, S2, S3) can proceed in parallel while HI120 is underway -- only S4 (and therefore S1b) is gated.

**Correction to round 3: fixing RD17 does NOT require a third epoch bump.** Add a semantic CONTENT bit (`X_SCALE_CHANNEL_DST`, describing what the dispatch actually is) plus a separate producer-knowledge bit (`X_SCALE_MODE_V1`, describing whether the producer correctly distinguishes the two modes) -- new RD17 signatures set the content bit (their digest correctly changes only for THEM), old producers lack X_SCALE_MODE_V1 (so their zero is correctly untrusted), and every UNRELATED signature keeps an identical digest. This is exactly the two-axis pattern applied correctly, and it's why epoch 2 can stay frozen under strategy (B) -- an epoch bump is only for genuine canonicalization-ALGORITHM breaks, not new independently-representable semantic dimensions. Same pattern applies to RD12's DST_GATE.

### Finding 2: S1 was mis-ordered -- split into S1a (safe now) / S1b (blocked on S0+HI120)

Withdrawn from round 3: 'a pure required_capabilities() implementation is safe to ship with no runtime effect.' Real hazard: even unused logic freezes incorrect applicability assumptions into tests/docs that are hard to dislodge later, and RD17 already proves today's applicability model is incomplete.
- **S1a (safe before S0 completes)**: capability bitset type, append-only allocation registry, C++/Python parity, subset/superset helper -- NO applicability predicate yet.
- **S1b (blocked until S0 + HI120's semantic-content fixes land)**: `required_capabilities(signature)` itself, its applicability truth table, exhaustive boundary tests. Made a direct dependency of S4.

### Finding 3: replay v6 BLOB(16)-per-entry confirmed as the right call, with real numbers

Current v5 `ENT_SIZE` confirmed real (replay.py:250, `assert ENT_SIZE == 93`) -- +16 bytes/entry is ~17% growth: 59 entries (HI101's real reference fixture) = +944 bytes, 1,000 entries = +16KB, 10,000 = +160KB, negligible next to model/runtime memory. Replay is not a per-launch hot path (cache loads once into runtime structures; dispatch resolution is then cached), so a more compact per-entry index-into-a-distinct-mask-table design would add real complexity (table bounds validation, extra wire format, another corruption/misassociation failure mode) for savings that don't matter at any realistic scale. Verdict: keep the simple inline 16-byte-per-entry representation; do not build the compact form unless a real cache reaches a scale where it matters (none currently does).

### Finding 4: S6 (legacy v1 rekey importer) DROPPED from scope, not merely deferred

Reversed from round 3. Two independent real reasons: (a) no demonstrated real v1 measurement is valuable enough to justify a migration subsystem -- HI108's actual fused-GLU case can't be rekeyed anyway (producer lacks required knowledge per Finding 1's own original counterexample); (b) a real structural blocker -- a bare replay cache stores only `signature_digest`/`dispatch_digest`, NOT the canonical signature fields a rekey would need to recompute a digest from, so rekey would require the original DB/record canonical JSON or equivalent provenance artifact, which most historical replay-only artifacts don't retain. Changed from 'S6 -- optional legacy-v1 rekey importer' to: **out of scope / future item, build only if a real v1 dataset with complete canonical provenance and demonstrated value turns up.** Strategy (B) does not depend on it.

### Finding 5 (NEW, strongest round-4 finding): capability claims need versioned correction semantics, or the design has no defined response to its own failure mode

A capability bit means 'producer implemented evaluator semantics CAP_X_V1', NOT 'producer was infallibly correct forever.' If HI118's own real X_BIAS/GATE_BIAS/X_SCALE/GATE_SCALE detection logic (or any future evaluator) is later found to have had a bug, the design as of round 3 had NO mechanism to un-trust measurements that already claimed that capability -- capabilities would just become permanently-trusted buggy claims, i.e. the exact same class of problem this whole redesign exists to solve, one level up.

**New governance rule, added as a hard requirement, not optional:** capability meanings are immutable once allocated (matches the existing append-only registry rule) -- a discovered evaluator bug is handled by (1) allocating a NEW corrected bit `CAP_X_V2` (never redefining `CAP_X_V1`), (2) `required_capabilities()` is updated to require V2 going forward, (3) V1 stays permanently tombstoned/historical, never redefined, (4) every V1-only row fails closed under the new requirement unless separately revalidated/re-attested. Deliberately conservative default: invalidate ALL V1-claiming measurements for that capability rather than building a fine-grained allow/deny list for 'only the actually-buggy subset' -- the existing per-entry provenance (manifest_hash, source_revision_digest, generation) already preserves the ability to do selective forensic recovery later if that ever proves worth building.

## Revised ordering (round 4)

```
S0   semantic-completeness audit/spec
S1a  capability registry/types only (no applicability logic)
HI120 semantic-content fixes (RD17 X_SCALE_CHANNEL_DST, RD12 DST_GATE) -- gates S1b/S4
S1b  required_capabilities() + applicability truth-table tests -- direct dependency of S4
S2   DB/record producer-capability persistence (build.producer_capabilities BLOB(16))
S3   replay v6 -- inline BLOB(16) per entry (confirmed right-sized, see Finding 3)
S4   atomic compatibility cutover -- BLOCKED on HI120 + S0 + S1b
S5   consumer migration
[S6 DROPPED -- see Finding 4]
```

## ROUND 3 (adversarial, 2026-08-25/26): round-2 proposal does NOT survive intact

dev-gpt-agent was explicitly instructed to attack, not refine, round 2. Verdict: the two-axis CONCEPT survives; the migration strategy, capability granularity, and the assumption that required_capabilities() alone is sufficient do NOT. Real, source-verified findings below (verified against actual hip-autotune-types.h before recording, not taken on faith):

### Finding 1 (CRITICAL, VERIFIED REAL): required_capabilities() cannot be correct until a semantic-completeness audit happens first

Concrete counterexample, confirmed against real source (hip-autotune-types.h lines 148-167, comments written earlier THIS session during HI118 work): patch 1207 (RD17, moe_topk_down_fold)'s `x_scale_channel_dst` mode changes x_scale's real semantics (indexed by destination channel/token, length dst->ne[1]) vs the upstream/pristine NVFP4 path's per-expert semantics (length src0->ne[2]) -- but the CURRENT signature's `GGML_HIP_SIG_FUSION_X_SCALE` flag (bit 9) does not distinguish these two modes at all. Two real operations with genuinely different semantics (per-expert scale vs per-token scale) can produce the SAME signature content (op=MUL_MAT_ID, fusion=NONE since ggml_hip_fusion_kind() only examines gate/bias not x_scale, X_SCALE flag=1) -- this is already a documented 'KNOWN REPO-WIDE GAP, not fixed here' in the real source, filed for 'whoever hardens 1207's own signature coverage' -- i.e. **this is HI120's own scope**, not a hypothetical.

This means: producer_capabilities CANNOT repair a signature that already aliases two semantically different operations under one digest -- no amount of correct capability-gating fixes a content-identity bug. **HI120 (RD12 dst_gate / RD17 x_scale_channel_dst signature hardening) is therefore promoted from 'separate, lower-priority follow-up' to a HARD PREREQUISITE for HI121's compatibility-cutover (step S4 below) to be safe.** Doing S4 before HI120's audit is complete would let the capability gate falsely certify RD17's two distinct scale semantics as compatible.

New mandatory step, added ahead of any implementation: **S0 -- semantic-completeness audit**. For every planned capability axis, prove all semantically distinct VALUES that axis could represent are already distinguishable in signature CONTENT (flags/fields), not just tracked as 'evaluated or not' via capabilities. RD17's x_scale_channel_dst and RD12's implicit dst_gate representation (fusion==GATE + glu_op==NONE relies on an unstated invariant, not an explicit flag) are the two known real gaps to close first.

### Finding 2: migration strategy changed from (A) to (B), reversibility argument won

Round 2 recommended (A) clean break (remove schema_version from canonical semantics, rehash everything). Challenged on reversibility: (B) is reversible (can still do A later), (A) is not (once rehashed, no cheap path back to comparing against pre-redesign v2 digests for audit/rollback). dev-gpt-agent conceded and reversed: **recommend (B)** -- freeze schema_version=2's value permanently as an 'identity epoch' (never incremented again for capability additions, only for genuine future canonicalization-algorithm breaks), producer_capabilities takes over all future compatibility decisions. The one real cost of (B) -- v1 digests still differ from v2 purely because they contain schema_version=1 -- is solved OFFLINE (not in the runtime hot path) via an explicit, capability-gated rekey importer (S6 below): recompute a v1 row's digest under the frozen v2 epoch ONLY if producer_caps=CORE_SIGNATURE_V1 is sufficient for that signature's required_capabilities(); refuse rekey (e.g. for HI108's fused GLU) otherwise; always preserve the original digest for provenance/audit.

### Finding 3: cap1=FUSION_AUX_PRESENCE_V1 was too coarse, split into 4 independent bits

Withdrawn: bundling x_bias+gate_bias+x_scale+gate_scale evaluation into one bit. RD17 is real, live proof these four facts evolve independently (x_scale's semantics are already changing independently of bias/gate_scale). A future partial implementation (e.g. a build that correctly handles bias but has an incomplete/gated scale implementation) would be forced to either lie (set the bundled bit) or discard valid bias knowledge (clear it) -- reproducing schema_version's own granularity problem one level down.

**Revised capability allocation:**
- `cap0 = CORE_SIGNATURE_V1` (unchanged)
- `cap1 = FUSION_X_BIAS_PRESENCE_V1`
- `cap2 = FUSION_GATE_BIAS_PRESENCE_V1`
- `cap3 = FUSION_X_SCALE_PRESENCE_V1`
- `cap4 = FUSION_GATE_SCALE_PRESENCE_V1`

Current schema-v2 producer sets cap0-cap4 (all five). 128-bit width recommendation unchanged, but now better justified by this finding (real independent-evolution headroom, not speculative).

### Finding 4: real dependency ordering (do NOT land as one atomic change)

```
S0 -- Semantic-completeness audit/spec (RD17/RD12 gaps, HI120's real scope). No runtime change.
S1 -- Capability vocabulary: append-only registry, C++/Python constants, pure required_capabilities()
      implementation + exhaustive tests. Still UNUSED for acceptance/replay -- safe to ship alone.
S2 -- Producer metadata: record producer_capabilities per build row (dispatch_db `build.
      producer_capabilities BLOB(16)` -- sufficient in SQLite since measurements bind to a build;
      no separate capabilities table needed). Historical schema-1/2 rows get their capability mask
      backfilled. Still no relaxed reuse -- safe to ship alone.
S3 -- Replay v6 plumbing: per-entry 16-byte producer_capabilities (NOT cache-global -- merged
      generations can come from different producers with different capability levels, replay
      already retains per-entry provenance). Runtime still preserves existing fail-closed behavior
      unchanged -- behavior-neutral, safe to ship alone.
S4 -- Compatibility cutover: ATOMIC SAFETY BOUNDARY. Capability gate enforced; only HERE does any
      existing blanket schema-equality gate get relaxed/replaced. Exact semantic digest match STILL
      mandatory (never fuzzy/partial matching). Existing provenance/candidate/can_execute gates
      unchanged. MUST NOT land before S0 is complete.
S5 -- Consumer migration: HI119's own schema_version!=2 guard in signature_to_moe_glu_file_line(),
      promotion/evidence, campaign tooling -- each old schema guard replaced atomically by its
      capability-predicate equivalent, one consumer at a time.
S6 -- Optional legacy-v1 rekey importer (see Finding 2) -- only after S4 is proven, never on the
      runtime hot path.
```
Must-be-atomic pairs (cannot partially land): the Python/C++ v6 wire layout definitions together; any removal of an existing fail-closed schema check together with installation of its capability-predicate replacement (S4's own definition).

### Finding 5: the predicted implementer mistake (explicit warning, not generic advice)

"The implementation will get to: exact digest matched + producer_caps satisfies required_caps => 'safe', and overlook that the digest itself can already alias two semantically different operations because a discriminator is absent [in CONTENT, not capabilities]." RD17's x_scale_channel_dst is the concrete, real, already-filed instance of exactly this. The full safety invariant is therefore FOUR conjuncts, not three: semantic representation proven complete (S0) AND exact semantic digest match AND producer_caps superset of required_caps AND existing provenance/candidate/can_execute gates -- dropping the first conjunct is the predicted failure mode.

## Current state (as of the v1->v2 bump this session)

```c
struct ggml_hip_dispatch_signature_v1 {
    uint16_t schema_version;   // GGML_HIP_SIGNATURE_SCHEMA_VERSION -- HASHED, folded into every digest
    uint16_t op; uint8_t src0_type, src1_type, dst_type, prec, fusion, glu_op;
    uint16_t flags;            // ggml_hip_signature_flag, 11 of 16 bits used, HASHED (correctly -- real content)
    int64_t ne0[4], ne1[4], ned[4], nb0[4], nb1[4], nbd[4];
    int64_t n_expert, n_expert_used;
    uint8_t has_refinements, alignment_class, occupancy_bucket, offset_modulo;
};
```
`ggml_hip_signature_digest()` blake2b-hashes the WHOLE canonical JSON string (schema_version included) as one flat unit -- so bumping schema_version 1->2 changed every signature's digest, not just fused-GLU ones. This was the correct fix for the bug it targeted (bias/scale bit ambiguity) but has unnecessarily broad blast radius as a general mechanism.

## Agreed target architecture (dev-gpt-agent, round 2, after adversarial pushback)

Two independent axes, not one bitmap:

1. **Semantic content flags** (existing `flags` field, extended as needed) -- 'this dispatch actually HAS property X'. Stays in the hashed canonical representation. This is what `flags` already correctly does today.
2. **`producer_capabilities`** (NEW, proposed 128 bits / 2x uint64_t) -- 'this producer's code knew how to correctly determine X'. NOT part of the semantic digest. Used only as a compatibility gate at lookup time: `(producer_caps & required_capabilities(signature)) == required_capabilities(signature)`.

`required_capabilities(signature)` must be computed from whether a semantic question COULD apply to this signature's class (op/fusion kind/etc), not from whether the currently-observed flags happen to show the property set -- getting this backwards recreates the exact 'zero means absent or unknown' ambiguity one level up.

### Today's real (not speculative) capability allocation
- `cap0 = CORE_SIGNATURE_V1`: producer correctly evaluates all original v1 identity (op/types/prec, coarse fusion+glu_op, extents/strides, expert counts, flags bits 0-6). Every historical schema-1 producer implicitly has this.
- `cap1 = FUSION_AUX_PRESENCE_V1`: producer explicitly evaluates x_bias/gate_bias/x_scale/gate_scale (HI118), making flags bits 7-10 authoritative rather than 'maybe just not implemented yet'. Only schema-2-and-later producers have this.

Worked examples (confirmed against real fields):
- Plain Q8_0 MUL_MAT: `required = CORE_SIGNATURE_V1` only (fusion genuinely inapplicable) -- a schema-1 measurement remains reusable under this model, unlike the blanket v1->v2 bump that invalidated it anyway.
- Real HI108 routed fused GLU (fusion=GATE, no bias/scale): `required = CORE_SIGNATURE_V1 | FUSION_AUX_PRESENCE_V1` -- an old schema-1 producer's zero bits 7-10 are NOT authoritative, so this measurement genuinely needs re-evaluation regardless of the two-axis model (this part of the v1->v2 invalidation was real, not collateral).
- Fused GLU with gate bias (hypothetical, not currently in production per HI108's GGUF inspection): content flags differ (FUSION_GATE_BIAS set) so digest naturally differs anyway; also requires FUSION_AUX_PRESENCE_V1.

### Versioning axes that remain, now cleanly separated by purpose
- `GGML_HIP_REPLAY_VERSION`: physical replay-cache byte layout (currently 5) -- unrelated to semantic content.
- Semantic canonicalization/encoding algorithm version: changes only when the canonicalization ALGORITHM itself changes (not when a new capability is added) -- expected to change extremely rarely.
- dispatch_db SQL schema_version (currently 6): physical/logical DB schema, independent axis, already separate.

### Migration strategy for schema_version's CURRENT value (open decision, not yet made)
Two options identified, not yet chosen:
- **(A) Clean break now**: remove schema_version from canonical semantics entirely, rehash everything under the new identity model, add producer_capabilities from scratch. Architecturally cleaner, one more digest migration on top of the v1->v2 one just shipped.
- **(B) Freeze current value**: keep schema_version=2 permanently as a fixed 'identity_epoch' inside canonical identity (never incremented again for capability additions); producer_capabilities takes over all FUTURE compatibility decisions. Minimizes immediate disruption, leaves one historical wart.

gpt-dev-agent's live recommendation (round 2): (A), specifically because schema-v2 hardware revalidation has not yet happened -- doing (A) after that revalidation would waste the hardware pass a second time. Not yet independently verified/challenged by a further round -- flag for the next discussion round.

### Proposed build scope (gpt-dev-agent's round-2 proposal, not yet critiqued further)
1. Semantic signature: remove schema_version from canonical semantics; flags remain actual-property bits; future boolean semantic additions consume content bits; future non-boolean fields scoped to applicable op/domain sections only (not unconditionally hashed for every signature).
2. Producer capabilities: 128 bits (2x uint64_t), cap0=CORE_SIGNATURE_V1, cap1=FUSION_AUX_PRESENCE_V1, append-only allocation registry (mirroring dispatch_abi.py's own C++/Python agreement-test pattern) -- numeric IDs permanent, never renamed, retired bits tombstoned not reused.
3. Record/DB: record producer_capabilities per build row; historical schema-1 rows map to CORE_SIGNATURE_V1 only, schema-2 rows map to CORE_SIGNATURE_V1|FUSION_AUX_PRESENCE_V1.
4. Replay: v6 entry format carries producer_capabilities PER ENTRY, not cache-global -- replay already retains per-entry generation/provenance, and a merged cache can contain entries from producers with different capability levels.
5. Lookup: exact semantic digest match, AND `(producer_caps & required_caps(signature)) == required_caps(signature)`, AND existing candidate/provenance/can_execute gates (unchanged).
6. Physical versioning: retain GGML_HIP_REPLAY_VERSION and DB schema_version as-is; add/freeze a separate non-hashed semantic-encoding-format identifier only if truly needed (not yet established that it is).

## Explicitly NOT yet decided / next discussion topics

- Migration strategy A vs B (above).
- Exact wire representation of producer_capabilities in the v6 replay entry (fixed 16 bytes per entry always, vs a more compact encoding given only 2 bits are meaningful today).
- Whether dispatch_db needs a new capabilities table/column now or can defer until the first real second-capability-axis event.
- Full audit of every real consumer (tune_promotion.py, campaign orchestration, CLI tools, e2e_smoke_campaign.py's S3b stage) for correctness under compatibility-gate lookup instead of simple digest equality -- not yet done.
- Whether this should also retroactively simplify/replace HI119's own schema_version!=2 guard in signature_to_moe_glu_file_line() once producer_capabilities exists (it would become a required_capabilities check instead).
- Real risk: this is a correctness-critical hashing/identity system: an error in required_capabilities() or in scoping logic could silently misidentify two different operations as compatible. Every design decision here needs the same adversarial scrutiny HI119's original harness got, not less, precisely because it's infrastructure UNDER the correctness-evidence system rather than sitting on top of it.

## Change Log

- 2026-08-25T23:37:07.072570+00:00 (created-by): Created by agent
- 2026-08-25T23:42:55.281512+00:00 (updated-by): Updated: section:detailed_solution, section:notes
- 2026-08-25T23:49:10.420218+00:00 (updated-by): Updated: section:detailed_solution, section:notes
- 2026-08-25T23:54:05.913548+00:00 (updated-by): Updated: section:detailed_solution, section:notes
- 2026-08-25T23:57:01.796865+00:00 (updated-by): Updated: section:detailed_solution, section:notes
- 2026-08-26T00:20:02.588406+00:00 (updated-by): Updated: section:files, section:notes
- 2026-08-26T00:48:49.509805+00:00 (updated-by): Updated: section:acceptance_criteria
- 2026-08-26T01:15:57.412130+00:00 (updated-by): Updated: section:code_samples
- 2026-08-26T02:23:17.033416+00:00 (updated-by): Updated: section:acceptance_criteria

## Ledger-events

- chg_20260826_022333_finished-the-dispatch-signatur_9583
- 2026-08-26T02:23:33.667720+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-26T05:09:44.854873+00:00 (updated-by): Updated: section:validation
- 2026-08-26T05:09:50.165486+00:00 (state-transition): State: pending → completed
- chg_20260826_050959_confirmed-on-real-gpu-hardware_9781
- 2026-08-26T05:09:59.683266+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-26T05:31:14.467340+00:00 (state-transition): State: completed → in_progress
- 2026-08-26T05:31:48.344718+00:00 (updated-by): Updated: section:validation
- 2026-08-26T05:32:38.890984+00:00 (state-transition): State: in_progress → completed
- chg_20260826_053256_fixed-six-real-bugs-an-indepen_8764
- 2026-08-26T05:32:56.595318+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-26T07:37:03.129306+00:00 (state-transition): State: completed → in_progress
- 2026-08-27T22:30:57.492664+00:00 (updated-by): Updated: section:notes
