---
id: HI80
order: 0
plan: hip-autotune
state: pending
created-at: '2026-08-21T13:26:26.785081+00:00'
breadth: ''
skill: advanced
created-by: agent
work: M
---

# Correctness-evidence generation CLI (op_filter/target_tensor derivation from a promoted row's signature)

## Description

HI67 slice 3's correctness GATE (tools/bigcherry/promotion_correctness_gate.py, called automatically inside tune_promotion.promote()) is real and enforced -- confirmed this session when two genuine routing-transform winners (batch_for_mmvf, +49.5%/+18.3%, both passing full statistical confirmation) correctly hit `rejected_no_correctness_evidence` because no evidence exists yet. The GENERATION side (tools/bigcherry/correctness_evidence.py: collect_seed_evidence/generate_correctness_evidence/write_correctness_evidence) is implemented as a library but has NO CLI entrypoint and no orchestration loop -- nothing calls it for a batch of promotable rows today. generate_correctness_evidence() requires the caller to supply `op_filter` (a `-p` regex matching test-backend-ops' own generated test-case description string, e.g. "MUL_MAT(type_a=f32,...,m=128,n=1,k=1057,...)") and `target_tensor` directly -- there is no existing helper mapping a promoted row's canonical/signature JSON into that exact regex+tensor-name pair, and getting this wrong would silently produce evidence for the WRONG signature (a correctness-of-the-safety-gate risk, not just a usability gap). Needs: (1) a signature->op_filter/target_tensor mapping helper, verified against real test-backend-ops output for known signatures; (2) a CLI (e.g. `bigcherry generate-correctness-evidence <measurements.jsonl> --dispatch-db <db> --binary <patched-test-backend-ops>`) that loops over pending_bh rows, runs generate_correctness_evidence per row with a versioned seed set, and calls write_correctness_evidence; (3) wiring into e2e_smoke_campaign.py as an optional stage between promote and export, so real transform/candidate winners can actually reach promoted status end-to-end.

## Steps



## Detailed Solution & Technical Design

New module tools/bigcherry/signature_correctness_mapping.py implements the first concrete gap HI80 identified: mapping a real BigCherry dispatch signature JSON into the exact op_filter regex and target_tensor name generate_correctness_evidence() needs.

load_ggml_type_names(vendor_root) and load_ggml_op_names(vendor_root) parse the real vendored source directly (ggml/include/ggml.h's enum ggml_type / enum ggml_op declarations, ggml/src/ggml.c's type_traits table and GGML_OP_NAME array) rather than hand-transcribing a lookup table -- this eliminates the drift risk a hardcoded dict would carry if the vendor pin moves and an enum gets reordered or a type gets renamed.

signature_to_op_filter(signature, vendor_root) is MUL_MAT-only this slice (the op actually blocking HI80's own referenced blocked wins). It derives type_a/type_b from src0_type/src1_type via the parsed type-name table, and m/n/k from the signature's ne0/ne1 4-element arrays (m=ne0[1], n=ne1[1], k=ne0[0]==ne1[0], verified against test-backend-ops.cpp's struct test_mul_mat build_graph()).

Corrected mid-implementation against the REAL caller: run_test_backend_ops() in correctness_evidence.py already hardcodes -o MUL_MAT and passes op_filter straight through as -p's value, and test-backend-ops' own -p matching (std::regex_search against test_case::vars() alone, main(), ~line 10602) does NOT include the op name or parentheses -- only the comma-joined vars() string. An earlier draft of this function built an op_filter shaped like 'MUL_MAT(type_a=...)' before this was caught and fixed by reading the actual caller and confirming on real Brutus hardware.

Also hardware-discovered and fixed: bs/nr/per/k_v/o cannot be reconstructed from a BigCherry signature (ne0[2]/ne0[3] carry the PRODUCT bs[i]*nr[i] with no way to recover the factors). Wildcarding them was tried first and found, on real hardware, to match 13 DISTINCT registered test cases for one (type_a,type_b,m,n,k) tuple -- all producing a tensor named 'out', which find_digest_for_tensor()/find_metric_for_tensor() match by name alone, so an unpinned filter could silently take evidence from the wrong one of the 13. Fixed by fully anchoring the filter (^...$) to the expected non-batched default (bs=[1,1], nr=[1,1], per=[0,1,2,3], k_v=0, o=1), confirmed by hardware to collapse the same query to exactly 1 match.

## Code Samples & Guidance

See tools/bigcherry/signature_correctness_mapping.py: load_ggml_type_names(), load_ggml_op_names(), signature_to_op_filter(), _mul_mat_op_filter(). Test patterns in tools/tests/test_signature_correctness_mapping.py.

## Files

- tools/bigcherry/signature_correctness_mapping.py -- load_ggml_type_names, load_ggml_op_names, signature_to_op_filter, _mul_mat_op_filter
- tools/tests/test_signature_correctness_mapping.py -- 13 offline tests, no HIP hardware needed to run (a fixture-based synthetic vendor tree plus a real-vendor cross-check that runs when vendor/llama.cpp is present)

## Validation

Full offline suite: 1683 passed, 3 skipped (up from 1670 before this change). 13 new tests in test_signature_correctness_mapping.py covering: type/op name table extraction from a synthetic fixture, missing-vendor-file fail-closed, the exact real-hardware-observed vars() string matching, rejection of a different signature, rejection of a same-mnk-different-batching variant (the 13-matches finding), different types producing different filters, non-MUL_MAT op rejection, unknown op id rejection, mismatched inner-dimension rejection, unknown type id rejection, malformed ne-array rejection, and a real-vendor-source cross-check that runs when vendor/llama.cpp is present (it is, in this working tree) confirming the fixture-based logic also works against the actual current pin.

Real hardware verification on Brutus (bc-build-hi64hi14, dual RX 7900 XTX): ran test-backend-ops -o MUL_MAT -p 'type_a=f32' and confirmed the real vars() string format exactly matches what this module predicts (type_a=f32,type_b=f32,m=16,n=1,k=256,bs=[1,1],nr=[1,1],per=[0,1,2,3],k_v=0,o=1); confirmed a wildcarded (type_a,type_b,m,n,k)-only filter matches 13 distinct real test cases (the ambiguity finding); confirmed the fully-anchored filter this module actually produces collapses that to exactly 1 match.

## Effort & Risk

Medium risk if extended carelessly to another op: the MUL_MAT-specific m/n/k derivation and the bs/nr/per pinning decision are NOT generic -- they were derived by reading test_mul_mat's specific build_graph() implementation, and another op (e.g. MUL_MAT_ID, which HAS n_expert/n_expert_used signature fields this module does not yet use) will have its own different vars() shape and its own different batching/reconstruction story. Do not copy this module's pinned-defaults pattern onto a new op without first reading that op's own test_case subclass and, ideally, repeating the same kind of real-hardware wildcard-vs-anchor check this session did for MUL_MAT.

## Standards



## Acceptance Criteria

- [x] A signature-to-op_filter/target_tensor mapping helper exists for at least one op (MUL_MAT).
- [x] The type-id and op-id name tables are mechanically extracted from the real vendored source, not hand-transcribed, so they cannot silently drift from whatever revision is checked out.
- [x] The exact op_filter format (matched against vars() alone, no op-name prefix) was verified against real test-backend-ops output on Brutus, not assumed from the item's own prose example.
- [x] The batching-ambiguity risk (13 distinct matches for one type_a/type_b/m/n/k tuple when bs/nr/per are wildcarded) was found and fixed via real hardware testing, not left as a theoretical concern.
- [ ] A CLI entrypoint (bigcherry generate-correctness-evidence ...) looping over pending_bh rows -- not done this session.
- [ ] Wiring into e2e_smoke_campaign.py as an optional stage between promote and export -- not done this session.
- [ ] Extension to any op other than MUL_MAT (e.g. MUL_MAT_ID for MoE candidates) -- not done this session; each op has its own bespoke test_case subclass in test-backend-ops.cpp and needs its own m/n/k-equivalent derivation read from that subclass first.

## Notes

Filed 2026-08-21. Blocks real promotion of the 2 confirmed transform wins found on Brutus R9700/gpt-oss-20B this session (dispatch 8f5e79f9.../a8ef5ddb..., +49.5%/+18.3%, 99-100/100 confirmation rounds). Requires patches 1222_hi67_deterministic_test_backend_ops_seed.py and 1223_hi67_machine_readable_correctness_metrics.py applied to the test-backend-ops binary used.

Implemented in one session after a GPT design-consult (dev-gpt-agent) truncated its replies to a few words twice in the same session and could not deliver a usable design in time; proceeded independently, reading the real source (correctness_evidence.py, test-backend-ops.cpp, hip-autotune-signature.cpp, hip-autotune-types.h, hip-autotune-signature.h) before writing any code, and catching one real design mistake (the 'MUL_MAT(...)' op_filter shape) by reading the actual caller rather than trusting the item's own prose example, plus one real correctness risk (the 13-distinct-matches ambiguity) by testing on real hardware rather than assuming a wildcard was safe.

Next concrete steps for whoever picks this up, in priority order: (1) build the CLI entrypoint that loops over pending_bh rows from a real dispatch DB and calls generate_correctness_evidence() + write_correctness_evidence() per row, using this module for MUL_MAT rows; (2) wire that CLI into e2e_smoke_campaign.py as HI80's own notes describe; (3) if a promotable MUL_MAT_ID row is found blocked (a MoE candidate), read struct test_mul_mat_id in test-backend-ops.cpp and repeat this session's process (derive the mapping, verify the op_filter format against the real caller, hardware-check for the same kind of batching/ambiguity trap) rather than assuming MUL_MAT's pinned-defaults pattern transfers directly.

## Change Log

- 2026-08-21T13:26:26.785081+00:00 (created-by): Created by agent
- 2026-08-22T14:56:45.460319+00:00 (updated-by): Updated: section:detailed_solution, section:code_samples, section:files, section:acceptance_criteria, section:validation, section:effort_risk, section:notes

## Ledger-events

- chg_20260822_145655_built-and-hardware-verified-th_4404
- 2026-08-22T14:56:55.327087+00:00 (updated-by): Updated: section:ledger-events
