---
id: HI125
order: 0
plan: hip-autotune
state: in_progress
created-at: '2026-08-26T09:33:32.267383+00:00'
breadth: ''
skill: advanced
created-by: agent
work: L
---

# HI121 close-out step 4: C++-authoritative canonical signature digest verification (RV84 P0-4)

## Description

RV84 P0-4, confirmed real by dev-gpt-agent's independent investigation (2026-08-26), NOT actually closed by round-3's repeat-sighting check: inventory._resolve_signature() now rejects a LATER canonical JSON differing from the first one stored for a digest (real corruption detection), but the FIRST (digest, canonical_json) pair for any digest is still accepted on faith -- no proof the real C++ signature hash actually produces that digest. signature_capabilities.py deliberately does NOT reimplement the C++ canonical serializer/hash in Python (correct decision, reaffirmed by both reviews), but the underlying invariant -- digest D really does correspond to canonical content C -- currently has NO verification mechanism at all. A poisoned first record establishes digest D -> harmless-looking canonical C, and C becomes semantic authority for all future compatibility decisions built on it (M4's capability-gating).

## Steps

1. DONE: signature_digest_verification.py with observed_test_backend_ops_signature_hex()/observed_signature_digest_hex()/make_signature_digest_verifier(), routing through hip_required_capabilities() then the existing test-file-line mappers, fail-closed on out-of-domain or unrepresentable (e.g. GATE_BIAS) signatures.
2. DONE: hi80_generate_correctness_evidence.py's _observed_signature_hex() refactored to delegate to the new shared primitive.
3. DONE: inventory.py no longer silently skips verification for a result row with no canonical content when a verifier is supplied.
4. DONE (real hardware): MUL_MAT confirmed exact-match against a real production signature from a live tune-campaign run.
5. DONE (real hardware, 2026-08-27/28): MUL_MAT_ID and routed (MoE) GLU both confirmed exact-match against REAL production canonicals captured from a genuine MoE model (Qwen3-30B-A3B-Instruct-2507-Q4_K_M, 128 experts/8 active) via a real llama-server completion request in record mode on Brutus. See notes for full detail. HI125's real-hardware acceptance gate is now fully closed for all three mapped op classes.
6. DONE (2026-08-27, 3-round adversarial review, PASS): production wiring complete -- see prior notes for full detail (dedicated verifier lane, require_strengthened_ingest, GPU single-device scoping, manual CLI opt-in).

## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files

tools/bigcherry/tuning/inventory.py, tools/bigcherry/tuning/signature_capabilities.py, tools/bigcherry/tuning/hi80_generate_correctness_evidence.py (reference pattern), real hardware (Brutus) for the C++ side

## Validation

A poisoned/arbitrary digest-canonical pair (like the existing tests' invented '11'*16/'22'*16 fixtures) is rejected rather than silently trusted as semantic authority; a real C++-verified pair is accepted; real hardware run confirming the verifier against actual compiled signature output.

## Effort & Risk

L, high correctness risk and genuinely needs real HIP compile + a real record sample per dev-gpt-agent's investigation -- this is architecturally the same caliber of gap as HI121's other largest open item (HI124).

## Standards



## Acceptance Criteria



## Notes

Per dev-gpt-agent's recommended order: pair with HI124 (candidate implementation identity) as the two items that must land before HI126 (retroactive re-attestation, step 7) -- both define what is actually being attested before any remediation of existing schema-7 data makes sense.

PARTIAL (2026-08-26, luna-lite-agent): added an opt-in signature_digest_verifier callable seam to inventory.load_measurements() -- when supplied by a caller with real compiled HIP runtime access, verifies each canonical signature's claimed digest against what the real runtime computes. With the default None (all current offline callers), behavior is byte-for-byte unchanged, including first-sighting trust. This does NOT close RV84 P0-4 -- it is infrastructure only. REMAINING: a future session with real Brutus hardware access must implement the actual verifier (subprocess/compiled HIP runtime invocation, likely following hi80_generate_correctness_evidence.py's _observed_signature_hex() pattern) and wire it into the real ingest path, then validate against real production C++ digest output.

IMPLEMENTED AND REAL-HARDWARE VALIDATED (2026-08-27). GPT design task (req_2599938d61774c31) gave a concrete, ready-to-implement design grounded in the existing hi80_generate_correctness_evidence.py::_observed_signature_hex() pattern and signature_mapping.py's existing test-file-line mappers -- implemented as tools/bigcherry/tuning/signature_digest_verification.py (observed_test_backend_ops_signature_hex/observed_signature_digest_hex/make_signature_digest_verifier), with hi80's own function refactored to a thin delegation (one implementation, not two). 11 new offline tests (routing, memoization, fail-closed cases including the GATE_BIAS distinction) plus 2 adjusted for an exception-type change. Full offline suite 2254 passed, no regressions. Committed 5c54d4b.

Real Brutus validation (2026-08-27): confirmed the C++-authoritative verifier reproduces a genuine production signature digest exactly -- extracted a real MUL_MAT canonical + its stored digest (f53d01727fa4b5c9ddad33c41c95ffb6) from today's real hi130-live2-20260827 tune-campaign run, ran observed_signature_digest_hex() against a freshly-built (from that exact run's own already-materialized, currently-patched source tree, with GGML_HIP_AUTOTUNE_RECORD=ON added) real compiled test-backend-ops, and got an EXACT match. Also confirmed correct fail-closed behavior on a real production dense-GLU signature genuinely outside the audited domain (UnsupportedSignatureDomain, not silently accepted).

PARTIAL vs the original acceptance gate: only verified MUL_MAT for real; MUL_MAT_ID and simple routed (MoE) GLU were NOT present in the one real measurements file checked (this run's model didn't happen to exercise MoE-routed dispatch shapes) and were not separately hand-constructed for real-hardware validation (an earlier attempt at hand-crafted synthetic shapes SIGSEGV'd test-backend-ops -- malformed shapes are a real crash risk, not a verifier bug; real production canonicals worked cleanly). A future pass with a model/workload that exercises MoE-routed dispatch should confirm the remaining two mapped classes for full acceptance-gate closure, but the core mechanism (real C++ authority, no Python-side digest reimplementation, correct routing, correct fail-closed domain gating) is now proven against real hardware and real production data, not just offline mocks.

Production wiring (workflow.py integration so tune-campaign actually USES this verifier by default) was NOT done in this pass -- GPT's design flagged it as the natural next step (construct one memoized verifier via make_signature_digest_verifier() and thread it into load_measurements() calls in workflow.py's _stage_load_and_promote()) but it's a separate, easy follow-up once this core primitive was proven, not bundled into this same change to keep the real-hardware validation pass focused.

Unrelated but real finding along the way: Brutus's own /mnt/vault/development/bc-branch checkout was found 5 commits ahead (stale releases/b10362.json audit-bookkeeping from Aug 19, no real code) and 18 commits behind origin -- not touched, flagged to the user, no action taken pending their call on whether/how to reconcile it.

CORE MECHANISM SOUND -- adversarial negotiation converged (2026-08-27, 3 rounds, req_63a2308dd7cc439c/req_38b1c5cfab66469e). Not a rubber-stamp: round 1 found a real, serious gap the initial real-hardware match had NOT proven -- digest equality alone doesn't prove the supplied canonical is what the hash was computed from, since signature_to_test_file_line() only reads ne0/ne1/ned/types and never nb0/nb1/nbd/prec/flags, always reconstructing natural contiguous strides regardless of what those fields claimed. A canonical poisoned in an ignored field would make the mapper reconstruct an identical real dispatch and observe the SAME digest -- exactly the poisoned-first-canonical attack this item exists to close.

Fixed (commit fbcf510): observed_test_backend_ops_signature_hex() now also returns the real observed canonical (hip-autotune-record.cpp already writes it alongside every observation's digest); observed_signature_digest_hex() requires it to exactly match the supplied canonical before trusting the digest. Re-validated against REAL Brutus hardware: honest MUL_MAT case unchanged (f53d01727fa4b5c9ddad33c41c95ffb6, proving no false rejection), poisoned nb0 correctly rejected.

Second fix (commit 92640af): inventory.py's signature_cache short-circuit was independently bypassing the repeat-sighting disagreement check for same-load (not just cross-load) different-canonical-same-digest cases -- fixed via signature_seen_canonical tracking.

Closing round confirmation traced the actual C++ source structure to justify soundness (not just trust the report): ggml_hip_signature_digest() and ggml_hip_record_observation() both invoke ggml_hip_signature_json(sig, true) -- the SAME serializer, SAME arguments -- so no field can be hashed without also being recorded, or vice versa. This is why whole-dict normalized-JSON equality (not a field whitelist) is the correct comparison -- explicitly validated as the right design, since a whitelist would recreate exactly the future-field-omission vulnerability class this fix eliminates. Final GPT position, verbatim: 'I now consider HI125's core canonical-to-digest authority claim sound... I see no remaining concrete hole... I would not manufacture one.'

All prior notes on remaining scope still apply unchanged: MUL_MAT_ID and simple routed GLU remain real-hardware-unconfirmed (not present in the one measurements file checked); production workflow.py wiring (threading make_signature_digest_verifier() into tune-campaign's load_measurements() calls) not yet done. Both are now explicitly agreed to be deployment/route-coverage completeness work, not soundness gaps in the core primitive -- HI127 (verification-state tracking) can safely begin design against this now-settled definition of 'passed the strengthened HI125 verifier' without waiting for those two remaining items.

Per dev-gpt-agent's recommended order: pair with HI124 (candidate implementation identity) as the two items that must land before HI126 (retroactive re-attestation, step 7) -- both define what is actually being attested before any remediation of existing schema-7 data makes sense.

[... existing history preserved above this point in the stored file ...]

STEP 6 CLOSED (2026-08-27) after a 3-round adversarial review (dev-gpt-agent, ses_b6a905dda4f0496d):

Round 1 (design negotiation, before implementation): established that the record-lane test-backend-ops build lacks the hi105-correctness experiment's MUL_MAT_ID/routed-GLU patches, and that adding extra_cmake_targets to the REAL production record lane would perturb its own build_plan_id/runtime-bundle identity (campaign/lane.py's _execute_build_phase() folds every requested target into that identity) -- so the verifier needs its own DEDICATED lane, not an addition to an existing one. Also established: same memoized verifier reused across both load-and-promote passes (correct, since the second pass reloads the same canonicals); production ingest must be mandatory-strengthened (signature_digest_verifier as a required kwarg, no default); a verifier-lane build failure or verification failure must hard-abort the campaign, never silently retry unverified.

Round 2 (code review) found two real defects: (a) 'mandatory strengthened ingest' wasn't actually guaranteed -- load_measurements() only checked manifest_path.is_file(), so a missing/typo'd manifest path (among other build_attested=False causes) silently produced a successful but UNATTESTED load even with a verifier supplied; fixed via a new require_strengthened_ingest parameter that raises RecordError instead. (b) the verifier's HIP_VISIBLE_DEVICES was set from the WHOLE multi-device campaign string (e.g. '0,1'), but test-backend-ops loops over every visible device and requires aggregate success -- this would run every unique canonical on every selected GPU and could abort verification on an unrelated failure on a second/heterogeneous device; fixed by scoping to just the first campaign device (HI125 only needs to prove canonical->digest correspondence once, not hardware equivalence across the whole device set).

Round 3: fixes verified sound, explicit verdict 'PASS for HI125 step 6 code. No remaining concrete trust hole found.' Confirmed the early build-level-provenance-before-per-row-validation error ordering is correct (a caller with no build proof at all should get that failure immediately, not a more specific but less important per-row error), and that winner insertion/attestation is fully transactional (a single commit() at the end, so any later row failure leaves zero partial evidence committed). Two non-blocking cleanups applied: env['HIP_VISIBLE_DEVICES'] assignment instead of setdefault (a promise, not a fallback), and this plan-item update itself (was stale, still said REMAINING).

Explicitly still open, NOT part of step 6: step 5 (MUL_MAT_ID/routed-GLU real-hardware confirmation) remains unconfirmed. Also explicitly flagged by GPT: this wiring itself is still mocks/offline-tested only -- a real end-to-end Brutus tune-campaign run producing actual winner_verification rows has NOT been done yet and is the natural next validation step before HI125 as a WHOLE can be called fully closed (step 6's CODE is sound; its real-hardware operational proof is separate and still pending, same as step 5).

HI125 STEP 5 CLOSED (2026-08-28), real Brutus hardware, following the same isolated-scratch discipline as step 4: cloned the existing correctness-evidence build (2e710a58e09014a081203c4d0992cf66/1678b14f4e50e80ccfec02f74ee786b3, already hi105-correctness-patched with --moe-glu-file support) plus its exact source tree to an isolated /tmp path (never touched the shared cache in place), reconfigured with GGML_HIP_AUTOTUNE_RECORD=ON, built bin/llama-server + bin/test-backend-ops fresh.

Found a real MoE model already present on Brutus (Qwen3-30B-A3B-Instruct-2507-Q4_K_M.gguf, 128 experts/8 active per layer, 18GB, fits one 7900XTX) -- neither existing tune-campaign (hi130-first-live, hi130-live2-20260827) had ever exercised MoE-routed dispatch (both only ever produced op=29 MUL_MAT and op=100 DENSE glu with n_expert=0, already known outside HI121's audited domain).

Ran the real llama-server in GGML_HIP_DISPATCH_MODE=record against this model with one genuine completion request ("Describe the water cycle in two sentences", 61 tokens generated) -- captured real dispatch signatures including op=30 (MUL_MAT_ID, n_expert=128/n_expert_used=8, glu_op=0/fusion=0) and op=100 with fusion=2/glu_op=2/n_expert=128 (genuinely ROUTED fused GLU, unlike the dense case seen before).

Ran sdv.observed_signature_digest_hex() against both real canonicals using the fresh binary: MUL_MAT_ID observed digest 11fa9417b6a8afdf504ab5bdd2c02f24 EXACTLY matched the real dispatch's own recorded digest; routed-GLU observed digest 99f8be9170df41f60faf172d828f2251 EXACTLY matched. Also re-confirmed the HI125 round-1 poisoned-canonical fix generalizes beyond MUL_MAT: a hand-poisoned nb0 field on the real MUL_MAT_ID canonical was correctly rejected with 'observed canonical content does not match the supplied canonical.'

All scratch state (build dir, source clone, transferred tools, running server) cleaned up afterward -- nothing left on Brutus.

This closes HI125's original acceptance gate ("A poisoned/arbitrary digest-canonical pair... is rejected... a real C++-verified pair is accepted; real hardware run confirming the verifier against actual compiled signature output") for ALL THREE mapped classes (MUL_MAT, MUL_MAT_ID, routed GLU), not just MUL_MAT.

HI125 REMAINS OPEN pending exactly one thing: a real end-to-end Brutus tune-campaign run exercising the NEW workflow.py production wiring (step 6) itself, producing actual winner_verification rows -- step 6's CODE is reviewed/sound (3-round PASS) but has only been exercised via mocks/offline tests so far, never a live campaign run. That live-wiring validation is naturally folded into HI129's own closure-validation scope (a real Brutus A->B cross-generation run), so HI125 can be considered functionally complete pending HI129's outcome rather than needing a separate dedicated validation pass.

## Change Log

- 2026-08-26T09:33:32.267383+00:00 (created-by): Created by agent
- 2026-08-26T13:17:57.414938+00:00 (updated-by): Updated: section:notes
- 2026-08-27T11:27:27.476058+00:00 (updated-by): Updated: section:notes, section:steps
- 2026-08-27T11:27:35.464059+00:00 (state-transition): State: pending → in_progress

## Ledger-events


- chg_20260827_112822_implemented-and-real-hardware_7604
- 2026-08-27T11:28:22.532900+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-27T12:12:33.877687+00:00 (updated-by): Updated: section:notes
- chg_20260827_152430_production-dispatch-tuning-run_3346
- 2026-08-27T15:24:30.153146+00:00 (updated-by): Updated: section:ledger-events
- chg_20260827_154353_closed-a-gap-where-the-new-str_2563
- 2026-08-27T15:43:53.290627+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-27T15:50:08.730561+00:00 (updated-by): Updated: section:steps, section:notes
- 2026-08-27T16:03:26.355773+00:00 (updated-by): Updated: section:steps, section:notes
- chg_20260827_160336_confirmed-on-real-gpu-hardware_4405
- 2026-08-27T16:03:36.009421+00:00 (updated-by): Updated: section:ledger-events
