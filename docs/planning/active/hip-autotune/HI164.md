---
id: HI164
order: 0
plan: hip-autotune
state: pending
created-at: '2026-09-05T21:56:58.865776+00:00'
breadth: ''
skill: advanced
created-by: claude-opus-5
work: L
priority: P1
---

# Resolve bindings at graph-node lifetime -- the dispatch lookup may not need to run at all

## Description

dev-gpt-agent (req_7bd85f40d3334469) names this the point it would investigate hardest, and it challenges the premise of every other optimisation in this area: 'if bindings are actually invariant over a reusable graph variant, optimizing an 8-entry per-dispatch lookup is optimizing machinery that should not execute at all.'

For a reusable graph node whose types, extents, strides, fusion semantics, device and replay generation are all fixed, the resolved Binding is invariant across launches. The evidence that this is the common case is already in hand: a single 27B MTP bench run logs graphs reused = 3086, and presents only 86 distinct signatures across 199,242 dispatches -- roughly 2,300 dispatches per distinct signature.

If the binding were resolved once per graph node and stored in execution metadata, then every launch would skip native_select, make_signature, the L1 lookup, the L2 lookup and the persistent digest entirely.

That is a different order of saving from anything else under consideration. HI158's deferral removes ~38.6 ms per run; this removes the entire lookup path from ~199,000 of ~199,242 dispatches.

## Steps

1. Confirm the invariance conditions hold for the graph-node lifetime actually used here, including how replay generation changes and when the cache is reloaded.
2. Identify where a resolved Binding can be stored in graph/node execution metadata with explicit invalidation.
3. Prototype for one family, measured against the current path.
4. Only then consider widening.

## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files



## Validation



## Effort & Risk

High value, high care. The hazard is caching at the wrong lifetime: gpt is explicit that this must be done only where graph lifetime and invalidation are explicit, and that it must NOT regress into tensor-pointer memoization -- safe reuse there would need invalidation covering tensor shape/type, buffer identity, buffer usage, allocation size and view_src.

## Standards



## Acceptance Criteria



## Notes

Sequencing: HI163 (separate runtime from persistent identity) is still worth doing first because it is smaller, strictly correct, and helps every path that remains. But if HI164 succeeds, the L1 capacity/associativity work becomes largely moot, so do NOT invest in elaborate cache tuning before evaluating this.

Related architectural point from the same review, worth folding in: native_select currently mirrors upstream's selection ladder by re-reading raw tensor metadata, which is both duplicated work and a drift surface. gpt recommends native_select consume the same canonical prepared facts the cache key is built from. That has a correctness benefit beyond speed -- if native selection cannot read anything outside (hardware, signature), then an identical cache key NECESSARILY implies an identical native decision, turning HI158's invariance question into a structural property. And if some required src0->nb fact cannot be reconstructed from the signature, that is direct proof the signature is incomplete (HI161).

## Change Log

- 2026-09-05T21:56:58.865776+00:00 (created-by): Created by claude-opus-5
