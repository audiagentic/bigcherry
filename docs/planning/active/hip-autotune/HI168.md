---
id: HI168
order: 0
plan: hip-autotune
state: in_progress
created-at: '2026-09-06T02:17:54.293655+00:00'
breadth: ''
skill: advanced
created-by: claude-opus-5
work: L
priority: P0
---

# Establish production native parity and measure tuned dispatch end to end

## Description

Owner mission: production diagnostics-OFF BC native parity with stock llama.cpp for prompt processing and generation, followed by tuned replay evaluation. Primary test: dual XTX with 27B; 9B for gfx1030 when needed. Seven corrected gfx1100 campaign builds are complete, but no current valid throughput baseline has been collected. Prioritize the existing endpoint runner and measured fixes over new infrastructure.

## Steps

1. Clean the execution path: preserve shared edits, isolate unfinished memory/ABI attestation under HI169, and use existing campaign binaries. No stash/reset/rebase; rebuild only for benchmark-relevant changes.
2. Complete stock and BC-native smoke cells using the maintained server-bench endpoint runner, matched production settings and diagnostics OFF. Resolve machine facts from config/environment.toml and BC_*; check availability and graceful shutdown. No llama-bench or new harness.
3. Collect balanced stock/native baseline blocks on dual XTX with 27B, initially non-speculative to isolate overhead. Retain raw outputs, model/build/config identities, order and validity limitations. Explicit exploratory capture stays unadmitted; do not silently weaken decision-grade gates or label missing telemetry a pass.
4. Analyze pp and generation separately with uncertainty and position effects. Fix measured regressions, then repeat the same contrast. Do not hide ~0.3% effects with an invented tolerance or keep sampling until a preferred result appears.
5. Exercise all remaining BC build types using the same runner; keep diagnostic and production roles separate. Compare cache-off/on using the same production binary where isolating winner effects; validate historical cache compatibility before use.
6. Validate the production MTP profile with work/acceptance equivalence and applicable replay activation evidence. Non-speculative findings and diagnostic throughput do not substitute for production MTP proof.
7. Streamline necessary build/test docs, record evidence and limitations, commit/push coherent verified changes, and complete HI168 only after required implementation and E2E validation.

1. Clean the execution path: preserve shared edits, isolate unfinished memory/ABI attestation under HI169, and use existing campaign binaries. No stash/reset/rebase; rebuild only for benchmark-relevant changes.
2. Complete stock and BC-native smoke cells using the maintained server-bench endpoint runner, matched production settings and diagnostics OFF. Resolve machine facts from config/environment.toml and BC_*; check availability and graceful shutdown. No llama-bench or new harness.
3. Collect balanced stock/native baseline blocks on dual XTX with 27B, initially non-speculative to isolate overhead. Retain raw outputs, model/build/config identities, order and validity limitations. Explicit exploratory capture stays unadmitted; do not silently weaken decision-grade gates or label missing telemetry a pass.
4. Analyze pp and generation separately with uncertainty and position effects. Fix measured regressions, then repeat the same contrast. Do not hide ~0.3% effects with an invented tolerance or keep sampling until a preferred result appears.
5. Exercise all remaining BC build types using the same runner; keep diagnostic and production roles separate. Compare cache-off/on using the same production binary where isolating winner effects; validate historical cache compatibility before use.
6. Validate the production MTP profile with work/acceptance equivalence and applicable replay activation evidence. Non-speculative findings and diagnostic throughput do not substitute for production MTP proof.
7. Complete the owner-requested 9B Q6_K matrix serially on each physical GPU (XTX GPU0, XTX GPU1, gfx1201 and gfx1030), comparing llama.cpp stock, BC native and validated BC replay through server-bench.
8. After the full 9B matrix completes, run a fresh matched dual-XTX 27B Q8_0 stock/native/replay comparison with the same production-shaped workload and balanced ordering. Use it to distinguish topology/model-dependent effects from a general native regression; preserve the earlier 27B capture as historical exploratory evidence.
9. Streamline necessary build/test docs, record evidence and limitations, commit/push coherent verified changes, and complete HI168 only after required implementation and E2E validation.

## Detailed Solution & Technical Design

ATTEMPTS, AND WHY EACH FAILED. Recorded in full because every one produced a reported conclusion that had to be retracted, and the failure modes recur.

1. replay vs control, 1 sample per arm, fixed order, replay last. VOID: arm confounded with position. Thermal drift over a ~10 minute run produces exactly the clean monotone pattern observed. Reported as a result before the confound was noticed.

2. Balanced rerun, 6 rounds. Showed replay worse on 5 metrics with complete separation -- but the arms differed by RD73, the REPLAY build flag AND the cache, three variables described as one. Attribution withdrawn.

3. Winner isolation, same binary, cache on/off. VOID: both arms torn down with kill -9, which destroys the shutdown-emitted replay report, so there was no evidence the cache was ever consulted. 'Winners are neutral' and 'the cache never hit' were the same data.

4. Everything before the mode fix. VOID: GGML_HIP_DISPATCH_MODE defaults to NATIVE and no script set it, so ggml_hip_dispatch_mul_mat returned false immediately -- the dispatch layer never ran, and no cache was ever loaded.

5. Counter runs that looked silent. NOT void, but misread: llama-server installs a log callback that swallows GGML_LOG_INFO, so the reports were invisible. Conclusion 'the layer never ran' was wrong; the evidence channel is the GGML_HIP_DISPATCH_COVERAGE JSON.

6. Tuned vs untuned on the MTP lane, 16 cells, order-balanced, same production binary, cache the only variable. Methodologically sound and still VOID as a throughput comparison: draft acceptance differs, so the arms did different work. tg128 +2.30%, tg512 -8.98%, tg2048 -5.54% describe two different workloads, not two speeds.

WHAT IS ESTABLISHED. The cache resolves on the model it was tuned for (exact 45 of 86 distinct signatures). L1 hit rate is 31.9%, not the 99.8% every prior estimate assumed. Calling native first costs 38.6ms per run, ~0.01% of wall time. Warm CUDA-graph replay bypasses the CPU dispatch path entirely, so dispatch-overhead work has a far smaller ceiling than assumed.

## Code Samples & Guidance



## Files



## Validation

Separate exploratory collection from final qualification. Baseline capture uses matched settings, uncontended GPUs, retained identities/raw output, balanced order and clean teardown; missing evidence is recorded and performance_admitted remains false. Final parity/tuning claims additionally require applicable execution identity, work equivalence and replay activation. No noisy estimate is proof of zero regression; no non-speculative result transfers automatically to MTP.

## Effort & Risk

Reuse completed builds and the maintained runner. HI169 optional memory/ABI attestation must not become an open-ended prerequisite for exploratory baseline collection. Historical contention and unequal MTP work remain genuine validity failures.

## Standards



## Acceptance Criteria

- Real stock/native server-bench baseline completed on dual XTX/27B, pp and generation analyzed separately.
- Native regressions addressed and repeat comparison supports the documented production parity conclusion, with unresolved uncertainty stated.
- All requested BC build types exercised, diagnostic observations separated from production timing.
- Replay/MTP conclusions supported by compatibility, activation and work-equivalence evidence.
- Reusable environment-based procedure documented; relevant changes tested, ledger-recorded, committed and pushed.

## Notes

CAVEATS THAT MUST TRAVEL WITH ANY ANSWER.

- A non-speculative answer does not transfer to the MTP lane. It isolates speed from work, which is what makes it interpretable, but production serves MTP.
- The cache under test was tuned on tierL-qwen27b-q8. Q6_K is the dominant quantisation (tierA-qwen4b-q6k is the default lane for 12 of 19 contracts), so an answer here describes one lane.
- Winners are hardware-keyed and signature-keyed; this result covers dual gfx1100 only.
- The tuned cache was produced on a build that INCLUDES RD73, which independently measures as a ~2% regression (HI162). If RD73 is demoted the composition changes and the cache may need retuning.
- 'No improvement' is a legitimate outcome. HI130 already reached it once and offered two live hypotheses that remain open: MTP's CPU-side sampling and RCCL tensor-split communication may dominate the critical path, or the candidate/promotion criteria may not be finding meaningfully different-performing candidates for this shape mix.

User steering 2026-09-06: review tune and dispatch overhead because prior tune parity appears to have regressed by a small consistent ~0.3% (uncertainty not yet established). Requirement: new dispatch patches add zero regression to pp and ts; inspect whether native BigCherry can improve through a more efficient path. Separate framework/build overhead from winner effects and work-equivalence; do not dismiss small effects through an invented materiality threshold.

Takeover audit RV134: repaired retained lab analyzer to stop with exit 2 on activation failure, identify intended cache arms from header despite swallowed logs, and require final tuned launch counts for each measured cache cell. Six regression tests pass. Active non-speculative run is preserved undisturbed; it lacks per-cell coverage capture and durable graceful-exit evidence, so no timing conclusion is admitted. Verified production CMake DIAGNOSTICS=OFF, AUTOTUNE=OFF, REPLAY=ON, source directory 1ad3df0e2664faf5c46f6d4ea748cb76. Overhead review found an additional concrete HI159 omission: 0700_coverage_counters._count emits reentrancy probe plus coverage_count_executed under DISPATCH only; coverage.cpp increment remains unconditional. Native mode reaches that counter. Quantitative attribution pending; do not assign the reported ~0.3% to it without controlled evidence.

RV136 incorporated: removed family-entry diagnostic calls/TLS probes via anchored upgrade edits; excluded coverage.cpp from diagnostics-OFF CMake source list and guarded coverage flush; switched runtime fingerprint word reads to memcpy (same native-endian word hash, alias-safe). Compiled OFF/ON emitted-hook link/run controls pass (OFF has deliberately undefined diagnostic symbols); real CMake tests cover OFF/diagnostic/record/tune; O3 strict-aliasing compiled fingerprint test verifies every storage byte against word-hash reference. Upgrade applied and reapplied idempotently on copied files from exact running Brutus source 1ad3df0e2664faf5c46f6d4ea748cb76. Real libggml-hip OFF/ON artifact inspection and timing qualification remain outstanding. Full Windows test process terminates during core/test_tree_activity.py: _pid_alive uses os.kill(pid,0), including own PID; investigating remainder with that module excluded, no full-suite pass claimed. Fixed disposition test/document count from 397 to 401 after registering the four existing lab tools.

Owner explicitly requested campaign-build end-to-end llama.cpp native / BigCherry native / BigCherry xx comparison when ready (2026-09-06). Interpret xx provisionally as tuned replay, confirm against campaign config. Preserve genuine stock vs BC native attribution and same-binary cache-off/on isolation where needed; no lifecycle promotion implied. Offline implementation verification: 44 focused tests + 5 subtests pass; broad suite excluding Windows-unsafe tree_activity test file yields 3112 passed, 8 skipped, 155 subtests passed, 14 failures in unchanged vendor fixture/catalog checks. Full suite is not green; retain limitations.

Owner expanded scope to all other BC build types and reusable standardization across future models/topologies. Implementing e2e-build-matrix in existing campaign config (not a new campaign engine), explicit production native build without changing historical AUTOTUNE control meaning, replay-diagnostic now includes both dispatch coverage and replay hit diagnostics. Read-only ab-benchmark --inspect-build inventories real compiler macros and coverage TU, not just CMake intent. ServerRunner now retains teardown outcome and supports opt-in POSIX SIGINT for genuine stock; default remains BC HTTP. Server-per-arm admission remains incomplete, including same-cell production activation after coverage compile-out. Brutus existing benchmark remains undisturbed; no new hardware run.

Pushed f1fba35c (coverage compile-out/alias fix) and c8df9e9175826069c46ffcff8516c7a5f0ea6faa (reusable matrix, compiler inventory, teardown result) to origin/hi168-overhead-audit, preserving divergent tuning-code-rebase remote and unrelated shared files. Build/campaign-focused suite: 681 passed, 3 skipped, 21 subtests passed. check --quick hygiene passes; pre-existing 1244 SUMMARY header catalog gate still fails. Inspector on copied real production CMake/compile database returns expected exit1: 149 HIP compile entries define DISPATCH/REPLAY, zero define diagnostics, coverage.cpp still compiled. dev-gpt-agent request req_2bae11dae74b4b01 in session ses_e679cfbc4bce4a22 is reviewing exact f1fba35c (not yet c8df9e91); retrieve terminal response before treating reviewed. Existing Brutus benchmark completed round4 at 03:37 UTC and remains active; do not build or run alongside it.

Post-push dev GPT review req_2bae11dae74b4b01 terminal success, verdict CHANGES REQUIRED: hot fixes correct, but tune-campaign and ab-benchmark depended on removed production coverage. RV138 records findings. Implemented tune-campaign production+replay-diagnostic lanes, source/catalog/generated registry and non-diagnostic compiler-option parity before export, diagnostic validation, schema4 receipt with separate replay_validation and explicit observation role. This repairs coverage availability without reintroducing production instrumentation. ab-benchmark stripped-production activation/server admission remains open; do not bypass coverage or use diagnostic throughput. Diagnostic globals/report bodies need full ELF check before literal zero-diagnostic-data claim. User confirmed bc xx means replay and requested other build types/standardization; primary three-arm contrast remains stock vs SAME BC release binary native vs tuned, with other build types diagnostic/framework comparisons explicitly separate.

RV139 incorporated following GPT req_7b59ce770e694578: actual CMake generated-input copy verified before configure, after configure/before compile, and after compile; compute real copied-input hash and bind compiled-copy-v1 into build metadata/runtime bundle. Historical unverified generated builds fail closed on reuse. Companion parity now requires build-bound recomputed digest and runtime-bundle record integrity. Clarified requested CMake-option parity is not effective compiler state. 687 broad build/campaign/workflow tests+21subtests pass3skip; final focused46+2subtests pass including copy mutations before/during configure and during compile. Owner reported competing XTX workload, invalidating prior eight-round performance attribution additionally; old run stays historical. Brutus released, point-in-time read-only check 04:33UTC all GPUs0%,VRAM0%,no KFD processes. No new hardware launched. Remote deployment needs supported MCP Git operation or explicit exception for SSH Git fetch/isolated worktree; no available remote Git tool.

Owner explicitly authorized qualification contracts/adapters for0100 and0700 after real matrix build admission blocked. RV140 tracks missing packages and focal-in-baseline runner limitation. Implementing explicit --baseline-source selection in existing validation_campaign (default unchanged), preserving exact composition and focal-already-present rejection; evidence records selected name and composition. This is prerequisite plumbing only, not qualification or permission to relabel production/tuning builds. No new GPU timings.

Dev GPT req_7733bd09636d4ad7 accepted4c1 compiled-copy fix. Qualification review identifies policy mismatch: zero-contract local framework adapters are supported by core verdict semantics but require_execution_package rejected all withoutcontract. Following reviewed direction, discarded uncommitted artificial performance-contract draft; implementing strict local/nonRD/noexternal-source adapter exception, preserving all existing evidence admission. New negative test found canonical plan-ids RD scope ignored by old helper; fixing alongside legacy support. Package-local host CMake and emitted-hook OFF/ON checks provide supplementary configuration evidence only. Remaining schema4 evidence requires tuning and causal validation build roles even for structural framework claims; requested bounded architecture review req_d601dfee02b746dd before implementing a framework-specific record variant. No fictitious control roles, GPU qualification, or timings.

Owner mission reaffirmed: process cleanup is subordinate to getting diagnostics-OFF production BC native to at least llama.cpp native parity in prompt processing and generation. Primary validation is dual XTX with 27B through campaign-built llama-server and maintained server-bench endpoint runner; 9B may be used for gfx1030. New framework-configuration path now runs package apply/build/configuration adapters against real single-composition production/diagnostic builds, rather than fabricated causal roles. Schema5 distinguishes compiled targets from runtime GPU qualification; exact current framework composition, full adapter identity and generated compiled-copy/build binding required. 121 focused tests+9subtests, patch-lint and check --quick pass; real HIP execution remains next. Host facts now resolve through config/environment.toml and BC_* variables; no new local server facts in procedural docs.

2026-09-06: fcc15bbf real isolated HIP framework-configuration runs compiled llama-server production and diagnostic successfully for 0100 and 0700; all real adapters PASS. These are configuration/build observations, not runtime GPU correctness or native parity. Gateway req_dba4c89f5c504f4f requires RV141 P0 source overlay/application semantics binding and persisted actual compiler diagnostics observation before admission; corrections implemented with negative regression coverage, fresh runs pending. P1 missing-framework-adapter classification exposes 25 historical package obligations; scope/migration review req_634f7c5c595c496f pending, no blanket adapters added. Windows full offline suite: 3190 passed, 12 failed, 8 skipped (known unrelated HI134/HI85/1233/HI104 fixtures); excluded Windows self-terminating test_tree_activity. Server-per-arm campaign orchestration and actual dual-XTX 27B performance comparison remain unfinished. Environment facts resolved from canonical config and BC_* variables.

2026-09-06: fresh2290ab84 framework production+diagnostic HIP builds completed for both0100/0700, all adapters PASS. Persisted evidence now passes patch-verify-evidence --no-legacy-grandfather on both. Original fcc15bbf evidence retained append-only, no runtime/GPU correctness or throughput claim. Matrix rerun correctly refused qualification checkout dirty from retained artifacts/generated release outputs; clean checkout with committed evidence required, no development override. Added existing ab-benchmark server-config capture mode and exact benchconfig retention,59focusedtests pass; captured timings remain unadmitted until execution/workequivalence/activation checks.

2026-09-06 runtime/build progress: clean f40af599 e2e matrix completed7/7 servers (stocka55fa53d6c9c63e01115aa09847f77eb, native24f1b7ad163f21d0b798b3e0064ab531; all othersalsoOK). Actualcompiler target override waslost: all3platformarches instead ofrequestedgfx1100; RV142fixb9a1ac31pushed, narrowedmatrix rebuilding. Two stock27B dualXTX startupcells loaded and shut downcleanly butwere rejected BEFORE bench: defaultlog3 hidesdeviceinfo; log4 exposesMeta() buffers/offloaded66/66layers butnotphysicalmembers, so canonicalserverattestorreturnsNone. No throughput/parityclaim. Read-onlyliveKFDinspection provedownedqueues+14,575,603,712VRAMbytesoneachXTX andzerootherGPUs; rawproducer underdevelopment, notadmission. P1blanketmissingframeworkadapterpolicyattempt reverted viaanchoredpatch becauseitcreates25historicalpackagefailures; RV141P1migrationremainspendingreviewreq_634f7c5c595c496f, not silentlygrandfathered. TensorMeta/defaultloggingadmissionmechanismreviewreq_0abd12f6e94e4556 pending.

2026-09-06: corrected b9a1ac31 clean campaign matrix completed 7/7 lanes. Actual CMakeCache AMDGPU_TARGETS is gfx1100 for stock, native, control, record, replay-diagnostic, replay and tune; this validates RV142 architecture propagation (closed). Build completion is not runtime correctness or throughput evidence. Tensor-split production logging device attestation remains under investigation before server-bench timing.

Owner correction incorporated through RV144: baseline-first execution; optional unfinished ABI/memory attestation tracked separately as HI169. No current throughput or parity claim. Preserve prototype edits until a deliberate isolation/disposition step; do not discard shared work.

2026-09-06 baseline-first execution: pushed34aa876f, isolated optional memory/ABI integration underHI169 (preserved ignored patch artifacts/hi169/server-integration-deferred.patch; remaining prototype files unstaged). Actual maintained server-bench smoke native-stock-smoke-003 completed all4cells, bothorders, defaultlogging/diagnosticsOFF/27B dualgfx1100/noMTP, clean teardown. Basic prompt/generatedcounts match acrossarms. Observed paired deltas+0.160%pp512,+0.073%tg128, smokeonly notparity; physicaldevice logevidence remainsmissing and admissionfalse. Compactrecord docs/evidence/hi168-baseline-20260906.md. Predeclared six-pair pp512/pp2048/tg512 baseline seed168 started usingexistingbuilds, noABIprobes/rebuilds.

Owner expansion (2026-09-06): run full maintained server-bench E2E with canonical tierB-qwen9b-q6k model on each physical GPU individually (both XTX cards separately, gfx1201, gfx1030), comparing stock llama.cpp native, BC native, and BC tuned/replay. Serialize hardware activity after the running 27B balanced comparison. Verify device visibility, use model/topology-specific recorded and validated tuning inputs, never assume the historical 27B cache qualifies 9B replay. Preserve diagnostics-off performance runs separately from diagnostic/tuning activity.

Six-pair native-stock-balanced-001 finished all 12 maintained server-bench cells cleanly. BC native exploratory geometric deltas: pp512 -0.213% (95% interval -0.827 to +0.421), tg512 -0.415% (-0.778 to +0.029); pp2048 +1.343% is dominated by a retained slow stock outlier. Matching basic work counts; missing physical-device log evidence and output equivalence still prevent performance admission. Report: docs/evidence/hi168-baseline-20260906.md. All GPUs idle after teardown. Started canonical tune-campaign hi168-9b-gpu0-20260906 on first XTX with production-safe-single and fresh 9B inputs; results pending. Four single-GPU three-arm benchmark configurations prepared locally. Optional HI169 prototype isolation completed; no implementation files remain modified.

HI170 added from Dev GPT review: implement the single validated unattended matrix entrypoint over existing tune-campaign/ab-benchmark workflows, selectable models/topologies/configurations, serialized GPU scheduling, automatic replay-cache/build identity wiring, and per-cell/run manifests. HI170 is subordinate to HI168 and must not create a second campaign engine or admission path.

Execution sequencing (owner 2026-09-06): finish all four single-GPU 9B stock/native/replay rows first. Then repeat the dual-XTX 27B Q8_0 comparison as a matched three-arm stock/native/replay run, using fresh validated replay artifacts and the maintained server-bench. This follow-up is required to resolve whether the earlier negative dual-XTX native signal is topology/model dependent. Do not overwrite the earlier six-pair native-stock-balanced-001 evidence; retain it as historical exploratory context.

## HANDOVER REFERENCE -- everything a new agent needs

### Read these first, in this order

1. docs/reference/testing/TEST.md, section "Comparative A/B benchmarking -- mandatory procedure". Authoritative. Every rule there exists because breaking it produced a retracted result in this investigation.
2. .claude/skills/bigcherry-benchmark/SKILL.md -- the skill that owns comparative measurement, including the five failure modes and the stop conditions that block reporting.
3. docs/planning/active/hip-autotune/HI130.md, and specifically its line 329 conclusion. It reached this same impasse first and its warning about draft_acceptance determinism was written before, and ignored during, this investigation.

### Use the MAINTAINED tooling -- do NOT write a harness

  bigcherry ab-benchmark      paired, interleaved A/B. --pairs, --schedule-seed,
                              --settle-seconds, --practical-threshold-pct,
                              --decision-grade, and build verification via
                              --stock-cmake-cache / --patched-cmake-cache.
                              Supports --server-config for managed server arms
                              using the maintained server-bench endpoint runner.
                              Capture is exploratory, not performance admission.
  bigcherry kernel-fraction   per-kernel time fractions from rocprofv3
                              --kernel-trace CSVs. This is the "what is slow"
                              answer.
  bigcherry impact            predicted saving from record observations +
                              tuning measurements, bootstrapped, --fail-on-slower.
  bigcherry profile-campaign  repeatable rocprofv3 deep profiling; see
                              docs/reference/tooling/PROFILING.md.
  bigcherry replay-inspect    registry/cache correctness, per-family counts,
                              individual winner entries.
  bigcherry tune-campaign     the full record -> tune -> correctness -> promote
                              -> replay pipeline.

Run `python3 -m bigcherry --help` BEFORE writing anything. Two harnesses and
three analyses were rebuilt in this session that already existed.

### Lab scripts (tools/lab/gp11-replay-bench/)

  ab-balanced.sh        order-balanced N-arm runner. ARMS entries are
                        NAME:DIGEST:CACHE[:MODE]. Env: ROUNDS, SPEC_ARGS
                        (empty disables MTP), BENCH_CONFIGS, CACHE_PATH, OUT,
                        MODEL, DEVICES, PORT. NOT a reference implementation --
                        its first result was retracted for a confound
                        ab-benchmark already handles.
  analyse.py            means, deltas, range separation, per-position means,
                        outliers, and an activation-evidence precondition that
                        REFUSES to summarise a run that cannot prove the cache
                        was consulted.
  dispatch-counters.sh  single-run hot-path counters via the coverage JSON.
  README.md             the disposition of every run, including the withdrawn
                        ones and why.

### Historical artifacts on the configured build server

  Production binary under test (diagnostics OFF, AUTOTUNE OFF, REPLAY ON):
    build_plan_id 6f1226a6bdbfa5750b567f3747410b4a
  Historical cache, 18 winners, tuned on tierL-qwen27b-q8 / dual gfx1100:
  Not validated for the current source/build; revalidate before use.
    $BC_CACHE/tune-campaigns/27e45ae32ec4/dispatch.cache
    receipt, inventory.json, coverage.json, behavioral-gate.json,
    recovery-result.json, promoted.jsonl all in that directory.
  Earlier UNGATED Q6_K caches (production-safe-single had no behavioural gate
  when these were produced -- treat with suspicion, see HI168 step 4):
    d3b48905ce0b  gfx1201 / 27B   12 winners
    beae0b57fdd9  gfx1030 / 9B    11 winners
    8bc58c34a3b7  gfx1201 / 9B     8 winners
  Per-quant candidate coverage (answers "do candidates exist for type X"):
    $BC_CACHE/runs/<run>/generate/catalog/hip-autotune-manifest.json
    -> supported_coverage.by_type -- q6_k, q8_0, q5_k, q4_k all have 129
       candidates / 124 alternatives across families [mmq, mmvq].

### Environment variables that silently invalidate a run if wrong

  GGML_HIP_DISPATCH_MODE       MUST be set (native|record|tune|replay).
                               UNSET DEFAULTS TO NATIVE and the entire
                               dispatch layer no-ops -- no cache load, no
                               lookup, no counters, while looking normal.
  GGML_HIP_DISPATCH_CACHE      path to the winner cache.
  GGML_HIP_DISPATCH_COVERAGE   path for the coverage JSON. THE ONLY usable
                               evidence channel: llama-server installs a log
                               callback that swallows the library's
                               GGML_LOG_INFO lines, so counter/cache-load/
                               replay reports NEVER reach stdout or stderr.
  GGML_HIP_DISPATCH_COUNTERS   enables hot-path counters (diagnostics build).
  LLAMA_SERVER_ENABLE_SHUTDOWN MUST be set or the /shutdown route from patch
                               0800 is never registered, the POST 404s, and
                               teardown falls back to kill -9 -- which
                               destroys the shutdown-emitted reports.
  GGML_HIP_DISPATCH_L1_SLOTS   L1 capacity, for measuring the hit-rate curve.

### Builds

  Production/measurement build:  GGML_HIP_DISPATCH_DIAGNOSTICS=OFF (default).
  Observation build, same revision: DIAGNOSTICS=ON, for activation evidence.
  Never take a performance number from the diagnostics build.
  bigcherry build --lane bigcherry:replay:linux-multi \
      --binary-relative-path bin/llama-server \
      --inventory <campaign>/inventory.json --winners <campaign>/promoted.jsonl

## RELATED WORK ITEMS

### Blocking or sequencing this item
  HI166  ordered per-step acceptance trace equality. P0. MUST land before any
         candidate-set expansion: without it a larger candidate space can
         manufacture winners that win by doing LESS WORK rather than by going
         faster -- which is exactly the 0.94734 -> 0.86391 failure.
  HTR07  adaptive recovery: bisect with the shortest vector that actually
         reproduces the failure. P0. Keeps the extended corpus affordable.
  HI162  RD73 measures as a ~2% regression on the workload it was promoted on,
         with complete separation on three metrics. It is the ONLY patch in
         validated-enhancements, so production ships it, and the cache under
         test was tuned on a build containing it.

### Machinery this question depends on
  HI158  deferred native selection (landed; native now forced only on L2 miss)
  HI159  diagnostics were not zero-cost (landed)
  HI160  final_tuned_launches at the executor -- the ONLY sufficient proof a
         tuned kernel ran; exact>0 is NOT sufficient (landed)
  HI161  signature identity completeness: ids fields, quantized stride
         normalisation, and the maintained proof table
  HI163  separate fast runtime identity from the persistent BLAKE2b digest;
         L1 set-associative sizing
  HI164  graph-node-lifetime bindings. PAUSED: warm CUDA-graph replay already
         bypasses the CPU dispatch path entirely, so the ceiling is far
         smaller than first estimated.
  HI165  stage advisories -- surfaces prior findings where results appear
  HI167  mmq_types=[] while MMQ carries 27% of dispatches. Evidence says this
         is a RECORD-COVERAGE problem, not a missing catalog: q6_k has 129
         candidates including mmq.

### Prior art that already answered parts of this
  HI130  reached the same impasse: "no evidence of a real tuning-driven
         performance improvement over either vanilla upstream llama.cpp or
         bigcherry's own untuned dispatch currently exists". Its two open
         hypotheses stand: MTP CPU-side sampling and RCCL tensor-split
         communication may dominate the critical path, or the promotion
         criteria may not be finding meaningfully different candidates for
         this shape mix.
  HI141  the original acceptance collapse (93.5% -> 62.1%) from a candidate
         that was numerically perfect and 23% faster.
  HI143  the behavioural gate built in response. Three-state contract.
  HI87   closed the native-select deferral once already on the grounds that
         the saving was below any intervention threshold. It was right:
         measured at 38.6ms/run, ~0.01% of wall time.

### Candidate work, re-prioritised against the real model registry
  RD07 / patch 1203  Q6_K MMQ sub-scale fold. P0 per dev-gpt-agent. Q6_K is
         the DOMINANT quantisation (models.toml: tierA-qwen4b-q6k is "the
         default lane for most contracts (12 of 19)"). Implementation exists,
         rebases CLEAN, state untested, selected by NO patch-set. Fork
         reported MMQ 40 -> 58 TFLOPS. CAVEAT: 1203 bundles RD05/RD06/RD07;
         RD07 is the mmq-vec-dot.cuh edit alone, RD05/RD06 are flash-attn in
         fattn-mma-f16.cuh/fattn.cu, so isolation is clean.
  EX02   mmq:q6_k:j112:fb0 illegal memory access on gfx1100, root cause OPEN,
         narrow quarantine live in ggml_hip_mmq_can_execute. Does NOT block
         Q6_K work generally; blocks unsafe inference about gfx1100 q6_k MMQ.
         RD07 on gfx1201: proceed. On gfx1100: preserve the quarantine.
  RD30 / patch 1237  implemented already; qualify rather than redesign.
  RD31, RD32  substantially absorbed into RD30/1237 -- reconcile, do not
         implement separately.
  RD98   Q4_K MMVQ family. Low priority: there is NO Q4_K model in the
         registry. Add a real Q4_K lane first if Q4_K matters operationally.

### dev-gpt-agent consultations (session ses_30497d5cc95c4224)
  req_b48d801c  mechanisms by which individually-faster kernels are
                collectively slower; the non-vacuous replacement for HI158's
                shadow assertion
  req_6075d7c2  hot-path audit: found the enabled() atomic RMW and that
                exact>0 does not prove a tuned launch
  req_7bd85f40  algorithms review: L1 structure, the second global mutex, and
                that the persistent digest must not be the runtime key
  req_bdb8d2b1  validated corpus v2; required ordered acceptance trace and
                adaptive recovery
  req_6b327cea  the missing-candidate history; the advisory false-assurance
                flaw
  req_2d8d879b  re-prioritisation against the real registry; confirmed the
                per-quant coverage data already exists in the manifest

## Change Log

- 2026-09-06T02:17:54.293655+00:00 (created-by): Created by claude-opus-5
- 2026-09-06T02:20:53.376390+00:00 (updated-by): Updated: section:code_samples, section:standards
- 2026-09-06T02:24:28.344682+00:00 (state-transition): State: pending → in_progress
- 2026-09-06T02:28:08.498301+00:00 (updated-by): Updated: section:notes
- 2026-09-06T03:01:14.668644+00:00 (updated-by): Updated: section:notes

## Ledger-events


- chg_20260906_030121_prevent-benchmark-reports-from_9231
- 2026-09-06T03:01:21.809496+00:00 (updated-by): Updated: section:ledger-events
- 2026-09-06T03:28:06.621130+00:00 (updated-by): Updated: section:notes
- chg_20260906_033109_remove-diagnostic-atomics-left_6242
- 2026-09-06T03:31:09.392649+00:00 (updated-by): Updated: section:ledger-events
- 2026-09-06T03:34:37.682289+00:00 (updated-by): Updated: section:notes
- 2026-09-06T03:43:49.976633+00:00 (updated-by): Updated: section:notes
- chg_20260906_034606_standardize-campaign-build-rol_5398
- 2026-09-06T03:46:06.721274+00:00 (updated-by): Updated: section:ledger-events
- 2026-09-06T03:47:24.329208+00:00 (updated-by): Updated: section:notes
- 2026-09-06T03:54:53.158570+00:00 (updated-by): Updated: section:notes
- chg_20260906_035546_keep-tuning-validation-observa_8601
- 2026-09-06T03:55:46.244323+00:00 (updated-by): Updated: section:ledger-events
- 2026-09-06T04:40:16.541304+00:00 (updated-by): Updated: section:notes
- chg_20260906_044022_prevent-build-validation-from_7155
- 2026-09-06T04:40:22.260311+00:00 (updated-by): Updated: section:ledger-events
- chg_20260906_044303_correct-a-patch-summary-status_3410
- 2026-09-06T04:43:03.220406+00:00 (updated-by): Updated: section:ledger-events
- 2026-09-06T04:54:43.212316+00:00 (updated-by): Updated: section:notes
- 2026-09-06T05:04:51.117991+00:00 (updated-by): Updated: section:notes
- chg_20260906_050737_add-verifiable-framework-patch_6081
- 2026-09-06T05:07:37.605969+00:00 (updated-by): Updated: section:ledger-events
- chg_20260906_052741_improve-server-test-bench-evid_6984
- 2026-09-06T05:27:41.207729+00:00 (updated-by): Updated: section:ledger-events
- 2026-09-06T05:43:24.272218+00:00 (updated-by): Updated: section:notes
- chg_20260906_054327_add-explicit-framework-configu_7860
- 2026-09-06T05:43:27.753489+00:00 (updated-by): Updated: section:ledger-events
- chg_20260906_054714_make-isolated-qualification-an_6316
- 2026-09-06T05:47:14.903406+00:00 (updated-by): Updated: section:ledger-events
- chg_20260906_055730_harden-framework-build-qualifi_6294
- 2026-09-06T05:57:30.960534+00:00 (updated-by): Updated: section:ledger-events
- 2026-09-06T05:57:49.347728+00:00 (updated-by): Updated: section:notes
- chg_20260906_060546_record-verified-framework-hip_8407
- 2026-09-06T06:05:46.030143+00:00 (updated-by): Updated: section:ledger-events
- chg_20260906_060549_add-reusable-balanced-server-b_6075
- 2026-09-06T06:05:49.133999+00:00 (updated-by): Updated: section:ledger-events
- 2026-09-06T06:06:13.998193+00:00 (updated-by): Updated: section:notes
- chg_20260906_060842_expose-balanced-server-capture_6715
- 2026-09-06T06:08:42.451611+00:00 (updated-by): Updated: section:ledger-events
- chg_20260906_061437_reject-server-timing-before-me_1625
- 2026-09-06T06:14:37.267058+00:00 (updated-by): Updated: section:ledger-events
- chg_20260906_062806_make-campaign-architecture-ove_5653
- 2026-09-06T06:28:06.980371+00:00 (updated-by): Updated: section:ledger-events
- 2026-09-06T06:33:23.026208+00:00 (updated-by): Updated: section:notes
- 2026-09-06T06:38:54.482600+00:00 (updated-by): Updated: section:notes
- 2026-09-06T06:39:40.963978+00:00 (updated-by): Updated: section:handover_reference_everything_a_new_agent_needs
- chg_20260906_064112_document-verified-campaign-bui_5607
- 2026-09-06T06:41:12.517471+00:00 (updated-by): Updated: section:ledger-events
- chg_20260906_064239_capture-process-owned-gpu-evid_5987
- 2026-09-06T06:42:39.823719+00:00 (updated-by): Updated: section:ledger-events
- chg_20260906_065333_verify-live-campaign-source-be_4645
- 2026-09-06T06:53:33.819478+00:00 (updated-by): Updated: section:ledger-events
- 2026-09-06T07:53:24.829501+00:00 (updated-by): Updated: section:title, section:description, section:steps, section:validation, section:effort_risk, section:acceptance_criteria, section:notes
- chg_20260906_075726_unblock-exploratory-stocknati_9659
- 2026-09-06T07:57:26.361609+00:00 (updated-by): Updated: section:ledger-events
- 2026-09-06T08:03:48.340105+00:00 (updated-by): Updated: section:notes
- 2026-09-06T08:08:53.910021+00:00 (updated-by): Updated: section:notes
- 2026-09-06T08:17:48.619068+00:00 (updated-by): Updated: section:notes
- chg_20260906_081751_completed-the-27b-exploratory_2092
- 2026-09-06T08:17:51.846541+00:00 (updated-by): Updated: section:ledger-events
- 2026-09-06T08:42:08.043988+00:00 (updated-by): Updated: section:notes
- chg_20260906_084211_added-hi170-to-make-modelgpu_6219
- 2026-09-06T08:42:11.544855+00:00 (updated-by): Updated: section:ledger-events
- 2026-09-06T09:06:58.659860+00:00 (updated-by): Updated: section:steps, section:notes
- chg_20260906_090710_queued-a-matched-dual-xtx-27b_5806
- 2026-09-06T09:07:10.113450+00:00 (updated-by): Updated: section:ledger-events
