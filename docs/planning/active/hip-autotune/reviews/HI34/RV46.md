---
id: RV46
review-of: HI34
plan: hip-autotune
state: created
reviewed-by: gpt-auto-agent (session ses_e12200c366a04801)
reviewed-at: ''
---

# GPT source review corrects Slice A close-out reconciliation: flush mechanism (step 3) is NOT in the tree at 0868962

## Notes

Session ses_e12200c366a04801, verdict on commit 0868962. The close-out submission claimed HI34 steps 1-3 were implemented (carried over from ledger event chg_20260813_064103's scope). Direct source search at 0868962 contradicts that for step 3: no flush_l2 config member, no GGML_HIP_TUNE_FLUSH_L2 / GGML_HIP_TUNE_FLUSH_MB parsing, no eviction buffer/kernel, no eviction in time_candidate(), no flush parameter on any measurement path. Genuinely present: native pilot, calibrated_launches_per_sample(), shared lps, result-line recording, MIN_SAMPLE_US / MAX_LPS, and tools/bigcherry/compare_tunes.py.

## Findings

- HI34 reconciliation note corrected to 'steps 1-2 implemented; step 3 pending' (this update).
- HI34 description's launches_per_sample paragraph now explicitly marked as the fixed defect so a future reader does not mistake it for current state.
- New invariant added from GPT beyond existing HI34 prose: measurement-affecting knobs belong in the evidence — flush configuration must be recorded in artifact provenance/header because flush=0 and flush=1 artifacts are not measurement-equivalent even with identical build/input digests.
- Slice B split locked: B0 (flush mechanism, diagnostic only, fail-closed lps=1 when flushing) -> B1 (residency falsification on Brutus unblocked, single variable, hard decision gate) -> B2 (screen-stage canary + adaptive retention band) -> Slice C (HI24 steps 5-6).
- HI64 description reworded: failure class is suspected, not established, until step 1 captures the driver error code.

## Conclusion

Incorporated. HI34 notes/description corrected, HI64 wording fixed, B0-B2 sequence recorded. No code findings — this was a plan-state accuracy correction caught before any B-scope implementation started.
