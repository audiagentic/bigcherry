---
id: HI87
order: 0
plan: hip-autotune
state: pending
created-at: '2026-08-22T14:18:26.932122+00:00'
breadth: ''
skill: advanced
created-by: agent
work: M
---

# Warm-cache-first dispatch: skip native_select() on guaranteed cache hits

## Description

Captured from critical assessment of an external design document ("BigCherry Replay and RCCL Tuning Architecture", pasted into chat 2026-08-23, not independently authored -- treated as an idea source, not a spec) against BigCherry's real current dispatch code. This is the one part of that document verified directly against source before writing this item (see Detailed Solution) -- explicitly NOT yet implemented, backlog only.

Current confirmed behavior (src/ggml/src/ggml-cuda/hip-autotune-dispatch.cu): ggml_hip_dispatch_mul_mat() calls ggml_hip_native_select() unconditionally at line ~900-901, BEFORE ggml_hip_dispatch_resolve() is ever invoked (line ~925-926). Inside resolve(), the thread-local 8-entry cache check (g_thread_bindings.find, line ~495-496) and the process-global g_bindings cache check (line ~590-593) both run strictly AFTER native_select() has already executed and its result was passed in as a parameter. So on every single dispatch -- including a guaranteed warm-cache hit that will just return the cached binding -- native_select() still runs first and its result is thrown away. The document's proposed fix: add a cheap warm-cache lookup (by signature+hardware digest) BEFORE calling native_select() at all, and only fall through to native_select() on an actual cache miss.

## Steps



## Detailed Solution & Technical Design

Document's proposed shape (unverified beyond the ordering claim above, which IS verified): an L0/L1/L2 hierarchy -- L0 a thread_local last-binding single-comparison fast path, L1 the existing 8-entry TLS ThreadBindingCache, L2 the existing process-global g_bindings map -- checked in that cheap-to-expensive order BEFORE native_select() runs, with native_select() only invoked on a genuine miss through all three. This is a re-ordering / early-exit optimization on top of caches that already exist (g_thread_bindings, g_bindings) -- it does not require new cache structures for L1/L2, only restructuring ggml_hip_dispatch_mul_mat()'s call order so the cheap lookup happens first. An L0 single-slot fast path would be new.

Open question needing real measurement before committing to this: how expensive is native_select() actually? If it's a handful of cheap comparisons against tensor metadata already in hand, this reorder may save little; if it does real work (shape analysis, capability probing), the saving could be meaningful on the hot decode-loop path where the same signature repeats every token. Profile before implementing, not just from claimed motivation.

## Code Samples & Guidance



## Files

src/ggml/src/ggml-cuda/hip-autotune-dispatch.cu (ggml_hip_dispatch_mul_mat, ggml_hip_dispatch_resolve, ggml_hip_native_select)

## Validation

Profile native_select() cost in isolation on the real hot path (steady-state decode, same signature repeating) before implementing, to confirm the reorder is worth the complexity. If implemented: verify identical dispatch decisions before/after reorder via the existing replay/telemetry evidence (no behavior change, only call-order/perf), plus a microbenchmark showing measured improvement on Brutus hardware -- not just reasoning about it.

## Effort & Risk

Medium risk of being a premature optimization if native_select() is already cheap -- must be measured, not assumed, per this session's established practice of verifying claims against real hardware/source before acting. Correctness risk is low (pure reordering of already-existing cache lookups), but any restructuring of this hot path touches code shared by every dispatched op, so needs the same real-hardware validation discipline as HI85/HI18.

## Standards

Should be checked against HIP_AUTOTUNE_STANDARDS before implementing, particularly any standard governing cache-key construction/hardware digest reuse (relevant since the document also proposes caching ggml_hip_make_hardware_key()/ggml_hip_hardware_digest() once per device/process instead of recomputing per dispatch -- plausible, not yet verified how often these are actually recomputed today).

## Acceptance Criteria



## Notes

Source-verified before filing (2026-08-23): the call-order claim above was independently confirmed by reading src/ggml/src/ggml-cuda/hip-autotune-dispatch.cu directly, not taken on the source document's word. This item captures ONLY the verified, judged-realistic slice of that document's Part I (replay hot-path). Do not treat the rest of that document's L0-L3 terminology or DeviceDispatchProfile proposal as verified -- only the specific ordering claim and its proposed fix are captured here.

Source-verified before filing (2026-08-23): the call-order claim above was independently confirmed by reading src/ggml/src/ggml-cuda/hip-autotune-dispatch.cu directly, not taken on the source document's word. This item captures ONLY the verified, judged-realistic slice of that document's Part I (replay hot-path). Do not treat the rest of that document's L0-L3 terminology or DeviceDispatchProfile proposal as verified -- only the specific ordering claim and its proposed fix are captured here.

**SCOPE NARROWED (2026-08-23):** this item now covers specifically RP2 (move native_select() below the warm-cache lookup) and RP3 (add an L0 last-binding fast path in front of the existing L1 8-entry cache) from the source document's own RP1->RP4 staging -- the two changes that are genuinely coupled (RP3's L0 cache only matters as a fast path feeding into RP2's reorder). RP1 (pure instrumentation/counters, no behavior change) and RP4 (cache per-device hardware key/digest once, independent of the reorder question) are carved out as separate, independently-implementable items: HI92 and HI93. This item's own validation section already required RP1-equivalent profiling before implementing -- HI92 is now that hard prerequisite, filed as its own item rather than an implicit blocking step inside this one.

## Change Log

- 2026-08-22T14:18:26.932122+00:00 (created-by): Created by agent

## Ledger-events


- chg_20260822_141935_investigated-whether-rccl-algo_5716
- 2026-08-22T14:19:35.686028+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-23T04:49:02.497485+00:00 (updated-by): Updated: section:notes
- chg_20260823_044909_split-a-bundled-hot-path-optim_2680
- 2026-08-23T04:49:09.851718+00:00 (updated-by): Updated: section:ledger-events
