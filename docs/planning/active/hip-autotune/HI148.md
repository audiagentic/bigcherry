---
id: HI148
order: 0
plan: hip-autotune
state: pending
created-at: '2026-08-29T12:19:13.060264+00:00'
breadth: ''
skill: intermediate
created-by: agent
work: S
---

# RCCL admission gate: promote to validated and wire into default build

## Description

Final implementation slice split from HI145/HI146/HI147. Once the two-level admission gate (HI146 level-1, HI147 level-2) is real, tested, and hardware-validated, this item promotes patch 1225 (or its replacement) from STATE=untested to STATE=validated and adds it to config/recipes.toml's framework patch-set so it actually compiles into default builds. Confirmed this session it currently does NOT ship anywhere despite RCCL_HETEROGENEOUS_RUNBOOK.md calling it 'the production safety boundary' -- this item closes that real gap, but only after the gate is actually correct (not a documentation-only promotion).

## Steps

1. Confirm HI146 and HI147 are both complete with real hardware validation evidence recorded.
2. Update patch 1225's (or its replacement's) patch.toml STATE field to validated.
3. Add the patch to config/recipes.toml's [patch-set.framework] patches list so bigcherry-native/bigcherry builds compile it in by default.
4. Rebuild a real bigcherry-native:control:linux-multi binary and confirm: {0,2}/{1,2}/{0,1,2} servers still start and serve real completions (reuse the same benchmark methodology from HI142 as a smoke check, not a full re-benchmark); a device-3-inclusive tensor-split request still aborts cleanly with the expected message.
5. Full patch test suite + broader test suite must stay green.
6. Update docs/reference/testing/RCCL_HETEROGENEOUS_RUNBOOK.md's closure section to reflect that the guard now actually ships (currently the runbook overstates this).

## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files



## Validation



## Effort & Risk



## Standards



## Acceptance Criteria



## Notes

Do not start until HI146 and HI147 are both functionally complete with their own hardware validation -- this item is the final 'make it real in production' step, not a substitute for either level's own testing.

## Change Log

- 2026-08-29T12:19:13.060264+00:00 (created-by): Created by agent
