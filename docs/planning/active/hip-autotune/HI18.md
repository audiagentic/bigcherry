---
id: HI18
order: 0
plan: hip-autotune
state: in_progress
created-at: '2026-08-05T00:00:00+00:00'
breadth: ''
skill: advanced
created-by: agent
work: M
---

# Tune the multi-device allreduce algorithm (SPLIT_REDUCE)

## Description

The runtime provider choice is still static at backend construction, but the HI58 observation boundary now has a separate, versioned reduction signature. Every HIP SPLIT_REDUCE event can carry the reduced slice geometry and an ordinal-independent topology key derived from the actual peer-access matrix; incomplete or unknown topology evidence is rejected or marked non-promotable offline. Provider selection, complete-path timing, correctness comparison, and promotion remain open.

## Steps

- Add a SPLIT_REDUCE family and its collection point at the allreduce call, retaining the existing `ggml_hip_dispatch_scope` re-entrancy guard.
- **Implemented first runtime contract:** define a reduction-only signature separate from matmul identity. The producer records version 1 element count/type, four-dimensional slice geometry, a topology key from `hipDeviceCanAccessPeer`, and complete/partial/unknown peer-access status. The key excludes raw device ordinals so later hardware grouping can remain topology-based.
- Validate the signature offline before candidate normalization: require version and cross-field agreement, reject malformed shape/topology values, derive a stable signature key, and mark unknown peer accessibility non-promotable.
- Define the remaining reduction signature inputs: device count, topology, split geometry and peer accessibility. Keep raw ordinals as evidence only; do not put them in candidate identity.
- Make `try_allreduce` selectable per call rather than fixed at backend construction, via a defaulted parameter (Option C).
- Implement the three `fallback_policy = none` candidates first — these are the measurement candidates.
- Record `observed.reduce_algorithm`, `observed.fallback_depth` and `observed.fallback_chain` on every call.
- Only then add fallback-enabled candidates, which reproduce production resilience.
- Extend the hardware key so a 2-device topology and a 4-device topology do not share a winner.

## Detailed Solution & Technical Design

The fallback-enabled candidates are **inadmissible as evidence about an
algorithm** unless actual-path telemetry is recorded. `winner = nccl` is false
if the call fell through to butterfly, and the failure is silent — a fallback
chain reports success either way. So telemetry is a prerequisite for those
candidates, not a follow-up: implement the `none` variants and the telemetry
first, and gate the fallback variants behind it.

The static selection at backend construction is the reason this is invisible
today. Making it per-call is the whole mechanism; the rest is signature and
eligibility work.

Measurement is harder than for matmul: an allreduce spans devices, so
complete-path timing (standards 7.1) must cover the slowest participant, not the
launching one. Time the operation from the host across a device-wide sync rather
than with per-device events, and record which device was the straggler.

## Code Samples & Guidance



## Files

- vendor/llama.cpp/ggml/src/ggml-cuda/ggml-cuda.cu — runtime seam remains open
- src/ggml/src/ggml-cuda/hip-autotune-reduce-telemetry.h — versioned reduction signature contract
- src/ggml/src/ggml-cuda/hip-autotune-reduce-telemetry.cpp — runtime shape/topology observation
- src/ggml/src/ggml-cuda/hip-autotune-dispatch.cu — per-call selection remains open
- src/ggml/src/ggml-cuda/hip-autotune-tuner.cu — multi-device timing remains open
- tools/bigcherry/telemetry.py — strict reduction-signature ingestion
- tools/tests/test_telemetry.py
- tools/tests/test_split_reduce_telemetry.py

## Validation

The focused HI18/HI58 telemetry and source-contract suite passes 35/35, and the full Python suite passes 519/519. The new source contract requires `ggml_hip_reduce_signature_v1`, versioned `reduction_signature` JSON, four-dimensional slice geometry, topology-key emission, and actual `hipDeviceCanAccessPeer` observation. Offline ingestion rejects missing or inconsistent signature fields, derives `reduction_signature_key`, and marks unknown peer access non-promotable.

Brutus was fast-forwarded to 97ab347, the overlay applied with all 33 patch files OK, and the rebuilt record target linked successfully. A fresh two-GPU Qwen3.5-9B tensor-split server using RCCL returned a valid completion and emitted 448 rows accepted by the strict loader: every row was requested/effective `rccl`, `handoff=none`, `fallback_depth=0`, with signature `{version:1, element_count:8192, element_type:f32, slice_shape:[4096,2,1,1], topology_key:n2:peer1001, peer_access:partial}`. A second fresh server with `GGML_CUDA_ALLREDUCE=none` returned a valid completion and emitted 896 strict-loader-clean rows: 448 provider-declined observations and 448 successful meta handoffs, all `fallback_depth=1`, with zero promotable rows. Durable artifact hashes: RCCL `500df96329518b35a61ba0033e590a548216cb485f5bec8232f203d30a2f383f`, forced-meta `9d55e166dd882ff5e7f15f162b7d63f1de6b1573660292584eb8f5f37c46da3d`, build log `3d1f49ce9358866805d7a482ce4ce28faeed253fdd595362c30afd58d53cad2e`.

No reduction candidate, per-call provider selection, complete-path timer, correctness comparison, or promotion behavior has changed.

## Effort & Risk

Medium-high risk, and the risk is measurement validity rather than
implementation. A multi-device operation is easy to time wrongly — the launching
device finishing is not the operation finishing. Verify the timing methodology
against a deliberately imbalanced split before trusting any winner.

Requires the 2-GPU server; cannot be validated on the single-GPU workstation.

## Standards

HIP_AUTOTUNE_STANDARDS 5.2 (device-local signature slicing), 7.1 (complete-path
timing), 7.2/7.3 (native as reference and baseline), 10.2 (hardware key without
device ordinal — but device *count* and topology do belong), 12.4 (hard
eligibility)

## Acceptance Criteria

- [x] Reduction signature is a distinct observation-only contract, separate from matmul signature/replay ABI.
- [x] Runtime telemetry records version, element shape, slice geometry, topology key, and peer-access status from the real HIP peer query.
- [x] Topology key excludes raw device ordinals and unknown peer results remain explicit.
- [x] Offline loader validates the signature and derives a stable reduction signature key.
- [x] Unknown peer access is fail-closed for promotion.
- [x] Brutus RCCL and forced-meta two-GPU server canaries emit strict-loader-clean signature evidence and complete valid requests.
- [ ] Per-call provider/algorithm selection is implemented without changing the production fallback contract.
- [ ] None-policy candidates have complete-path multi-device timing and correctness evidence.
- [ ] Fallback-enabled candidates have effective-path telemetry, topology-aware hardware identity, and promotion evidence.

## Notes

The first HI18 implementation slice establishes the missing reduction signature without pretending that provider telemetry is algorithm selection. Raw device ordinals remain useful evidence, but the derived topology key is based on device count and the peer-access matrix. The fresh Brutus evidence proves the signature on both RCCL success and forced-meta fallback: `n2:peer1001` and `peer_access=partial` are observed from the real two-GPU runtime, while fallback rows remain non-promotable. Existing HI58 normalization still maps runtime `rccl` to the HI18 `nccl` identity. HI18 remains open for per-call selection, multi-device complete-path timing, correctness comparison, topology-aware hardware key integration, and promotion.

## Candidate identity

```text
(preferred_algorithm, fallback_policy)
```

Initial HIP candidate set after the HI58 telemetry-only prerequisite:

```text
(nccl, none)
(auto, upstream_default)
```

`(internal, none)` and `(butterfly, none)` are invalid measurement candidates:
internal initialization is unavailable on this branch, and butterfly returns
failure. `meta` is an observed fallback path, not a standalone `none`
candidate. Device count and topology remain execution context in the hardware
key, not candidate identity.

## Change Log

- 2026-08-12T07:39:17.070751+00:00 (updated-by): Updated: section:notes
- 2026-08-13T06:33:12.438737+00:00 (state-transition): State: pending → in_progress

## Ledger-events

- chg_20260814_000252_validated-the-new-hi18-reducti_2286
- chg_20260814_000952_added-the-next-hi18-runtime-sl_3069
- 2026-08-14T00:09:52.868683+00:00 (updated-by): Updated: section:ledger-events
- chg_20260814_001710_corrected-hi18-per-call-reduct_7990
- 2026-08-14T00:17:10.947373+00:00 (updated-by): Updated: section:ledger-events
