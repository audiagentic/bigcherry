---
id: PA26
order: 3
plan: patching-patch-system
state: pending
created-at: '2026-09-11T23:45:58.486781+00:00'
breadth: ''
skill: null
created-by: agent
work: null
priority: P1
---

# PA26

## Description



## Steps



## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files



## Validation



## Effort & Risk



## Standards



## Acceptance Criteria



## Notes

Authoritative sequence is PA27 -> PA26 -> PA28. Older text implying PA26 had to precede the mechanical extraction is superseded.

## 2026-09-15 (continued): real two-arm hardware pass runs end-to-end for the first time -- EVALUATED/non-equivalent receipt, but per-dispatch data shows zero control-vs-candidate divergence

Fresh pass, continuing from the prior real build-identity divergence (`effective_build_id` mismatch note above). GPT design consult (dev-gpt-agent session `ses_c2892cdae7f14feb`) gave a PA26-local build-identity fairness redesign, implemented across three real hardware iterations as each subsequent real gap surfaced -- never forced past, each fix GPT-reviewed before landing:

1. **req_9cd5b140ca544ef8** (commit `8f8516cc`): `_PA26_REMOVED_OPTION_OWNERS` / `pa26_effective_configure_projection()` -- narrow, directional OFF(control)->absent(candidate) CMakeCache normalization, scoped ONLY to options owned by a module actually removed for the comparison at hand (0110's 5 options, 0810's `GGML_HIP_REPLAY_DIAGNOSTICS`). `require_shared_build_inputs` now requires `generated_compile_inputs_hash`/BuildPlan-projection/resolved_revision/input_hashes exact plus this normalized `effective_configure` exact, no longer raw `effective_build_id` equality (still recorded for provenance). Added PA26's own ephemeral diagnostic build profile (`PA26_DIAGNOSTIC_BUILD_NAME`/`build_pa26_diagnostic_build_config`: replay + 0810's hit observer ON, 0110's `GGML_HIP_DISPATCH_DIAGNOSTICS` forced OFF) since `[build.replay-diagnostic]` itself turns that option ON -- a genuine observer asymmetry the OFF/absent rule must not paper over. Never touches `config/recipes.toml`. 46 new offline tests.

2. **req_579777e899454ed9** (commit `db2c7296`): rerun hit a NEW real divergence one stage earlier -- `generated_compile_inputs_hash` itself differs, because the generate-stage worker reads each arm's actual MATERIALIZED SOURCE TREE (`candidate_implementation_digest()` hashes raw candidate source bytes; `hip-autotune-build-hash.h` embeds manifest/build identity) -- an expected removed-module source diff legitimately changes this raw hash. Replaced with a semantic projection: exact compile-input filename set, byte-identical `STRICT_GENERATED_FILES` (the real compiled registry/arch/MMVQ-instance surface), and an identical generated manifest after stripping real per-run identity fields (`generated_at`/`manifest_hash`/`build_descriptor`, per-candidate `implementation_digest`/`implementation_source_files`). `hip-autotune-build-hash.h` is the one file explicitly NOT required to match. 4 new offline tests.

   **Same GPT consult separately flagged a real PA20 serving-boundary concern** (not yet independently confirmed on real hardware, and NOT acted on by patch surgery in this pass -- out of scope for a hardware-comparison pass): `0700_coverage_counters`' family-entry block calls `ggml_hip_dispatch_family(...)` under `GGML_HIP_DISPATCH` outside the diagnostics-only counter guard, i.e. real dispatch-interception plumbing, not just coverage instrumentation; GPT recommended splitting 0700 into serving-core (the interception hooks) vs campaign-support (the diagnostics-only counters) before PA28, and separately flagged 0830 (touches the `GGML_HIP_REDUCE_PLAN` seam in `ggml-cuda.cu` under active dispatch) for the same kind of adjudication. **This remains an open, real, GPT-raised concern requiring its own dedicated patch-authoring pass** (bigcherry-patch-author skill territory) -- see "Real hardware result and what it does/doesn't say about the 0700 concern" below for what this pass's actual runtime data says about it.

3. **req_13b71975cbe94c8d** (commit `6dc605ba`): rerun cleared the generated-catalog gate too, but hit a third, narrower divergence: `GGML_HIP_AUTOTUNE_GENERATED_DIR` (a local filesystem path `build_directory()` derives from `(source_slice_id, build_plan_id)`, both of which already legitimately differ between arms by PA26 design) was the ONLY remaining differing key across the entire captured CMakeCache record. GPT: validate-then-canonicalize (never blindly drop) -- `_PA26_DERIVED_PATH_CONFIGURE_KEYS` (one entry), applied before the removed-module projection, confirms the value ends with the expected `/builds/<source_slice_id>/<build_plan_id>/generated-inputs` suffix for that arm's own identity, then replaces it with a fixed placeholder. Explicitly NOT a generic "differs because it contains source_slice_id" rule (could hide a real future divergence). `GGML_HIP_AUTOTUNE_SIGNATURE_FILE` left uncompared/unexcluded (content-addressed, not build-plan-id-keyed, matched anyway). 2 new offline tests.

4. **Real bug, not a design question** (commit `881a842d`): with build identity now passing for real, runtime execution started for real and crashed immediately: `ShutdownResult.clean` (`tuning/server_runner.py`) is a `@property`, not a method -- `make_real_hardware_runtime_runner` called it as `shutdown.clean()`. One-line fix, only catchable by actually exercising the runtime path on hardware.

Each commit's offline suite (`tools/tests/campaign` + `tools/tests/tuning`) stayed at the same 3 pre-existing unrelated failures (HI104 C++ tuner-flush field test, experiment-contract count off-by-one, VA24 lint) throughout -- confirmed unrelated each time via `git status`.

**Real hardware result** (workdir `/home/audumla/pa26-hw-workdir/pa26-hardware-receipt.json` on Brutus, commit `881a842d`, real RHA15-verify2 winners corpus, tierA-qwen4b-q6k, gfx1100, current pin b10901): all 4 real builds (production control/candidate + diagnostic control/candidate) compiled clean and passed the build-identity gate for the first time. Runtime replay actually executed against the real diagnostic pair. `decision_equivalence.status = EVALUATED`, `runtime.equivalent = false` -- 126 real differences, but their SHAPE matters:

- **0 `arm-divergence` entries** -- for every single one of the 124 mismatched dispatch entries, control and candidate reported the IDENTICAL `(from_cache, candidate, signature)` triple as each other. This is real, hardware-measured evidence AGAINST a per-dispatch behavioral regression from the removed modules in THIS run -- it does not confirm GPT's 0700 family-entry-hook concern above (candidate and control behaved identically to each other on every observed dispatch).
- **124 `mismatch` entries (62 dispatches x 2 arms)**: BOTH arms independently reported `from_cache=False` against the frozen corpus's STATIC expected winner (e.g. expected `mmvq:q6_k:w4:nw4:rpb2:sk0:v1`, both arms observed `mmvq:native:v1`). Since this happens identically in both arms, it reads as a replay/corpus-coverage gap (the synthesized workload via `run_tune_signature_workload` not exercising the exact recorded signatures, or the winners cache not binding as expected in this replay invocation) -- NOT a control-vs-candidate difference. Not root-caused in this pass.
- **2 arm-level field differences**: `output_digest` (real, unexplained -- could be sampling non-determinism from the fixed completion prompt, not yet checked for a fixed seed) and `runtime_args_digest` (a KNOWN harness-measurement artifact, not a real config difference -- `make_real_hardware_runtime_runner`'s env dict includes `GGML_HIP_DISPATCH_HIT_LOG=<hit_log_path>`, and `hit_log_path` is built from `result.source_slice_id[:16]`/`build_plan_id[:12]`, which legitimately differ between arms by PA26 design; this digest should exclude that key the same way other per-arm-identity-derived fields are excluded elsewhere in this harness -- not yet fixed).

**PA26 remains open, honestly.** This is real progress -- the harness now runs the complete real two-arm build+replay pipeline end-to-end for the first time, four real build-identity gaps were found and fixed (three GPT-designed, one a real runtime bug), and the raw per-dispatch data gives a positive signal on the specific removed-module question this pass targeted. But the receipt is not a clean positive, and closing it requires: (a) fixing `runtime_args_digest` to exclude the per-arm hit-log path, (b) root-causing why the replay workload isn't hitting the frozen corpus's recorded winners in either arm, (c) explaining the `output_digest` difference, and (d) separately, still-open: GPT's 0700/0830 serving-boundary concern needs its own dedicated patch-authoring investigation (does NOT block re-running once a/b/c are fixed, since this run's 0-arm-divergence data is real evidence it may not manifest in practice -- but does not resolve the concern either, since the corpus wasn't actually exercising real dispatch selection differences between arms in this run). No hardware left running (verified idle, 0% GPU, no live processes after every pass). No result forced or fabricated.

Commits (this fork, in order): `8f8516cc`, `e85637ed` (lab script fix), `db2c7296`, `6dc605ba`, `881a842d` -- all pushed to both `origin` and `brutus` remotes.

## 2026-09-15 (continued): real two-arm hardware pass run -- all 4 builds succeeded; genuine build-identity divergence found, runtime not yet reached

Implemented the GPT-corrected `real_hardware_runtime_runner` design (previous note's spec) for real: `ReplayExpectation`/`HitRecord` split (reusing `tuning.execution_audit.HitRecord` directly rather than inventing a second per-dispatch type), `compare_runtime_results` changed to `expected <= observed` coverage (extra/fallback dispatches are not a failure) and from_cache/candidate/signature-only entry comparison (recorded_calls excluded, per execution_audit's own non-authoritative-counter caveat), and fixed the real blocking bug in `build_hardware_receipt` (a non-default `runtime_runner` now builds and runs against the DIAGNOSTIC pair via new `diagnostic_control_spec`/`diagnostic_candidate_spec` parameters, validated with `require_diagnostic_matches_production` -- previously it would have wrongly run against the diagnostics-free production pair). Extracted `run_tune_signature_workload()` out of `tuning/workflow.py::_stage_tune` into a shared helper, reused by the new hardware runner. Commit `2781cb6d`; offline suite `tools/tests/campaign` + `tools/tests/tuning`: 1966 passed, only the same 3 pre-existing unrelated failures (HI104, experiment-contract count, VA24 lint -- none touch these files).

**Ran the real hardware pass on Brutus** (commit `f3babbf5`, driver script `tools/lab/pa26/pa26_hw_run.py`) against the real RHA15-verify2 winners corpus (`dispatch.cache`, current pin b10901) and the real `tierA-qwen4b-q6k` model. Verified Brutus idle before starting.

**Real result: all 4 real GPU builds compiled cleanly** (production control/candidate ~502/502 objects each, diagnostic control/candidate likewise) -- roughly 35-40 minutes of real compute. Then `require_shared_build_inputs` (the pre-runtime build-identity fairness gate) **genuinely failed closed**: `control and candidate resolved different effective_build_id identities`. Runtime replay execution was never reached -- the gate correctly stopped before any server launch.

**Root-caused via a real `CMakeCache.txt` diff between the two builds** (not guessed): removing the PA20 serving boundary (0110/0700/0800/0810/0820/0830/0900/1100) from the framework composition causes several `GGML_HIP_*` cmake options (`GGML_HIP_AUTOTUNE`, `GGML_HIP_AUTOTUNE_RECORD`, `GGML_HIP_DISPATCH_DIAGNOSTICS`, `GGML_HIP_REPLAY_DIAGNOSTICS`, `GGML_HIP_ROUTING_TRANSFORM`, `GGML_HIP_WORKSPACE_METRICS`) to be entirely UNDECLARED in the candidate's CMakeCache, vs present-and-OFF in control. `effective_build_id` hashes the raw CMakeCache text for these keys, so absent-vs-OFF differs even though every one of these options' VALUE is inert (OFF) on both arms for a plain `replay`-purpose build -- there is no evidence yet that the actual compiled binary behaves differently, only that its build-script surface differs.

**This is a real, hardware-verified finding -- not forced past, not fabricated.** Two honest ways to close it, deliberately NOT decided unilaterally in this pass:
1. The removed modules genuinely are not build-identity-inert (one or more of them contributes a cmake `option()` declaration that changing framework composition changes) -- a real PA20 serving-boundary gap, reclassify before PA28.
2. `effective_build_id`'s cmake-cache-key hashing is stricter than the behavioral-equivalence question PA26 actually asks -- keys that are absent in one arm and OFF (the default/no-op state) in the other should not count as a fairness-check failure when nothing about resulting binary behavior differs.

Neither was decided here -- this needs a GPT design consult (per this item's own standing practice of consulting before implementation decisions) before either the fairness check or the module composition is changed. No hardware was left running; GPU confirmed idle (0% all 4 devices) and no leftover llama-server/cmake/ninja processes after the run.

**PA26 remains open.** The real two-arm hardware comparison has still not produced an EVALUATED (equivalent-or-divergent) runtime receipt -- it produced a real, hardware-grounded divergence at the earlier build-identity stage instead. This is progress (the harness now runs real builds end-to-end and the runner implementation is real and offline-tested), but the acceptance bar (a reproducible positive receipt, or a genuine divergence with any discovered serving dependency reflected before PA28) is not yet met. Next step: GPT consult on the two options above, then either fix the composition or relax the fairness check accordingly, and re-run.

## 2026-09-15 (continued): RHA15 resolved on real hardware -- fresh real winners corpus now exists for the current pin

RHA15 (the correctness-evidence pipeline bug blocking all winners-corpus production) is now genuinely fixed and verified on real hardware (see RHA15.md, now in docs/planning/completed/) -- root cause was `_stage_correctness_evidence()` routing the mandatory signature-verification preflight through a tune-lane binary lacking `GGML_HIP_AUTOTUNE_RECORD` capability, fixed by threading the already-built record-capable verifier callable through (commit `c2ee3a99`, GPT design review req_f7f5a793c0ce4244).

Re-ran the real campaign end-to-end (`pa26-rha15-verify2`, tierA-qwen4b-q6k / production-safe-single / gfx1100 / devices=0 / current pin) with the fix: **real, clean success** -- 19 candidates genuinely promoted with real correctness evidence (was 0), 62 verified winners, replay build/export/verify passed its own behavioral gate (coverage 5895/5895 dispatched=executed, 0 rerun_required, not stale). This produced a real `promoted.jsonl` (4.1MB, real winners) and a real `dispatch.cache` (6.3KB) for the current pin -- preserved at `/home/audumla/bc-pa-artifacts/pa26-rha15-verify2/workdir/` before any cleanup, alongside the full campaign receipt (`tune-campaign-receipt.json`) and inventory/tune sqlite databases.

**This satisfies PA26 step 1's prerequisite** (a known-real promoted winners/replay database with recorded hashes -- the receipt's `tune`/`record`/`signature_verifier`/`replay_validation` sections each carry `source_slice_id`/`build_plan_id`/`manifest_path`, and the current pin is `b10901`/`28ff0958291ce3465fabd7bd679d4b0edd742bd9` as established earlier this session).

**Not yet done in this pass** (honest status, nothing fabricated): PA26's own remaining core work -- building the PA27 serving-core candidate from the same resolved source, running the actual two-arm HIP hardware replay-equivalence comparison via `build_hardware_receipt()` in `tools/bigcherry/campaign/replay_equivalence_hardware.py`, and implementing/exercising GPT's corrected `real_hardware_runtime_runner` design (the `ReplayExpectation`/`HitRecord` split, `expected<=observed` coverage, `build_diagnostic_hardware_specs` wiring correction -- see this file's own 2026-09-15 notes above for the full corrected design) against this real corpus. That remains open, unattempted this pass -- a substantial next step in its own right, not a quick follow-up. PA26 stays active/pending; do not mark completed until that real two-arm comparison actually runs and reports a genuine equivalent-or-divergent result.

## 2026-09-15: correctness-evidence bug spun out to RHA15

After 3 forked investigation passes (see notes above) ruled out several
hypotheses without confirming the root cause of the tune-campaign
correctness-evidence FAIL blocking PA26's winners-corpus generation, this
is now tracked as its own item: RHA15 (docs/planning/active/run-hip-autotune/RHA15.md)
-- it's a general dispatch/correctness-evidence pipeline bug affecting any
future tune campaign, not PA26-specific, so it deserves its own scoped
investigation rather than more open-ended forking under PA26.

PA26 remains blocked on RHA15's resolution for its corpus-generation and
real hardware comparison steps. Everything else (offline oracle, hardware
harness scaffolding, all 15 framework patches' current-pin evidence) is
done and GPT-reviewed.

## 2026-09-15 (continued): cwd hypothesis directly tested and RULED OUT via real hardware

Continuing the correctness-evidence root-cause investigation. Built the exact real lane the pipeline uses (`bigcherry-native:tune:linux-multi+hi105-correctness`, `bin/test-backend-ops`, via `python -m bigcherry build --lane ... --experiment hi105-correctness --binary-relative-path bin/test-backend-ops --arch gfx1100 --inventory <real inventory>`) to get a binary at the SAME kind of artifact-store path (`~/.cache/bigcherry/builds/<source_slice_id>/<build_plan_id>/bin/test-backend-ops`) the real campaign resolves, rather than reusing an old ad-hoc build.

Directly tested the one concrete untested hypothesis from the prior note ("cwd the real subprocess call inherits (bc-pa-work) vs the binary's own directory"): ran the SAME forced-candidate STRICT invocation (`GGML_HIP_DISPATCH_MODE=replay GGML_HIP_FORCE_CANDIDATE=<real registry candidate name> GGML_HIP_FORCE_CANDIDATE_STRICT=1`, near-empty `env -i` matching `run_test_backend_ops`'s real env shape exactly) from BOTH the binary's own `bin/` directory AND from `/home/audumla/bc-pa-work` (the real campaign's actual cwd).

**Result: byte-for-byte identical abort in BOTH cwds** -- `ggml_hip_dispatch_resolve` -> `ggml_abort` (SIGABRT), same call stack, same libraries, in both directories. **cwd is conclusively ruled out as the explanation.**

**Caveat on this specific run**: this used `-p ".*"` (the fixed synthetic MUL_MAT corpus) rather than a `--test-file` line for the exact original failing signature (m=2560/n=512/k=9216) -- constructing that requires the real binary dispatch-signature dict shape (op id + type fields decoded from a real registry entry), which needs more of `signature_mapping.py`'s internals than was practical to hand-assemble in this pass. The wildcard `-p` mode legitimately hits many shapes the forced candidate is NOT eligible for, so this abort is EXPECTED/correct STRICT behavior for most of those shapes, not necessarily reproducing the original bug -- it does NOT independently confirm the original signature's forced-candidate combination is broken, it only rules out cwd as a variable, since the SAME abort-or-not outcome was identical across both directories for every shape the wildcard corpus covers.

**Net effect on the two remaining hypotheses from the prior note**:
- cwd: RULED OUT (this test).
- cross-call/order-dependent state across ~31 sequential correctness-evidence subprocess invocations in the real pipeline: still untested, and now the more likely remaining explanation given cwd is eliminated.

**Decision: stopping this investigation pass here rather than continuing to dig.** Root-causing an order/state-dependent effect across many sequential subprocess invocations needs either a full real tune-campaign re-run with fresh diagnostic-hardening instrumentation in place (the 4a1939d0/8ce29dfc commits, not yet exercised against a real failure) or a deliberate multi-invocation stress harness -- both substantial, dedicated efforts, not a quick follow-up. This bug remains real, reproducible, structural, pre-existing, and affects the whole `tune-campaign` correctness-evidence pipeline (not PA26-specific, not caused by PA26's patches). No fix was attempted in this pass; no evidence fabricated.

**Recommendation reaffirmed**: this deserves its own dedicated plan item/investigation (HI-prefixed, e.g. under `patching-hip-autotune` or a `tuning-` group) with real budget for a full re-run under the new diagnostics, rather than continuing to fold it into PA26 passes. PA26 itself remains blocked on this bug for real winners-corpus production and therefore on the real two-arm hardware comparison; the offline oracle, hardware-harness scaffolding, and all 15/15 framework patches' evidence remain the completed, durable progress from this session. PA26 not marked completed.

## 2026-09-15 (continued): correctness-evidence diagnostic hardening (not a root-cause fix)

Following the real tune-campaign correctness-evidence failure documented above
(every candidate: candidate=failed on all 3 seeds, reproduced identically
across two independent full campaign runs; an isolated manual repro of the
same binary+signature+candidate+environment passed cleanly rc=0; root cause
not isolated), investigated the actual C++/Python code path rather than
re-running hardware blind.

**Real finding**: `GGML_HIP_FORCE_CANDIDATE_STRICT=1` has TWO distinct SIGABRT
abort paths in `hip-autotune-dispatch.cu`, not one -- the "not found in
registry" path (already detected by `correctness_evidence.py`'s
`_REGISTRY_MISMATCH_RE`, HI106, already ruled out by the prior fork) and a
second, PREVIOUSLY UNDETECTED path: `can_execute(candidate, sig, hw)`
returning false ("is not eligible for this signature") for a candidate that
IS registered but fails its own hard-eligibility check for the exact
requested signature. Before this fix, that second path silently folded into
the same generic, undiagnostic "failed" execution_status the aggregate error
reports -- exactly the class of misdiagnosis HI106's own fix comment
describes, now closed for its sibling.

**GPT consultation** (session `ses_c2892cdae7f14feb`, request
`req_488640d20a7f4fa6`): asked whether this eligibility theory mechanically
explains "manual repro passes, pipeline's own run fails" for a documented-pure
function of (candidate, sig, hw). GPT's answer: device-index-mismatch theory
weakened (rocm-smi confirms device 0 = gfx1100 on Brutus, matching the
campaign's real `devices=0`); the stronger mechanism is an EFFECTIVE-SIGNATURE
MISMATCH -- the evidence path does not replay the original live dispatch, it
reconstructs a SYNTHETIC test-backend-ops graph from canonical signature JSON
(`signature_mapping.py` explicitly documents this as reconstructed/assumed
tensor shapes/strides), then C++ REGENERATES a signature from that synthetic
graph and forces the candidate against the REGENERATED signature -- which
might not bit-for-bit equal the originally requested production signature.
The fused-GLU evidence path (HI119) already has an explicit preflight check
for exactly this (`_observed_signature_hex`, comparing the C++-observed
regenerated signature against the row's own claimed signature before trusting
any comparison), but the ordinary MUL_MAT/MUL_MAT_ID path never had it.

**What was landed** (commit `4a1939d0`, pushed both remotes):
1. `_INELIGIBLE_CANDIDATE_RE` + detection in `collect_candidate_seed_evidence`
   (`tools/bigcherry/tuning/correctness_evidence.py`) -- raises a specific
   `EvidenceError` naming the candidate and exact requested signature
   (src0/src1/dst types, m/n/k) instead of the opaque generic failure.
2. Extended `_observed_signature_hex()` (already signature-shape-generic
   underneath, just not parameterized for `test_file=`) and
   `generate_for_candidate()`'s ordinary (non-GLU) branch
   (`tools/bigcherry/hi80_generate_correctness_evidence.py`) to run the SAME
   observed-signature preflight the GLU branch already has, closing the gap
   GPT identified for the far more common non-fused case.
3. Updated 4 affected test fixtures (all needed to handle
   `GGML_HIP_DISPATCH_MODE=record` now that the ordinary branch also drives
   it) and 2 `subprocess_runs` count assertions (+1 each for the new
   preflight call); added 1 new test for the ineligible-candidate diagnostic.
   `tools/tests/tuning`: 1089 tests, 1 pre-existing unrelated failure (HI104
   C++ tuner-flush field test, untouched by this change, confirmed via
   `git status` showing only these 4 files modified).

**Explicitly NOT claimed** (per GPT's own instruction: "do not describe the
commit as fixing or identifying the prior PA26 failure... describe it as
diagnostic hardening discovered during that investigation"): this commit does
NOT prove what actually happened in the two prior failing campaign runs --
the temp build/dispatch-db artifacts from those runs are gone
(`tempfile.TemporaryDirectory` auto-cleanup), so there was no way to get real
hardware confirmation of which of the two new diagnostics (if either) would
have fired. GPT was explicit that hardware confirmation of the historical
incident is not required before landing this -- the diagnostic value is
independently justified.

**GPT's recommendation for the next real campaign attempt** (not yet done):
persist the first failing candidate's full forensic bundle (candidate name,
seed, requested dispatch/signature digests, canonical signature JSON,
generated test-file/moe-glu line, argv, relevant env, binary/build identity,
GPU identity, returncode, stderr) BEFORE temp-dir cleanup, so a future real
tune-campaign run either gets sharply localized immediately by these two new
diagnostics, or leaves enough evidence for root-cause analysis without
re-running hours of hardware work again.

**PA26 remains open.** No real winners corpus has been produced yet; the
real two-arm hardware comparison has not run. Next real step: a fresh
`bigcherry tune-campaign` (same tierA-qwen4b-q6k / production-safe-single /
gfx1100 parameters) with forensic-bundle persistence added first, to see
whether these new diagnostics now explain a real failure (if one recurs) or
whether the campaign proceeds cleanly this time.

## 2026-09-15 (continued): corpus-generation failure confirmed non-transient across 2 independent runs

Ran a full second, independent `tune-campaign` (`campaign2`/`log2.txt`, same source/model/devices/profile) to check whether the correctness-evidence failure was transient/order-dependent. **Result: byte-identical failure** -- `EvidenceError: one or more seeds did not execute cleanly, refusing to aggregate: seed=1 native=ok candidate=failed, seed=2 native=ok candidate=failed, seed=3 native=ok candidate=failed`, same traceback. This rules out simple flakiness/resource contention as the explanation.

Also confirmed (via build-directory mtimes) that the binary manually probed earlier (`3373f2a2...`, the newer of the two `hi105-correctness`-experiment build dirs under the same build-plan hash `57de47ef...`) is very likely the same binary the real correctness stage actually used -- so the standalone repro that passed cleanly (rc=0) was almost certainly against the correct binary, not a red herring from testing the wrong build.

**Root cause genuinely not isolated in this pass.** Candidates considered and ruled out: registry mismatch (raises a distinct, regex-matched diagnostic -- not what occurred), the near-empty subprocess environment `run_test_backend_ops` constructs (reproduced exactly via `env -i` + the same 3-4 vars -- still passed), wrong binary (timestamps suggest the right one was probed), transient/flaky failure (ruled out by the second identical run). Remaining untested hypotheses: `cwd` the real subprocess call inherits (`/home/audumla/bc-pa-work`, the Python process's cwd) vs the binary's own directory (my manual repro ran from within the build's `bin/` dir); some form of cross-call state carried across the ~31 sequential correctness-evidence subprocess invocations in the real run (e.g. a shared GPU/driver resource, a stale env var leaking between iterations) that a single isolated invocation cannot reproduce.

**This is a real, structural, pre-existing bug in BigCherry's standard `tune-campaign` correctness-evidence pipeline** (`tools/bigcherry/tuning/correctness_evidence.py` / `hi80_generate_correctness_evidence.py`), not a PA26-specific problem and not something PA26's own patches caused. It blocks ALL real winners-corpus production via this pipeline, not just PA26's. Given the diagnostic effort already invested without a confirmed root cause, further investigation is its own separate, scoped debugging effort (likely warrants its own plan item) rather than something to keep chasing inside this PA26 pass.

**Summary of this pass**: no working corpus produced, no code changed, no evidence fabricated, GPT's corrected `real_hardware_runtime_runner` design (previous note) remains unimplemented pending a real corpus to test it against. PA26 remains open. Recommended next step: open a dedicated investigation/plan item for the correctness-evidence pipeline bug (real, reproducible, deterministic, currently blocking all real tune-campaign corpus production), then retry PA26's corpus generation once that's fixed.

## 2026-09-15 (continued): real tune-campaign attempt -- correctness-evidence stage failed; GPT gave a corrected real_hardware_runtime_runner design

With all 15/15 framework patches now evidenced, ran a real `bigcherry tune-campaign` on Brutus (source=bigcherry-native, platform=linux-multi, model=tierA-qwen4b-q6k (Qwen3.5-4B-UD-Q6_K_XL.gguf), devices=0 (gfx1100), runtime-profile=production-safe-single -- GPT's earlier recommendation) to produce PA26's real current-pin winners corpus.

**Real result: FAILED at the correctness-evidence stage**, not at build/record/tune. 67 dispatch rows produced (0 auto-promoted; 31 `rejected_no_correctness_evidence`, 30 `native`, 6 `confirmation_rejected`) -- i.e. tuning itself worked and found real candidates faster than native, but generating the required correctness evidence for the top provisional winner failed: `EvidenceError: one or more seeds did not execute cleanly, refusing to aggregate: seed=1 native=ok candidate=failed, seed=2 native=ok candidate=failed, seed=3 native=ok candidate=failed`.

**Deep-dived this (not just logged and moved on)**: extracted the exact failing row (`provisional_winner="mmq:q8_0:j64:fb0:t128:o1:i64:sram-q8_0:k256:sk0:v1"`, signature m=2560/n=512/k=9216) from the surviving `promoted.jsonl`, built the exact `--test-file` line via `signature_mapping.signature_to_any_test_file_line`, and manually re-ran `test-backend-ops` directly against the SAME built binary with `GGML_HIP_DISPATCH_MODE=replay GGML_HIP_FORCE_CANDIDATE=<name> GGML_HIP_FORCE_CANDIDATE_STRICT=1` -- **it passed cleanly (rc=0, err=3.46e-08, well within threshold)**, both with a full environment and with the exact near-empty environment `run_test_backend_ops` actually constructs (verified via `env -i` reproduction matching `correctness_evidence.py`'s real `run_env = dict(env or {})` call shape, which the caller never populates with `env=`). So this is NOT a registry-mismatch (confirmed: that raises a distinct, regex-matched diagnostic `_REGISTRY_MISMATCH_RE`, and a deliberate probe with a bogus candidate name DID produce that exact message) and NOT explained by the near-empty subprocess environment either -- both manual repros with the exact real binary/signature/candidate succeeded. **Root cause not yet found**; most likely something order/state-dependent across the real pipeline's many sequential correctness-evidence calls (residual GPU/process state, wrong build variant selected in the real run vs the one I probed, or something else not yet isolated) rather than a simple, deterministically-reproducible bug in this one candidate. Launched a full retry (`campaign2`/`log2.txt`) to check whether this is transient -- result pending as of this note.

**Separately, asked GPT (req_a6a387bdefbc474c) to correct `real_hardware_runtime_runner`'s design** before implementing it, having found a real field-shape mismatch: `RuntimeEntryResult` (from the harness commit 0c00135e) invented `config_binding`/`transform_id`/`match_kind`/`outcome` fields that the REAL `GGML_HIP_DISPATCH_HIT_LOG` JSONL format (parsed today by `tuning/execution_audit.py::load_hit_log` into `HitRecord`) does not carry at all -- the real format is only `{dispatch, signature, candidate, calls, from_cache}`. GPT's correction (full text in ledger event / GPT session `ses_c2892cdae7f14feb`):
- Split into `ReplayExpectation` (static, from the frozen cache's own `transform_id`/`match_kind`/`manifest_hash`/etc via `replay.read_cache`) vs runtime observation (reuse `execution_audit.HitRecord` directly -- do not invent a second type).
- Coverage check should be `expected_dispatches <= observed_dispatches` (extra/fallback dispatches in the hit log are NOT a PA26 failure), not exact-set equality.
- Per-entry comparison should NOT include `recorded_calls` in equality (L1/L2 cache bypass makes it a non-authoritative counter per `execution_audit.py`'s own documented caveat) -- compare `from_cache`, `candidate==expected.winner`, `signature` only.
- **Blocking correction to `build_hardware_receipt`**: it currently builds and passes the PRODUCTION pair's `CampaignLaneResult` to `runtime_runner`, but the production pair is intentionally diagnostics-free and CANNOT emit a hit log at all -- the function must build/use the DIAGNOSTIC pair (`build_diagnostic_hardware_specs` + `build_serving_core_diagnostic_config`, already implemented) for runtime execution, validated via `require_diagnostic_matches_production` (already implemented), while the production pair remains what output/correctness equivalence is proven against. This wasn't wired up correctly yet in the existing harness code -- a real bug in the current implementation, not something new to build from scratch.
- Reuse `tuning/workflow.py::_stage_tune()`'s existing inline request-generation sequence (one decode-shaped completion + a bounded synthetic prefill sweep sized to the runtime profile's `tune_context`) as the real replay-driving workload -- extract it into a reusable `run_tune_signature_workload()` helper shared by `_stage_tune()` and PA26, rather than PA26 inventing per-dispatch-digest request construction (which GPT confirmed has no real inverse mapping/CLI support and should not be attempted).

**Not yet implemented**: this design correction (real code changes to `replay_equivalence_hardware.py`) -- blocked on first getting a real corpus, since there's no point re-wiring the runner without real data to run it against. No code was changed in this pass beyond diagnosis. No hardware evidence fabricated. PA26 remains open.

## 2026-09-15: all 15/15 framework patches now have current hardware evidence

User gave standing authorization ("start it - always start hardware test when
needed") for all future hardware work on this project, not just this
instance. Re-verified Brutus idle (0% GPU, no live processes) before each
step below.

**Found and fixed the real cause of the prior 11-patch blocker**: the
README-authoring pass gave those 11 patches `validator="apply"/"build"`
(the generic builtin validators), which require a real `control_source`/
`subject_source` directory pair -- a source DIFF, which `--framework-
configuration` mode never produces (it populates `configuration_evidence`
instead: single-composition apply proof + production/diagnostic build
completion). Confirmed via a real hardware run: all 11 built successfully
(both framework-production and framework-diagnostic compiled clean) but
failed evidence generation with "control and subject sources are required
for apply evidence" -- a validator-shape bug, not a hardware or gate
problem. Fixed by giving all 11 patch-local `validation/checks.py` with
`check_apply`/`check_build` reading `configuration_evidence`, identical in
shape to 0100/0110/0300/0700's existing pattern (no patch-specific
"configuration" check added, consistent with GPT's earlier confirmation
that apply+build alone is the correct bar). Verified offline via
`validation_policy.require_execution_package()` before spending any more
hardware time. Commit `d5afeb10`.

**Then generated real evidence for all 11** on Brutus (gfx1100, clean
checkout `/home/audumla/bc-pa-work`): rc=0 for every one --
`0200_dispatch_hook`, `0400_mmvf_forced_block`, `0500_mmf_forced_nwarps`,
`0600_mmvq_geometry`, `0650_mmvq_native_variant`,
`0800_server_shutdown_endpoint`, `0810_replay_hit_diagnostics`,
`0820_measurement_signature_shapes`, `0830_split_reduce_telemetry`,
`0900_pool_workspace_metrics`, `1100_hi70_direct_op_evidence`. Real GPU
compiles, real evidence records bound to the current pin (`b10901`,
`28ff0958291ce3465fabd7bd679d4b0edd742bd9`). Commit `967cf17a`, pushed to
both remotes.

**Result: all 15/15 framework patches now carry current-pin validation
evidence.** This was the exact prerequisite the earlier hardware-run
attempt was missing -- the framework build admission gate should now pass
for the full `bigcherry-native` composition.

**Next step**: produce the real bounded winners corpus (GPT's earlier
recommendation: `tierA-qwen4b-q6k`, `production-safe-single` runtime
profile, gfx1100, via the record->tune->promotion/correctness->export
pipeline / `bigcherry tune-campaign`), then implement/exercise
`real_hardware_runtime_runner` in `replay_equivalence_hardware.py` and run
PA26's actual two-arm HIP build+replay comparison via
`build_hardware_receipt()`. PA26 remains open pending that real comparison
result.

## 2026-09-15: README/validation.toml authored for the 11 missing framework patches

Unblocked `require_execution_package()`'s README gate for 0200, 0400, 0500,
0600, 0650, 0800, 0810, 0820, 0830, 0900, 1100 -- the 11 of 15 framework
patches a prior fork this session found had no README.md at all, which
failed closed before any real evidence generation could start. Commit
`b923bfc2`, pushed to both remotes.

Each README describes the patch's real implementation (from its own
patch.py docstring and existing SUMMARY.md) and states explicitly what
apply+build validation does/does not prove. `validation.toml` is the
minimal template shape (apply+build only, no custom check) for all 11.

GPT reviewed (dev-gpt-agent session `ses_c2892cdae7f14feb`,
`req_84fb9ad1e65c4ec0`) and confirmed apply+build-only IS the appropriate
bar for these local framework-configuration packages (no RD/contract
binding => no additional obligation), and explicitly said not to add
lightweight custom checks just to look stronger -- a source-shape check
would not prove e.g. 0650's abort/routing semantics or 0900's counter
correctness, which need real behavioral evidence apply+build never claims.
GPT caught two real README inaccuracies before commit (0900's actual field
names -- `bc_workspace_in_use`/`bc_workspace_peak`, not a single
"bc_requested_size counter"; 1100's overstated "never hit" claim, corrected
to "hard-to-reach/not reliably exercised"), both fixed in the committed
text.

Verified all 11 now resolve a real `ValidationPlan` via
`validation_policy.require_execution_package()` directly (not just
assumed). `patch-lint` clean apart from 4 pre-existing unrelated
RD-experimental failures (1200/1208/1217/1221, outside the framework set).

**Real, pre-existing (not caused by this change) test debt found and
isolated-confirmed**: `tools/tests/patch/test_framework_configuration_campaign.py`,
`test_framework_configuration_evidence.py`, and `test_patch_selection.py`
have 4 failures that reproduce identically with these 22 new files
temporarily removed -- they predate this change. Two root causes: (1) PA27's
earlier rename of 0100's validation check id
(`coverage-source-selection` -> `serving-source-selection`) was never
reflected in these three test files, which still assert the old id; (2)
`test_patch_selection.py`'s `test_source_selection_has_real_patch_set_identity`
hardcodes `len(native.patch_ids) == 15` but `bigcherry-native` composes
`framework` (15) + `upstream-fixes` (1, `1000_rdna4_mmq_q2k_q6k_fix`) = 16 --
a stale assertion, not a real bug. Left unfixed as out of scope for this
README-authoring pass; flagged here for whoever next touches
`tools/tests/patch/` test hygiene.

**Next step for PA26 hardware closure** (per the fork before this one):
retry the GPT-recommended bounded real evidence-generation run
(`--framework-configuration`, tierA-qwen4b-q6k, production-safe-single,
gfx1100) now that all 15 framework patches have real README+validation.toml
packages -- this was the exact prerequisite that attempt was missing.

## 2026-09-15: real hardware evidence generated for 4/15 framework patches; new structural blocker found

User gave standing authorization ("start it - always start hardware test when
needed") and confirmed Brutus free. Verified independently (0% GPU across
all 4 GPUs, no live processes, no tree-activity leases) before touching
anything.

**Ran the recommended unblock step**: `python -m bigcherry.patch.validation_campaign
--patch <id> --framework-configuration --baseline-source bigcherry-native
--hip-path $BC_ROCM_SHIM --amdgpu-targets gfx1100 --workdir ... --build-root ...
--worktree-root ...` on Brutus (clean checkout `/home/audumla/bc-pa-work`,
fast-forwarded to e890d516) for all 15 framework patches.

**Real result**: 4 succeeded (real GPU compiles, production + diagnostic
framework builds verified) -- **0100_cmake_options, 0110_campaign_tune_record_build,
0300_mmq_forced_j, 0700_coverage_counters** -- committed as `05feaa92`, pushed to
both remotes. This unblocks the exact three patches this session's earlier
blocker named as stale (0100, 0110, 0300).

**New real blocker found** (structural, not hardware-capacity): the other 11
of 15 framework patches -- 0200_dispatch_hook, 0400_mmvf_forced_block,
0500_mmf_forced_nwarps, 0600_mmvq_geometry, 0650_mmvq_native_variant,
0800_server_shutdown_endpoint, 0810_replay_hit_diagnostics,
0820_measurement_signature_shapes, 0830_split_reduce_telemetry,
0900_pool_workspace_metrics, 1100_hi70_direct_op_evidence -- have no
`README.md`. `validation_policy.require_execution_package()` fails closed
before any build is attempted: `"<id>: cannot start a new validation run --
missing README.md (structural grandfathering never authorizes execution)"`.
This is real, pre-existing, and correctly fail-closed -- not something to
override. **Fixing it is patch-authoring work (writing 11 real, accurate
README.md files per the bigcherry-patch-author skill), not hardware-test
work** -- doing it well requires reading each patch's actual implementation,
not template-filling, so it was not attempted in this pass rather than risk
fabricated/inaccurate documentation.

Since PA26's control arm (`bigcherry-native` = full `framework` patch-set)
requires current evidence for EVERY framework patch, not just the ones this
session touched, **this blocks PA26's real winners-corpus generation and
two-arm hardware comparison until those 11 README.md files exist and their
evidence is generated the same way**.

**Secondary finding (worked around, not fixed)**: a real, separate,
pre-existing bug in `validation_campaign.py`'s `generate_registry()` -- its
internal `bigcherry generate` subprocess runs with `cwd=REPO_ROOT/tools`, so
a relative `--workdir`/`--build-root` CLI argument is silently reinterpreted
against that directory instead of the campaign's own cwd, writing generated
compiler inputs to the wrong location and producing a confusing "generated
compiler inputs missing" error rather than a path bug. Worked around by
using absolute paths for every invocation; did not touch the source. Worth
fixing separately (`docs/reference/testing/PATCH_VALIDATION.md`'s own
example command uses a relative `<new-run-directory>`, which would hit this).

Scratch build artifacts (2.0GB) left at `/home/audumla/pa26-evidence-run` on
Brutus in case a follow-up pass can reuse the build-root for patches once
their README.md exists (same isolated-worktree content hash).

**Next step**: author real README.md files for the 11 listed patches (a
separate, scoped effort -- likely its own plan item or at minimum its own
pass), then re-run `--framework-configuration` for each, then retry the
real winners-corpus generation (`bigcherry tune-campaign` against
`tierA-qwen4b-q6k` / `production-safe-single` / gfx1100, per GPT's earlier
recommendation) and finally PA26's real two-arm hardware comparison via
`build_hardware_receipt()`. PA26 remains open.

## 2026-09-14 (later still): deeper real blocker found -- framework-wide validation evidence is stale

User authorized proceeding autonomously and confirmed Brutus free. GPT
(req_63c55140aa2543f1) recommended a bounded, real, current-pin acceptance
corpus (not the full production tune campaign) as legitimate PA26 evidence,
covering the five serving-candidate dimensions (0300/0400/0500/0600/0650)
via the normal record->tune->promotion/correctness->export pipeline
(`bigcherry tune-campaign`).

Acted on this: identified a real, small, already-registered model
(`tierA-qwen4b-q6k`, Qwen3.5-4B-UD-Q6_K_XL.gguf, MTP-capable, dense+GDN
hybrid -- "the cheapest real model that still exercises the production
kernel paths" per config/models.toml) and the existing `production-safe-single`
runtime profile (which HI143/GP11 gave a real behavioral gate). Attempt 1
failed cleanly: Brutus's configured campaign tree
(/mnt/vault/development/projects/bigcherry/workspaces/main) has uncommitted
local changes in the BigCherry repo itself (not just vendor/llama.cpp), and
`require_clean_bigcherry()` correctly refused rather than silently building
from an ambiguous tree -- did not override this (per the bump-llamacpp
skill's own guidance to prefer a separate known-clean checkout over forcing
past tree ambiguity). Attempt 2 used a separate, already-existing clean
checkout (/home/audumla/bc-pa-work, tracking the `brutus` remote/
patch-refactor branch), fast-forwarded to eb9bf612 (this session's latest
PA26 push), pointed --llama-root at the shared vendor/llama.cpp checkout
(read-only reuse, correct pin/tag b10901 already materialized there).

**Real blocker found (deeper than the missing-corpus problem)**: the
record-arm build failed patch admission with `source materialization
identity mismatch; validation digest is stale; implementation digest
mismatch; ... compiled target coverage incomplete; required check identity
mismatch: serving-source-selection; required manifest check missing`
repeated for essentially every framework patch, with two spelled out
explicitly by name: `0110_campaign_tune_record_build: no current framework
configuration evidence` and `0300_mmq_forced_j: no current framework
configuration evidence`. This is `tools/bigcherry/patch/validation_campaign.py`'s
`--framework-configuration` admission gate: a framework patch cannot enter a
real build until its own current-build-identity-bound validation evidence has
been generated by actually RUNNING that mode
(`python -m bigcherry.patch.validation_campaign --patch <id>
--framework-configuration --baseline-source bigcherry-native --hip-path ...
--amdgpu-targets gfx1100 --workdir ...`) -- `patch-validate` only VERIFIES
existing evidence, it does not generate it.

This is NOT something to override (no `--allow-dirty`/force flag exists for
it, and none should be used even if one did -- the gate is correctly
fail-closed: it is refusing to build a real serving/campaign composition
whose own patches' evidence doesn't match this exact source/build identity,
which is exactly the kind of silent drift PA26 itself exists to prevent).
Generating current framework-configuration evidence for the affected
patches (0100, 0110, 0300 at minimum -- likely more, given the generic
staleness messages for the rest of the 15-module framework set) is itself
real, separate hardware work, probably comparable in scope to a full
validation pass across the framework patch-set, not a quick unblock.

**No hardware was left running** (verified: no bigcherry/llama-server/
ninja/cmake processes on Brutus after this pass). No gates were overridden,
no evidence was fabricated. PA26 remains open.

**Recommended next step**: before re-attempting PA26's tune-campaign,
generate current `--framework-configuration` evidence for every framework
patch whose evidence this admission gate reports stale/missing (starting
with the three PA27/PA26-touched ones this session: 0100, 0110, 0300) --
this is real, substantial, separate work and should be tracked/scoped as
its own step (or its own plan item) rather than folded silently into PA26's
next attempt.

## 2026-09-14 (later): real hardware run attempted, blocked on missing current-pin winners corpus

Brutus came free (confirmed idle: 0% GPU across all 4 GPUs, no live processes,
no tree-activity leases). Attempted to proceed to PA26's real two-arm hardware
build+replay comparison (implementing `real_hardware_runtime_runner` in
`tools/bigcherry/campaign/replay_equivalence_hardware.py` per its own
docstring).

**Real blocker found before any hardware was touched**: searched this repo's
`docs/evidence/` and Brutus's `/mnt/vault/development` tree for a real,
current-pin promoted-winners corpus (cache or measurements) to use as PA26's
fixed test corpus (step 1 explicitly requires "a known-real promoted
winners/replay database"). Every artifact found is stale:

- Newest measurements file found: `docs/evidence/2026-09-10-rha08-build-matrix/
  rha08-tune.jsonl.measurements.jsonl`, `source_revision =
  2578138397d7b422bb0e160efdd429976c55fb55`.
- Current pin (`config/recipes.toml` `pinned = "b10901"`) resolves to full SHA
  `28ff0958291ce3465fabd7bd679d4b0edd742bd9` -- a completely different
  revision.
- No inventory/dispatch `.db` files matching the current pin exist anywhere
  searched.
- All other `.cache`/`.measurements.jsonl` artifacts found are older still
  (2026-08-21, 2026-08-27) and/or live only in archived/superseded trees
  (`bc-branch`, `archive/home/bigcherry*`).

Per this session's own established rule: never use the historical schema-1
`dispatch-27b-v5.cache` directly on this path, and never fabricate a corpus.
Producing a real current-pin corpus requires running a full tune campaign
(build the tune arm, run a real production workload, gather measurements,
export via `replay.build()`) -- itself a separate, multi-hour undertaking,
not something to start unilaterally mid-task without explicit authorization.

Submitted this exact situation to GPT (dev-gpt-agent session
ses_c2892cdae7f14feb, request req_63c55140aa2543f1) asking whether to (a)
stop and leave this for a dedicated fresh-tune-campaign pass, (b) whether a
smaller real (not fabricated) minimal tune run would count as legitimate PA26
evidence, or (c) something else. That request was still running (confirmed
healthy via diagnostics, not stalled) after ~25 minutes of waiting; its
answer was not available before this pass had to conclude -- check
req_63c55140aa2543f1 in that session for GPT's actual answer before deciding
how to proceed.

**No hardware was touched, no corpus was fabricated, no code was changed in
this pass.** PA26 remains open. Next step: either (1) get GPT's answer on
the corpus question and act on it, or (2) explicitly authorize and run a
fresh tune campaign against the current pin to produce a real winners corpus
before attempting the two-arm hardware comparison again.

## 2026-09-14 (continued): hardware-run harness scaffolding (offline-testable)

Per GPT's explicit next-step guidance after the offline oracle landed
("proceed immediately with the hardware-run harness now rather than
waiting for Brutus... leave only actual HIP execution NOT_EVALUATED"),
built stage 2 -- commit 0c00135e, reviewed by dev-gpt-agent
(req_a4974e46d4d643e0) before commit, all four blocking findings applied:

- `tools/bigcherry/campaign/replay_equivalence_hardware.py`: materializes
  and builds BOTH arms through the REAL production pipeline
  (`campaign.lane.execute_campaign_lane`, never reimplemented) behind an
  injectable `lane_executor` (defaults to the real function; tests inject a
  fake returning hand-built real `CampaignLaneResult`/`BuildPlan`/
  `ArtifactRef` objects -- the real execute_campaign_lane is never invoked
  by any test, since that would require a real HIP compile).
- Build-identity fairness (GPT finding): compares a `BuildPlan` PROJECTION
  EXCLUDING `source_slice_id` (which legitimately differs -- composition is
  the variable under test) plus `CampaignLaneResult.effective_build_id`
  equality (the actual resolved configure/toolchain identity, not just the
  requested plan). `source_slice_id`/`build_plan_id` themselves are
  deliberately NOT required to match.
- Diagnostic-observability fix (GPT finding, blocking): the PRODUCTION pair
  (exact control vs exact serving-core candidate) is intentionally
  diagnostics-free on both arms and cannot emit a per-dispatch hit log. Added
  a separate DIAGNOSTIC companion pair -- candidate = serving-core +
  0810_replay_hit_diagnostics re-added as an ephemeral PA26-only probe
  (`replay_equivalence.build_serving_core_diagnostic_config`), explicitly
  bound back to the production candidate's composition via
  `require_diagnostic_matches_production` (diagnostic == production + 0810,
  nothing else) before any diagnostic result is trusted.
- Runtime comparison (GPT finding, blocking -- coverage gap): arm-level
  fields (process success, clean shutdown, output digest, correctness,
  model/GPU/runtime-args identity) separated from per-dispatch fields
  (dispatch, signature, winner, config_binding digest, transform_id,
  match_kind, call_count, outcome). Compares BOTH exactly, AND requires each
  arm's observed dispatch set exactly equal the frozen corpus's expected set
  -- closes the gap where two EMPTY per-dispatch result sets could otherwise
  read as spuriously equivalent.
- Failure-handling boundary (GPT finding, blocking): introduced
  `RuntimeNotEvaluated`, a specific exception distinct from
  `NotImplementedError`. The default `real_hardware_runtime_runner` stub
  raises it; `build_hardware_receipt` catches ONLY this exception to emit a
  `NOT_EVALUATED` receipt. Any other exception (a parser bug, an SSH/process
  error after execution starts, a malformed log, a real runner defect)
  propagates as a genuine failure -- never silently downgraded to
  "not evaluated".
- 47 tests in `tools/tests/campaign/test_replay_equivalence_hardware.py`
  (plus the unchanged 14 offline tests), all offline; full
  `tools/tests/campaign` suite: 730 tests, same 2 pre-existing unrelated
  failures as every prior PA26 commit (contract-registry count, migration
  lint) -- confirmed unrelated via `git status`.

**Explicitly NOT done** (GPT's own "would additionally implement offline
now" wishlist -- non-blocking per GPT, but real remaining scope): a strict
hit-log JSONL parser, a runtime-binding dataclass carrying model SHA256 +
ordered GPU identity + args/environment as a single canonical unit, and (the
actual hardware gap) real Brutus execution itself -- launching both built
arms, running the fixed corpus through them, and parsing real output into
`ArmRuntimeResult`/`RuntimeEntryResult`. Per GPT: "the production lane API
already gives you the correct materialize->generate->build path and
content-addressed identities, so no further build infrastructure is
needed" -- real Brutus time is now genuinely the only remaining gap. PA26
stays open until that lands.

## 2026-09-14: offline oracle implemented (design reviewed by GPT)

Brutus was busy serving production when this work started, so the offline
(no-GPU) half of PA26 was implemented first, per GPT design guidance
(dev-gpt-agent session ses_c2892cdae7f14feb, req_accf444d1eaa4537):

- `tools/bigcherry/campaign/replay_equivalence.py`: resolves control
  (`bigcherry-native` = framework + upstream-fixes, via the normal
  `campaign.resolution` path) and an EPHEMERAL PA26 serving-core candidate
  (framework minus the 8-module PA20 serving boundary: 0110, 0700, 0800,
  0810, 0820, 0830, 0900, 1100) -- constructed in-memory only, never written
  to `config/recipes.toml` (PA28 owns any real public serving-core
  taxonomy). Fails closed if `config/recipes.toml`'s `framework` set drifts
  from the hardcoded expected-removed list.
- Asserts the exact ordered composition delta AND shared-module content-hash
  equality between arms (catches a same-ID-different-hash divergence, not
  just membership).
- Binds a fixed v5 winners corpus via `tuning.replay.read_cache(...,
  enforce_schema=True)` only -- GPT flagged that the historical
  `dispatch-27b-v5.cache` reference artifact is schema-1 and must be
  re-exported under current gates before use here, never read via the
  `enforce_schema=False` historical-inspection escape hatch.
- Emits a receipt (`build_offline_receipt`) with `runtime.status =
  "NOT_EVALUATED"` -- this proves composition + corpus identity only, NOT
  behavioral replay equivalence.
- 12 tests in `tools/tests/campaign/test_replay_equivalence.py`, including
  the negative fixtures GPT called out as most important: dropping a real
  serving module (0650) from the candidate must fail the oracle; an
  unexpected extra module must fail; an empty/wrong-version cache must fail.
  All pass; ran full `tools/tests/campaign` suite (695 tests) -- only 2
  pre-existing unrelated failures (contract-registry count, migration lint),
  confirmed unrelated via `git status` (only the 2 new PA26 files untracked).

GPT's scope correction (important): PA26 is NOT "does 0100 vs 0100+0110
change 0300-0650's presence" (they're identical in both arms by
construction) -- it is "is removing 0110 + the other 7 campaign/qualification
modules behaviorally inert for production replay." Any hardware divergence
found later reclassifies that module into serving-core before PA28, per the
plan item's own acceptance criteria.

**Still required to close PA26** (explicitly NOT done): the real two-arm
HIP hardware run on Brutus -- build control and candidate from the same
pinned source/generated inputs, run the fixed workload through both real
replay binaries, compare resolved signature/winner/config, process success,
and output/correctness artifact. No throughput-equivalence criterion; any
behavioral difference is FAIL + reclassification, never tolerance. Brutus
was actively serving production (`llama-server` on both GPUs) when this
session checked, so this step is queued for when hardware is free.

## Change Log

- 2026-09-11: created.
- 2026-09-14: rebased to current resolver/build identity and clarified the evidence oracle and ordering.

## Ledger-events

- chg_20260914_003255_fixed-the-patch-system-plans_2121
- 2026-09-14T00:33:04.060359+00:00 (updated-by): Updated: section:ledger-events
- chg_20260914_125856_added-the-offline-half-of-pa26_3094
- 2026-09-14T12:58:59.696501+00:00 (updated-by): Updated: section:ledger-events
- 2026-09-14T12:59:26.691028+00:00 (updated-by): Updated: section:notes
- chg_20260914_131055_fixed-a-real-ordering-gap-and_7458
- 2026-09-14T13:10:58.011961+00:00 (updated-by): Updated: section:ledger-events
- chg_20260914_133056_added-the-two-arm-hip-buildru_8858
- 2026-09-14T13:30:59.234189+00:00 (updated-by): Updated: section:ledger-events
- 2026-09-14T13:31:18.163812+00:00 (updated-by): Updated: section:notes
- 2026-09-14T13:38:10.375603+00:00 (updated-by): Updated: section:notes
- 2026-09-14T13:46:03.734424+00:00 (updated-by): Updated: section:notes
- chg_20260914_234824_generated-real-hardware-valida_3779
- 2026-09-14T23:48:33.300008+00:00 (updated-by): Updated: section:ledger-events
- 2026-09-14T23:48:52.250719+00:00 (updated-by): Updated: section:notes
- chg_20260915_000937_authored-real-documentation-an_7798
- 2026-09-15T00:09:40.027754+00:00 (updated-by): Updated: section:ledger-events
- 2026-09-15T00:09:56.332531+00:00 (updated-by): Updated: section:notes
- chg_20260915_004802_fixed-a-validator-bug-and-gene_3765
- 2026-09-15T00:48:05.626553+00:00 (updated-by): Updated: section:ledger-events
- 2026-09-15T00:48:22.366831+00:00 (updated-by): Updated: section:notes
- 2026-09-15T01:14:20.402727+00:00 (updated-by): Updated: section:notes
- chg_20260915_011426_pa26s-real-corpus-generation_3414
- 2026-09-15T01:14:29.655509+00:00 (updated-by): Updated: section:ledger-events
- 2026-09-15T01:17:19.331966+00:00 (updated-by): Updated: section:notes
- chg_20260915_011724_confirmed-a-real-deterministi_9919
- 2026-09-15T01:17:27.068163+00:00 (updated-by): Updated: section:ledger-events
- chg_20260915_014358_hardened-bigcherrys-correctne_7582
- 2026-09-15T01:44:00.958144+00:00 (updated-by): Updated: section:ledger-events
- 2026-09-15T01:44:25.020618+00:00 (updated-by): Updated: section:notes
- 2026-09-15T02:17:52.759033+00:00 (updated-by): Updated: section:notes
- chg_20260915_021758_ruled-out-one-of-two-remaining_2987
- 2026-09-15T02:18:01.566020+00:00 (updated-by): Updated: section:ledger-events
- 2026-09-15T02:19:22.713981+00:00 (updated-by): Updated: section:notes
- chg_20260915_023104_fixed-a-real-bug-where-a-singl_7530
- 2026-09-15T02:31:10.480817+00:00 (updated-by): Updated: section:ledger-events
- chg_20260915_034425_found-and-verified-the-real-ca_3031
- 2026-09-15T03:44:31.567924+00:00 (updated-by): Updated: section:ledger-events
- chg_20260915_040708_fixed-the-real-tune-campaign-c_8272
- 2026-09-15T04:07:13.993241+00:00 (updated-by): Updated: section:ledger-events
- 2026-09-15T04:08:32.444747+00:00 (updated-by): Updated: section:title, work=None, skill=None, priority=None, section:description, section:steps, section:detailed_solution, section:code_samples, section:files, section:validation, section:effort_risk, section:standards, section:acceptance_criteria, section:notes
- chg_20260915_042417_pa26s-real-two-arm-hardware-p_4079
- 2026-09-15T04:24:19.804720+00:00 (updated-by): Updated: section:ledger-events
- 2026-09-15T04:24:41.497509+00:00 (updated-by): Updated: priority='P1', section:notes
- chg_20260915_052732_pa26s-real-two-arm-hip-hardwa_1564
- 2026-09-15T05:27:35.132817+00:00 (updated-by): Updated: section:ledger-events
- 2026-09-15T05:28:16.729413+00:00 (updated-by): Updated: section:notes
