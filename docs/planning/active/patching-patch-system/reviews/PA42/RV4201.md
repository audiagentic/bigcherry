---
id: RV4201
review-of: PA42
plan: patching-patch-system
state: created
reviewed-by: agent
reviewed-at: ''
---

# RD13 performance and controls lane design approval

## Notes

Independent clean-worktree implementation review completed at pushed revision 860cf636. No source edits were made. The reviewer confirmed the contract requires tierA-qwen4b-q6k positive and tierM-gptoss20b-q6k controls, decode workload, 10 paired rounds, CI95 threshold policy, and separate performance/controls evidence. Current producer truthfully remains BLOCKED because it builds a server pair for correctness, while the generic paired benchmark seam consumes llama-bench binaries and the producer has no declared control_model input. Focused tests passed 24 with 1 deselected and 2 subtests; the undeselected run had one isolated-worktree failure because vendor/llama.cpp/ggml/src/ggml-cuda/ggml-cuda.cu was absent.

## Findings

- Approve the scaffold llama-bench route or explicitly authorize a new benchmark executor; never pass llama-server paths to run_paired_llama_benchmark().
- Add a required control_model producer input only after approval, validate it against the fixed tierM registry identity, and bind its identity/provenance into campaign evidence.
- Explicitly map positive and controls lanes, use exactly 10 paired rounds, return typed LaneEffect data, and leave threshold aggregation to the dispatcher.
- Keep the server pair for backend-reference correctness and decide how scaffold benchmark identities bind alongside the canonical validation identity.
- Add hardware-free tests for missing/wrong control input, exact benchmark calls, identity binding, and promotion-lane invocation without threshold changes.

## Conclusion

Design approval is still required before implementation. PA42 remains pending and PA40 remains blocked; no performance or controls PASS is claimed.
