---
id: RV4201
review-of: PA42
plan: patching-patch-system
state: considered
reviewed-by: agent + isolated worker
reviewed-at: ''
---

# RD13 performance and controls lane design approval

## Notes

The independent worker review recommends the standard scaffold llama-bench route. This is accepted as the working architecture: retain RD13's server pair for backend-reference correctness; use the scaffold llama-bench control/subject pair for performance and controls; add a required control_model producer input with fixed tierM registry/content provenance; bind both benchmark identities into evidence separately from the canonical correctness identity. A new runtime seam is not authorized unless implementation proves the scaffold route cannot satisfy the contract.

## Findings

- Approve the scaffold llama-bench route or explicitly authorize a new benchmark executor; never pass llama-server paths to run_paired_llama_benchmark().
- Add a required control_model producer input only after approval, validate it against the fixed tierM registry identity, and bind its identity/provenance into campaign evidence.
- Explicitly map positive and controls lanes, use exactly 10 paired rounds, return typed LaneEffect data, and leave threshold aggregation to the dispatcher.
- Keep the server pair for backend-reference correctness and decide how scaffold benchmark identities bind alongside the canonical validation identity.
- Add hardware-free tests for missing/wrong control input, exact benchmark calls, identity binding, and promotion-lane invocation without threshold changes.

## Conclusion

Architecture decision: APPROVED for a bounded patch-local implementation using the scaffold llama-bench route, with fail-closed control-model identity/provenance and explicit separate identity binding. Implementation may begin; thresholds and shared dispatcher semantics must remain unchanged.
