---
id: RV4201
review-of: PA42
plan: patching-patch-system
state: considered
reviewed-by: agent + isolated worker
reviewed-at: ''
---

# RD13 performance and controls lane design approval

## Notes

Direct GPT decision response req_ce0a3abf439f4e75 at pushed revision 77cf625f independently confirms Route A: use only ctx.validation_binaries control/subject llama-bench for promotion-critical performance/controls; retain llama-server pair solely for backend-reference; canonical ProducerResult.validation_build_identities must be ctx.validation_build_identities; require control_model input validated against models.toml tierM identity with digest/provenance; run positive and control decode lanes with 10 pairs each; return LaneEffect data and tg128 target; create real run_trace_probe trigger evidence; emit rd13-performance.json and keep thresholds dispatcher-owned. Missing binaries, wrong identity, partial lanes, missing trigger, or server-pair canonical identity must fail closed.

## Findings

- Approve the scaffold llama-bench route or explicitly authorize a new benchmark executor; never pass llama-server paths to run_paired_llama_benchmark().
- Add a required control_model producer input only after approval, validate it against the fixed tierM registry identity, and bind its identity/provenance into campaign evidence.
- Explicitly map positive and controls lanes, use exactly 10 paired rounds, return typed LaneEffect data, and leave threshold aggregation to the dispatcher.
- Keep the server pair for backend-reference correctness and decide how scaffold benchmark identities bind alongside the canonical validation identity.
- Add hardware-free tests for missing/wrong control input, exact benchmark calls, identity binding, and promotion-lane invocation without threshold changes.

## Conclusion

Architecture decision: APPROVED for a bounded patch-local implementation using the scaffold llama-bench route, with fail-closed control-model identity/provenance and explicit separate identity binding. This conclusion is independently supported by the direct GPT decision response req_ce0a3abf439f4e75 and isolated worker review. Implementation is in progress; thresholds and shared dispatcher semantics remain unchanged.
