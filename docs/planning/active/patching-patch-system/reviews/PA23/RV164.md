---
id: RV164
review-of: PA23
plan: patching-patch-system
state: created
reviewed-by: dev-gpt-agent via agents gateway
reviewed-at: '2026-09-12T15:03:18.144628Z'
---

# GPT validation: bind lint adapter to LINT matrix and add real parity coverage

## Notes

Static review only; GPT could not execute repository Python tests because the host Python launcher was unavailable.

## Findings

GPT validation request req_db9268ab22e9441f reviewed main@e03225df. Production wiring otherwise preserved cross_check summary suppression, CLI shape/order/stderr/0-1 behavior, PA22 parser choices, operational PA21 intents, and no evidence/upstream/hardware/mutation/BRC01 path.

Required corrections:
1. evaluate_repository_lint_gates() hardcodes G1/G3 instead of consulting gate_applies(GateId, GateIntent.LINT), so the shared applicability matrix can drift without changing lint behavior. Dispatch repository-specific G1/G3 evaluators through the shared matrix.
2. _evaluate_lint_package() can return BLOCKED for an unknown package status while LintGateReport.problems omits that detail, allowing cmd_patch_lint to exit 0. Project every applicable BLOCKED result into problems and keep existing recognized-status output unchanged.
3. Add deterministic temporary-tree integration/parity coverage using real static authorities for current, invalid, not-required, grandfathered, legacy-flat, and performance-marker cases, plus a test-only legacy projection proving exact problems/grandfathered ordering.
4. Correct PA23 wording that the canonical registry is loaded only once end-to-end: check_validation_packages() reloads its own registry. Do not widen scope with a validation-policy snapshot API unless required.

## Conclusion

CHANGES REQUIRED: fix matrix dispatch and fail-closed projection, then add real authority/parity tests before revalidation.
