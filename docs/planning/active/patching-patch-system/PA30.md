---
id: PA30
order: 11
plan: patching-patch-system
state: pending
created-at: '2026-09-11T23:46:01.801384+00:00'
breadth: ''
skill: advanced
created-by: agent
work: M
priority: P2
---

# Run final semantic-equivalence gates before deleting old framework/native names

## Description

Verification gate after PA29 and before PA31. Compare old and migrated roles while both names still resolve. This item validates semantics, not merely that the new config parses.

## Steps



## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files

Verification artifacts/receipts only unless a discovered defect requires a separately reviewed fix.

## Validation

Use the same resolved llama.cpp commit, BigCherry revision, model/workload hashes, GPU/device mapping, build profile, generated registry, and winner database for each paired comparison. Balanced/rotated performance runs are required for any claimed parity/regression conclusion; a single llama-bench sample is only a smoke signal.

## Effort & Risk



## Standards



## Acceptance Criteria

All six gates pass or have an explicitly reviewed intentional difference with evidence. No BLOCKED/unknown selector/report is treated as PASS. The final receipt is sufficient for PA31 to delete old names without relying on compatibility fallbacks.

## Notes


Ledger event: chg_20260915_103549_pa30-gates-4-and-5-tuning-and_4428


## 2026-09-15 (continued): gates 4/5 attempt -- blocked before hardware by GPT consultation gateway stall (real infra failure, not skipped by choice)

Started this fork's gate 4 (tuning smoke through bigcherry-tuning) and gate 5 (qualification smoke through bigcherry-qualification, 0830+1100) per PA30.md's gate text. Read PA26.md/RHA15.md for the real precedent invocation shape (`bigcherry tune-campaign --source ... --platform linux-multi --model <tierA-qwen4b-q6k> --devices 0 --runtime-profile production-safe-single --run-id ... --json`, run from `/home/audumla/bc-pa-work` via the pinned venv) and PROFILING.md for the real HI134/0830 telemetry example (`profile-campaign --source bigcherry-qualification --experiment hi134-meta-stage-trace` with `GGML_HIP_REDUCE_TELEMETRY=<path>`). Confirmed via code: `tune-campaign`'s own help text is literally "run the full record->tune->correctness->promote->replay pipeline as one command" (matches gate 4's required pipeline exactly), with real bounding flags (`--tune-screen-samples`, `--tune-final-samples`, `--correctness-seeds`) available for a lighter smoke pass; `profile-campaign` has `--control-reps`/`--profile-passes` for the same purpose. Read patches 0830/1100's README/validation.toml: both have only generic apply/build checks (no patch-specific custom check); 1100's own README states its deterministic test_mul_mat corpus only produces real candidate/correctness evidence when run in tune mode.

Verified Brutus idle (0% GPU x4, no live processes) before touching anything -- confirmed again at the end, still idle; no hardware time was spent in this pass.

Per this project's established PA26-PA30 pattern, submitted the gate 4/5 execution plan to dev-gpt-agent (session ses_c2892cdae7f14feb) for design review before touching real hardware, same as every prior pass. **The GPT consultation channel itself failed, confirmed via agent_task_diagnostics/agent_task_gateway_overview, not merely slow**: first request (req_8890d1cbc5ae4a6e) sat at unchanged activity for ~6 minutes then diagnostics reported `classification: stale-progress`, `reason-code: activity-lease-expired`, requiring recovery action -- ran `agent_task_recover(reconcile)` (accepted, no change), then `agent_task_recover(abandon)` (accepted) per the allowed recovery actions. Resubmitted the identical design question fresh in the same session as req_91eca96d67c34105, noting the prior stall. That resubmission then sat `state: queued`, `watcher-state: not-started` for 25+ minutes without ever being picked up. `agent_task_gateway_overview` independently confirmed the `gpt-auto` provider was actively failing other concurrent requests during this window with real errors: `EXT-GPTAUTO-003 composer-operation-timeout`, `EXT-GPTAUTO-004 frozen-response-correlation-conflict` / `unresolved-turn-not-reconciled`, and `INT-AGW-098 illegal-transition` -- i.e. a genuine, independently-evidenced provider/gateway outage affecting this session, not an artifact of this session's own requests.

**Honest status**: gates 4 and 5 are NOT attempted on real hardware in this pass -- correctly blocked before any build/GPU time was spent, per this project's own established discipline (every prior PA26-PA30 pass gets GPT design review before real hardware, and skipping it here would contradict that pattern and this task's explicit instruction not to force or fabricate). This is a real, external, evidenced infrastructure blocker (dev-gpt-agent/gpt-auto gateway), not a decision to skip the step, and not a claimed pass. Brutus untouched and left idle. Next pass should retry the same GPT consultation once the gpt-auto provider queue/composer issue clears (or route through a different available session/provider if one becomes available), then proceed with the real gate 4/5 hardware runs using the invocation shapes already scoped above. Do not mark PA30 completed -- gates 4/5 (this note) and gate 1's performance sub-clause remain open.

## 2026-09-15 (final for this fork): gates 1/2/3 real hardware receipt -- EVALUATED, equivalent=true

Real two-arm HIP hardware run completed on Brutus (bc-pa-work @ 4c70b737, pin 28ff0958291ce3465fabd7bd679d4b0edd742bd9/b10901) via `tools/lab/pa30/pa30_hw_run.py`, against the real RHA15-verify2 winners corpus (same corpus/model/arch/devices/runtime-profile PA26 used). Production pair (bigcherry-native vs bigcherry-serving-base) and diagnostic companion pair (+0810) both built through the real production lane pipeline; `require_shared_build_inputs` passed with PA30's real 7-module (production) / 6-module (diagnostic) `allowed_removed_modules` override; `require_corpus_candidate_semantics` passed for both diagnostic arms (cross-build winner-candidate semantics verified against the corpus producer manifest before relying on `GGML_HIP_DISPATCH_REPLAY_REVISION_MATCH=0`).

**Real result: `decision_equivalence` = EVALUATED, 0 differences. `runtime` = EVALUATED, equivalent=true.** Composition delta matched the expected real 7-module set exactly. Full receipt saved at `tools/lab/pa30/pa30-hardware-receipt.json` (committed).

Empirically confirmed (not assumed): `bigcherry` == `bigcherry-serving-base` and the explicit pre-cutover reconstruction (framework+upstream-fixes+validated-enhancements) == `bigcherry-native`, byte-identical patch_ids/module_hashes both ways -- so this ONE hardware execution's receipt legitimately closes gates 1, 2, and 3 together (G1 control identity == G2's reconstructed-old-release identity; G1 candidate identity == current migrated `bigcherry` identity), per GPT design review (req_92fb5a4fe596449e).

**Gates 2 and 3: PASS, real evidence.** Release-equivalence (gate 2) and replay-winner-equivalence (gate 3) are satisfied by this receipt at the same evidentiary bar PA26 itself used to close (docs/planning/completed/patching-patch-system/PA26.md explicitly states 'No throughput-equivalence criterion' for this class of comparison) -- process success, clean shutdown, output digest, correctness status, and exact per-dispatch signature/winner/config match, all equal across arms.

**Gate 1: PARTIAL.** The correctness/replay-decision-equivalence claim is real and PASS (same receipt as above). The gate's explicit 'representative performance' sub-clause is NOT yet satisfied -- no dedicated balanced/rotated llama-bench throughput comparison was run in this pass (out of scope for the time spent on the harness adaptation + GPT review + the hardware run itself). This is a real, named open item, not fabricated or silently dropped.

**Honest status for this fork**: gate 6 = PASS (prior note). Gates 2/3 = PASS (this note, real hardware evidence). Gate 1 = PARTIAL (correctness proven; performance sub-clause open). Do not mark PA30 completed -- gates 4/5 (separate pass) and gate 1's performance evidence remain open. Brutus returned to idle after the run.

Commits: 2007d3ec (harness), 40f9a689 (driver script), 4c70b737 (notes), and the receipt commit. Pushed to origin and brutus.

## 2026-09-15 (continued): gates 1-3 real hardware harness built + GPT-reviewed; real run launched

Built `tools/bigcherry/campaign/replay_equivalence_pa30.py::build_real_hardware_receipt` (+ `build_real_hardware_specs`/`build_real_diagnostic_hardware_specs`/`require_pre_cutover_release_identity`), the harness adaptation the prior pass identified as missing. Design reviewed by dev-gpt-agent (session ses_c2892cdae7f14feb, req_92fb5a4fe596449e) before implementing: reuses `replay_equivalence_hardware.py`'s `execute_two_arm_build`/`require_corpus_candidate_semantics`/`compare_runtime_results`/`make_real_hardware_runtime_runner` UNCHANGED, wired against the real named sources with `allowed_removed_modules` overridden to PA30's real 7-module (production) / 6-module (diagnostic) delta instead of PA26's own 8-module set (which still includes 0700). `require_pre_cutover_release_identity` proves the explicit pre-cutover reconstruction (framework+upstream-fixes+validated-enhancements) equals `bigcherry-native`'s own resolution rather than assuming it from validated-enhancements currently being empty -- confirmed true (16/16 patch_ids and module_hashes match). Also empirically confirmed `bigcherry` == `bigcherry-serving-base` (9/9 patch_ids and module_hashes byte-identical) -- per GPT's guidance this licenses ONE hardware execution to satisfy gates 1/2/3 together (G1 control==G2 reconstructed-old-release, G1 candidate==current migrated bigcherry), with separate G1/G2/G3 receipts referencing the same hardware-run ID rather than independent duplicate runs.

Offline: full campaign test suite run (761 passed, 2 pre-existing/unrelated failures from a concurrent pass's 20th experiment-contract -- confirmed via clean `git status` that only replay_equivalence_pa30.py was touched by this change). New offline preflight smoke (composition delta, pre-cutover identity proof, diagnostic overlay match) all pass.

Brutus verified idle (0% GPU x4, no live processes) before starting. Launched `tools/lab/pa30/pa30_hw_run.py` on Brutus (bc-pa-work fast-forwarded to 40f9a689) against the real RHA15-verify2 corpus -- real two-arm build (bigcherry-native vs bigcherry-serving-base) + diagnostic companion pair + real replay runtime comparison. Commits: 2007d3ec (harness), 40f9a689 (driver script), pushed to origin and brutus. Run in progress as of this note; real result (EVALUATED equivalent=true/false, or a real divergence) to be recorded in a follow-up note once the run completes -- not fabricated here.

## 2026-09-15: gates 1/2/3/6 pass (this fork covers ONLY gates 1/2/3/6; gates 4/5 are a separate concurrent pass -- do not mark PA30 completed from this note alone)

GPT design consult first (dev-gpt-agent session ses_c2892cdae7f14feb, req_c7cc2be3aca4415d), confirming: the real post-cutover comparison needs a distinct 7-module removed-module delta (0700 excluded, since PA28 kept 0700 in serving-core -- PA26's own historical EXPECTED_REMOVED_MODULES (8 modules, includes 0700) is correctly left untouched as the record of what PA26 actually proved, not mutated for PA30); gates 1/2/3 can share one hardware execution since source.bigcherry now composes identically to bigcherry-serving-base while validated-enhancements is empty, but need separate resolver/binding receipts; gate 6 plan (selector-matched G2 + G0/G4/G6/G7, patch-lint, --all coverage) confirmed correct as scoped.

**Gate 6: PASS, real evidence.** On Brutus (bc-pa-work, fast-forwarded to 082749c9/70938706), against a freshly cloned clean llama.cpp checkout at the exact current pin (28ff0958291ce3465fabd7bd679d4b0edd742bd9 / b10901, cloned from the local upstream mirror to avoid touching any dirty/live vendor/llama.cpp tree the concurrent gates-4/5 pass might be using): `patch-lint` shows only the 4 pre-existing unrelated RD-experimental problems (1200/1208/1217/1221, tracked throughout PA26/28/29); `patch-rebase-check --source bigcherry-native` and `--source bigcherry-serving-base` and `--source bigcherry` and `--source bigcherry-tuning` are each 100% CLEAN, with bigcherry-serving-base and bigcherry producing byte-identical CLEAN reports (9/9 patches each); `patch-rebase-check --all` is 61/61 CLEAN (G6 coverage report, correctly not substituted for exact G2); `patch-gates --intent build` for representative patches in both bigcherry-serving-base (0600_mmvq_geometry) and bigcherry-tuning (0110_campaign_tune_record_build), fed the real selector-matched rebase report plus the --all report, show selection_status=EVALUATED with G0/G1/G2/G4/G6/G7 all PASS.

**Gates 1/2/3: PARTIAL / blocked-with-reason -- real config-level composition proof done, real hardware receipt NOT produced.** Landed `tools/bigcherry/campaign/replay_equivalence_pa30.py` (commit 70938706, GPT-reviewed design): `resolve_real_composition_delta`/`require_real_expected_composition_delta` resolve the REAL `bigcherry-native`/`bigcherry-serving-base`/`bigcherry` sources (no ephemeral cfg injection) and fail closed on the exact real 7-module delta; `build_serving_base_diagnostic_config`/`require_real_diagnostic_matches_production` construct the 0810 diagnostic overlay directly over the real `bigcherry-serving-base` composition. 6 new offline tests pass; confirmed live that `bigcherry-native` vs `bigcherry-serving-base` and `bigcherry-native` vs `bigcherry` (post-PA29 migrated release) produce byte-identical candidate patch_ids/module_hashes -- i.e. gate 2's 'release equivalence' claim (migrated source.bigcherry == serving-core+upstream-fixes, matching the PA26-proven serving-core composition) is confirmed at the config/selector level. Full offline suite (tools/tests/campaign+patch+tuning): 3054 passed, only the same 4 pre-existing unrelated failures.

**What's still missing for gates 1/2/3 to reach PASS**: the actual two-arm real-hardware build+replay+balanced/rotated-performance receipt. `replay_equivalence_hardware.build_hardware_receipt()` cannot simply be re-invoked with different `CampaignLaneExecutionSpec.source_name` values -- it hardcodes PA26's ephemeral `offline.build_serving_core_config`/`offline.SERVING_CORE_DIAGNOSTIC_SOURCE_NAME` internally in its own `build_offline_receipt` call and in the diagnostic-composition-match check, so those need real adaptation (accept an injectable composition validator / real-source diagnostic identity) before a real hardware pass can run through it. This is genuine, scoped, additional work -- not attempted in this pass to avoid forcing a result or fabricating a receipt. Per PA30's own validation section, gate 1 also explicitly requires representative/balanced-rotated performance data, which a correctness-only replay receipt would not by itself satisfy.

**Honest status for this fork**: gate 6 = PASS (real evidence, all sub-checks green). Gates 1/2/3 = blocked-with-reason (real composition-level proof done and GPT-reviewed; real hardware receipt not yet produced -- harness adaptation identified as the concrete next step, comparable in scope to PA26's own multi-pass hardware history). Brutus left idle (verified 0% GPU x4, no live processes) throughout -- no build/GPU time was spent since the harness gap was found before any hardware was touched. Do not mark PA30 completed -- gates 4/5 (separate pass) and the gates-1/2/3 hardware receipt remain open.

## Gates

1. **Serving control equivalence:** old `bigcherry-native` vs new `bigcherry-serving-base` under the same serving/replay build profile and generated inputs. Compare correctness, replay decisions, and representative performance.
2. **Release equivalence:** pre-cutover `source.bigcherry` composition vs migrated `source.bigcherry` serving-core composition plus the same upstream-fixes/validated-enhancements. Differences caused by intentionally removing campaign/qualification patch surface must be enumerated and shown not to alter serving behavior.
3. **Replay-winner equivalence:** re-run PA26 against the final migrated serving source, not only the PA27 candidate.
4. **Tuning smoke:** one real record -> inventory/generate -> tune -> promote/replay smoke through `bigcherry-tuning`, with durable build/generated/winner identities.
5. **Qualification smoke:** exercise 0830 telemetry and 1100 direct-op evidence through `bigcherry-qualification`; prove required artifacts are still produced.
6. **Patch-system gates:** selector-matched G2 plus G0/G4/G6/G7 as applicable, patch-lint, and all-patches rebase/disposition coverage. Use PA34 selector identity; `--all` remains a G6 coverage report, not a substitute for exact G2.

## Change Log

- 2026-09-11: created.
- 2026-09-14: expanded from five informal checks to role-preserving control/release/tuning/qualification and selector-bound gate verification.

## Ledger-events

- chg_20260914_003255_fixed-the-patch-system-plans_2121
- 2026-09-14T00:33:15.384547+00:00 (updated-by): Updated: section:ledger-events
- chg_20260915_094907_pa30-gate-6-patch-system-sele_4498
- 2026-09-15T09:49:10.550904+00:00 (updated-by): Updated: section:ledger-events


chg_20260915_094907_pa30-gate-6-patch-system-sele_4498
- 2026-09-15T09:49:35.396172+00:00 (updated-by): Updated: section:notes, section:ledger-events
- chg_20260915_095904_pa30-gates-1-3-built-and-gpt_7823
- 2026-09-15T09:59:07.676204+00:00 (updated-by): Updated: section:ledger-events

- chg_20260915_100212_pa30-gates-123-real-two-arm_1692

- chg_20260915_103549_pa30-gates-4-and-5-tuning-and_4428

## Append

- notes
- 2026-09-15T10:02:29.895596+00:00 (updated-by): Updated: section:notes, section:append
- 2026-09-15T10:35:40.156397+00:00 (updated-by): Updated: section:notes
- 2026-09-15T10:35:52.431916+00:00 (updated-by): Updated: section:ledger-events
- 2026-09-15T10:35:57.551423+00:00 (updated-by): Updated: section:notes
