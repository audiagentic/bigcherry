---
id: PA37
order: 0
plan: patching-patch-system
state: pending
created-at: '2026-09-13T10:08:52.022888+00:00'
breadth: ''
skill: ''
created-by: agent
priority: P1
work: M
---

# RD1203 (RD05/RD06/RD07) package prerequisites: validation.toml + build-once fat control/subject pair

## Description

Part of the GPT design (req_ead5338ca09348c5, 2026-09-13) for wiring RD04/RD05/RD06/RD07/RD12 correctness producers into the tracked evidence-binding CLI flow (parent: [[PA35]]). This item covers the RD1203 (patches/1203_rd050607_rdna4_wmma_fa_q6k_mmq) prerequisite work that must land before --run-rd050607-contract can execute.

Current gaps (confirmed real, not yet fixed):
- 1203 has NO validation.toml. require_execution_package() will reject the package before any new CLI flag can run against it -- do not bypass this gate.
- RD05/RD06/RD07 share one atomic patch/evidence record (1203) -- they must NOT be treated as three independent build-producing functions. Build ONE fat control/subject pair (gfx1100;gfx1201;gfx1030) for the whole atomic patch, execute the real PPL comparison once per architecture, then route results: gfx1201 backend_reference satisfies RD05 and RD06 (their contract scope is gfx1201-only -- do not force gfx1100/gfx1030 coverage onto them just because extra hardware runs happened diagnostically); all three architectures' results aggregate for RD07.
- No confirmed production activation marker exists for 1203 in the inspected source -- do not fabricate a trace-marker adapter; that is a separate package-authoring gap, track it in [[PA39]] if pursued.

## Steps

1. Author patches/1203_rd050607_rdna4_wmma_fa_q6k_mmq/validation.toml with a correctness check using the new correctness-summary validator (landed in commit 41ff665a), scoped to all three contract IDs (RD05-WMMA-FA-CORRECTNESS-BARRIERS, RD06-RDNA4-WMMA-FA-CONFIG, RD07-Q6K-MMQ-PREFILL-FOLD).
2. Add the multi-contract aggregation helper (_aggregate_contract_correctness_results()) to validation_campaign.py per GPT's spec: takes a contract + per_arch_results dict, validates required architectures are present, aggregates named CorrectnessResults per required check.
3. Refactor the shared RD05/06/07 producer to build ONE fat control/subject pair (not per-architecture rebuilds relying on cache reuse alone -- PA35's stricter convention requires build_tree() calls genuinely outside the per-device loop, matching run_patch1000_verification()'s pattern), execute per-device, and return the {named_results_by_contract, per_arch_results, artifacts, gpu_architectures, validation_build_evidence} shape GPT specified.
4. Do not add an activation check to 1203's validation.toml unless a real marker is confirmed to exist -- leave it declared-but-blocked or omit it, whichever this project's existing convention (see RD04's validation.toml) uses for an honestly-missing producer.

## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files



## Validation



## Effort & Risk



## Standards



## Acceptance Criteria



## Notes

Depends on nothing external; can proceed independently of PA38 (RD12) and PA37 (CLI wiring itself), but PA37's --run-rd050607-contract flag cannot execute until this item's validation.toml lands.

BLOCKED on a deeper gap than originally scoped, discovered 2026-09-13: bind_contract() (tools/bigcherry/patch/validation.py:420) shows expected_effect="performance" (RD06 and RD07 both have this) ALSO requires the 'activation' capability, not just 'performance'. 1203's patches/1203_rd050607_rdna4_wmma_fa_q6k_mmq/patch.py has NO BIGCHERRY_PATCH_TRACE marker at all (confirmed via grep -- zero matches), so there is no real regex to declare an honest (even if currently-BLOCKED) trace-marker check against, unlike RD04's activation check which references a real, specific marker string format.

This means validation.toml cannot be authored to make 1203's plan build successfully (ConfigurationError: missing capability producer for 'activation') without FIRST adding a real activation marker to patch.py -- that is bigcherry-patch-author territory (adding a BIGCHERRY_PATCH_TRACE-gated marker to the actual source transform), not a validation-package-authoring task. Do not fabricate a marker regex that doesn't correspond to real instrumented code.

Confirmed correctness contract details for when this unblocks: all three contracts (RD05/RD06/RD07) require only correctness.backend_reference (never ppl_equality, unlike RD04) -- correctness-summary is still the right validator. RD05 scope=[gfx1201] only, RD06 scope=[gfx1201] only, RD07 scope=[gfx1100,gfx1201,gfx1030]. RD06 has a real, unusual acceptance policy already (effect_evidence_policy="ci95_threshold_bound_v1", min_paired_rounds=10) -- the performance/controls checks, once unblocked, must respect this, not just copy RD04's benchmark check verbatim.

Next step: either (a) add a real BIGCHERRY_PATCH_TRACE marker to patch.py as its own small patch-authoring task, then resume this item, or (b) confirm with the user whether RD06/RD07's performance claims should instead be demoted/scoped down to avoid requiring activation evidence that will never exist for this patch.

## Change Log

- 2026-09-13T10:08:52.022888+00:00 (created-by): Created by agent
- 2026-09-13T10:11:39.922548+00:00 (updated-by): Updated: section:notes
