---
id: PA42
order: 42
plan: patching-patch-system
state: pending
created-at: '2026-09-21T09:17:08.343085+00:00'
breadth: ''
skill: advanced
created-by: agent
priority: P1
work: L
---

# Authorize and implement RD13 performance and controls producer lanes

## Description

The RD13 contract is bound and requires performance and controls, but the current patch-local producer intentionally emits only backend-reference correctness while producer.toml forbids the generic performance-benchmark CLI. PA40 therefore records the full campaign as BLOCKED. This item owns the explicit scope decision and, if authorized, the bounded producer implementation; it must not silently promote correctness/activation-only evidence.

## Steps

- Obtain an independent design review of the exact producer/runtime seam, model bindings, workload, device, and artifact requirements before implementation.
- Choose and record one disposition: implement contract-faithful performance/controls lanes, or explicitly re-disposition the contract/package scope while retaining PA40 BLOCKED.
- If implementing, use the existing generic paired-benchmark runtime and standard scaffold binaries; preserve one control/subject build pair, exact tierA positive/tierM control model identities, decode workload, 10 paired rounds, CI95 threshold gate, control-regression gate, and promotion-lane evidence.
- Add patch-local artifacts and typed ProducerResult fields for lane effects, target metric, trigger evidence as applicable, and performance/controls check binding; do not add RD13 branches to validation_campaign.py.
- Add hardware-free tests covering missing inputs/evidence and gate invocation, then run the focused suite and full documented baseline comparison.
- Commit and push implementation before any GPT review or hardware campaign. Only then run an isolated current-pin multi-architecture campaign and update PA40 with complete receipt/hash evidence.

## Detailed Solution & Technical Design

The current RD13 backend-reference producer builds a llama-server pair, while the standard_campaign='run' scaffold provides validation binaries and build identities. A design must decide whether those scaffold llama-bench binaries are authoritative for paired performance or whether the producer must request an expanded build pair. The contract's positive model is tierA-qwen4b-q6k, control model tierM-gptoss20b-q6k, decode workload, target_kernel_gain_pct=0.5, max_control_regression_pct=1, effect_evidence_policy=ci95_threshold_bound_v1, and min_paired_rounds=10. Any implementation must preserve these identities and fail closed when either lane or the required current device evidence is absent.

## Code Samples & Guidance



## Files

- patches/1206_rd13_mul_mat_add_view_fusion/validation/producer.py
- patches/1206_rd13_mul_mat_add_view_fusion/validation/producer.toml
- patches/1206_rd13_mul_mat_add_view_fusion/validation.toml
- tools/tests/patch/
- docs/planning/active/patching-patch-system/PA40.md

## Validation

Focused producer tests pass; full patch/campaign/tuning suite has no new failures versus documented baseline; current-pin hardware campaign produces performance/controls artifacts and a contract verdict, with PASS/FAIL/blocked truthfully recorded. No PASS inferred from build or process exit alone.

## Effort & Risk

High: changes promotion semantics and requires real hardware. Incorrect model/build identity or lane aggregation could create a false promotion; require adversarial review and fresh evidence.

## Standards



## Acceptance Criteria

- Approved binary/runtime domain for RD13 performance lane; no llama-server path passed to the llama-bench executor.
- Required tierM control-model input with registry/content provenance bound into campaign evidence.
- Exact positive/control lane mapping with 10 paired rounds, typed lane effects, unchanged thresholds, and dispatcher gate invocation.
- Separate correctness-server and benchmark-scaffold identity binding documented and tested.
- Focused hardware-free tests and full regression comparison pass without new failures; hardware campaign receipt records performance and controls truthfully.

## Notes

2026-09-21 design-review status: focused dev-GPT request `req_e7a544b0a3a348ed` was cancelled after the gateway remained in thinking without a response; no GPT conclusion is inferred. Source inspection confirms the implementation seam is non-trivial: RD13 currently builds llama-server binaries for backend-reference, while the standard scaffold owns llama-bench validation binaries; the contract also requires a distinct `tierM-gptoss20b-q6k` control model that is not currently an input in `producer.toml`. Do not implement by guessing binary ownership, model inputs, or lane aggregation.

2026-09-21 design-review status: focused dev-GPT request req_e7a544b0a3a348ed was cancelled after the gateway remained in thinking without a response; no GPT conclusion is inferred. Source inspection confirms the implementation seam is non-trivial: RD13 currently builds llama-server binaries for backend-reference, while the standard scaffold owns llama-bench validation binaries; the contract also requires a distinct tierM-gptoss20b-q6k control model that is not currently an input in producer.toml. Do not implement by guessing binary ownership, model inputs, or lane aggregation.

Independent clean-worktree implementation review at pushed revision 860cf636 made no source edits and confirmed PA42 is not safely implementable without an explicit design decision. The recommended route is the scaffold llama-bench pair, with a required control_model input validated against the fixed tierM registry identity and bound into campaign evidence. The server pair must remain dedicated to backend-reference correctness; the relationship between canonical validation identity and scaffold benchmark identity must be explicitly approved. Focused RD13 tests passed 24 with 1 deselected and 2 subtests; the undeselected run had one isolated-worktree failure because vendor/llama.cpp/ggml/src/ggml-cuda/ggml-cuda.cu was absent. Review RV4201 records these findings; PA42 remains pending.

## Change Log

- 2026-09-21T09:17:08.343085+00:00 (created-by): Created by agent

## Ledger-events


- chg_20260921_091723_opened-an-explicit-owner-item_8460
- 2026-09-21T09:17:25.947695+00:00 (updated-by): Updated: section:ledger-events
- chg_20260921_091923_documented-why-rd13-performanc_3413
- 2026-09-21T09:19:26.816289+00:00 (updated-by): Updated: section:ledger-events

- chg_20260921_092644_documented-the-rd13-design-blo_3158
## Reviews

- RV4201
- 2026-09-21T09:26:36.283939+00:00 (updated-by): Updated: section:acceptance_criteria, section:notes
- 2026-09-21T09:26:47.409829+00:00 (updated-by): Updated: section:ledger-events
