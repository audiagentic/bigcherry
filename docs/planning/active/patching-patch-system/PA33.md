---
id: PA33
order: 0
plan: patching-patch-system
state: pending
created-at: '2026-09-12T08:52:41.944558+00:00'
breadth: ''
skill: advanced
created-by: agent
work: L
---

# Build bigcherry patch-validate orchestrator (GPT-designed, req_6cd6ba90c95f4a8e)

## Description

Provide a thin patch-validate execution/evidence facade over PA21 preflight and existing validation_campaign/PVPS02 primitives. It is not another gate evaluator, scheduler, lifecycle promotion engine or generic resume state machine.

## Steps

1. Resolve validation.toml/Experiment Contract, patch IDs and declared composition.
2. Run PA21 validate preflight and require_execution_package; fail fast on missing/stale/malformed authority data.
3. Resolve canonical platform/device/fixture identity from recipes.toml and runtime profiles.
4. Select a declared oracle adapter (backend_ops, ppl_equality, completion_diff, trace_marker or custom_contract).
5. Reuse validation_campaign materialize/build/correctness primitives and PVPS02 _run_performance_benchmark()/performance-matrix.json.
6. Bind artifacts into patches/<id>/evidence/validation.json and emit compact run-summary.json/md projections.
7. Persist atomic phase status materialize→control-build→subject-build→correctness→performance→evidence→complete with run.pid, run.log and status.json; liveness uses the recorded PID, never pgrep heuristics.

## Detailed Solution & Technical Design

Target CLI: bigcherry patch-validate --patch <id> [--patch <id> ...] --model-tier <tier> --platform <platform> --mode correctness|performance|both --validation-tier <tier>, with explicit --experiment composition override. Define ValidationRequest, resolved execution type, adapter protocol, schema/versioned terminal statuses, fixture hashes, lock/liveness contract and first 2-3 migrated patch validations. Use platform.linux-multi and recipes.toml rather than hand-entered AMDGPU_TARGETS. Run bigcherry generate when dispatch/autotune builds require generated registry before configure. Keep patch-verify-evidence as read-only verifier and disposition/promotion separate.

## Code Samples & Guidance



## Files

tools/bigcherry/patch-validate or cli modules; tools/bigcherry/patch/validation_campaign.py; tools/bigcherry/campaign/build.py; tools/bigcherry/tuning/server_runner.py; tools/bigcherry/campaign/run_advisories.py; validation.toml/Experiment Contracts; patches/<id>/evidence/validation.json; run-summary schema/tests.

## Validation

Hardware-free contract/adapter/phase tests; preflight fail-fast tests; fixture/hash/device/build identity tests; atomic status/liveness tests; migrate 2-3 real validations; verify reuse of PVPS02 performance path and patch-verify-evidence; no duplicate executor/report schema.

## Effort & Risk



## Standards

Canonical recipes/platforms/build identity; existing ServerRunner and validation_campaign; deterministic evidence; no pgrep; no second orchestrator; no lifecycle decisions in execution facade.

## Acceptance Criteria

One patch-validate facade reuses PA21 and existing campaign/evidence primitives, supports declared adapters and atomic receipts, produces durable validation evidence plus summary projections, and migrates 2-3 real validations without bespoke scripts.

## Notes

Filed 2026-09-12 after a session of repeated manual, ad-hoc per-patch validation (hand-written materialize/build scripts, hand-typed llama-bench/perplexity invocations, hand-computed paired-A/B statistics, freehand README writeups) surfaced the same mechanical steps re-derived from scratch for nearly every patch, plus recurring low-level mistakes (a pgrep-self-matching-via-ssh-command-line bug hit twice, re-deriving AMDGPU_TARGETS and the wikitext2 corpus path each time).

GPT's design (req_6cd6ba90c95f4a8e), concrete and prioritized:

Target CLI: `bigcherry patch-validate --patch <id> [--patch <id> ...] --model-tier <tier> --platform <platform> --mode correctness|performance|both --validation-tier <tier>`. --experiment <name> as an explicit composition override; patch ID normally auto-resolves its [experiment.*] recipes.toml entry. --mode is an execution filter only, not a new definition of correctness.

Do NOT encode patch-specific test logic in the orchestrator. Add a small oracle-adapter registry for the recurring real shapes: backend_ops, ppl_equality, completion_diff, trace_marker, custom_contract. validation.toml/the Experiment Contract selects the adapter + params; the orchestrator only executes it.

Stop hand-writing paired A/B loops -- PVPS02 already solved that (generic paired/rotated run_performance_benchmark, --run-performance-benchmark, performance-matrix.json). The missing piece is orchestration/adaptation, not another benchmark framework.

Stop hand-deriving build targets -- config/recipes.toml's [platform.linux-multi] already declares gfx1100/gfx1201/gfx1030/RCCL/tests/tools/server.

Flow: validation.toml -> adapter registry -> existing validation_campaign primitives (materialize/build, performance-matrix, correctness producers, activation) -> existing evidence/verdict machinery. Do NOT fold GPT solution-approval into this tool -- measurement terminates in deterministic evidence; disposition/review consumes that evidence separately, as a distinct step.

Do NOT invent a second full report schema -- patches/<id>/evidence/validation.json remains the durable record. Add only a compact generated run-summary.json projection (schema below) plus a mechanically-rendered run-summary.md (paste-ready README/plan evidence block; do not auto-edit README in v1).

Minimum run-summary.json projection: {schema, run_id, patches[], base_revision, subject_composition_digest, platform, gpu_architectures[], model:{tier,sha256}, workload:{kind,corpus_sha256}, checks:{build,activation,correctness,performance:{status,pairs,mean_delta_pct,stdev_delta_pct,artifact}}, verdict, blockers[], evidence:<path>}.

P0.5 (fold into the same change, these are the recurring operational mistakes to eliminate):
- Never use pgrep for remote-run liveness/ownership (self-matches its own ssh command line). Every run gets a directory, run.pid (the real $!), run.log, and an atomically-written status.json; test liveness via `kill -0 $(cat run.pid)`; completion means a terminal status.json, not a grep heuristic.
- Use platform.linux-multi (and sibling platform entries) instead of hand-entering AMDGPU_TARGETS; add a separate small runtime/device profile only for what recipes don't express (HIP_VISIBLE_DEVICES, canonical -sm tensor -ngl 99).
- A fixture registry/ensure-helper for corpora/models -- wikitext2 gets one symbolic ID with a cache path, source, and SHA256; patch-validate asks for "wikitext2", nobody re-derives the download path.
- Every run self-describing: save resolved commands, effective env, composition IDs, binary hashes, model/corpus hashes, device visibility, repetition count, and A/B ordering BEFORE execution.
- Preflight once, fail fast before a multi-hour run if model/corpus/binaries/device-count/required-architecture/composition are wrong.
- Atomic phase status: materialize -> control-build -> subject-build -> correctness -> performance -> evidence -> complete (enables safe polling and a future --resume without a complex resume state machine yet).

Build-next scope, explicitly bounded: one patch-validate command + oracle adapter registry + runtime/fixture resolution + summary renderer, reusing validation_campaign/PVPS02/recipes/platforms/validation.toml/Experiment Contracts/patch-verify-evidence. Then migrate the next 2-3 real patch validations onto it before generalizing further -- do not build ahead of that.

### 2026-09-12: real gap confirmed -- ad-hoc build_tree() calls miss required pipeline steps for dispatch-enabled builds

While investigating patch 1242 (HI134 META stage trace), found that a bare `build_tree(extra_cmake_args=[])` call never defines `GGML_HIP_DISPATCH` (gated behind `GGML_HIP_AUTOTUNE` or `GGML_HIP_DISPATCH_REPLAY` in `ggml/src/ggml-hip/CMakeLists.txt`), so dispatch/telemetry-dependent patches silently compile inert -- confirmed via `strings` on the resulting .so showing none of the expected symbols. Setting `-DGGML_HIP_DISPATCH_REPLAY=ON` then hits a real, documented prerequisite: `hip-autotune-registry.inc` must exist, generated by `python -m bigcherry generate` before configuring -- a real pipeline step no ad-hoc single-patch build script in this session's investigations ever ran.

Concrete addition to this item's scope: the patch-validate orchestrator must call the correct, full build pipeline (including `bigcherry generate` when the target build type needs dispatch/autotune compiled in), not a bare `build_tree()` with empty `extra_cmake_args` -- matching what `bigcherry profile-campaign` and the real `[build.*]` recipe types in `config/recipes.toml` already do correctly. This session's many ad-hoc scripts got away with `extra_cmake_args=[]` only because they targeted plain `llama-bench`/`llama-perplexity`/`test-backend-ops` correctness/performance paths that don't require the dispatch layer at all -- it only surfaced as a real gap for this one dispatch/telemetry-dependent diagnostic patch.

PA33 consumes PA21 preflight. It replaces the existing patch-validate alias semantics while retaining patch-verify-evidence as the read-only verifier. PA30 and RHA14 may use it for execution.

## Change Log

- 2026-09-12T08:52:41.944558+00:00 (created-by): Created by agent

## Ledger-events


- chg_20260912_091821_validated-a-gpu-concurrency-op_8669
- 2026-09-12T09:18:21.072851+00:00 (updated-by): Updated: section:ledger-events
- 2026-09-12T09:43:51.941345+00:00 (updated-by): Updated: section:notes
- chg_20260912_094443_documented-a-build-tooling-gap_1424
- 2026-09-12T09:44:43.675890+00:00 (updated-by): Updated: section:ledger-events
- 2026-09-12T10:30:56.216302+00:00 (updated-by): Updated: section:description, section:steps, section:detailed_solution, section:files, section:validation, section:standards, section:acceptance_criteria, section:notes
- chg_20260912_103200_updated-the-active-buildrunp_8222
- 2026-09-12T10:32:01.024618+00:00 (updated-by): Updated: section:ledger-events
