---
id: PA39
order: 6
plan: patching-patch-system
state: pending
created-at: '2026-09-13T10:09:29.880537+00:00'
breadth: ''
skill: advanced
created-by: agent
priority: P1
work: M
---

# Complete RD1203 evidence binding through the generic validation-producer path

## Description

Original PA39 planned three dedicated flags. Two are already implemented and hardware exercised: `--run-rd12-contract` landed in `4312a2d5` (with follow-up fixes/evidence) and `--run-rd04-contract` landed in `8a5641e9` (with corrected evidence binding and a fresh gfx1100 run in `abd47f2a`). Those are historical bridge paths now scheduled for removal by PA36.

The only unfinished functional scope is atomic patch 1203 (RD05/RD06/RD07). Do **not** add the previously planned `--run-rd050607-contract`; PA36 now provides the generic producer architecture and adding another dedicated branch would create debt that PA36 immediately removes.

## Dependencies

- PA37: 1203 must have a truthful executable multi-contract validation package, including activation resolution.
- PA36: generic producer dispatch/evidence wiring must be available. The 1203 migration itself can be the plural-contract proof case.

## Steps

1. Register/select 1203's patch-local producer through `validation/producer.toml` and the PA36 generic dispatcher.
2. Execute one fat control/subject pair and per-device runs; produce typed named correctness results keyed by contract/check plus actual executed architecture set and validation build identities.
3. Bind canonical `correctness.json` and declared raw artifacts through the producer artifact allowlist. Do not bind a raw producer JSON where a validator expects the canonical disposition schema.
4. Evaluate contract correctness for all three bound contracts without using `ValidationPlan.contract` singular compatibility access. Multi-contract code uses `plan.contracts` and explicit check scope.
5. Thread actual control/subject identities, activation, performance/controls results, contract gates, and selector/current-pin identity into the existing evidence record.
6. Never populate promotion eligibility from correctness alone. `eligible_for_validated_state` remains the consequence of all declared checks/contract policy.
7. Add hardware-free tests for architecture routing, one failed architecture, artifact binding, plural gate serialization, build identities, and no dedicated RD flag/branch.
8. Run real hardware through the generic path and compare the new evidence semantics to the already-known correctness behavior before closing.

## Files

1203 patch-local validation producer/package; PA36 generic dispatcher/evidence plumbing only where genuinely generic; focused tests. No new `--run-rd050607-contract` in `validation_campaign.py`.

## Validation

RD05 and RD06 consume gfx1201 only; RD07 consumes gfx1100/gfx1201/gfx1030. One missing/failed required architecture fails that contract. Artifact hashes, build identities, selector/current-pin identity, and plural contract gates are present and machine-readable.

## Acceptance Criteria

1203 executes end-to-end through the generic patch-local producer path, binds truthful multi-contract evidence at the current pin, and adds no dedicated CLI/monolith branch. RD04/RD12 completed bridge work is preserved as history and separately removed during their PA36 migrations.

## Change Log

- 2026-09-13: created; RD12 and RD04 dedicated bridge flags subsequently landed.
- 2026-09-14: narrowed to the only unfinished RD1203 integration and aligned it with PA36 instead of adding another legacy flag.
