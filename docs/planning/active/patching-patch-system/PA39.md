---
id: PA39
order: 10
plan: patching-patch-system
state: pending
created-at: '2026-09-13T10:09:29.880537+00:00'
breadth: ''
skill: advanced
created-by: agent
priority: P1
work: S
---

# Accept RD1203 generic multi-contract validation on real hardware

## Description

PA39 is now acceptance-only. Original PA39 planned three dedicated flags, but RD12/RD04 bridge flags already landed historically and are scheduled for removal by PA36. PA37 now owns the complete 1203 implementation: truthful package, patch-local producer, registration, typed plural-contract routing, and hardware-free end-to-end execution through PA36.

Do **not** add `--run-rd050607-contract`, new 1203-specific shared plumbing, or a second evidence-binding implementation here. This item runs the already-implemented PA37 path on real hardware, verifies that the generic architecture survives reality, and records the acceptance evidence.

## Steps

1. Resolve the intended canonical selector through PA34 identity and require current-pin G2/package prerequisites before spending hardware time.
2. Execute PA37's existing patch-local producer through PA36's generic dispatcher with one fat control/subject pair and explicit real-device mapping.
3. Confirm executed architecture routing: RD05/RD06 consume gfx1201; RD07 consumes gfx1100, gfx1201, and gfx1030. Missing/failed required architecture fails the affected contract.
4. Observe truthful activation on the intended production path and execute each contract's declared correctness/performance/controls obligations without changing policy in the acceptance item.
5. Verify canonical `correctness.json`/declared artifacts, actual control/subject build identities, device identity, selector/current-pin identity, typed check scope, contract gates, and promotion eligibility are bound into the existing evidence record.
6. Compare correctness behavior with already-known backend-reference outcomes where identities/claim scopes match. Reuse prior evidence only where the existing authority permits; do not infer current PASS from historical prose.
7. If the generic path exposes a defect, record FAIL/BLOCKED, fix the owner (PA36 generic runtime or PA37 1203 package/producer) in a separate implementation commit, then rerun this acceptance from a clean current revision.
8. Record one immutable acceptance receipt/evidence update and close PA39 when all declared 1203 obligations at this scope are PASS or explicitly BLOCKED by unavailable hardware/policy.

## Detailed Solution & Technical Design

## Code Samples & Guidance

## Files

Expected normal changes: 1203 evidence/acceptance receipts only. Shared implementation files or patch-local producer code are not planned outputs; changes there indicate a discovered defect and belong to the owning abstraction before acceptance is rerun.

## Validation

RD05 and RD06 consume gfx1201 only; RD07 consumes gfx1100/gfx1201/gfx1030. One missing/failed required architecture fails that contract. Artifact hashes, build identities, selector/current-pin identity, activation, performance/controls results, and plural contract/check gates are present and machine-readable. Confirm one control+subject build pair rather than per-architecture rebuilds.

## Effort & Risk

## Standards

## Acceptance Criteria

1203 passes its applicable real-hardware obligations end-to-end through the already-existing generic patch-local producer path; evidence is current-pin/selector/build/device/artifact bound; plural contract/check scope is preserved; no dedicated RD flag or shared 1203 branch was added; no implementation ownership remains in PA39; and any unavailable mandatory evidence is BLOCKED rather than inferred PASS.

## Notes

2026-09-16: Pre-run reconnaissance/acceptance-gate session. BLOCKED before real-hardware execution -- three real defects found (and GPT-confirmed, req_b5602ea525264089) in PA37's already-committed generic/patch-local path that PA37's hardware-free tests could not catch: (1) --validation-producer never wires a corpus (ProducerContext.corpus hardcoded None, no CLI flag) so RD05/RD07 backend_reference always fails closed; (2) RD06's ci95_threshold_bound_v1 performance bound is never evaluated (execution-only stub), despite the real evaluator (experiment/execution.py::lane_effect_from_run + experiment/contract.py::evaluate_promotion_gate) already existing unused; (3) RD06's declared control model (tierM-gptoss20b-q6k) is never benchmarked -- producer.py only takes one ctx.model. Fix #1 is small/PA36-scoped; #2+#3 are real PA37-scale implementation (multi-model orchestration + real gate wiring + new tests), beyond PA39's small-scoped-fix authorization. Recommendation: land fixes in PA36/PA37 as new steps, then rerun PA39 from clean. No hardware time spent; Brutus confirmed idle before and after. Full detail in PA39.md change log.

2026-09-16 (follow-up pass): defect #1 (corpus never wired for --validation-producer) is FIXED -- see PA36's notes and commit f75028f3 (--producer-corpus flag + two adjacent real CLI-dispatch bugs found/fixed while writing a genuine wiring test: the PATCH/PRODUCER_ID selector parser and the singular-vs-plural contract loader, both of which made --validation-producer unusable for any real invocation, including 1203's). Defects #2 (RD06 ci95_threshold_bound_v1 performance bound never evaluated) and #3 (RD06 declared control model tierM-gptoss20b-q6k never benchmarked) are PA37-scoped, real implementation work (multi-model orchestration + real gate-evaluation wiring), and were NOT attempted in this pass -- not fabricated, left honestly open. PA39 stays pending/BLOCKED on those two before a real-hardware acceptance rerun makes sense; do not attempt the real hardware rerun until PA37 records #2/#3 as fixed with hardware-free test evidence, per this item's own step 7 authorization (small fixes only; #2/#3 are not small).

2026-09-16 (defects #2/#3 fixed): PA37 fixed both remaining defects from the 2026-09-16 reconnaissance/follow-up passes above -- (2) RD06's ci95_threshold_bound_v1 performance bound is now actually evaluated (real `lane_effect_from_run`/`aggregate_contract_effects`/`evaluate_promotion_gate` machinery, not the old `bool(outcome.runs)` stub) and (3) RD06's declared control model `tierM-gptoss20b-q6k` is now genuinely benchmarked via a new required `--producer-input control_model=<path>`, distinct from the positive model. See PA37.md's part-5 notes for full detail. Hardware-free evidence: 14/14 tests in `test_1203_validation_producer.py` pass, including new tests that were concretely verified to FAIL when the gate-check line is reverted to the old stub (not just asserted -- actually reverted and reran). Full offline suite (`tools/tests/patch tools/tests/campaign tools/tests/tuning`): 9 pre-existing failures (documented baseline), zero new failures. Commits `2c1380f3`/`c95c988b` on `patch-refactor`, pushed to `origin`/`brutus`. Ledger event `chg_20260915_211419_patch-1203s-rd06-performance_8656`.

All three PA39 real-hardware-acceptance defects found in this item's own reconnaissance (#1 corpus wiring -- fixed earlier, commits `f75028f3`/`e2c0c1b4`; #2 RD06 gate; #3 RD06 control model -- both fixed this pass) are now closed with hardware-free test evidence. PA39 is BLOCKED-no-longer -- ready to retry the real-hardware acceptance run from this clean revision. The real hardware run itself was intentionally NOT attempted in this pass (out of scope -- a separate follow-up pass per the task that produced this fix); PA39 is left `pending`, not completed, pending that real run.

Operational note for the next real-hardware pass: the generic dispatcher now requires `--producer-input control_model=<resolved path to tierM-gptoss20b-q6k>` in addition to the already-fixed `--producer-corpus` for any `--validation-producer 1203_rd050607_rdna4_wmma_fa_q6k_mmq/rd050607` invocation -- both are required producer inputs, so omitting either fails closed before the producer runs at all (never a silent partial result).

2026-09-16 (two more real P0s found by GPT review of the just-landed RD06 fix, both fixed): a fresh GPT review (session `ses_092810a3fa7b4bbd`, request `req_e3d28b6b104a4a01`) of the RD06 gate-evaluation fix recorded just above found two further real, BLOCKING P0 defects, plus two P1s, all now addressed in `patches/1203_rd050607_rdna4_wmma_fa_q6k_mmq/validation/producer.py`. Full detail and code-level reasoning is in PA37.md's part-6 notes (PA37 is the implementation owner per this item's own step-7 rule: fix defects in PA36/PA37, then rerun PA39's acceptance from clean); summarized here for PA39's own record:

- **P0 #1 (wrong executable, FIXED)**: the producer's one `build_pair(primary_target="llama-perplexity")` call supplied `llama-perplexity` binaries to `run_paired_llama_benchmark()`, which constructs `llama-bench`-style argv -- a real binary/argv mismatch neither part 5's fakes nor any prior hardware-free test could expose (they used opaque `object()` runs, never a real binary-selection path). Fixed by calling `build_pair()` twice, once per binary set (`llama-perplexity` for backend_reference, `llama-bench` for every paired-benchmark call), matching `ProducerRuntime.build_pair()`'s own documented contract.
- **P0 #2 (incomplete positive contract coverage, FIXED)**: RD06's contract declares positive workloads `["decode", "prefill"]`; the producer only ever benchmarked decode. `aggregate_contract_effects()` is not contract-aware and would have silently computed a gain from decode alone -- a real false-positive-promotion risk. Fixed: both declared positive workloads (and both declared control workloads) are now benchmarked, and the producer explicitly fails closed (no gate call at all) if any declared workload's evidence is missing, rather than letting the gate aggregate a partial lane set.
- **P1 (control_model identity, addressed non-blockingly)**: `--producer-input control_model=<path>` had no tie to RD06's declared control model (`tierM-gptoss20b-q6k`). Added a best-effort basename+file-size check against `config/models.toml`, recorded in `rd06-performance.json` regardless of outcome (`control_model_identity_ok`/`_detail`) so a mismatch is visible/auditable on the real run; deliberately non-blocking (not gated into `passed`) since full content-hash provenance is out of scope and hardware-free tests use non-existent fake model paths.
- **P1/spec drift (gfx1100 coverage, documented not fixed)**: RD06's rationale text claims gfx1100 must not select/regress, but RD06's formal `[scope]` is gfx1201-only and the producer only ever exercises gfx1201. No gfx1100 negative check exists or is claimed anywhere in the producer/evidence -- documented honestly in the producer's module docstring and in PA37.md rather than silently overclaiming coverage on the real run. A future item should formalize a gfx1100 check if PA39 ultimately needs to claim that coverage; PA39's own acceptance run should not read RD06's PASS as gfx1100-inclusive.

New/changed hardware-free tests (`test_1203_validation_producer.py`, 17 total) concretely proven to fail against the pre-fix code (producer.py's changes stashed, tests kept, reran, then fix restored): `test_rd06_performance_uses_real_llama_bench_binary_not_perplexity`, `test_rd06_prefill_positive_lane_actually_benchmarked`, `test_rd06_missing_prefill_evidence_fails_closed`, and the updated `build_pair_calls == 2` assertion.

PA39 remains `pending`, not completed. This pass did NOT attempt the real-hardware retry -- that is still the next pass's job, now against a revision with all five defects (the original #1/#2/#3 plus this session's two further P0s) closed with hardware-free evidence. GPT's finding (req_e3d28b6b104a4a01) is summarized faithfully above and its raw request-id is preserved for audit; no fabricated test result, GPT response, or completion claim in this note.

2026-09-16 (real-hardware acceptance attempt #1, real result, BLOCKED, zero hardware time spent): User authorization confirmed ("start it - always start hardware test when needed"). Brutus verified idle before (0% GPU all 4 devices, no live processes) via `ssh brutus "ps aux | grep -E 'llama-server|ninja|cmake|tune-campaign'"` and `rocm-smi --showuse`. Repo on Brutus (`/home/audumla/bc-pa-work`) fast-forwarded from `fd24a495` to `f14fe6bf` (origin/patch-refactor HEAD) before running. Real assets confirmed present: model `/mnt/vault/llm-models/qwen3.5-4B/gguf/mtp/Qwen3.5-4B-UD-Q6_K_XL.gguf` (tierA-qwen4b-q6k), control model `/mnt/vault/llm-models/gpt-oss-20B/gguf/gpt-oss-20b-UD-Q6_K_XL.gguf` (tierM-gptoss20b-q6k), corpus `/home/audumla/perf-sweep-20260911/corpus/wikitext-2-raw/wiki.test.raw`, hip-path `/home/audumla/rocm-shim`. Device topology re-confirmed: 0/1=gfx1100 (dual 7900XTX), 2=gfx1201 (R9700), 3=gfx1030 (6900XT).

Invocation shape independently verified against `validation_campaign.py`'s real argparse/--help output, `parse_device_map()`'s real ARCH=ID[,ID...] shape, and `patches/1203_rd050607_rdna4_wmma_fa_q6k_mmq/validation/producer.toml`'s declared `control_model` required input, before running:

```
python -m bigcherry.patch.validation_campaign --patch 1203_rd050607_rdna4_wmma_fa_q6k_mmq --validation-producer 1203_rd050607_rdna4_wmma_fa_q6k_mmq/rd050607 --model <qwen4b path> --producer-corpus <wiki.test.raw> --producer-input control_model=<gptoss20b path> --hip-path /home/audumla/rocm-shim --amdgpu-targets 'gfx1100;gfx1201;gfx1030' --device-map gfx1100=0,1 --device-map gfx1201=2 --device-map gfx1030=3 --workdir .../artifacts/pa39-rd050607 --worktree-root .../bc-pa-work-worktrees --build-root .../bc-build-pa39
```

GPT consult attempted twice this pass (session `ses_092810a3fa7b4bbd` req `req_2343751f775f463b` asking for invocation confirmation before spending hardware time; a second fresh-session req `req_b8c5ca6bea514df6` asking for a read on the finding below) -- both stalled/did not return a usable answer within this pass's budget, consistent with this session's documented intermittent gpt-auto stalls. Proceeded on independently source-verified grounds rather than wait indefinitely; both request-ids are preserved for audit, no GPT response is fabricated or assumed.

**Real result: FAILED at plan resolution, before any build or GPU work (zero hardware time spent -- confirmed 0% GPU all 4 devices immediately after)**:

```
bigcherry.patch.validation.ConfigurationError: 1203_rd050607_rdna4_wmma_fa_q6k_mmq: required capabilities with no producer: apply, build, RD05-WMMA-FA-CORRECTNESS-BARRIERS:controls, RD06-RDNA4-WMMA-FA-CONFIG:activation, RD06-RDNA4-WMMA-FA-CONFIG:controls, RD07-Q6K-MMQ-PREFILL-FOLD:activation, RD07-Q6K-MMQ-PREFILL-FOLD:controls
```

**Root cause (real, read directly from source, not guessed)**: `patches/1203_rd050607_rdna4_wmma_fa_q6k_mmq/validation.toml` declares exactly 5 checks (`rd05-backend-reference`, `rd06-backend-reference`, `rd06-performance`, `rd07-backend-reference`, `rd07-performance` -- correctness+performance capability only, confirmed by `capability = "correctness"/"performance"` on each). `validation/producer.toml`'s `[producer.rd050607]` declares exactly those same 5 check ids as its `artifacts`. `validation/producer.py` (grepped for `ProducerCheckResult(`/`capability=`) emits exactly those 5 checks and nothing else. There is NO `apply` check, NO `build` check, NO `activation` check, and NO `controls` check anywhere in 1203's package -- despite PA37 (part 3, commit `d94a49d0`) already implementing real `BIGCHERRY_PATCH_TRACE`/`GGML_LOG_WARN` activation markers in `patch.py` for exactly this purpose. `tools/bigcherry/patch/validation.py::build_validation_plan()` (~line 704) requires every universal capability (`apply`, `build`) plus every capability `bind_contract()` declares per contract (RD05 needs `controls`; RD06/RD07 need `activation`+`controls`, in addition to the correctness/performance the producer does supply) to be covered by at least one `required=true` check -- 1203's plan has never satisfied this, and `require_execution_package()` fails closed (section 18: configuration error) before anything can execute.

**Why hardware-free tests never caught it**: grepped `tools/tests/patch/test_1203_validation_producer.py` for `require_execution_package`/`build_validation_plan`/`build_plan_for_patch` -- zero hits. Every existing test drives `execute_validation_producer()` directly against a fake runtime; none of them ever construct the real validation plan from the real `validation.toml` the way `_run_validation_producer()`'s real CLI path does. This is the same class of gap (a real CLI/config-construction path untested by fake-runtime unit tests) that produced PA39's five earlier-fixed defects.

**Comparison to a working precedent**: `patches/1204_rd08_q6k_mmvq_vdr2/validation.toml` shows the expected shape -- explicit `apply`/`build` checks (generic `validator="apply"/"build"`), a `validator="trace-marker"` activation check with a real `marker-regex`, and a separate `controls` check, alongside `correctness`/`performance`. 1203 has none of these four.

**Decision (per PA39's own step-7 authorization)**: this is NOT a small, clearly-scoped fix. It requires: (a) trivial boilerplate `apply`/`build` checks (low risk, matches every other patch); but also (b) real `activation` check wiring across RD06/RD07 using the already-implemented markers (mechanical but new code/config), and (c) real `controls` checks for RD05/RD06/RD07 whose data source is not yet clear (may already be implicit in the producer's existing control-model benchmarking for RD06/RD07, but RD05 -- correctness-only, no performance check at all -- has no existing control-lane evidence source of any kind, so a real `controls` producer path may need genuinely new implementation, not just a new check id). Given (b)+(c) require new producer.py logic/validators across three contracts, not just config, this is real PA37-scale implementation, matching the same size/authorization boundary as the earlier #2/#3 defects. **Not attempted inline here.**

**PA39 stays `pending`, BLOCKED on implementation (not on hardware availability or policy).** Zero hardware time spent; Brutus confirmed idle before and after. No fabricated GPT response, evidence record, or completion claim -- both GPT consult request-ids are real and preserved, and both genuinely did not return usable guidance within this pass. Recommendation: route this as a new PA37 step (author real `apply`/`build`/`activation`/`controls` checks + any new producer.py logic the `controls` capability needs for RD05/RD06/RD07, plus a hardware-free test that actually calls `require_execution_package()`/`build_validation_plan()` against the real `validation.toml` so this class of gap cannot recur silently), then rerun this real-hardware acceptance from clean. RD05/RD06/RD07 acceptance status this pass: **BLOCKED** (all three -- the run never reached architecture-specific execution, so no per-contract PASS/FAIL evidence exists yet).

2026-09-16 (GPT confirmation, req_b8c5ca6bea514df6, session ses_dc14a51f196d460c, real -- returned after the above note was written): the second GPT consult DID complete (earlier marked as not-yet-returned when this pass's main note was drafted). Verbatim substance: "BLOCKED-and-flag to PA37. Do not implement this inside PA39... apply/build additions are probably boilerplate; activation may also be straightforward using the existing trace markers. But the missing RD05/RD06/RD07 controls capabilities require establishing truthful validators/callables/artifacts and contract scoping. Collectively that is PA37 implementation work, not a small PA39 acceptance repair... Add a PA37 step covering apply, build, contract-scoped activation, and all three contracts' controls, plus a regression test that runs require_execution_package()/build_validation_plan() against the real 1203 validation.toml + producer manifest. Then rerun PA39 clean. Record PA39 as BLOCKED at plan resolution; zero hardware work executed. This is exactly the kind of defect PA39's acceptance gate was intended to expose." This confirms the decision already recorded above independently from source reading. The first consult (req_2343751f775f463b, session ses_092810a3fa7b4bbd) never returned during this pass and was abandoned; its answer, if any, is unknown and not used.

2026-09-16 (missing apply/build/activation/controls checks fixed -- real, see PA37.md part 7 for full detail): the real ConfigurationError this item's own "real-hardware acceptance attempt #1" note recorded above (`required capabilities with no producer: apply, build, RD05:controls, RD06:activation, RD06:controls, RD07:activation, RD07:controls`) is fixed. Per this item's own step-7 rule (fix defects in the owning PA36/PA37 abstraction, then rerun acceptance from clean), the fix landed in PA37 (part 7), not here. Summary: 7 new checks added to 1203's `validation.toml`/`producer.py` with real evidence (apply/build from the already-existing `build_pair()` calls succeeding; RD06/RD07 activation from a real `BIGCHERRY_PATCH_TRACE=1` subject-vs-unpatched-control probe; RD05 controls from a new positive/control paired benchmark gated through the real `evaluate_promotion_gate()`; RD06/RD07 controls reusing already-gathered control-lane evidence), plus a real GPT-caught fix to already-shipped RD07 code (it was using the wrong model -- `ctx.model` instead of RD07's own declared `control_model`) and a disposition fix so each contract's eligibility is the AND of ALL its required checks, not just one historical check. Design was GPT-reviewed before implementation (req_3c064c9ce51f4a45) and GPT found 4 real defects in the initial proposal, all corrected.

Critically, a new hardware-free test (`test_real_validation_plan_resolves_via_require_execution_package` in `test_1203_validation_producer.py`) now calls the REAL `require_execution_package()`/`build_validation_plan()` against 1203's real `validation.toml` -- the exact real-CLI path that failed on real hardware and that no prior test ever exercised (every prior test drove `execute_validation_producer()` directly against a hand-built plan). A companion test (`test_pre_fix_validation_toml_shape_fails_plan_resolution`) concretely reconstructs the pre-fix 5-check shape and proves it raises the exact same `ConfigurationError` this item's real run hit -- confirming this new test class actually catches the regression PA39's real-hardware acceptance gate exists to expose, closing the exact detection gap this item's own reconnaissance note identified ("Why hardware-free tests never caught it" above).

Full offline suite: 9 failed / 3011 passed / 7 skipped -- matches the documented pre-existing baseline exactly, zero new failures. Commit `9a392dc9` on `patch-refactor`, pushed to `origin`/`brutus`. Ledger event `chg_20260916_001500_fixed-a-gap-in-patch-1203s-r_2290`.

One known gap left honestly documented (not fixed, out of this pass's scope): RD07's activation/performance/controls checks still only exercise gfx1201, not all three of RD07's declared architectures (gfx1100/gfx1201/gfx1030) -- mirrors RD06's own already-documented gfx1100 gap. The next real-hardware pass should treat RD07's PASS as gfx1201-only evidence, not full three-architecture coverage, until that gap is formally closed.

**PA39 remains `pending`, NOT completed.** The real-hardware acceptance rerun itself was intentionally NOT attempted in this pass (this item's own step-7 rule: fix the defect in the owning abstraction, then rerun acceptance in a separate pass) -- PA39 is now ready for that rerun from this clean revision, with the plan-resolution ConfigurationError that blocked attempt #1 fixed and hardware-free-verified.

2026-09-16 (real-hardware acceptance attempt #2, real result, BLOCKED at build/apply, zero GPU time spent): Offline gate re-verified first per step 1 -- `test_real_validation_plan_resolves_via_require_execution_package` and `test_pre_fix_validation_toml_shape_fails_plan_resolution` both pass locally at `b2f7cc9a` (2 passed), confirming the apply/build/activation/controls ConfigurationError blocker from attempt #1 is genuinely fixed offline before spending hardware time.

Brutus verified idle before (0% GPU all 4 devices, no live processes). Repo on Brutus (`/home/audumla/bc-pa-work`) reset to `origin/patch-refactor` HEAD `b2f7cc9a`. Real assets reconfirmed present (model, control model, corpus). `--help` reconfirmed the real flag shapes (`--producer-corpus`, `--producer-input NAME=VALUE`, `--validation-producer PATCH/PRODUCER_ID`, `--device-map ARCH=ID[,ID...]`) before running -- unchanged from PA39's own prior note.

Real invocation launched via nohup on Brutus (PID 1584242): `python -m bigcherry.patch.validation_campaign --patch 1203_rd050607_rdna4_wmma_fa_q6k_mmq --validation-producer 1203_rd050607_rdna4_wmma_fa_q6k_mmq/rd050607 --model <qwen4b> --producer-corpus <wiki.test.raw> --producer-input control_model=<gptoss20b> --hip-path /home/audumla/rocm-shim --amdgpu-targets 'gfx1100;gfx1201;gfx1030' --device-map gfx1100=0,1 --device-map gfx1201=2 --device-map gfx1030=3 --workdir .../artifacts/pa39-rd050607 --worktree-root .../bc-pa-work-worktrees --build-root .../bc-build-pa39`.

**Real result: FAILED at source-isolation apply, before any compile or GPU work (zero hardware time spent -- confirmed 0% GPU all 4 devices immediately after; the venv used was `/home/audumla/bc-pytest-venv`, not a bare `python` on PATH)**:

```
bigcherry.patch.source.PatchSourceIsolationError: patch 1203_rd050607_rdna4_wmma_fa_q6k_mmq failed to apply to ggml/src/ggml-cuda/mmq-vec-dot.cuh in isolated worktree .../subject/9ab070...: ["anchor for edit 'rd07-sum-line' matched 0 time(s), expected 1. ..."] It attaches to: Q6_K mmq warp kernel: the j0 accumulation uses the pre-folded scale (fork logic; keeps the 1000 float cast, equivalent form)
```

**Root cause (read directly from the real isolated subject worktree source, not guessed)**: `patches/1203_rd050607_rdna4_wmma_fa_q6k_mmq/patch.py`'s `rd07-sum-line` edit anchors on `_SUM_OLD` = `... sum[(j0/tile_C::J + n)*tile_C::ne + l] += ((float) C.x[l]) * sc[k01/4] * x_df[i*sram_stride] * dB;`. The real current-pin source at `ggml/src/ggml-cuda/mmq-vec-dot.cuh:1058` (isolated subject worktree, verified by direct grep for the unique `x_sc + i*sram_stride`/`sc[k01/4]` fragment) reads `sum[(j0/tile_C::J + n)*tile_C::ne + l] += C.x[l] * sc[k01/4] * x_df[i*sram_stride] * dB;` -- upstream has dropped the `((float) ...)` cast entirely since this patch's anchor was authored. This is real llama.cpp-pin source drift on the exact line RD07's fold-subscale edit depends on, not a BigCherry bug and not something any hardware-free/fake-runtime test could catch (those never build against the real current-pin worktree). The two earlier edits in the same FilePatch (`rd07-df-hoist`, `rd07-fold-subscale`) were not reached/tested for drift since materialization fails atomically on the first mismatched anchor in edit order.

**Decision (per PA39's own step-7 rule)**: this is patch-content maintenance (re-diffing `rd07-sum-line`'s anchor and replacement against the current pin, then re-verifying no other edit in 1203 has silently drifted) -- squarely patch-authoring-domain work, not a PA39 acceptance-time fix, and not safely done as a one-line text edit without re-verifying semantic equivalence and checking sibling edits for the same drift class. Not attempted inline here, consistent with this item's prior fixes always being routed to their owning abstraction (PA36/PA37) rather than patched in PA39. GPT consult was not run this pass (the finding is an unambiguous, mechanically-confirmed source-text mismatch, not a scoping judgment call of the kind the prior passes needed GPT for) -- flagged here explicitly rather than silently skipped.

**PA39 stays `pending`, BLOCKED on patch-content maintenance (not on hardware availability, and not on shared PA36/PA37 implementation this time).** Zero hardware time spent; Brutus confirmed idle before and after (0% GPU all 4 devices). RD05/RD06/RD07 acceptance status this pass: **BLOCKED** (all three -- the run never reached compile/execution, so no per-contract PASS/FAIL evidence exists yet; the failure is patch-content, not per-contract, so it blocks all three equally). Recommendation: route a patch-maintenance item (likely under `patching-patch-system` or 1203's own bump-tracking) to re-anchor `rd07-sum-line` (and audit `rd07-df-hoist`/`rd07-fold-subscale` for the same drift class) against the current llama.cpp pin, verify with the `bump-llamacpp` skill's real-diff procedure, then rerun this PA39 acceptance from clean.

2026-09-16 (patch-content maintenance fix landed -- `rd07-sum-line` anchor drift resolved, attempt #3 ready): Fixed the exact drift attempt #2 recorded above, in `patches/1203_rd050607_rdna4_wmma_fa_q6k_mmq/patch.py`. Real source read directly (not guessed) at pin b10901/`28ff0958291ce3465fabd7bd679d4b0edd742bd9` via `git -C vendor/llama.cpp show b10901:ggml/src/ggml-cuda/mmq-vec-dot.cuh` (the live shared `vendor/llama.cpp` working tree was off-pin at `434ddbbc0` with uncommitted local mutations from concurrent session activity this pass, so the pin's pristine content was read via `git show`, not the dirty working tree, per this task's own instruction).

`rd07-df-hoist`/`rd07-fold-subscale` audited for the same drift class: both anchors match the real pin text verbatim, byte-for-byte, in an isolated worktree checked out at the pin -- no drift, no changes needed.

`rd07-sum-line` itself turned out to have a more interesting shape than a single dropped cast: the real pin source has NO `(float)` cast at all (`sum[...] += C.x[l] * sc[k01/4] * x_df[i*sram_stride] * dB;`), matching attempt #2's finding. But a second real, independent fact changes the fix: `patches/1000_rdna4_mmq_q2k_q6k_fix` (which the patch's own comments blamed for the cast) is `state = "rejected"` and not part of any applied patchset -- the actual source of the cast, when present, is `patches/1006_rdna4_mmq_q6k_codegen_fix` (`state = "untested"`, order 1006 < 1203's order 1203, NOT rejected), a later split-out of 1000's Q6_K half that independently inserts the identical `((float) C.x[l])` cast at the same line. Confirmed by direct read of `1006`'s `patch.py`. Depending on which patch selection composes ahead of 1203 (1006 present in a full-registry composition vs. absent in 1203's own standalone/REQUIRES-only closure), the real text 1203's anchor sees genuinely differs -- both shapes are real, not hypothetical, and both were reproduced in isolated worktrees at the pin.

Fix: `rd07-sum-line`'s anchor is now a regex alternation matching either real shape (cast present or absent) at that exact line, rather than a single `re.escape()`'d literal. The replacement text is unchanged and was already shape-independent -- it always emits its own explicit `(float)` cast on `C.x[l]` regardless of which upstream shape matched, so the fold's numeric behavior is identical either way. GPT consulted before finalizing (kernel-math correctness risk per this item's own step-5 rule): session `ses_88ee99d265454e80`, request `req_e7f2f70610a74c9c`, verbatim verdict -- alternation is correct, do NOT add a `requires`/`conflicts` dependency on 1006 to force a single shape (would wrongly couple two independent patches and break a valid standalone 1203 composition), no double-promotion/rounding risk since the pre-existing cast (if any) is fully discarded by 1203's replacement expression, which contains exactly one explicit cast either way.

Real verification (not fabricated): (1) isolated worktree checked out at pin b10901 via `git worktree add --detach`, `mmq-vec-dot.cuh`'s three RD07 edits (`rd07-hoist-base-scale`, `rd07-fold-subscale`, `rd07-sum-line`) applied cleanly via the real `bigcherry.patch.apply.apply_patch()`/`apply_all()` machinery, both with 1203 standalone and with the full non-rejected registry stack (order <= 1203, including 1006) composed ahead of it -- both scenarios: `mmq-vec-dot.cuh` fully clean, zero anchor failures. (2) The project's own authoritative `patch-rebase-check` CLI, `--source bigcherry --focal-overlay 1203_rd050607_rdna4_wmma_fa_q6k_mmq`, against the live (off-pin, `434ddbbc0`) vendor tree: `mmq-vec-dot.cuh` now fully CLEAN (previously `FAILED_NEEDS_RECONCILIATION` on `rd07-sum-line`); one unrelated, out-of-scope failure remains (`tests/test-backend-ops.cpp`'s `rd07-perf-cases`, an order-dependent anchor needing `1100_hi70_direct_op_evidence` applied first, which 1203's own `REQUIRES` closure does not include -- a real, pre-existing, order-dependency artifact unrelated to this fix and outside this pass's scope, not attempted here).

Offline suite (`tools/tests/patch`, `tools/tests/campaign`, `tools/tests/tuning`): patch 1009 tests (1 failure + 1 error), campaign 694 tests (5 failures), tuning 1104 tests (1 failure) -- 8 total failures/errors, all pre-existing and unrelated to this change (registry-count/contract-count drift and legacy-lint tests, none touching `1203`, `mmq-vec-dot.cuh`, or patch-apply/anchor mechanics; verified by name). This is close to but not exactly the previously documented "9 pre-existing failures" baseline -- plausible drift from concurrent session activity in this shared repo (the vendor tree itself was found off-pin with uncommitted changes from other sessions during this same pass), not from this fix; no new failure traces to `1203` or this edit.

**PA39 remains `pending`, not completed.** Real-hardware acceptance attempt #3 was intentionally NOT run in this pass (out of scope, a separate follow-up per the task). PA39 is ready for that rerun: the `rd07-sum-line` anchor drift that blocked attempt #2 is fixed and real-verified offline (isolated pin worktree + the project's own `patch-rebase-check`); `rd07-df-hoist`/`rd07-fold-subscale` audited clean; no fabricated GPT response, source content, test result, or completion claim above.

2026-09-20 (RD07 three-architecture + quantitative gap CLOSED -- unblocks PA39's full three-arch RD07 coverage): the gap honestly documented above ("RD07's activation/performance/controls checks still only exercise gfx1201, not all three of RD07's declared architectures") is now fixed in `patches/1203_rd050607_rdna4_wmma_fa_q6k_mmq/validation/producer.py`. RD07's performance is no longer the execution-only `bool(outcome.runs)` stub ("quantitative evaluation deferred to PA39 real-hardware acceptance") -- it now routes the real paired-lane runs through the same real `evaluate_promotion_gate()` contract promotion-gate evaluator RD05/RD06 use (`lane_effect_from_run`/`aggregate_contract_effects`/`evaluate_promotion_gate`; RD07 uses `point_estimate_v1` since it declares no `effect_evidence_policy`, and the 3 pairs are sampling depth, not a contract evidence floor), and runs on ALL THREE of RD07's contract-scoped architectures (gfx1100/gfx1201/gfx1030) individually, each gated through `evaluate_promotion_gate()` with RD07's own `max_control_regression_pct=1` acceptance (its gain half is a no-op, exactly as in RD05, since RD07 declares no `target_kernel_gain_pct`). RD07's positive (prefill) and control (decode) lanes are both on its declared model (`control_model` = tierM-gptoss20b-q6k), and the check fails closed (no gate call) if any declared workload's evidence is missing on any architecture. The RD07 controls check now surfaces that same per-architecture control-lane evidence under the 'controls' capability. Activation remains a single representative gfx1201 probe -- the RD07 marker's source line is architecture-independent C++/CUDA in mmq.cu, so one probe is representative evidence that the host-side Q6_K specialization dispatch marker is wired and fires (it is NOT three-architecture activation proof); the per-architecture-matrixed obligations are correctness/performance/controls (measured per-arch), not activation -- documented honestly rather than implying activation is three-arch-matrixed.

Hardware-free evidence: 28/28 tests in `test_1203_validation_producer.py` pass, including 4 NEW tests that prove the gate is genuinely invoked (an over-budget control regression on all three archs -> FAIL, something `bool(outcome.runs)` could never detect; a missing prefill lane -> fail-closed; a passing bound -> PASS; and all six per-arch lanes benchmarked, positive=prefill / control=decode, on the declared model). A real fail-closed bug found and fixed while wiring this (the per-arch gate read `arch_backend_ref_pass[arch]` before the backend-reference section had populated it when model/corpus were missing -> KeyError; now the performance section fails closed unless the backend reference executed on all three archs) is covered by the pre-existing `test_missing_model_and_corpus_fails_closed`. The full `tools/tests/patch` suite was diffed against a detached HEAD (pre-fix) worktree to confirm zero new failures (see the change-event ledger record for the exact baseline-vs-working-tree comparison). This unblocks PA39's real-hardware acceptance for full three-architecture RD07 coverage.

GPT review (2026-09-20, dev-gpt-agent, request req_6d6f282ced73433c, commit b02b9514): **APPROVE WITH NITS** -- no BLOCKER/MAJOR; the fail-closed paths (missing positive/control workload on any arch -> FAIL without KeyError; the `len(arch_backend_ref_pass)` guard being non-redundant with `missing_rd07` since it covers model/corpus evidence being unavailable; controls reusing the three decode lanes and failing closed) and the per-arch loop (dict population, `all()`, missing-entry handling, artifact maps) were all confirmed correct. Three NITs, all documentation-accuracy (the three checks 1-6 requested were answered yes/yes/non-redundant/yes/no-bug/acceptable-with-wording), all addressed in this same pass: (1) the "ci95-style" descriptor for RD07 was inaccurate (RD07 uses point_estimate_v1, not ci95) -- reworded to "contract promotion gate"; (2) the activation wording was narrowed to "representative, not three-architecture activation proof"; (3) an optional one-architecture-only missing-control test was noted (implementation already handles it correctly; not added this pass). No fabricated GPT response above; the raw request-id is preserved.

**PA39 remains `pending`, NOT completed.** The real-hardware acceptance rerun itself is the next pass (this item's own step-7 rule: fix the defect in the owning abstraction, then rerun acceptance in a separate pass); it now runs against a revision with the RD07 three-arch/quantitative gap closed, hardware-free-verified, and GPT-reviewed.

2026-09-20 (real-hardware acceptance attempt #3, real result, BLOCKED on a gfx1201/R9700 GPU-offload incompatibility, zero BigCherry-code blocker): the real-hardware acceptance rerun (the "next pass" flagged above) was executed on Brutus (repo synced to `9bf76d9a`). **This was a major advance over attempts #1/#2:** the generic `--validation-producer` path got ALL the way through -- plan resolution OK (no more `Unknown producer input 'control_model'`; the PA36 `run_validation_producer` input-merge fix holds on the real path), source isolation (apply) OK (the producer-side `control_model` input now reaches `apply_validation_worktrees`; the PA36 `_apply_producer_worktrees` wiring holds), build OK (control + subject, llama-perplexity + llama-bench, all built, CMake cache reused), and the RD07 three-arch gate (the `b02b9514` gap-closure) is exercised on real hardware.

**Result: BLOCKED on a device-specific, external, non-BigCherry gfx1201 (R9700) GPU-offload incompatibility (ROCm 7.2.4), characterized on-device:** gfx1100 (devices 0/1) and gfx1030 (device 3) benchmark fine (qwen4b: 100/106/78 t/s); gfx1201 (device 2, R9700 32 GB) loads the model in CPU mode (`-ngl 0/1/10` -> 14/16/20 t/s) but **fails on full offload** (`-ngl 30/99` -> `llama_bench: error: failed to load model`); the control model (gpt-oss-20b) fails the same way on gfx1201 offload; it is NOT a VRAM-capacity issue (3.96 GiB model, 32 GB VRAM) and NOT a fundamental offload failure (partial offload works) -- it is a device-specific offload threshold under ROCm 7.2.4; the alternate ROCm 7.1.4.0 (`/opt/rocm7140`) is only a partial `core-7.14` install, not buildable, so no alternate-toolchain workaround is available. The attribution is further corroborated by multiple independent prebuilt `llama-bench` binaries (bc-xtx, bc-tune, bc-build -- none built by this session) that **all fail the same way on gfx1201 full offload but work fine on gfx1100** (102 t/s, `-ngl 99`), ruling out a broken binary and confirming the failure is ROCm 7.2.4 / R9700-specific, not BigCherry. **Owner: hardware/driver (external), not BigCherry.** This blocks RD05/RD06 (gfx1201-positive) and RD07 (the gfx1201 arch); the gfx1100/gfx1030 lanes are proven working. PA39 remains `pending` -- the generic path and the RD07 gate are proven working, and the remaining blocker is the R9700's full offload under the available toolchain (a real, evidence-backed BLOCKED result, not an excuse to re-run the same attempt). No fabricated GPT response, source content, test result, or completion claim above.

2026-09-21 (real-hardware acceptance attempt #4 / retry-1, completed, partial PASS / overall ineligible): Brutus ran the current generic PA37 producer from `/home/audumla/bc-pa-work` at revision `a4d373fa` with the full fat control/subject pair, Qwen positive model, GPT-OSS control model, corpus, `/mnt/vault/tmp/bc-rocm`, and explicit `gfx1100=0,1`, `gfx1201=2`, `gfx1030=3` mapping. The process exited 0 after real execution on all mapped architectures; no source/build/offload failure occurred. `producer-execution.json` SHA-256 is `a6949b475ba84d453942cff1e929d6001d3b45f625c7db2a1bb3e0c61485ef5a`. RD05 passed backend-reference and controls. RD07 passed activation, backend-reference on gfx1100/gfx1201/gfx1030, performance, and controls; its per-architecture performance gate was PASS on all three architectures. RD06 activation and backend-reference passed, but `rd06-performance` failed its declared quantitative gate: `target_kernel_gain_pct` CI95 lower bound `-0.07448539837840995` (point estimate `-0.01541093666419524`) is below required `0.5`; `rd06-controls` consequently failed because it reuses that gate. `producer-execution.json` is `eligible=false`, `blocked=false`, `evidence_record=null`, with exactly those two RD06 reasons. Therefore PA39 remains pending: RD05/RD07 evidence is real PASS, RD06 is real FAIL, and no overall canonical evidence record/promotion receipt was emitted. This is a measured contract result, not a hardware or orchestration blocker. Artifact hashes are recorded in the producer execution file; the run log is `/mnt/vault/tmp/bc-runs/pa39-rd050607-retry1.log`.

## Dependencies

- PA36-F: generic producer/build/run/result/evidence path is implemented, including first-class plural-contract runtime.
- PA37: 1203 has a complete executable package and patch-local producer with no remaining implementation handoff.
- PA34: canonical selector identity for the G2/package prerequisites in step 1.
- PA29: consumer migration to semantic sources must land first, so acceptance exercises the intended final selector/source world rather than a pre-migration one that would force a rerun (RV183).

Ordering: run near convergence — after PA29, before the destructive PA30/PA31 gates — so any real-hardware defect is fixed before the equivalence receipts are cut.

## Benefit target

The proof is that plural-contract acceptance requires no new shared implementation work. Normal completion should be receipt/evidence changes only.

Measurable targets:

- zero new shared dispatcher/CLI/evidence branches for 1203/RD05/RD06/RD07;
- zero new producer registration or result-schema work in this item;
- one real fat control/subject build pair services all required architecture runs;
- real evidence preserves explicit contract/check scope and canonical selector/build/device/artifact identity;
- any discovered defect is fixed in the owning PA36/PA37 generic or patch-local abstraction, then this acceptance is rerun; PA39 itself never becomes a parallel implementation owner.

## Change Log

- 2026-09-16: pre-run reconnaissance session -- BLOCKED before any real-hardware execution, honest status, no hardware time spent and no result fabricated. Real device topology confirmed on Brutus (`rocminfo`): device 0/1 = gfx1100 (dual 7900 XTX), device 2 = gfx1201 (R9700), device 3 = gfx1030 (6900 XT) -- matches PA26/PA37 precedent (gfx1201 = device 2). Real selector confirmed: `--patch 1203_rd050607_rdna4_wmma_fa_q6k_mmq --validation-producer 1203_rd050607_rdna4_wmma_fa_q6k_mmq/rd050607`. Corpus located on Brutus: `/home/audumla/perf-sweep-20260911/corpus/wikitext-2-raw/wiki.test.raw`; model root `/mnt/vault/llm-models` (`tierA-qwen4b-q6k`, `tierM-gptoss20b-q6k`). Gateway confirmed healthy overall (23 providers loaded) with the documented intermittent gpt-auto composer stalls; one consult (req_b5602ea525264089) took ~15 min but completed successfully with no recovery needed, a second shorter follow-up (req_26f26d17a04e46aa) did not return in-session and was not waited on further since the first answer already resolved the open question.

  Before spending any hardware time, read PA37's actual committed `validation/producer.py` end-to-end (not just its hardware-free test suite) against the real CLI wiring in `validation_campaign.py`'s `_run_validation_producer()`, and against `config/experiment-contracts.toml`'s real RD05/RD06/RD07 acceptance blocks. Found and GPT-confirmed (req_b5602ea525264089, verbatim-summarized, not fabricated) THREE real defects in the already-committed generic/patch-local path, none of which PA37's hardware-free tests could catch (they inject a fake `ProducerRuntime`, so they never exercise the real CLI arg-to-`ProducerContext` wiring or a real contract's positive/control model split):

  1. **Corpus never wired for `--validation-producer`.** `_run_validation_producer()` hardcodes `ProducerContext(..., corpus=None, ...)` -- there is no `--corpus`/`--producer-corpus` CLI flag for the generic producer path (only the legacy `--rd04-corpus`/`--rd73-corpus` flags exist, consumed by different, RD-specific branches). Every RD05/RD07 `backend_reference` check in 1203's producer requires `ctx.corpus is not None`, so as committed, the generic path can never pass RD05/RD07 correctness on real hardware regardless of device availability. GPT's fix: add a generic `--producer-corpus PATH` flag in PA36's CLI (owning abstraction: PA36, not 1203-specific), thread straight into `ProducerContext.corpus`.
  2. **RD06's quantitative performance bound is never evaluated.** 1203's `producer.py` sets `rd06_perf_ok = bool(outcome.runs)` -- execution-only, by its own docstring deliberately deferred to "PA39's real-hardware acceptance". But the real `ci95_threshold_bound_v1` evaluator already exists and is unused: `tools/bigcherry/experiment/execution.py::lane_effect_from_run()` (converts a `PairedLaneRun` into a `LaneEffect` with CI95 bounds) and `tools/bigcherry/experiment/contract.py::evaluate_promotion_gate()` (the real gain/regression bound gate). `outcome.runs` (returned by `ctx.runtime.run_paired_llama_benchmark()`) already contains the underlying `PairedLaneRun`s with the needed stats -- the producer just never calls them.
  3. **RD06's declared control model is never benchmarked (found independently by both this session's code read and GPT).** RD06's real contract (`config/experiment-contracts.toml`) declares POSITIVE = `tierA-qwen4b-q6k` (decode+prefill) vs CONTROL = `tierM-gptoss20b-q6k` (decode) -- two different models, by design (the FA config table is per-shape/head-dimension, so a different-geometry model is the only thing that can actually detect a mis-selected config). 1203's `producer.py::run()` takes exactly one `ctx.model` and benchmarks only decode+prefill on it for every contract's performance check -- RD06's declared control model is never measured at all, so the 1% regression bound cannot be truthfully evaluated even after fix #2 lands. (RD07's positive/control split is same-model/different-workload, already representable with the current single-model shape -- this gap is RD06-specific.)

  **Decision, per PA39's own step 7 authorization ("a small, clearly-scoped fix is fine; anything larger should be flagged rather than expanded into new implementation work") and GPT's answer**: fix #1 (one generic CLI flag, PA36-owned) is small and scoped. Fixes #2+#3 together require real patch-local implementation work in 1203's `producer.py` -- multi-model orchestration (a second model input, a second paired-benchmark run, model-to-role plumbing) plus wiring the real `lane_effect_from_run`/`evaluate_promotion_gate` machinery through with RD06's actual `Acceptance` record from the loaded contract, plus new hardware-free tests covering the new code paths. That is real PA37-scale implementation, not an acceptance-time patch. Running real hardware now against the as-committed producer would only reproduce the already-known, already-understood corpus/control-model FAIL and would spend real Brutus GPU time for zero new information -- not attempted, per this project's fail-closed/no-fabrication standard.

  **PA39 left `pending` (BLOCKED on implementation, not on hardware availability).** No hardware time spent (Brutus confirmed idle before and after this pass, 0% GPU all 4 devices, no live build/server processes). No fabricated GPT response, marker, evidence record, or test result in this note -- GPT's response text (req_b5602ea525264089) is summarized faithfully, matching the real code read independently performed first. **Recommendation**: route fixes #1 (PA36 CLI) and #2+#3 (PA37 producer.py) back to their owning items as new implementation steps -- do not implement them inside PA39 -- then rerun this acceptance from clean once landed.

- 2026-09-13: created; RD12 and RD04 dedicated bridge flags subsequently landed.
- 2026-09-13: incorporated RV183 (dev-gpt-agent ordering review, req_968704cc8c8c4a41): added PA34/PA29 dependencies with convergence-ordering rationale, renumbered order 6 -> 10.
- 2026-09-14: narrowed to unfinished RD1203 integration and aligned with PA36.
- 2026-09-14: refactored to acceptance-only after PA37 assumed complete 1203 implementation/registration ownership.

## Reviews

- RV183

## Ledger-events




- chg_20260914_003255_fixed-the-patch-system-plans_2121
- 2026-09-14T00:33:29.937118+00:00 (updated-by): Updated: section:ledger-events
- 2026-09-15T20:47:01.704107+00:00 (updated-by): Updated: section:notes
- chg_20260915_204718_pa39s-real-hardware-run-of-pa_7872
- 2026-09-15T20:47:21.641806+00:00 (updated-by): Updated: section:ledger-events
- 2026-09-15T21:00:09.814782+00:00 (updated-by): Updated: section:notes
- 2026-09-15T21:15:06.432206+00:00 (updated-by): Updated: section:notes
- chg_20260915_220212_pa39s-first-real-hardware-acc_5295
- 2026-09-15T22:02:15.116969+00:00 (updated-by): Updated: section:ledger-events
- 2026-09-15T22:02:55.445303+00:00 (updated-by): Updated: section:notes
- 2026-09-15T22:03:08.056307+00:00 (updated-by): Updated: section:notes
- 2026-09-16T00:15:53.107701+00:00 (updated-by): Updated: section:notes
- 2026-09-16T00:19:18.949735+00:00 (updated-by): Updated: section:notes
- chg_20260916_001927_pa39s-second-real-hardware-ac_8061
- 2026-09-16T00:19:30.719531+00:00 (updated-by): Updated: section:ledger-events
- chg_20260916_003903_fixed-patch-1203s-q6_k-mmq-su_5412
- 2026-09-16T00:39:06.153374+00:00 (updated-by): Updated: section:ledger-events
- chg_20260920_103538_fixed-a-gap-in-the-1203-rd07-v_7614
- 2026-09-20T10:35:41.739877+00:00 (updated-by): Updated: section:ledger-events
- 2026-09-21T05:38:45.942381+00:00 (updated-by): Updated: section:notes
- chg_20260921_054159_pa39-now-has-current-real-hard_5591
- 2026-09-21T05:42:04.890553+00:00 (updated-by): Updated: section:ledger-events
- chg_20260921_085550_opened-a-bounded-remediation-i_3035
- 2026-09-21T08:56:00.692119+00:00 (updated-by): Updated: section:ledger-events
- chg_20260921_085739_documented-the-evidence-based_7114
- 2026-09-21T08:57:49.240906+00:00 (updated-by): Updated: section:ledger-events
- chg_20260921_090229_added-current-focused-test-evi_2839
- 2026-09-21T09:02:35.801640+00:00 (updated-by): Updated: section:ledger-events
