---
id: EX02
order: 0
plan: external-fixes
state: pending
created-at: '2026-08-11T23:58:33.034169+00:00'
breadth: ''
skill: advanced
created-by: agent
work: M
---

# MMQ q6_K/J=112 illegal-memory-access incident on gfx1100 -- quarantined, root cause open

## Description

The historical incident involved a specific generated MMQ kernel candidate -- `mmq:q6_k:j112:fb0:t256:o2:i128:sram-q6_k:k256:sk0:v1` (Q6_K quantized input, J-tile size 112, thread config t256/o2/i128, shared-memory q6_k SRAM path) -- and recorded `ROCm error: an illegal memory access was encountered` on AMD RX 7900 XTX (gfx1100), reproduced deterministically 3 times across two independent instrumentation approaches. Only reachable under bigcherry's own exhaustive tuning sweep: native dispatch's own J-selection (`ggml_cuda_mmq_native_j_best` in mmq.cu) always picks the smallest J that covers a batch in one tile, so it never forces J=112 onto MTP speculative-decoding's narrow draft-decode batch widths (3-16) the way an exhaustive sweep does -- a genuinely new code path, not a previously-known-bad one.

Environment: AMD RX 7900 XTX (gfx1100), ROCm 7.2.4, Clang 22.0.0 (roc-7.2.4 26084), llama.cpp 4801e3c567d5 (tag b10362), Qwen3.5-9B MTP variant Q6_K, `--spec-type draft-mtp --spec-draft-n-max 5`.

Checked whether already reported upstream: no. Nearby-but-different issues found (#21140 device -1 in recurrent-state restore, #22052 RX 7900 XTX memory fault via Oculink, #20839 invalid-device-function from architecture spoofing) -- none match this trigger combination (WMMA q6_K MMQ + J=112 + MTP-narrow batch widths).

Two hypotheses formed and ruled out with real numbers/source, not guesses:
1. Shared-memory overflow via HIP's no-op `CUDA_SET_SHARED_MEMORY_LIMIT` (confirmed real platform gap, but this GPU's sharedMemPerBlock==sharedMemPerBlockOptin==65536, and the crashing config's real requirement computed by hand is 55,744 bytes -- 9.75KB of headroom, not an overflow).
2. Y-tile global-memory over-read of the src1_q8_1 staging buffer for narrow batches -- ruled out, mmq.cu's own allocation already pads by `ggml_cuda_mmq_get_J_max(...) * sizeof(block_q8_1_mmq)` specifically to make this safe (the same J_max term bigcherry's own ggml_hip_mmq_workspace mirrors, HI54).

Current mitigation (already implemented and live in `src/ggml/src/ggml-cuda/hip-autotune-dispatch.cu`, inside `ggml_hip_mmq_can_execute`, immediately before its final `return ggml_cuda_mmq_variant_is_eligible(...)`): a narrow, reversible, gfx1100-scoped quarantine of exactly this candidate identity (src0_type==GGML_TYPE_Q6_K && primary==112 && fallback==0 && architecture_code==GGML_HIP_ARCH_GFX1100). Explicitly NOT a broad disable of the candidate elsewhere -- this config row is shared with other architectures' own tables that were never proven to crash, and a blanket exclusion would quarantine hardware unnecessarily. Verified fix (not a guess): rebuilt with the block compiled in, reran the exact MTP-9B reproduction twice; `grep -c "mmq:q6_k:j112" journal.jsonl` = 0 and `tune-journal status` reported `complete: true, interrupted: false` -- first fully completed run of that workload in four attempts.

Diagnostic status, 2026-08-14: the quarantine was bypassed only through the opt-in `GGML_HIP_TUNE_TEST_DISABLE_EX02_QUARANTINE=1` switch on Brutus gfx1100. With the exact shared Qwen3.5-9B MTP Q6_K model, `HIP_LAUNCH_BLOCKING=1`, graphs disabled, the `full-max` manifest, and the standard `server-bench` pp512/tg128 workload, the tuner measured the exact J=112/fallback=0 candidate 39 times and all Q6_K MMQ candidates 526 times. The run completed with 89 result records and zero measurement failures, HIP errors, or assertions. A second run under `rocgdb` measured J=112/fallback=0 39 times again and also completed without a fault; the debugger was stopped only by a deliberate SIGTERM after the bench completed. This is clean non-reproduction evidence, not a root-cause capture and not sufficient to remove the quarantine.

GPT-auto review, 2026-08-14: the candidate stable name is coarser than the complete dispatch identity, so these counts do not prove that the historical failing shape was exercised. The separate `/tmp/bc-tune-*` artifact is a Qwen3.5-0.8B run and contains no EX02 fault. The opt-in `GGML_HIP_TUNE_TRACE_ATTEMPTS` record now includes the complete canonical signature JSON beside its digest, so a future killed measurement can be replayed exactly; normal production tracing remains off. The next step remains recovery of the historical digest/full signature and exact tuner-path replay under `HIP_LAUNCH_BLOCKING=1`, graphs disabled, tracing, and `rocgdb`.

Trace implementation validation, 2026-08-14: after applying the pushed overlay to the Brutus vendor checkout and rebuilding the HIP tuner from `15cc257`, one real Qwen3.5-0.8B completion under `HIP_LAUNCH_BLOCKING=1` produced 1,395 durable attempt events. Offline parsing found 1,395/1,395 object-valued `signature_json` payloads, including the complete `ne0`/`ne1`/`ned`, stride, type, flags, op, and refinement fields. Journal SHA-256: `d0eb2dc70bbe2ac1d0de1627ae09755d06fd05b749704d98ec80dcf0079ef9d3`. This validates the diagnostic mechanism only; it is not the historical EX02 reproduction.

Trace integrity validation, 2026-08-14: independently reconstructing the canonical JSON for every one of those 1,395 attempts and applying the production BLAKE2b-128 digest algorithm yielded `digest_matches=1395`, `mismatches=0`. This proves that the emitted signature digest and complete JSON agree for that run; it does not identify the historical failing signature.

Historical-artifact audit, 2026-08-14: the retained `/tmp/ex02-*` journals, logs, measurement exports, `/tmp/bc-tune.sqlite`, repository incident records, and llamacpp raw-log tree were searched for the exact candidate and a complete signature. The exact candidate is present in the clean `ex02-gdb`/`ex02-sweep` diagnostic journals, but those pre-existing attempt records contain no `signature_json` and both runs completed without a fault. The other candidate hits are measurement/catalog records or ineligible forced-probe warnings. No historical fault journal, complete canonical signature, or historical signature digest survived. EX02 therefore remains pending and quarantined; no exact replay or kernel change is justified until that identity is recovered.

The earlier synthetic probe was not an EX02 reproduction: `MUL_MAT(type_a=q6_K,type_b=f32,m=16,n=1,k=256)` requires fallback=1 because the runtime derives `shape_fallback` from `sig.ne0[1] % 128`, so it cannot exercise the `fb0` candidate. Direct `GGML_HIP_FORCE_CANDIDATE` mode is also not a tuner reproduction because an eligible forced candidate returns before the normal measurement path. The next diagnosis must preserve the original failing signature/shape and run through the exhaustive measurement path.

Root cause is still open -- both hypotheses above were real platform/behavior facts but not the actual cause. A live faulting instruction/address inside `ggml_cuda_mmq_vec_dot_q6_K_q8_1_mma` (mmq-vec-dot.cuh) is still required before proposing a kernel or quarantine change; clean generic runs and the new diagnostic bypass do not satisfy that gate.

## Steps

1. Perform a final offline-only provenance check only if original 2026-08-11 agent/tool transcripts, Git recovery metadata, or an already-known snapshot becomes readily available. Accept only an original canonical signature, original signature digest, original measurement/attempt record carrying either, or a recoverable historical artifact containing them. Candidate-only matches and reconstructed shapes are not historical identity evidence.
2. If exact historical identity is recovered, replay only that signature through the exhaustive tuner measurement path with the diagnostic quarantine bypass, HIP_LAUNCH_BLOCKING=1, graphs disabled, attempt tracing, and rocgdb. If it is not recovered, do not run another generic GPU sweep; leave EX02 pending and the quarantine unchanged.
3. Once a root cause is identified, implement a real fix (either in bigcherry's generated candidate code, or as an upstream-fixes patch if the bug is in llama.cpp's own kernel), then rerun the exact trigger and a clean native control.
4. Remove the quarantine block in hip-autotune-dispatch.cu only after the exact trigger passes under the fixed build on gfx1100 and the evidence identifies why the old fault is prevented.
5. Do not widen the quarantine's scope while root cause is open.

## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files

src/ggml/src/ggml-cuda/hip-autotune-dispatch.cu; src/ggml/src/ggml-cuda/hip-autotune-journal.h; src/ggml/src/ggml-cuda/hip-autotune-journal.cpp; src/ggml/src/ggml-cuda/hip-autotune-tuner.cu; docs/reference/FINDINGS.md

## Validation

Historical reproduction is conditional on exact identity recovery. Do not run another generic exhaustive Qwen3.5-9B sweep for EX02. If the historical canonical signature/digest is recovered, exercise that exact tuner-path trigger with the diagnostic quarantine bypass, HIP_LAUNCH_BLOCKING=1, graphs disabled, tracing, and rocgdb; otherwise retain the quarantine and wait for new evidence.

## Effort & Risk



## Standards



## Acceptance Criteria

- A clean generic MTP/server-bench run, including the 2026-08-14 two-run result, is recorded as non-reproduction evidence and does not by itself close EX02 or remove the quarantine.
- The historical trigger must be identified by its complete canonical signature/shape and candidate identity, and must be exercised through the exhaustive tuner measurement path rather than only native dispatch or `GGML_HIP_FORCE_CANDIDATE`.
- Before any production quarantine change, rocgdb or equivalent evidence must identify the faulting instruction/address and distinguish kernel fault, workspace/allocator fault, and measurement-harness fault.
- A proposed fix must pass the exact trigger with `HIP_LAUNCH_BLOCKING=1`, a native control, and a fresh full-tune run with zero HIP errors, assertions, measurement failures, and candidate-attribution gaps on gfx1100.
- The diagnostic bypass remains opt-in and exact-candidate scoped; it is never enabled by default or used as a production mitigation.

## Notes

Diagnostic tooling built to pin this down (opt-in GGML_HIP_TUNE_TRACE_ATTEMPTS journal 'attempt' event kind, HI48 mechanics) is now a permanent part of the tuner. Full investigation log and evidence also recorded in docs/reference/FINDINGS.md under the historical illegal-memory-access report for the generated MMQ candidate.

Additional confirmation, 2026-08-12: rebuilt bigcherry-replay from the post-recovery, now-committed source (byte-identical quarantine logic to what was hardware-verified on 2026-08-11, only a comment reference changed EX01->EX02). Ran a real Qwen3.6-27B-v2-MTP Q8_0 completion under live MTP speculative decoding (--spec-type draft-mtp --spec-draft-n-max 5) on dual gfx1100 -- the same architecture and dispatch family (mmq q6_k-adjacent quantization path under narrow MTP draft-decode batches) the quarantine protects. Completed cleanly: 56.85 tok/s generation, 62.8% draft acceptance (59/94), no illegal-memory-access crash. Not a byte-for-byte repro of the original failing shape (different model/quant), but real end-to-end evidence the quarantined build path is stable under exactly the workload class (MTP narrow-batch MMQ) that originally crashed.

Workstream status — deferred / non-blocking mainline. EX02 remains an open safety record and the exact gfx1100 q6_K J=112/fallback=0 production quarantine remains active. Known retained artifacts do not contain the original complete canonical signature or digest; no further EX02 GPU investigation is scheduled. Resume only if new historical provenance appears, a naturally occurring fault is captured by the complete attempt trace, or a quarantine change is proposed. Deferral is not closure and does not relax the acceptance criteria.

RDNA run-sheet review (2026-08-15): RB07 is safety-gated by EX02. Preserve the exact gfx1100 quarantine, patch 1000 baseline, shape-specific reproduction checks, and fail-closed promotion rules; generic external cleanliness cannot close EX02.

HI71 CROSS-REFERENCE (2026-08-18): a new, independently-triggered illegal-memory-access incident on gfx1201 (dense MUL_MAT under real MTP speculative decoding, tune-mode dispatch -- see HI71, hip-autotune plan) produced a synchronized (AMD_SERIALIZE_KERNEL=3) crash stack landing in an MMQ candidate's own kernel launch while being timed by the tuner, at a small forced multi-token batch width -- the same general shape class (narrow N under MTP draft-decode batches) as EX02's original trigger.

An independent gpt-auto-agent deep-dive on HI71 (2026-08-18, not prompted with EX02's own history, arrived at this by reading source directly) proposed a sharper version of EX02's own previously-'ruled-out' Hypothesis 2 (Y-tile global-memory over-read of src1_q8_1): the earlier ruling-out only checked THAT ggml_cuda_mmq_get_J_max(...) pads the allocation, never WHETHER that padding -- sized from a J-selection search over the REAL N -- is sufficient when the tuner FORCES a J far outside what that search over the real N would ever produce. For EX02's own historical trigger (q6_k, J=112, narrow MTP batch widths 3-16), forcing J=112 at N in [3,16] would require on the order of 96-109 padding columns beyond what a J_max search anchored to that small real N would allocate -- exactly the kind of mismatch the sharper framing predicts should fault.

This does NOT by itself close EX02 (the exact historical canonical signature/digest was never recovered, so EX02's own acceptance criteria for closure are unmet -- this reasoning does not substitute for that recovered identity). It DOES mean the Y-staging-overread hypothesis should be treated as REOPENED and the leading theory again, not permanently ruled out as the current notes state. If HI71's own diagnostic step (capture exact failing candidate + compute required_tail=(J-N%J)%J vs available_tail=ggml_cuda_mmq_get_J_max(type,fallback,cc,N), confirm required_tail > available_tail) confirms the mechanism there, the SAME fix (dense MMQ launch-envelope shape-awareness in ggml_cuda_mmq_variant_is_eligible, scoped to dense MMQ, per HI71's detailed_solution) would very plausibly ALSO be the correct fix for EX02's quarantined gfx1100 q6_k/J=112/fallback=0 candidate -- worth re-testing removal of the EX02 quarantine once HI71's fix lands and passes its own acceptance criteria, rather than treating them as unrelated incidents. Do not remove the EX02 quarantine based on HI71 alone -- EX02's own acceptance criteria (exact historical trigger reproduction, rocgdb/equivalent fault identification) still apply and are unchanged by this cross-reference.

## Change Log

- 2026-08-11T23:58:33.034169+00:00 (created-by): Created by agent

## Ledger-events


- chg_20260812_000319_finished-recovering-the-last-5_8173
- 2026-08-12T00:03:21.024306+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-12T00:21:18.389587+00:00 (updated-by): Updated: section:notes
- chg_20260814_054556_reconciled-stale-review-and-pl_6469
- 2026-08-14T05:45:56.563626+00:00 (updated-by): Updated: section:ledger-events
- chg_20260814_083851_prepared-a-safe-opt-in-ex02-d_3692
- 2026-08-14T08:38:51.885009+00:00 (updated-by): Updated: section:ledger-events
- chg_20260814_090129_recorded-the-real-brutusrocgd_6852
- 2026-08-14T09:01:29.166222+00:00 (updated-by): Updated: section:ledger-events
- chg_20260814_090931_added-the-missing-opt-in-ex02_6722
- 2026-08-14T09:09:31.391819+00:00 (updated-by): Updated: section:ledger-events
- chg_20260814_091657_fixed-the-ex02-diagnostic-trac_9548
- 2026-08-14T09:16:57.852818+00:00 (updated-by): Updated: section:ledger-events
- chg_20260814_092145_validated-the-corrected-ex02-s_1183
- 2026-08-14T09:21:45.571594+00:00 (updated-by): Updated: section:ledger-events
- chg_20260814_100203_updated-ex02-evidence-to-disti_3276
- 2026-08-14T10:02:03.270343+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-14T10:39:35.867221+00:00 (updated-by): Updated: section:steps, section:notes, section:validation
- chg_20260814_105748_reconciled-the-incident-record_3634
- 2026-08-14T10:57:48.435719+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-15T03:34:10.593835+00:00 (updated-by): Updated: section:notes
- 2026-08-18T10:38:52.317091+00:00 (updated-by): Updated: section:notes
- chg_20260818_103922_got-a-thorough-second-opinion_6299
- 2026-08-18T10:39:22.331473+00:00 (updated-by): Updated: section:ledger-events
