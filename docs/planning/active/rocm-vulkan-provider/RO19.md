---
id: RO19
order: 19
plan: rocm-vulkan-provider
state: pending
created-at: '2026-09-05T00:32:58.162809+00:00'
breadth: ''
skill: advanced
created-by: agent
priority: P1
work: M
---

# Capture and pin PR #27952 Vulkan CM1 source

## Description

Capture llama.cpp PR #27952 as a reproducible, squashed BigCherry Vulkan experimental source candidate without enabling it or reproducing the PR's 50-commit history. Reconcile the immutable PR head against the current vendor pin before implementation.

## Steps

1. Record the PR URL, immutable head SHA, base SHA, current BigCherry vendor pin, and the final-state diff boundary.
2. Diff the final PR state against the current pin, not the PR base; classify each hunk as shader, generator, capability, pipeline, selection, or fallback integration.
3. Re-verify the current Vulkan seams and architecture enums at the current pin.
4. Produce one packaged downstream patch identity with deterministic application order and no behavioral enablement.
5. If the PR merges or its head changes, repeat provenance review before applying the patch.

Implementation packet: create `patches/1246_ro19_vulkan_cm1_pr27952/` with `patch.toml`, `patch.py`, `SUMMARY.md`, `validation.toml`, and validation support as required by the packaged-patch loader. Register `llama-cpp-pr-27952` in `config/external-sources.toml`. Freeze the PR head/base/current pin, diff `cc83d7b..965e571` against `2578138`, audit each final hunk, and materialize one deterministic semantic transplant. Any PR head change requires a new snapshot and review.

## Detailed Solution & Technical Design

The captured source must include the coupled host and shader changes: ggml-vulkan.cpp; vulkan-shaders/mul_mmq_cm1.comp; vulkan-shaders/mul_mmq_cm1_funcs.glsl; and vulkan-shaders/vulkan-shaders-gen.cpp. Preserve the upstream optimization history only as provenance; the BigCherry patch is a final-state source transformation. Do not cherry-pick individual commits blindly, and do not split the shader from generator/host registration. The patch remains availability-only until RO20 and RO21 provide capability and forced-execution gates.

Use `1246_ro19_vulkan_cm1_pr27952` as the upstream implementation patch only. Metadata must remain `state = "untested"`, `kind = "upstream-backport"`, `origin = "upstream-pr"`, `backend = "vulkan"`, `group = "vulkan-cm1"`, with the PR URL, external source ID, retirement rule, plan IDs RO19/RO20, Vulkan subsystem/hardware declarations, and real dependency/conflict fields copied from the current packaged-patch schema. Do not invent a `stream` field; express experimental composition through `config/recipes.toml`. Inspect the packaged loader and `tools/bigcherry/patch/patchset.py` before implementing dependency semantics.

Package-only policy: the package directory name must equal `1246_ro19_vulkan_cm1_pr27952`; at minimum it contains `patch.toml` and `patch.py`. Keep `README.md`, `validation.toml`, `validation/`, and `evidence/validation.json` out of the initial untested capture unless the patch is being made validation-ready. Do not place Experiment Contract hypothesis, thresholds, workload, controls, or hardware claims in patch metadata or adapter configuration.

## Code Samples & Guidance

upstream-ref = "https://github.com/ggml-org/llama.cpp/pull/27952"
upstream-head = "965e571..."  # verify immediately before capture
backend = "vulkan"
state = "untested"
requires = ["RE30"]

```toml
id = "1246_ro19_vulkan_cm1_pr27952"
order = 1246
group = "vulkan-cm1"
state = "untested"
kind = "upstream-backport"
origin = "upstream-pr"
backend = "vulkan"
upstream-ref = "https://github.com/ggml-org/llama.cpp/pull/27952"
external-source = "llama-cpp-pr-27952"
retirement = "remove when the pinned llama.cpp contains PR #27952 or an audited equivalent"
plan-ids = ["RO19", "RO20"]
subsystems = ["ggml-vulkan", "vulkan-shaders"]
hardware = ["amd-rdna3", "amd-rdna3.5", "amd-rdna4"]
backends = ["vulkan"]
```
External source snapshot must record head `965e57103fce2c4329cdfc2b8300f8f7ed57c9fe`, semantic base `cc83d7b4824f73cfdda4dfbb47ee39804f71b328`, reviewed date, and target pin `2578138397d7b422bb0e160efdd429976c55fb55`. Copy an existing valid tracked-source status value rather than guessing a new enum.

## Files

vendor/llama.cpp/ggml/src/ggml-vulkan/ggml-vulkan.cpp; vendor/llama.cpp/ggml/src/ggml-vulkan/vulkan-shaders/mul_mmq_cm1.comp; vendor/llama.cpp/ggml/src/ggml-vulkan/vulkan-shaders/mul_mmq_cm1_funcs.glsl; vendor/llama.cpp/ggml/src/ggml-vulkan/vulkan-shaders/vulkan-shaders-gen.cpp; packaged patch metadata and provenance

`config/external-sources.toml`; `patches/1246_ro19_vulkan_cm1_pr27952/patch.toml`; `patch.py`; `SUMMARY.md`; `validation.toml`; four vendor paths under `vendor/llama.cpp/ggml/src/ggml-vulkan/`; source/provenance tests only where existing coverage lacks the snapshot invariant.

Initial RO19 package: `patches/1246_ro19_vulkan_cm1_pr27952/patch.toml`, `patch.py`, and patch-local documentation such as `SUMMARY.md`. Validation-ready expansion later requires `README.md`, `validation.toml`, optional `validation/`, and `evidence/validation.json`; large logs belong under `artifacts/patch-validation/<patch-id>/<campaign-identity>/`.

## Validation

Materialize a pristine current-pin checkout, apply exactly the packaged patch, run the Vulkan source audit, and compile host plus shader generator. Confirm the final diff accounts for all four PR files and that the unselected/native build has no route change. Confirm the patch identity changes when the upstream head or current pin changes.

Run the existing source/external-source and packaged-patch gates, then pristine materialization and patch rebase checking. Confirm exactly the four PR files are accounted for, the patch applies to the current pin, no unrelated HIP patches enter the dependency closure, and the unselected/native build has no behavioral route change.

Initial validation is static/source-level only: `bigcherry check`, `patch-lint`, source/external-source checks, pristine materialization, and compile/application verification. Do not call this patch benched or validated. When entering validation, first bind exactly one CM1 Experiment Contract, then add the required package contents and execute the real validation campaign; `patch-validate` only verifies existing evidence.

## Effort & Risk

M/medium. Main risks are moving upstream head, current-pin drift, and accidentally omitting coupled host/generator hunks.

## Standards

Use immutable external-source provenance, packaged patch identity, current-pin comparison, backend-aware audit, and fail-closed applicability.

## Acceptance Criteria

[ ] Immutable head/base/current-pin provenance recorded. [ ] Final-state diff against current pin is fully accounted for. [ ] Patch materializes and compiles cleanly. [ ] No default route or production replay behavior changes. [ ] Missing or stale upstream provenance blocks application.

[ ] Head/base/current pin are immutable and recorded. [ ] Final-state semantic delta is audited against the current pin. [ ] Packaged patch applies cleanly in pristine materialization. [ ] `patch-lint`/`patch-validate`/source checks pass. [ ] No default CM1 enablement or production replay behavior changes. [ ] Moving PR head blocks reuse of the snapshot.

[ ] Package-only production layout is used with directory ID equal to patch ID. [ ] Initial untested capture has no fabricated validation evidence. [ ] Validation-ready transition requires one resolved Experiment Contract, README.md, validation.toml, and evidence/validation.json. [ ] Large logs remain outside the patch package. [ ] Current-pin evidence is verified by resolved SHA, not tag alone.

## Notes

This is the source-capture item for PR #27952. It must not depend on RO18 solely because of numbering; it depends on RE30 stock/audit readiness.

Use the final-state semantic delta as implementation input and retain the 50-commit series only as provenance/debugging context. Do not make RO19 depend on RO18 merely because of numeric ordering.

RV93 correction: source capture and validation qualification are separate lifecycle steps. A clean apply/build result proves neither the Experiment Contract's correctness checks nor its performance claim.

## Change Log

- 2026-09-05T00:32:58.162809+00:00 (created-by): Created by agent

## Ledger-events


- chg_20260905_003358_reviewed-llamacpp-pr-27952-w_9029
- 2026-09-05T00:33:58.872287+00:00 (updated-by): Updated: section:ledger-events
- 2026-09-05T00:54:17.595658+00:00 (updated-by): Updated: section:steps, section:detailed_solution, section:code_samples, section:files, section:validation, section:acceptance_criteria, section:notes
- chg_20260905_005440_added-the-full-executable-impl_4324
- 2026-09-05T00:54:40.026416+00:00 (updated-by): Updated: section:ledger-events
- 2026-09-05T00:58:44.550123+00:00 (updated-by): Updated: section:detailed_solution, section:files, section:validation, section:acceptance_criteria, section:notes
- chg_20260905_005911_aligned-the-ro19-ro22-roadmap_2443
- 2026-09-05T00:59:11.845661+00:00 (updated-by): Updated: section:ledger-events
