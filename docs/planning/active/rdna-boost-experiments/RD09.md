---
id: RD09
order: 9
plan: rdna-boost-experiments
state: in_progress
created-at: '2026-08-15T03:36:14.785393+00:00'
breadth: ''
skill: advanced
created-by: agent
work: L
---

# Implement per-graph Q8_1 activation cache foundation

## Description

Future foundational Q8_1 activation cache experiment from ff6fde5, explicitly overlapping HI26 and serving as the only base for its dependent Q8 children.

## Steps

1. Define generation and graph lifetime. 2. Design view-root, dimensions, strides, stream, and device key. 3. Define arena ownership and invalidation. 4. Add capture and multi-GPU/tensor-split behavior. 5. Measure hit/miss and quantize-launch reduction without stale pointers.

## Detailed Solution & Technical Design

Use a bounded execution-scoped cache with explicit ownership and generation invalidation. Cache identity must include pointer/view and all shape/stride/device/stream dimensions needed to prevent false hits. Keep the native standalone quantizer as a correctness reference.

## Code Samples & Guidance



## Files

Future HIP cache/runtime sources; RD09 tests/evidence; HI26 cross-link

## Validation

Graph lifetime, invalidation, view-root key, dimensions/strides, stream, arena/device ownership, graph capture, multi-GPU/tensor split, pointer reuse, hit/miss, launch reduction, bit-identical blocks, memory peak.

## Effort & Risk

High runtime/memory risk; stale cache entries could corrupt outputs.

## Standards

Bounded cache, generation safety, no stale pointers, exact correctness reference.

## Acceptance Criteria



## Notes

Planning-only. Depends on RD02/RD03. HI26 remains open; children RD10–RD12 require causal comparisons.

Planning-only until this session. Depends on RD02/RD03 (both completed). HI26 remains open. RD14 is superseded by RD24 (no longer a live child); RD12 is already materialized/in-progress and co-tenants common.cuh/ggml-cuda.cu/mmvq.cu -- RD09 must use narrow independent anchors and not absorb RD12's fusion work.

**DESIGN CONVERGED 2026-08-24 (dev-gpt-agent, session ses_76b0fef0c94c434a, req_60a41664e0de43d6). Verdict: AGREE, with 2 required adaptations.**

**Source identity (verified, not from the old truncated hash):** the plan item's original 'ff6fde5' resolves to the full commit `ff6fde5046ffb86672e05da640d2bfb20d4bfdfc` ("CUDA: cache quantized Q8_1 matmul inputs per graph") -- a much smaller change than previously assumed: 89 additions / 7 deletions across exactly 3 files (common.cuh, ggml-cuda.cu, mmvq.cu), not +464/+324 across two files. BigCherry's active stew675 snapshot is v2 (all 23 logical changes patch-id verified content-identical to v1), so the port freezes against the REBASED commit `299f6eaf73b5eeb888bd94eaa66122d003136e6a` (original=ff6fde5046ff...), not the stale short hash.

**Fork mechanism:** cache storage lives on ggml_backend_cuda_context; ggml_backend_cuda_graph_compute() clears the logical cache at the start of every graph execution; ggml_cuda_mul_mat_vec_q() replaces its per-call ggml_cuda_pool_alloc + quantize_row_q8_1_cuda with a cache lookup, quantizing only on a miss.

**Real correctness weakness found in the fork (must NOT be reproduced):** its cache key uses view-root pointer + shape/stride/stream but NOT the actual view data address/offset -- two views of the same root with identical shape/strides but different offsets can collide. This is exactly the false-hit risk this item's own risk statement warns about. Fix: add the exact view data address/offset into the key (see key struct below).

**Second fork choice to reject:** its cache arena is relocatable (grows by allocating new + copying + freeing old), which conflicts with HIP/CUDA graph-capture lifetime (captured buffer addresses must stay stable once a graph is recorded). Adaptation: use stable retained slabs (survive until backend-context destruction; logical entries/bump positions reset each generation), hard byte+entry cap, native-path fallback on exhaustion, never grow while capture is active.

**Integration point:** at MMVQ materialization inside ggml_cuda_mul_mat_vec_q() -- NOT the 0200_dispatch_hook.py layer, and NOT a graph rewrite. 0200 intercepts selection at ggml_cuda_mul_mat; 0650_mmvq_native_variant threads forced geometry back down through the native ggml_cuda_mul_mat_vec_q() path, so native and BigCherry-forced MMVQ variants already converge at the exact seam RD09 needs -- 0200 stays completely unaware of RD09.

**Cache key** (strengthened vs fork): { generation (uint64), view_root (tensor identity ptr), data (exact view byte start -- the fork's missing field), stream_no, ne0/ne0_padded/ne1/ne2/ne3, s1/s2/s3 }. Device is NOT in the key (cache is owned by one ggml_backend_cuda_context already); keep device id as diagnostic/assertion metadata only. Stream stays in the key -- no cross-stream reuse.

**API shape:** separate find/reserve/publish (a miss must not become a visible cache entry before the producer kernel is at least enqueued) -- `find(key)` -> on miss `reserve(bytes)` -> on success `quantize_row_q8_1_cuda(...)` then `publish(key, reserved)`; on reserve failure, fall back to the exact current native allocation/quantizer path. This reserve->launch->publish shape is deliberately the same API RD10/RD11/RD24 will need for their own direct-Q8 producers.

**Validation approach:** the canonical correctness reference stays quantize_row_q8_1_cuda() itself (deterministic on this pin; type_src0 arg is unused). A `verify` mode does an expensive independent re-quantize + full byte-compare on every cache hit -- final-logit equality alone is explicitly called out as too weak for this foundation. Required correctness matrix before any performance work: same-tensor-twice->hit+exact bytes; same-root-different-offset->MUST miss (non-negotiable -- proves the fork's key weakness is fixed); dimension/stride change->miss; different stream->miss; new generation->miss; address reused by a different tensor in the same generation->miss; capacity exhausted->native fallback with exact output; dual-GPU/tensor-split->per-backend cache isolation; direct-producer prefill->exact match vs standalone quantizer. Graph-capture validation is a SEPARATE, later step: exhaust key correctness with graphs disabled first, only then enable graphs (no synchronous byte verification inside capture) and confirm warm-up/capture/replay/stable-addresses/output-parity/no-slab-growth-during-capture.

**Env gating:** `GGML_HIP_Q8_1_CACHE_MODE=off|on|verify` (default off; off must be byte-for-byte the current unmodified path), plus `GGML_HIP_Q8_1_CACHE_STATS=1` for counters (lookups/hits/misses/quantize-launches-saved/capacity-bypasses/capture-capacity-bypasses/bytes/high-water/generations/verify-mismatches). Deliberately kept INDEPENDENT of GGML_HIP_DISPATCH_MODE (native/record/tune/replay) -- RD09 is an activation-materialization optimization underneath MMVQ that must behave identically regardless of how MMVQ geometry was chosen; coupling the two would poison the native control.

**Staged implementation plan (7 stages, each with an exit gate):**
0. Freeze source/design -- provenance = v2 299f6eaf.../original ff6fde504.../ strengthened key + stable-slab adaptations documented. Exit: no unresolved source identity.
1. Cache foundation -- context-owned generation, key builder, stable bounded slab ownership, find/reserve/publish, counters; NO graph rewrite. Exit: host/source-contract tests pass; off mode is zero behavioral change.
2. MMVQ integration -- replace only the current local Q8_1 materialization block in ggml_cuda_mul_mat_vec_q(); preserve the 0600/0650 launch chain untouched. Exit: native + forced MMVQ both use the same cache seam.
3. Adversarial correctness -- verify mode + the full key matrix above, especially view-offset collision + pointer reuse + generation boundary. Exit: zero Q8 block mismatches.
4. Capture/topology -- graph warm-up/capture/replay; dual-XTX tensor-split; multiple streams if a concurrent graph path exercises them. Exit: stable pointers, no capture-time allocation, exact outputs.
5. Causal bench -- same binary, cache off vs on, count actual Q8 quantizer launches plus TG/PP/memory. Exit: demonstrated launch reduction, no material memory/perf regression.
6. Foundation closure -- update source status/evidence; leave dependent children (RD10/RD11/RD15/RD24) as separate items that may now consume the proven cache API.

First implementation slice: isolated rd09-only experiment (STATE=untested, GGML_HIP_Q8_1_CACHE_MODE defaults off), no change to 0100_cmake_options or the production build surface -- gives a same-binary A/B (cache off vs on) exactly like RD19's methodology.

Planning-only. Depends on RD02/RD03 (both completed). HI26 remains open. RD14 is superseded by RD24 (no longer a live child); RD12 is already materialized/in-progress and co-tenants common.cuh/ggml-cuda.cu/mmvq.cu -- RD09 must use narrow independent anchors and not absorb RD12's fusion work.

**DESIGN CONVERGED 2026-08-24 (dev-gpt-agent, session ses_76b0fef0c94c434a, req_60a41664e0de43d6). Verdict: AGREE, with 2 required adaptations.**

**Source identity (verified, not from the old truncated hash):** the plan item's original 'ff6fde5' resolves to the full commit `ff6fde5046ffb86672e05da640d2bfb20d4bfdfc` ("CUDA: cache quantized Q8_1 matmul inputs per graph") -- a much smaller change than previously assumed: 89 additions / 7 deletions across exactly 3 files (common.cuh, ggml-cuda.cu, mmvq.cu), not +464/+324 across two files. BigCherry's active stew675 snapshot is v2 (all 23 logical changes patch-id verified content-identical to v1), so the port freezes against the REBASED commit `299f6eaf73b5eeb888bd94eaa66122d003136e6a` (original=ff6fde5046ff...), not the stale short hash.

**Fork mechanism:** cache storage lives on ggml_backend_cuda_context; ggml_backend_cuda_graph_compute() clears the logical cache at the start of every graph execution; ggml_cuda_mul_mat_vec_q() replaces its per-call ggml_cuda_pool_alloc + quantize_row_q8_1_cuda with a cache lookup, quantizing only on a miss.

**Real correctness weakness found in the fork (must NOT be reproduced):** its cache key uses view-root pointer + shape/stride/stream but NOT the actual view data address/offset -- two views of the same root with identical shape/strides but different offsets can collide. Fix: add the exact view data address/offset into the key.

**Second fork choice rejected:** its cache arena is relocatable (grows by allocating new + copying + freeing old), which conflicts with HIP/CUDA graph-capture lifetime. Adaptation: stable retained slabs, hard byte+entry cap, native-path fallback on exhaustion, never grow while capture is active.

**Integration point:** at MMVQ materialization inside ggml_cuda_mul_mat_vec_q() -- NOT the 0200_dispatch_hook.py layer, and NOT a graph rewrite.

**Staged implementation plan (7 stages):** 0 freeze source/design, 1 cache foundation, 2 MMVQ integration, 3 adversarial correctness, 4 capture/topology, 5 causal bench, 6 foundation closure.

---

**STAGE 1 (cache foundation) CLOSED 2026-08-24.**

Implemented: src/ggml/src/ggml-cuda/hip-q81-cache.{h,cpp} (generation-scoped find/reserve/publish, stable never-relocated slabs, GGML_HIP_Q8_1_CACHE_MODE=off|on|verify default off, GGML_HIP_Q8_1_CACHE_STATS=1 counters) + patches/1235_rd09_q81_activation_cache_foundation.py (GROUP=rdna-boosts, STATE=untested; 3 anchored edits: ggml/src/ggml-hip/CMakeLists.txt source-list addition, ggml/src/ggml-cuda/common.cuh opaque `void * q81_cache` member on ggml_backend_cuda_context, ggml/src/ggml-cuda/ggml-cuda.cu include + destructor teardown call). No caller in mmvq.cu yet -- entirely inert by construction. 30 source-contract tests (tools/tests/test_rd09_q81_cache_foundation.py).

**dev-gpt-agent code review** (session ses_76b0fef0c94c434a): first pass (req_9bd7db08f19d4e53) on commit 4827a4fea70e2509a51c82326c037fbff84c4a58 found 2 real P1 bugs before stage 2 could safely build on this: (1) reserve() grew immediately on a slab-boundary miss instead of walking existing later slabs first, which could return an overlapping pointer -- fixed by making reserve() loop through existing slabs (via a new slab_base() helper) before ever calling hipMalloc; (2) the cache was a device-global singleton (std::unordered_map<int, unique_ptr<cache>>), weaker than the context-scoped lifetime the design called for, since upstream doesn't guarantee one ggml_backend_cuda_context per device -- fixed by moving ownership onto the context itself via the opaque q81_cache member, lazily constructed via ggml_hip_q81_cache_for_context(ctx) and torn down via ggml_hip_q81_cache_destroy_for_context(ctx) called from the context's own destructor. Also added per the review: ggml_hip_q81_cache_make_key() (stage 2 never hand-assembles cache identity), an explicit 4096-entry cap in publish() (P2), and current_bytes now resets in begin_generation() (P2). Fixes landed in commit 4f33d4d3f21f4b186636690403885c11581e87d9.

Second review pass (req_c56ae6af774f4a2c) on the fix commit: **PASS** -- "the two P1 findings are fixed, and the foundation is now suitable for stage 2... The allocator fix is correct... The ownership fix is also the right one... I would close RD09 stage 1 at the design/code-review level." One closure gate specified: "I don't see evidence... of an actual materialized HIP compile of patch 1235... Stage 1 PASS once the isolated core + rdna-boosts/1235 HIP build compiles successfully. No runtime benchmark is required."

**Real isolated HIP compile confirmation** (Brutus, gfx1100, `bigcherry build --lane bigcherry-native:control:linux-multi --experiment rd09-only`, added via [experiment.rd09-only] in config/recipes.toml, commit 37752de290c1615865432b0b1e2e3d2579a63685): first attempt caught 2 [[nodiscard]] warnings on ignored hipError_t return values from hipFree (destructor + reset_for_test) -- fixed across 2 commits (21103d58440f57519a0fd923211e440f81817606, then 652d62eb02f76afd9b45e217065abb1763e2501a after the first commit's replace_all missed the second call site due to differing indentation). Final rebuild: `ok build_plan_id=c0c1dd2b456c16e7b6a09200fd674cbd`, zero warnings, zero errors -- hip-q81-cache.cpp compiles and links cleanly alongside the patched common.cuh/ggml-cuda.cu.

**STAGE 1 IS CLOSED.** Per gpt's PASS verdict, this is a sound foundation for stage 2, with one explicit scope correction: stage 2 must wire BOTH (a) ggml_cuda_mul_mat_vec_q() cache integration in mmvq.cu AND (b) ggml_hip_q81_cache_begin_generation() called at graph-evaluation entry in ggml_backend_cuda_graph_compute() -- these are not separable, since without (b) cached entries could survive across graph evaluations and become a real stale-activation bug even with (a) done correctly. gpt also flagged: wire the existing capture_active API as part of stage 2 (even though hardware capture validation stays stage 4), since GGML_HIP_Q8_1_CACHE_MODE=on with normal graph capture enabled could otherwise allocate during capture before stage 4 exists to catch it. RD12 (in_progress, touches the same 3 files in unrelated fusion-argument regions) does not anchor-collide with RD09's stage 1 additions; stage 2 must keep to the shared activation-materialization seam only.

First implementation slice used: isolated rd09-only experiment (STATE=untested, GGML_HIP_Q8_1_CACHE_MODE defaults off), no change to 0100_cmake_options or the production build surface -- gives a same-binary A/B (cache off vs on) exactly like RD19's methodology, to be exercised once stage 2 lands.

## Change Log

- 2026-08-15T03:36:14.785393+00:00 (created-by): Created by agent

## Ledger-events


- chg_20260815_033735_added-a-complete-evidence-gat_9636
- 2026-08-15T03:37:35.452139+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-23T23:27:33.238953+00:00 (state-transition): State: pending → in_progress
- 2026-08-23T23:28:06.249856+00:00 (updated-by): Updated: section:notes
- chg_20260823_235230_started-building-a-shared-cach_5100
- 2026-08-23T23:52:30.204347+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-24T00:45:47.502895+00:00 (updated-by): Updated: section:notes
- chg_20260824_004606_closed-out-the-first-phase-of_1574
- 2026-08-24T00:46:06.638075+00:00 (updated-by): Updated: section:ledger-events
