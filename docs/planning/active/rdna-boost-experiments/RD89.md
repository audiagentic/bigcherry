---
id: RD89
order: 0
plan: rdna-boost-experiments
state: pending
created-at: '2026-08-20T06:17:45.867430+00:00'
breadth: ''
skill: basic
created-by: agent
work: S
---

# RDNA experiment backlog: priority wave, model/workload matrix, and boundary-sweep vocabulary

## Description

Cross-cutting reference content from docs/reference/BigCherry_External_AMD_RDNA_Experiment_Backlog.md that is NOT itself a work item -- the shared priority ranking, model/workload qualification matrix, boundary-sweep vocabulary, and delivery rule that RD28-RD88 (and EC13-EC15) all draw from. Filed as its own item so this context isn't lost or duplicated across 64 individual items.

## Steps



## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files



## Validation

This item has no acceptance criteria of its own -- it is reference content consumed by RD28-RD88 and EC13-EC15's own validation sections. Reconciliation audit 2026-09-08: full 105-item RD-namespace pagination cross-referenced against patches/ directories and external-sources.toml's fork registry (no gaps, 5/5 forks accounted for at the time, nasone fork found missing and registered same day); ~24 implemented, ~26 resolved/rejected/no-patch-needed, ~51 genuinely pending, 6 meta/infra. Broader plan-group audit (GP/HI-collectives/NRO/EX/KC/RO/HTR/TO + hip-autotune title scan) found no other candidate gaps beyond NRO (fork registration fixed) and HI25/HI26 (cross-referenced, correctly filed as internal designs, not renumbered).

## Effort & Risk



## Standards



## Acceptance Criteria



## Notes

Source: docs/reference/BigCherry_External_AMD_RDNA_Experiment_Backlog.md §7, §8, §9, §11. Filed as part of the 65-item backlog batch (2026-08-20). Real prerequisite gap found and tracked across the 8 FORK-* items (RD69-RD76): external-sources.toml has zero registry entry for MrLordCat/llama.cpp-with-GUI as of this filing -- register it (fork/branch as locator only, immutable commit SHA as identity) before starting any FORK-* port work.

Source: docs/reference/BigCherry_External_AMD_RDNA_Experiment_Backlog.md §7, §8, §9, §11. Filed as part of the 65-item backlog batch (2026-08-20). Real prerequisite gap found and tracked across the 8 FORK-* items (RD69-RD76): external-sources.toml has zero registry entry for MrLordCat/llama.cpp-with-GUI as of this filing -- register it (fork/branch as locator only, immutable commit SHA as identity) before starting any FORK-* port work.

### 2026-09-08 RECONCILIATION (full 105-item RD-namespace audit)

The P0/P1/P2 priority wave below is from 2026-08-20 and is now substantially resolved. Reconciled status, same row order as the original table:

- RD29 (P0): superseded, closed (no patch needed -- reason recorded on the item itself).
- RD30 (P0): IMPLEMENTED -- patch 1237_rd30_moe_mmq_compact_grid. Real converged evidence: correctness clean (native+tune dispatch, 2 quant types, hostile-routing 0/5 fail at n_expert=256 scale), real interleaved production-model timing +0.7-0.9% pp512/pp2048, decode unaffected as expected. State corrected pending->in_progress today (was mislabeled despite complete evidence). Remaining: promotion judgment call only (modest gain vs added code complexity), not further hardware work.
- RD54 (P0): real hardware evidence gathered, explicitly NOT ported (inconclusive at scale) -- closed, not a gap.
- PRBE46 (P0): still genuinely open, no patch. Real next candidate.
- RD87 (P0): in_progress, no patch yet. Real next candidate, lowest risk (vendor-library headroom probe, not a new kernel).
- PRBE29/RD38 (P1): still pending, no patch -- high value but VRAM-heavy, real remaining candidates.
- PRBE24/RD32 (P1): still pending -- their prerequisite (RD30's compacted-grid mechanism) is now DONE, so these are newly unblocked, not blocked as originally filed.
- PRBE37/RD46/RD47 (P1): still pending, no patch -- small cumulative GEMV epilogue fusions, real remaining candidates.
- PRBE52 (P1): still pending, no patch -- directly overlaps this session's live HI171/HI130 dual-XTX MTP tuning work; already has dual-R9700 evidence per this item's own §7 sourcing. Worth prioritizing given active adjacent work.
- RD70/RD74 (P1): completed.
- PRBE42-53 (P2): RD50 (the base GDN kernel) is now DONE (patch 1221); PRBE43/52/53 are direct extensions of it, newly unblocked, still pending with no patch.
- PRBE48, RD81, RD88 (P2): still pending, research/strategic, no change.

NOT in the original 2026-08-20 wave, filed later, real open candidates: RD98 (Q4_K MMVQ wide-load/VDR family -- closest sibling to RD08, its own spec already encodes the correctness lesson RD08 just taught: reassociation changes bits even when NMSE matches, so its acceptance bar should be numerical-equivalence from the start, not bit-identical), RD101 (BridgeSpec third-party MTP sidecar architecture, not yet started), RD104 (Vulkan tensor-parallel AllReduce, upstream-tracking, not a kernel port).

Tracker hygiene also corrected today: RD33 state pending->in_progress (real converged null-result evidence plus an explicit, cheap, not-yet-done follow-up -- extend its ncols_dst==1 eligibility gate to cover ncols=5/6, the actual dominant MTP speculative-verify shape on real production traffic, per its own 2026-09-04 rocprof finding that the original gate almost never fired on the benched workload). RD08 closed 2026-09-08 (real qualification failure, twice independently confirmed on live hardware).

Reconciled top-3 candidates for immediate next work, given this session's context (RD08 just closed, VA26 execution pipeline freshly proven on real hardware, active MTP/dual-XTX tuning underway in HI171): RD87 (lowest risk, foundational), RD62 (directly relevant to concurrent MTP work, has prior evidence), RD98 (closest technical sibling to RD08, already correctly scoped around the lesson just learned).

Source: docs/reference/BigCherry_External_AMD_RDNA_Experiment_Backlog.md §7, §8, §9, §11. Filed as part of the 65-item backlog batch (2026-08-20). Real prerequisite gap found and tracked across the 8 FORK-* items (RD69-RD76): external-sources.toml has zero registry entry for MrLordCat/llama.cpp-with-GUI as of this filing -- register it (fork/branch as locator only, immutable commit SHA as identity) before starting any FORK-* port work.

### 2026-09-08 RECONCILIATION (full 105-item RD-namespace audit)

The P0/P1/P2 priority wave below is from 2026-08-20 and is now substantially resolved. Reconciled status, same row order as the original table:

- RD29 (P0): superseded, closed (no patch needed -- reason recorded on the item itself).
- RD30 (P0): IMPLEMENTED -- patch 1237_rd30_moe_mmq_compact_grid. Real converged evidence: correctness clean (native+tune dispatch, 2 quant types, hostile-routing 0/5 fail at n_expert=256 scale), real interleaved production-model timing +0.7-0.9% pp512/pp2048, decode unaffected as expected. State corrected pending->in_progress today (was mislabeled despite complete evidence). Remaining: promotion judgment call only (modest gain vs added code complexity), not further hardware work.
- RD54 (P0): real hardware evidence gathered, explicitly NOT ported (inconclusive at scale) -- closed, not a gap.
- PRBE46 (P0): still genuinely open, no patch. Real next candidate.
- RD87 (P0): in_progress, no patch yet. Real next candidate, lowest risk (vendor-library headroom probe, not a new kernel).
- RD36/PRBE31 (P1): still pending, no patch -- high value but VRAM-heavy, real remaining candidates.
- PRBE24/PRBE25 (P1): still pending -- their prerequisite (RD30's compacted-grid mechanism) is now DONE, so these are newly unblocked, not blocked as originally filed.
- RD45/PRBE38/RD47 (P1): still pending, no patch -- small cumulative GEMV epilogue fusions, real remaining candidates.
- RD62 (P1): still pending, no patch -- directly overlaps this session's live THA15/HI130 dual-XTX MTP tuning work; already has dual-R9700 evidence per this item's own §7 sourcing. Worth prioritizing given active adjacent work. DUPLICATE FLAGGED 2026-09-08: nasone-rdna-optimizations/NRO06 proposes the same feature from a different source citation -- see RD62's own notes, reconcile before working either.
- RD70/RD74 (P1): completed.
- RD50-53 (P2): RD50 (the base GDN kernel) is now DONE (patch 1221); PRBE43/52/53 are direct extensions of it, newly unblocked, still pending with no patch.
- RD57, PRBE64, RD88 (P2): still pending, research/strategic, no change.

NOT in the original 2026-08-20 wave, filed later, real open candidates: RD98 (Q4_K MMVQ wide-load/VDR family -- closest sibling to RD08, its own spec already encodes the correctness lesson RD08 just taught: reassociation changes bits even when NMSE matches, so its acceptance bar should be numerical-equivalence from the start, not bit-identical), RD101 (BridgeSpec third-party MTP sidecar architecture, not yet started), RD104 (Vulkan tensor-parallel AllReduce, upstream-tracking, not a kernel port).

Tracker hygiene also corrected today: RD33 state pending->in_progress (real converged null-result evidence plus an explicit, cheap, not-yet-done follow-up -- extend its ncols_dst==1 eligibility gate to cover ncols=5/6, the actual dominant MTP speculative-verify shape on real production traffic, per its own 2026-09-04 rocprof finding that the original gate almost never fired on the benched workload). RD08 closed 2026-09-08 (real qualification failure, twice independently confirmed on live hardware). RD73 demoted 2026-09-09 (HI162: shipped, promoted patch found to be a real production regression by better-controlled evidence -- see docs/planning/completed/hip-autotune/HI162.md).

### 2026-09-08: kernel-candidate work exists OUTSIDE the RD-numbered namespace too

Broader audit (GP/HI-collectives/NRO/EX/KC/RO/HTR/TO plan groups, plus a title-only scan of hip-autotune's 44 items) found:

- **nasone-rdna-optimizations (PNRO01-PNRO15, 16 items, all pending)**, filed 2026-09-08 from a fork (nasone32/llama.cpp-RDNA3-7900xtx-opt) that was NOT registered in external-sources.toml until today (fixed). PNRO03 (P2P transport provider) is tightly coupled to this session's own PGC03/P2P correctness work -- sequence together, don't run independently. NRO16 (dequant-float MMVDQ for Q4_K/Q5_K/Q6_K) is adjacent to RD98 -- evaluate together, same decode-path real estate.
- **HI25/HI26** (hip-autotune plan, in_progress) are real, extensively-designed kernel candidates -- a custom gfx1100 Q8_0xQ8_1 WMMA kernel family (HI25, "cherrypips") and a Q8_1 activation-reuse/K-tile-tuning/fused-gate-up-prefill design (HI26) -- but they are INTERNALLY-DESIGNED (no external fork citation), unlike every RD-numbered item, which is why they live under hip-autotune rather than rdna-boost-experiments. Not a misfiling to fix by renumbering; just worth knowing they exist when picking the next kernel candidate, since RD-numbering alone does not cover them.
- All other groups checked (GP, HI-collectives, EX, KC, RO, HTR, TO) are infrastructure/tracking, not kernel candidates, and clean (no staleness found).

Reconciled top-3 candidates for immediate next work, given this session's context (RD08 just closed, RD73 just demoted, VA26 execution pipeline freshly proven on real hardware, active MTP/dual-XTX tuning underway in HI171): RD87 (lowest risk, foundational), RD62/NRO06 (directly relevant to concurrent MTP work, has prior evidence -- reconcile the duplicate first), RD98 (closest technical sibling to RD08 and RD73's own lessons, already correctly scoped around numerical-equivalence rather than bit-identical).

## Recommended first implementation wave (§7)

| Priority | Experiment | Filed as | Reason |
|---|---|---|---|
| P0 | AMD-Q8-001 | RD29 | Directly targets the Q8 tiny-M/MTP gap and fits existing MMVQ/MMQ candidate machinery |
| P0 | AMD-MOE-001 | PRBE23 | Small selector, potentially large MoE PP payoff, stress-testable with controlled routing |
| P0 | UP-HIP-001 | RD54 | Dynamic MMVQ warps has direct R9700/XTX evidence, may already be discoverable by BigCherry's tuner |
| P0 | UP-VK-001 | PRBE46 | Vulkan B=8/9 cliff easy to reproduce, low-risk to isolate |
| P0 | HIPBLAS-001 | RD87 | Very low implementation risk, establishes vendor-library headroom before new kernels |
| P1 | AMD-GEMM-002/004 | PRBE29/RD38 | High-value Q8 prefill experiment but VRAM-heavy; test after crossover tooling ready |
| P1 | AMD-MOE-002/003 | PRBE24/RD32 | Compact launch after routing benchmark infrastructure (EC13) |
| P1 | AMD-FUS-001/002/003 | PRBE37/RD46/RD47 | Small decode fusions, useful cumulative lane |
| P1 | UP-MTP-001 | PRBE52 | Adaptive MTP already has dual-R9700 evidence |
| P1 | FORK-MGPU-001/002 | RD70/RD74 | Topology/copy placement can dwarf kernel tweaks on non-P2P systems |
| P2 | AMD-GDN-001..004 | PRBE42-PRBE45 | Potentially large but model-specific/invasive |
| P2 | UP-HRX-001 | PRBE48 | Strategic comparative backend lane |
| P2 | VK-ACO-001/CK-001 | PRBE64/RD88 | Research-oracle work after top bottlenecks measured |

## Model/workload qualification matrix (§8)

9 model lanes, each mapped to which experiment families it serves as positive/control: Qwen3.6-27B Q8_0+MTP, Qwen3.6-27B Q4_K/Q6_K, Qwen3.6-35B-A3B Q8_0, Qwen3.6-35B-A3B Q4_K_M, Qwen hybrid/GDN, Llama 3.x 8B Q8_0, alternative MoE Gemma/DeepSeek, DeepSeek-V4 Flash, small Qwen 0.5B-4B. See the source doc §8 for the full per-model positive/control mapping table.

## Delivery rule (§11)

"Before implementing any item, recheck current upstream status and the BigCherry pin. If the exact change is already ancestral, record it as a baseline and do not create a patch. If a source PR is compound, port only one logical hypothesis at a time unless the source proves the pieces are inseparable for correctness. Update external-sources.toml with immutable identities and create the Experiment Contract before benchmarking."

## Common boundary sweeps (§9) -- shared vocabulary across all 64 filed items

- MMQ/MMVQ physical M: 1,2,3,4,8,16,24,32,48,64,128,256,512
- Prefill 128,512,1K,4K,16K,64K+ x ubatch 128..4096
- FA q_rows 1,3,8,128,512,2048 x KV depth 0..240K
- MTP width 1,2,3,4,5,6,8,10 x content class
- MoE natural + uniform/Zipf/skew/single-hot routing
- Multi-GPU 1/2/3 GPU x device order x split mode x P2P
- **Driver/ICD version is part of evidence identity -- do not compare RADV and proprietary results as one population** (see EC15/VK-DRV-001)

## Change Log

- 2026-08-20T06:17:45.867430+00:00 (created-by): Created by agent
- 2026-09-08T17:30:54.357688+00:00 (updated-by): Updated: section:validation, section:notes
- 2026-09-08T17:47:17.872304+00:00 (updated-by): Updated: section:validation, section:notes
