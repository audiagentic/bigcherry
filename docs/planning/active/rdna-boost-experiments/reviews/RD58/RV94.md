---
id: RV94
review-of: RD58
plan: rdna-boost-experiments
state: created
reviewed-by: claude-opus-5
reviewed-at: ''
---

# RD58's control lane was its own treatment lane -- its no-regression claim was never tested

## Notes

Related: gpt-dev-agent (req_6eea1c280d824cee) separately froze the effect-evidence outlier policy in the same session -- no statistical outlier handling; validity predicates only for protocol failure, never thresholds on the measured ratio.

## Findings

RD58 was marked `completed` with `positive` and `controls` both set to models=["tierA-qwen4b-q6k"], workloads=["state_restore"] -- byte-identical sets. Since an EvaluationSet lane is identified solely by (model, workload), RD58's control WAS its treatment. Its `max_control_regression_pct = 1` budget was therefore evaluated against the very measurement it was supposed to hold constant, and could not have detected collateral damage from pinning the state buffer.

This was found by a new per-lane overlap check added to parse_contract (commit 0fd94601), which flagged 4 of 19 registered contracts: RD06, RD21, RD44 and RD58. The other three are pending/in_progress; RD58 is the only one already completed.

## Conclusion

The contract's control lane has been corrected to `decode` on the same model -- pinning the state buffer changes the host allocation path, so steady-state decode is the lane that could pay for the fix without being claimed by it.

However, RD58's RECORDED evidence measured the old (vacuous) control. The correctness result RD58 was accepted for is unaffected -- the ROCm SDMA fault fix either works or does not, and that was verified independently. What is NOT established is RD58's no-regression claim: no lane outside the fix was ever measured. Anyone relying on 'RD58 showed no regression' should treat that as unmeasured rather than as a passed check.

Deliberately NOT re-running RD58 to manufacture the missing control number: the correctness fix is already in, and re-qualifying it now would be scope this finding does not justify. The honest state is recorded instead.
