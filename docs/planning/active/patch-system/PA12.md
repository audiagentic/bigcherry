---
id: PA12
order: 0
plan: patch-system
state: pending
created-at: '2026-08-26T07:26:31.227751+00:00'
breadth: ''
skill: intermediate
created-by: agent
work: S
---

# Source/patch identity hardening: atomic metadata writes + cross-process plan locking (L6.1/L6.2)

## Description

Review found canonical campaign metadata (campaign/build.py::materialize_source()) uses plain write_text() for its metadata, while HI82 (patch/source.py) already uses temp-file + flush + fsync + os.replace(). A crash mid-write can leave truncated canonical-campaign metadata beside an otherwise-valid source worktree. Separately, two processes computing the same materialization plan can race to create/use the same worktree/cache record; current behavior is described as 'largely fail-closed but can create false corruption/partial-state failures' rather than cleanly handling the race. NOT yet independently verified against current source this session.

## Steps

1. Extract HI82's existing atomic-write helper into a shared utility (atomic_write_json(path, document, fsync_file=True, fsync_parent=True) per review's suggested signature) and have campaign/build.py's materialize_source() use it too, instead of maintaining two implementations.
2. Add plan-scoped locking: <work-root>/locks/source-<plan-id>.lock, held across inspect/reverify-cache -> create-worktree-if-needed -> apply-composition -> attest-output -> atomically-publish-metadata. Must be process-safe, auto-release on process exit, have an explicit timeout/error (never hang forever), and avoid fixed Path.parents[N] assumptions.

## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files

tools/bigcherry/campaign/build.py, tools/bigcherry/patch/source.py (extract shared atomic-write helper from here), a new shared location for the helper + lock utility

## Validation

L6.1: fault-inject a write interruption, prove the previous valid metadata survives untouched. L6.2: two concurrent requests for the same plan produce exactly one valid materialization result and no partial/corrupted state.

## Effort & Risk

Low risk for L6.1 (mechanical extraction of an existing working pattern). L6.2 locking needs a real cross-platform (Windows + POSIX) file-lock implementation with timeout semantics -- check whether one already exists elsewhere in this codebase before writing a new one.

## Standards



## Acceptance Criteria



## Notes

P2 per review -- lowest priority of this set, but cheap to do (mostly extracting an existing, already-correct HI82 pattern to a second call site) and removes one of the duplicated-authority rows in the review's Layer 7 comparison table.

STATUS: L6.1 (atomic_write_json shared helper) done and wired into both patch/source.py's _write_manifest() and campaign/build.py's materialize_source() metadata write -- committed. L6.2 (cross-process plan-scoped locking) deliberately deferred: this codebase has no existing cross-platform (Windows+POSIX) file-lock-with-timeout primitive to build on, and building one correctly (process-safe, auto-release on process exit, explicit timeout, no fixed Path.parents[N] assumptions) is real new infrastructure, not a mechanical extraction like L6.1 was. Given P2 priority (lowest of the whole review) and remaining session budget spent on P0/P1 items, this was consciously scoped out rather than rushed. Remains open for a future pass -- do not mark this item completed until L6.2 lands or a decision is made to close it out of scope.

## Change Log

- 2026-08-26T07:26:31.227751+00:00 (created-by): Created by agent
- 2026-08-26T08:22:56.232439+00:00 (updated-by): Updated: section:notes
