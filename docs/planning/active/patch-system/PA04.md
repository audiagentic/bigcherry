---
id: PA04
order: 4
plan: patch-system
state: pending
created-at: '2026-08-23T23:31:24.809735+00:00'
breadth: ''
skill: advanced
created-by: agent
work: L
---

# Local CI + docs + pilot migrations + acceptance (RS12–RS18)

## Description

Ship the `bigcherry check` local CI, CLI integration, patch template, the four reference docs, and the three pilot migrations (RD12, RD13, RD08) with the full acceptance gauntlet. Keep one flat patch first-class. Normative source of truth: PATCH_REFACTOR_RUNBOOK.md §39-§51, §56-§58, §65-§88, RS12-RS18. Depends on PA02 + PA03.

## Steps

- RS12 tools/bigcherry/check.py + tests: CheckSpec/CheckResult, --quick/--default/--full tiers, --fail-fast, --json PATH (no artifacts by default), exit codes 0/1/2. DEFAULT audit gate is the NON-MUTATING strict source audit (reuse source_audit.audit()/passed(), do not subprocess cmd_audit). Flake policy: no retry-to-green; quarantine with unittest.skip + tracked plan-item, reported as skipped. Exit: §65 tests — aggregate/single+multiple failure/fail-fast/timeout/exception/JSON/exit-codes/tier/deterministic ordering
- RS13 __main__.py: add check/patch-lint/patch-validate without breaking patches/patch-status/patch-explain/patch-graph/patch-verify-evidence
- RS14 patches/_template/ with the exact README do/don't guidance
- RS15 migrate RD12 (1205) flat->package; move activation-marker knowledge from generic campaign into validation.toml. Before deleting the old flat module: apply old + new representations to the same base and require OLD_TREE_HASH == NEW_TREE_HASH (STOP on mismatch, do not rationalize)
- RS16 migrate RD13 (1206) the same way — second trace-marker proof. After both, _TRACE_PROBE_SPECS must be gone
- RS17 migrate RD08 (1204) — the decisive complex test (correctness + performance + VDR attribution). Package owns implementation/MUL_MAT mapping/VDR logic/architecture/docs; the Experiment Contract owns hypothesis/workloads/controls/boundaries/thresholds
- RS18 keep a flat patch (e.g. 1002_hip_unsafe_math_opt_in) and run it through the new registry/campaign — proves simple patches remain first-class
- Acceptance gauntlet: §72 identity A-F, §73 discovery adversarial, §74 local CI, §75-§77 hardware (flat simple; RD12/13; RD08), §79 cross-machine, §80 pin-bump staleness, §82 full regression. Ship the four reference docs (§83), PATCH_REFACTOR_RUNBOOK.md stating it supersedes the flat-only RE41 layout decision
- Verification against the §88 Definition-of-Done table

## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files

- tools/bigcherry/check.py (new)
- tools/bigcherry/__main__.py (check/patch-lint/patch-validate)
- tools/bigcherry/paths.py (PATCH_VALIDATION_EVIDENCE/ARTIFACTS)
- tools/tests/test_check.py (new)
- patches/_template/** (new)
- patches/rd/1205_rd12_*/{patch.toml,patch.py,validation.toml,validation/} (migrated)
- patches/rd/1206_rd13_*/{...} (migrated)
- patches/rd/1204_rd08_*/{...} (migrated)
- docs/reference/PATCH_SYSTEM.md, PATCH_AUTHORING.md, PATCH_VALIDATION.md, PATCH_REFACTOR_RUNBOOK.md (new)

## Validation

§65 check.py unit tests; §74 acceptance (quick x2 deterministic, default PASS, full PASS, deliberate schema break names exact package/file/problem with other checks continuing, --fail-fast stops, --json parses independently); each migrated patch proves OLD_TREE_HASH == NEW_TREE_HASH before the old module is deleted; §75-§77 hardware acceptance on declared architectures (unavailable required hardware -> BLOCKED, never PASS); §80 pin-bump leaves old evidence stale + requires revalidation with no manual cache deletion; §88 Definition-of-Done table fully green; at least one flat patch validated through the new infrastructure.

## Effort & Risk

L blast radius, advanced. Largest item but mostly integration + migration + acceptance. The migrations are the real proof: RD12/RD13 (trace-marker), RD08 (complex correctness + performance), and a deliberately-kept-flat patch. Hardware acceptance is the longest pole and is environment-specific — it must not be faked or converted to PASS when hardware is unavailable.

## Standards



## Acceptance Criteria



## Notes

Hardware validation is explicit (patch-validate) and environment-specific — never let bigcherry check implicitly compile ROCm or launch model campaigns (runbook §45). Record ledger change events with plan-item-ids=[PA04] after each substantive check-in group, before completing this item.

## Change Log

- 2026-08-23T23:31:24.809735+00:00 (created-by): Created by agent

## Ledger-events

- chg_20260824_084418_added-local-ci-gates-patch-li_1500
- 2026-08-24T08:44:18.030587+00:00 (updated-by): Updated: section:ledger-events
