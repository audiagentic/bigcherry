---
id: RV80
review-of: PA02
plan: patch-system
state: created
reviewed-by: gpt-dev-agent (user relay)
reviewed-at: ''
---

# GPT adversarial PA02 identity review: 6 blockers — exact-composition source isolation, canonical contract hash, topological ordering, loader digest re-check

## Notes

Spec amendments A1–A3 applied to PATCH_REFACTOR_RUNBOOK.md: A1 (registry may import experiment_contract for the canonical contract hash — amends DAG pin N6), A2 (framework version constant in patch_registry, re-exported — confirmed), A3 (descriptor paths registry-root-relative — confirmed). New identity payload: bigcherry-patch-source-v2 = sha256({schema, resolved_revision, overlay_digest, composition:[(patch_id, impl_digest)]}) where composition is the EXACT ORDERED list as returned by patchset.resolve_exact() — NEVER lexicographically re-sorted (deep-review HIGH defect: a sort could give two different application orders the same key when a packaged patch.toml changes requires/order with unchanged patch.py digests) — state-independent, overlay-inclusive, SHA-anchored. New APIs: patchset.topological_order, psi.resolve_base_revision, psi.materialize_composition, psi.composition_digest. materialize_source (implicit baseline) removed; campaign + rd08 VDR script pass explicit named compositions.

## Findings

1. B1 — framework_baseline_digest() and _apply_baseline_and_stack() scan state=='validated'; identity/baseline must come from the explicitly resolved named composition (recipes [patch-set.*] for source bigcherry), never a lifecycle-state scan.
2. B2 — source identity payload (bigcherry-patch-source-v1) lacks the source-overlay digest and used raw mutable base-revision strings; overlay changes and ref movement were invisible to identity.
3. B3 — load_implementation() executed current file bytes without re-checking them against descriptor.implementation_digest.
4. B4 — expand_composition() re-sorted DFS order by (order, patch_id) and resolve_exact() globally sorted by the same key: both destroy topological order when numbering and REQUIRES disagree (verified at patchset.py:326 and :231).
5. B5 — no static import validation for packaged patch.py before exec (frozen v1 rule: stdlib + bigcherry.patcher only). Live flat tree verified 100% conformant (re, bigcherry.patcher only).
6. B6 — source_key carried the raw base_revision ref string; `git worktree add --detach <ref>` resolved at materialize time, so a moved ref reused the old identity.
Plus: focal plans must run control AND subject through the authoritative exact-composition validator (resolve_exact) — catches reverse conflicts, rejected members, missing requires; validation.toml symlink-escape negative test missing; minor: _validation_identity did not _safe_resolve the manifest path itself.
Fix order per GPT: canonical contract hash -> loader digest re-check -> packaged import restriction -> topological ordering -> exact-composition identity (v2: resolved SHA + overlay digest + composition entries) -> focal strict validation -> path-safety tests -> full suite with 12 required tests.
Provenance note: this review was relayed by the user from the ChatGPT surface (gateway does not hold the artifact); the shallow gateway pass req_dcf8945018ee41a1 said CLEAN — the deeper pass is the governing directive.

## Conclusion

CHANGES REQUIRED. Six identity/source-isolation blockers must be fixed before PA03 work proceeds (RS08–RS11 remain valid as designed; D7/D8/D9 were accepted in the earlier gateway pass req_dcf8945018ee41a1 and are unaffected). All six claims were verified true against the code before fixing.
