---
id: RE14
order: 14
plan: reusable-build-campaign
state: pending
created-at: '2026-08-15T02:30:33.865332+00:00'
breadth: ''
skill: advanced
created-by: agent
work: L
---

# Cut over campaign execution and retire legacy mutable mechanics

## Description

Route normal campaign execution through the new source/build services, prove legacy/new parity, then remove duplicated mutable-checkout assumptions.

## Steps

1. Freeze a parity fixture: one pinned llama.cpp release, exact production patch set, architecture/target, toolchain, inventory/workload, normal campaign shape. Capture legacy path's source state, manifest, descriptor, candidate set, smoke result as reference evidence.
2. Migrate stages behind the new services incrementally (PipelineService/CampaignGraph/CampaignRun/ArtifactStore/BuildPlan/workspace.materialize -- confirmed zero production callers today, only exercised by their own unit tests), but keep normal execution on the legacy path until parity is proven. Each migrated stage must consume explicit identities/artifact refs, not cwd/checkout/prior-stage-build-dir/release-record state.
3. Shadow-run the new path end-to-end against the fixture in a fully separate work root -- must not share checkout, generated directory, CMake build dir, or ArtifactStore namespace with the legacy run.
4. Machine-generated parity comparison (not visual/log diff): source (effective Git tree OID/source-slice ID + exact patch IDs/order/hashes), manifest+descriptor (canonical semantic content, not paths/timestamps), build (BuildPlan ID + effective build ID recompute, expected toolchain, actual binary SHA-256 via validate_reuse() semantics), candidate coverage (exact stable-ID set equality, not just count equality).
5. Runtime parity: at least one real gfx1100 smoke using the artifact resolved from the new descriptor/store (not a manually selected binary path).
6. Negative/fail-closed parity: deliberately tamper each of source/patch-order/inventory-source-slice/published-artifact/descriptor/binary/provenance-namespace/build-dir-reuse/cross-campaign-artifact and confirm every case fails closed before downstream work happens.
7. Flip normal/default execution to the new path with no automatic fallback; legacy path may remain only as an explicit diagnostic/compatibility command.
8. Only then delete legacy mutable-checkout mechanics: tree-state switching (_ensure_tree_state, _reset_tree), the checkout-path-suffixed _build_dir identity, GENERATED_IN_TREE in-checkout generation output, and any tests/CLI code that assume them.

## Detailed Solution & Technical Design

Investigation finding (2026-08-16): every new-service class already exists and is unit-tested but has ZERO production callers anywhere in the repo (checked via grep across tools/bigcherry and docs/scripts) -- PipelineService, CampaignGraph, CampaignRun, ResourceLock, ArtifactStore, BuildPlan, workspace.materialize. 'Normal campaign execution' today is entirely the legacy path in __main__.py's cmd_build, operating on one shared mutable checkout (paths.llama_root()). There is no campaign orchestrator script anywhere; the Brutus campaign-*.sqlite/log artifacts produced this session were built by manually invoking the legacy CLI per build variant (build/bigcherry-tune, build/bigcherry-record, etc).

Root coupling identified (confirmed against source, not assumed): the checkout is used as an implicit inter-stage state bus, not just a mutable git tree. Concretely:
- `_generate_for()`/`GENERATED_IN_TREE` (tools/bigcherry/__main__.py ~L320) writes hip-autotune-registry.inc, manifest, descriptor, mmvq-instances etc DIRECTLY INTO THE SHARED CHECKOUT. A later stage reads these by conventional in-tree path, not by artifact reference -- this is why generate must run immediately before the build that consumes it and why variants must stay sequential (cannot safely share this mutable generated state).
- `_build_dir()` (tools/bigcherry/__main__.py L299) identity is recipe+build name plus a checkout-path hash suffix for non-default roots -- NOT content-addressed by effective source tree + complete build plan. Contrast with the new `builds.build_directory()` = work_root/builds/<source_slice_id>/<build_plan_id>, whose `validate_reuse()` additionally recomputes effective configure record, toolchain identity, and binary SHA-256.
- `_reset_tree()`/`_ensure_tree_state()` flip the ONE shared checkout between recipes rather than materializing an isolated worktree per build (workspace.materialize exists for exactly this and is unused).

Additional integration hazard for the new path itself (not yet built, but must be designed in from the start): PipelineService checks provenance compatibility on ArtifactRef inputs/outputs but does not itself verify ArtifactRef.path bytes match content_hash -- ArtifactStore provides that verification and immutable-publish semantics separately. RE14's executor wiring must make artifact consumption require ArtifactStore-verified bytes, not provenance-field agreement alone. Similarly CampaignRun's reuse hook must validate recorded output hashes/artifacts, not trust StageRecord alone.

Removal criterion (do not delete legacy code before all of this holds): a clean-room pinned campaign through the new path produces source/patch/manifest/descriptor/candidate parity against the legacy reference, a hash-verified real-GPU smoke succeeds, a second execution demonstrates identity-safe reuse/resume from an empty work root, all deliberate mismatch/tamper cases fail closed, and the default campaign path contains no implicit fallback to legacy.

Scope note re: RE15 overlap -- if legacy and new paths both produce the same complete candidate universe (e.g. the same 101 applicable candidates from the RE09 replay-full investigation) and the new binary passes real-GPU smoke, RE14 does not need to wait on RE15's remaining 25-candidate correctness-evidence gap to close -- that gap is acceptance evidence across candidates, whereas RE14 is proving cutover did not alter which candidates exist or how they're identified/built. Only let the missing 25 block RE14 if they reveal an actual difference between the two execution paths.

Advice above obtained via gpt-auto-agent check-in (req_c9a6a4ae428d42ca, session ses_6021f693e5414d17, completed 2026-08-16T01:00 after ~14min), independently verified against the actual source (_generate_for/GENERATED_IN_TREE and _build_dir claims both confirmed correct by direct code read) before being recorded here.

## Code Samples & Guidance



## Files

tools/bigcherry existing recipe/build paths

## Validation

Pinned release parity covers source, manifest, descriptor, candidate coverage, and runtime smoke.

## Effort & Risk



## Standards



## Acceptance Criteria



## Notes

Source: BIGCHERRY_REUSABLE_BUILD_CAMPAIGN_RUN_SHEET_REVISED.md; preserve fail-closed identity and provenance rules.

[... rounds 1-12 preserved above this line (parity gate + loaders, cross-campaign-artifact proof, scripted+real-verified parity harness, real validate_reuse()-based build reuse proven on hardware), see change_log for full history ...]

### Round 13 (2026-08-16): gpt-auto-agent review found a real P0 false-reuse gap in round 12 -- fixed, regression-tested, AND reproduced+closed for real on Brutus in the same round.

User ran the review directly (gateway was intermittently failing turn submission this round -- VAL-GPTAUTO-001 on trivial pings, then repeated EXT-GPTAUTO-004 unresolved-turn loops even after session_close+resume, matching the earlier-seen unrecoverable pattern; a fresh session eventually got through but the user's own manual run landed first) and pasted the full response.

**P0, confirmed against source before fixing, not taken on faith:** BuildPlan has no field for the catalog's candidate architectures (--arch only feeds autotune_catalog.build_manifest via make_generate_worker, never BuildPlan's fields; platform.targets, which DOES feed BuildPlan.targets, controls AMDGPU_TARGETS/what gets compiled FOR, not which candidates get generated). So two generate runs given different --arch values can produce different generated/ catalogs while sharing an identical build_plan_id and therefore build_directory(). Round 12's validate_reuse() call only proved a cached build_dir's OWN recorded identity was self-consistent -- it never compared the cached binary's generated catalog against what the CURRENT run's generate stage actually just produced. Confirmed generated_tree.py's own docstring already states compile_inputs_hash exists specifically to answer "would two otherwise-identical generation runs produce a build that could reuse each other's binary" -- exactly the missing check.

**Fix** (campaign_workers.make_build_worker): fresh-build metadata now records generated_compile_inputs_hash (from the generate stage's generated_tree_document); a cache hit additionally requires this to match the CURRENT run's compile_inputs_hash before accepting reuse. A mismatch is NOT tampering (validate_reuse() already covers that, still fails closed on a build_dir whose own identity doesn't check out) -- it's a legitimate cache miss, so it falls through to a fresh build rather than raising.

**Regression test** (test_campaign_workers_build.py): two builds sharing a build_plan_id but given DIFFERENT generated catalogs (simulating different --arch, via distinct registry.inc content + distinct ArtifactStore paths to avoid immutable-publish collision) now both genuinely recompile -- subprocess.run is not patched to permit a silent second reuse, so an accidental false-positive would fail the test outright.

**Reproduced and closed for real on Brutus, same round:** ran re14_real_run.py twice, SAME source/inventory/upstream-repo (same source_slice_id=7c026aac..., same build_plan_id=555d26a8... -- confirmed identical in both runs' output), but --arch gfx1100 vs --arch gfx1100,gfx1201,gfx1030 (compile-inputs-check-a, compile-inputs-check-b). BOTH runs genuinely recompiled (161 real "Building HIP object" lines each, not a ~2-second reuse skip) -- exactly the fix working as intended: before this round, the second run would have silently reused the first's narrower-catalog binary. Brutus metadata file confirmed generated_compile_inputs_hash present and updated to the second run's value. GPUs confirmed idle before and after.

**Also fixed, same review, real always-active bug (not merely theoretical):** re14_parity_run.py threaded --recipe to the legacy arm only; the new arm's --platform silently defaulted to re14_real_run.py's own default regardless of what platform the requested recipe actually names. run_legacy_arm now returns the recipe's real platform name; main() threads --source/--build/--platform through to the new arm symmetrically.

**Reviewed but deliberately deferred, tracked explicitly rather than silently dropped** (GPT's review, not yet acted on):
- Compiler-override asymmetry (--c-compiler/--cxx-compiler apply to the legacy arm only in re14_parity_run.py) -- real gap, but did NOT affect round 11's actual result (recipes.toml declares no compiler for linux-multi; Brutus's system PATH already resolves cc/c++ to the correct ROCm clang, confirmed via both arms' CMakeCache.txt recording identical resolved paths). Fixing properly requires adding equivalent override flags to re14_real_run.py itself (it has none today).
- Runtime artifact closure: current binary/reuse identity covers only the requested launcher (llama-bench), not libggml-hip.so where round 9 itself found the real dispatch logic lives. A libggml-hip.so change with an unchanged llama-bench launcher could currently pass reuse.
- Toolchain identity is resolved COMPILER PATH only, not compiler content/version -- an in-place ROCm upgrade at the same path would not invalidate the build cache today.
- BuildPlan.environment exists but re14_real_run.py never populates it; build subprocesses inherit the ambient process environment uncontrolled.
- parse_effective_configure()'s prefix list is a reasonable but hand-maintained approximation, not derived from the actual set of -D keys cmake_configure_args supplies (GPT's suggested more-principled alternative).
- validate_reuse() checks that stored build_id round-trips against the STORED effective_configure record (self-consistency), not that build_dir's CURRENT CMakeCache.txt still matches it -- worth making explicit even if not changing behavior.
- Productionisation blocker for the CLI cutover (step 7), not a parity-harness issue: re14_real_run.py publishes inventory at the fixed immutable path inputs/inventory.json; two campaigns with different inventories in the same work root will collide on ArtifactStore's immutability check. Needs inputs/inventory/<sha256>.json before this becomes normal execution.

Full suite after round 13: 700 passed, 1 skipped. ruff clean.

**GPT's recommended ordering for what comes next** (recorded, not yet re-litigated): (1) generated-compile-inputs closure -- DONE this round; (2) runtime-output-closure (HIP .so, not just launcher); (3) toolchain/environment identity closure; (4) parity-gate generality (symmetric selection -- partially done this round; normalized build/effective/toolchain identity instead of blanket-allowing binary_hash); (5) THEN the real Brutus negative/tamper campaign (deliberately after the above, not before -- GPT's explicit point being that a tamper test proves a branch the unit tests already prove, while the structural false-reuse path was the actual gap); (6) production CLI/cutover entrypoint with no hidden legacy fallback; (7) run parity through that real CLI, then flip default; (8) delete legacy mechanics only after a short compatibility window.

## Change Log

- 2026-08-15T02:30:33.865332+00:00 (created-by): Created by agent

## Ledger-events


- chg_20260815_025939_repeated-strict-audits-now-pre_6189
- 2026-08-15T02:59:39.739462+00:00 (updated-by): Updated: section:ledger-events
- chg_20260815_111610_added-a-single-handover-docume_1411
- 2026-08-15T11:16:10.234748+00:00 (updated-by): Updated: section:ledger-events
- chg_20260815_111659_updated-the-handover-to-point_3682
- 2026-08-15T11:16:59.368216+00:00 (updated-by): Updated: section:ledger-events
- chg_20260815_111727_made-the-handovers-commit-ref_4302
- 2026-08-15T11:17:27.506504+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-16T01:01:38.061379+00:00 (updated-by): Updated: section:steps, section:detailed_solution, section:notes
- 2026-08-16T01:19:40.974474+00:00 (updated-by): Updated: section:notes
- chg_20260816_011954_started-building-the-real-impl_6164
- 2026-08-16T01:19:54.183529+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-16T01:28:20.896370+00:00 (updated-by): Updated: section:notes
- chg_20260816_012829_continued-re14-build-campaign_6746
- 2026-08-16T01:28:29.620516+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-16T01:37:15.319321+00:00 (updated-by): Updated: section:notes
- chg_20260816_013723_continued-re14-build-campaign_2193
- 2026-08-16T01:37:23.055765+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-16T01:40:59.634550+00:00 (updated-by): Updated: section:notes
- chg_20260816_014107_finished-the-execution-logic-f_5225
- 2026-08-16T01:41:07.092165+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-16T01:56:06.876928+00:00 (updated-by): Updated: section:notes
- 2026-08-16T01:56:35.311771+00:00 (updated-by): Updated: section:notes
- chg_20260816_015645_major-milestone-on-re14-build_2698
- 2026-08-16T01:56:45.756130+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-16T02:18:38.380512+00:00 (updated-by): Updated: section:notes
- chg_20260816_021859_a-second-pass-code-review-thi_6100
- 2026-08-16T02:18:59.141879+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-16T02:42:48.491188+00:00 (updated-by): Updated: section:notes
- chg_20260816_024300_closed-one-of-the-biggest-rema_6547
- 2026-08-16T02:43:00.410208+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-16T03:47:32.404298+00:00 (updated-by): Updated: section:notes
- chg_20260816_034741_closed-a-real-gap-in-the-build_7365
- 2026-08-16T03:47:41.510578+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-16T04:05:46.611637+00:00 (updated-by): Updated: section:notes
- chg_20260816_040556_answered-the-real-question-beh_6639
- 2026-08-16T04:05:56.744348+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-16T04:39:25.426058+00:00 (updated-by): Updated: section:notes
- chg_20260816_043935_hardened-the-new-build-campaig_3707
- 2026-08-16T04:39:35.572657+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-16T04:54:21.224267+00:00 (updated-by): Updated: section:notes
- chg_20260816_045427_automated-and-proved-on-real_9057
- 2026-08-16T04:54:27.964247+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-16T06:56:50.739393+00:00 (updated-by): Updated: section:notes
- chg_20260816_065657_the-new-build-campaign-path-no_1883
- 2026-08-16T06:56:57.323403+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-16T10:16:32.624730+00:00 (updated-by): Updated: section:notes
- chg_20260816_101641_fixed-a-real-bug-where-the-new_4680
- 2026-08-16T10:16:41.233124+00:00 (updated-by): Updated: section:ledger-events
