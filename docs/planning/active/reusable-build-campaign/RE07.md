---
id: RE07
order: 7
plan: reusable-build-campaign
state: in_progress
created-at: '2026-08-15T02:30:33.776064+00:00'
breadth: ''
skill: advanced
created-by: agent
work: L
---

# Implement content-addressed build plans and effective build identity

## Description

Capture CMake options, environment, toolchain, generated inputs, effective configure state, build IDs, binary hashes, and verified cache reuse.

## Steps



## Detailed Solution & Technical Design

SECOND ROUND (RV48 audit). What already works (verified): source slice, options, target platform, toolchain request, selected environment, effective CMake state, binary SHA and runtime-bundle hash all participate in planning or reuse validation; build dirs are content-addressed by (source_slice_id, build_plan_id); reuse re-hashes the FULL runtime .so closure, not just the launcher.

VERIFIED GAPS: (1) BuildPlan carries separate inventory_hash/winners_hash fields while execution hard-codes SUPPORTED_BUILD_NEEDS = {inventory, promoted-winners} - HI66's candidate-qualification correctness evidence is exactly the case this breaks; do NOT add a third special case. (2) The candidate-generation architecture set is NOT in build_plan_id: campaign_workers deliberately treats a changed generated_compile_inputs_hash as a cache miss INSIDE the same build dir, and the immutable ArtifactStore path is still keyed by that common plan ID - so one 'content-addressed' identity can represent two different generated catalogs at different times. (3) The build worker publishes only the launcher binary into ArtifactStore; the runtime .so closure and build metadata remain in the mutable build directory.

REQUIRED: (a) BuildPlan gains catalog_architectures: tuple[str, ...] (distinct from targets, which correctly means compiled AMDGPU targets) and input_hashes: tuple[(kind, content_hash), ...] replacing the named hash fields - compatibility properties may remain temporarily. Invariant: EVERY external artifact that can affect generated or compiled bytes is represented exactly once in BuildPlan identity (this absorbs HI66's evidence input naturally, whatever shape it takes). (b) Keep post-generation generated_compile_inputs_hash as an independent verification layer - not a substitute for correctly identifying the requested build. (c) Build publishes a runtime-bundle manifest ArtifactRef: entrypoint (bin/llama-bench), regular runtime files + hashes, required symlink/loader layout, runtime_bundle_hash, effective_build_id, generated_compile_inputs_hash, toolchain identity - and CampaignLaneResult returns that ref; downstream smoke/benchmark/release consume the bundle, never the build directory.

## Code Samples & Guidance



## Files

tools/bigcherry/builds.py, tools/bigcherry/toolchain.py

## Validation

NEGATIVE/FALSIFICATION (RV48): changing the catalog architecture selection changes build_plan_id and build dir; changing ANY declared build need (bytes of inventory, winners, or correctness evidence) changes build identity; a missing or tampered runtime-bundle member fails closed at consumption; two lanes with the same plan but different requested architectures can never share a build dir. Existing test_build_identity.py coverage keeps passing.

## Effort & Risk



## Standards



## Acceptance Criteria



## Notes

In progress: added BuildPlan/toolchain/environment identity, content-addressed build directories, effective configure/build IDs, binary hashes, and strict reuse validation. Execution integration and real Brutus configure/build proof remain open.

RDNA run-sheet review (2026-08-15): Build identity must change with each resolved independent, dependent, or interaction patch set and its dependency closure; preserve cache rejection on identity mismatch.

AUDIT RV48 (2026-08-17): second round opened. Stale 'execution integration and real Brutus proof remain open' is no longer accurate - execution is integrated (RE16-RE18) and proven on real hardware (RE20/RE21); what remains is the identity-completeness and runtime-bundle work above. HI66 landing on top of the generic input model is explicit sequencing: land (a) before wiring correctness evidence as a declared need.



## Change Log

- 2026-08-15T02:30:33.776064+00:00 (created-by): Created by agent
- 2026-08-15T02:49:43.593063+00:00 (updated-by): Updated: section:notes, section:validation
- 2026-08-15T02:49:43.625841+00:00 (state-transition): State: pending → in_progress
- 2026-08-15T02:51:31.764418+00:00 (updated-by): Updated: section:notes, section:validation

## Ledger-events


- chg_20260815_025139_added-the-next-reusable-campai_4148
- 2026-08-15T02:51:39.519282+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-15T03:34:10.713019+00:00 (updated-by): Updated: section:notes
- 2026-08-17T03:59:27.165170+00:00 (updated-by): Updated: section:detailed_solution, section:validation, section:notes
- chg_20260817_040144_a-deep-audit-of-the-reusable-b_4365
- 2026-08-17T04:01:44.074681+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-17T04:38:16.804496+00:00 (state-transition): State: in_progress → in_progress
