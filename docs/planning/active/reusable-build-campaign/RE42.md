---
id: RE42
order: 0
plan: reusable-build-campaign
state: pending
created-at: '2026-08-20T11:53:49.960733+00:00'
breadth: ''
skill: advanced
created-by: agent
work: M
---

# Split resolver into resolve_exact() (fail-closed, explicit) and expand_composition() (dependency-closure expansion)

## Description

Adopt the already-implemented two-layer resolver deliberately: resolve_exact() remains the fail-closed explicit HIP-compatible resolver; expand_composition() computes visible REQUIRES closure but is not yet used by production recipes.

## Steps

Add a snapshot-aware expansion API; prove closure against every existing hand-written dependency chain; add explicit opt-in composition mode to one non-production experiment; compare expanded IDs/order/content/patch_set_id/generated source with the current manual recipe; only then simplify recipes.toml in a separate change.

## Detailed Solution & Technical Design

Do not reimplement resolution. Keep resolve_exact() as the safety boundary and make expand_composition() return requested/expanded/pulled_in data for CLI/reporting. Use module REQUIRES/CONFLICTS as the single relationship source. Production HIP standard/replay lanes must remain exact until equivalence evidence is accepted.

## Code Samples & Guidance

```python
expansion = patchset.expand_composition([terminal_id], directory=snapshot.root)
print({"requested": expansion.requested,
       "expanded": expansion.expanded,
       "pulled_in": expansion.pulled_in})
resolved = patchset.resolve_exact(expansion.expanded,
                                  directory=snapshot.root,
                                  required_state=declared.required_state)
```
For adoption, add `composition_mode = "exact" | "expand"` to a non-production experiment or request, never silently change existing recipes. Compare `module_ids`, hashes, source output, and patch_set_id between manually listed and expanded forms before changing config.

## Files

tools/bigcherry/patchset.py; tools/bigcherry/campaign_resolution.py; tools/bigcherry/campaign_source.py; config/recipes.toml; tools/tests/test_patch_resolution.py; tools/tests/test_campaign_resolution.py

## Validation

Build a fixture chain A<-B<-C plus conflict D; test deterministic dependency-first output, cycle diagnostics, rejected-state behavior, and exact/manual equivalence. Add a golden report showing pulled-in IDs.

## Effort & Risk

M/medium; identity drift is the main risk.

## Standards

Align directly with HIP's exact resolver, explicit dependencies, fail-closed rejected-state handling, and immutable patch-set identity.

## Acceptance Criteria



## Notes

Filed 2026-08-20 from an external patch-management review. Depends on the sibling item extending patch_catalog.py with REQUIRES/CONFLICTS metadata (nothing to expand from without it). This is explicitly the LAST step in the review's own recommended migration order -- do this only after CatalogSnapshot, metadata validation, and the physical file move are stable, not combined with any of them.

RE42 depends on RE39 snapshot plumbing and RE40 metadata/relationship validation, but the existing implementation can be tested independently.

## Current Status (2026-08-23, plan-hygiene) -- reframed as adoption/equivalence, state stays pending, AFTER RE47

expand_composition() ALREADY EXISTS (patchset.py) -- the two-layer resolver is implemented (resolve_exact() fail-closed + expand_composition() REQUIRES closure). This is now an ADOPTION/EQUIVALENCE item, not an implementation item.

OPEN (safe production adoption, blocked on RE47):
- FIRST fix RE47: expand_composition()'s final line re-sorts the correct topological DFS order by (order, patch_id), discarding real dependency order (only topologically valid today by coincidence -- the 1215->1216->1217 chain has ascending numeric order matching dependency order).
- THEN: create a deliberately scrambled dependency fixture (a lower-numbered patch REQUIRES a higher-numbered one) proving the old behavior was wrong and the fix is right.
- Add a non-production composition_mode=expand; compare against the current explicit recipe on: module IDs, order, patch hashes, materialized source, patch-set identity.
- Only then consider simplifying production recipes.

Recommended order: RE47 -> RE39 -> RE42 production adoption.

## Change Log

- 2026-08-20T11:53:49.960733+00:00 (created-by): Created by agent
- 2026-08-20T12:14:58.882803+00:00 (updated-by): Updated: work='M', skill='advanced', section:description, section:steps, section:detailed_solution, section:files, section:validation, section:effort_risk, section:standards, section:notes
- 2026-08-20T12:18:33.568723+00:00 (updated-by): Updated: section:code_samples, section:validation
- 2026-08-23T06:43:55.623972+00:00 (updated-by): Updated: section:notes

## Ledger-events

- chg_20260823_064417_made-the-active-engineering-qu_3908
- 2026-08-23T06:44:17.900886+00:00 (updated-by): Updated: section:ledger-events
