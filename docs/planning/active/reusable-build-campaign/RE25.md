---
id: RE25
order: 0
plan: reusable-build-campaign
state: pending
created-at: '2026-08-17T04:56:43.967484+00:00'
breadth: ''
skill: advanced
created-by: agent
work: M
---

# ArtifactDescriptor persistence + kind-specific provenance contracts

## Description

RE08's remaining RV48-required work, split out because RE08's core verified defect (raw-path provenance laundering) is closed and doesn't need to wait for this broader design surface. Add ArtifactDescriptor -- a frozen, independently-serializable dataclass(artifact_id, kind, relative_path, content_hash, provenance: ProvenanceV2) -- that ArtifactStore can persist and rehydrate (RE11's cross-process resume needs exactly this: today CampaignRun.records is process memory only). Add kind-specific provenance contracts so provenance.validate() stops accepting all-five-namespaces-empty as sufficient for production artifact kinds.

## Steps

1. Design ArtifactDescriptor as a frozen dataclass, JSON-serializable (no Path objects -- relative_path as a string), with a stable artifact_id derivation.
2. Add ArtifactStore.persist_descriptor()/rehydrate_descriptor() (or equivalent) so a descriptor round-trips: persist, rehydrate, byte-verify against the store.
3. Define kind-specific provenance contracts: runtime-bundle requires source slice + source-plan/patch lineage + build plan + effective build + runtime-bundle identity; inventory requires producer source/build identity; promoted-winner/correctness artifact requires producer tune namespace; release report requires campaign/report identities.
4. Wire contract validation into provenance.validate() (or a new provenance.validate_for_kind()) so a document missing its kind's required fields is rejected, not silently accepted because the five namespaces merely exist as (possibly empty) dicts.
5. Migrate real producers (campaign_workers.py's generate/build/smoke workers, campaign_lane.py's materialize/input-resolution) to populate the now-required fields per kind.

## Detailed Solution & Technical Design

RV48 audit (2026-08-17), items (1)-(2) of RE08's required fixes, deferred out of RE08 itself once its core verified defect (raw-path provenance laundering, item 3) was closed for real and proven on Brutus hardware. See RE08's own notes/ledger event (chg_20260817_0449..-ish, the RE08 completion event) for the full context this split came from.

## Code Samples & Guidance



## Files

tools/bigcherry/artifacts.py, tools/bigcherry/provenance.py, tools/bigcherry/campaign_workers.py, tools/bigcherry/campaign_lane.py

## Validation

An ArtifactDescriptor round-trips through the store (persist, rehydrate, byte-verify). Each kind-specific contract rejects a document missing its required fields. Existing empty-namespace test fixtures either get real fields or are explicitly grandfathered as non-production kinds -- audit however deliberately calls out empty namespaces as 'a useful early-stage shape but not complete production provenance', so production kinds must fail without real fields.

## Effort & Risk

M -- touches provenance.py's validation contract, a shared primitive multiple producers depend on; must not silently break the real hardware-proven RE16-RE22 provenance flow while tightening it.

## Standards



## Acceptance Criteria



## Notes

Parent: RE14 umbrella, split from RE08 (2026-08-17). Depends on RE08 (closed). Gates the full RE24 adversarial matrix's provenance-related test groups (item 4, PROVENANCE LAUNDERING -- already covered by RE08's own fix -- and any future descriptor-persistence-specific group).

GPT review (2026-08-17, second pass on RE04/RE07/RE08): split this item internally.

RE25a (should land BEFORE RE24): provenance admission/contracts. Production kinds (inventory, promoted-winners, correctness evidence, runtime bundle) get required provenance fields -- provenance.validate() currently only requires the five namespaces exist as dicts, which may each be empty, 'a useful early-stage shape but not complete production provenance' per the audit. GPT's specific recommendation: a canonical production consumer should REJECT unproven/imported-legacy input outright (not merely label it), since this project has no taint-propagation mechanism to safely let an imported-legacy artifact flow through to a promoted/released result while carrying a visible non-promotable marker.

OPEN DESIGN QUESTION flagged back for RE25a (verified against real code before recording, not assumed): the real record->tune/replay workflow is INHERENTLY imported-legacy under the current single-process model. record and tune/replay are separate execute_campaign_lane() calls -- normally separate process runs entirely (record today, tune days later against the same inventory file) -- and there is currently no mechanism for a later lane to reference an earlier run's real ArtifactRef output except via raw Path (--inventory). This is not a hypothetical edge case: RE17/RE20/RE21's own real Brutus hardware proofs all used --inventory as a raw path for real tune AND replay builds. Blanket-rejecting imported-legacy in the canonical `build` path, as GPT's RE24-wording suggestion implies, would break this entirely real, already-proven, everyday workflow. RE25a must therefore resolve HOW rejection is scoped before implementing it -- e.g. reject imported-legacy only for artifact kinds where a real ArtifactRef alternative is expected to exist within the same run (build-produced artifacts), while still allowing legitimately cross-run raw imports (inventory/winners loaded via CLI flags) -- OR RE25b's ArtifactDescriptor rehydration needs to land FIRST so a real record output can be referenced by persistent ID across separate `build` invocations, making raw-Path import a genuinely rare expert-only escape hatch that CAN be safely rejected by default. Do not implement blanket rejection without resolving this tension -- confirm the intended scope with GPT before RE25a lands.

RE25b (may follow RE24): persistent ArtifactDescriptor -- a frozen, independently-serializable dataclass(artifact_id, kind, relative_path, content_hash, provenance) that ArtifactStore can persist and rehydrate. Needed for RE11's cross-process resume (today CampaignRun.records is process memory only) and, per the open design question above, may also be the real fix that makes RE25a's rejection safe to turn on without breaking the record->tune/replay workflow -- i.e. RE25b may need to land BEFORE RE25a's rejection semantics, not after, despite GPT's default sequencing suggestion. Reassess ordering once RE25a's scope question is resolved.

Not blocking RE24 on RE25b (ArtifactDescriptor) per GPT's original guidance -- RE25a's provenance-contract subset is what RE24 test group 4 actually needs.

## Change Log

- 2026-08-17T04:56:43.967484+00:00 (created-by): Created by agent

## Ledger-events


- chg_20260817_045655_closed-a-real-gap-where-a-data_2906
- 2026-08-17T04:56:55.267849+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-17T05:12:33.498156+00:00 (updated-by): Updated: section:notes
- chg_20260817_051244_closed-two-more-real-gaps-an-e_8767
- 2026-08-17T05:12:45.020730+00:00 (updated-by): Updated: section:ledger-events
