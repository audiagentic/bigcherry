---
id: RE15
order: 15
plan: reusable-build-campaign
state: pending
created-at: '2026-08-15T02:30:33.878066+00:00'
breadth: ''
skill: advanced
created-by: agent
work: L
---

# Run real GPU end-to-end campaign acceptance

## Description

Full post-cutover production lifecycle acceptance on real hardware (REWRITTEN per RV48 audit 2026-08-17; the old 'real GPU end-to-end campaign acceptance' was too vague and overlapped RE20 - which already closed with the five-lane proof). This is the final evidence-chain demonstration: source -> record -> inventory -> tune -> correctness/promotion -> replay-full -> replay-cache export -> runtime validation/coverage -> balanced comparison -> campaign report -> release promotion, all on real hardware, all identity-bound, and it is the gate for RE23 legacy deletion.

## Steps



## Detailed Solution & Technical Design

### Execution order (locked per RV50 — one complete release evidence chain, the single integration finish line)

Dependencies: RE25.3 closed + RE09 + RE10-min + RE12-min green. After this runs, stop adding architecture.

Use a clean production-class BigCherry checkout and a fresh acceptance store.

A. Source/build — materialize; build the record lane; verify source/runtime-bundle descriptors rehydrate; record ALL artifact IDs in an acceptance manifest.
B. Record — run the representative workload through the RECORD runtime bundle (immutable, never the build dir); publish record-jsonl.
C. Inventory — consume that record descriptor; produce production inventory + schema-4 dispatch-db; query the DB and assert non-null campaign identity columns.
D. Tune — build/use the tune runtime bundle; copy the dispatch DB to a working run location; run tuning; publish the resulting DB + tuning evidence as NEW immutable artifacts.
E. Promotion — run the existing promotion policy; publish promoted-winners; require promotable provenance.
F. Replay — build replay-full from descriptor-backed production inventory/winners; verify its runtime bundle is still production; export + publish the replay-cache descriptor.
G. Runtime validation — run the replay workload from the immutable runtime bundle; verify EXACT replay coverage (no misses, incompatibles, candidate-unavailable or rerun-required outcomes).
H. Balanced performance evidence — construct artifact-backed comparison arms; run the RE12 balanced comparison; produce the immutable comparison-report; confirm valid + decision-grade.
I. Release pointer — build the promotion pointer from the STORED report (never caller booleans); persist the release promotion; reload and validate pointer/report/artifacts independently.
J. Negative evidence — deliberate tamper rejection for at least THREE distinct stages, each on a DISPOSABLE copy of the acceptance store (never corrupt the canonical evidence): flip an inventory byte → downstream tune must refuse; flip a replay-cache/runtime member → validation/comparison must refuse; flip the comparison-report or its descriptor → release promotion must refuse.
K. Legacy absence — capture the exact commands/code paths used; verify no legacy-build, mutable checkout, _reset_tree, _ensure_tree_state or old _build_dir participated (static + runtime check per acceptance criterion 8).
L. Platform verification — full Windows suite; full Brutus/Linux suite; relevant RE24 adversarial matrix; one current-head real GPU run.

Record all final artifact IDs/hashes and the exact branch SHA in this item's notes, then mark completed. Acceptance criteria (1)–(8) above remain authoritative; this order is how they are executed.

## Code Samples & Guidance



## Files

Brutus campaign artifacts and reports

## Validation

ACCEPTANCE CRITERIA (RV48): (1) one continuous run on real hardware (Brutus) drives every transition of the evidence chain with ArtifactDescriptors at each hop; (2) acceptance DB rows have non-null campaign identities wherever semantically applicable; (3) replay-full emits only candidates whose correctness evidence is complete per HI66's full model; (4) runtime execution and smoke run from the immutable runtime bundle artifact; (5) the balanced comparison produces RE12's report artifact linking arm descriptors, raw logs, validity checks, effect estimates and confidence intervals; (6) a promotion pointer naming the report hash is created by RE13 machinery; (7) tampering any one intermediate artifact (byte flip in an inventory/promotion/replay/report file) blocks every downstream transition with a named failure - demonstrated for at least three distinct artifacts; (8) no legacy-build/mutable-checkout call appears anywhere in the executed chain (static + runtime check).

## Effort & Risk



## Standards



## Acceptance Criteria



## Notes

Source: BIGCHERRY_REUSABLE_BUILD_CAMPAIGN_RUN_SHEET_REVISED.md; preserve fail-closed identity and provenance rules.

RDNA run-sheet review (2026-08-15): RDNA acceptance is future and evidence-gated. Current Brutus evidence does not validate unimplemented RDNA items; retain open replay-cache identity and multi-GPU RCCL blockers.

AUDIT RV48 (2026-08-17): rewritten per audit. Depends on RE07 (runtime bundle + generic input hashes), RE09 (DB identities), RE12 (comparison report), RE13 (promotion), RE04/RE05 (content-safe materialisation). Gates RE23.

IN PROGRESS on real Brutus hardware (device 2/gfx1201, never 0/1). Stages A-E of the locked A-L sequence now run for real end to end: real materialize (found and fixed a stale-worktree-registration hazard specific to backgrounding via nohup+& over SSH -- use the harness's own background execution, not manual nohup), real record capture, real inventory with non-null campaign identity columns, real tune measurement, real promotion. Two genuine hardware/build bugs found and fixed this session: (1) campaign_lane.smoke_environment_for_hip_devices() double-filtered GPU selection by setting both HIP_VISIBLE_DEVICES and ROCR_VISIBLE_DEVICES to the same raw index, which breaks ROCm init for any nonzero device index (only device 0 degenerately worked, which is why this was never caught before -- this project always uses device 2/3); (2) lifecycle.py's execute_record_stage/execute_tune_stage never set GGML_HIP_DISPATCH_MODE, so the dispatch layer silently stayed in native mode and recorded/measured nothing. Both fixed and covered by regression tests/assertions.

Stage F (replay-full generate) is blocked by a THIRD, deeper finding: build_manifest()'s correctness-evidence gate requires every enumerated candidate (100 non-native candidates for gfx1201 restricted to this inventory) to have a status='ok' correctness measurement. 76/100 are reachable; the SAME 24 candidates (mmq j16/j32/j64/j128 batch-tile variants for q8_0/q4_k/q5_k/q6_k source types, plus mmf:f16 nw1-nw8 warp-count variants) never get measured regardless of workload: tried llama-bench batch/ubatch sweeps, a broad llama-server + Brutus bench-runner sweep (bench-configs=full, pp256..pp4096/tg128..tg2048), and a second sweep with flash-attention + q8_0 KV-cache quantization explicitly enabled -- identical 24 missing every time. Also found (and fixed with GGML_CUDA_DISABLE_GRAPHS=1, per docs/reference/TEST.md's own 'required for complete tuning' note) a real SIGABRT/illegal-HIP-memory-access crash at small ubatch (16/32) in tune mode without that env var -- but even with the crash fixed, those candidates still never reach 'ok'. This looks like a real, workload-independent gap in candidate eligibility/dispatch-selection for these specific small-batch-tile and warp-count variants on gfx1201 (RDNA4, wave32) -- plausibly a genuine autotune-catalog/dispatch bug, not a coverage problem, and needs its own C++/HIP-level investigation (a new HI-prefixed item) rather than a bigger acceptance-run workload. Paused here per explicit user decision after this finding.

Next session: (a) file/triage the 24-candidate gap as its own hip-autotune investigation item; (b) once resolved (or once a documented exemption/blacklist mechanism is confirmed for these candidates), resume RE15 stage F onward (G runtime validation, H balanced comparison, I release pointer, J negative-evidence tamper tests, K legacy-absence check, L platform verification).

IN PROGRESS on real Brutus hardware (device 2/gfx1201, never 0/1). Stages A-E of the locked A-L sequence now run for real end to end: real materialize, real record capture, real inventory with non-null campaign identity columns, real tune measurement, real promotion. Three genuine hardware/build bugs found and fixed this session, all verified by an independent gpt-auto-agent review pass (2026-08-18): (1) GPU device double-filtering (HIP_VISIBLE_DEVICES + ROCR_VISIBLE_DEVICES set to the same raw index); (2) missing GGML_HIP_DISPATCH_MODE for record/tune stages; (3) missing GGML_CUDA_DISABLE_GRAPHS=1 for the tune stage (HIP graph replay is keyed on shape/pointers, not on which candidate the tune dispatcher rotated to -- fixed tune-stage only, production replay path unaffected).

Stage F (replay-full generate) is blocked -- tracked as its own investigation, HI70 (hip-autotune plan): 24 of 100 non-native candidates for gfx1201 never reach correctness evidence regardless of workload (llama-bench sweeps, llama-server + Brutus bench-runner full sweep, flash-attention + q8_0 KV-cache). The reviewing pass revised the diagnosis toward catalog-over-generation / runtime-applicability mismatch rather than a coverage problem -- see HI70 for the full analysis and diagnostic path. Do NOT weaken the replay-full correctness gate to route around this.

Next session: once HI70 resolves (or documents why those 24 candidates are genuinely unreachable and the catalog generator is fixed to reflect that), resume RE15 stage F onward (G runtime validation, H balanced comparison, I release pointer, J negative-evidence tamper tests, K legacy-absence check, L platform verification) -- the already-produced A-E descriptor chain should not need re-running, provided it stays tied to the exact source/build identities the eventual fix targets.

## Change Log

- 2026-08-15T02:30:33.878066+00:00 (created-by): Created by agent

## Ledger-events

- chg_20260815_030451_fixed-the-reusable-patch-campa_3798
- 2026-08-15T03:04:51.556229+00:00 (updated-by): Updated: section:ledger-events
- chg_20260815_031414_fixed-tune-promotion-so-native_6567
- 2026-08-15T03:14:14.813481+00:00 (updated-by): Updated: section:ledger-events
- chg_20260815_031610_fixed-promotion-validation-for_9014
- 2026-08-15T03:16:10.896327+00:00 (updated-by): Updated: section:ledger-events
- chg_20260815_031718_fixed-promotion-validation-for_5034
- 2026-08-15T03:17:18.365519+00:00 (updated-by): Updated: section:ledger-events
- chg_20260815_031825_aligned-promotion-effect-valid_1632
- 2026-08-15T03:18:25.730679+00:00 (updated-by): Updated: section:ledger-events
- chg_20260815_031917_hardened-tune-promotion-agains_2369
- 2026-08-15T03:19:17.637397+00:00 (updated-by): Updated: section:ledger-events
- chg_20260815_032330_production-replay-catalog-gene_6306
- 2026-08-15T03:23:30.986008+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-15T03:34:10.854977+00:00 (updated-by): Updated: section:notes
- chg_20260815_111610_added-a-single-handover-docume_1411
- 2026-08-15T11:16:10.238284+00:00 (updated-by): Updated: section:ledger-events
- chg_20260815_111659_updated-the-handover-to-point_3682
- 2026-08-15T11:16:59.371930+00:00 (updated-by): Updated: section:ledger-events
- chg_20260815_111727_made-the-handovers-commit-ref_4302
- 2026-08-15T11:17:27.510260+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-17T03:58:58.562682+00:00 (updated-by): Updated: section:detailed_solution, section:description, section:validation, section:notes
- chg_20260817_040144_a-deep-audit-of-the-reusable-b_4365
- 2026-08-17T04:01:44.174868+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-17T14:03:34.476525+00:00 (updated-by): Updated: section:detailed_solution
- chg_20260817_140439_rescoped-the-reusable-build-ca_6101
- 2026-08-17T14:04:40.028663+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-18T01:51:35.076197+00:00 (updated-by): Updated: section:notes
- chg_20260818_015144_a-real-end-to-end-run-on-gpu-h_7000
- 2026-08-18T01:51:44.849996+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-18T03:51:11.188236+00:00 (updated-by): Updated: section:notes
