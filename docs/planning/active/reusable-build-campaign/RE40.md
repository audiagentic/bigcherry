---
id: RE40
order: 0
plan: reusable-build-campaign
state: pending
created-at: '2026-08-20T11:53:34.649972+00:00'
breadth: ''
skill: advanced
created-by: agent
work: M
---

# Extend patch_catalog.py metadata: PLAN_IDS, BACKENDS, SUBSYSTEMS, REQUIRES, CONFLICTS, HARDWARE

## Description

Complete declarative patch metadata and validation without creating duplicate relationship authorities. Catalog already carries kind/origin/backend/state plus plan_ids/backends/subsystems/hardware/options; patch modules already own and enforce REQUIRES/CONFLICTS.

## Steps

Validate catalog/module 1:1 coverage and state agreement; validate plural fields and plan IDs; add catalog references for descriptive plan/backend/subsystem/hardware data; cross-check catalog backend/options against PatchContext; expose module REQUIRES/CONFLICTS through snapshot/CLI rather than duplicating them; add provenance/retirement link checks against external-sources.toml.

## Detailed Solution & Technical Design

Keep patchset.py module constants authoritative for executable dependency/conflict resolution, as HIP's exact resolver already enforces them. Keep catalog.toml authoritative for descriptive classification and lane applicability metadata. If a relationship needs to become declarative, migrate it deliberately with one source of truth; never read two optional requires/conflicts fields. Include canonical metadata in future identity only when the identity contract is versioned and tested.

## Code Samples & Guidance

Catalog records may evolve additively:
```toml
[[patch]]
id = "1207_rd17_moe_topk_down_fold"
kind = "enhancement"
origin = "external-fork"
backend = "hip"
backends = ["hip"]
plan-ids = ["RD17", "HI73"]
subsystems = ["moe", "mmvq"]
hardware = ["gfx1100", "gfx1201"]
external-source = "stew675-rdna-boosts"
```
Do not add `requires` or `conflicts` here while module constants remain executable authority. Add `CatalogEntry.plan_ids_for()` compatibility that returns plural values or falls back to singular `plan_item`.

## Files

tools/bigcherry/patch_catalog.py; tools/bigcherry/patchset.py; tools/bigcherry/external_sources.py; patches/catalog.toml; tools/tests/test_catalog_snapshot.py; tools/tests/test_patch_governance.py

## Validation



## Effort & Risk

M/medium; metadata migration is safe if module relationship authority remains singular.

## Standards

Align with HIP's exact resolver, fail-closed applicability, and provenance/retirement rules.

## Acceptance Criteria



## Notes

Filed 2026-08-20 from an external patch-management review. Prerequisite for RE40 (dependency-closure expansion) and RE42 (patch explain/graph CLI, which needs REQUIRES/CONFLICTS/PLAN_IDS to have anything real to display).

This supersedes the original idea of backfilling catalog REQUIRES/CONFLICTS. RE42 consumes module relationships through the existing resolver.

STATUS UPDATE 2026-09-08 (dev-gpt-agent review, req_79d52537bace4b95): plan-text correction only, no implementation this pass.

The residual '1:1 module/catalog validation' scope needs re-wording for the packaged-patch architecture: production metadata is no longer just patches/catalog.toml. build_snapshot() (patch/catalog.py) deliberately synthesizes CatalogEntry records from packaged patch.toml descriptors and merges them with legacy catalog.toml entries, rejecting dual authority (a packaged patch with BOTH a patch.toml and a catalog.toml entry is a hard error). So the real invariant RE40's residual scope should validate is: every registry module (patchset.catalog()) has exactly one metadata source (packaged patch.toml OR a legacy catalog.toml entry, never neither, never both) and is consistently represented in the MERGED CatalogSnapshot.metadata -- not literally 'every module has a patches/catalog.toml entry', which is now false by design for packaged patches.

Keep this item's remaining scope narrow: provenance/external-source link checks against external-sources.toml, retirement-link consistency, backend/options applicability cross-checks, CLI exposure. Do NOT absorb RE47's now-completed digest-immutability fix (different item, already closed) or CO01's legacy-recipe removal (different item, different mechanism).

## Current Status (2026-08-23, plan-hygiene) -- narrowed, state stays pending

Much of the original intended structure ALREADY LANDED (38941fe patch-management cleanup). Do not read this item as if all of it is future work:

DONE:
- Plural descriptive metadata fields (CatalogEntry: kind/origin/backend/plan_ids/...).
- Real module-owned REQUIRES/CONFLICTS (patchset, cross-checked).
- Catalog snapshot support (CatalogSnapshot bundles selection + metadata).
- Dependency/conflict tests.

OPEN (residual scope only):
- Full module/catalog 1:1 validation (every module has a catalog entry and vice versa; cf. RE47's note that build_snapshot() should call the existing cross_check()).
- External-source / provenance links.
- Retirement-link consistency.
- Backend/options applicability cross-checks.
- CLI exposure.

Note: an earlier self-reference in this item's notes is stale -- the 'not yet done' items above are largely landed; see DONE. Narrow the acceptance to the OPEN list.

## Change Log

- 2026-08-20T11:53:34.649972+00:00 (created-by): Created by agent
- 2026-08-20T12:02:30.056723+00:00 (updated-by): Updated (no visible changes)
- 2026-08-20T12:14:58.872001+00:00 (updated-by): Updated: work='M', skill='advanced', section:description, section:steps, section:detailed_solution, section:files, section:validation, section:effort_risk, section:standards, section:notes
- 2026-08-20T12:18:33.563087+00:00 (updated-by): Updated: section:code_samples, section:validation
- 2026-08-23T06:43:44.860142+00:00 (updated-by): Updated: section:notes

## Ledger-events

- chg_20260823_064417_made-the-active-engineering-qu_3908
- 2026-08-23T06:44:17.891670+00:00 (updated-by): Updated: section:ledger-events
- 2026-09-08T11:47:43.058528+00:00 (updated-by): Updated: section:validation, section:notes
