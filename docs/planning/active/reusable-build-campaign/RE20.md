---
id: RE20
order: 23
plan: reusable-build-campaign
state: in_progress
created-at: '2026-08-16T11:22:01.449541+00:00'
breadth: ''
skill: advanced
created-by: agent
work: L
---

# Multi-lane real Brutus parity proof for the canonical standard profile

## Description

The final pre-flip evidence gate: run the canonical standard profile (RE19) through the new multi-lane planner/runner (RE18) on real Brutus hardware, across all its lanes (not just tune, which rounds 9-18 already proved exhaustively), and confirm each lane independently: correct source identity, intended CMake configuration, correct generated-input presence/absence for its build kind, build artifacts and runtime bundle hashes, dependencies rejected when missing, no cross-lane contamination, and at least one real GPU smoke from the resulting new-path artifacts.

## Steps

1. Run the new multi-lane runner against the canonical standard profile on Brutus.
2. For each lane, verify identity/artifact correctness -- reuse round 11's parity-harness machinery per-lane where a legacy equivalent exists for comparison, rather than re-deriving a new comparison method.
3. Deliberately verify NO cross-lane contamination: one lane's ArtifactStore writes/build_dir must not be reachable by or confusable with another lane's, even when lanes share a source_slice_id.
4. Confirm GPUs are idle before and after, matching the verification discipline of every prior round.

## Detailed Solution & Technical Design

gpt-auto-agent design (RE14 round 19): 'You don't necessarily need the exhaustive round-11-level comparison for every lane if the common source/build machinery has already been heavily proven, but I would at least establish' the checks listed above per lane.

## Code Samples & Guidance



## Files



## Validation

This item IS the validation gate for RE14.24 (the flip) -- it has no further downstream test of its own beyond the real hardware run succeeding for every canonical lane.

## Effort & Risk

L -- real hardware time across multiple lanes (each a real compile), plus genuine risk of surfacing a lane-specific bug (e.g. a build kind whose generate-stage-skip or promoted-winners wiring from RE17 has an edge case only a real replay-shaped build exposes).

## Standards



## Acceptance Criteria



## Notes

Parent: RE14. Depends on RE16, RE17, RE18, RE19 all landing first. This is the acceptance gate before RE14.24 (the flip) -- do not flip without this passing for every canonical lane.

GPT review (round 20 check-in, after RE17/RE18 landed for real): tighten acceptance evidence rather than broaden scope. [... prior guidance preserved, see earlier notes ...]

2026-08-17 real-hardware run results: stock/control/record/tune all fully proven on real gfx1100/gfx1201 hardware, exactly matching every acceptance check (distinct run_ids, matching result keys, correct optional-ref shapes, tune's workload_id verified against inventory hash, no path collisions). replay failed at generate: autotune_catalog.build_manifest()'s correctness-evidence gate rejected it for missing candidate coverage in the available measurements artifact.

Deep-dived this for real (not just declared 'pre-existing debt and moved on'): 5 different real production-shaped workloads on idle Brutus GPUs (never touching the production-serving XTX cards) progressively raised gfx1201 correctness-evidence coverage from 55 to 131 entries (Qwen3.5-0.8B -> +Qwen3.5-9B -> +Qwen3.6-35B-A3B -> +Qwen3.8-27B with MTP speculative drafting -> +q8_0-quantized KV cache matching the real production server's exact config) but never closed the single remaining candidate mmq:q8_0:j16:fb1:t128:o2:i64:sram-q8_0:k256:sk0:v1. GPT root-caused this via the actual runtime eligibility code: MMQ's fb1 (fallback) variant is only eligible when the q8_0 weight matrix's ne0[1] % 128 != 0 -- a narrow shape condition no realistic model's matmul dimensions are likely to hit by chance, regardless of how many/how-large models are run against it. This is 'legal but workload-unobserved', not dead code, and is NOT something more model-hunting will reliably close.

GPT's refined RE20 acceptance condition for the replay lane specifically (replaces the naive reading): the canonical replay-full lane must succeed when supplied with its COMPLETE DECLARED PRODUCTION EVIDENCE DEPENDENCIES -- it does NOT mean one realistic end-to-end model workload must incidentally exercise every replay-full catalog candidate (those are different, and only the first is achievable/sound). GPT explicitly rejected both of the tempting workarounds: (a) do NOT prune the mmq:q8_0:j16 candidate from the catalog (it's reachable via a synthetic odd-row shape, per the runtime code -- pruning is only justified if a bounded synthetic witness search proves NO valid dispatch signature exists for it), (b) do NOT switch the standard profile's `replay` build from replay-full to replay-slim as a workaround (would hide the evidence-model gap by shrinking the product and undo RE19's semantic parity).

Real fix tracked as new item HI66 (hip-autotune plan): split replay-full's correctness-evidence requirement into workload evidence UNION candidate-qualification evidence (a synthetic q8_0 odd-row MUL_MAT test proves the missing candidate is reachable and lets the tuner measure it directly, rather than requiring it show up incidentally in end-to-end inference). RE20's replay lane is blocked on HI66 landing (or at minimum HI66's synthetic-shape diagnostic step alone, without the full API split, if that alone produces complete evidence for the current architecture set).

Status: RE20 at 4/5 lanes proven for real; replay blocked on HI66, not abandoned or worked around. Do not flip RE21 until HI66 unblocks replay and the full 5-lane proof passes.

## Change Log

- 2026-08-16T11:22:01.449541+00:00 (created-by): Created by agent
- 2026-08-16T23:13:52.715331+00:00 (updated-by): Updated: section:notes
- 2026-08-16T23:15:01.896621+00:00 (state-transition): State: pending → in_progress
- 2026-08-17T01:28:04.399351+00:00 (updated-by): Updated: section:notes

## Ledger-events

- chg_20260817_012817_ran-the-new-multi-lane-build-s_2185
- 2026-08-17T01:28:17.396774+00:00 (updated-by): Updated: section:ledger-events
