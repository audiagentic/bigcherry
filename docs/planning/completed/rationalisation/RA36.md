---
id: RA36
order: 0
plan: rationalisation
state: completed
created-at: '2026-08-25T12:15:34.228610+00:00'
breadth: ''
skill: advanced
created-by: agent
work: XL
---

# TR11 Continue: classify and move remaining tests by domain

## Description

Continue TR11 test reorganization after the RA35 pilot (release-domain, 3 files/70 tests, done and validated). 163 of 166 tools/tests/*.py files remain flat and unclassified, needing to move into tools/tests/{source,build,patch,campaign,tuning,core,analysis,integration,hardware,fixtures}/ per the target filesystem. Unlike TR10 (which had an explicit file->domain mapping from the run sheet), classifying these requires reading each file to determine what it actually tests -- a heavier analysis task than the mechanical moves themselves.

## Steps

- For each remaining domain (source, build, patch, campaign, tuning, core, analysis are the clearest; integration/hardware/fixtures need their own criteria per the run sheet: hardware tests explicit and not part of normal discovery, generic C/C++/CUDA fixtures under fixtures/<domain>/, patch-specific durable fixtures stay package-local not here).
- Classify by content (imports/what's under test), not just filename pattern -- filenames don't reliably indicate domain (e.g. test_cli_tooling_surface.py, test_recipes.py touch multiple domains).
- For every moved file: increment its sys.path.insert(0, str(Path(__file__).resolve().parents[N])) depth by one (126 of 166 files use this pattern) -- same bug class as TR10's REPO_ROOT issue.
- Add tools/tests/<domain>/__init__.py for every new subdirectory -- unittest discover -s tools/tests -p 'test*.py' silently drops tests in a subdirectory with no __init__.py (confirmed: pattern-based discovery requires it even though explicit dotted-path execution doesn't).
- After each domain slice: full unittest discover must find the same total test count as before the slice (currently 1816, only the pre-existing Windows symlink error as a known failure) -- a dropped count means a missing __init__.py, not a real test loss.
- Do not rename thousands of test functions merely to remove HI/RD/RE history (run sheet TR11 rule) -- only the file's directory location changes, not test names/content.
- Recommend doing this as several more domain-sized slices (source domain next, similar size to release), each with its own plan item + ledger event + commit, same discipline as the TR10 domain moves.

## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files



## Validation

Per slice: py_compile clean; moved files pass individually via explicit module path; full unittest discover finds the same total (1816) as pre-slice baseline; bigcherry --help byte-identical (confirms no production code touched).

## Effort & Risk



## Standards



## Acceptance Criteria



## Notes

Completed source-domain slice: moved test_ancestry_check.py, test_external_sources.py, test_source_audit.py, and test_source_identity.py into tools/tests/source/ with __init__.py. Adjusted tool/repository root resolution for nested paths and preserved test behavior. Explicit source slice passes 35/35. Full discovery was rerun; command remains nonzero due existing unrelated suite failures, not test discovery loss.

Completed source-domain slice: moved test_ancestry_check.py, test_external_sources.py, test_source_audit.py, and test_source_identity.py into tools/tests/source/ with __init__.py. Adjusted tool/repository root resolution for nested paths and preserved test behavior. Explicit source slice passes 35/35. Full discovery was rerun; command remains nonzero due existing unrelated suite failures, not test discovery loss.

Completed RA36/TR11 remainder: classified all 162 remaining flat test_*.py files by imports/content and moved them into build (8), patch (30), campaign (24), tuning (61), core (10), analysis (11), integration (11), hardware (6), plus the remaining release-domain test into release (1). Added package markers, corrected nested __file__-relative sys.path/repository roots, added missing bootstraps in analysis/test_analyze_gaps.py and analysis/test_check.py exposed by package-order discovery, and moved generic C/CUDA fixtures to tools/tests/fixtures/hardware with references corrected. No residual flat test_*.py files remain; no in-scope residual list.

Validation: python -m unittest discover -s tools/tests -p 'test*.py' -q ran 1824 tests, matching the pre-move current run and +8 versus the stale 1816 plan baseline. Package counts: build 92, patch 269, campaign 336, tuning 735, core 75, analysis 88, integration 98, hardware 25, source 35, release 71. py_compile/AST validation passed. The final run retained the pre-existing 9 failures, 13 errors, and 3 skips: experiment-bundle CLI exit-127 failures; Windows subprocess executable/path errors in benchmark/workload/replay-inspect tests; Windows symlink privilege error; and unavailable C++/HIP standard headers for the explicit hardware probes. Ledger event: chg_20260825_233744_completed-ra36tr11-test-domai_2223.

Completed source-domain slice: moved test_ancestry_check.py, test_external_sources.py, test_source_audit.py, and test_source_identity.py into tools/tests/source/ with __init__.py. Adjusted tool/repository root resolution for nested paths and preserved test behavior. Explicit source slice passes 35/35. Full discovery was rerun; command remains nonzero due existing unrelated suite failures, not test discovery loss.

Completed source-domain slice: moved test_ancestry_check.py, test_external_sources.py, test_source_audit.py, and test_source_identity.py into tools/tests/source/ with __init__.py. Adjusted tool/repository root resolution for nested paths and preserved test behavior. Explicit source slice passes 35/35. Full discovery was rerun; command remains nonzero due existing unrelated suite failures, not test discovery loss.

Completed RA36/TR11 remainder: classified all 162 remaining flat test_*.py files by imports/content and moved them into build (8), patch (30), campaign (24), tuning (61), core (10), analysis (11), integration (11), hardware (6), plus the remaining release-domain test into release (1). Added package markers, corrected nested __file__-relative sys.path/repository roots, added missing bootstraps in analysis/test_analyze_gaps.py and analysis/test_check.py exposed by package-order discovery, and moved generic C/CUDA fixtures to tools/tests/fixtures/hardware with references corrected. No residual flat test_*.py files remain; no in-scope residual list.

Validation: python -m unittest discover -s tools/tests -p 'test*.py' -q ran 1824 tests, matching the pre-move current run and +8 versus the stale 1816 plan baseline. Package counts: build 92, patch 269, campaign 336, tuning 735, core 75, analysis 88, integration 98, hardware 25, source 35, release 71. py_compile/AST validation passed. The final run retained the pre-existing 9 failures, 13 errors, and 3 skips: experiment-bundle CLI exit-127 failures; Windows subprocess executable/path errors in benchmark/workload/replay-inspect tests; Windows symlink privilege error; and unavailable C++/HIP standard headers for the explicit hardware probes. Ledger event: chg_20260825_233744_completed-ra36tr11-test-domai_2223.

Post-RA36 validation follow-up (2026-08-26): fixed all validation-run failures/errors without changing production behavior. Test-side subprocess helpers now select a launchable Python executable when Windows sys.executable is a non-file alias; symlink and hardware probes now preserve truthful, explicit capability skips. Targeted affected modules: 82 tests, OK (skipped=3). Full discovery: 1824 tests, OK (skipped=6), down from 9 failures/13 errors/3 skips to 0 failures/0 errors/6 skips. The six remaining skips are genuine environment capabilities: two build symlink fixtures, campaign toolchain symlink swap, RV80 symlink escape privilege, missing host C++ <cmath>, and missing HIP C++ <cmath>/<cstdlib>.

Completed source-domain slice: moved test_ancestry_check.py, test_external_sources.py, test_source_audit.py, and test_source_identity.py into tools/tests/source/ with __init__.py. Adjusted tool/repository root resolution for nested paths and preserved test behavior. Explicit source slice passes 35/35. Full discovery was rerun; command remains nonzero due existing unrelated suite failures, not test discovery loss.

Completed source-domain slice: moved test_ancestry_check.py, test_external_sources.py, test_source_audit.py, and test_source_identity.py into tools/tests/source/ with __init__.py. Adjusted tool/repository root resolution for nested paths and preserved test behavior. Explicit source slice passes 35/35. Full discovery was rerun; command remains nonzero due existing unrelated suite failures, not test discovery loss.

Completed RA36/TR11 remainder: classified all 162 remaining flat test_*.py files by imports/content and moved them into build (8), patch (30), campaign (24), tuning (61), core (10), analysis (11), integration (11), hardware (6), plus the remaining release-domain test into release (1). Added package markers, corrected nested __file__-relative sys.path/repository roots, added missing bootstraps in analysis/test_analyze_gaps.py and analysis/test_check.py exposed by package-order discovery, and moved generic C/CUDA fixtures to tools/tests/fixtures/hardware with references corrected. No residual flat test_*.py files remain; no in-scope residual list.

Validation: python -m unittest discover -s tools/tests -p 'test*.py' -q ran 1824 tests, matching the pre-move current run and +8 versus the stale 1816 plan baseline. Package counts: build 92, patch 269, campaign 336, tuning 735, core 75, analysis 88, integration 98, hardware 25, source 35, release 71. py_compile/AST validation passed. The final run retained the pre-existing 9 failures, 13 errors, and 3 skips: experiment-bundle CLI exit-127 failures; Windows subprocess executable/path errors; Windows symlink privilege error; and unavailable C++/HIP standard headers for the explicit hardware probes. Ledger event: chg_20260825_233744_completed-ra36tr11-test-domai_2223.

Post-RA36 validation follow-up (2026-08-26): fixed all validation-run failures/errors without changing production behavior. Test-side subprocess helpers now select a launchable Python executable when Windows sys.executable is a non-file alias; symlink and hardware probes now preserve truthful, explicit capability skips. Targeted affected modules: 82 tests, OK (skipped=3). The final current shared-worktree discovery ran 1826 tests, OK (0 failures, 0 errors, 6 skips); the two-test increase versus the 1824 post-fix snapshot is in the analysis package (currently 90 tests) and is outside the RA36 edits. Counts changed from 9 failures/13 errors/3 skips to 0 failures/0 errors/6 skips. The six remaining skips are genuine environment capabilities: two build symlink fixtures, campaign toolchain symlink swap, RV80 symlink escape privilege, missing host C++ <cmath>, and missing HIP C++ <cmath>/<cstdlib>.

## Change Log

- 2026-08-25T12:15:34.228610+00:00 (created-by): Created by agent
- 2026-08-25T23:07:35.143611+00:00 (updated-by): Updated: section:notes

## Ledger-events


- chg_20260825_230742_reorganized-four-source-domain_4506
- 2026-08-25T23:07:42.444002+00:00 (updated-by): Updated: section:ledger-events
- chg_20260825_233744_completed-ra36tr11-test-domai_2223
- 2026-08-25T23:37:44.362028+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-25T23:37:55.531194+00:00 (updated-by): Updated: section:notes
- 2026-08-25T23:37:55.563371+00:00 (state-transition): State: pending → completed
- 2026-08-26T00:02:04.754753+00:00 (updated-by): Updated (no visible changes)
- 2026-08-26T00:02:22.836789+00:00 (updated-by): Updated: section:notes
- chg_20260826_000230_ra36-validation-cleanup-now-pa_5151
- 2026-08-26T00:02:30.955520+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-26T00:06:08.011479+00:00 (updated-by): Updated: section:notes
- chg_20260826_000624_final-shared-worktree-verifica_6065
- 2026-08-26T00:06:24.415487+00:00 (updated-by): Updated: section:ledger-events
