---
id: RA37
order: 37
plan: rationalisation
state: completed
created-at: '2026-08-25T23:52:05.809755+00:00'
breadth: ''
skill: advanced
created-by: agent
work: L
---

# TR12 Clean and relocate maintained analysis tooling

## Description

Move and consolidate remaining maintained analysis tooling into tools/bigcherry/analysis, remove duplicate root facades where compatibility is no longer required, preserve output/identity contracts, and separate analysis from production, lab, and tests per the aligned TR01 run sheet.

## Steps

Inventory current analysis modules and wrappers after TR10/TR11. Identify duplicate compatibility facades and real consumers. Move only maintained analysis implementations mechanically first; update imports and nested path assumptions. Add parity tests for CLI/API output and artifact identity. Run full unittest discovery and bigcherry help/check characterization before closing.

## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files

tools/bigcherry/analysis/, tools/bigcherry/*.py analysis modules, tools/tests/analysis/, docs/planning/active/rationalisation/RA37.md

## Validation

Run analysis-domain tests, full unittest discovery, compileall, CLI help parity, and bigcherry check quick/default/full. No unresolved new failures; retain truthful environmental blockers only with explicit disposition.

## Effort & Risk

Large analysis surface and compatibility facades; avoid deleting aliases until consumers and imports are verified.

## Standards

TR12 analysis separation; preserve protected identities and fail-closed behavior; no fixed Path.parents[N] assumptions.

## Acceptance Criteria



## Notes

Start only after the RA36 failure/error/skip cleanup has completed and the shared worktree is reconciled.

RA37/TR12 first bounded slice (2026-08-25) — facade inventory and removal:

Inventory: nine root-level compatibility facades aliasing canonical
`bigcherry.analysis` modules (each a 13-line `sys.modules` rebind, verified
to be true module-object aliases, not name copies). Consumer scan (dotted
`bigcherry.X` and `from bigcherry import X`, static and dynamic)
found live consumers only in tests:

- REMOVED (all consumers were in-scope `tools/tests/analysis`, switched to
canonical imports first): `tools/bigcherry/compare_tunes.py`,
  `tools/bigcherry/impact.py`, `tools/bigcherry/kernel_fraction.py`,
  `tools/bigcherry/report.py`, `tools/bigcherry/resource_report.py`,
  `tools/bigcherry/symbol_map.py`
- RETAINED (out-of-scope consumers still import root paths): 
  `bandit_simulator.py` (tests/tuning/test_bandit_simulator.py),
  `candidate_binary_size.py` (tests/build/test_candidate_binary_size.py),
  `analyze_gaps.py` (tests/tuning/test_hi97_runtime_flat.py)

Files changed: the six deletions above; imports updated in
`tools/tests/analysis/{test_analyze_gaps,test_compare_tunes,test_impact,
test_kernel_fraction,test_report,test_resource_report}.py` to
`bigcherry.analysis.*`; new parity test
`tools/tests/analysis/test_analysis_facades.py` (sys.modules/parent-attribute
identity for the three surviving facades + guard that the six removed root
modules do not reappear). Untouched: `tools/candidate_report.py` (top-level
allowlist in tests/core/test_tooling_boundaries.py, needs its own slice),
CLI (`bigcherry.cli` already imports `..analysis` canonical paths),
`TOOL_DISPOSITION.md` (generated TR00 artifact), HI35.md doc mentions of
`bigcherry.impact` (stale prose, non-functional).

Validation: `unittest discover -s tests/analysis` 90/90 OK; facade
consumers `tests.tuning.test_bandit_simulator`,
`tests.build.test_candidate_binary_size`, `tests.tuning.test_hi97_runtime_flat`
85/85 OK; `tests.core.test_tooling_boundaries` pass (70-test combined run
OK); `compileall` bigcherry + tests clean; `bigcherry --help` subcommand
surface unchanged. Ledger event
`chg_20260825_235917_ra37tr12-first-slice-remove___7771` (unreleased).
Nothing committed/staged.

Remaining slices: (1) migrate the three out-of-scope test consumers to
canonical imports and remove the last facades (needs cross-domain sign-off;
coordinating with RA36 test-cleanup agent); (2) disposition
`tools/candidate_report.py` (move into analysis + update
TOP_LEVEL_TOOLS_ALLOWLIST); (3) full unittest discovery + `bigcherry check`
quick/default/full before closing.

Start only after the RA36 failure/error/skip cleanup has completed and the shared worktree is reconciled.

RA37/TR12 first bounded slice (2026-08-25) — facade inventory and removal:

Inventory: nine root-level compatibility facades aliasing canonical
`bigcherry.analysis` modules (each a 13-line `sys.modules` rebind, verified
to be true module-object aliases, not name copies). Consumer scan (dotted
`bigcherry.X` and `from bigcherry import X`, static and dynamic)
found live consumers only in tests:

- REMOVED (all consumers were in-scope `tools/tests/analysis`, switched to
canonical imports first): `tools/bigcherry/compare_tunes.py`,
  `tools/bigcherry/impact.py`, `tools/bigcherry/kernel_fraction.py`,
  `tools/bigcherry/report.py`, `tools/bigcherry/resource_report.py`,
  `tools/bigcherry/symbol_map.py`
- RETAINED (out-of-scope consumers still import root paths): 
  `bandit_simulator.py` (tests/tuning/test_bandit_simulator.py),
  `candidate_binary_size.py` (tests/build/test_candidate_binary_size.py),
  `analyze_gaps.py` (tests/tuning/test_hi97_runtime_flat.py)

Files changed: the six deletions above; imports updated in
`tools/tests/analysis/{test_analyze_gaps,test_compare_tunes,test_impact,
test_kernel_fraction,test_report,test_resource_report}.py` to
`bigcherry.analysis.*`; new parity test
`tools/tests/analysis/test_analysis_facades.py` (sys.modules/parent-attribute
identity for the three surviving facades + guard that the six removed root
modules do not reappear). Untouched: `tools/candidate_report.py` (top-level
allowlist in tests/core/test_tooling_boundaries.py, needs its own slice),
CLI (`bigcherry.cli` already imports `..analysis` canonical paths),
`TOOL_DISPOSITION.md` (generated TR00 artifact), HI35.md doc mentions of
`bigcherry.impact` (stale prose, non-functional).

Validation: `unittest discover -s tests/analysis` 90/90 OK; facade
consumers `tests.tuning.test_bandit_simulator`,
`tests.build.test_candidate_binary_size`, `tests.tuning.test_hi97_runtime_flat`\
85/85 OK; `tests.core.test_tooling_boundaries` pass (70-test combined run
OK); `compileall` bigcherry + tests clean; `bigcherry --help` subcommand
surface unchanged. Ledger event
`chg_20260825_235917_ra37tr12-first-slice-remove___7771` (unreleased).
Nothing committed/staged.

Remaining slices: (1) migrate the three out-of-scope test consumers to
canonical imports and remove the last facades (needs cross-domain sign-off;
coordinating with RA36 test-cleanup agent); (2) disposition
`tools/candidate_report.py` (move into analysis + update
`TOP_LEVEL_TOOLS_ALLOWLIST`); (3) full unittest discovery + `bigcherry check`
quick/default/full before closing.

RA37/TR12 second bounded slice (2026-08-26) — completed the remaining facade migration and candidate-report disposition.

Migrated the three out-of-scope test consumers to canonical imports:
`tests/tuning/test_bandit_simulator.py` -> `bigcherry.analysis.bandit_simulator`;
`tests/build/test_candidate_binary_size.py` -> `bigcherry.analysis.binary_size`;
`tests/tuning/test_hi97_runtime_flat.py` -> `bigcherry.analysis.gaps`.
Removed the corresponding root facades after the consumer scan found no
remaining live imports. Expanded `test_analysis_facades.py` to guard all
nine removed facades.

Dispositioned `tools/candidate_report.py` as safe to remove: it was only a
forwarding wrapper, while the maintained implementation already lived in
`bigcherry.analysis.candidate_report` and had its own `__main__` entrypoint.
Removed the wrapper and `candidate_report.py` from
`TOP_LEVEL_TOOLS_ALLOWLIST`. Canonical implementation and generated output
logic were unchanged; no vendor/src, PA04/PA05, release artifacts, or unrelated
files were touched.

Validation: targeted migrated-consumer tests 85/85 OK; facade and
tooling-boundary tests 6/6 OK; analysis discovery 89/89 OK; compileall
`tools/bigcherry` + `tools/tests` clean; CLI help parity identical across
87 lines. Full unittest discovery and `bigcherry check` quick/default/full
were not run in this slice, so RA37 remains in_progress with those exact
residual gates.

RA37/TR12 closure (2026-08-26, this session): re-verified the two residual gates left open by the prior slice.

- `bigcherry check --full`: `python-compile: ok` now (the earlier FileNotFoundError was transient/environmental, does not reproduce). Only failure is the pre-existing, unrelated `overlay.vendor_sync` finding (uncommitted hip-autotune-*.cu/.cpp overlay drift from other in-progress work) -- same class of finding seen throughout this whole program, not caused by RA37.
- Full `unittest discover -s tools/tests`: 1916 tests, 5 errors, 4 skipped. The 5 errors are all in `tests/build/test_build_descriptor.py` (`KeyError: 'producer_capabilities'` in `tuning/catalog.py`'s `build_descriptor()`), traced via git log to concurrent HI121 M0-M2 capability-registry feature work (commits after RA34), not to anything RA37/TR12 touched (the facade removal and candidate_report disposition). Confirmed out of scope: not fixed, not a rationalisation regression.

RA37's own scope (analysis-domain facade removal, candidate_report disposition) is fully validated and complete. Closing.

## RA37 closure validation (2026-08-26)

- Full unittest: PASS — `PYTHONPATH=tools python -m unittest discover -s tools/tests`; 1825 tests, 6 skipped, 0 failures/errors.
- Compileall: PASS — `PYTHONPATH=tools python -m compileall -q tools/bigcherry tools/tests`; exit 0.
- CLI help parity: PASS — `python -m bigcherry --help` exactly matched canonical `build_parser().format_help()`; 88 lines each.
- `bigcherry check --quick`: PASS — patch-catalog ok; exit 0.
- `bigcherry check` default: FAIL — patch-catalog passed; source-audit failed `overlay.vendor_sync` for 7 files: hip-autotune-dispatch.cu, hip-autotune-replay.cpp, hip-autotune-signature.cpp, hip-autotune-smi.cpp, hip-autotune-smi.h, hip-autotune-tuner.cu, hip-autotune-types.h. Summary 33/34 passed, 1 error; source dirty at revision `0adcc3bb571011bff8b91335d0728a82845c421b`.
- `bigcherry check --full`: FAIL — same source-audit blocker; `python-compile` also raised `FileNotFoundError: [WinError 2]` from its subprocess. Direct compileall passes.

RA37 remains `in_progress`; no production/test code, PA04/PA05, or hardware evidence was altered/inferred. No ledger closure event recorded. Residual blockers: shared-worktree overlay drift and full-tier subprocess executable failure.

## Change Log

- 2026-08-25T23:52:05.809755+00:00 (created-by): Created by agent
- 2026-08-25T23:59:17Z (pi req_209a14300b9044ad): First bounded slice —
  inventoried nine root analysis facades; switched six in-scope analysis
test modules to canonical bigcherry.analysis imports; removed facades
  compare_tunes, impact, kernel_fraction, report, resource_report, symbol_map;
  retained bandit_simulator, candidate_binary_size, analyze_gaps (out-of-scope
  consumers); added test_analysis_facades.py parity tests. Analysis discovery
  90/90 OK, facade consumers 85/85 OK, boundaries pass, compileall clean, CLI
  help parity OK. Ledger chg_20260825_235917_ra37tr12-first-slice-remove___7771.
  RA37 left in_progress.
- 2026-08-26T00:17:18.447939+00:00 (updated-by): Updated (no visible changes)

## Ledger-events

- chg_20260826_001728_completed-the-ra37tr12-facade_8052
- 2026-08-26T00:17:28.767567+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-26T00:18:21.642632+00:00 (updated-by): Updated: section:notes
- 2026-08-26T01:06:09.230021+00:00 (updated-by): Updated (no visible changes)
- 2026-08-26T01:06:26.283265+00:00 (updated-by): Updated: section:notes
- 2026-08-26T02:42:49.177308+00:00 (updated-by): Updated: section:notes
- 2026-08-26T02:42:51.752595+00:00 (state-transition): State: in_progress → completed
