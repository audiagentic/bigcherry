---
id: RD68
order: 0
plan: rdna-boost-experiments
state: completed
created-at: '2026-08-20T06:14:33.806242+00:00'
breadth: ''
skill: intermediate
created-by: agent
work: S
---

# Verify Vulkan baseline PRs #25494/#26585 are ancestral to the current pin before any Vulkan lane work

## Description

Combines BASE-VK-001 and BASE-VK-002 from the backlog doc -- these are "verify ancestral to pin" checks, not patch candidates, structurally identical to docs/reference/PIN_REBASE_REVIEW_B10502.md's existing per-commit ancestry-check pattern (that review's A9 next-bump-watch-list), just never checked for Vulkan since BigCherry does not currently build a Vulkan lane. Source status: Merged upstream for both -- verify pinned base contains them, do not treat as new patches unless genuinely absent.

## Steps



## Detailed Solution & Technical Design

BASE-VK-001 -- merged q8_0 KV dequant-once Vulkan FlashAttention baseline (llama.cpp PR #25494, merged).
Hypothesis: BigCherry Vulkan comparisons must include the dequant-once q8_0 KV FA path if present upstream.
What/why: Substantially changes long-context q8 KV prefill; treating an older base as stock Vulkan would misattribute gains to local patches.
Where: pin/rebase validation, not a new patch if ancestral.

BASE-VK-002 -- merged tiled 0<->2 Vulkan transpose baseline (llama.cpp PR #26585, merged).
Hypothesis: Performance investigations involving lightning-indexer/transpose-heavy graphs must include the tiled transpose baseline.
What/why: A generic strided copy previously dominated some prefill workloads; this changes the baseline dramatically.
Where: pin/rebase validation.

## Code Samples & Guidance

BASE-VK-001 trigger: Q8_0 KV pp512 at 32K/64K/128K. Controls: F16/Q4 KV; TG. Boundary: depth 32K/64K/128K/240K.

BASE-VK-002 trigger: DeepSeek V4/lightning-indexer graph; any captured 0<->2 transpose signatures. Controls: other CONT/permutation patterns. Boundary: relevant tensor sizes.

## Files



## Validation

BASE-VK-001: backend ops + model output; PP/TG and scratch memory. BASE-VK-002: backend parity; bandwidth and end-to-end PP.

Acceptance for both: record as baseline capability; port ONLY if the currently pinned base genuinely predates the PR (unlikely given both are already merged upstream and the project's pin has moved forward substantially, per PIN_REBASE_REVIEW_B10502.md's own b10362->b10502 rebase this session) -- this is primarily a verification task, not expected to produce new patch work.

## Effort & Risk



## Standards



## Acceptance Criteria



## Notes

Same check pattern as PIN_REBASE_REVIEW_B10502.md §3.3's next-bump watch list, just extended to cover Vulkan-side commits (never checked before since no Vulkan lane exists yet). Relevant prerequisite for RE30 (Vulkan tunable-build-line scoping item) and UP-VK-001/002/003/004 (RD55/RD59/RD60/RD65) if a Vulkan lane is ever built. From docs/reference/BigCherry_External_AMD_RDNA_Experiment_Backlog.md §4. Filed as part of the 65-item backlog batch (2026-08-20).

Same check pattern as PIN_REBASE_REVIEW_B10502.md §3.3's next-bump watch list, just extended to cover Vulkan-side commits (never checked before since no Vulkan lane exists yet). Relevant prerequisite for RE30 (Vulkan tunable-build-line scoping item) and UP-VK-001/002/003/004 (RD55/RD59/RD60/RD65) if a Vulkan lane is ever built. From docs/reference/BigCherry_External_AMD_RDNA_Experiment_Backlog.md §4. Filed as part of the 65-item backlog batch (2026-08-20).

## RESULT (2026-08-24, dev-gpt-agent session ses_76b0fef0c94c434a + independent verification)

The item's own stated expectation ("unlikely given both are already merged upstream and the project's pin has moved forward substantially") was WRONG. Verified directly against real upstream git history (gh CLI + git merge-base --is-ancestor on the actual vendor/llama.cpp checkout), not taken on GPT's word alone:

- Current pin (b10502) = 0adcc3bb571011bff8b91335d0728a82845c421b (confirmed: matches vendor/llama.cpp HEAD).
- PR #25494 ("vulkan: dequant q8_0 KV once in coopmat1"), merge commit dc72703fc69698b1ea68ece8d2dd8a96e6a4e1fe: `git merge-base --is-ancestor` returns NOT ancestral. `git merge-base <merge> <pin>` == the pin itself, i.e. the PR sits 15 commits strictly ahead of the current pin (`git rev-list --count pin..merge` = 15).
- PR #26585 ("vulkan: tiled transpose for 0<->2 permuted CONT"), merge commit 98d1e92c2185d574c8b812e9e79f666af73bed8d: same result -- NOT ancestral, 2 commits strictly ahead of the pin.

Disposition: verification-only, as scoped -- no Vulkan patch was written (RE30 still gates the first real Vulkan-backend patch; not touched here). Both PRs are real upstream capability that BigCherry's Vulkan baseline does NOT yet include. This is now the documented, verified baseline gap: any future Vulkan lane work (RE30's later phases, or RD55/RD59/RD60/RD65) must either (a) bump the pin past both merges, or (b) explicitly backport them as declared baseline commits -- not silently absorb their effect and credit it to a local optimisation patch. No changes made to RE30 or the RD55/59/60/65 items themselves; this note exists so a future agent working any of them sees the gap before assuming today's Vulkan-stock build already reflects upstream's current Q8_0-KV-FA and transpose behavior.

## Change Log

- 2026-08-20T06:14:33.806242+00:00 (created-by): Created by agent
- 2026-08-20T06:42:48.983167+00:00 (updated-by): Updated (no visible changes)
- 2026-08-23T23:13:05.125495+00:00 (updated-by): Updated: section:notes

## Ledger-events

- chg_20260823_231319_confirmed-two-upstream-vulkan_4130
- 2026-08-23T23:13:19.692093+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-23T23:13:24.312569+00:00 (state-transition): State: pending → completed
