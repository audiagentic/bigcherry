---
id: RD97
order: 0
plan: rdna-boost-experiments
state: completed
created-at: '2026-08-23T12:32:26.848258+00:00'
breadth: ''
skill: intermediate
created-by: agent
work: S
---

# P0: audit AMD PR #75 MMQ tile-reuse barrier race against the pinned tree (structural audit: hazard absent at b10502)

## Description

P0 correctness audit from the 2026-08-23 AMD follow-up handoff: determine whether BigCherry's pinned/materialized llama.cpp contains the shared-tile lifetime hazard AMD fixed in PR #75 (merged 812a47a89991, head 4ea5f294f821). AMD's in-fork evidence: 115/1568 tested MMQ configurations produced nondeterministic wrong results when load_tiles() overwrote shared tile_x while another warp could still be executing the previous iteration's vec_dot; fixed by replacing the elidable mmq_hip_tile_barrier with a full __syncthreads() at both loop sites. AMD reports ~1.3% cost at mmq_x=16, effectively none above 32. The PR's RDNA3.5 tile-width tuning is a SEPARATE performance change and is out of scope for this item.

STRUCTURAL AUDIT ALREADY DONE at baseline d76bdc2 (pin b10502 + patches, vendor + patch inspection):
- No mmq_hip* symbols anywhere in vendor/llama.cpp/ggml/src/ggml-cuda or in any patch -- the fork's elidable barrier does not exist in the materialized tree.
- The only mul_mat_q_process_tile K loop (ggml/src/ggml-cuda/mmq.cuh ~line 899-937) is: load_tiles(x, tile_x, ...) -> tile_y write -> __syncthreads() -> vec_dot(..,0) -> __syncthreads() -> tile_y write -> __syncthreads() -> vec_dot(..,MMQ_TILE_NE_K) -> __syncthreads(). The final full-block __syncthreads() at every iteration end means no warp can reach the next iteration's load_tiles before all warps have passed the last vec_dot. The hazard requires an elidable barrier; upstream's is not elidable.
- Patches touching mmq* files (0300 forced_J, 0700 counters, 1000, 1100, 1203, 1204) do not edit the process_tile K loop; 1203's mmq.cuh edit is the J_max env override only.

Working conclusion: ALREADY FIXED / NOT APPLICABLE at the current pin. Expected disposition: close as baseline/already-fixed with this structural record after the materialized-build confirmation below. If the materialized build contradicts the structural audit (e.g. a patch changes the barrier at build time in a way the text inspection missed), materialize a barrier-only patch as patches/1233_rd97_mmq_tile_reuse_barrier.py (1233 is free at d76bdc2 -- re-list first) with the exact anchor regenerated from the materialized source.

## Steps

- Confirm on a fully materialized tree (bigcherry pull + apply on the b10502 pin): grep the materialized mmq.cuh for the K-loop barrier sequence and for any mmq_hip* symbol; verify the __syncthreads() after the last vec_dot is present at the only loop site.
- Confirm no patch in the applied set rewrites the process_tile loop (patch-graph inspection; 1203's mmq.cuh edit is J_max only at baseline).
- If confirmed hazard-free: close this item as baseline/already-fixed, recording pin b10502 and the structural audit; no patch is carried.
- Only if the hazard surfaces in the materialized tree: materialize a barrier-only patch (1233 if still free) with anchors regenerated from the materialized source; NEVER include the PR's RDNA3.5 tile-width tuning.
- Re-audit on every future pin bump via the RD95 ancestry/redundancy gate (upstream could restructure the loop).

## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files

- vendor/llama.cpp/ggml/src/ggml-cuda/mmq.cuh
- vendor/llama.cpp/ggml/src/ggml-cuda/mmq-load-tiles.cuh
- patches/catalog.toml (only if a patch is materialized)
- config/external-sources.toml (tracked row already added: 812a47a89991, evidence-only)

## Validation

Materialized build: bigcherry apply on the b10502 pin succeeds; the materialized mmq.cuh shows the full __syncthreads() at the end of the only K loop and zero mmq_hip* symbols. If (and only if) a hazard is found: direct MUL_MAT + MUL_MAT_ID repeated fixed-seed runs across narrow-tile (mmq_x<=32) multi-warp configs are stable across repeats pre/post patch, and all locally available supported HIP targets (gfx1100/gfx1201/gfx1030) compile. Closure as already-fixed requires only the first two validation lines.

## Effort & Risk

S. The structural audit points to a no-patch closure; the residual risk is that text-level inspection of vendor+patches missed a materialization-time transformation. One materialized-build confirmation pass eliminates it. No performance selector is in scope.

## Standards



## Acceptance Criteria



## Notes

Ordered FIRST by the handoff for a reason: a tuner can correctly measure a candidate that is nondeterministically wrong, poisoning candidate timing, winner promotion, generalisation evidence, hostile-routing comparisons, and replay caches. Resolve before treating new MMQ timing evidence as promotable. Handoff source: BigCherry_AMD_Followup_Agent_Handoff_2026-08-23.md (assessed 2026-08-23 against d76bdc2). Provenance: external-sources.toml tracked row 812a47a89991 (PR #75 merge), status evidence-only.

RESOLVED 2026-08-23 (branch head 58e2255, pin b10502): hazard ABSENT from pinned baseline -- NO patch carried. Terminal state completed (the correctness audit was performed and produced "no patch required"; not deprecated, which would wrongly imply the item went obsolete).

The AMD PR #75 hazard (elidable pre-load barrier letting one warp overwrite shared tile_x while another warp is still executing the previous iteration's vec_dot) is absent from BigCherry's pinned baseline b10502:

1. No alternate path. Zero mmq_hip* symbols anywhere in the materialized ggml-cuda tree, and NO alternate HIP/MMQ process-tile implementation or barrier abstraction exists in the materialized tree -- the fork's elidable mmq_hip_tile_barrier does not exist here. Exactly ONE mul_mat_q_process_tile definition (mmq.cuh:868; the other 3 references are call sites).

2. The single K loop is non-elidable. Body: load_tiles(x, tile_x, ...) -> tile_y write -> __syncthreads -> vec_dot(..,0) -> __syncthreads -> tile_y write -> __syncthreads -> vec_dot(..,MMQ_TILE_NE_K) -> __syncthreads. The final full-block __syncthreads() at EVERY iteration end means no warp can reach the next iteration's load_tiles before all warps have passed the last vec_dot. #75's overwrite-before-consumption race requires an ELIDABLE barrier; upstream's is a plain __syncthreads() (not elidable).

3. No patch clobbers the loop. The only patches touching mmq.cuh are 0300 (J-forcing case dispatch) and 1203 (GGML_CUDA_MMQ_J_MAX env override in ggml_cuda_mmq_get_J_max); neither references load_tiles / vec_dot / kb0_start / the process_tile loop body.

4. Materialized-build confirmation (bigcherry apply --dry-run --force, groups=all states=all): mmq.cuh / mmq.cu / mmq-vec-dot.cuh all ok with ZERO anchor drift; the single overall FAIL is the documented, intentional RD17/RD12 common.cuh struct-slot conflict (rd17-struct-host/device; unrelated to MMQ, pre-existing per the HI81 ledger note).

Continuing gate: re-audit on every future pin bump via the RD95 ancestry/redundancy gate (upstream could restructure the loop). AMD #75's in-fork evidence (115/1568 MMQ configs nondeterministically wrong pre-fix) stays evidence-only in external-sources.toml (812a47a89991) for the RD30 experiment path; its RDNA3.5 tile-width tuning stays excluded from any correctness change.

External review: dev-gpt-agent req_ffff89ef1dbe4a01 -- VERDICT AGREE-WITH-CHANGES (conclusion sound); the three wording/scope refinements above (explicit no-alternate-implementation sentence, "hazard absent from pinned baseline" phrasing, completed state + RD95 gate) are incorporated.

RE-AUDITED 2026-08-30 at pin b10680 (per this item's own "re-audit on every future pin bump" instruction, and gpt's HI154 sources-check triage flagging this as the highest-priority stale conclusion since the original was explicitly bound to b10502). Hazard CONFIRMED STILL ABSENT -- no patch carried, conclusion unchanged.

1. Zero `mmq_hip*` symbols anywhere in vendor/llama.cpp/ggml/src/ggml-cuda at b10680 (grep -rn, real count 0) -- same as b10502.
2. mul_mat_q_process_tile (mmq.cuh:874, shifted from :868 at b10502 -- line drift only, same one-definition/three-call-site shape) K loop body read directly: load_tiles(...) -> tile_y write -> __syncthreads() -> vec_dot(..,0) -> __syncthreads() -> tile_y write -> __syncthreads() -> vec_dot(..,MMQ_TILE_NE_K) -> __syncthreads() -- identical structure, still a plain non-elidable __syncthreads() at every iteration's end before the next load_tiles.
3. Patches touching mmq.cuh at b10680: 0300 (J-forcing, same as before), 1203 (J_max env override, same as before), and ONE NEW ONE since the original b10502 audit -- 1237_rd30_moe_mmq_compact_grid (RD30, group=rdna-boosts, ported-untested). Verified by reading its anchors directly: it only edits mul_mat_q's outer kernel signature and the block-index derivation (wt/zt/jt/it -- which expert/tile a block handles) that runs BEFORE calling mul_mat_q_process_tile; it references load_tiles/vec_dot/tile_x/tile_y nowhere and does not touch the process_tile loop body at all. Also confirmed 1237 applies cleanly (dry-run, all 5 edits 'applied') against the current b10680 vendor tree, so this isn't a stale/broken patch being waved through untested.

Conclusion unchanged: hazard absent, no patch required, at b10680. Continuing gate (re-audit on every future pin bump) still applies -- this note is that re-audit's record for this bump.

## Change Log

- 2026-08-23T12:32:26.848258+00:00 (created-by): Created by agent

## Ledger-events

- chg_20260823_123928_reviewed-an-external-amd-optim_4813
- 2026-08-23T12:39:28.128131+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-23T12:58:48.513890+00:00 (updated-by): Updated: section:notes
- 2026-08-23T13:02:36.425887+00:00 (state-transition): State: pending → completed
- chg_20260823_130251_answered-the-p0-question-the_1425
- 2026-08-23T13:02:51.514629+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-29T20:40:06.796751+00:00 (updated-by): Updated: section:notes
