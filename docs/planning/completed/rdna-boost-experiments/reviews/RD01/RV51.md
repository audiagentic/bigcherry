---
id: RV51
review-of: RD01
plan: rdna-boost-experiments
state: closed
reviewed-by: agent
reviewed-at: ''
---

# rdna-boosts branch rebase: snapshot v2 (33 commits) supersedes the 23-commit frozen ledger

## Notes

The stew675/llama.cpp rdna-boosts branch moved between the 2026-08-15 review and today. Read-only clone re-audit (2026-08-18) findings:

## Findings

1. REBASED: the branch was rebased onto mainline base 4df29be4f (was a94d563). v1 head ed89854 is NOT an ancestor of the new head 9e46e1fd. Our pin 4801e3c is 92 commits below the new base (was 61), but ALL ggml-cuda kernel files remain byte-identical between pin and new base; only ggml-cuda.cu (+41/-5), src/llama.cpp (+16/-5), src/llama-model.cpp (+32/-3) drifted. Semantic-port burden unchanged.
2. CONTENT-IDENTICAL: git patch-id on all 23 v1-vs-v2 pairs matches byte-for-byte -- every planned RD port remains valid on content; only the commit SHAs changed.
3. TEN NEW COMMITS (v2-only): (a) 4a4da30ef 'fuse the SSM pre-scan chain (conv, l2_norm, gate/beta)' is ONE 16-node kernel that strictly subsumes RD14's 9-node gate/beta pattern and RD16's 3-node L2-norm-pair pattern (verified by op-pattern inspection) -- RD14/RD16 are superseded, not complementary; (b) 8cdf1ab08 'fix MTP batch-vs-seq consistency for fused kernels' is a CORRECTNESS fix touching mmvq.cu -- the frozen RD12 and RD21 are known-buggy without it (porting them as-planned would be unneeded/wrong work); audit RD08; (c) 5 'make decode/verify batches bit-identical' commits form a determinism/correctness cluster (RD26); (d) 0510d7cfa 'fold SSM conv_input concat into qkv mmvq; rpb=2 small-K MoE' is a new candidate (RD27); (e) 2 'adaptive MTP draft depth' commits are an MTP speculative-decoding FEATURE -- excluded from the RDNA port scope, declared in the registry.
4. MAINLINE DEDUP: git cherry against ggml-org master tip 6d0549831 (2026-08-18): 0 of the 33 commits merged upstream -- none are 'free on a pin bump' as of this audit.
5. TRACKING MACHINERY LANDED (this review's companion work): external-sources.toml registry (snapshots v1 historical + v2 active, all 33 commits tracked with statuses), PROVENANCE dicts in patch modules cross-checked by tools/tests/test_external_sources.py, and `python -m bigcherry sources status|check` (check = online branch-moved/rebased/merged-to-mainline/content-drift report). RD19 and RD20 are the first materialized isolated-test patches (1200/1201, group rdna-boosts, STATE=untested), both ported verbatim with anchors verified byte-identical on pin 4801e3c; isolated benches pending on Brutus.

## Conclusion

Incorporated: RD01 notes updated with snapshot v2 as the active provenance (v1 retained historical); RD14 and RD16 transitioned to superseded (successor RD24); RD24/RD25/RD26/RD27 created for the new commits; RD12/RD21 notes now require 8cdf1ab08; RD19/RD20 notes record the 1200/1201 port. The 23-item ledger below remains the v1 record and is not rewritten.
