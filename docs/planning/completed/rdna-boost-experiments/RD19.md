---
id: RD19
order: 19
plan: rdna-boost-experiments
state: completed
created-at: '2026-08-15T03:36:14.907618+00:00'
breadth: ''
skill: advanced
created-by: agent
work: M
---

# Evaluate single-GPU Meta wrapper bypass

## Description

Future runtime/topology experiment from a6e774d that bypasses the Meta device wrapper only for a single GPU.

## Steps

Post-port (the port itself is done -- see Notes):
1. [DONE 2026-08-19] Verbatim port to patches/1200_rd19_single_gpu_meta_bypass.py (GROUP=rdna-boosts, STATE=untested); anchor and applied-region byte-identity vs fork commit 3c48ecd63 verified; idempotent re-apply verified.
2. Isolated bench on one FREE Brutus device (do NOT take the two XTXs -- held by llama-server per standing instruction; device 2 gfx1201 or device 3 gfx1030 is valid: the change is architecture-independent runtime topology, gfx1100 is not required). Arms: bigcherry-native baseline vs baseline+1200, single-GPU tensor-split workload so the Meta wrapper would engage without the patch.
3. Record: model output equality between arms; decode/prefill timing delta; graph-compute call count with vs without the wrapper; two-GPU run proving no behavior change.
4. Decide and record: pass -> flip 1200 STATE and update external-sources.toml status, then (and only then) consider composition with other RD patches; fail/revert -> record the evidence and set the registry status accordingly.

## Detailed Solution & Technical Design

The wrapper bypass is a topology optimization, not a generic removal. It must not alter multi-GPU dispatch or cache reset semantics.

## Code Samples & Guidance



## Files

Future runtime/topology source; RD19 tests/evidence

## Validation

One-GPU tensor split, plain-device path, unchanged multi-GPU, graph calls, RD09 interaction, end-to-end decode, no behavior change with two GPUs.

## Effort & Risk

High runtime/topology risk, especially with graph and cache lifetime.

## Standards

Architecture/topology gating, multi-GPU preservation, causal arms.

## Acceptance Criteria

Isolated bench passes on a free Brutus device: (1) identical model output baseline vs +1200; (2) two-GPU runs show no behavior change (patch is single-GPU-gated); (3) graph capture unbroken and call-count/timing delta recorded; (4) patch STATE stays untested until then and is flipped only with the bench evidence recorded in this item + external-sources.toml status updated.

## Notes

Planning-only. If RD09 lands use B, B+RD09, B+RD19, B+RD09+RD19.

PORTED as patches/1200_rd19_single_gpu_meta_bypass.py (2026-08-18). Source: snapshot-v2 commit 3c48ecd63 (v1 a6e774d41), ported VERBATIM -- both hunks anchor byte-identical on pinned llama.cpp 4801e3c, verified region-byte-identical to the fork commit file and idempotent on re-apply. GROUP=rdna-boosts, STATE=untested: excluded from all production patch-sets until the isolated bench passes. Isolated test: bigcherry-native (framework) bench vs framework+this-patch, single-GPU tensor split (Meta wrapper only engages with -sm tensor); Bruntus device 0 or 1 (gfx1100). Registry: external-sources.toml (status=ported-untested); provenance in the patch module; tracking via `python -m bigcherry sources check`.

Planning-only. If RD09 lands use B, B+RD09, B+RD19, B+RD09+RD19.

PORTED as patches/1200_rd19_single_gpu_meta_bypass.py (2026-08-18). Source: snapshot-v2 commit 3c48ecd63 (v1 a6e774d41), ported VERBATIM -- both hunks anchor byte-identical on pinned llama.cpp 4801e3c, verified region-byte-identical to the fork commit file and idempotent on re-apply. GROUP=rdna-boosts, STATE=untested: excluded from all production patch-sets until the isolated bench passes. Isolated test: bigcherry-native (framework) bench vs framework+this-patch, single-GPU tensor split (Meta wrapper only engages with -sm tensor); Bruntus device 0 or 1 (gfx1100). Registry: external-sources.toml (status=ported-untested); provenance in the patch module; tracking via `python -m bigcherry sources check`.

CORRECTION (2026-08-19): the bench device guidance above is superseded by Steps -- the two XTX GPUs are held by llama-server per standing instruction, so run on a free device (gfx1201/gfx1030); the change is arch-independent, so gfx1100 is not a requirement.

2026-08-24 (this session): ran the isolated bench this item's own acceptance criteria require. Real hardware (Brutus, SSH-accessible at 10.10.100.10), isolated `git clone --local` copies (not the shared checkout another concurrent session was actively using for its own RD73 work at the same time) so as not to disturb anyone. Three arms, identical pinned llama.cpp commit 0adcc3b: base (zero BigCherry patches), control (BigCherry core only), treatment (core + patch 1200/RD19).

**Single-GPU** (device 2, gfx1201 R9700, HIP_VISIBLE_DEVICES=2, `-sm tensor`, Qwen3.5-0.8B.Q8_0.gguf, 5 reps each via llama-server /completion, seed=42/temp=0/deterministic): base mean 197.24 t/s, control mean 197.56 t/s (matches base -- confirms BigCherry's core patches don't change native-path behavior), treatment mean 215.24 t/s (**+8.95% vs control**). Zero overlap across all 5x5 samples (control max 197.94 < treatment min 212.21). All 15 responses across all three arms byte-identical (verified programmatically).

**Two-GPU** (devices 0+1, gfx1100 XTX pair, RCCL-enabled rebuild after an initial segfault traced to a build-config gap on my end -- wrong AMDGPU_TARGETS + missing GGML_HIP_RCCL, NOT an RD19 defect, confirmed by control segfaulting identically before the fix): output byte-identical between control and treatment. First single-sample reading (control 157 t/s vs treatment 193 t/s) was a cold-start artifact from the first request after server startup; 3 warmed reps each afterward overlap heavily (control 186.8/196.9/197.6, treatment 194.0/197.0/195.2) -- no real treatment effect, exactly as expected since both patched hunks branch on n_devices==1 only and leave the n_devices>1 Meta path untouched.

**dev-gpt-agent review** (fresh session, real-hardware-evidence review not a design conversation, req_40e2bb69b218488f): verdict **HOLD** -- narrow, not a rejection. Confirmed cleared: single-GPU performance signal (emphatically -- clean causal arms, base/control agreement rules out BigCherry-framework-layer artifacts, small model is a legitimate mechanism probe that exaggerates the exact fixed per-launch overhead RD19 targets, not a promotion blocker); gfx1201 substitution (explicitly sanctioned by this item's own prior correction, arch-independent change); two-GPU functional preservation (structural argument from the source -- both hunks gate on n_devices==1 -- plus the overlapping warmed timing and byte-identical output). NOT cleared: acceptance criterion 3, 'graph capture unbroken and call-count/timing delta recorded' -- the throughput result is consistent with the claimed mechanism (Meta wrapper multiplying per-token ggml_backend_graph_compute_async() calls across subgraphs) but doesn't distinguish it from an adjacent mechanism or an unintended graph-capture/replay side effect, which matters because this item's own risk assessment flags graph/cache lifetime as high-risk. Reviewer's bounded ask before promotion: instrument one deterministic control request and one treatment request, record the actual underlying HIP graph-compute call count (or Meta subgraph count) for each, confirming Control multiplies calls and Treatment doesn't, and that graph capture/replay stays active in both. Explicitly NOT required again: another 5-rep throughput campaign (already sufficient), a gfx1100 rerun of the single-GPU test, or resolving RD09 composition (real but future, RD09 hasn't landed).

**Status: HOLD, not promoted this session.** Patch 1200 STATE remains 'untested' pending the graph-call-count instrumentation above -- do not flip to 'validated' or add to recipes.toml's validated-enhancements until that bounded check is done. This is the single remaining acceptance gate; per the reviewer, clearing it moves the verdict directly to PROMOTE with no further campaign needed.

All Brutus scratch build directories and test artifacts cleaned up after this session's work (disk usage restored).

**PROMOTED 2026-08-24.** Patch 1200_rd19_single_gpu_meta_bypass STATE=validated; added to config/recipes.toml's [patch-set.validated-enhancements]; external-sources.toml status updated to "validated".

Ported as patches/1200_rd19_single_gpu_meta_bypass.py (2026-08-18). Source: snapshot-v2 commit 3c48ecd63 (v1 a6e774d41), ported VERBATIM -- both hunks anchor byte-identical on pinned llama.cpp 4801e3c, region-byte-identical to the fork commit and idempotent on re-apply. Both hunks in llama_prepare_model_devices gate strictly on n_devices==1; multi-GPU Meta construction is structurally untouched.

**Full evidence trail (real Brutus hardware, all this session):**

1. Single-GPU synthetic causal bench (device 2, gfx1201 R9700, `-sm tensor`, Qwen3.5-0.8B.Q8_0, 5 reps/arm, deterministic seed=42/temp=0): base (pristine upstream) 197.24 t/s, control (BigCherry core) 197.56 t/s, treatment (core+RD19) 215.24 t/s -- **+8.95% vs control**, zero sample overlap across 5x5 reps, all 15 responses byte-identical.
2. Two-GPU functional-preservation check (devices 0+1, gfx1100 XTX pair, RCCL): output byte-identical control vs treatment; warmed timings overlap heavily (no treatment effect), exactly as expected since the patch only branches on n_devices==1.
3. Scheduler-split evidence (`GGML_SCHED_DEBUG=1 -lv 5`): control's compute split assigned to `Meta(ROCm0)`, treatment's assigned directly to `ROCm0` -- confirms the claimed mechanism at the outer ggml-scheduler level.
4. **Meta-internal subgraph probe** (temporary diagnostic print in a scratch build of ggml-backend-meta.cpp, single GPU, `-sm tensor`, Qwen3.8-27B-Q4_K_M): control (BigCherry core, no RD19) produces `n_subgraphs=129, n_backends=1` for a 3654-node graph -- i.e. 129 separate `ggml_backend_graph_compute_async()` calls per eval from the trivial 1-member Meta wrapper alone, vs RD19's single direct call. Stable across repeated identical requests (not noise).
5. **HIP graph capture-positive confirmation** (RD19 Treatment binary, single GPU, `-sm tensor`, RD73's known-capture-positive Qwen3.5-4B MTP workload, `-lv 5`): log shows `CUDA graph warmup complete` followed by repeated `CUDA Graph id N reused` -- capture/replay is active and healthy under the RD19 direct single-GPU path, not broken by removing the Meta layer.
6. **Production-scale dual-XTX 3-arm bench** (real Qwen3.8-27B-Q8_0 with MTP draft decode, `-sm tensor --tensor-split 1,1`, production flags, 5 reps/config across pp256/pp1024/pp4096/tg512/tg2048): base/control/treatment all matched within run-to-run noise -- confirms RD19 is a true no-op under the real 2-GPU tensor-split production config, as expected.

**dev-gpt-agent review** (session ses_866bf44313864664): first pass (req_40e2bb69b218488f) returned HOLD pending direct call-count/graph-capture evidence for acceptance criterion 3 (narrow, not a rejection -- everything else already cleared). Second pass (req_32a3a13712414049), after scheduler-split evidence, still HOLD: the outer-scheduler split doesn't capture Meta's own internal subgraph fan-out, which is one layer deeper. Third pass (req_39ecb34a29ed45fb), after the n_subgraphs=129 probe plus the dual-XTX production bench: HOLD narrowed to one final point -- direct confirmation that HIP graph capture still activates under the RD19 Treatment path (RD19 changes the cgraph shape presented to HIP, so capture eligibility could in principle change even though RD19 doesn't touch ggml-cuda.cu). Fourth pass (req_7e438dc45ca84554), after the capture-positive confirmation: **PROMOTE**, no further evidence required -- call-count gate (129->1) and two-GPU production safety both pass conclusively, capture is confirmed active, and requiring anything further would move the acceptance bar beyond RD19's written criteria.

If RD09 lands, compose B / B+RD09 / B+RD19 / B+RD09+RD19 as a follow-up (RD09 itself is unstarted and out of scope for this item).

All Brutus scratch build/test directories cleaned up after this session's work.

## Change Log

- 2026-08-15T03:36:14.907618+00:00 (created-by): Created by agent

## Ledger-events


- chg_20260815_033735_added-a-complete-evidence-gat_9636
- 2026-08-15T03:37:35.485495+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-19T04:18:12.121087+00:00 (updated-by): Updated: section:notes
- chg_20260819_041847_re-audited-the-external-rdna-b_4893
- 2026-08-19T04:18:47.549330+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-19T05:00:56.710906+00:00 (state-transition): State: pending → in_progress
- 2026-08-19T05:06:01.784343+00:00 (updated-by): Updated: section:acceptance_criteria, section:steps, section:notes
- 2026-08-23T19:50:59.311384+00:00 (updated-by): Updated: section:notes
- chg_20260823_195112_ran-the-required-real-hardware_4704
- 2026-08-23T19:51:12.272075+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-23T22:49:33.726499+00:00 (updated-by): Updated: section:notes
- chg_20260823_225230_enabled-a-small-dispatch-optim_7295
- 2026-08-23T22:52:30.064382+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-23T22:52:36.202020+00:00 (state-transition): State: in_progress → completed
