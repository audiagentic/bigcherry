---
id: VA20
order: 0
plan: validation-package-standard
state: completed
created-at: '2026-08-31T07:27:50.683423+00:00'
breadth: ''
skill: intermediate
created-by: agent
work: S
---

# VA08A: resolve pin to immutable SHA in cmd_patch_verify_evidence()

## Description

Isolated slice of VA08 per GPT's reprioritization (req_1c131ba025834afe): cli/patch.py::cmd_patch_verify_evidence() previously passed only pinned_ref=cfg.pinned (the tag string) to validation_evidence_statuses(), even though the underlying evidence verifier already supports resolved_base_revision matching. Fixed by resolving the configured pin to its real commit SHA via source.workspace.UpstreamRepository(paths.llama_root()).resolve_ref(cfg.pinned) (local git rev-parse, no fetch) and passing it through. If the configured pin cannot resolve locally, patch-verify-evidence now fails closed with a real CLI error (exit 2) instead of silently omitting resolved-SHA freshness checking.

## Steps



## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files



## Validation

2 new tests (mocked UpstreamRepository): resolved SHA is correctly threaded into validation_evidence_statuses()'s resolved_base_revision kwarg; an unresolvable pin (WorkspaceError) fails closed with exit 2 and never calls validation_evidence_statuses(). Full patch suite: 495 tests (was 493), all pass.

## Effort & Risk



## Standards



## Acceptance Criteria



## Notes

Kept strictly freshness-only, per GPT's explicit scoping -- no other cmd_patch_verify_evidence behavior touched. Real-repo smoke test: `bigcherry patch-verify-evidence 1204_rd08_q6k_mmvq_vdr2` resolves the local llama.cpp pin successfully and returns not-required (correct for an untested-state patch).

Kept strictly freshness-only, per GPT's explicit scoping -- no other cmd_patch_verify_evidence behavior touched. Real-repo smoke test: `bigcherry patch-verify-evidence 1204_rd08_q6k_mmvq_vdr2` resolves the local llama.cpp pin successfully and returns not-required (correct for an untested-state patch).

AUDITED AND CLOSED (2026-09-01, GPT session ses_8a915986b0e64312): GPT flagged this item as appearing already implemented but left pending -- verified directly against the real repo. tools/bigcherry/cli/patch.py::cmd_patch_verify_evidence() (line ~262) already calls UpstreamRepository(paths.llama_root()).resolve_ref(cfg.pinned) and threads resolved_base_revision through to validation_evidence_statuses(), exactly as this item specifies. tools/tests/patch/test_cli_patch_verify_evidence_resolved_pin.py's 2 tests (resolved SHA threaded correctly; unresolvable pin fails closed with exit 2) both pass against the real current code. No new implementation needed -- this was metadata drift, not missing work.

## Change Log

- 2026-08-31T07:27:50.683423+00:00 (created-by): Created by agent
- 2026-09-01T02:28:46.044124+00:00 (updated-by): Updated: section:notes
- 2026-09-01T02:28:48.751665+00:00 (state-transition): State: pending → completed
