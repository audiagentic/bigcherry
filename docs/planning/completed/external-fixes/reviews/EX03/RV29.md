---
id: RV29
review-of: EX03
plan: external-fixes
state: closed
reviewed-by: gpt-auto
reviewed-at: ''
---

# GPT-auto fail-closed review at 6d73a91

## Notes

Fresh persistent GPT-auto review of branch tuning-code-rebase at 6d73a91 using the pushed branch, original runbook context, EX03 plan item, recent Brutus gfx1201 clean-overlay q1_0 evidence, and focused/full Python validation.

## Findings

The EX03 implementation is materially safer and the clean Brutus run is useful, but three bounded fail-closed gaps remain: host_sync_overhead_us HIP event/timing failures do not poison the tuner; raw correctness-reference/candidate copies and stream synchronization are outside the poison state machine; and replay export can accept a measurement_failure=true native row because the promotion gate focuses on candidate winner safety. The process-wide poison does not invalidate already cached dispatch bindings, which should remain an explicit scope distinction pending a reproduced fault. Current tests are predominantly structural, so deterministic runtime fault-injection coverage is recommended.

## Conclusion

Keep EX03 in_progress. Implement the three bounded fail-closed holes, add focused tests and stale-documentation correction, then rerun validation. Do not quarantine q1_0 or broaden dispatcher architecture without a reproduced fault.
