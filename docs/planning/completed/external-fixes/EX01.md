---
id: EX01
order: 0
plan: external-fixes
state: completed
created-at: '2026-08-11T23:57:22.261392+00:00'
breadth: ''
skill: intermediate
created-by: agent
work: M
---

# 1005_prompt_cache_checkpoint_selection anchors broken -- need re-derivation against real source

## Description

patches/1005_prompt_cache_checkpoint_selection.py (an upstream backport of a squashed 4-commit fix for hybrid/recurrent memory checkpoint selection and prompt-cache entry selection, PR #24055 + follow-ups) was reconstructed from a session transcript after an external `git reset --hard` wiped uncommitted work on 2026-08-11/12. It parses and loads fine, but a dry-run `patcher.apply_all()` against the real pinned vendor tree (b10362) shows all 4 edits in `tools/server/server-context.cpp` and `tools/server/server-task.cpp` fail to match: `anchor ... matched 0 time(s), expected 1`.

Root cause confirmed by reading patcher.py's own module docstring: 'No anchoring into comments. Anchors are matched against a noise-stripped copy of the file with comments and string literals blanked out.' The failing anchors in 1005 include full `//` comment lines (e.g. `// n_tokens_cur: the number of tokens added...`, `// evict checkpoints within min-step...`) and `SLT_TRC(slot, "...")` string-literal log text as part of the regex match text -- both categories are blanked in the stripped copy the patcher actually matches against, so these anchors can structurally never match, regardless of whether the real source still has the described code. This is not a source-drift issue; it's a design/reconstruction defect in the anchor text itself (contrast with 1002_hip_unsafe_math_opt_in.py, which explicitly documents avoiding this exact mistake).

Verified directly: `sed -n '2354,2358p' vendor/llama.cpp/tools/server/server-context.cpp` shows the described `create_checkpoint` code is present, byte-identical to what the docstring/comments in 1005 describe -- so the underlying fix content and rationale in the docstring are sound. Only the anchor regexes need to be redone to key off non-comment, non-string code tokens (following the pattern already used successfully in 1002 and the other 10 core patches).

Other 4 recovered patch files in this batch (0900_pool_workspace_metrics, 1000_rdna4_mmq_q2k_q6k_fix, 1002_hip_unsafe_math_opt_in, 1003_quantized_cpy_thread_block_fix) all apply cleanly (dry-run verified) against the pinned b10362 vendor tree.

## Steps

1. Read the real, current tools/server/server-context.cpp and tools/server/server-task.cpp at the pinned ref (b10362) around the 4 failing edit sites.
2. Re-derive each anchor regex to key off non-comment, non-string code (e.g. the function signature line, a preceding/following control-flow token, a unique variable declaration) rather than comment prose or SLT_TRC string literals -- matching the convention already used in 1002 and the core patches.
3. Re-run `patcher.apply_all(patches, root, dry_run=True)` (or `python -m bigcherry apply --dry-run --groups upstream-fixes`) and confirm all 4 edits in this file report `ok`.
4. Leave STATE as 'untested' until an actual (non-dry-run) apply + build + server-side test of the checkpoint-selection behavior has been done; do not promote to 'validated' on anchor-matching alone.

## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files

- patches/1005_prompt_cache_checkpoint_selection.py
- tools/tests/test_patcher.py
- tools/server/server-context.cpp — clean pinned overlay applied and built
- tools/server/server-task.cpp — clean pinned overlay applied and built
- `/tmp/bc-ex01-clean` and `/tmp/bc-ex01-build` on Brutus — validation worktrees
- real tokenized Qwen3.5-4B model used only as Brutus test input

## Validation

Offline: at fd8e0f7, `$env:PYTHONPATH='tools'; python -m bigcherry apply --dry-run --groups upstream-fixes` reports all 8 patch files [ok], including 7 already-applied server-context edits and 3 already-applied server-task edits, with 0 anchor failures; `python -m pytest tools/tests/test_patcher.py` passes 12/12. Brutus clean pinned overlay: detached worktree `/tmp/bc-ex01-clean` from vendor revision `4801e3c567d5`; real apply writes 27 overlay files and applies 2 mmq, 1 HIP CMake, 7 server-context, and 3 server-task edits, with all other patch hunks already present; isolated CPU build `/tmp/bc-ex01-build` links `llama-server`, `test-llama-archs`, and `test-recurrent-state-rollback`. `ctest --test-dir /tmp/bc-ex01-build -R test-recurrent-state-rollback --output-on-failure` passes model generation and recurrent rollback (2/2). The generated Qwen35 fixture direct test passes, and the real tokenized Qwen3.5-4B-UD-Q4_K_XL model was hash-verified before transfer (SHA256 d2bfbee4de17c74e6308a4dc750be4c2271f92ae2df1e28f5c5afa0dcdb6fccc). With the clean overlay server on Brutus, `--ctx-checkpoints 8 --checkpoint-min-step 0 --cache-ram 128 --cache-idle-slots --kv-unified` completed the divergent three-request sequence without crash: first prompt `cache_n=0,prompt_n=211`, unrelated second prompt `cache_n=0,prompt_n=10`, repeated first prompt `cache_n=210,prompt_n=1`; server log records `created context checkpoint`, idle-slot cache save/update, `restored context checkpoint`, and no assertion/error. Full local Python suite passes 506/506.

## Effort & Risk



## Standards



## Acceptance Criteria



## Notes

EX01 closure gate is satisfied. The reconstructed upstream-fixes patch applies on a pristine pinned b10362 overlay, the affected server target builds, the formal recurrent checkpoint test passes, and a real tokenized Qwen3.5-4B server run proves checkpoint creation/restoration plus prompt-cache reuse across a divergent request. The generated no-vocabulary Qwen35 fixture is retained only as the deterministic CTest fixture; the production-like server proof uses the hash-verified real model. Preserve unrelated release artifacts on both checkouts.

## Change Log

- 2026-08-11T23:57:22.261392+00:00 (created-by): Created by agent

## Ledger-events

- chg_20260813_155833_updated-ex01-with-current-gree_6348
- 2026-08-13T15:58:33.757577+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-13T16:15:27.647808+00:00 (updated-by): Updated: section:validation, section:notes, section:files
- 2026-08-13T16:15:30.310756+00:00 (state-transition): State: pending → completed

## Ledger-events

- chg_20260813_161541_closed-ex01-with-pristine-over_4172
- 2026-08-13T16:15:41.502591+00:00 (updated-by): Updated: section:ledger-events
