---
id: RV139
review-of: HI168
plan: hip-autotune
state: closed
reviewed-by: ''
reviewed-at: ''
---

# Bind companion parity to the actual compiled generated-input copy

## Notes



## Findings

GPT review req_7b59ce770e694578 confirmed the worker verified run-scoped generated_root but CMake compiled build_dir/generated-inputs. Original-tree hashes could therefore be reported despite copy mutation. Companion option parity also compared requested BuildPlan.cmake_options, not observed compiler state.

## Conclusion

Verify copied inputs immediately before configure, after configure before compile, and after compile; recompute actual compile-input digest, record compiled-copy-v1 in build metadata/runtime bundle, reject reuse of historical unverified generated builds. Companion validation requires matching build-bound evidence and recomputed input hash. Describe option check strictly as requested CMake-option parity. No hardware parity claim.
