---
id: RV79
review-of: HI97
plan: hip-autotune
state: closed
reviewed-by: dev-gpt-agent (external GPT dev review)
reviewed-at: ''
---

# Post-implementation review (be9590f): pin artifact_version, type-validate flat optionals

## Notes

External adversarial review of the HI97+HI101 implementation commit be9590f, conducted by the dev-gpt-agent (GPT dev) per the agreed review protocol. First full-diff attempt (req_4465d00d7a084a63) failed on a provider rate-limit timeout with no captured findings; the slimmed retry (req_61ce779939fa4b66) delivered the verdict.

## Findings

1. _validate_runtime_header accepted any positive artifact_version but parsed the v1 layout and labeled records runtime-flat-v1 -- a future schema version with a changed shape would be silently mis-read. Fix: require exactly 1 (bool rejected); future versions fail closed.
2. Flat optional fields were str()-coerced or truth-tested: rejection_reason/tried reasons coerced via str() (false -> 'False', 5 -> '5'); present nulls and other malformed values dropped or coerced instead of rejected. Fix: new _optional_text() helper -- absent legal, present must be str (null/number/bool rejected loudly), the writer's empty-string sentinel normalizes to None; est_bytes when present must be a real integer (present null is malformed, not absent).

## Conclusion

Both findings fixed in 023951e with focused negative tests (future-version header rejection; 9 malformed-optional cases asserting the offending field name in each error). Full offline suite 1805 passed, 1 skipped, 61 subtests. Re-verification by the same reviewer (dev-gpt req_a433393f890b4574): 'HI97: CLOSE; HI101: CLOSE ... both prior HI97 blockers are correctly resolved and tested, with no HI101 regression surface.'
