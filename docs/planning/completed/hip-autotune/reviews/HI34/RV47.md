---
id: RV47
review-of: HI34
plan: hip-autotune
state: closed
reviewed-by: gpt-auto-agent (ses_0b27e98b49bc4e9f)
reviewed-at: ''
---

# GPT source review of Slice B0 at e2b1920: CHANGES REQUIRED (P0 host-timing contamination, P1 device-scoped eviction buffer) + corrected B1 design

## Notes

New gateway session ses_0b27e98b49bc4e9f (prior session wedged on a provider transport fault; B0 review prompt resent self-contained). Verdict: CHANGES REQUIRED. P0: launch_cache_evict only enqueued the eviction kernel and checked hipGetLastError; host_start is taken immediately after, and the final hipEventSynchronize(stop) waits for all earlier stream work including the eviction tail, so host_us (and thus effective_us = max(gpu, host - sync overhead)) absorbs the 256 MB eviction: exactly the constant-added-to-both-sides distortion HI34 exists to remove. GPU event interval was clean; host window was not. Required fix applied: checked hipStreamSynchronize(lc.stream) inside launch_cache_evict after the kernel launch, before return true; contract test pins eviction-completion < host_start < hipEventRecord(start). P1: the process-lifetime eviction buffer was one function-static pointer from hipMalloc, which is device-specific; a multi-GPU tuning pass (Brutus runs 4 GPUs) would launch the eviction on GPU B with a pointer allocated for GPU A. Required fix applied: per-device slot storage (ggml_hip_flush_evict_slot vector), current device resolved at the eviction seam via hipGetDevice; contract test forbids the lone static pointer and requires device-keyed storage. B1 design corrected per GPT: three runs H1 (flush=0, lps=1) -> C (flush=1, lps=1, 256MB) -> H2 (flush=0, lps=1); H1<->H2 repeatability is the prerequisite gate (if unstable, B1 inconclusive, do not interpret H vs C); hard pass criterion = zero replicated material winner reversals surviving against both hot replicates, with crossover penalties computed on medians; rank correlation (tau-b) is secondary diagnostic calibrated against the H1/H2 envelope, NOT a fixed 0.95; keep HIP_LAUNCH_BLOCKING off; a positive cold result establishes measurement-context sensitivity but does not by itself promote flush=1 to calibration default -- the eviction intervention must first be validated as the cause (size saturation sweep 128/256/512 on a diagnostic subset, plus DVFS/thermal preconditioning caveat). Implementation note from GPT: compare_tunes.py at this SHA computes Spearman, not Kendall; its rank_correlation is mislabeled if ever cited as Kendall -- fix or rename before B1 gates use it.

## Findings

- P0 (experiment-invalidating): eviction enqueued but not completed before host timing begins; hipEventSynchronize(stop) waits for the eviction tail so host_us and effective_us are contaminated by roughly the remaining 256 MB eviction time. Fix: checked hipStreamSynchronize inside launch_cache_evict before returning true (done), plus a contract test pinning completion < host_start < event-record (done).
- P1 (multi-device correctness): process-lifetime eviction buffer is one function-static hipMalloc pointer; HIP allocations are device-specific, so a later GPU would reuse a foreign pointer (fails outright without peer access; wrong to depend on peer capability for an eviction experiment). Fix: per-device slot storage keyed by hipGetDevice at the seam (done), plus source-contract test forbidding the lone static (done).
- B1 two-arm design upgraded to H1->C->H2 three-run design with H1/H2 repeatability as prerequisite; winner-change hard gate replaces fixed tau 0.95 (tau-b diagnostic only, calibrated against H1/H2 envelope); positive branch tightened: flush=1 not promoted to default until the eviction intervention itself is validated as cause (size saturation, DVFS/thermal preconditioning).
- compare_tunes.py rank_correlation is Spearman at this SHA; any B1 gate citing Kendall must fix or rename the helper first.

## Conclusion

considered
