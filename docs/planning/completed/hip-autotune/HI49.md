---
id: HI49
order: 0
plan: hip-autotune
state: completed
created-at: '2026-08-12T06:53:02.788794+00:00'
breadth: ''
skill: advanced
created-by: agent
work: M
---

# Restore hip-autotune-build-descriptor.json generation in autotune_catalog.py

## Description

__main__.py's GENERATED_IN_TREE constant (from the SE02 recovery pass) lists hip-autotune-build-descriptor.json as a file `generate` writes, but current autotune_catalog.py has no code that actually emits it -- confirmed by direct grep, zero references. This is a dangling assumption left over from SE02, not a verified-working piece. Surfaced while working on [[HI46]] (release_validate.py), whose validate_build_descriptor() function needs this file's descriptor_hash/manifest_hash/candidate_count fields embedded in the compiled binary to prove configured/generated/compiled catalog identity agree -- see old release_validate.py and tools/tests/test_build_descriptor.py (both at commit 2c2fe7c) for the exact contract expected (ProfileDescriptorTests: test_generated_header_embeds_canonical_descriptor, test_inventory_accepts_only_native_wrappers, test_profile_mismatch_is_fatal, test_workload_rejects_five_candidate_inventory; InventoryIdentityTests: test_identity_is_order_independent).

## Steps



## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files

- tools/bigcherry/autotune_catalog.py
- tools/bigcherry/build_descriptor.py

## Validation



## Effort & Risk



## Standards



## Acceptance Criteria



## Notes

Restored: build_descriptor()/validate_profile_descriptor() added to autotune_catalog.py (adapted from 2c2fe7c, dropped inventory_hash/producer_manifest_hash/resource_report fields since current manifest generation doesn't have those inputs), wired into manifest generation, render_build_hash (embeds descriptor_hash + full canonical JSON as C string literals), and emit() (writes hip-autotune-build-descriptor.json beside the manifest and into the tree). Verified end to end on brutus: real `bigcherry generate` run produced a correct descriptor and the header embedded it byte-for-byte. 5 new tests. NOT re-wired into release_validate.py's binary-scanner: while checking, found bigcherry build's output directory (build/<recipe>-<build>) is keyed only by recipe+build name, not by --llama-root, so two probes (or a probe and a real build) sharing a recipe name would clobber each other's build directory -- a real, separate isolation gap in `build` itself. Flagging rather than building the binary-scan check on top of an unsafe foundation.

## Change Log

- 2026-08-12T06:53:02.788794+00:00 (created-by): Created by agent
- 2026-08-12T07:00:14.323345+00:00 (state-transition): State: pending → in_progress
- 2026-08-12T07:07:30.598003+00:00 (updated-by): Updated: section:notes
- 2026-08-12T07:07:30.878976+00:00 (state-transition): State: in_progress → completed
