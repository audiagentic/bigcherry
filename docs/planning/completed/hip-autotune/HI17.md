---
id: HI17
order: 0
plan: hip-autotune
state: completed
created-at: '2026-08-05T00:00:00+00:00'
breadth: ''
skill: advanced
created-by: agent
work: L
---

# Decompose the opaque BLAS candidate into a controlled execution plan

## Description

The BLAS-1 boundary is implemented and closed. The catalog retains blas:native:v1 as the correctness fallback and emits one structured forced-native plan whose stable identity carries operand, accumulation, output, source-conversion, output-conversion, and numerical-class fields. Runtime native-only resolve/apply/execute is implemented, and the opt-in per-call GGML_HIP_BLAS_EXPERIMENT=f16_temp route transports runtime-only execution options through the shared vendor launcher without changing candidate or replay identity. Record-mode telemetry captures effective API/provider/backend, conversions, temporary bytes, calculated workspace, and execution-options state. The required same-representative native-versus-forced execution, complete effective telemetry, and quantitative timing-parity gate are evidenced in HI17_BD4C552_PARITY_EVIDENCE.md; no provider/API alternative or performance promotion was authorized.

## Steps

- **`compute_type` as a forced variant.** `ggml_cuda_mul_mat_cublas` already
  selects F32/F16/BF16 through a template `switch`, and upstream already ships a
  global `GGML_CUDA_CUBLAS_COMPUTE_TYPE` override. Add a defaulted parameter
  (the Option C pattern used by HI06–HI08) so the choice can be made per
  signature instead of per process.
- **Precision-class gating.** `dst->op_params[0] == GGML_PREC_F32` is the
  request's precision class, and the signature already carries it as `prec`
  ([hip-autotune-types.h:135](../../../../src/ggml/src/ggml-cuda/hip-autotune-types.h#L135)).
  Gate `compute_type` on it; no ABI change needed. Never infer the class from
  source type — a quantised source does not make every accumulation mode
  equivalent.
- **`numerical_class` on every candidate** — `exact_baseline`,
  `equivalent_within_backend_tolerance`, `reduced_precision`. The objective
  admits or rejects classes *before* performance ranking, so a reduced-precision
  candidate cannot win by default against a strict request.
- **`conversion_route` as an explicit field.** `convert` (contiguous) and
  `convert_nc` (strided) are already two routes for one type pair, so a
  `convert_type` field would be wrong even today.
- **`output_conversion_route`.** `prefer_f32_output`
  ([ggml-cuda.cu:1515-1520](../../../../vendor/llama.cpp/ggml/src/ggml-cuda/ggml-cuda.cu#L1515-L1520))
  gives RDNA4/CDNA/Volta direct F32 output and denies it to RDNA3. The 2× XTX
  rig is gfx1100, so every F16-compute BLAS call there allocates an F16 temp and
  runs a full extra pass. This is a same-hardware A/B, not a hypothetical.
- **`api_strategy`, not `api`.** The API is chosen by a hard if/else chain over
  signature properties, so exactly one is reachable per shape by default.
  `native-auto | prefer-strided | prefer-pointer-batched | force-gemmex`; the
  API actually called is an observation. Upstream's own comment at
  [ggml-cuda.cu:1545](../../../../vendor/llama.cpp/ggml/src/ggml-cuda/ggml-cuda.cu#L1545)
  ("Theoretically … would always work … probably because the internal kernel
  selection logic is suboptimal") is the documented reason `prefer-strided`
  exists as a candidate.
- **`provider_policy`** — `hipblas-auto | hipblas-force-tensile |
  hipblas-prefer-hipblaslt`, driven by `ROCBLAS_USE_HIPBLASLT`. Policies under
  one family, not three families.
- **Runtime BLAS-1 seam.** The first runtime slice now transports the exact
  seven-field plan through a generated `runtime_id` side table, resolves the
  native call facts, applies only the native/none/exact-baseline plan, and
  terminates native and forced-native through one shared executor. The replay
  v3 variant payload remains unchanged.
- **Effective-path telemetry** — `provider_requested`, `provider_effective`,
  `observed.api`, `library_backend`, `solution_id`, `kernel_name`,
  `conversion_kernels`, `temporary_bytes`, `workspace_bytes`. A winner without
  it is marked unverified and excluded from promotion: reporting
  `winner = hipblas-prefer-hipblaslt` without establishing that hipBLASLt
  handled the call is not a measurement.
- **Build namespace extension** — ROCm, hipBLAS, rocBLAS and hipBLASLt versions
  plus `ROCBLAS_USE_HIPBLASLT`, since internal selection changes across
  releases.

## Detailed Solution & Technical Design

DQ_BLAS and BLAS are **not two entry points**. There is one
`ggml_cuda_mul_mat_cublas`, and whether a call dequantises is decided by
`src0->type == compute_type` — where `compute_type` is the candidate's own
choice. The same candidate is therefore DQ_BLAS against a q8_0 signature and
plain BLAS against an f16 one.

So this implements **one `BLAS` family** with `conversion_route = none` meaning
no dequantisation, and reports `actual_family = dq_blas | blas` as a derived
observation. Splitting the family by source type would re-import a request
attribute into family membership, which is the thing the taxonomy exists to
avoid. Grouping and cost attribution work identically either way.

Sequencing: `compute_type` first. It is the cheapest (the template switch and
the env override already exist), it is the choice that determines whether
conversion happens at all, and it validates the precision-class machinery that
everything else depends on.

## Code Samples & Guidance



## Files

- tools/bigcherry/autotune_schema.py
- tools/bigcherry/autotune_catalog.py
- tools/tests/test_blas_plan_contract.py
- tools/tests/test_hi17_blas_runtime_seam.py
- src/ggml/src/ggml-cuda/hip-autotune-dispatch.cu
- src/ggml/src/ggml-cuda/hip-autotune-dispatch.cuh
- src/ggml/src/ggml-cuda/hip-autotune-types.h
- patches/0200_dispatch_hook.py
- src/ggml/src/ggml-cuda/hip-autotune-record.h/.cpp — effective BLAS execution-state telemetry
- tools/bigcherry/telemetry.py — strict effective execution-state ingestion
- tools/tests/test_dispatch_safety.py
- tools/tests/test_telemetry.py

## Validation

The older focused/full counts in this section (63/63, 38/38, 522/522, 39/39,
523/523, and 529/529) are historical checkpoints from earlier commits. The
authoritative post-closure validation at 4732f47 is 530 passed with 4 subtests;
the quantitative Brutus parity reduction is recorded below.

HI57's BLAS-0 Brutus evidence remains the prerequisite: 1186/1186 record-mode cases, 1188/1188 dispatch launches, 342 BLAS executions, and 340 strict parser-clean observations across all four native cuBLAS APIs. The BLAS-1 offline contract validates explicit operand, accumulation, output, source-conversion, output-conversion, and numerical-class fields; rejects reduced_precision when prec == GGML_PREC_F32; emits one stable forced-native plan alongside the native fallback; and emits no provider/API candidate dimensions. The runtime seam resolves compute/precision class, conversion routes, API family, dimensions, strides, temporary bytes, and workspace estimates, then applies only the exact native plan before entering the shared vendor forwarder. Focused BLAS/runtime seam tests pass 63/63 before the validation hardening and 38/38 after it; the full Python suite passes 522/522. On Brutus, the pristine full-max overlay compiled successfully for 3,766 candidates, and a durable 24-invocation matrix covered native and forced-native execution of SGEMM, GemmEx, strided-batched, and non-contiguous pointer-batched representatives on gfx1030, gfx1100, and gfx1201. All native and forced-native representatives were CPU-reference clean; forced-native dispatch coverage was 100% on each architecture. A rebuilt broad record sweep captured 821 native BLAS observations across the three architectures (259/284/278), covering cublasSgemm, cublasGemmEx, cublasGemmStridedBatchedEx, and cublasGemmBatchedEx; every row had complete effective API, provider/backend, operand/accumulation/output, conversion, requested-precision, temporary-byte, workspace, and common source/manifest provenance. The summary is /tmp/hi17-blas-broad-summary.json with SHA-256 6bb33c434b8431148f6828d816bb93ce0d347d07a30b01f3c261acf90e02cf17. The exact small representatives natively select MMVF, so their native telemetry fields are intentionally empty. The required timing probe was then run against the production perf catalog on all three Brutus architectures for the two F16/F32 cases containing 16416. Each native and forced-native run passed CPU-reference correctness, with native coverage 0% and forced-native coverage 100%. The first case (m=16416,n=1,k=128) was not a parity comparison because the native and forced paths have different intended work shapes; the large-K case (m=128,n=1,k=16416) is the comparable BLAS candidate. Its native versus forced-native timings were gfx1030 85.35 vs 442.75 us/run, gfx1100 75.46 vs 133.38 us/run, and gfx1201 99.70 vs 12293.97 us/run. These are materially outside noise, so timing parity is not proven and no provider/API alternative or performance promotion is authorized. Post-hardening Brutus build at ed72311 completed all 16 HIP/CXX link steps; build log SHA-256 is 0bd4777dff1e8ddf3efe8f1eadc8d27bab5f397e7d057db5f377d4be67361797. The rebuilt binary reran the 24 native/forced representative groups across gfx1030/gfx1100/gfx1201 with all 24 groups passing and matrix log SHA-256 dd92be9c453cad88d9d2437f673a1b67a0bd006364ab1662b14394899eb1237d. The new plan/call matcher rejects malformed enum, dimension, stride, precision, conversion-temporary, and workspace states before the shared executor; the production build compiles this fail-closed check. The final checked-in tree at e367611 was rebuilt on Brutus after the portability migration: the production record target completed 108/108 with exit 0; build log SHA-256 083460d4a1812dc6a747ba96d15cbf55a97118d06974bc1df0876fc814c4273f. The opt-in `GGML_HIP_BLAS_EXPERIMENT=f16_temp` route then passed the CPU-reference F16 representative on gfx1030, gfx1100, and gfx1201, with 2/2 tests and 100% measured-dispatch coverage on each architecture; matrix log SHA-256 3f89e612ba022a9fd557abe2c23e6c40c515e865f5282c5ca950e548a43f1939. On gfx1100, the comparable long-K forced-BLAS perf case measured 133.16 us/run without the option and 133.01 us/run with the per-call option (native log SHA-256 7ebc5fd907de1c1128ee9f961cc9ba5bac7b752a94b09cc945ddfa6e58c603e2; option log SHA-256 3f5fa80cd0f830518496dfae4facc6f5c57d11341a20a6276a5477d2364f1566). The two runs both covered 2978/2978 dispatches. This validates per-call plumbing and no measurable overhead, but does not authorize a persisted performance variant or close the pre-existing native-versus-forced BLAS parity gate. The release-mode guard follow-up is validated at pushed 5db2f4f: core overlay application reported all 25 patch files OK before the known release-stage bookkeeping exception, and Brutus production record rebuilt 108/108 with exit 0. Build log SHA-256 39f6b83051dbed4e9bd813b2eea027e9ac90ab307e9bffaa783ac4d6619a16fa; production llama-server SHA-256 4bdc12c102f1b1f024d1f5db2d02b2919feadccc81a55caaa0ee8c6b380a881b; production llama SHA-256 eba94cd2d5bb3b4c26c724b1315953cdfc06238ef965b1c292d8f4a1a1766aab.

Post-f5d1f72 overlay evidence: the exact malformed already-applied vendor state was repaired once, the native declaration appears exactly once in the correct order, and the repeat dry-run reported all 25 patch files OK with zero writes and 30 already-applied edits (log SHA-256 e097284c7ea39719a995a19cc8b7c0b53036ca531906dda8134ccfd97f9ab298). The repaired production `llama-server` target completed with numeric exit 0; final build log SHA-256 cc6b6f6f50ac2fb0fb80e10bcc0351d13752305206b0b2bde6f2cae6226f2218 and binary SHA-256 4bdc12c102f1b1f024d1f5db2d02b2919feadccc81a55caaa0ee8c6b380a881b. The local patcher regression exercises old, malformed, pristine, and repeated states.

## Effort & Risk

Medium risk. The conversion temporaries come from the pool allocator, so
workspace accounting must be attributed to the candidate rather than lost in the
pool — otherwise a candidate that allocates two large temps looks free.
`GGML_PREC_F32` gating is the correctness-critical part: getting it wrong lets a
reduced-precision candidate win a strict-precision signature.

## Standards

HIP_AUTOTUNE_STANDARDS 2.1 (stable name is durable identity), 2.2 (name carries
resolved config), 3.1 (precision equivalence), 7.1 (complete-path timing —
conversions are part of the path), 12.4 (hard eligibility), 13.1 (build
namespace), 16.1/16.2 (library-internal algorithms and version namespacing)

## Acceptance Criteria

- [x] The seven-field BLAS plan is transported in a generated runtime-id side
  table; `ggml_hip_variant_params` and replay v3 remain unchanged.
- [x] Native and forced-native resolve through one explicit
  resolve/apply/execute seam, with native-only plan application fail-closed.
- [x] The resolver records compute/precision class, conversion routes, API
  family, dimensions, strides, temporary bytes, and workspace estimates.
- [x] Pristine Brutus overlay/build succeeds with the full-max candidate set.
- [x] Native and forced-native correctness passes cover Sgemm, GemmEx,
  strided-batched, and non-contiguous pointer-batched representatives on
  gfx1030, gfx1100, and gfx1201.
- [x] Capture effective API/temporary/workspace telemetry and timing parity
  within the declared noise tolerance for the same representatives. The
  quantitative reduction is in
  [HI17_BD4C552_PARITY_EVIDENCE.md](HI17_BD4C552_PARITY_EVIDENCE.md); all 12
  cells pass `abs(forced-native - native) <= max(5%, native repeatability
  spread)`.
- [x] No provider/API alternatives were added and no performance variant was
  promoted before or during this parity gate.

## Notes

The long validation narrative below preserves interim measurements from before
the final parity reduction. Any wording that describes the parity gate as open
or HI17 as in progress refers to that earlier checkpoint; the authoritative
state is completed at 4732f47 and the closure evidence is
`HI17_BD4C552_PARITY_EVIDENCE.md`.

BLAS-1 offline plan, eligibility, identity-binding, the first runtime resolve/apply/execute seam, resolved-call validation, Brutus native/forced correctness, broad effective BLAS telemetry, and the cross-architecture timing probe are complete. The catalog identity is `blas:forced-native:...:v1`, while `blas:native:v1` remains the correctness fallback for every requested architecture. Provider and effective API values are observation data; they are intentionally not candidate identity. The exact small correctness representatives are MMVF under the native baseline, while the broad production record sweep proves the effective BLAS metadata path on all four native APIs. The production perf probe demonstrates a real native/forced timing gap on the comparable large-K case (gfx1030 85.35/442.75 us, gfx1100 75.46/133.38 us, gfx1201 99.70/12293.97 us), so the complete-path timing parity gate remains open. The post-hardening production build at ed72311 and 24-group Brutus rerun pass, proving the new fail-closed plan/call validation is compiled and exercised. No performance-winning BLAS variant was promoted. The bounded gfx1100 accumulation/output-conversion experiment then compared the forced BLAS candidate under the existing process-level `GGML_CUDA_CUBLAS_COMPUTE_TYPE` control on the exact long-K perf cases. The F16 arm (RDNA3 temporary-output conversion path) measured 1153.75 us/run for m=16416,n=1,k=128 and 133.49 us/run for m=128,n=1,k=16416; the F32 arm (direct-F32-output path) measured 1405.39 and 1771.36 us/run respectively. Both runs completed without errors, and the comparable large-K forced dispatch coverage was 2978/2978 for F16 and 1490/1490 for F32. The F16 path is materially faster on gfx1100, while F32 direct output is not a promotion candidate. Because this control is process-level and the experiment is not a complete per-call provider/API/timing-parity gate, HI17 remains in progress and no alternative was promoted. Artifact log SHA-256 values are c227865451a5ad8b198fd0568977fb3074ce9870649a53b5f7aa64e4d4bdac4f (F16) and 5c11d1ae75632f9f3f2c154acb067410a3f0f85411d171e6babafb7f5d30b621 (F32). The next bounded runtime-only experiment seam is now implemented: `GGML_HIP_BLAS_EXPERIMENT=f16_temp` creates a per-call execution-options object for the already-proven F16 temporary-output route, while native/replay behavior is unchanged when unset. The options are rejected for MUL_MAT_ID, strict precision, Sgemm, non-exact numerical class, or mismatched F16 conversion facts, and they are not part of `ggml_hip_blas_plan_v1` or replay v3. Local focused seam/contract/safety tests pass 39/39 and the full Python suite passes 523/523; production HIP build and Brutus execution remain the validation gate. Final Brutus validation of e367611 is complete: 108/108 production HIP build, three-architecture F16 option correctness matrix at 2/2 per architecture, and gfx1100 forced-BLAS option timing within noise of the same-call native plan. The option remains intentionally non-persistent and no candidate/replay identity changed. The resolve/apply/execute path now has explicit release-mode guards in addition to assertions: unresolved calls, rejected plans, and malformed resolved facts return before the vendor launcher. This is a fail-closed safety fix; it does not change the open native-versus-forced parity gate.

Current authoritative status: runtime plan application, release-mode fail-closed resolve/apply/execute guards, observation-only effective execution state, and the prescribed native-versus-forced parity gate are complete. The execution_options value is explicit for new record artifacts (native, f16_temp, or rejected) and legacy artifacts normalize to unknown; calculated workspace is not claimed as measured pool peak. The earlier large-K non-parity artifact remains historical diagnostic evidence, but the required same-representative matrix passes the declared quantitative noise gate. No provider/API alternative or performance variant was promoted.

## Review follow-up — same-input Brutus parity matrix

The latest review correctly required same-input execution evidence before HI17
could be considered for closure. The rebuilt production tune target on Brutus
was exercised with `GGML_CUDA_DISABLE_GRAPHS=1`, `HIP_LAUNCH_BLOCKING=1`, the
experiment and process-level compute overrides unset, and
`GGML_HIP_TUNE_FINAL_SAMPLES=100`. The exact Sgemm, GemmEx, strided-batched,
and non-contiguous pointer-batched representatives were run on gfx1030,
gfx1100, and gfx1201. Three independent 12-cell runs were captured under
`/tmp/hi17-parity-final-tune`, `/tmp/hi17-parity-repeat-tune`, and
`/tmp/hi17-parity-third-tune`.

All 36 cell-runs completed with two measured candidates, no measurement
failure, 100 final samples per native and forced-native candidate, complete
sample arrays, `nmse=0`, and `max_abs=0`. Native and forced-native record
telemetry matched on effective API/call family, BLAS metadata, and workspace.
The offline reduction in
`HI17_BD4C552_PARITY_EVIDENCE.md` compares all native and forced median/p95/MAD
values, derives the native cross-run envelope, and passes all 12 cells against
the declared 5% noise tolerance. The earlier large-K non-parity artifact is
retained as historical diagnostic evidence but is superseded for this
prescribed same-representative parity decision. HI17 was closed at 4732f47.

The existing `Measurement::is_native_twin` field remains intentionally
unpopulated. Its double-native measurement, `#twin` artifact identity, ranking
exclusion, loader handling, and aggregate reporting are HI24 work and are not
being folded into this BLAS-1 slice. The three independent tune runs are
empirical repeatability support only; they do not claim HI24's native-twin
implementation.

## Candidate identity

```text
(operand_type, accumulation_type, output_type,
 source_a_conversion, source_b_conversion, output_conversion,
 numerical_class)
```

This seven-field resolved BLAS plan is the authoritative candidate identity.
Provider and API choices remain effective-path observations, not candidate
fields. Source layout, batch structure and `prec` stay in the signature — they
describe what arrived, not what was chosen. One candidate serves many source
types provided each has an eligible conversion route.

## Ledger-events

- chg_20260813_151346_updated-hi17-to-acknowledge-th_7037
- chg_20260813_154749_added-the-bounded-blas-1-catal_4081
- chg_20260813_161846_hardened-the-offline-blas-cata_5165
- chg_20260813_225555_implemented-the-first-hi17-run_8979
- chg_20260813_225802_fixed-the-first-brutus-hip-com_1458
- chg_20260813_232158_validated-the-hi17-nativeforc_2864
- chg_20260813_234313_recorded-rebuilt-brutus-blas-t_9978
- chg_20260813_234910_recorded-the-required-brutus-b_5724
- chg_20260814_002958_closed-the-hi17-seam-validatio_8412
- chg_20260814_004621_rebuilt-and-exercised-the-hard_2824
- chg_20260814_005406_documented-the-gfx1100-blas-ac_9132
- chg_20260814_010343_added-a-fail-closed-per-call-b_7885
- chg_20260814_010853_hardened-the-per-call-blas-for_7514
- chg_20260814_011635_made-the-blas-experiment-patch_6428
- chg_20260814_012230_validated-the-final-per-call-b_9305
- chg_20260814_013704_hardened-the-hi17-blas-seam-so_8557
- chg_20260814_013909_rebuilt-the-production-brutus_7865
- 2026-08-14T01:39:09.727456+00:00 (updated-by): Updated: section:ledger-events
- chg_20260814_014122_added-strict-effective-executi_4549
- 2026-08-14T01:41:23.012891+00:00 (updated-by): Updated: section:ledger-events
- chg_20260814_014254_updated-hi17-documentation-to_2144
- 2026-08-14T01:42:54.544591+00:00 (updated-by): Updated: section:ledger-events
- chg_20260814_014657_fixed-the-hi17-overlay-migrati_8681
- 2026-08-14T01:46:57.581944+00:00 (updated-by): Updated: section:ledger-events
- chg_20260814_015203_fixed-the-hi17-overlay-migrati_9059
- 2026-08-14T01:52:03.108632+00:00 (updated-by): Updated: section:ledger-events
- chg_20260814_015327_made-the-hi17-migration-portab_3190
- 2026-08-14T01:53:27.392141+00:00 (updated-by): Updated: section:ledger-events
- chg_20260814_015423_preserved-the-complete-hi17-au_9568
- 2026-08-14T01:54:24.020791+00:00 (updated-by): Updated: section:ledger-events
- chg_20260814_015532_corrected-the-second-overlay-m_5446
- 2026-08-14T01:55:32.135115+00:00 (updated-by): Updated: section:ledger-events
- chg_20260814_020231_closed-the-overlay-safety-evid_8503
- 2026-08-14T02:02:31.804617+00:00 (updated-by): Updated: section:ledger-events
- chg_20260814_022156_hardened-hi17-record-mode-tele_7889
- 2026-08-14T02:21:56.153153+00:00 (updated-by): Updated: section:ledger-events
- chg_20260814_022654_corrected-the-forced-record-te_3567
- 2026-08-14T02:26:54.204818+00:00 (updated-by): Updated: section:ledger-events
- chg_20260814_023552_repaired-hi17-release-history_5686
- 2026-08-14T02:35:52.125231+00:00 (updated-by): Updated: section:ledger-events
- chg_20260814_025358_added-the-missing-same-input-b_9192
- 2026-08-14T02:53:58.657704+00:00 (updated-by): Updated: section:ledger-events
- chg_20260814_035616_completed-and-closed-hi17-usin_9829
- 2026-08-14T03:56:16.074175+00:00 (updated-by): Updated: section:ledger-events

- chg_20260814_050248_aligned-the-completed-hi17-pla_2906
## Change Log

- 2026-08-05T05:52:50.250715+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-12T07:39:10.074849+00:00 (updated-by): Updated: section:notes
- 2026-08-12T07:41:36.153706+00:00 (state-transition): State: pending → in_progress
- 2026-08-12T07:42:56.636758+00:00 (state-transition): State: in_progress → pending
- 2026-08-13T06:36:40.755583+00:00 (state-transition): State: pending → in_progress
- 2026-08-13T15:13:45.980117+00:00 (updated-by): Updated: section:validation, section:notes
- 2026-08-13T15:47:28.181819+00:00 (updated-by): Updated: section:files, section:validation, section:notes
- 2026-08-13T15:50:07.392074+00:00 (updated-by): Updated: section:description
- 2026-08-13T15:52:47.383920+00:00 (updated-by): Updated: section:validation, section:notes
- 2026-08-13T16:18:15.353806+00:00 (updated-by): Updated: section:validation, section:notes
- 2026-08-13T23:43:01.373608+00:00 (updated-by): Updated: section:validation, section:notes
- 2026-08-13T23:49:30.622315+00:00 (updated-by): Updated: section:validation, section:notes
- 2026-08-14T00:45:08.723736+00:00 (updated-by): Updated: section:validation, section:notes
- 2026-08-14T00:46:28.411238+00:00 (updated-by): Updated: section:notes
- 2026-08-14T00:54:06.224608+00:00 (updated-by): Updated: section:notes
- 2026-08-14T01:03:36.229538+00:00 (updated-by): Updated: section:notes
- 2026-08-14T01:22:20.365342+00:00 (updated-by): Updated: section:validation, section:notes
- 2026-08-14T01:39:21.587499+00:00 (updated-by): Updated: section:validation, section:notes
- 2026-08-14T01:42:39.654508+00:00 (updated-by): Updated: section:description, section:files, section:notes
- 2026-08-14T03:55:41.279525+00:00 (state-transition): State: in_progress → completed
- 2026-08-14T05:02:48.466159+00:00 (updated-by): Updated: section:ledger-events
