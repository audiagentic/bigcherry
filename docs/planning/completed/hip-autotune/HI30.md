---
id: HI30
order: 35
plan: hip-autotune
state: completed
created-at: '2026-08-06T03:05:59.606024+00:00'
breadth: ''
skill: advanced
created-by: agent
work: L
---

# Tuner integration — try routing transformations when native selection yields a single-family result

## Description

Integrate routing transformations into the tuning flow. When the tuner encounters a signature where native selection picks a family but no eligible candidates exist (or only BLAS is eligible), try registered routing transformations to produce alternative launch contexts that other families can serve.

The integration point is in ggml_hip_tuner_resolve() — after the candidate set is gathered and screened, if the result shows generated==eligible==1 (only BLAS) or eligible==0, attempt transformations.

Each transformation produces a new launch context. The tuner measures both paths with complete-path timing: original (BLAS) vs transformed (whatever family the transform targets). If the transformed path wins by replacement_threshold_pct, install it as the winner with the transformation attached.

## Steps

[NOT STARTED] Rewritten per the 2026-08-21 GPT adjudication above -- the item's own original 'steps'/'code_samples' sections (2026-08-06) describe a hand-rolled batch loop and a since-superseded inverse_ctx design and must NOT be implemented as written. Real next steps: (1) freeze the route-identity contract (candidate+variant+transform_id) across Result/Measurement/schedule/journal before touching the resolve loop; (2) implement the direct_eligible_families-based trigger; (3) route every launch call site through ggml_hip_transform_launch(); (4) wire HI29's recording calls; (5) leave production promotion gating exactly as HI67 slice 3 already built it (no shortcut for transform-sourced winners).

## Detailed Solution & Technical Design



## Code Samples & Guidance

// hip-autotune-tuner.cu — inserted after screening phase, before scratch allocation:
#ifdef GGML_HIP_ROUTING_TRANSFORM
// --- routing transform attempt (standards 11.3) -----------------------
//
// Trigger: eligible candidates are only BLAS, or none exist.
// Try registered transforms to produce alternative launch contexts.
{
    const bool single_family = [&]() {
        if (screening.empty()) return false;
        const int fam = screening[0]->candidate->family;
        for (auto * m : screening)
            if (m->candidate->family != fam) return false;
        return true;
    }();

    if (!fused && single_family && screening.size() <= 1) {
        const int transform_count = ggml_hip_transform_count();
        std::vector<AttemptResult> tried_results; // for gap record

        for (int t = 0; t < transform_count; ++t) {
            const ggml_hip_routing_transformation * xform =
                ggml_hip_transform_find((ggml_hip_transform_id)(t + 1));
            if (!xform || !xform->can_apply(sig)) {
                tried_results.push_back({xform ? xform->id : 0,
                                         xform ? "can_apply failed" : "not found"});
                continue;
            }

            ggml_hip_dispatch_signature_v1 trans_sig = {};
            ggml_hip_launch_context trans_lc;
            void * inverse_ctx = nullptr;

            if (xform->apply(sig, &trans_sig, &trans_lc, &inverse_ctx) != 0) {
                tried_results.push_back({xform->id, "apply failed"});
                continue;
            }

            // Check that the target family can serve.
            const int target_family = (xform->id == GGML_HIP_TRANSFORM_TRANSPOSE_WEIGHT_FOR_MMVF)
                ? GGML_HIP_FAMILY_MMVF
                : GGML_HIP_FAMILY_MMVF;
            if (!ggml_hip_family_can_serve(target_family, trans_sig)) {
                tried_results.push_back({xform->id, "family_can_serve false"});
                continue;
            }

            // Build candidate set from transformed sig.
            std::vector<Measurement> trans_measurements;
            for (size_t i = 0; i < ggml_hip_registry_size(); ++i) {
                const auto * c = ggml_hip_registry_at(i);
                Measurement m;
                m.candidate = c;
                if (c->can_execute(c, trans_sig, hw)) {
                    m.workspace_bytes = c->workspace(c, trans_sig);
                    // Only consider target family candidates.
                    if (c->family == target_family) {
                        // Eligible — measure it.
                    }
                }
                trans_measurements.push_back(m);
            }

            // Find the best eligible candidate in the target family.
            Measurement * best_trans = nullptr;
            for (auto & m : trans_measurements) {
                if (m.candidate && m.candidate->family == target_family &&
                    m.candidate->can_execute(m.candidate, trans_sig, hw)) {
                    if (!best_trans || m.workspace_bytes < best_trans->workspace_bytes)
                        best_trans = &m;
                }
            }

            if (!best_trans) {
                tried_results.push_back({xform->id, "no eligible candidate in target family"});
                continue;
            }

            // --- measure both paths ------------------------------------
            // Time original (BLAS) path — already measured as native_m.
            const double orig_us = native_m->median_us;

            // Time transformed path with complete-path timing.
            ggml_tensor trans_dst = *lc_in.dst;
            trans_dst.data = candidate_buf.get();
            std::vector<double> gpu, host;

            if (xform->id == GGML_HIP_TRANSFORM_BATCH_FOR_MMVF) {
                // Batch iteration: time the full loop.
                auto * bctx = reinterpret_cast<batch_inverse_ctx *>(inverse_ctx);
                for (int batch = 0; batch < bctx->num_batches; ++batch) {
                    // Adjust pointers for this batch slice.
                    // ... (pointer arithmetic based on bctx)
                    if (!time_candidate(best_trans->candidate, trans_lc,
                                        config.warmup_launches, config.screen_samples,
                                        config.launches_per_sample, gpu, host)) {
                        tried_results.push_back({xform->id, "launch failed"});
                        goto next_transform;
                    }
                }
            } else {
                // Single launch.
                if (!time_candidate(best_trans->candidate, trans_lc,
                                    config.warmup_launches, config.screen_samples,
                                    config.launches_per_sample, gpu, host)) {
                    tried_results.push_back({xform->id, "launch failed"});
                    goto next_transform;
                }
            }

            const double trans_us = median_of(gpu);

            // Correctness: compare against native reference.
            CUDA_CHECK(hipMemcpyAsync(candidate_host.data(), candidate_buf.get(),
                                       dst_bytes, hipMemcpyDeviceToHost, lc.stream));
            CUDA_CHECK(hipStreamSynchronize(lc.stream));

            double nmse = 0.0, max_abs = 0.0;
            if (!compare_outputs(reference_host, candidate_host, nmse, max_abs) ||
                nmse > config.max_nmse || max_abs > config.max_abs_error) {
                tried_results.push_back({xform->id, fmt::format("nmse={:.6g}", nmse)});
                goto next_transform;
            }

            // --- transform succeeded — compare timings -------------------
            const double improvement = 100.0 * (orig_us - trans_us) / orig_us;

            ggml_hip_record_transform_attempt(
                result.signature_digest, xform, true,
                nullptr, &trans_sig, best_trans->candidate,
                orig_us, trans_us, nmse, max_abs);

            if (improvement >= config.replacement_threshold_pct) {
                // Install transformed winner.
                binding.candidate = best_trans->candidate;
                binding.variant   = best_trans->candidate->variant;
                binding.from_cache = true;
                // TODO: store transform_id in binding (see HI31)
            }

            goto done_transforms; // success — no need to try more

        next_transform:
            tried_results.push_back({xform->id, "handled"});
        } // for each transform

        // No transform worked — record gap.
        if (binding.candidate == native.candidate) {
            ggml_hip_record_transform_gap(
                result.signature_digest, result.hardware_digest,
                nullptr, // pattern — caller fills or omit
                (int)native.candidate->family,
                0,       // calls — not known in tune mode
                (int64_t)sig.ne0[0] * sig.ned[1] * sizeof(float), // est_bytes
                tried_results);
        }
    } // single_family check
}
done_transforms:
#endif // GGML_HIP_ROUTING_TRANSFORM

## Files

- vendor/llama.cpp/ggml/src/ggml-cuda/hip-autotune-tuner.cu — transform attempt logic inserted after screening phase in ggml_hip_tuner_resolve()

## Validation

- For F32[1×5120] × F32[5120×4096] falling to BLAS: TRANSPOSE_WEIGHT_FOR_MMVF is tried, mmvf family is eligible, both paths measured, winner installed if improvement >= threshold
- Correctness: transformed output is bit-identical (compare_outputs passes)
- Complete-path timing: batch transform times all iterations, not just one
- Recording: transform-attempt or transform-gap written for every signature where transforms were tried
- Cache: result stored under original dispatch_digest — no re-tuning on warm path

## Effort & Risk



## Standards

- Transform attempt must use complete-path timing — include overhead of the transformation itself (not just kernel time)
- Transformed path must be validated against native reference output before being considered as winner
- If fused, do not try transformations (same rule as cross-family tuning — graph-level question)
- Transform attempt is zero-cost when GGML_HIP_ROUTING_TRANSFORM=OFF

## Acceptance Criteria



## Notes

Depends on HI27, HI28, HI29. Integration point in ggml_hip_tuner_resolve(). Tries transformations when eligible candidates are only BLAS or empty. Records results via HI29's APIs.

### 2026-08-20 PH01 audit — code sample is a measurement-integrity trap; rewrite before implementing

The current Code Samples must NOT be implemented as written (rubric-1-class defect risk):
- It measures `median_of(gpu)` over a hand-rolled batch loop, but dispatch would run the complete `ggml_hip_transform_launch` path (the shared complete-path function from HI27/HI28). A winner chosen on a different workload than dispatch runs is exactly the defect class this project's standards (7.1) exist to prevent.
- `binding.from_cache = true` is set on a FRESHLY-MEASURED binding — wrong on its face.
- The gap record's `est_bytes` expression `sig.ne0[0]*sig.ned[1]` is wrong for an F32 dst (missing sizeof and dimension mapping).
- Dangling `goto next_transform` label placement and an unfilled `// ... (pointer arithmetic based on bctx)` body — draft-grade.

Rewrite the sample to call `ggml_hip_transform_launch` for timing (measure what dispatch runs), then proceed. The item itself remains the highest-value piece of the transform chain: it is the only one that changes which candidate serves a signature at runtime. HI29's record API should be reconciled to the shipped `tools/bigcherry/transform_records.py` schema (fail-closed JSONL with evidence references), not the 2026-08-06 draft.

### 2026-08-20 PH01 GPT second pass — HI30 owns the binding-with-transform seam

The binding-with-transform mechanism that HI31 sketches (the `Binding` struct
carrying candidate + transform_id + inverse_ctx, and the dispatch writeback)
is owned HERE, not in HI31. HI31's samples are directional scaffolding only and
are superseded by whatever HI30 implements. HI30's ownership of this seam
closes when the `Binding` struct extension and the dispatch writeback are
landed and the guard-against-re-tuning (install under the original
dispatch_digest) is proven. Until then HI31 must not be implemented against its
own sample as a final design. This is receiver-side ownership: HI30 states the
obligation, not just HI31 pointing at HI30.

### Original note

Depends on HI27, HI28, HI29. Integration point in ggml_hip_tuner_resolve(). Tries transformations when eligible candidates are only BLAS or empty. Records results via HI29's APIs.

Depends on HI27, HI28 (both DONE, compiled/verified on Brutus), HI29 (DONE, 2026-08-21, recording infra ready to be called). Integration point in ggml_hip_tuner_resolve(). Tries transformations when eligible candidates are only BLAS or empty. Records results via HI29's APIs.

### 2026-08-21 GPT design adjudication (req_b0223558ac134aee) -- do NOT implement against this item's own stale 2026-08-06 code_samples

Re-designed against the REAL current HI27/HI28 API (ggml_hip_transform_launch/ggml_hip_transform_ctx/select_batch -- no heap allocation, no separate inverse-writeback step, superseding this item's stale draft entirely). GPT's verdict: 'the overall direction is sound... I would not implement HI30/HI31 exactly as written yet' -- corrections required before coding:

1. TRIGGER CONDITION is wrong as originally scoped: use `direct_eligible_families.empty() || direct_eligible_families == {BLAS}` (the set of families that survived ordinary screening), NOT `native.candidate->family == GGML_HIP_FAMILY_BLAS` -- native being BLAS does not imply no ordinary non-BLAS candidate already survived screening; in that case transforms must NOT fire.
2. Scan EVERY eligible candidate in transform->target_family, not just 'find one' -- a transform creates a routing possibility, not a single nomination; if six MMVF variants can execute the rewritten signature, all six are legitimate alternatives subject to the normal adaptive screening/ranking policy.
3. CRITICAL: tuning identity must become route-aware from creation: (candidate stable_name, variant, transform_id) -- NOT candidate alone. A single MMVF candidate could be reachable via transpose_weight_for_mmvf OR batch_for_mmvf; those are different measured alternatives and must not be conflated in Measurement/Result::winner/schedule names/provisional_winner/confirmation/journal output. Do not bolt transform_id on at the end.
4. Abstract the launch operation ONCE, not just at the timing loop: every effective.launch(...) call site in the tuner (warmup, rewarm, timed samples, correctness/output acquisition, determinism, confirmation/holdout, retiming) must route through ggml_hip_transform_launch() for a transformed route -- the 'measure one thing, dispatch another' defect class this project's standards (7.1) already exist to prevent.
5. Use equivalence_verified==true as a hard requirement; workspace screening must be candidate->workspace(candidate, out_sig) + transform->overhead_bytes(original_sig) against max_workspace_bytes (overflow-safe), using the transform descriptor's own real fields (target_family, overhead_bytes) -- both current transforms report 0 overhead today but the API anticipates non-zero ones.
6. Treat a transformed tuner result as PROVISIONAL evidence only -- it still needs the EXACT SAME RV49 CPU-reference correctness gate (correctness_evidence/correctness_evidence_seed, schema 6, via promotion_correctness_gate.py -- already implemented for ordinary candidates under HI67 slice 3) before promotion. 'transform equivalence verified' is not a correctness-evidence shortcut -- batch_for_mmvf's pointer arithmetic could be wrong even though the underlying math transform is provably correct.

HI29's existing schema (transformation_id/transformation_name/transformed_sig fields) already matches this correctly and needs no rework.

See HI31's notes for the paired Part B (dispatch-side) adjudication -- particularly the binary replay-cache format bump (v4->v5), which this item's original scope did not account for at all.

## Current Status (2026-08-23, plan-hygiene closure)

DONE (verified in committed source, not just the card's stale '[NOT STARTED]' steps):
- Tuner transform trigger: hip-autotune-tuner.cu computes `direct_eligible_families`, sets `transform_trigger` (empty or BLAS-only), iterates the transform registry, checks `ggml_hip_transform_signature_is_eligible(sig)` + `transform->can_apply(sig)`, and records transform_scan_gaps.
- Transforms are actually applied and measured at tune time via `ggml_hip_transform_launch()` (two call sites), and `record_transform_diagnostics()` derives HI29's transform-attempt/gap rows from the scan.
- Real routing-transform winners have been produced (e.g. batch_for_mmvf, +49.5%/+18.3% on Brutus), proving the mechanism is live, not speculative.

OPEN -- but owned by OTHER items (not HI30):
- Runtime DISPATCH of a transformed winner + fresh-process transformed replay -> HI31.
- Promotion correctness gating for transform-sourced winners -> HI67 (slice 3). 
- Correctness-evidence generation CLI for blocked transform wins -> HI80 (completed). 

Closing HI30 on its implemented tuner-integration body. The remaining transform productionization questions are integration/productionization, not 'implement transform tuning' -- they live on HI31/HI67/HI80.

## Integration Point

Transforms are tried **after** the normal candidate screening phase in `ggml_hip_tuner_resolve()`. The trigger condition:
1. The screening phase completed, AND
2. Eligible candidates exist but all belong to the same family (typically BLAS), OR
3. No eligible candidates exist at all.

Transforms are NOT tried when:
- The signature is fused (same rule as cross-family tuning — graph-level question)
- Multiple non-BLAS families already compete (tuner can pick the best natively)
- GGML_HIP_ROUTING_TRANSFORM is OFF

## Flow

```
ggml_hip_tuner_resolve()
  │
  ├── [existing] Gather candidates, screen by can_execute
  │     └── result.eligible measurements
  │
  ├── [new] Check: eligible == 1 (only BLAS) or eligible == 0?
  │     └── YES → try transforms
  │           │
  │           ├── For each transform in registry:
  │           │   ├── can_apply()? NO → skip, record rejection
  │           │   ├── apply() → transformed_sig + transformed_lc
  │           │   ├── family_can_serve(target_family, transformed_sig)? NO → skip
  │           │   ├── Re-run screening on transformed_sig → transformed_eligible
  │           │   │
  │           │   ├── transformed_eligible == 0? → record rejected attempt
  │           │   └── YES → measure both paths
  │           │         ├── Time original (BLAS) path → orig_us
  │           │         ├── Time best transformed path → trans_us
  │           │         ├── compare_outputs(transformed, native_reference)
  │           │         ├── record_transform_attempt(success/failed)
  │           │         └── If trans_us < orig_us by threshold:
  │           │             └── Install transformed winner + transform_id
  │           │
  │           └── No transform worked? → record_transform_gap()
  │
  ├── [existing] Winner selection, install binding
  │
  └── Return winner (original or transformed)
```

## Measuring the Transformed Path

The tuner already knows how to measure a candidate given a launch context. For transforms:

1. **Build an alternative candidate set** from the transformed signature: iterate registry, check `can_execute` on the transformed sig. This is the same screening loop as the existing code.
2. **Time the complete path**: for batch transforms, time the full batch iteration (not one batch). The dispatch layer will run the batch loop; we time exactly what it runs.
3. **Correctness check**: compare_outputs against the native reference output. This is critical — a transformation that produces different results must be rejected.

## Complete-Path Timing

For BATCH_FOR_MMVF, the complete-path timing must include:
- All batch iterations (num_batches kernel launches)
- Any pointer manipulation overhead
- Synchronization between batches (if any)

The `time_candidate` function already times the complete path of one candidate. For batch transforms, we need a wrapper that iterates:
```c
for (int batch = 0; batch < num_batches; ++batch) {
    // adjust src1 pointer and dst pointer for this batch slice
    time_candidate(candidate, lc_batch, ...);
}
```

This measures the actual dispatch-time cost.

## Binding with Transform

When a transformed path wins, the binding stored in `g_bindings` must carry:
- The original candidate (for the transformed sig)
- The transform_id
- The inverse_ctx data (for dispatch writeback)

The existing `Binding` struct needs extension. See HI31 for details.

## Guard Against Re-tuning

The result cache (`g_results`) is keyed on `dispatch_digest`. A transformed path produces a different signature, hence a different digest. But the **original** dispatch key still maps to the transformed winner — so we install the result under the **original** dispatch_digest. This prevents re-tuning: if the same original signature appears again, the cache returns the transformed winner immediately.

## Error Handling

- If the transformed path's can_execute fails at measurement time → record rejected attempt, try next transform
- If compare_outputs fails (nmse too high) → reject this transform, try next
- If no transform works → keep native binding, record gap

## Change Log

- 2026-08-06T03:05:59.606024+00:00 (created-by): Created by agent
- 2026-08-06T03:07:31.714428+00:00 (updated-by): Updated: section:notes
- 2026-08-06T03:37:51.547054+00:00 (updated-by): Updated: section:detailed_solution, section:code_samples, section:files, section:steps, section:validation
- 2026-08-20T07:23:22.474142+00:00 (updated-by): Updated: section:notes

## Ledger-events


- chg_20260820_081136_closed-out-the-13-outstanding_7991
- 2026-08-20T08:11:36.595946+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-21T00:05:11.382052+00:00 (updated-by): Updated: section:notes, section:steps
- chg_20260821_054740_the-routing-transform-tuner-co_1173
- 2026-08-21T05:47:40.499128+00:00 (updated-by): Updated: section:ledger-events
- chg_20260821_121835_fixed-a-production-breaking-co_3880
- 2026-08-21T12:18:35.948614+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-23T06:25:21.188774+00:00 (updated-by): Updated: section:notes
- 2026-08-23T06:25:28.319960+00:00 (state-transition): State: pending → completed
- chg_20260823_062912_closed-three-plan-items-whose_6343
- 2026-08-23T06:29:12.133195+00:00 (updated-by): Updated: section:ledger-events
