---
id: HI99
order: 0
plan: hip-autotune
state: completed
created-at: '2026-08-23T06:59:21.813011+00:00'
breadth: ''
skill: advanced
created-by: agent
work: M
---

# Tuner config X-macro: single source of truth for env overrides + header emission (HI96A)

## Description

Split out of HI96 (2026-08-23, plan-hygiene, audit §5.10 HI96A). This is the real correctness/provenance improvement of the three concerns and must not be blocked on the optional telemetry or cache-UX concerns.

Scope: GGML_HIP_TUNER_CONFIG_FIELDS X-macro generating BOTH the env-override block and the header emission from one field list, so a new tuning knob cannot be added without being recorded. This closes the specific failure mode that invalidated RV01-RV05 and recurred when the `compiler` knob was dropped for weeks.

Key design constraint (reassessed 2026-08-23): this is a real, careful refactor, not a mechanical drop-in. The ~20 existing env-override lambdas each carry bespoke per-field min/max validation bounds (e.g. FINAL_SAMPLES 2..100000, ALPHA 0.0..1.0, NOISE_PCT 0.0..max), which a simplified F(type,name,env) macro sketch does not account for. Design the macro to carry bounds too (e.g. F(type, name, env, min, max)) BEFORE attempting the mechanical rewrite of every existing call site.

## Steps

- Design the X-macro to carry per-field bounds: F(type, name, env, min, max), covering all ~20 existing bespoke bounds.
- Generate both the env-override block and the header emission from the single field list.
- Mechanically rewrite every existing env-override call site against the macro.
- Add a test asserting the header's config key set equals the macro's field set, so the omission class that recurred with `compiler` is caught mechanically.

## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files



## Validation

Adding a field to GGML_HIP_TUNER_CONFIG_FIELDS and rebuilding makes it appear in the header with no other edit; a test asserts the header's config key set equals the macro's field set (catching the `compiler`-omission class). All ~20 existing knobs' bespoke min/max bounds are preserved. Real HIP compiler + GPU available on this workstation for hardware validation (vendor/rocm/7.1, local RX 7900 GRE) -- see docs/reference/BUILD.md.

## Effort & Risk



## Standards



## Acceptance Criteria



## Notes

Split from HI96 (2026-08-23, plan-hygiene, audit §5.10). The optional machine-state telemetry is now HI100 and the --cache variant is now HI101.

CLOSED 2026-08-23. Design agreed with dev-gpt-agent (session ses_cb161714681840a2) before implementing, per standing process: request req_1857ea4ff8eb4ee5 established the plan (GGML_HIP_TUNER_CONFIG_FIELDS X-macro with an explicit wire_key column since confidence_alpha/"alpha" already disagree; flush_l2/flush_rewarm stay hand-written since they resolve two mutually-exclusive request flags into one enum plus a derived wire mirror, not a 1:1 scalar override; flush_evict_mb is an ordinary scalar and IS macro-driven; size_env() extended to accept min/max so INT/SIZE/DOUBLE fields share one table shape; macro-generated doubles use %.17g not the old %.4f display precision).

Implemented in feb1ac7: GGML_HIP_TUNER_CONFIG_FIELDS(F) declared in hip-autotune-tuner.cuh (17 direct scalar env-backed fields); env-override parsing and measurements.jsonl header emission both generated from the same table in hip-autotune-tuner.cu. 11 of the 17 fields were previously env-overridable but never appeared in the header at all (max_workspace_bytes, noise_canary_pct, hot_share_pct, noisy_mad_ratio, verify_determinism, emit_samples, pilot_samples, min_sample_us, max_launches_per_sample, elapsed_time_retry_max, elapsed_time_retry_backoff_us) -- a real provenance gap, the same failure-mode class as the historical `compiler` header field going missing for weeks.

Real hardware validation (gfx1100, ROCm 7.1): a real tuning run with GGML_HIP_TUNE_NOISE_PCT=7.5/MAX_WORKSPACE=12345678/ELAPSED_RETRY=4 produced a real measurements.jsonl header where all three (previously never emitted) now appear with the exact overridden values (`"max_workspace_bytes":12345678,"noise_canary_pct":7.5,...,"elapsed_time_retry_max":4`), alpha now records at full precision (0.025000000000000001, not the old 0.0250), and all pre-existing wire names are unchanged.

10 new source-contract tests (test_hi99_tuner_config_macro.py), including a governed-key-set-equality test (neither whole-header equality nor a bare subset, since the header legitimately carries non-config provenance the macro must never own -- checks (macro's own wire keys) union (literal hand-written keys) equals (expected governed config) union (expected provenance keys), with a disjointness check catching a hand-added config field bypassing the registry). 4 existing test files updated where they asserted on the old hand-written literal source shape the refactor intentionally changed (test_dispatch_safety.py, test_hi24_double_native.py, test_hi24_hot_list.py, test_hi34_flush.py).

Post-implementation review (dev-gpt-agent, same session, req_c40a1876389a469b): verdict CLOSE. Confirmed X-macro mechanics correct (adjacent-string-literal concatenation for the FMT fragment, leading-comma splice for the ARG fragment, FMT/ARG using the same macro in the same order so format/argument ordering cannot independently drift), confirmed size_env's extended bounds check cannot reject any previously-valid max_workspace_bytes value (0/SIZE_MAX bounds are no-ops given the pre-existing overflow check), and endorsed the governed-key-set test's static-analysis approach as a reasonable offline substitute for running a real preprocessor. Two non-blocking hardening suggestions applied in 7a13ecf: derive the test's macro-key set from the actual macro rows rather than a separate EXPECTED_FIELDS constant (so a future one-row macro addition needs only that one edit), and assert both the macro rows and literal header keys are duplicate-free before set-conversion (a set silently collapses duplicates otherwise).

Full offline suite: 1813 passed (up from 1805), 3 skipped, 61 subtests. No behavior change to tuning decisions -- purely a provenance/anti-drift refactor.

## Change Log

- 2026-08-23T06:59:21.813011+00:00 (created-by): Created by agent

## Ledger-events


- chg_20260823_070037_resolved-the-four-this-item-i_5123
- 2026-08-23T07:00:37.445452+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-23T13:18:26.958803+00:00 (updated-by): Updated: section:notes
- 2026-08-23T13:18:31.321575+00:00 (state-transition): State: pending → completed
- chg_20260823_131843_fixed-a-real-provenance-gap-in_3809
- 2026-08-23T13:18:43.432913+00:00 (updated-by): Updated: section:ledger-events
