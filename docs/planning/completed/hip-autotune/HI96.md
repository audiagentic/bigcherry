---
id: HI96
order: 0
plan: hip-autotune
state: superseded
created-at: '2026-08-23T04:59:56.991794+00:00'
breadth: ''
skill: advanced
created-by: agent
work: M
---

# Tuner config X-macro, machine-state telemetry, and workload-check --cache variant (HI37 Part 3)

## Description

Split off from HI37 (2026-08-23) so HI37's genuinely-completed scope (DB joinability Part 1, workload digest + call-weighted overlap Part 2, ggml_hip_diagnostics cleanup) can close honestly. This item is HI37's remaining Part 3 scope: (1) GGML_HIP_TUNER_CONFIG_FIELDS X-macro generating both the env-override block and the header emission from one field list, so a new tuning knob cannot be added without being recorded (closes the specific failure mode that invalidated RV01-RV05 and recurred when `compiler` was dropped for weeks); (2) optional Linux-only machine-state telemetry via sysfs (GPU clock/temp/power at run start/end); (3) a --cache variant of `bigcherry inventory workload-check` reading a real binary v5 replay cache instead of only a .measurements.jsonl tuned-signature set, once replay_cache.py is free of concurrent edits.

## Steps

1. Config X-macro: on closer inspection (2026-08-23) this is a real, careful refactor, not a mechanical drop-in -- the ~20 existing env-override lambdas each carry bespoke per-field min/max validation bounds (e.g. FINAL_SAMPLES 2..100000, ALPHA 0.0..1.0, NOISE_PCT 0.0..max), which the plan item's own simplified F(type,name,env) macro sketch does not account for. Design the macro to carry bounds too (e.g. F(type, name, env, min, max)) before attempting the mechanical rewrite of every existing call site.
2. Machine state: hipDeviceGetPCIBusId -> /sys/bus/pci/devices/<bdf>/drm/card*/... mapping, absent-not-zero on sysfs read failure. Lowest priority per the original item's own code_samples ("should land last, if at all").
3. --cache variant: reuse tools/bigcherry/inventory.workload_overlap() unchanged (it only needs a set[str] of tuned signature digests) once replay_cache.py's read_cache() can be safely imported from a session not actively editing it.

## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files



## Validation

Per HI37's original validation section: adding a field to GGML_HIP_TUNER_CONFIG_FIELDS and rebuilding must make it appear in the header with no other edit -- a test asserting the header's config key set equals the macro's field set catches the omission class that recurred with `compiler`. Real HIP compiler + GPU now confirmed available on this workstation for hardware validation (vendor/rocm/7.1, local RX 7900 GRE) -- see docs/reference/BUILD.md.

## Effort & Risk



## Standards



## Acceptance Criteria



## Notes

Split from HI37 2026-08-23 -- the config X-macro specifically was reassessed mid-session as materially riskier than first estimated (bespoke per-field bounds, ~20 call sites) and deliberately not attempted without more care than the remaining session budget allowed. HI37's other scope (DB joinability, workload digest + call-weighted overlap, dead-struct cleanup) is genuinely done and real-hardware-validated (cross-language digest agreement confirmed byte-for-byte on real data, 2026-08-23) and should not be blocked on this separate, higher-risk remaining piece.

## Disposition (2026-08-23, plan-hygiene, audit §5.10): SPLIT -- superseded by HI99 / HI100 / HI101

HI96 combined three concerns that should not share one completion state. Split into:
- HI99 -- Tuner config X-macro single source of truth (the real correctness/provenance improvement; the ~20 bespoke per-field bounds design constraint is preserved there).
- HI100 -- Optional Linux machine-state telemetry (lowest priority; should land last, if at all).
- HI101 -- workload-check --cache binary replay-cache input (tooling convenience).

Splitting prevents an optional sysfs feature (HI100) or a tooling convenience (HI101) from blocking closure of the config-schema provenance invariant (HI99). Do not reactivate HI96 as a mega-item; work the three sub-items.

## Change Log

- 2026-08-23T04:59:56.991794+00:00 (created-by): Created by agent

## Ledger-events


- chg_20260823_050048_closed-out-several-long-runnin_6246
- 2026-08-23T05:00:48.603906+00:00 (updated-by): Updated: section:ledger-events
- chg_20260823_050056_closed-out-hi16-hi24-and-hi3_2841
- 2026-08-23T05:00:56.310622+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-23T06:59:44.108183+00:00 (updated-by): Updated: section:notes
- 2026-08-23T06:59:45.783079+00:00 (state-transition): State: pending → superseded
- chg_20260823_070037_resolved-the-four-this-item-i_5123
- 2026-08-23T07:00:37.434795+00:00 (updated-by): Updated: section:ledger-events
