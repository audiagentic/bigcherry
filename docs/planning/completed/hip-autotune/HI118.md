---
id: HI118
order: 0
plan: hip-autotune
state: completed
created-at: '2026-08-25T06:40:01.842337+00:00'
breadth: ''
skill: advanced
created-by: agent
work: M
---

# Capture fused MUL_MAT(_ID)+GLU gate-tensor geometry in the dispatch signature schema

## Description

HI108's investigation (2026-08-25, confirmed by dev-gpt-agent) found that every GLU-op signature this tuner ever records is a fused MUL_MAT(_ID)+GLU epilogue: ggml_hip_dispatch_mul_mat() and ggml_hip_dispatch_family() both call the same ggml_hip_make_signature(src0, src1, ids, dst, fusion) (src/ggml/src/ggml-cuda/hip-autotune-signature.cpp ~line 270), which records only a coarse fusion-kind byte (sig.fusion, from ggml_hip_fusion_kind()) -- it does NOT capture the real gate tensor's shape/type at all. `fusion->gate` is a genuinely separate ggml_tensor* (vendor/llama.cpp/ggml/src/ggml-cuda/common.cuh ~line 1591, struct ggml_cuda_mm_fusion_args_host), so a fused GLU dispatch's recorded ne0/ne1/ned describe only ONE of the two underlying mul_mat_id projections (the x/up side) -- the gate side's geometry is irretrievably absent from every artifact this tuner has ever written for such a dispatch. dev-gpt-agent's explicit warning: do not infer the gate shape from the x side even where models happen to make them equal -- capture it for real.

## Steps

1. Extend ggml_hip_dispatch_signature_v1 (hip-autotune-types.h) with real gate-tensor geometry fields (at minimum gate ne/type; check whether gate_bias/x_bias/x_scale/gate_scale also need capturing for a faithful replay -- read ggml_cuda_mm_fusion_args_host's full field list before deciding scope).
2. Update ggml_hip_make_signature() (hip-autotune-signature.cpp) to populate the new fields from `fusion->gate` (and any other fusion fields decided in step 1) when fusion != nullptr.
3. Bump the signature schema version; update every schema-version-aware consumer (dispatch_db ingestion, replay, etc.) to handle the new field additively -- do not break existing artifacts written under the old schema.
4. Offline tests: a synthetic fused-GLU signature round-trips the new fields correctly; an old-schema artifact still loads (backward compat).
5. Real-hardware validation: rebuild, run a real GLU-fusing MoE campaign (Qwen3.6-35B-A3B, matching HI105/HI108's own real workload), confirm the new fields populate with real, sane gate-tensor geometry for the two dispatches HI108 already found (7ef2471585a5aa6fbb49384efe566ac5, d816ffce280de2a7ce2292c6406da23d).

## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files

- src/ggml/src/ggml-cuda/hip-autotune-types.h
- src/ggml/src/ggml-cuda/hip-autotune-signature.cpp
- tools/tests/ (signature schema tests)

## Validation

DONE 2026-08-25 -- REVISED SCOPE, much smaller than originally estimated. CORRECTED 2026-08-25 per dev-gpt-agent deep review (real, valuable findings, all verified against source before acting):

Gate shape proof CORRECTED: mmvq.cu's `ggml_are_same_stride(fusion->gate, src0)` assert only compares nb[] (byte strides), NOT ne[] (extents) -- confirmed directly (ggml.c's ggml_are_same_stride implementation is a pure nb[0..3] equality check). The REAL proof that gate shares src0's shape lives one layer up, in ggml-cuda.cu's ggml_cuda_should_fuse_mul_mat() (~line 1998): `ffn_up->src[0]->type != ffn_gate->src[0]->type || !ggml_are_same_shape(...) || !ggml_are_same_stride(...)` gates whether fusion happens AT ALL -- so a fusion this tuner ever observes already had gate's shape/type/stride matched against src0 before it could fuse; mmvq.cu's assert is a redundant downstream safety check, not the source of the guarantee. Corrected the code comments (hip-autotune-types.h, hip-autotune-signature.cpp) and test docstring to cite the real source.

Bias/scale wording WEAKENED per review: changed from 'geometry is algebraically derivable' to 'enough geometry to build a semantically equivalent synthetic replacement is derivable' -- exact original ne[2]/ne[3]/nb[] of the real bias/scale tensors is not fully reconstructible, only what's actually needed (F32, correct element count) to synthesize a working stand-in.

dst_gate attribution CORRECTED: it is NOT from patch 1207_rd17_moe_topk_down_fold.py as originally stated -- verified directly (grep) that patches/rd/1205_rd12_paired_mmvq_dual_output/patch.py (RD12) is the patch that actually adds the dst_gate field/kernel plumbing; 1207 only comments on sharing RD12's struct slot. Fixed the misattribution everywhere (code comments, test docstring).

Two real, non-blocking gaps identified and filed separately (HI120, not fixed here per dev-gpt-agent's own recommendation and confirmed correct scope call -- neither blocks HI119's upstream-GLU-fusion-only target): (1) RD12's real dst_gate fusion mode (gate set, dst_gate set, glu_op==NONE) reaches this tuner's dispatch hook and gets signed today via the implicit 'GATE+NONE means RD12' inference rather than an explicit flag -- not silently aliased with real GLU (glu_op is never NONE for real GLU), just less explicit than ideal. (2) RD17's x_scale_channel_dst mode changes x_scale's real semantics (length dst->ne[1], non-NVFP4-gated, channel-indexed) in a way the new X_SCALE presence flag doesn't distinguish from the upstream NVFP4 n_expert/1 sizing -- a real repo-wide completeness gap if RD17 is ever promoted.

Test coverage assessed as 'mechanically useful but semantically too weak' (fair) -- the 3 offline tests (bit uniqueness, field->flag wiring, sig.flags assignment ordering) catch wiring regressions but don't prove toggling a field changes the actual signature digest, or that legacy pre-HI118 artifacts are handled safely. Deferred a behavioral signature/digest test to HI119 (per review's own recommendation), where it's more naturally exercised against a real fused dispatch. HI119 must also fail closed (not silently treat as 'absent') on legacy v1 fusion records that predate these flags, since a zero bit there means 'writer predates HI118,' not 'genuinely absent.'

Real-hardware re-validated after all corrections: incremental rebuild on Brutus succeeds cleanly, offline suite green (2065 passed, only the known pre-existing Windows symlink-privilege test unrelated). Committed (comment corrections + doc updates, tuning-code-rebase, pushed).

## Effort & Risk



## Standards



## Acceptance Criteria



## Notes

Split from HI108 (2026-08-25) -- prerequisite for a real fused-GLU correctness-evidence harness (see the follow-up item this schema work unblocks). Do NOT attempt to build the evidence harness before this lands; without real gate geometry, any harness would have to guess the gate tensor's shape, which is exactly the mistake dev-gpt-agent's review warned against. Cross-reference: HI108 (closed, investigation only), HI105 (the MUL_MAT_ID mapper pattern this cannot directly reuse).

## Change Log

- 2026-08-25T06:40:01.842337+00:00 (created-by): Created by agent

## Ledger-events


- chg_20260825_064046_investigated-extending-correct_1939
- 2026-08-25T06:40:46.421536+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-25T07:41:05.429192+00:00 (updated-by): Updated: section:validation
- 2026-08-25T07:41:09.148047+00:00 (state-transition): State: pending → completed
- chg_20260825_074124_extended-the-tuning-dispatch-s_4182
- 2026-08-25T07:41:24.201351+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-25T08:11:31.014822+00:00 (updated-by): Updated: section:validation
- chg_20260825_081152_independent-review-caught-a-re_7832
- 2026-08-25T08:11:52.166115+00:00 (updated-by): Updated: section:ledger-events
