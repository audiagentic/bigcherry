---
id: HI108
order: 0
plan: hip-autotune
state: completed
created-at: '2026-08-24T07:35:07.812402+00:00'
breadth: ''
skill: advanced
created-by: agent
work: M
---

# Extend HI80 correctness-evidence generation to GLU (op=100) signatures, including MoE-routed GLU

## Description

HI105 extended HI80's correctness-evidence generation from MUL_MAT-only to also cover MUL_MAT_ID, but explicitly left GLU (op=100) out of scope -- deliberately, per HI80's own doctrine of deriving each op's mapping from its own real test_case subclass (test_glu) rather than assuming shared semantics from overlapping canonical fields (has_ids/n_expert appear on GLU rows too, since some GLU nodes are MoE-routed, but that does not make them MUL_MAT_ID).

During HI105's broader real MoE tune campaign on Qwen3.6-35B-A3B (2026-08-24), two real GLU candidates were found with genuine isolated kernel wins -- both currently stuck at rejected_no_correctness_evidence purely because the GLU mapper doesn't exist, the exact same tooling-gap shape HI105 fixed for MUL_MAT_ID:
- dispatch=7ef2471585a5aa6fbb49384efe566ac5: MoE-routed GLU (has_ids=true), ne0=[2048,256,256,1] ne1=[2048,1,1,1] ned=[256,8,1,1] -- candidate mmvq:q8_0:w1:nw1:rpb1:sk0:v1 found 14.1% isolated improvement.
- dispatch=d816ffce280de2a7ce2292c6406da23d: non-routed GLU (has_ids=false), ne0=[2048,512,1,1] ne1=[2048,1,1,1] ned=[512,1,1,1] -- candidate mmvq:q8_0:w1:nw4:rpb1:sk0:v1 found 4.3% isolated improvement.

A dev-gpt-agent deep-dive (2026-08-24) independently flagged this same gap, and separately noted (worth verifying against real source before acting): mainline llama.cpp already has CUDA-graph fusion recognizing {MUL_MAT_ID, MUL_MAT_ID, GLU} sequences (from merged upstream PR #16715, restricted to ncols_dst==1/decode per HI26's own notes) -- meaning the routed GLU signature above may already be running through a FUSED epilogue in production, which would change what 'the candidate' for that dispatch even means and needs to be understood before writing a mapper.

## Steps

1. Read test-backend-ops' real test_glu case (mirror HI105's own process for test_mul_mat_id) to derive the real op_filter/target_tensor/shape semantics for GLU -- do not assume MUL_MAT_ID's conventions transfer.
2. Verify GPT's PR #16715 CUDA-graph-fusion claim against real source (ggml-cuda's graph fusion code) before assuming it's accurate or relevant -- if the routed GLU signature is already running fused in production, the correctness-evidence generator needs to evidence the FUSED path, not GLU in isolation, or the evidence would be for an op that never actually executes standalone.
3. Add signature_to_glu_test_file_line() (or equivalent) to signature_correctness_mapping.py and route it through signature_to_any_test_file_line()'s dispatcher, following HI105's exact pattern.
4. Validate on real hardware against the two real dispatches above; confirm via HI80 CLI + tune-promote whether they actually reach promoted status.
5. If promoted, consider whether a real end-to-end A/B is warranted given HI105's finding that MoE-routed candidate wins of this size (4-14%) did not move full-model throughput for the MUL_MAT_ID case -- do not assume this one will either without measuring.

## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files



## Validation

INVESTIGATED 2026-08-25, NOT IMPLEMENTABLE AS ORIGINALLY SCOPED -- split into HI118 (prerequisite: capture gate-tensor geometry in the signature schema) and HI119 (the real fused correctness-evidence harness). Did steps 1-2 of this item's own steps for real before writing any mapper code:

Step 1/2 finding: read ggml_hip_dispatch_mul_mat() (src/ggml/src/ggml-cuda/hip-autotune-dispatch.cu ~line 1085) and confirmed (with dev-gpt-agent's correction) that BOTH ggml_hip_dispatch_mul_mat() and ggml_hip_dispatch_family() call the same ggml_hip_make_signature(src0, src1, ids, dst, fusion) (hip-autotune-signature.cpp ~line 270) -- every GLU-op signature this tuner has ever recorded is therefore a fused MUL_MAT(_ID)+GLU epilogue (dst is the GLU output; src0/src1 are the underlying mul_mat_id's own weight/activation tensors for ONE of the two projections), never a standalone GLU built from two already-materialized tensors the way test_glu_split builds its test-backend-ops graph. This confirms dev-gpt-agent's earlier PR #16715 CUDA-graph-fusion suspicion was correct.

Decisive finding beyond the original suspicion: read the real struct (vendor/llama.cpp/ggml/src/ggml-cuda/common.cuh ~line 1591, ggml_cuda_mm_fusion_args_host) -- `fusion->gate` is a SEPARATE ggml_tensor*, not folded into src0/src1 at all. ggml_hip_make_signature() only records a coarse fusion-kind byte (sig.fusion / ggml_hip_fusion_kind()); it never captures the gate tensor's own shape. So the recorded ne0/ne1/ned for a fused-GLU dispatch describe only the x/up-side projection -- the gate side's real geometry is irretrievably absent from every artifact this tuner has ever written for such a dispatch. HI108's own real data (candidate names like 'mmvq:q8_0:w1:nw1:rpb1:sk0:v1') confirms what's actually tunable here is the underlying MUL_MAT_ID kernel choice, fused with a fixed GLU epilogue -- not a standalone GLU kernel.

Consequence: test-backend-ops' --test-file/test_generic_op escape hatch (HI80/HI105's mapper pattern) can only build ONE op per test case (confirmed via make_test_cases_from_file(), test-backend-ops.cpp ~line 10528) and cannot represent this fused two-op compound. There is no safe one-line mapper for this op -- attempting one (e.g. treating recorded src0/src1 as a GLU a/b pair) would silently evidence the wrong computation. signature_to_any_test_file_line() ALREADY fails closed correctly for GLU today (falls through to its final SignatureMappingError with a clear message) -- this is not a safety bug, just a genuinely unimplemented op, and the two real blocked dispatches correctly stay at rejected_no_correctness_evidence rather than getting bad evidence.

dev-gpt-agent confirmed this analysis independently (asked to verify against the real struct/source before answering) and added one important warning this item's follow-ups must honor: do not infer the gate tensor's shape from the x-side tensor's shape even where a given model happens to make them equal -- capture it for real.

Split into: HI118 (schema work -- capture real gate-tensor geometry, hard prerequisite) and HI119 (the actual fused correctness-evidence harness, gated on HI118). Both real, well-scoped, materially bigger than HI105's single-op mapper pattern this item originally assumed would transfer. Steps 3-5 of this item's own original steps (write the mapper, validate on real hardware, consider an A/B) are absorbed into HI118/HI119 rather than closed here as done, since the mapper approach they described is now known not to work.

## Effort & Risk



## Standards



## Acceptance Criteria



## Notes

Filed 2026-08-24, direct follow-up from HI105's real campaign and a dev-gpt-agent deep-dive on the same day. Ranked by GPT as 'worth completing but validation completeness rather than the next likely end-to-end win' -- lower priority than [[HI107]] (signature time-share audit) and RD30 (compacted MoE MMQ tiling), but real and well-scoped. Cross-reference: [[HI105]] for the exact pattern to follow and the real dispatch data.

## Change Log

- 2026-08-24T07:35:07.812402+00:00 (created-by): Created by agent
- 2026-08-24T21:16:50.566807+00:00 (updated-by): Updated (no visible changes)
- 2026-08-25T06:40:36.175257+00:00 (updated-by): Updated: section:validation
- 2026-08-25T06:40:38.555012+00:00 (state-transition): State: pending → completed

## Ledger-events

- chg_20260825_064046_investigated-extending-correct_1939
- 2026-08-25T06:40:46.408808+00:00 (updated-by): Updated: section:ledger-events
