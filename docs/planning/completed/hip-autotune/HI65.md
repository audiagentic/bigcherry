---
id: HI65
order: 9
plan: hip-autotune
state: completed
created-at: '2026-08-16T15:36:50.327788+00:00'
breadth: ''
skill: advanced
created-by: agent
work: M
---

# Attribute B1 measurement-context sensitivity: cache residency vs eviction preconditioning

## Description

HI34 B1 (2026-08-17, closed per GPT verdict) falsified measurement-context invariance: four replicated material median-crossover winner reversals (all blas<->mmvf), plus 63 cold-arm fail-closed native-instability rejections. But the absolute shifts were not uniformly in the eviction direction (some candidates faster under flush), so B1 established MEASUREMENT-CONTEXT SENSITIVITY without attributing causation to L2/Infinity-Cache residency. This item produces that attribution — or explicitly fails to — via a bounded diagnostic matrix, so that B2 (HI24 step 7) calibrates against a settled measurement context and the HI35 re-baseline uses settled semantics.

Purpose is NOT 'prove cache causation'. Purpose: reach a defensible MEASUREMENT-POLICY DECISION with orthogonal controls. The item must not pass because one explanation looks plausible; its closure condition is a supported policy decision, one of exactly three terminal states.

## Steps

- Bounded diagnostic matrix on Brutus (same GPU/build as B1, lps=1, unblocked, hot brackets around the experiment): the 4 B1 material crossover signatures (2bd325e861971d70687bec3ba6667f48, 38ee4d7037e60cdb0405ec302b191ed2, ee8a2c18d47bd4b7a9244a1dbe7454dd, f33c999707eec598f5fd24618ca00a29) PLUS at least 4 matched negative controls that did not move in B1, covering similar blas/mmvf families and latency/working-set ranges. Positive-only sampling would make almost any intervention look explanatory.
- Intervention arms: size sweep 128 / 256 / 512 MB (locked in RV47) plus a small-size control (NOT treated as the sole DVFS control - a smaller buffer simultaneously changes cache coverage and the amount/duration of GPU work, so it does not orthogonalize those effects), plus an evict-then-rewarm arm: 256 MB eviction -> synchronize -> UNTIMED candidate/operand rewarm -> synchronize -> timed candidate. The rewarm preserves much of the preceding eviction workload while deliberately restoring hot residency; it is not mathematically perfect (the rewarm itself affects clocks) but combined with the size response and device telemetry is far more diagnostic than a tiny-buffer arm alone.
- Implement the pre-sample intervention in hip-autotune-tuner.cu as a SINGLE internal enum NONE / EVICT / EVICT_REWARM (per GPT sign-off condition): env vars map into that one mode, invalid combinations are unrepresentable rather than runtime-rejected, and the resolved mode is recorded in the artifact header. Threaded through time_candidate like the existing flush seam, with contract tests pinning: kernel ordering eviction -> sync -> untimed rewarm -> sync -> timed window; forced lps=1 for EVICT and EVICT_REWARM; default NONE.
- Collect HI60-style SMI clock/thermal/power telemetry around every arm and apply HI60's stable/corrected/unresolved treatment - reuse that vocabulary and tooling, do not invent a new telemetry system.
- Evaluate with tools/residency_gates.py (or an extension of it): crossover penalties and absolute candidate shifts per signature per arm; native-unstable rejection counts per arm.
- Record the terminal decision in HI34 notes + COVERAGE_AUDIT.md, whichever of the three states results, and set the follow-on policy for B2/HI35 accordingly.

## Detailed Solution & Technical Design

TERMINAL DECISIONS (exactly one, mandatory):

1. CACHE-SUPPORTED: intervention evidence identifies residency as material enough that measurement-context design must be reopened before B2 (e.g. crossover tracks eviction size with onset/saturation, collapses under evict-then-rewarm, and telemetry is clean).
2. PRECONDITIONING-SUPPORTED: B1's 256 MB mechanism perturbed selection principally through non-cache state (crossover independent of rewarm, tracks clock/power/thermal shifts, or size response flat while telemetry moves); hot/default context remains the policy baseline; B2 may proceed.
3. UNRESOLVED-MATERIAL: material inversions persist but cause cannot be separated; do NOT silently choose flush=0 or flush=1 as 'truth'; measurement-context design stays blocked and a narrower follow-up is required.

ACCEPTANCE CRITERIA (locked before running):
1. Reproducibility prerequisite: hot bracket runs show no material crossover on the diagnostic signatures. If a B1 positive cannot reproduce in the same basic hot context, causal attribution is INCONCLUSIVE.
2. B1 reproduction: at 256 MB the four B1 inversions must reproduce IN DIRECTION against both hot brackets. Failure to reproduce means B1 remains historically valid evidence of context sensitivity but those four cannot be used as stable causal probes.
3. Size-response evidence: record crossover penalties and absolute candidate shifts at 128/256/512 MB. Do NOT require naive monotonicity (cache replacement and DVFS are nonlinear); what matters is whether an onset/saturation pattern exists and whether 256/512 give materially consistent ranking behavior.
4. Rewarm discriminator: if eviction causes the inversion but eviction->rewarm collapses it back to the hot ordering, strong support for residency/cache-state causation; if the inversion survives rewarm, cache residency alone is insufficient and preconditioning/fabric/DVFS remains implicated.
5. Telemetry discriminator: no causal 'cache' conclusion is permitted when the relevant runs have unresolved clock-drift evidence or when the crossover tracks a systematic clock/power/thermal state change (HI60 fail-closed vocabulary).
6. Negative controls: the matched non-B1 signatures must be reported under EVERY intervention; if many controls flip too, that points to a global preconditioning artifact rather than shape-specific cache residency.
7. Cold-instability accounting: record native-unstable rejection counts by 128/256/512 and rewarm control. B1's count of 63 is a dependent variable: does instability scale with intervention strength, survive rewarm, or disappear?
8. Terminal decision recorded per the three states above; closure requires the supported policy decision, not plausibility.

SEQUENCE CONTEXT (locked by GPT B1 close-out): B1 close -> THIS ITEM -> HI35 re-baseline under settled semantics -> B2 -> Slice C. B2 non-semantic scaffolding may proceed in parallel, but no B2 acceptance experiment or retention-default freeze before this item closes.

## Code Samples & Guidance



## Files

- src/ggml/src/ggml-cuda/hip-autotune-tuner.cu
- src/ggml/src/ggml-cuda/hip-autotune-tuner.cuh
- tools/residency_gates.py
- tools/tests/test_hi34_flush.py

## Validation

Contract tests for the intervention enum (kernel ordering: eviction -> sync -> untimed rewarm -> sync -> timed window; resolved mode recorded in header; forced lps=1 for both active modes; default NONE; no independent second boolean exists to combine). Diagnostic matrix artifacts under artifacts/ on the Brutus tree. Full offline gate green (tests + audit) at the commit that adds the mode.

## Effort & Risk

M. The tuner mode is small and mechanical (threads through like flush_l2); the risk is interpretive, not code: the whole item can be invalidated by DVFS confounds, hence the telemetry discriminator and the mandatory tri-state decision rather than a binary pass.

## Standards

HIP_AUTOTUNE_STANDARDS 8 (measurement hygiene), HI60 telemetry vocabulary (stable/corrected/unresolved), fail-closed acceptance list from the locked B1 design

## Acceptance Criteria



## Notes

Created 2026-08-17 per GPT B1 close-out verdict (session ses_0b27e98b49bc4e9f, request req_759a9cadad8d49a8, at 97c1b54), which chose option (i): cause-validation BEFORE B2. Calibrating B2 against a context that may shortly change is unnecessary churn; do not run or close B2 in parallel.

Created 2026-08-17 per GPT B1 close-out verdict (session ses_0b27e98b49bc4e9f, request req_759a9cadad8d49a8, at 97c1b54), which chose option (i): cause-validation BEFORE B2. Calibrating B2 against a context that may shortly change is unnecessary churn; do not run or close B2 in parallel.

IMPLEMENTATION ADJUSTMENT (GPT sign-off condition, request req_fb4243ba99a0488e): model the pre-sample intervention internally as a SINGLE enum/state - NONE / EVICT / EVICT_REWARM - rather than adding a second independent flush_rewarm boolean beside flush_l2. Invalid combinations must be UNREPRESENTABLE, not rejected at runtime. Env vars still map into that one mode (GGML_HIP_TUNE_FLUSH_L2 selects EVICT when set; the rewarm arm gets its own env var mapping to EVICT_REWARM), and the RESOLVED mode is what gets recorded in artifact provenance/header so arms stay measurement-distinguishable. The existing B0 flush_l2 bool may remain as the wire format for NONE vs EVICT but the tuner core must branch on the enum, and any code path that would combine EVICT_REWARM with an independent second flag is a compile-time impossibility.



STEP 1 DONE (commit 510ecaa, pushed): single-enum pre-sample intervention implemented per the locked sign-off condition. ggml_hip_pre_sample_mode {NONE=0 default, EVICT=1, EVICT_REWARM=2} in hip-autotune-tuner.cuh; get_config maps GGML_HIP_TUNE_FLUSH_L2->EVICT and new GGML_HIP_TUNE_FLUSH_REWARM->EVICT_REWARM, both-set fails closed at parse time (valid=false). flush_l2 retained only as resolved 0/1 wire mirror for artifact emission. EVICT_REWARM = evict + checked stream sync, then ONE untimed rewarm launch of the candidate with checked hipGetLastError + hipStreamSynchronize, all before host_start/hipEventRecord(start). lps=1 clamp keys on the enum (covers both eviction modes). Artifact header emits pre_sample_mode provenance string (none/evict/evict_rewarm) alongside flush_l2/flush_evict_mb. time_candidate and run_counterbalanced_round take the enum as a required parameter; all six call sites pass config.pre_sample_mode. 8 new contract tests in tools/tests/test_hi65_pre_sample.py pin kernel ordering, fail-closed parse, wire-mirror derivation, header provenance, stable mode vocabulary; B0 threading tests updated to the enum (test_hi34_flush.py), one stale pin fixed in test_dispatch_safety.py. Gates: 641 tests OK (3 skipped), audit 33/33 PASS, tuner object compiles clean under ROCm 7.1.

GPT review of 510ecaa pending (session ses_0b27e98b4e9f, request req_d14d40e5cd374bcd). After approval: Brutus diagnostic matrix per the locked acceptance criteria (4 B1 crossover signatures + >=4 matched negative controls; size sweep 128/256/512 MB + small-size control; EVICT_REWARM arm; HI60 telemetry discriminator).



GPT STEP 1 VERDICT: PASS (session ses_a8ba61729ac84ee8, request req_6f6860495a954ade). One pre-run integration requirement attached: the matrix evaluator must fail closed on the new header provenance before the expensive run.

PRE-RUN EVALUATOR DONE (commit e644661, pushed): tools/residency_gates.py gains repeatable --arm NAME=MODE[@MB] declarations; evaluate(expected_arms) checks exact pre_sample_mode per declared arm plus optional flush_evict_mb and cross-arm source_revision/manifest_hash identity; mismatch => header_check.status=fail + CLI exit 2, gates diagnostic-only. Artifacts lacking pre_sample_mode are unattributable and fail closed (they cannot be told apart EVICT vs EVICT_REWARM via the legacy flush_l2=1 mirror). 11 new tests; 652 total OK, audit 33/33 PASS.

MATRIX PLAN SUBMITTED TO GPT (request req_9ad9cf43d38c46ad): Brutus GPU0, B1 harness/env (MUL_MAT sweep, 15/15 samples, lps=1, graphs disabled, unblocked), 7 arms in order: n1(NONE) -> e128 -> e256 -> e512 -> e32(small control) -> r256(EVICT_REWARM@256) -> n2(NONE); continuous SMI telemetry per arm, HI60 classification. Open questions to GPT: small-size value (proposed 32MB), outer vs per-arm NONE brackets (7 vs 13 sweeps), control selection method.

NEGATIVE CONTROL SELECTION (from local B1 artifacts, tmp/hi65-controls.py): B1 direction is hot=blas -> cold=mmvf on ALL FOUR crossovers. Stable (winner unchanged h1/cold/h2) signatures with the exact crossover family mix {blas:native:v1, mmvf:native:v1, mmvf:native:v1#twin}: 11 in 20-90us, all mmvf hot-winners; none with a blas hot-winner exists (inherent B1 asymmetry to flag to GPT). Proposed 5 controls spanning the latency band: af4142f75eab2a50e87edeb55fe9fba5 (20.1us), 341a29207a79c726920083c49850fc18 (22.9us), 003090c88abb484538028cc3489b0a67 (31.2us), 7d87f5ddc86bf97feed2720a420e857a (38.3us), 65dde5d538db7df4dae411fbee7c5f7d (103.4us, covers the ~80-103us end of crossovers 38ee4d70/f33c9997).

PRE-RUN BRUTUS PREP: B1 tree at /mnt/vault/development/llmhosts/bigcherry-b1 predates 510ecaa; must fetch, python3 -m bigcherry apply --force, rebuild the tuner object + relink test-backend-ops before launching. Sweep script ready at tmp/hi65-matrix.sh (7 arms, per-arm DBs + 5s telemetry logs).



GPT MATRIX LAUNCH VERDICT: PASS (request req_9ad9cf43d38c46ad), with locked adjustments:
- E32 kept, interpreted as small-intervention / partial-cache-pressure point (below Navi31 LLC scale, above reported 6MB L2); NOT a DVFS control.
- 8 sweeps instead of 7, pre-registered deliberately non-monotonic order with midpoint NONE checkpoint: n1(NONE) -> e256 -> e32 -> e512 -> nm(NONE) -> r256(EVICT_REWARM@256) -> e128 -> n2(NONE). E256/E32/E512 interpreted vs N1+NM; R256/E128 vs NM+N2; all three NONE arms = experiment-wide baseline envelope.
- Before each sweep, telemetry must return to the baseline idle envelope (not a fixed duration); a NONE checkpoint with material crossover or unresolved HI60 clock state makes its segment inconclusive, not evidence.
- Deterministic matched-control procedure locked: one unique control per positive; eligibility requires both blas:native and mmvf:native successfully measured in all three B1 arms, same winner across H1/C/H2, no material median crossover; cold data used ONLY for eligibility; ranking on H1/H2 characteristics only (winner latency scale, BLAS<->MMVF hot margin per hot arm, canonical ne0/ne1 shape); ties by signature digest; positives in ascending digest order.

PRE-REGISTERED SELECTION TABLE (procedure tmp/hi65-select-controls.py; weights 0.5 latency + 0.25 margin + 0.25 shape; 106 eligible controls): positive -> control (control winner, positive h1 med us, control h1 med us):
- 2bd325e861971d70687bec3ba6667f48 -> 36a2fba20fb1f6160686aaf156fcf285 (mmvf, 37.96 / 41.56) score 0.051
- 38ee4d7037e60cdb0405ec302b191ed2 -> a54ad462eabb52ba0293e92cb601f4e9 (mmvf, 83.44 / 31.36) score 0.582
- ee8a2c18d47bd4b7a9244a1dbe7454dd -> 9734d537d5b1da4a5e7c0c1c8aecc498 (mmvf, 25.96 / 26.44) score 0.106
- f33c999707eec598f5fd24618ca00a29 -> 003090c88abb484538028cc3489b0a67 (mmvf, 82.84 / 31.16) score 0.624
Known limitation to report: NO stable control with a blas hot-winner exists in the B1 population (all eligible controls are mmvf hot-winners), so margin matching is on magnitude only for the two ~80us positives; best available latency-scale match under the locked scoring.

Sweep script tmp/hi65-matrix.sh updated to the approved 8-arm order with per-arm 5s telemetry logs and a pre-sweep baseline idle gate (util<=5%, 10-min cap, timeout marks the segment inconclusive).



MATRIX LAUNCHED (2026-08-17 09:02 +10:00 Brutus time, GPU0): tree at cf83b4a, rebuilt clean; EVICT_REWARM path smoke-verified first (3-sample full MUL_MAT run: clean exit, 1156 rows, header pre_sample_mode=evict_rewarm / flush_evict_mb=256, kept at artifacts/h65-smoke/). Full 8-arm sweep running via tools/h65-matrix.sh on Brutus (nohup): n1 -> e256 -> e32 -> e512 -> nm -> r256 -> e128 -> n2, per-arm DBs + 5s telemetry logs under artifacts/h65/, baseline idle gate before each arm. ETA ~5-6 hours. GPT notified (request req_3510265987934e4f). On completion: copy artifacts locally, run residency_gates with declared arms per the approved interpretation pairs (e256/e32/e512 vs n1+nm; r256/e128 vs nm+n2), classify telemetry with HI60 vocabulary, evaluate negative controls under every arm, then terminal decision recommendation to GPT.



HARDWARE ADAPTATION (user-directed, 2026-08-17): matrix moves from Brutus to the LOCAL workstation 7900 GRE. Both Brutus XTX cards hold ~94 percent VRAM under a production llama-server workload; running sweeps there would block that serving, and the free Brutus cards are gfx1201/gfx1030 (different architectures). The local GRE reports gfx1100 (same RDNA3 family as B1's XTX) and is dedicated to this work. Declared deviations:
- D1: different chip within the family (7900 GRE vs 7900 XTX, fewer CUs). Criterion-2 B1 reproduction is best-effort; fallback = fresh positive set from a preliminary NONE vs EVICT@256 scan on the GRE before the full matrix.
- D2: WDDM - every arm runs HIP_LAUNCH_BLOCKING=1 (uniform across arms; serialised launches). Context deviation from B1's unblocked runs, disclosed in records.
- D3: Windows telemetry = GPU-engine utilization + adapter memory only (no rocm-smi/amdsmi/WMI clocks on this install); clock state is UNRESOLVED under HI60 vocabulary unless utilization envelope + cross-arm consistency support stable. Consequence: a CACHE-SUPPORTED terminal decision additionally requires unambiguous rewarm collapse AND size response AND no systematic utilization drift; otherwise PRECONDITIONING-SUPPORTED or UNRESOLVED-MATERIAL.
GPT sign-off on the adaptation requested (session ses_f3356f0e68004b0e, request req_79d9f487cfbc4c40) but the gpt-auto provider is in a reconciliation loop (EXT-GPTAUTO-004 unresolved-turn-not-reconciled after its CDP/browser outage); launch proceeded per user direction and will be submitted for review as soon as the provider recovers.

LOCAL PREP DONE: vendor re-synced, test-backend-ops.exe rebuilt with the enum (ninja clean), EVICT_REWARM smoke on WDDM passed (3-sample full MUL_MAT sweep: exit 0, 1157 lines, header pre_sample_mode=evict_rewarm / flush_evict_mb=256, only 8/1156 genuinely incomplete rows). Smoke wall time 3m46s at 3 samples -> ~20-25 min per production arm at 15/15; full matrix ETA ~3-4 hours. Learned: tuner silently skips DB recording when the parent directory does not exist (sweep script pre-creates it); local runs need ROCm 7.1 bin+lib on PATH and HIP_VISIBLE_DEVICES=0. Sweep driver: tmp/hi65-matrix-local.ps1 (8 arms, per-arm typeperf utilization+memory telemetry at 10s, idle gate util<=5 percent with 10-min cap, single-instance lock).



LOCAL MATRIX RESULTS (2026-08-17, 7900 GRE gfx1100, all arms HIP_LAUNCH_BLOCKING=1):
Execution: pass 1 (arms n1-e512 valid; nm/r256/e128/n2 invalidated by a PowerShell env-leak bug - Set-Item never unsets, so flush vars leaked across arms; corrupted files archived in artifacts/h65-local-pass1-corrupted/) + pass 2 re-runs of the four broken arms with per-arm env clearing. All 8 final headers verified fail-closed against intended modes (none/evict@32|128|256|512/evict_rewarm@256), 1156 signatures each, source_revision 4801e3c5 identical across arms. Artifacts: artifacts/h65-local/<arm>.jsonl.measurements.jsonl (~1.18GB each).

B1 reproduction (criterion 2 / D1): the four B1 XTX crossover signatures do NOT reproduce on GRE - all arms win mmvf for all four, no flips. Fallback clause applies: fresh positive set from the matrix itself.

Noise floor (exact-winner differences, 1156 sigs): NONE-vs-NONE pairs n1/nm=67, n1/n2=60, nm/n2=52 (4.5-5.8% - WDDM launch-blocking context is noisier than B1's XTX).
Intervention vs NONE difference counts: e32=50-60 (at floor), e128=71 (slightly above), e256=85-93, e512=107-116 (~2x floor), r256=77-93. Monotonic size response in the EVICT arms; REWARM does NOT collapse toward NONE - r256 sits at e256 level.
Family-level replicated flips (all 3 NONE arms agree F1, >=2 interventions agree F2): 3 of 1150 stable-baseline signatures: 73963166 mmvf->mmf (e512,e128,r256), e3f22ddb mmq->mmvq (e256,e512,r256), f24c61e9 mmvf->blas (e256,e512,r256). Size onset between 128 and 256 MB; rewarm persists.
Official gates (tools/residency_gates.py, declared-arm provenance, all header_check=ok): gate1 flip rates 4.5-5.8%. Gate-2 hard-gate survivors (replicated + material median crossover vs BOTH hot arms, 5% materiality): e256=2 (c6227cbe mmvf bs128-vs-bs64, cold_adv 15%; d26beab2 mmq native-vs-iq4 kernel, cold_adv 28%), r256=1 (16331a62), e32/e512/e128=0. Note e512's larger raw flip count is dominated by sub-material variant churn at this noise floor.
Telemetry (10s typeperf GPU-engine util, D3): no systematic cross-arm drift; per-arm means 0.4-3.4%, adapter mem 4.6-5.2GB; clock state UNRESOLVED for all arms under HI60 vocabulary (unobservable on WDDM).

TERMINAL DECISION RECOMMENDATION: PRECONDITIONING-SUPPORTED (weak-magnitude, mechanism-unconfirmed). Rationale against locked definitions: (1) CACHE-SUPPORTED requires collapse under evict_rewarm - NOT observed; r256 reproduces the e256/e512 flips and sits at their difference level, so residual cache residency is not the principal driver. (2) PRECONDITIONING-SUPPORTED condition 'crossover independent of rewarm' IS directly observed, with monotonic size onset in EVICT arms; the clock/power/thermal-tracking alternative condition is untestable under D3. (3) Not UNRESOLVED-MATERIAL because the intervention effect IS separable from the rewarm control in direction, though its physical mechanism (DVFS vs allocator vs launch-serialization interaction) stays unidentified and B1's XTX crossover population itself does not reproduce on this chip. Residual caveat: D2 (launch-blocking on all arms) means the effect magnitude is a WDDM-context bound, not an XTX transferable number; B1's materiality stands as established on XTX with cause now attributed to non-cache preconditioning in the evict context pending a clock-instrumented rerun (requires Brutus XTX availability). GPT adjudication of this recommendation PENDING - provider was down (all sessions closed, EXT-GPTAUTO reconciliation loop) at recording time; request req_79d9f487cfbc4c40 (hardware adaptation acceptance) also still undelivered and must be submitted together with the terminal-decision question once the provider recovers.



LOCAL MATRIX EXECUTED AND ANALYZED (2026-08-17, 7900 GRE gfx1100, WDDM, HIP_LAUNCH_BLOCKING=1 all arms):
Execution: pass 1 (arms n1-e512 valid; nm/r256/e128/n2 INVALIDATED by a PowerShell env-leak bug - Set-Item never unsets, so e512's FLUSH_L2/MB leaked into nm and dual-flag disabled tuning for r256/e128/n2; corrupted artifacts archived in artifacts/h65-local-pass1-corrupted/) + pass 2 re-runs of the four broken arms with per-arm env clearing. All 8 final headers verified fail-closed against intended modes (n1/nm/n2=none, e32/e128/e256/e512=evict@size, r256=evict_rewarm@256), 1157 lines each, source_revision 4801e3c5 identical across all arms. Artifacts: artifacts/h65-local/<arm>.jsonl.measurements.jsonl (~1.18GB each).

B1 reproduction (criterion 2 / D1 fallback): the four B1 XTX crossover signatures do NOT reproduce on the GRE - all eight arms win mmvf for all four, no flips anywhere. D1 fallback clause applies: fresh positive set from this matrix's own population.

Fresh-population results (exact-winner differences over 1156 signatures):
- NONE baseline noise floor (WDDM context): n1/nm=67, n1/n2=60, nm/n2=52.
- Intervention vs each NONE arm: e32=50-60 (at floor), e128=71, e256=85-93, e512=107-116 (~2x floor), r256=77-93.
- MONOTONIC size response in EVICT arms; REWARM does NOT collapse (r256 sits at e256 level).
- Family-level replicated flips (all 3 NONE agree F1, >=2 interventions agree F2): 3 of 1150 stable-baseline: 73963166 mmvf->mmf, e3f22ddb mmq->mmvq, f24c61e9 mmvf->blas; all three flip under e256/e512 and persist under r256 (size onset between 128 and 256 MB).
Official gates (tools/residency_gates.py with declared-arm provenance, all header_check=ok): gate1 flip rates 4.5-5.8% (WDDM noisier than B1's XTX hot arms). Gate-2 hard-gate survivors (replicated + material median crossover vs BOTH hot arms at 5% materiality): e256=2 (c6227cbe mmvf bs128-vs-bs64 cold_adv 15%; d26beab2 mmq native-vs-iq4 kernel cold_adv 28%), r256=1 (16331a62), e32/e512/e128=0. No cross-family material crossovers of B1's blas<->mmvf kind beyond the single f24c61e9 family flip (sub-material at gate threshold).
Telemetry (D3): typeperf 3D-engine utilization + adapter memory, 10s cadence; no systematic cross-arm drift (means 0.4-3.4%, mem 4.6-5.2GB); clock state UNRESOLVED for every arm under HI60 vocabulary (unobservable on WDDM).

TERMINAL DECISION RECOMMENDATION: PRECONDITIONING-SUPPORTED (weak-magnitude, mechanism-unconfirmed). Locked-definition mapping: (1) CACHE-SUPPORTED requires collapse under evict_rewarm - NOT observed; r256 reproduces the e256/e512 flips and sits at their difference level, so residual cache residency is not the principal driver. (2) PRECONDITIONING-SUPPORTED's 'crossover independent of rewarm' condition IS directly observed, with monotonic size onset in the EVICT arms; the clock/power/thermal alternative sub-condition is untestable under D3 and left open. (3) UNRESOLVED-MATERIAL not selected because the intervention effect IS separable from the rewarm control in direction, though its physical mechanism (DVFS vs allocator vs launch-serialization interaction) stays unidentified. Residual caveats: B1's XTX crossover population does not reproduce on this chip, so B1's materiality stands as an XTX-context finding; D2 (launch-blocking everywhere) bounds the observed magnitudes to this WDDM context; a clock-instrumented rerun on Brutus XTX (when llama-server frees VRAM) is the definitive follow-up for mechanism identification. GPT adjudication of this recommendation PENDING - the gpt-auto provider was down at analysis time (all sessions closed, EXT-GPTAUTO reconciliation loop); the hardware-adaptation acceptance question (req_79d9f487cfbc4c40) is also still undelivered and must be resubmitted together with the terminal-decision request once the provider recovers.



GPT ADJUDICATION (final, 2026-08-17) - ALL THREE QUESTIONS ANSWERED IN FAVOR:
Q1 HARDWARE ADAPTATION: ACCEPTED with deviations exactly as scoped. D1 acceptable under the fresh-positive fallback (criterion-2 reproduction explicitly NOT satisfied; the GRE matrix stands on its own fresh material positives). D2 acceptable for within-matrix causal contrasts because invariant across NONE/EVICT/REWARM arms, but must be stated as conditional on gfx1100 GRE + Windows/WDDM + launch blocking (launch-blocking can alter inter-launch spacing/synchronization and thus GPU state - more than just bounding magnitudes). D3 acceptable as HI60 UNRESOLVED telemetry; prevents DVFS attribution or exclusion but does not invalidate the observed EVICT-vs-REWARM relationship. The GRE matrix must NOT supersede B1: B1 remains the XTX-context finding that flush can materially alter candidate selection; GRE is the causal follow-up in an adapted same-family environment. No rerun required merely because it was a GRE.
Q2 TERMINAL DECISION: PRECONDITIONING-SUPPORTED ACCEPTED (not UNRESOLVED-MATERIAL). Discriminating result: the intervention effect survives the rewarm control - r256 approx e256, materially separated from NONE, with monotonic size onset around 128-256 MB, three replicated family-level flips surviving rewarm, and a hard-gate material r256 crossover (28 percent). This rejects the simple cache-residency explanation as SUFFICIENT (it does not prove caches contribute zero - a cache effect could coexist with persistent state). UNRESOLVED-MATERIAL rejected because mechanism-unresolved is not discrimination-unresolved: the directional discriminator (EVICT changes selection, REWARM does not undo it) was obtained in both aggregate population behaviour and replicated material crossovers. D3 limits the conclusion to: persistent preconditioning exists; the particular physical state variable responsible is unidentified.
TERMINAL WORDING TO RECORD: HI65 / B1 follow-up: PRECONDITIONING-SUPPORTED. The eviction intervention produces a size-dependent material change in candidate selection beginning between approximately 128-256 MB. Restoring workload warmth after eviction does not collapse the effect toward the NONE control: EVICT_REWARM remains at approximately the corresponding EVICT level, including replicated family-level and hard-gate material crossovers. Therefore simple cache-residency loss is not supported as the explanation for the B1 sensitivity; a longer-lived measurement preconditioning effect is supported. Its physical mechanism remains unresolved because GPU clock/power telemetry was unavailable in the adapted Windows experiment.
Q3 SLICE B UNBLOCKED: YES - proceed to HI35 re-baseline with flush_l2=0 SETTLED AS THE NORMAL/DEFAULT MEASUREMENT POLICY (the experiment gives no basis for eviction as a standard cleaner state; it is itself an experimentally material preconditioning intervention), then B2, then Slice C if B2 passes. The clock-instrumented XTX/Linux rerun becomes a FOLLOW-UP investigation, not a gate on Slice B.
B2 EXECUTION CONTRACT IMPLICATIONS (recorded on HI24): (1) HI35 establishes the new baseline with flush=0 - no retention threshold/noise estimate from the flush experiments carries into B2; the screen retention band is calibrated against settled production measurement semantics. (2) Screen and later-stage comparisons stay under IDENTICAL preconditioning policy - no flush before one stage but not another, no implicit cleanup between them, no selective rewarm (otherwise screen prediction error confounds with the proven state effect). (3) Counterbalance ordering rather than resetting the GPU - candidate/order randomization and existing same-kernel/noise canaries carry the history-resistance; eviction is not a normalization technique. Do NOT widen the B2 retention band because of GRE e256/e512 churn (deliberately perturbative intervention under WDDM + launch blocking); HI35's no-flush re-baseline determines the ordinary band.
Provider note: the adjudication text was delivered by the user from the GPT conversation after gateway admission failures (EXT-GPTAUTO-004 unresolved-turn-not-reconciled on req_290c3c985a644449 / req_e78c41b284384818, session ses_9b17bc43761345fc); both requests never sent, no duplicate prompts in the conversation.



POST-HOC CONFOUND DISCLOSURE (2026-08-17, from RV49/HI68): the noise-canary retry-replacement behavior later identified as defect F2 was LIVE in the code during this matrix's execution (GPT requested the fix before launch, but the adjudication arrived after the run completed). The behavior - a self-selected fresh-draw swap of native/twin ranking statistics when the canary first fails - applies uniformly to every arm because it is a property of the measurement path, not of any intervention. Consequences: (1) the within-matrix discrimination (rewarm does not collapse toward NONE; monotonic size onset) is NOT invalidated by this; (2) but retry-replacement can inflate marginal ranking differences on noisy WDDM/WDDM-launch-blocking hardware, so part of the observed exact-winner churn (including e512 at ~2x the NONE-noise floor) may be partially attributable to F2 rather than purely to the intervention or its physical mechanism; (3) the definitive clock-instrumented XTX/Linux follow-up run MUST execute with HI68 landed first, and should re-check whether the crossover magnitudes shrink. The terminal decision (PRECONDITIONING-SUPPORTED) stands as recorded; this disclosure refines the confidence language for the magnitude claims.


## Change Log

- 2026-08-16T15:36:50.327788+00:00 (created-by): Created by agent

## Ledger-events


- chg_20260816_154008_applied-the-external-reviewer_5808
- 2026-08-16T15:40:08.579790+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-16T16:03:30.957323+00:00 (updated-by): Updated: section:notes, section:steps, section:validation
- 2026-08-16T16:46:28.049970+00:00 (state-transition): State: pending → in_progress
- chg_20260816_221829_the-cache-eviction-experiment_4219
- 2026-08-16T22:18:29.518732+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-16T22:19:45.723915+00:00 (updated-by): Updated: section:notes
- 2026-08-16T22:44:11.020631+00:00 (updated-by): Updated: section:notes
- 2026-08-16T22:53:38.874984+00:00 (updated-by): Updated: section:notes
- 2026-08-16T23:05:34.199651+00:00 (updated-by): Updated: section:notes
- 2026-08-16T23:45:48.091990+00:00 (updated-by): Updated: section:notes
- 2026-08-17T02:22:53.068665+00:00 (updated-by): Updated: section:notes
- 2026-08-17T02:23:22.087503+00:00 (updated-by): Updated: section:notes
- chg_20260817_022337_ran-the-eight-arm-cache-interv_7806
- 2026-08-17T02:23:37.301936+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-17T02:24:10.322887+00:00 (updated-by): Updated: section:notes
- 2026-08-17T02:24:34.932262+00:00 (updated-by): Updated: section:notes
- chg_20260817_022446_ran-the-full-eight-arm-cache-i_8433
- 2026-08-17T02:24:46.887045+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-17T02:25:11.444974+00:00 (updated-by): Updated: section:notes
- chg_20260817_022521_ran-the-eight-arm-cache-interv_5402
- 2026-08-17T02:25:21.972735+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-17T02:25:46.627548+00:00 (updated-by): Updated: section:notes
- 2026-08-17T03:29:12.983175+00:00 (updated-by): Updated: section:notes
- 2026-08-17T03:29:53.964240+00:00 (state-transition): State: in_progress → completed
- chg_20260817_032955_gpt-closed-the-cache-vs-precon_7407
- 2026-08-17T03:29:55.323477+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-17T04:43:58.717749+00:00 (updated-by): Updated: section:notes
- chg_20260817_044430_gpt-reviewed-the-tuning-code-f_3860
- 2026-08-17T04:44:30.255210+00:00 (updated-by): Updated: section:ledger-events
