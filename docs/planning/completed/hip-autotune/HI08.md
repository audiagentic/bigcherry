---
id: HI08
order: 0
plan: hip-autotune
state: completed
created-at: '2026-08-04T01:21:34.106452+00:00'
breadth: ''
skill: intermediate
created-by: agent
work: M
---

# Implement MMF forced-nwarps variant dispatch

## Description

Patch 6 — extract the existing MMF nwarps switch (1..8 on RDNA wave32) into a forced-nwarps launcher. Native path calculates nwarps_best; tune/replay passes stored nwarps. Recalculate shared-memory requirements with the forced value before launch.

## Steps

- Extract nwarps switch into template `launch_mmf_nwarps(..., int nwarps)` — valid values are exactly `range(1, 9)`: 1 through 8
- Implement ggml_cuda_mmf_variant_is_eligible() with nwarps, shared memory, architecture checks
- Recalculate dynamic shared-memory use using forced nwarps (not native choice)
- Native path calculates nwarps_best then calls same launcher

## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files



## Validation

- All current nwarps switch cases are discoverable as variants
- Forced nwarps=nwarps_best produces same output as native
- Resource-invalid choices are rejected before launch

## Effort & Risk



## Standards

HIP_AUTOTUNE_STANDARDS sections 12.4 (hard eligibility), 13.2 (source audit invariants)

## Acceptance Criteria



## Notes

### Design revision -- explicit parameter, not a hidden override

An earlier implementation passed the forced value through a file-scope
thread-local, because threading a parameter through the dispatcher chain looked
like the larger diff. That was wrong, and the reason only shows up in
production.

A `replay-full` binary would read that override on **every** launch of the
family, and in production it is always the "no override" value. The native path
would be paying, on the hottest path in the system, for a feature only the tuner
uses. Since tuning and production are separate binaries by design (standards
9.1), production should carry none of it.

The forced value is therefore an **explicit, appended, defaulted parameter**:

```cpp
void launch_...(..., cudaStream_t stream, int forced_value = 0);
```

Appending and defaulting is what keeps the diff small -- every call site not on
the forced path compiles unchanged. Only the calls a forced value actually
travels through are updated, and repetitive forwarding is done with a single
`replace_all` edit whose match count is asserted, so upstream adding a case
fails loudly rather than leaving the patch half-applied.

**Consequence for testing.** With a hidden override, native and forced were
literally the same code, so "forced with the native value gives identical
output" was structurally guaranteed. With an explicit parameter the forced path
is separate code, and that property becomes something HI16 must actually check.
It was always a specified test; it is now load-bearing.


### Review update

Same shape as HI06. Note that `mul_mat_f_cuda` is templated on
`<T, rows_per_block, cols_per_block>` and the nwarps switch is *inside* it, so
the lifted launcher inherits all three template parameters.

**Shared memory must be recomputed from the forced value.** Upstream derives
`nbytes_shared_iter` and `nbytes_shared_combine` from `nwarps_best` before the
switch. A forced nwarps that reuses the natively computed shared-memory size
will either over-allocate or, worse, under-allocate. The recomputation has to
move inside the forced launcher.

**nwarps is bounded by `mmf_get_max_block_size(cc) / warp_size`**, not by 8
directly. The switch covers 1..8, but the reachable maximum is architecture
dependent -- eligibility must use the same expression upstream does.

## Change Log

- 2026-08-04T01:21:34.106452+00:00 (created-by): Created by agent
- 2026-08-04T01:27:05.014700+00:00 (updated-by): Updated: skill='intermediate'
- 2026-08-05T01:19:18.722017+00:00 (state-transition): State: pending → completed

## Ledger-events

- chg_20260805_012059_validated-hip-autotune-code-ag_8663
- 2026-08-05T01:20:59.431588+00:00 (updated-by): Updated: section:ledger-events
