---
id: HI128
order: 0
plan: hip-autotune
state: completed
created-at: '2026-08-26T09:34:01.464545+00:00'
breadth: ''
skill: advanced
created-by: agent
work: L
---

# HI121 close-out step 7: retroactive re-attestation/remediation of existing schema-7 data

## Description

dev-gpt-agent's recommended step 7 (2026-08-26): re-verify existing schema-7 builds/measurements from their original artifacts against the strengthened checks (HI122-126) -- row binding, manifest/descriptor, candidate implementation identity, canonical digest authority. Quarantine rows whose proof is unavailable rather than inferring validity from existing build_capability alone. Operational risk: this touches real, already-persisted data, not just code paths.

## Steps

SCOPE RENEGOTIATED (2026-08-27) via GPT design negotiation before implementation: HI127 already closed the negative/quarantine half of this item's original scope for free -- migration 0008 creates zero historical winner_verification rows, and replay_projection.py already treats absence as an ordinary counted omission, so 'do not silently grandfather old schema-7/8 data' was already true with zero HI128 code. What remained, and what was built:
1. DONE: tools/bigcherry/tuning/reattest.py's reattest_winners() -- a positive re-attestation pass that re-verifies an existing winner row against its ORIGINAL measurements JSONL + manifest (never replays load_measurements(), which would mutate the evidence under examination) and writes winner_verification only when it genuinely passes HI127's strengthened-ingest checks, including a real HI125 signature_digest_verifier call (no offline substitute exists for proving canonical->digest correspondence).
2. DONE: extracted inventory.verify_hip_build_artifacts() -> HipBuildProof as the pure, DB-independent half of HI121 M2's capability proof (behavior-preserving refactor out of _verify_and_persist_hip_capabilities()), so reattest.py can establish capability proof for a build with NO existing build_capability row and a NULL build_descriptor_hash -- exactly the pre-HI121 historical UNKNOWN state this item exists to remediate. Phase B backfills those fields only when absent, never when conflicting (conflict aborts the whole pass).
3. DONE: two-phase TOCTOU-safe design -- Phase A proves everything (row binding, canonical agreement, the real hardware verifier call) against a read-only connection with zero writes; Phase B re-opens a short BEGIN IMMEDIATE, re-verifies BOTH per-winner identity AND the shared build-level provenance (source_revision/manifest_hash/build_descriptor_hash/build_capability) before writing anything -- a build-level disagreement aborts the whole pass (every proven row shared that provenance); a single row's own identity changing is instead that row's own non-fatal changed_during_remediation outcome. dry_run runs a second independent read-only final-state check so its report reflects what a real Phase B would actually do.
4. DONE: CLI `bigcherry inventory reattest`. Fresh remeasurement campaigns are explicitly OUT OF SCOPE (GPT: 'HI128 != full hardware tuning campaign; HI128 == hardware-assisted attestation' -- an ordinary tune-campaign rerun handles anything that can't be re-attested from existing evidence).

## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files

tools/bigcherry/tuning/inventory.py, tools/bigcherry/tuning/replay_projection.py, a new remediation script/CLI command

## Validation

Every existing schema-7 row is either re-verified and marked as such, or quarantined with a clear reason; no row is silently trusted without going through the new checks.

## Effort & Risk

L, operational risk -- this is data remediation on real persisted state, not just a code change. Conditional Brutus remeasurement where trustworthy original evidence is unavailable.

## Standards



## Acceptance Criteria



## Notes

Hard-blocked on HI124 and HI125. Do not start this even if HI122/HI123/HI126/HI127 are already done.

Hard-blocked on HI124 and HI125 (both landed). Original plan text's 'quarantine + remediation + possible remeasurement system' scope was explicitly rewritten downward after negotiation with dev-gpt-agent -- see steps above.

IMPLEMENTATION COMPLETE (2026-08-27) across three commits: 547a64b (initial reattest.py + CLI + 8 tests), a68ab17 (fixed two review-round-2 blockers: historical-build backfill via the new verify_hip_build_artifacts() extraction, and Phase B's TOCTOU recheck extended to build-level provenance, not just per-winner identity -- 9 new tests including two that simulate the actual race via a verifier callable that mutates the DB through a second connection as a side effect), 79fb0c7 (review-round-3 fix: normalized every whole-pass failure path to ReattestationError so cli/tuning.py's cmd_reattest(), which only catches that type, cannot be bypassed by a bare RecordError/SystemExit/IntegrityError -- 1 new test). Full offline suite green throughout (2299 tests at close).

Three-round adversarial review (dev-gpt-agent, ses_b6a905dda4f0496d): round 1 identified two real blockers (both fixed and independently re-verified in round 2 as genuinely closing real gaps, not just passing tests by construction -- confirmed via the TOCTOU test discriminating old vs. new code, which I verified was NOT simply exercising SQLite's own read-consistency); round 2 confirmed 'no further attestation-integrity hole found' and 'the historical capability/descriptor recovery is now real, the Phase-B race closure is correctly implemented' but flagged the error-boundary escape as a required small fix; round 3 fix applied, explicit verdict: 'Fix the error-boundary issue above and I would PASS HI128.'

Confirmed coherent design decision (round 2): build-level backfill (build_descriptor_hash/build_capability) is allowed to commit even if every proven row's own per-row recheck fails in Phase B (zero attestations that run) -- these are independently proven facts about the BUILD, not about any particular winner, and cannot make an unattested winner reusable since replay_projection.py independently requires winner_verification per row regardless of build-level state.

## Change Log

- 2026-08-26T09:34:01.464545+00:00 (created-by): Created by agent

## Ledger-events

- chg_20260827_142350_added-a-tool-to-re-verify-and_4485
- 2026-08-27T14:23:50.876787+00:00 (updated-by): Updated: section:ledger-events
- chg_20260827_144351_fixed-two-real-gaps-found-in-r_7605
- 2026-08-27T14:43:51.137933+00:00 (updated-by): Updated: section:ledger-events
- chg_20260827_150015_closed-a-gap-where-certain-bad_6116
- 2026-08-27T15:00:15.935941+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-27T15:00:37.121330+00:00 (updated-by): Updated: section:steps, section:notes
- 2026-08-27T15:00:41.940494+00:00 (state-transition): State: pending → completed
