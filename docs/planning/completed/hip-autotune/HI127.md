---
id: HI127
order: 0
plan: hip-autotune
state: completed
created-at: '2026-08-26T09:33:52.821927+00:00'
breadth: ''
skill: advanced
created-by: agent
work: M
---

# HI121 close-out step 6: schema-7 verification/attestation state tracking

## Description

dev-gpt-agent's recommended step 6 (2026-08-26): once HI122-HI125 land, there needs to be a way to distinguish rows/builds admitted under the STRENGTHENED verifier from rows admitted before those fixes existed. Do not silently grandfather old schema-7 data as if it had passed the new checks -- that would be exactly the kind of unverified-trust gap the whole HI121 hardening effort exists to close.

## Steps

1. DONE: schema 7->8 migration (sql/migrations/0008_winner_verification.sql) adds winner_verification, a per-WINNER-row attestation table (PK winner_id REFERENCES winner(winner_id) ON DELETE CASCADE, CHECK verification_profile = 'hi121-strengthened-ingest-v1'). Per-winner granularity is structurally forced: winner's own INSERT OR REPLACE omits winner_id from its column list, so a UNIQUE-constraint conflict deletes the old row and inserts a fresh one via delete-then-insert -- ON DELETE CASCADE then destroys any stale attestation with zero extra bookkeeping. No backfill for existing winners (that is HI128's job, not this one's -- absence means truthful UNKNOWN, never a fabricated PASS).
2. DONE, structurally: enforcement is the CHECK constraint (only the current profile string is ever accepted) plus the CASCADE FK (a replaced winner row cannot silently keep an attestation that belonged to a different row) -- both DB-level, not just application-level.
3. DONE for M4 (replay_projection.py): row-binding (_require_row_belongs_to_build) now returns winner_id; project_measurements() gates each row on verification_state.is_winner_verified() immediately after row-binding succeeds and BEFORE canonical/capability checks -- an unattested genuine row is a counted omission (omitted_unverified_source), a forged/mismatched row remains a hard ProjectionError regardless of attestation state. Promotion-gate consultation is EXPLICITLY OUT OF SCOPE for this item per the negotiated boundary with dev-gpt-agent: promotion_gate.py's resolve_promotion_identity() does not currently resolve winner_id/candidate_id the way a gate check would need, and production workflow.py does not yet pass a signature_digest_verifier to load_measurements() at all -- enabling a hard promotion-side gate now would reject 100% of today's real promotions. That wiring is HI125's own still-open REMAINING item, a separate deployment dependency, not part of HI127's closure.

## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files

sql/dispatch-db.sql, sql/migrations/ (new migration), tools/bigcherry/tuning/inventory.py, tools/bigcherry/tuning/replay_projection.py

## Validation

A build/measurement admitted before HI122-125 is distinguishable from one admitted after; projection/promotion logic can tell the difference where it matters.

## Effort & Risk

M -- no hardware required per dev-gpt-agent's investigation.

## Standards



## Acceptance Criteria



## Notes

Depends on HI122-HI125 landing first (the checks being tracked need to exist before you can mark rows as having passed them).

Depends on HI122-HI125 landing first (the checks being tracked need to exist before you can mark rows as having passed them).

IMPLEMENTATION COMPLETE (2026-08-27), commit 94b719f. Writer: inventory.py's load_measurements() attests a winner row (verification_state.record_winner_verification, keyed on cursor.lastrowid from the winner INSERT OR REPLACE) only when BOTH _verify_and_persist_hip_capabilities() succeeded (build_attested) AND a signature_digest_verifier was supplied -- neither alone is 'strengthened ingest'. Reader: replay_projection.py as described in steps above. Full offline suite (2267 tests) green; migration test (0008), writer tests, and reader tests added.

Adversarial review round (dev-gpt-agent, ses_b6a905dda4f0496d, 2026-08-27): verdict CHANGES-REQUIRED (small) then accepted after fixes. Findings and resolutions: (1) confirmed winner_cursor.lastrowid after INSERT OR REPLACE is a real Python sqlite3/SQLite guarantee for an ordinary (non-WITHOUT-ROWID) INTEGER PRIMARY KEY table -- REPLACE is delete-then-insert and lastrowid updates on the insert; SAFE. (2) REJECTED an over-claim in this session's own test/comments: SQLite does NOT guarantee a replaced row's new winner_id is numerically different from the old one (no AUTOINCREMENT on this table) -- fixed the test to assert the real invariant instead (CASCADE removes the stale attestation; an unverified reload leaves zero attestations; a subsequent verified reload attests exactly the CURRENT winner row), not ID inequality. (3) row-binding-before-verification-state ordering confirmed correct (integrity over any confidentiality concern -- there is no meaningful confidentiality boundary here). (4) fixed a stale replay_projection.py module-docstring claim that every row's canonical must be parseable or the whole projection fails -- now documents that HI127's gate runs first and an unattested row with unresolvable canonical content is quarantined as unverified-source, never a whole-projection failure. (5) confirmed no additional consumer is required for HI127 to be meaningfully closed given the promotion-gate exclusion above. (6) confirmed deferring backfill to HI128 is still correct -- migrating an existing schema-7 DB to schema-8 makes its historical winners omitted-as-unverified (an availability/performance regression until HI128 re-attests them), never a silent correctness regression, since HI127 never claims they passed anything they didn't.

STILL OPEN, tracked elsewhere, NOT a defect in this item: production workflow.py/manual inventory tuning still do not pass a signature_digest_verifier to load_measurements() at all, so production currently creates ZERO strengthened attestations -- HI127's state machinery is correct and ready, but should not be described as operationally deployed until HI125's own REMAINING wiring lands.

## Change Log

- 2026-08-26T09:33:52.821927+00:00 (created-by): Created by agent

## Ledger-events

- chg_20260827_125536_dispatch-tuning-now-tracks-pe_8543
- 2026-08-27T12:55:36.159163+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-27T13:14:00.961649+00:00 (updated-by): Updated: section:steps, section:notes
- 2026-08-27T13:14:05.866030+00:00 (state-transition): State: pending → completed
