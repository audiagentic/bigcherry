---
id: HI93
order: 0
plan: hip-autotune
state: completed
created-at: '2026-08-23T04:48:54.842465+00:00'
breadth: ''
skill: intermediate
created-by: agent
work: S
---

# Cache per-device hardware key/digest once per process (RP4 slice)

## Description

Carved out of HI87 as an independently implementable slice (RP4 in the source document's own staged breakdown -- 'BigCherry Replay and RCCL Tuning Architecture', assessed 2026-08-23). The cold-path resolver currently derives the hardware key (architecture, wave size, compute units, feature flags, shared memory per block) and its digest fresh on every cold signature encounter, even though these properties never change for a given device within a process. Cache them once per device, keyed by hipGetDevice()'s current value (same pattern this session's HI64 work already used for PerDeviceState<T>).

Deliberately independent of HI87's native-selector reorder question: this only removes redundant reconstruction work on the cold path (the path that already runs native_select() and full resolution), not the warm-hit fast path HI87/RP2-RP3 target. It can ship on its own, is low-risk (pure caching of an already-deterministic value, no semantic change), and does not need HI92's profiling data to justify -- the redundancy itself is the justification, independent of how large or small its measured cost turns out to be.

## Steps

- Add a DeviceDispatchProfile-style cache (hardware key + hardware digest) keyed by device, constructed once on first use per device and reused for the life of the process.
- Wire the cold-path resolver to consult this cache instead of reconstructing the hardware key/digest on every cold signature.
- Verify identical dispatch decisions before/after (pure caching, no behavior change) via existing replay/telemetry evidence.

## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files

src/ggml/src/ggml-cuda/hip-autotune-dispatch.cu (wherever ggml_hip_make_hardware_key()/ggml_hip_hardware_digest() are currently invoked per cold dispatch -- not yet confirmed how often today, see HI87's own effort_risk note on this same open question)

## Validation

Before/after dispatch-decision equivalence (no behavior change expected, only removed redundant construction work). A real hardware microbenchmark showing the actual redundant-construction cost removed is a nice-to-have, not a hard requirement the way it is for HI87's reorder -- this slice's justification (a deterministic, device-scoped value should not be rebuilt every cold dispatch) doesn't depend on the number being large.

## Effort & Risk

Low. Same caching pattern already used successfully for HI64's PerDeviceState<T> (device-keyed, no auto-clear, thread-safe). No behavior change, only removes redundant work on an already-slow path (cold dispatch, not the warm hot path).

## Standards



## Acceptance Criteria



## Notes

Filed 2026-08-23 alongside HI92 (RP1 instrumentation) as the other independently-implementable slice carved out of HI87's original bundle. See HI92's notes for the full RP1->RP2->RP3->RP4 staging context. Unlike HI92, this item does not need to wait on measurement data -- it's justified by the redundancy itself, so it could be picked up any time independent of HI87's own reorder decision.

CLOSED 2026-08-23. Added a `HardwareIdentity` cache (key + digest) keyed directly by `ctx.device` (unlike HI64's `PerDeviceState<T>`, which queries `hipGetDevice()` itself, every real call site here already has the target device index, so there is nothing to ask). Both original call sites (`ForcedCandidate` path, main resolution path) now read from the cache instead of calling `ggml_hip_make_hardware_key()`/`ggml_hip_hardware_digest()` directly -- that construction now happens exactly once, inside the cache's own miss branch. unordered_map entries are never erased, so the returned reference stays valid for the life of the process even across later insertions.

**Real hardware validation** (local AMD Radeon RX 7900 GRE, gfx1100, vendor/rocm/7.1), using HI92's own new counters as the measurement instrument: a full q4_0 MUL_MAT sweep (46 distinct cold signatures, all on the same device) showed `entries=46 sig_digest_builds=46 hw_key_builds=1` -- the hardware key/digest was built exactly once and reused across every one of the 46 cold dispatches, while each signature correctly got its own fresh digest (signatures differ, hardware doesn't). This is direct, real proof of the redundant-construction elimination this item exists for, not merely a before/after equivalence check.

7 new source-contract tests. Full offline suite: 1776 passed (up from 1769 after HI92). No behavior change: dispatch decisions are identical, only the redundant reconstruction work is removed.

## Change Log

- 2026-08-23T04:48:54.842465+00:00 (created-by): Created by agent

## Ledger-events


- chg_20260823_044909_split-a-bundled-hot-path-optim_2680
- 2026-08-23T04:49:09.871153+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-23T05:28:14.689171+00:00 (updated-by): Updated: section:notes
- 2026-08-23T05:28:18.169340+00:00 (state-transition): State: pending → completed
- chg_20260823_052835_eliminated-redundant-per-dispa_2704
- 2026-08-23T05:28:35.912034+00:00 (updated-by): Updated: section:ledger-events
