---
id: HI67
order: 67
plan: hip-autotune
state: completed
created-at: '2026-08-17T04:43:12.912754+00:00'
breadth: ''
skill: advanced
created-by: agent
work: L
---

# CPU-reference correctness contract: headroom rule, deterministic seeds, tune mode stays native

## Description

CPU-reference correctness contract for tuner acceptance and promotion evidence (F1, P0): the tuner's native-relative acceptance does not bound end-to-end error, so marginal signatures can intermittently pass tuning yet fail the project's own gate (RV08). Replace the production correctness contract with CPU-reference-based evidence.

## Steps

ALL THREE SLICES NOW DONE (uncommitted, 2026-08-21). RV49 contract (GPT-adjudicated in RV49/RV77) is fully implemented and offline-verified end to end.

1. [DONE] Slice 1 -- tune mode stays native. Commit 692342e, pushed.

2. [DONE] Slice 2 -- CPU-reference evidence generator: patches/1222 (deterministic seeding), patches/1223 (machine-readable NMSE/max_abs), tools/bigcherry/correctness_evidence.py (evidence driver, with a corrected GGML_HIP_DISPATCH_MODE mechanism vs RV77's own unverified assumption), dispatch DB schema 5->6 (correctness_evidence/correctness_evidence_seed). 38 tests total across 4 test files.

3. [DONE] Slice 3 -- promotion gating, per the RV77-follow-up GPT adjudication (req_5e04cb1d9c254fe5): tools/bigcherry/promotion_correctness_gate.py (read-only identity resolution -- signature_id/hardware_id via their own globally-unique digest columns, build_id via the measurement table's own already-ingested row, NEVER via the legacy source_revision+manifest_hash key; hard-AND correctness evaluation that recomputes worst-of-seeds from the child seed rows rather than trusting the parent aggregate, verifies native_candidate_id, and produces 3 distinct rejection statuses: rejected_no_correctness_evidence/rejected_correctness_contract/rejected_correctness). tools/bigcherry/tune_promotion.py's promote() now takes a REQUIRED dispatch_db parameter and applies the gate as a hard AND with the existing BH/bootstrap statistical criteria -- correctness only ever narrows an already-passing row, never rescues a failing one, and an already-statistically-rejected row keeps its own rejection reason. lifecycle.py's execute_promotion_stage() grew a required dispatch_db LaneInputValue parameter, rehydrates it as a second provenance parent alongside measurements. re15_acceptance_run.py passes tune_result.database_ref (the FINAL tune-stage DB, correctly post-direct-op-stage-replacement when that ran) -- not inventory_result.database_ref. 12 (gate module) + 3 (tune_promotion integration) + 1 (lifecycle end-to-end) new/updated tests.

3f. [DOCUMENTATION DONE, BEHAVIOR NOT WIRED] read_correctness_evidence() in autotune_catalog.py now has a prominent docstring correction: it reduces NATIVE-RELATIVE deltas (a cheap screening check, conceptually 'delta_nmse_vs_native'), explicitly NOT RV49 CPU-reference production proof, and documents that its consumer (build_manifest()'s PRODUCTION_VARIANT_SETS gate) has NOT been switched to require real correctness_evidence -- that is separate, larger follow-up scope (build_manifest() would need its own dispatch_db input and its own identity-resolution against manifest candidates, a different pipeline stage/shape than promotion's) and was deliberately NOT attempted in this pass to avoid an unverified behavioral change to the live manifest-generation gate this late in a long session. See RE47-adjacent follow-up note below.

Full offline suite green throughout: 1364 passed, 3 skipped, 32 subtests (final count after all three slices). No compiler available in this dev environment -- everything is offline-verified (patch apply/idempotence, source-level C++ contract tests matching this codebase's existing testing philosophy for .cu files, real sqlite schema/constraint tests, real end-to-end Python promotion-pipeline tests with faked-but-real sqlite fixtures). Needs a real HIP build + hardware run to move from 'offline-verified' to 'validated'.

UNCOMMITTED FILES (full list as of 2026-08-21, this HI67 body of work): patches/1222_hi67_deterministic_test_backend_ops_seed.py, patches/1223_hi67_machine_readable_correctness_metrics.py, patches/catalog.toml, sql/dispatch-db.sql, tools/bigcherry/inventory.py, tools/bigcherry/correctness_evidence.py, tools/bigcherry/promotion_correctness_gate.py, tools/bigcherry/tune_promotion.py, tools/bigcherry/lifecycle.py, tools/bigcherry/re15_acceptance_run.py, tools/bigcherry/autotune_catalog.py, src/ggml/src/ggml-cuda/hip-autotune-tuner.cu (also carries HI29/HI64), tools/tests/test_hi67_deterministic_seed_patch.py, tools/tests/test_hi67_correctness_metrics_patch.py, tools/tests/test_hi67_correctness_evidence_schema.py, tools/tests/test_correctness_evidence.py, tools/tests/test_promotion_correctness_gate.py, tools/tests/test_tune_promotion.py, tools/tests/test_re10_lifecycle.py, tools/tests/test_cli_patches_catalog_filter.py, tools/tests/test_vk_autotune_types.py, tools/tests/test_dispatch_safety.py.

## Detailed Solution & Technical Design

CONTRACT (GPT-adjudicated in RV49): CPU/reference correctness is the canonical production correctness oracle; native-relative correctness is only a cheap tuner screening invariant. For a candidate to become dispatchable in replay, with R = CPU reference semantics used by test-backend-ops, N = native GPU result, C = candidate GPU result, T = upstream correctness threshold for that operation (5e-4 NMSE for the relevant MUL_MAT cases at b10362): require E_N = NMSE(R,N) <= T AND E_C = NMSE(R,C) <= E_N + 0.5 * (T - E_N). A candidate may consume at most half of native's remaining CPU-reference NMSE budget; if native itself fails, the signature produces no non-native winner; a more-accurate candidate naturally passes. 0.5 becomes a named/versioned correctness_headroom_fraction (default 0.5) recorded in evidence - deliberately stricter than E_C <= T because sitting on the project cliff edge is exactly what failed intermittently on q4_1 k=32 (RV08). Use the WORST result across >=3 deterministic input seeds, not an average; the evidence records the seeds (upstream test init itself uses random inputs, so marginal implementations can cross the boundary between invocations). max_abs_error = 1e-2 is replaced for reference-grade evidence by max_abs(C,R) <= max_abs(N,R) across the same trials - deliberately conservative, no new fixed absolute tolerance; record full distributions and relax only with evidence. The existing 1e-6 candidate-vs-native check stays as an additional local screening check but is renamed/described accurately as delta_nmse_vs_native - it is NOT production correctness evidence.

VERIFIED PYTHON LEAK (must change with the C++ semantics, or F1 is not closed): autotune_catalog.py read_correctness_evidence() currently builds production evidence from native-relative metrics and hardcodes tolerances {nmse: 1e-6, max_abs: 1e-2} (line ~651), and production manifest generation (line ~775) accepts it. It must stop treating ordinary tuner-native deltas as production proof.

MODE CONSEQUENCE: until reference-grade correctness exists, tune mode = measure/select/record with ACTUAL WORKLOAD DISPATCH REMAINING NATIVE. Today tune installs the candidate returned by ggml_hip_tuner_resolve() into the normal dispatch binding so subsequent execution uses it - that must stop. Replay = execute only externally validated/promoted winners (already true). Promotion pipeline requires CPU-reference evidence keyed by at least source/manifest + architecture + signature + candidate.

## Code Samples & Guidance

GPT-adjudicated correctness_evidence schema (schema 6), replacing the earlier unreviewed draft:

```sql
CREATE TABLE correctness_evidence (
    id INTEGER PRIMARY KEY,
    build_id INTEGER NOT NULL REFERENCES build(id),
    hardware_id INTEGER NOT NULL REFERENCES hardware(id),
    signature_id INTEGER NOT NULL REFERENCES signature(id),
    candidate_id INTEGER NOT NULL REFERENCES candidate(id),
    native_candidate_id INTEGER NOT NULL REFERENCES candidate(id),
    contract_version TEXT NOT NULL,        -- e.g. 'hi67-rv49-v1'; promotion independently validates this, never trusts it blindly
    threshold_t REAL NOT NULL,             -- producer-recorded; promotion re-derives/validates independently
    headroom_fraction REAL NOT NULL,       -- producer-recorded; promotion re-derives/validates independently
    e_n_nmse REAL NOT NULL,                -- worst-of-seeds, derived from correctness_evidence_seed rows
    e_c_nmse REAL NOT NULL,
    max_abs_native REAL NOT NULL,
    max_abs_candidate REAL NOT NULL,
    computed_at TEXT NOT NULL,
    tool_version TEXT NOT NULL
);

CREATE TABLE correctness_evidence_seed (
    id INTEGER PRIMARY KEY,
    correctness_evidence_id INTEGER NOT NULL REFERENCES correctness_evidence(id),
    seed INTEGER NOT NULL,
    reference_digest TEXT NOT NULL,        -- proves native and candidate runs saw the same R
    e_n_nmse REAL NOT NULL,
    e_c_nmse REAL NOT NULL,
    max_abs_native REAL NOT NULL,
    max_abs_candidate REAL NOT NULL,
    native_execution_status TEXT NOT NULL,
    candidate_execution_status TEXT NOT NULL
);
```

Confirmed blocking dependency (source-verified 2026-08-20): vendor/llama.cpp/tests/test-backend-ops.cpp:54-83 init_tensor_uniform() uses `thread_local std::default_random_engine gen(std::random_device{}())`, parallel across N_THREADS -- must be patched to accept a deterministic seed before 2c/2d can produce valid evidence. Same issue in init_tensor_kq_mask (line ~155) and init_tensor_tril (line ~203).

## Files

- src/ggml/src/ggml-cuda/hip-autotune-tuner.cu
- src/ggml/src/ggml-cuda/hip-autotune-dispatch.cu (tune-mode binding install)
- tools/bigcherry/autotune_catalog.py (read_correctness_evidence, manifest evidence)
- tools/bigcherry/tune_promotion.py
- sql or artifact schema for the new correctness-evidence shape
- tools/tests/ (contract tests)

## Validation

Contract tests: a candidate within 1e-6 of native but pushing E_C past E_N + 0.5*(T-E_N) is rejected; a more-accurate-than-native candidate passes; a signature whose native fails E_N <= T yields no non-native winner; evidence records the deterministic seeds and the headroom fraction name/version; tune mode leaves the real workload dispatch on native (verified by an actual run where a faster candidate is selected but the binding stays native); replay refuses a winner lacking CPU-reference evidence keyed correctly; read_correctness_evidence() rejects legacy native-delta evidence as production proof; marginal q4_1 k=32 RDNA2 case no longer intermittently trips the 5e-4 gate on repeated sweeps.

## Effort & Risk



## Standards



## Acceptance Criteria



## Notes

Created 2026-08-17 from RV49 (GPT adjudication of the tuning-code review), P0 rank 1. Operates AFTER HI68 in execution order (F2 is smaller and operationally first) but is the higher-ranked defect. Supersedes RV08's open design question - RV08's options (CPU-reference bound vs headroom rule vs tighter epsilons) are resolved as: CPU-reference bound WITH the 50%-remaining-headroom rule, plus worst-of-N deterministic seeds and candidate-max-abs-no-worse-than-native. F5 extraction of the correctness-evaluation stage happens under this item per RV49.

Created 2026-08-17 from RV49 (GPT adjudication of the tuning-code review), P0 rank 1. Operates AFTER HI68 in execution order (F2 is smaller and operationally first) but is the higher-ranked defect. Supersedes RV08's open design question - RV08's options (CPU-reference bound vs headroom rule vs tighter epsilons) are resolved as: CPU-reference bound WITH the 50%-remaining-headroom rule, plus worst-of-N deterministic seeds and candidate-max-abs-no-worse-than-native. F5 extraction of the correctness-evaluation stage happens under this item per RV49.

STATUS (2026-08-19): slice 1 committed (692342e) and pushed; tune mode is read-only with the stays-native contract tests in place and the full offline suite green over them. Slices 2-3 not started; slice 2 has open design questions queued for GPT (see Steps). State moved pending -> in_progress 2026-08-19 to reflect that slice 1 is landed.

Created 2026-08-17 from RV49 (GPT adjudication of the tuning-code review), P0 rank 1. Operates AFTER HI68 in execution order (F2 is smaller and operationally first) but is the higher-ranked defect. Supersedes RV08's open design question - RV08's options (CPU-reference bound vs headroom rule vs tighter epsilons) are resolved as: CPU-reference bound WITH the 50%-remaining-headroom rule, plus worst-of-N deterministic seeds and candidate-max-abs-no-worse-than-native. F5 extraction of the correctness-evaluation stage happens under this item per RV49.

Created 2026-08-17 from RV49 (GPT adjudication of the tuning-code review), P0 rank 1. Operates AFTER HI68 in execution order (F2 is smaller and operationally first) but is the higher-ranked defect. Supersedes RV08's open design question - RV08's options (CPU-reference bound vs headroom rule vs tighter epsilons) are resolved as: CPU-reference bound WITH the 50%-remaining-headroom rule, plus worst-of-N deterministic seeds and candidate-max-abs-no-worse-than-native. F5 extraction of the correctness-evaluation stage happens under this item per RV49.

STATUS (2026-08-19): slice 1 committed (692342e) and pushed; tune mode is read-only with the stays-native contract tests in place and the full offline suite green over them. Slices 2-3 not started; slice 2 has open design questions queued for GPT (see Steps).

**CORRECTION (2026-08-22): the Steps section's "UNCOMMITTED FILES" list is stale.** Verified via `git log`/`git status`: all ~20 files it names (patches/1222, 1223, catalog.toml, sql/dispatch-db.sql, correctness_evidence.py, promotion_correctness_gate.py, tune_promotion.py, lifecycle.py, re15_acceptance_run.py, autotune_catalog.py, hip-autotune-tuner.cu, and every listed test file) are tracked and clean -- they landed in commit `5e03cb4` ("HI64/HI67 slice 3/HI29: per-device poison scoping, promotion correctness gating, transform recording infra"), which is on `origin/tuning-code-rebase`. No exposed uncommitted work; nothing to rescue. Slices 1-3 are all committed and pushed; what remains open is the real engineering gap the Steps section already documents (3f: `read_correctness_evidence()`'s docstring correction is committed, but `build_manifest()`'s PRODUCTION_VARIANT_SETS gate has NOT been switched to require real correctness_evidence -- that wiring is still unstarted), plus the real-HIP-hardware validation pass everything above still needs (offline-verified only so far).

Created 2026-08-17 from RV49 (GPT adjudication of the tuning-code review), P0 rank 1. Operates AFTER HI68 in execution order (F2 is smaller and operationally first) but is the higher-ranked defect. Supersedes RV08's open design question - RV08's options (CPU-reference bound vs headroom rule vs tighter epsilons) are resolved as: CPU-reference bound WITH the 50%-remaining-headroom rule, plus worst-of-N deterministic seeds and candidate-max-abs-no-worse-than-native. F5 extraction of the correctness-evaluation stage happens under this item per RV49.

Created 2026-08-17 from RV49 (GPT adjudication of the tuning-code review), P0 rank 1. Operates AFTER HI68 in execution order (F2 is smaller and operationally first) but is the higher-ranked defect. Supersedes RV08's open design question - RV08's options (CPU-reference bound vs headroom rule vs tighter epsilons) are resolved as: CPU-reference bound WITH the 50%-remaining-headroom rule, plus worst-of-N deterministic seeds and candidate-max-abs-no-worse-than-native. F5 extraction of the correctness-evaluation stage happens under this item per RV49.

STATUS (2026-08-19): slice 1 committed (692342e) and pushed; tune mode is read-only with the stays-native contract tests in place and the full offline suite green over them. Slices 2-3 not started; slice 2 has open design questions queued for GPT (see Steps).

**CORRECTION (2026-08-22): the Steps section's "UNCOMMITTED FILES" list is stale.** Verified via `git log`/`git status`: all ~20 files it names (patches/1222, 1223, catalog.toml, sql/dispatch-db.sql, correctness_evidence.py, promotion_correctness_gate.py, tune_promotion.py, lifecycle.py, re15_acceptance_run.py, autotune_catalog.py, hip-autotune-tuner.cu, and every listed test file) are tracked and clean -- they landed in commit `5e03cb4` ("HI64/HI67 slice 3/HI29: per-device poison scoping, promotion correctness gating, transform recording infra"), which is on `origin/tuning-code-rebase`. No exposed uncommitted work; nothing to rescue. Slices 1-3 are all committed and pushed; what remains open is the real engineering gap the Steps section already documents (3f: `read_correctness_evidence()`'s docstring correction is committed, but `build_manifest()`'s PRODUCTION_VARIANT_SETS gate has NOT been switched to require real correctness_evidence -- that wiring is still unstarted), plus the real-HIP-hardware validation pass everything above still needs (offline-verified only so far).

### 2026-08-22: strict forced-candidate proof + threshold authority (commit 35e1841)

GPT (req_559bb03e75494a74, req_52b5095ab1aa4efa) identified 3 fail-closed integration gaps that needed closing before real hardware validation makes sense. Two landed: (1) `GGML_HIP_FORCE_CANDIDATE_STRICT=1` in `hip-autotune-dispatch.cu` -- an ineligible/unregistered forced candidate now `GGML_ABORT`s instead of silently falling through to normal resolution (a correctness producer asking for a specific candidate could otherwise get ordinary resolution instead with zero signal in exit code or stderr); success emits a per-dispatch `BIGCHERRY_FORCE_CANDIDATE_EXECUTED stable_name=<name>` marker. (2) Threshold authority -- `SeedEvidence`/`correctness_evidence_seed` gained a `threshold_t` column sourced only from test-backend-ops' own emitted `BIGCHERRY_CORRECTNESS_METRIC threshold=...` field, never a caller-supplied Python float; `aggregate_seed_evidence()` derives T by requiring every seed agree instead of taking it as a parameter; `promotion_correctness_gate.py`'s existing re-derivation-vs-parent cross-check now covers `threshold_t` too. 8 new tests, full offline suite (1532 tests) passed at the time.

### 2026-08-22: gap 3 (production-manifest gate) -- design revised, partially implemented, blocked

GPT's own re-verification of the real code corrected my initial framing: `autotune_schema.py`'s `_validate_correctness_reference()` (called from `validate_and_summarise()` for every non-native candidate in a `PRODUCTION_VARIANT_SETS` manifest) already fails closed when `correctness_source` is omitted -- a production manifest can never ship with literally zero correctness evidence. The real gap is narrower: it can only ever be given the wrong evidence (native-relative screening), never real RV49 schema-6 CPU-reference evidence, because nothing in manifest generation ever consults `correctness_evidence`/`correctness_evidence_seed`.

GPT's revised recommendation (a real architectural improvement over what I originally asked for): do NOT wire schema-6 evidence into the manifest-candidate level at all. A `Candidate` is reusable across many signatures and can advertise several architectures, so "candidate C has schema-6 evidence" is not a meaningful production statement -- the meaningful statement is "candidate C is safe for exactly this signature S on hardware H against native N", which is precisely the granularity schema-6 evidence is keyed at, and precisely the granularity a replay-cache export decision needs (compiling a candidate into a build is not the same as authorizing it to be dispatched). So the real hard gate belongs at **replay-cache export time** (`replay_cache.py`'s `build()`), not manifest generation: `build_manifest()` should drop its `correctness_source` parameter entirely (and the old candidate-level `_validate_correctness_reference()` call for production variant sets should be removed from schema validation), while `replay_cache.build()` should gain a mandatory `dispatch_db` parameter and independently re-check the RV49 gate for every non-native cache entry -- including manual `--seed` overrides, which currently bypass promotion-gate checks entirely and would otherwise become the new escape hatch the moment this lands. GPT was explicit: never land the old gate's removal before the new one exists in the same commit -- there must be no intermediate revision with no gate at all.

**Implemented so far (commit pending):** the purely additive first piece in `promotion_correctness_gate.py` -- `CorrectnessBinding` (a named wrapper around the same 5 fields `resolve_promotion_identity()` already takes: dispatch/signature/hardware hex, native/candidate stable names), `resolve_correctness_binding()`, and `require_correctness_binding()` (resolve + hard-require the gate or raise `CorrectnessGateError`). 5 new tests, full offline suite (1537 tests) passes.

**BLOCKED:** the actual gate-swap (removing the old manifest-level checks, adding the new binding-level check to `replay_cache.py`'s `build()` and its CLI) cannot land yet -- `tools/bigcherry/replay_cache.py` is under active edit by a concurrent session in this shared working tree this whole session, and per the standing rule for this repo that file must never be touched by this session. Landing only the removal half without the replacement would create exactly the dangerous intermediate state GPT warned against. **Next session picking this up: wait for `replay_cache.py` to be free, then implement GPT's full design in req_52b5095ab1aa4efa's response in one atomic commit** (generic reusable binding validator -- now landed --, `replay_cache.build(..., dispatch_db=...)` mandatory, CLI `--dispatch-db` required for cache export, non-native manual seeds brought under the same rule, removal of `_validate_correctness_reference()` from generic production manifests, removal of `correctness_source` from `build_manifest()`, production regression tests per GPT's test table in that response).

2026-08-23: replay-cache export gate (GPT's revised design, req_52b5095ab1aa4efa) IMPLEMENTED and offline-verified. This was the last open piece of HI67's slice-3 scope, previously BLOCKED on replay_cache.py being under active edit by a concurrent session -- that session moved on and the file is clean, so this landed.

What changed, per GPT's design: production correctness proof moved from manifest-generation time (candidate-level, wrong granularity -- a Candidate is reused across many signatures/architectures) to replay-cache export time (binding-level: exact dispatch+signature+hardware+candidate, the granularity a replay decision actually needs).

- `autotune_schema.py`: removed `_validate_correctness_reference()` and its call from `validate_manifest()` for PRODUCTION_VARIANT_SETS. A production manifest no longer needs (or is checked for) a `correctness_evidence` blob on its candidates.
- `autotune_catalog.py`: removed `correctness_source` param and its evidence-injection block from `build_manifest()`; updated `read_correctness_evidence()`'s docstring (no longer "not yet switched" -- explicitly not a production gate consumer any more, screening-only).
- `campaign_workers.py`: stopped passing `correctness_source=` into `build_manifest()`.
- `replay_cache.py`: new `_validate_correctness_gate()`, called on every entry (including seed overrides, after they're applied -- unlike `_validate_promotion_gate`, this one is never bypassable) via a new `dispatch_db: Path | None = None` param on `build()`. For every non-native winner it resolves a `promotion_correctness_gate.CorrectnessBinding` (dispatch/signature/hardware hex + native/candidate stable names) and calls `require_correctness_binding()` against the real dispatch_db -- fails closed with a clear per-entry reason list if evidence, hardware identity, or the DB itself is missing. Native winners are exempt (no evidence needed -- they ARE the baseline), so the ~90 pre-existing native-only test fixtures across test_replay_v5.py/test_tune_journal.py etc needed zero changes. Seed overrides gained optional `hardware`/`native` fields (mirroring `signature`'s existing override-wins precedence) since an unseen dispatch has no measured row to inherit them from. New CLI `--dispatch-db` flag.
- `lifecycle.py`: `execute_replay_export_stage()` gained a required `dispatch_db: LaneInputValue` parameter, mirroring `execute_promotion_stage()`'s own -- rehydrated and passed through to `replay_cache.build()`, added to the stage's provenance parents.
- `re15_acceptance_run.py`: passes the same `tune_result.database_ref` to the export stage that the promotion stage already uses.

Test fixture work: two existing tests in test_replay_cache_promotion_gate.py (`test_explicit_seed_bypasses_the_gate`, `test_override_winner_precedes_measurement_but_measurement_signature_wins`) previously demonstrated a seed override exporting a non-native winner with ZERO correctness evidence -- exactly the escape hatch GPT flagged. Rewrote both against a real schema-6 dispatch_db fixture with passing RV49 evidence, using `replay_cache.portable_tuning_key()` to compute a real dispatch digest once "hardware" is present (the existing hardware/signature/dispatch consistency check requires it). One test_replay_cache_wire.py transform_id test also needed a real dispatch_db + hardware field for the same reason. test_hi16_correctness_reference.py (HI16's original candidate-level contract tests) rewritten to document the new contract: schema validation no longer requires or inspects `correctness_evidence` on a candidate at all -- absence is a deliberate design decision now, not a gap.

Full offline suite: 1694 passed, 3 skipped, 61 subtests (one unrelated pre-existing failure in test_hi24_double_native.py, caused by a concurrent session's in-flight uncommitted edit to hip-autotune-tuner.cu -- not touched by this change, confirmed via git status). git diff --check clean.

STILL OPEN: real-hardware validation of this whole HI67 slice (headroom rule, deterministic seeds, promotion gate, and now the export gate) remains offline-verified only -- no compiler in this dev environment. Needs a real HIP build + hardware run to move from offline-verified to validated, same as the rest of this item's history.

### 2026-08-22: real forced-candidate proof + threshold authority (commit 35e1841)

GPT (req_559bb03e75494a74, req_52b5095ab1aa4efa) identified 3 fail-closed integration gaps that needed closing before real hardware validation makes sense. `GGML_HIP_FORCE_CANDIDATE_STRICT=1` in hip-autotune-dispatch.cu -- an ineligible/unregistered forced candidate now GGML_ABORTs instead of silently falling through; threshold authority -- SeedEvidence/correctness_evidence_seed gained a threshold_t column sourced only from test-backend-ops' own emitted marker, never a caller-supplied float. 8 new tests, full offline suite (1532 tests) passed at the time.

ALL THREE SLICES DONE (uncommitted, 2026-08-21). RV49 contract (GPT-adjudicated in RV49/RV77) is fully implemented and offline-verified end to end.

1. [DONE] Slice 1 -- tune mode stays native. Commit 692342e, pushed.

2. [DONE] Slice 2 -- CPU-reference evidence generator: patches/1222 (deterministic seeding), patches/1223 (machine-readable NMSE/max_abs), tools/bigcherry/correctness_evidence.py.

3. [DONE] Slice 3 -- promotion gating (tools/bigcherry/promotion_correctness_gate.py, tune_promotion.py's promote() hard-AND, lifecycle.py's execute_promotion_stage()) plus, as of 2026-08-23, the replay-cache export gate above.

Committed and pushed in commit 5e03cb4 ("HI64/HI67 slice 3/HI29: per-device poison scoping, promotion correctness gating, transform recording infra"), on origin/tuning-code-rebase -- verified 2026-08-22, the earlier "UNCOMMITTED FILES" list in this item's Steps section was stale.

Created 2026-08-17 from RV49 (GPT adjudication of the tuning-code review), P0 rank 1. Supersedes RV08's open design question - RV08's options (CPU-reference bound vs headroom rule vs tighter epsilons) are resolved as: CPU-reference bound WITH the 50%-remaining-headroom rule, plus worst-of-N deterministic seeds and candidate-max-abs-no-worse-than-native.

## Current Status (2026-08-23, plan-hygiene closure)

DONE -- CPU-reference correctness contract fully implemented, committed, wired, offline-verified:
- Slice 1: tune mode stays native (692342e).
- Slice 2: CPU-reference evidence generator -- patches/1222 (deterministic seed), 1223 (machine-readable NMSE/max_abs), correctness_evidence.py, schema 6.
- Slice 3: promotion gate -- promotion_correctness_gate.py (hard-AND in tune_promotion.promote(), REQUIRED dispatch_db, 3 distinct rejection statuses); lifecycle.execute_promotion_stage.
- Threshold authority + strict forced-candidate (35e1841): threshold_t sourced only from test-backend-ops' own marker; ineligible forced candidate ABORTs.
- REPLAY-CACHE EXPORT GATE (the last slice-3 piece, landed 2026-08-23): replay_cache.build(dispatch_db=...) -> _validate_correctness_gate -> CorrectnessBinding + require_correctness_binding against a real read-only DB, on EVERY non-native entry including manual --seed overrides (never bypassable); native winners exempt. Verified committed (latest touch 5a70516/HI89), no uncommitted changes. HI89 (completed) closed the export-gate follow-ups (orphaned screening reducer, seed-override native identity).

RESIDUAL (routed, not a contract gap):
- Real-HARDWARE validation of the full correctness->promotion->export chain. The evidence-PRODUCTION path was already hardware-validated on real Brutus by HI80 (completed: real gpt-oss-20b MUL_MAT, native+candidate runs, digest match, correctness metric). The end-to-end hardware pass (one real transformed winner through the whole chain, incl. fresh-process replay) is the acceptance event now owned by HI31's integration scope, not a separate HI67 deliverable. Offline contract tests (1694-pass suite, hard-AND semantics, seed-override non-bypass) are the primary assurance for a correctness GATE.

SUPERSEDED / routed elsewhere:
- Future optimization of the correctness machinery (on-device reduction) -> HI69 (profile-gated).

Closing HI67: the correctness contract it exists to define is complete and enforced. See HI31 (chain hardware pass) and HI69 (future optimization) for the routed residuals.

## Change Log

- 2026-08-17T04:43:12.912754+00:00 (created-by): Created by agent

## Ledger-events


- chg_20260817_044430_gpt-reviewed-the-tuning-code-f_3860
- 2026-08-17T04:44:30.205268+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-19T05:04:04.249095+00:00 (state-transition): State: pending → in_progress
- 2026-08-19T05:05:43.864703+00:00 (updated-by): Updated: section:steps, section:notes
- 2026-08-20T07:26:30.416577+00:00 (state-transition): State: in_progress → in_progress
- 2026-08-20T23:03:37.071824+00:00 (updated-by): Updated: section:steps, section:code_samples
- 2026-08-20T23:19:01.256537+00:00 (updated-by): Updated: section:steps
- 2026-08-20T23:24:38.180760+00:00 (updated-by): Updated: section:steps
- 2026-08-20T23:29:53.226575+00:00 (updated-by): Updated: section:steps
- 2026-08-20T23:51:40.145025+00:00 (updated-by): Updated: section:steps
- 2026-08-21T00:06:01.710478+00:00 (updated-by): Updated: section:steps
- chg_20260821_000942_scoped-tuning-faults-to-the-si_9212
- 2026-08-21T00:09:42.242971+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-22T04:48:21.181832+00:00 (updated-by): Updated: section:notes
- chg_20260822_051323_closed-two-correctness-evidenc_7657
- 2026-08-22T05:13:23.761867+00:00 (updated-by): Updated: section:ledger-events
- chg_20260822_052318_added-the-identity-resolution_2734
- 2026-08-22T05:23:18.454649+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-22T05:23:47.072891+00:00 (updated-by): Updated: section:notes
- 2026-08-22T14:57:49.902580+00:00 (updated-by): Updated (no visible changes)
- 2026-08-23T02:53:31.030960+00:00 (updated-by): Updated: section:notes
- chg_20260823_025340_closed-the-last-gap-in-the-tun_8698
- 2026-08-23T02:53:40.750506+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-23T06:27:57.569188+00:00 (updated-by): Updated: section:notes
- 2026-08-23T06:28:04.153461+00:00 (state-transition): State: in_progress → completed
- chg_20260823_062912_closed-three-plan-items-whose_6343
- 2026-08-23T06:29:12.144394+00:00 (updated-by): Updated: section:ledger-events
