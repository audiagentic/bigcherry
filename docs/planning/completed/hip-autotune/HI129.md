---
id: HI129
order: 0
plan: hip-autotune
state: completed
created-at: '2026-08-26T09:34:10.895240+00:00'
breadth: ''
skill: advanced
created-by: agent
work: M
---

# HI121 close-out step 8: closure validation (real Brutus A→B cross-generation run + independent review)

## Description

dev-gpt-agent's recommended final step (2026-08-26): run adversarial tests reproducing every RV84 attack, the full offline suite, then a REAL A→B cross-generation projection on Brutus through the whole pipeline: projection -> replay build -> correctness/promotion resolution -> replay execution. Obtain another independent review before returning HI121 to completed. Per dev-gpt-agent: 'the project should not close HI121 merely by restoring a green unit suite; the final Brutus cross-generation replay/promotion path is warranted.'

## Steps

1. DONE: full offline suite (2317+ tests) confirmed clean at validation HEAD before starting.
2. DONE: real RV84 attack regressions confirmed present/passing (HI122-127's own test suites, all closed items).
3. DONE (real Brutus hardware, 2 rounds, both independently reviewed): real A (existing hi130-live2-20260827 campaign, 19 real promoted winners) -> real B (genuinely different git revision, freshly cloned+patched+built via bigcherry's real overlay/patch system, real replay-full manifest+binary) cross-generation projection. Round 1 was correctly rejected (NO-GO) by independent review for not physically ingesting B's real measurements as a second build row. Round 2 fixed this: ran B's own real tune build, genuinely ingested its real measurements via the real CLI, confirmed via direct SQL that the specific witness (dispatch,signature,hardware) triple has real rows under BOTH build_id=1(A) and build_id=2(B) -- genuine, not synthetic, ambiguity. Directly called promotion_gate.resolve_promotion_identity() three ways as the definitive black-box proof: source_build_id=1 -> resolves A; source_build_id=None -> correctly fails closed ('found 2'); source_build_id=2 -> resolves B. Then real replay.build() export against this real ambiguous DB succeeded, and B's real replay binary executed a real completion with GGML_HIP_DISPATCH_MODE=replay: entries=1, exact=1, rerun_required=0, incompatible=0, stale=false.
4. DONE: fresh independent review (reviewer-gpt-agent, a genuinely separate reviewer identity from dev-gpt-agent, the implementation/design partner) explicitly returned GO on this evidence: 'HI129 = GO... Your previously reported run satisfies that hardware gate.'

## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files

N/A -- validation/process step, not a code change

## Validation

Real Brutus A→B cross-generation run succeeds end-to-end; independent review returns GO (not NO-GO) on the accumulated HI122-127 work.

## Effort & Risk

M, but REQUIRES real hardware access (Brutus) -- this cannot be validated purely offline.

## Standards



## Acceptance Criteria



## Notes

This is the actual HI121 closure gate. Do not mark HI121 'completed' without this step, matching the pattern already established across rounds 1-4 of this item's review history (repeated premature-completion mistakes this project has already made and corrected).

REAL-HARDWARE CLOSURE ACHIEVED (2026-08-28), HI129 = GO per independent review. Full evidence chain: pre-attestation baseline (witness correctly omitted_unverified_source), real HI128 reattestation (67 real winners attested via A's real compiled HI125 verifier, idempotent on reload), post-attestation projection (retained=1, header correctly rewritten to B with hi121_source_provenance.source_build_id=1 preserved), genuine multi-build DB ambiguity created via B's own real tune measurement, direct resolve_promotion_identity() adversarial proof (all 3 cases -- correct build, ambiguous-fails-closed, wrong-build -- behave exactly as designed), real replay.build() export, real B replay execution with exact=1/stale=false/rerun_required=0. All Brutus scratch state cleaned up after every pass; A's real shared dispatch_db confirmed byte-identical (md5sum) before and after, nothing shared ever mutated in place.

Separately attempted (same session): a live end-to-end `bigcherry tune-campaign` run to prove HI125 step 6's NEW production wiring produces real winner_verification rows from a fresh campaign. This correctly exercised the real dedicated signature-verifier lane and require_strengthened_ingest=True, then correctly hard-aborted on a real UnsupportedSignatureDomain (fused ADD op outside HI121's audited domain) -- confirmed by dev-gpt-agent to be the CORRECT designed behavior, not a defect, but real NEW evidence of an operational coverage gap (see HI136, the tracked follow-up). Per reviewer-gpt-agent's explicit read: this does NOT block HI129's own closure gate (which is the A->B chain above, not a successful fresh campaign) -- HI129 is GO -- but it DOES mean HI125's own stricter self-imposed closure text ('pending a real end-to-end campaign producing actual winner_verification rows') remains unsatisfied, so HI121 as a WHOLE stays open pending either a workload that happens to stay in-domain, or HI136's per-row quarantine work landing first.

## Change Log

- 2026-08-26T09:34:10.895240+00:00 (created-by): Created by agent
- 2026-08-27T21:42:00.906393+00:00 (updated-by): Updated: section:steps, section:notes
- 2026-08-27T21:42:05.120357+00:00 (state-transition): State: pending → completed

## Ledger-events

- chg_20260827_214214_completed-the-final-real-hardw_9421
- 2026-08-27T21:42:14.877179+00:00 (updated-by): Updated: section:ledger-events
