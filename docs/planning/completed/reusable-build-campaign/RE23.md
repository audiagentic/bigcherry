---
id: RE23
order: 23
plan: reusable-build-campaign
state: completed
created-at: '2026-08-16T11:22:34.426331+00:00'
breadth: ''
skill: advanced
created-by: agent
work: M
---

# Delete legacy mutable-checkout mechanics (RE14 step 8)

## Description

Only after RE21 (flip) and RE22 (fail-closed cutover tests) are solid and have run for a real compatibility window: delete _ensure_tree_state, _reset_tree, the checkout-path-suffixed legacy _build_dir identity, GENERATED_IN_TREE in-checkout generation output, cmd_build/legacy-build itself, and any tests that only exist to cover the mutable path.

## Steps

1. Confirm RE22's fail-closed tests have been green for a real elapsed compatibility window (not immediately after RE21 lands) before deleting anything -- this is a one-way door.
2. Delete _ensure_tree_state, _reset_tree, legacy _build_dir, GENERATED_IN_TREE, cmd_build/legacy-build and their dedicated tests.
3. Remove now-dead recipes.py legacy-only code paths that existed solely to feed cmd_build (verify nothing else still imports them first).
4. Update or retire RE14's own legacy-dependent parity harnesses (tools/re14_parity_run.py, tools/re14_real_run.py, tools/parity_loaders.py -- found by the 2026-08-19 reference-inventory audit to actively depend on the legacy path and NOT previously on this deletion list) BEFORE deleting the mechanics they depend on, or they will break silently.
5. Full test suite green after deletion, on both Windows (local) and Brutus (Linux) -- matching this whole project's dual-environment verification discipline.

## Detailed Solution & Technical Design

### Deletion sequence (locked per RV50 — after RE15, this is no longer speculative)

Objective prerequisites (all must hold): the new standard profile ran successfully (RE20); the release compatibility probe used the new path (RE13); RE15 completed without legacy (criteria 7+8); both platform suites green; repository-wide legacy reference inventory clean; integrity/adversarial matrix passing (RE22/RE24).

BEFORE touching code:
1. Repository-wide search for: `legacy-build`, `cmd_build`, `_ensure_tree_state`, `_reset_tree`, legacy `_build_dir`, `GENERATED_IN_TREE`, old mutable-checkout generation.
2. Classify EVERY hit as code / test / docs / script / CI / release tooling; verify no required production consumer remains.

Then delete: the legacy-build CLI registration; old cmd_build; _ensure_tree_state; _reset_tree; old _build_dir; the in-checkout generated-output path; recipes helpers used EXCLUSIVELY by those mechanics (a helper that originated in legacy code but is still genuinely consumed by the new path STAYS); tests existing solely for the legacy implementation.

Then verify: repository-wide search again shows no production legacy references; cutover/adversarial tests pass; full Windows suite; full Brutus suite; one `bigcherry build --all` current-head real run; release compatibility probe at current head. Then close RE23.

## Code Samples & Guidance



## Files

tools/bigcherry/__main__.py, tools/bigcherry/recipes.py, tools/tests/ (legacy-only tests removed)

## Validation

DELETION PREREQUISITES - OBJECTIVE COMPATIBILITY CYCLE (revised per RV48 audit; replaces the time-based 'real elapsed window'): (1) the new build has handled the standard profile (satisfied by RE21's real run - re-verified at deletion time); (2) the canonical release probe (post-RE13 migration) has handled at least one upstream-ref compatibility run; (3) RE15 has completed the full lifecycle WITHOUT legacy; (4) Windows and Linux suites green; (5) repository-wide reference inventory before deletion covering Python imports, scripts, docs, CI and release tooling - not only tools/tests. Delete the mutable build mechanics only once those references are gone.

ADDITIONAL GATE (RV48): the RE04/RE05/RE07/RE08 integrity fixes must be landed first - deletion retires the mutable-checkout mechanics precisely because isolated materialisation and build identity have to be content-safe, and RE24's extended adversarial matrix must be green.

## Effort & Risk

M -- deletion-only, but genuinely irreversible in spirit (git history keeps it, but this is the point where RE14 is actually complete) -- do not rush this past RE22's compatibility window.

## Standards



## Acceptance Criteria



## Notes

Parent: RE14. Depends on RE21, RE22. This is the final item -- RE14 itself can be marked completed once this lands and the full suite is green on both platforms.

2026-08-17: RE21 (flip) and RE22 (fail-closed cutover tests) both completed and verified on real hardware the same day. User explicitly asked whether to proceed straight to RE23's deletion; decided to hold off -- RE23's own step 1 requires 'a real elapsed compatibility window (not immediately after RE21 lands)' before deleting anything, since this is a one-way door. Zero real-world usage of the new `build` path has occurred yet beyond same-session verification. Do not start RE23 until real elapsed time/usage has passed -- revisit later, not as a continuation of the RE20-22 session.

AUDIT RV48 (2026-08-17): 'real elapsed compatibility window' replaced with the objective cycle above. RE15 now gates this item - before permanently deleting the old mechanics, demonstrate not only that all five builds work but that the complete release workflow no longer needs them.

## Change Log

- 2026-08-16T11:22:34.426331+00:00 (created-by): Created by agent
- 2026-08-17T03:52:35.211617+00:00 (updated-by): Updated: section:notes
- 2026-08-17T04:00:20.567738+00:00 (updated-by): Updated: section:validation, section:notes

## Ledger-events


- chg_20260817_040144_a-deep-audit-of-the-reusable-b_4365
- 2026-08-17T04:01:44.249170+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-17T14:03:34.480980+00:00 (updated-by): Updated: section:detailed_solution, order=23
- chg_20260817_140439_rescoped-the-reusable-build-ca_6101
- 2026-08-17T14:04:40.039570+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-19T02:18:47.458982+00:00 (updated-by): Updated (no visible changes)
- chg_20260819_021854_catalogued-exactly-what-code-n_6154
- 2026-08-19T02:18:54.208930+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-19T02:33:23.683198+00:00 (updated-by): Updated: section:steps
- 2026-08-20T07:28:46.840441+00:00 (updated-by): Updated (no visible changes)
- 2026-08-20T07:28:51.519850+00:00 (state-transition): State: pending → completed
- chg_20260820_072914_removed-the-old-mutable-checko_6964
- 2026-08-20T07:29:14.536260+00:00 (updated-by): Updated: section:ledger-events
