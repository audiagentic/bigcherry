---
id: RE22
order: 25
plan: reusable-build-campaign
state: completed
created-at: '2026-08-16T11:22:23.761600+00:00'
breadth: ''
skill: advanced
created-by: agent
work: M
---

# Fail-closed cutover tests for the multi-lane build flip

## Description

The tests that actually prove RE14's acceptance criterion ('the default campaign path contains no implicit fallback to legacy') holds after the RE21 flip. Without these, the flip is just an assertion, not evidence.

## Steps

1. Poison/mock legacy cmd_build (e.g. monkeypatch it to raise if called) and confirm `bigcherry build` for every canonical-profile lane still succeeds without ever invoking it.
2. Confirm an unsupported legacy-only option (--groups, --states) passed to `build` fails closed with exit 2 and a message naming `legacy-build`, performing zero work -- not a silent redirect.
3. Confirm `build --all` produces exactly the canonical lane set from RE19, no more, no fewer.
4. Confirm one lane failing (fault-injected) does not corrupt or misidentify a sibling lane's result (extends RE18's own test with a real CLI-level check).
5. Confirm no lane's execution path references _ensure_tree_state, _reset_tree, or legacy _build_dir -- a static/grep-based check plus a runtime assertion that the new path's work_root never overlaps the legacy checkout path.

## Detailed Solution & Technical Design

gpt-auto-agent design (RE14 round 19): 'Then step 7 is legitimately closed' once these pass -- named as the explicit acceptance bar for the flip, not optional hardening after it.

## Code Samples & Guidance



## Files

tools/tests/test_campaign_build_flip.py (new), tools/bigcherry/__main__.py

## Validation

This item IS itself the validation for RE14 step 7. All five checks must pass before RE14.26 (legacy deletion) is considered.

## Effort & Risk

M -- test-only, but must be genuinely adversarial (poisoning legacy, fault injection) rather than happy-path only, matching this whole project's established fail-closed testing discipline.

## Standards



## Acceptance Criteria



## Notes

Completed 2026-08-17. Implemented all five acceptance checks from the description/steps -- this item IS the validation for RE14 step 7, per its own doctrine.

test_build_flip.py (RE21, already committed) covers check 2 (broadened unsupported-flag surface: --recipe/--groups/--states/--variant-set/--force/--target/--dry-run all rejected with exit 2 on the new `build` parser, legacy-build still accepts groups/states) and the exit-1-vs-exit-2 distinction.

test_campaign_build_flip.py (new, 5 tests) covers the remaining four:
1. Poisoned legacy cmd_build (monkeypatched to raise) -- `build --all` succeeds without ever invoking it.
2. `build --all` produces exactly RE19's canonical standard-profile lane set against the REAL recipes.toml (not mocked) -- no more, no fewer.
3. Fault isolation at the CLI/result level: captured stdout/stderr prove a failing lane's error and a succeeding sibling's own build_plan_id are correctly attributed to their own lane ids, never swapped or corrupted (stronger than just checking an aggregate failure count).
4. Static check that no new-path module (campaign_planner/campaign_lane/campaign_execution/campaign_workers/campaign_plan/campaign_build/campaign_source/builds/artifacts/context) references legacy _ensure_tree_state/_reset_tree/_build_dir, plus a REAL runtime check (not assumed) that ProjectContext.resolve()'s work_root default structurally never overlaps paths.llama_root()'s default -- read both real default-resolution code paths from source before asserting (context.py: work_root defaults to LOCALAPPDATA/BigCherry/work or ~/.cache/bigcherry; paths.py: llama_root() defaults to REPO_ROOT/vendor/llama.cpp -- distinct roots by construction).

All 5 new tests passed on first run (Windows and Linux/Brutus both, 22/22 total across test_build_flip.py + test_campaign_build_flip.py) against the real RE21 implementation and real recipes.toml -- no implementation changes needed, RE21 already satisfied every check. Full suite green (832 passed, 3 skipped), ruff-clean.

RE14 step 7 is now genuinely closed per GPT's round-19 standard ('once these pass, step 7 is legitimately closed'). RE23 (delete legacy mutable-checkout mechanics) is next, but per its own notes must wait for 'a real elapsed compatibility window' post-RE22 -- do not rush straight into deletion.

## Change Log

- 2026-08-16T11:22:23.761600+00:00 (created-by): Created by agent
- 2026-08-16T23:14:07.139842+00:00 (updated-by): Updated: section:notes
- 2026-08-17T03:48:11.340952+00:00 (state-transition): State: pending → in_progress
- 2026-08-17T03:50:02.824061+00:00 (updated-by): Updated: section:notes
- 2026-08-17T03:50:05.686135+00:00 (state-transition): State: in_progress → completed

## Ledger-events

- chg_20260817_035013_added-the-adversarial-tests-th_9504
- 2026-08-17T03:50:13.481473+00:00 (updated-by): Updated: section:ledger-events
