---
id: RV50
review-of: RE14
plan: reusable-build-campaign
state: closed
reviewed-by: gpt-auto-agent (direction) + local agent (code verification)
reviewed-at: ''
---

# RV50: Campaign rescoped — reduced critical path, RE11 deferred, old 14-task order superseded

## Notes

GPT-auto-agent direction (validated by user, 2026-08-17) after re-checking tuning-code-rebase at 7afbaf3/9d55a9b against the active/completed RE plan tree. All load-bearing claims verified against code before recording: (1) plan tree is 8 active / 17 completed; RE13+RE24 completed; cutover already happened (RE20 five-lane gfx1201 proof, RE21 flip, RE22 adversarial fail-closed, RE24 post-audit matrix). (2) RE09 defect real: CURRENT_DB_SCHEMA_VERSION="3", only migration 0003 exists, build table retains the legacy UNIQUE(source_revision, manifest_hash, signature_schema, hardware_schema, variant_set, build_descriptor_hash) with nullable campaign-identity columns. (3) RE12 gap real: BenchmarkArm carries a raw executable Path + caller-supplied hashes; pointer_from_campaign_result() takes caller-supplied campaign_plan_id/report bytes/valid flag; pointer is schema 2; ab_benchmark.py already owns schedule()/block_bootstrap_effect()/validate_replay_coverage()/_run_arm(). (4) RE25.3 residual real: _resolve_lane_inputs() raw-Path branch stamps the CURRENT lane's source_slice_id onto imported-legacy provenance (unearned producer claim) and publishes without a descriptor; build/smoke derive() calls pass parents=(); sticky-taint machinery from RE25.1 is unexercised by real producers.

## Findings

New locked critical path: RE25.3 -> RE09 -> RE10-min -> RE12-min -> RE15 (single Brutus lifecycle acceptance) -> RE23 (legacy deletion) -> close RE14. RE11 (durable DAG/resume) DEPRECATED from this campaign as a separate campaign-durability backlog item — RE15's acceptance is explicitly ONE CONTINUOUS RUN, not crash/resume; nothing gates on RE11 (only RE14/RE25 reference it). The old locked 14-task sequence (RE25.2 -> RE25.3 -> RE10.1..4 -> RE09-v4 -> RE12.1..3 -> RE11.1..3 -> RE15) is SUPERSEDED. Internal contradiction found and resolved: old RE10.4 ('allow_legacy_inputs=False by default, raw Path rejected') conflicts with the new explicit non-goal 'do not remove raw Path compatibility' — RESOLVED: raw Path stays legal, becomes descriptor-backed imported-legacy via RE25.3; no allow_legacy_inputs flag is added. Second correction: publish-time kind validation (RE25.3 step 3.4) goes in _publish_ref(), NOT persist_descriptor() — RE25.1 tamper tests call persist_descriptor directly with deliberately corrupt descriptors and would break.

## Conclusion

Approved. Applied as documentation-only changes to RE09/RE10/RE12/RE14/RE15/RE23/RE25, RE11 deprecated + campaign-durability backlog item created. This is the architecture-freeze basis: no new reusable-build plan item unless a real campaign exposes a correctness/reproducibility defect.
