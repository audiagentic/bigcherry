---
id: RV52
review-of: RE30
plan: reusable-build-campaign
state: closed
reviewed-by: agent
reviewed-at: ''
---

# Verification of RE30's technical claims + structural findings (split, pin identity, sequencing)

## Notes

Full review of RE30 (Vulkan tunable build line). Every mechanical claim in the item was verified against the codebase on 2026-08-20; results below. No implementation has started, consistent with state=pending.

## Findings

VERIFIED CORRECT (mechanically, 2026-08-20):
1. All four Vulkan seam symbols (ggml_vk_mul_mat_q_f16, ggml_vk_guess_matmul_pipeline, ggml_vk_dispatch_pipeline, GGML_VK_PERF_LOGGER) exist in the pinned vendor (71 matches) AND at b10502 (same count). Only 6 Vulkan commits in the b10362->b10502 window; none rename the seams. Seam claims survive the repin.
2. campaign_build.py always emits AMDGPU_TARGETS + GGML_HIP_AUTOTUNE_* (lines 236-243) - backend-adapter split is genuinely required, as claimed.
3. Strict source audit is HIP/MMQ/rocBLAS-specific (MMQ config headers, standards 13.2 invariants) - a Vulkan audit profile is genuinely required.
4. All named persistence files exist (sql/dispatch-db.sql, replay_cache.py, inventory.py, tune_journal.py).
5. sources.py assumes flat patches/<id>.py (sources.py:171) and campaign_resolution.py infers catalog_directory from catalog[0].path.parent (lines 75, 158) - the 5-change taxonomy plan in Notes correctly identifies both.
6. The framework/1000 group-vs-recipe mismatch is real: 1000 is in [patch-set.framework] but declares GROUP=upstream-fixes. 1002-1005 are untested and outside the standard set. Both taxonomy findings are accurate.
7. Focused baseline tests pass (28/28 across test_patch_selection, test_external_sources, test_campaign_source; item claims 38/38 over a wider set - consistent).

FINDINGS (gap-level, none block the pending scope decision):
F1. Pin identity not recorded: the evaluation ran against the b10362-era vendor; the b10502 repin landed the same day (later). Seam claims re-verified at b10502 today and hold, but the item should state which pin the evaluation targeted and re-verify seams at whichever pin becomes the Vulkan baseline.
F2. Acceptance Criteria section is empty while the item is a pending, user-decision-gated L-work item. Needs at minimum an item-level definition of done (scope decided / sub-items created / step 1 stock lane proven), distinct from the staged validation list.
F3. The item bundles two separable initiatives: (a) the Vulkan initiative (steps 1-7) and (b) the declarative patch-stream catalog/taxonomy (the Notes blocks: catalog.toml, resolver, 5-change plan, folder decision). The taxonomy work is valuable WITHOUT Vulkan (it fixes the 1000 mismatch, and its upstream-retirement rule is the formal mechanism for the 1003/1004 deprecations recommended in the b10502 rebase review), is a prerequisite for step 2 (Vulkan patch groups), and is itself a large L. Recommend splitting into a sibling item (RE31, backend-agnostic patch-stream catalog) with RE30 depending on it for the patch-group portion. Decision for the user.
F4. work=L conflicts with Effort & Risk ('XL/high for full parity'). Set work after the scope decision (stock-only M vs full parity XL), not before.
F5. Sequencing: the campaign_build.py adapter split and source_audit profile refactor touch the same layer the b10502 rebase acceptance (re-inventory/re-tune, RE acceptance pass) exercises. Start the config split only after b10502 acceptance completes; record as explicit prerequisite. Not a blocker (item is pending), but should be stated.
F6. Design detail: the proposed ggml_vk_native_mul_mat_recipe() shape implies extracting a 'native recipe' object out of the monolithic upstream ggml_vk_mul_mat_q_f16 - itself a non-trivial upstream refactor, larger than the stated 'tightly scoped hook'. The vertical-slice design must define how much native code is refactored vs wrapped-around.

## Conclusion

The item's technical content is accurate - every checkable claim verified true, and the taxonomy analysis matches the code. Ship-ready as a scoped sub-initiative. Recommended before any design work: (1) user scope decision (stock-only vs full parity), (2) decide the RE30/RE31 split, (3) fill Acceptance Criteria, (4) record pin identity + b10502-acceptance prerequisite in Notes, (5) set work size to match decided scope. Incorporated into RE30 Notes: b10502 seam re-verification, sequencing prerequisite, and the split recommendation.
