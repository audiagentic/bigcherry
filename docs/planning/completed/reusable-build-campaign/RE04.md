---
id: RE04
order: 4
plan: reusable-build-campaign
state: completed
created-at: '2026-08-15T02:30:33.739351+00:00'
breadth: ''
skill: advanced
created-by: agent
work: L
---

# Add host-local upstream repository and isolated source materialisation

## Description

Materialise an exact, immutable source-input specification into a detached worktree and reuse it only after BOTH its requested materialisation inputs AND its resulting effective tree have been revalidated (revised objective per RV48 audit - this item needs a correction, not just documentation).

## Steps



## Detailed Solution & Technical Design

VERIFIED DEFECTS (RV48, all against current code): (1) campaign_source.source_plan_id hashes only upstream_revision / overlay_enabled / sorted patch IDs / required_state - no patch content, no overlay content; an in-place edit of a patch module under its canonical ID reuses the old materialisation. (2) materialize_source's cache-hit path compares exactly those same ID-level fields and returns the stored metadata without recomputing anything - the cached worktree is never re-hashed against the stored source_tree_oid/source_slice_id, so a modified cached source dir is another false reuse. (3) The clean-BigCherry check lives only inside workspace.materialize (fresh-creation path); a cache hit returns before it ever runs. (4) campaign_lane._materialize_worker hard-codes allow_dirty_bigcherry=True and execute_campaign_lane exposes no override - dirty-tree execution is implicit in the public lane API, contradicting RE16's design that it be an explicit development/harness escape hatch only.

REQUIRED FIXES: (a) Introduce a materialisation-plan identity containing the fully resolved upstream commit, overlay content identity, patch_set_id with ordered module IDs + content hashes, and required state; key the destination directory by THAT identity. Do not make source_slice_id carry these labels - it remains resulting-content identity (see RE05). (b) On a cache hit, recompute the effective source tree and require equality with the stored source_tree_oid/source_slice_id before reuse; a modified cached worktree fails closed rather than compiling stale bytes. (c) Move the clean-project check so it surrounds BOTH fresh creation and cache reuse: unless an explicit development override is set, dirty BigCherry source rejects either path. (d) execute_campaign_lane defaults to allow_dirty_bigcherry=False with an explicit argument; only the RE14 harness passes True.

Preserve what already works: detached-worktree semantics, temporary-index effective-tree computation, allowed-untracked overlay identity, exact patch application.

## Code Samples & Guidance



## Files

tools/bigcherry/workspace.py, tools/tests/test_workspace.py

## Validation

NEGATIVE/FALSIFICATION TESTS (RV48): (1) edit a patch module's contents without changing its canonical ID - the next execution must NOT reuse the old source materialisation; (2) change an overlay file's content - same requirement; (3) modify an already-cached source worktree - reuse fails closed; (4) dirty BigCherry source rejects both fresh materialisation and cache reuse unless the explicit development override is set; (5) existing workspace/temp-repo tests (exact detached HEAD, non-empty target rejection, explicit removal) keep passing. REAL-HARDWARE: one real lane run on Brutus through the rekeyed materialisation path.

## Effort & Risk



## Standards



## Acceptance Criteria



## Notes

RE04 reopened and re-closed (commit d9b393c) after a comprehensive GPT review found a fresh gap in the cache-hit reuse path: it re-hashed the worktree's git tree and compared physical patch/overlay identity, but the persisted source_slice_id/source_plan_id/materialization_plan_id fields in the sibling .metadata.json (which lives BESIDE the worktree, never covered by git_tree_oid()) were returned to the caller verbatim, unverified. A directly forged source_slice_id in that file -- independent of any worktree byte change -- would have been trusted for the rest of campaign execution. Fixed: on cache-hit, each of those three identities is now re-derived from the freshly re-verified (upstream_revision, tree_oid, object_format) and the current request, and the reuse fails closed (CampaignBuildError) on any disagreement -- the persisted record is now treated as an assertion to re-prove, not an authority to trust. Verified with two new real reproducers (forged source_slice_id, forged source_plan_id in a real persisted metadata file, both now correctly rejected on reuse). Verified on Windows and Brutus; full suite green both places.

## Change Log

- 2026-08-15T02:30:33.739351+00:00 (created-by): Created by agent
- 2026-08-15T02:46:08.512710+00:00 (updated-by): Updated: section:notes, section:validation
- 2026-08-15T02:46:08.625395+00:00 (state-transition): State: pending → in_progress

## Ledger-events


- chg_20260815_024659_added-the-reusable-campaign-fo_4618
- 2026-08-15T02:46:59.691429+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-15T03:34:10.668403+00:00 (updated-by): Updated: section:notes
- 2026-08-17T03:58:10.233598+00:00 (updated-by): Updated: section:description, section:detailed_solution, section:validation, section:notes
- chg_20260817_040144_a-deep-audit-of-the-reusable-b_4365
- 2026-08-17T04:01:44.038993+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-17T04:18:13.921041+00:00 (updated-by): Updated: section:notes
- 2026-08-17T04:18:18.164557+00:00 (state-transition): State: in_progress → completed
- chg_20260817_041829_fixed-a-real-gap-where-editing_9729
- 2026-08-17T04:18:29.719953+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-17T10:16:57.936489+00:00 (updated-by): Updated: section:notes
- chg_20260817_101706_a-deeper-review-pass-found-and_1747
- 2026-08-17T10:17:06.181984+00:00 (updated-by): Updated: section:ledger-events
