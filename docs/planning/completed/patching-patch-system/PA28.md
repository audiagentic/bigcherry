---
id: PA28
order: 4
plan: patching-patch-system
state: completed
created-at: '2026-09-11T23:46:00.279817+00:00'
breadth: ''
skill: advanced
created-by: agent
work: M
priority: P2
---

# Add semantic patch-sets and transitional sources without moving live consumers

## Description

Create the PA20 taxonomy after PA27 and PA26 pass. This step is additive only: old `patch-set.framework`, `source.bigcherry-native`, and the current `source.bigcherry` behavior remain available until PA29 performs the cutover.

## Steps

1. Add `patch-set.serving-core` from the PA26-confirmed serving classification.
2. Add `patch-set.campaign-support` for the PA27 tune/record half plus 0700, 0800, 0810, 0820, 0900.
3. Add `patch-set.qualification-support` for 0830 and 1100.
4. Keep `patch-set.upstream-fixes` and `patch-set.validated-enhancements` independent.
5. Add an explicit serving control source (`bigcherry-serving-base`) = serving-core + upstream-fixes.
6. Add `bigcherry-tuning` = serving-core + campaign-support + upstream-fixes. Enhancements/experiments remain explicit overlays rather than implicit additions.
7. Add `bigcherry-qualification` = serving-core + qualification-support + upstream-fixes; campaigns that also need campaign-support must request the composition explicitly.
8. Do not rename the canonical release source: `source.bigcherry` remains the production release owner and is cut over to serving-core only in PA29 after migration inventory is complete.

## Detailed Solution & Technical Design

All identities must come from existing `campaign.resolution.resolve_patch_set()/resolve_lane()/resolve_canonical_selection()`. Do not create a second recipe resolver. Add tests over ordered module IDs/hashes and `patch_set_id`, not only set membership.

During this coexistence phase, the old and new names may intentionally resolve to overlapping module sets. That is temporary migration scaffolding, not a permanent compatibility layer; PA31 removes the old framework/native names after PA30.

## Code Samples & Guidance



## Files

`config/recipes.toml`; resolution/config contract tests; source/build documentation that enumerates canonical source purposes.

## Validation

Resolve every new source and record source ref, patch-set ID, ordered module hashes, required-state policy, backend, and overlay flag. Build one representative profile for each new source. Assert 0800 and 1100 are not orphaned.

## Effort & Risk



## Standards



## Acceptance Criteria

Met 2026-09-15: new semantic sets/sources (serving-core, campaign-support, qualification-support, bigcherry-serving-base, bigcherry-tuning, bigcherry-qualification) resolve deterministically (pinned by test, distinct patch_set_ids, repeat-resolution byte-identical); current live consumers (framework, bigcherry-native, bigcherry) unchanged (pinned by test); source.bigcherry not cut over (still framework+upstream-fixes+validated-enhancements); every old framework patch has exactly one documented semantic owner among the three new sets (pinned by test, with 0700 deliberately owned by serving-core rather than campaign-support per GPT review req_d9bf628dc9954595 -- see notes). 0800 and 1100 confirmed not orphaned.

## Notes

2026-09-15: Implemented. Added additive-only PA20 taxonomy to config/recipes.toml: patch-set.serving-core (8 modules: framework's 15 minus PA26's EXPECTED_REMOVED_MODULES 0110/0700/0800/0810/0820/0830/0900/1100, EXCEPT 0700_coverage_counters which was deliberately kept in serving-core rather than moved to campaign-support), patch-set.campaign-support (5 modules: 0110/0800/0810/0820/0900), patch-set.qualification-support (2 modules: 0830/1100), and three new composed sources bigcherry-serving-base/bigcherry-tuning/bigcherry-qualification per PA28's documented compositions. Old patch-set.framework/source.bigcherry-native/source.bigcherry left completely untouched (confirmed by test: their module sets/patch_set_ids are unchanged).

**Deviation from PA28's literal step 2 text, with GPT sign-off**: step 2 says campaign-support gets '...plus 0700, 0800, 0810, 0820, 0900'. GPT design-review consult (dev-gpt-agent, req_d9bf628dc9954595) flagged that 0700_coverage_counters is not purely diagnostic -- its family-entry block calls ggml_hip_dispatch_family(...) under GGML_HIP_DISPATCH outside the diagnostics-only counter guard, i.e. real serving-relevant dispatch-interception plumbing (this is the same 0700 concern PA26's own notes flagged and explicitly deferred to PA28 for adjudication). Splitting 0700 into a serving half and a diagnostics half is real patch-authoring work, out of scope for this additive/config-only pass. Applied GPT's approved minimal-safe fallback instead: kept 0700 whole, placed it in serving-core (not campaign-support) so no serving-relevant code can ever be omitted by a source that only requests serving-core -- bigcherry-tuning already composes serving-core, so nothing is lost for tuning consumers either way. No orphan/duplicate-claim property still holds: serving-core ∪ campaign-support ∪ qualification-support reconstitutes the full 15-module framework set exactly, each module owned by exactly one of the three new sets. 0830 stays in qualification-support and 1100 stays in qualification-support, matching PA28.md literally (GPT confirmed both as correct/acceptable).

Added 10 new tests (PA28SemanticPatchSetTests in tools/tests/campaign/test_campaign_resolution.py) covering: serving-core membership including the 0700 deviation, campaign-support/qualification-support partition of the remaining 7 removed modules, no module claimed by more than one of the three new sets, each of the three new sources' exact composition, distinct+deterministic patch_set_ids for the three new sources, old framework/native/release sources unchanged, and 0800/1100 explicitly not orphaned (per PA28's own Validation section).

Full offline suites re-run clean: tools/tests/campaign (755 passed, 2 pre-existing unrelated failures: experiment-contract count off-by-one, VA24 lint), tools/tests/patch (1055 passed, 1 pre-existing unrelated failure: test_framework_validation_packages.py's 0100 check-id assertion, stale from PA27's serving-source-selection rename, confirmed via git log the test file predates this session), tools/tests/tuning (1238 passed, 1 pre-existing unrelated failure: HI104 C++ tuner-flush field test). No new failures introduced by PA28's changes.

GPT design review: req_d9bf628dc9954595 (initial review, flagged the 0700 issue, confirmed everything else -- 'PA27 tune/record half' = 0110 confirmed, three source compositions confirmed correct) and req_b038495c92c64824 (confirmation follow-up after applying the fix, sent -- no reply required unless GPT flags a remaining issue).

Per PA28's own Acceptance Criteria, real hardware validation was not required (additive/config-only, PA26 already proved the underlying serving-core equivalence on real hardware) -- validation here is config-resolution-level only (deterministic resolve, no orphan, old sources unchanged), matching what PA28.md's Validation section actually asks for. Marking PA28 completed.

## Change Log

- 2026-09-11: created.
- 2026-09-14: replaced stale three-set/source design with a no-orphan taxonomy and preserved the canonical release source.

## Ledger-events

- chg_20260914_003255_fixed-the-patch-system-plans_2121
- 2026-09-14T00:33:09.729280+00:00 (updated-by): Updated: section:ledger-events
- chg_20260915_090435_added-the-new-serving-corecam_3329
- 2026-09-15T09:04:38.423419+00:00 (updated-by): Updated: section:ledger-events
- 2026-09-15T09:05:00.173499+00:00 (updated-by): Updated: section:acceptance_criteria, section:notes
- 2026-09-15T09:05:04.628081+00:00 (state-transition): State: pending → completed
