---
id: RV166
review-of: PA24
plan: patching-patch-system
state: closed
reviewed-by: dev-gpt-agent via gateway
reviewed-at: '2026-09-12T16:25:34Z'
---

# GPT implementation design for PA24 canonical validation documentation

## Notes

Gateway request req_aae6ddb34f6943d2 in reused session ses_76206cac3e6b4be0; prior failed request req_df8b720401e1430c was reconciled then abandoned without adopting any response.

## Findings

Inspected exact pushed main commit 0c5fa66df957f4c7390d91df926c0230f890032f. RV165's five corrections are present. Four additional corrections are required before implementation:

1. docs/reference/testing/PATCH_VALIDATION.md: preserve current guidance but map current blocks into the planned order; G1 must distinguish focal evaluate_summary_gate() from repository-LINT evaluate_repository_lint_gates() -> _evaluate_lint_summary(), and the live SUMMARY check covers Status and Plan item, not Group. G3 must distinguish focal evaluate_package_gate() from LINT _evaluate_lint_package(); operational not-required returns NA before require_execution_package() or performance-documentation checking, while repository LINT still invokes per-descriptor check_performance_evidence_for_patch(). A real new validation campaign still requires require_execution_package(), independent of lint grandfathering. Correct --source <source-tree> to <source-name>, use executable examples with positional patch ID, document G4 via validation_evidence_statuses(), G6 policy, and G7 evaluator versus current bootstrap seam.
2. docs/reference/patches/PATCH_AUTHORING.md needs a factual fix in the optimization section: it currently says aggregate check_performance_evidence() enforces the rule in patch-lint; PA23 now uses the repository lint adapter and per-descriptor check_performance_evidence_for_patch(). Keep all other policy text.
3. docs/reference/patches/PATCH_SYSTEM.md needs a factual fix: live G1 checks SUMMARY Status and Plan item, not Group. Remove Group from the claim; do not add a second matrix.
4. tools/tests/patch/test_patch_validation_docs.py must be a documentation-contract test: dynamically derive expected gate applicability from GateId/GateIntent/gate_applies() with no EXPECTED_APPLICABILITY map; dynamically derive public patch-gates --intent choices from build_parser(); require one matrix and ordered G0-G7 headings after fenced-code removal; check fully-qualified backticked authority symbols including LINT paths; scan only the changed/reference-chain documents, normalizing line endings, stripping fenced code, ignoring external/mailto/fragment-only URLs, and resolving file.md#fragment. Add in-memory parser fixtures for fake fenced headings/table rows, external/fragment URLs, and relative links with fragments.
5. The plan's file list and acceptance criteria must add PATCH_AUTHORING.md and PATCH_SYSTEM.md for these factual corrections, while README/index files remain verification-only and PATCH_REFACTOR_RUNBOOK.md remains cross-link-only.
No tests were run.

## Conclusion

CHANGES REQUIRED
