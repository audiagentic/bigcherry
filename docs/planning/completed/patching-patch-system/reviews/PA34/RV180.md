---
id: RV180
review-of: PA34
plan: patching-patch-system
state: closed
reviewed-by: dev-gpt-agent via gateway
reviewed-at: '2026-09-12T21:33:28Z'
---

# GPT roadmap review: PA34.1 exact selector and G2 identity is next

## Notes

The gateway response is an exact whole-project static review. It did not run repository commands or tests. Preserve test-runner unavailability as a validation limitation; do not close PA34 from this review alone.

## Findings

- Whole-project order is approved with corrections: BRC01 is implemented but validation-open; PA34 is the next production-code slice; PA33 follows PA34 and PRBC01; PA20 children remain PA27 -> PA26 -> PA28 -> PA29 -> PA30 -> PA31.
- PA34.1 must establish one canonical source/experiment/focal-overlay selector identity before adding patch-gates CLI options.
- Extend campaign.resolution.CanonicalSelection with module_hashes, selector_kind, and selector_name; extend resolve_canonical_selection with mutually exclusive experiment and focal_overlay_patch_id while reusing resolve_lane, patchset.expand_composition, and resolve_lane_overlay.
- Extend patch.rebase report schema and freshness checks to persist and compare selector kind/name, source/ref, patch_set_id, ordered patch IDs, and ordered content hashes; preserve --all behavior for G6.
- Keep patch.gates policy unchanged except for the minimum expected-selector handoff required for G2. PA34.2 will add CLI presentation after this identity seam is tested.
- Do not implement arbitrary overlays, new resolvers, second evaluators, recipe changes, compatibility shims, or PA20 taxonomy migration in this slice.
- Execution evidence: request req_34a41c4fda8149df, same gateway session ses_76206cac3e6b4be0, exact terminal response retrieved after lifecycle success.

## Conclusion

APPROVE WITH CORRECTIONS; implement PA34.1 first.
