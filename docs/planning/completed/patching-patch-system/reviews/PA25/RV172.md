---
id: RV172
review-of: PA25
plan: patching-patch-system
state: closed
reviewed-by: dev-gpt-agent
reviewed-at: '2026-09-12T17:56:31Z'
---

# GPT final validation: separate G4 evaluation state and fix focal-overlay seam

## Notes

Gateway request req_7aad71bd36ba4e28, same GPT session ses_76206cac3e6b4be0. No tests or local commands were run; validation was static against the exact pushed commit.

## Findings

On pushed 650237068b43d82593945a4ecd38d5192d4a1f3c, PA25 still conflates NOT_EVALUATED with GateResult in its receipt schema and acceptance. Use separate G4 evaluation values EVALUATED/NOT_EVALUATED/NOT_RUN and G4 GateResult values PASS/FAIL/BLOCKED/NA/none; 1225/1232 should report evidence-authority output with G4 evaluation NOT_EVALUATED, not a GateResult named NOT_EVALUATED. Update title/description language so all seven do not appear to have focal G4 statuses. PA34 needs a concrete focal-overlay implementation seam: expand focal dependency closure with patchset.expand_composition((focal,)), remove IDs already in the resolved base while preserving order, pass remaining exact set to resolve_lane_overlay(), and use deterministic identity focal:<patch-id>. Choose CLI selector --focal-overlay, mutually exclusive with --experiment and requiring --source, and test dependencies split between base and overlay.

## Conclusion

CHANGES REQUIRED
