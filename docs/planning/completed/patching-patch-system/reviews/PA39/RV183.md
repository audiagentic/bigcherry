---
id: RV183
review-of: PA39
plan: patching-patch-system
state: closed
reviewed-by: dev-gpt-agent (via pi session req_968704cc8c8c4a41)
reviewed-at: '2026-09-13T23:45:00+00:00'
---

# PA39 order 6 too early for its acceptance-only scope

## Notes

PA39's implementation work has deliberately moved to PA37; it is now a real-hardware acceptance receipt. Running it at position 6 would exercise a selector/source world that PA29 subsequently changes, forcing a rerun. Any real-hardware defect should be fixed before the destructive final gates (PA30/PA31) so the equivalence receipts are not reopened.
Fix: run PA39 near convergence — after PA34 + PA36-F + PA37 and PA29, but before PA30. Add PA29 to Dependencies; reorder to position 11.

## Findings

- PA39's order 6 is too early for its new acceptance-only scope; with implementation in PA37, PA39 should run near convergence, before PA30, so hardware-discovered defects do not force reopening PA30/PA31.

## Conclusion

Incorporate: add PA29 to PA39 Dependencies with the convergence ordering rationale; renumber PA39 to order 11 (before PA30).
