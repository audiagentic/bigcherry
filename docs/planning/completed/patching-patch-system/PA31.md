---
id: PA31
order: 12
plan: patching-patch-system
state: completed
created-at: '2026-09-11T23:46:02.488224+00:00'
breadth: ''
skill: intermediate
created-by: agent
work: S
priority: P2
---

# Delete patch-set.framework and source.bigcherry-native after PA30

## Description

Final PA20 cutover. Remove the obsolete aggregate patch-set and ambiguous control-source name only after PA30 proves the migrated roles. Do not add aliases, deprecated options, forwarding config, or fallback lookup.

## Steps

1. Require PA30's final receipt at the exact revision being changed.
2. Remove `[patch-set.framework]` from `config/recipes.toml`.
3. Remove `[source.bigcherry-native]`; `bigcherry-serving-base` is the explicit control source.
4. Confirm `source.bigcherry`, `bigcherry-tuning`, and `bigcherry-qualification` resolve only through semantic patch-sets plus the independent policy sets they require.
5. Search all live code/config/current docs/tests for old identifiers. Historical plans, reviews, commit messages, ledger entries, and immutable receipts may retain them.
6. Run config/resolution tests, full offline suite, patch-lint, exact-source rebase checks, and all-patches disposition coverage.
7. Record the no-shim decision and close PA20/PA26-PA31 when all children are complete.

## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files

`config/recipes.toml` plus any final live-reference cleanup exposed by the deletion test.

## Validation

Full offline verification suite run after the deletion/migration: tools/tests/campaign (694 tests), tools/tests/patch (976), tools/tests/tuning (1104), tools/tests/integration (100), tools/tests/release (136), tools/tests/cli (10) -- all pass except pre-existing failures individually confirmed unrelated to this change (no reference to framework/bigcherry-native, present in files this change did not touch): test_experiment_contract.py's contract-count assertion, test_va24_legacy_migration_lint.py, test_hi104_nested_runtime.py (C++-build dependent), test_releases.py (Windows /tmp path + fixture bug), and 4 tools/tests/core gaps (retired telemetry facade, a stale doc anchor link, an undocumented tools/lab file). patch-lint: exits with only pre-existing unrelated RD-patch warnings. patch-rebase-check --all: 61/61 CLEAN. Repo-wide grep for bigcherry-native/patch-set.framework confirmed no remaining live reference outside historical/completed docs, receipts, and synthetic test fixtures that never touch real recipes.toml. Final diff reviewed and approved by dev-gpt-agent (session ses_ba24f7255d674a87, requests req_9955e9e38a7a4443/req_ab813e6ff4d74021/req_b566b97d366c418c) across 3 rounds, including one real defect it caught and had fixed+re-verified before final go-ahead (see notes). Committed as 1bf4118c on patch-refactor.

## Effort & Risk



## Standards



## Acceptance Criteria

The old patch-set/source identifiers no longer exist in live configuration or live consumers; no compatibility layer was introduced; semantic sources resolve and validate cleanly; historical provenance remains intact; and PA30's accepted receipt matches the deletion revision or is revalidated after any intervening change.

## Notes

2026-09-16: Fresh-agent pass. Confirmed PA30's accepted receipt (1cb31c7d) is still HEAD -- no intervening changes to recipes.toml's framework/bigcherry-native entries. Did full live-reference inventory via grep across config/tools/docs/tests: found the real live consumers that would break on deletion --
- tools/bigcherry/campaign/replay_equivalence.py (PA26-era harness resolving cfg.patch_sets['framework']/cfg.sources['bigcherry-native'] against real config; fully superseded by PA28's real serving-core + PA30's real hardware proof) plus its test file tools/tests/campaign/test_replay_equivalence.py
- tools/bigcherry/campaign/replay_equivalence_hardware.py (default param control_source='bigcherry-native', imports replay_equivalence.py's ephemeral SERVING_CORE_SOURCE_NAME)
- tools/bigcherry/campaign/replay_equivalence_pa30.py's require_pre_cutover_release_identity() (PA30's own gate-2 check, structurally needs patch_sets['framework']; PA30 is closed so this is retireable)
- tools/bigcherry/patch/evidence.py's verify_framework_configuration_patch() -- LIVE gate, hard-codes source_name=='bigcherry-native' and resolves lane 'bigcherry-native'
- tools/bigcherry/patch/validation_campaign.py line 5980 baseline_source='bigcherry-native' -- LIVE --framework-configuration command
- ~10 test files asserting against real cfg.patch_sets['framework']/source 'bigcherry-native' (test_campaign_resolution.py's PA28SemanticPatchSetTests incl. test_old_framework_and_native_still_resolve_unchanged_after_pa29 which is now structurally obsolete, test_campaign_source.py, test_pin_bump.py, test_framework_configuration_evidence.py, test_experiment_contract_cli.py)
Confirmed doctor.py's 'framework' checks and registry.py PATCH_KINDS are an UNRELATED concept (patch kind, not patch-set) and must not be touched. Several other test files (test_config_v2.py, test_cli_source_pull.py, test_re04_materialization_safety.py, test_qualification_matrix.py) use 'framework'/'bigcherry-native' only as arbitrary names in synthetic inline-TOML/hand-built Config fixtures that never touch real recipes.toml -- not broken by the deletion, no change required.

BLOCKED before any edit: per this project's established pattern (and this task's explicit instruction), attempted the required GPT design-review consult THREE times (dev-gpt-agent, req_2626938c0c4f4b11, req_3ca58c8261614e35, req_4098475309294ec9 -- two of them new_session:true retries per the established stall-workaround pattern). All three stalled identically; task-diagnostics on the cancel response confirmed the root cause explicitly: template-context.session.endpoint_reachable=false on every attempt -- the gpt-auto ChatGPT session endpoint is genuinely unreachable right now, not a prompt-complexity or session-specific issue. This matches the precedent already recorded in this branch's own history (commit b901d7d2: 'PA30: gates 4/5 blocked before hardware by GPT gateway stall (real infra failure)'). All three requests were cancelled rather than left running. Per explicit instruction not to fabricate a GPT review outcome, and this project's own doctrine that config/recipes.toml edits get a design review first, NO EDITS WERE MADE. PA31 remains pending. The full deletion plan above (inventory + proposed migrations to bigcherry-serving-base) is ready to execute as soon as the gpt-auto gateway is reachable again -- re-run the design-review consult (the 3 prompts are reconstructable from this note), get go/no-go, then implement steps 2-7 of PA31.md in one pass.

2026-09-16: Implemented per GPT's concrete disposition (dev-gpt-agent session ses_ba24f7255d674a87, req_cc8875a76696488d) once the gateway outage flagged in the prior note cleared. Deleted [patch-set.framework]/[source.bigcherry-native] from config/recipes.toml (no alias/shim). git rm'd the PA26/PA30 replay-equivalence executable apparatus (replay_equivalence.py, replay_equivalence_hardware.py, replay_equivalence_pa30.py + 3 test files) plus two throwaway pa26/pa30 hw-run lab scripts (tools/lab/pa26/pa26_hw_run.py, tools/lab/pa30/pa30_hw_run.py) that imported them directly -- their JSON/JSONL receipts kept as historical provenance, confirmed with GPT as in-scope. Migrated evidence.py::verify_framework_configuration_patch() and validation_campaign.py::_run_framework_configuration() to bigcherry-qualification-tuning (serving-core+campaign-support+qualification-support+upstream-fixes -- the canonical semantic replacement for the deleted framework+upstream-fixes composition, per GPT). Removed the source_name=="bigcherry-native" authority check from evidence.py; source_name is now receipt provenance only, composition/base/source identity remain the enforced authority via source_identity/source_composition. Migrated ~12 test files by role (serving-control->bigcherry-serving-base, tuning/campaign->bigcherry-tuning, full-aggregate-composition assertions->bigcherry-qualification-tuning); replaced test_old_framework_and_native_still_resolve_unchanged_after_pa29 with test_old_framework_and_native_identifiers_are_gone_after_pa31. Synthetic-fixture tests using "framework"/"bigcherry-native" as arbitrary inline-TOML names (test_config_v2.py's other method, test_cli_source_pull.py, test_patch_source_isolation.py, test_re04_materialization_safety.py) confirmed untouched by real config and left as-is. Updated current-usage docs (GETTING_STARTED.md, docs/reference/build/BUILD.md, docs/reference/testing/PATCH_VALIDATION.md); historical/completed docs and receipts retain the old names as provenance, untouched. Repo-wide grep confirmed no remaining live reference. Verification: tools/tests/campaign (694), tools/tests/patch (976), tools/tests/tuning (1104), tools/tests/integration (100), tools/tests/release (136), tools/tests/cli (10) all pass except pre-existing failures individually confirmed unrelated (no reference to deleted identifiers, in files untouched by this change: test_experiment_contract.py contract-count, test_va24_legacy_migration_lint.py, test_hi104_nested_runtime.py, test_releases.py, 4 tools/tests/core gaps). patch-lint passes (pre-existing unrelated RD-patch warnings only). patch-rebase-check --all: 61/61 CLEAN. GPT reviewed the full final diff across 3 rounds (req_9955e9e38a7a4443, req_ab813e6ff4d74021, req_b566b97d366c418c) and caught one real defect: a test constant (_NON_SERVING_MODULES) wrongly reused PA26's stale 8-module EXPECTED_REMOVED_MODULES semantics (including 0700_coverage_counters, which PA28 later moved back into serving-core) instead of the current 7-module campaign/qualification ownership -- fixed (renamed to _CAMPAIGN_AND_QUALIFICATION_MODULES/_FORMER_FRAMEWORK_MEMBERSHIP, tests rewritten to assert pairwise disjointness + correct union) and re-verified (33/33). GPT gave final explicit go-ahead to mark PA31 completed. PA30's accepted receipt (1cb31c7d) is the direct parent state of this change (HEAD was 1a895c20, a no-edit commit) -- no revalidation needed. Committed 1bf4118c. Checked PA20 (parent item): NOT closed -- PA27 (mechanically split 0100) remains genuinely pending its own step 6 real-hardware validation per PA27.md's own notes, so PA20's acceptance criteria are not yet met regardless of PA31's completion.

## Change Log

- 2026-09-11: created.
- 2026-09-14: rebased to the corrected source-role design and explicit no-shim deletion contract.

## Ledger-events

- chg_20260914_003255_fixed-the-patch-system-plans_2121
- 2026-09-14T00:33:18.194662+00:00 (updated-by): Updated: section:ledger-events
- 2026-09-15T14:27:28.743880+00:00 (updated-by): Updated: section:notes
- chg_20260915_151541_removed-the-old-no-longer-nee_3920
- 2026-09-15T15:15:44.694422+00:00 (updated-by): Updated: section:ledger-events
- 2026-09-15T15:16:02.160934+00:00 (updated-by): Updated: section:notes
- 2026-09-15T15:16:18.177932+00:00 (updated-by): Updated: section:validation
- 2026-09-15T15:16:22.471265+00:00 (state-transition): State: pending → completed
