---
id: PA27
order: 2
plan: patching-patch-system
state: completed
created-at: '2026-09-11T23:46:00.170773+00:00'
breadth: ''
skill: advanced
created-by: agent
work: M
priority: P2
---

# Mechanically split 0100 into serving dispatch/replay and campaign tune/record build plumbing

## Description

First implementation step of PA20. Split `patches/0100_cmake_options` by runtime ownership while preserving the current aggregate behavior and patch ordering.

The previous plan's "autotune/SQLite half" is stale. Current 0100 explicitly has no SQLite link edit and says record mode writes JSONL with offline stdlib SQLite processing. `GGML_HIP_AUTOTUNE_SQLITE` still appears as an option declaration in 0100, so this item must first determine whether it has any live consumer; if not, remove that dead option instead of creating a new patch around it.

## Steps

1. Inventory every 0100 edit and every live reference to each introduced CMake option/compile definition. Classify each symbol as serving dispatch/replay, campaign tune/record/diagnostic, shared validation, or dead.
2. Prove whether `GGML_HIP_AUTOTUNE_SQLITE` has a live consumer. Delete it if declaration-only; do not preserve a compatibility option without a consumer.
3. Keep the serving half responsible only for what a replay/serving build needs: dispatch/replay enablement, generated registry inclusion, production source list, and validation that makes invalid replay builds fail closed.
4. Move tune/record-only options, tuner source inclusion, workload/record routing, and campaign-only diagnostics to a separately selectable package. Shared definitions must have one owner; avoid two patches editing the same semantic block in incompatible ways.
5. Preserve the old aggregate behavior by composing both halves in the transitional framework set. Add exact apply/idempotence and composition tests before changing any source recipe.
6. Build at least: stock/no-dispatch, replay serving, record, and tune. Use the normal generate/build path; dispatch-enabled builds require generated `hip-autotune-registry.inc`.
7. Verify a serving build has no tuner/record-only symbols or dependencies while still resolving and replaying winners.

## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files

`patches/0100_cmake_options/**`; one new packaged patch allocated through the normal patch-authoring rules; `config/recipes.toml` only for transitional aggregate composition; focused patch/apply/build tests.

## Validation

Patch apply and idempotence on the pinned base; aggregate tree equivalence for all behavior intentionally preserved; source/symbol inspection of replay vs tune builds; generated-input identity; focused CMake negative tests for mutually exclusive/invalid option combinations.

## Effort & Risk



## Standards



## Acceptance Criteria

Two independently selectable packages exist; composing them reproduces the current intended 0100 behavior; serving replay builds contain no tune/record-only code; dead SQLite compatibility is either proven live and correctly owned or removed; no source taxonomy is cut over until PA26 passes.

## Notes

Do not add aliases, forwarding patches, or an opt-out switch that leaves the monolith intact. Git history is the compatibility record.

Do not add aliases, forwarding patches, or an opt-out switch that leaves the monolith intact. Git history is the compatibility record.

## Progress (2026-09-16) -- Step 6 closed

Consulted dev-gpt-agent (session ses_eb90c710fe4c442e, req_c5775b6977a34830) before doing any new hardware work. GPT verdict: PA26/PA30's existing real two-arm hardware receipts (bigcherry-native vs bigcherry-serving-base, decision_equivalence.status=EVALUATED, runtime.equivalent=true, zero dispatch-arm divergence) already satisfy the "still resolving and replaying winners correctly" half of step 6 -- do not rerun a GPU equivalence campaign. But they do NOT satisfy the literal acceptance clause "serving replay builds contain no tune/record-only code", since CMakeCache/module-composition equivalence and runtime-decision equivalence never inspected the compiled artifact itself. GPT specified a minimal, narrow, build-only closure pass instead: build one dispatch-enabled bigcherry-serving-base replay configuration via the normal path and inspect its build/link graph and symbols; confirm the four PA27 build modes (stock/replay/record/tune) actually build under the real current sources; no new hardware run needed.

Did exactly that on Brutus (idle-verified first: `ps aux` clean, `rocm-smi --showuse` 0% all 4 GPUs), against the real current sources (not the deleted transitional bigcherry-native/framework names), via the normal `python3 -m bigcherry build --lane` path, after removing a stray untracked `.rocprofv3/` trace directory (unrelated leftover from a prior session) that was blocking the campaign engine's dirty-repo check:

1. `bigcherry-serving-base:stock:linux-multi` --arch gfx1100 -- built OK, build_plan_id=3f20d6c628866b8be948ee68f8274a7c.
2. `bigcherry-serving-base:replay:linux-multi` --arch gfx1100, --inventory/--winners = the real RHA15-verify2 corpus (`~/.cache/bigcherry/tune-campaigns/pa26-rha15-verify2/{inventory.json,promoted.jsonl}`) -- built OK, build_plan_id=0f0cab245e5a4b7c73806a27806f5ffc, using the generated hip-autotune-registry.inc as PA27 step 6 requires.
3. `bigcherry-tuning:record:linux-multi` --arch gfx1100 -- built OK, build_plan_id=0256ef13f05c60e01ab5366f03dbc6e6.
4. `bigcherry-tuning:tune:linux-multi` --arch gfx1100, --inventory = same corpus, workload-max variant-set -- built OK, build_plan_id=cf2c4192ecc028c9ce132fc1dbc4b4a7.

Build-graph inspection of build #2's object directory: `find` for hip-autotune-tuner.cu.o, hip-autotune-record.cpp.o, hip-autotune-io.cpp.o, hip-autotune-journal.cpp.o, hip-autotune-smi.cpp.o, hip-autotune-coverage.cpp.o (0110's six campaign-only source files per patch.py) came back empty -- none were compiled.

`nm -C --defined-only` on build #2's libggml-hip.so: zero symbols matching tuner|autotune_record|autotune_io|autotune_journal|autotune_smi|autotune_coverage. The only hip_autotune_*/hip_dispatch_*/hip_replay_* symbols present (ggml_hip_dispatch_*, ggml_hip_replay_*, ggml_hip_autotune_available/candidate_count/candidate_name/can_tune/flush/manifest_hash/source_revision/write_report, ggml_hip_mmf/mmq/mmvf/mmvq/blas_can_execute/workspace, signature digest/json helpers) are all 0100-owned dispatch/replay/reporting symbols, not 0110 tune/record internals.

Negative control (same inspection on build #4, the tune build, same pin): hip-autotune-tuner.cu.o/journal.cpp.o/smi.cpp.o/io.cpp.o DID compile, and nm shows the full ggml_hip_tuner_* symbol set (resolve/resolve_impl/get_config/flush, select_latency_v1, g_tuner_poisoned, etc.) -- confirms the detection method actually finds 0110 code when present, ruling out a false-negative search.

No dependency/link-edit check was needed beyond this: 0110 only adds compile definitions and conditionally-included sources, no separate link-library edits (GPT confirmed this reading of 0110's patch.py).

GPT final sign-off (req_9b4940ffa26d4f06, same session): "PA27 is satisfied and can close. No material gap remains," with an explicit clause-by-clause mapping (four build modes PASS on the four build_plan_ids above; generated-replay-input use PASS; no-tune/record-code clause PASS via the object/symbol evidence above; inspection-validity PASS via the negative control; no-tuner/record dependencies PASS since 0110 adds no link edits; "still resolving and replaying winners" PASS by reuse of PA30's existing Brutus receipt, no rerun needed).

Offline suite (tools/tests/campaign+patch+tuning) run on Brutus after the builds as an extra sanity check (not requested by GPT, not required for closure): 55 failed / 2897 passed / 33 errors, mostly in tools/tests/tuning (test_rd54_mmvq_narrow_moe_coverage, test_rd09_q81_cache_foundation, test_variant_params, test_split_reduce_telemetry). These are pre-existing repo state -- this pass made zero source-code changes (only ran builds and removed the unrelated stray `.rocprofv3/` directory) -- not a regression caused by this work, and not a PA27 blocker per GPT's scoped sign-off. Flagging for a separate follow-up, not investigated further here.

PA27 acceptance criteria are now fully met: independently selectable 0100/0110 packages (steps 1-5), aggregate-behavior preservation (steps 1-5 cmake -P evidence), dead SQLite removal (steps 1-5), four build modes build under the normal path (this pass), and serving replay builds contain no tune/record-only code (this pass, object+symbol evidence with negative control).

## Progress (2026-09-14)

Steps 1-5 done, committed and pushed (d9b4f235, origin+brutus): mechanically split 0100_cmake_options into a narrowed serving/replay package (0100) and a new patches/0110_campaign_tune_record_build campaign package, per dev-gpt-agent design (req_42717536998244ea). GGML_HIP_AUTOTUNE_SQLITE confirmed dead (declared, never referenced, no link edit) and removed. Cross-package integrity is fail-closed at CMake configure time via two top-level sentinels (_BC_HIP_SERVING_BUILD_PLUMBING / _BC_HIP_CAMPAIGN_BUILD_PLUMBING) rather than a patch `requires` dependency, per PA28's future serving-core/campaign-support composition needs. Two rounds of dev-gpt-agent adversarial review (req_4c330960a8db450f, req_67cf86a080444c1e) found and fixed real gaps: a sentinel set in the wrong CMake file (HIP backend subdirectory processes AFTER top-level validation, so a sentinel set only there doesn't exist yet when needed), a reciprocal foreign-option fail-closed hole (undeclared -D flags could partially activate one package's guard without the other's plumbing), and a broken validation-adapter dependency (0100's old coverage-source-selection check directly referenced HIP_BACKEND_PATCH.edits[1:], which became empty once narrowed) -- moved that whole check to 0110 (renamed campaign-source-selection) and added a new serving-source-selection check to 0100. Added 0110 to config/recipes.toml's framework patch-set.

Verified via real cmake -P execution (not hand-set variables, against the real pristine source at the pinned commit 28ff0958291c/b10901) across all 5 canonical build profiles GPT specified (control: AUTOTUNE=ON; record: AUTOTUNE=ON+AUTOTUNE_RECORD=ON; audit: AUTOTUNE=ON+variant-set=full-max; replay: DISPATCH_REPLAY=ON; tune: AUTOTUNE=ON+WORKSPACE_METRICS=ON+variant-set=workload-max+signature-file) -- both packages' backend definitions AND top-level validation blocks pass for every profile. Guard re-ownership verified explicitly: both packages report genuine `applied` (not falsely `already-applied`) for their own edits in both application orders. Apply/idempotence/order-independence hold in both directions.

**Step 6 (real hardware builds on Brutus) NOT started** -- that is separate, larger work: build stock/replay/record/tune on real hardware, verify a serving build has no tuner/record-only symbols/dependencies while still resolving and replaying winners correctly. PA27 stays open until that lands.

## Change Log

- 2026-09-11: created.
- 2026-09-14: corrected the obsolete SQLite premise and rebased the split onto the actual current 0100 CMake/source graph.

## Ledger-events


- chg_20260914_003255_fixed-the-patch-system-plans_2121
- 2026-09-14T00:33:06.878759+00:00 (updated-by): Updated: section:ledger-events
- chg_20260914_124046_split-the-hip-build-options-cm_7114
- 2026-09-14T12:40:49.213841+00:00 (updated-by): Updated: section:ledger-events
- 2026-09-14T12:41:11.403266+00:00 (updated-by): Updated: section:notes
- 2026-09-15T16:45:47.451115+00:00 (updated-by): Updated: section:notes
- chg_20260915_164601_verified-that-bigcherrys-serv_3306
- 2026-09-15T16:46:04.647126+00:00 (updated-by): Updated: section:ledger-events
- 2026-09-15T16:46:09.380045+00:00 (state-transition): State: pending → completed
