---
id: PA14
order: 0
plan: patch-system
state: completed
created-at: '2026-08-26T09:34:23.410507+00:00'
breadth: ''
skill: advanced
created-by: agent
work: L
---

# PA10 residual: independently authoritative source output binding (deferred from PA10)

## Description

reviewer-gpt-agent's adversarial follow-up (2026-08-26) confirmed real via a constructed test (xfail: test_tampered_tree_with_fully_self_consistent_forged_manifest_rejected in test_patch_source_isolation.py): HI82's _verify_reuse() compares every live-recomputed fact (actual_head, actual_tree, recomputed source_slice_id) against a field in the SAME mutable manifest.json sidecar. A tamperer who edits the worktree AND updates patched_tree/source_tree_oid/source_slice_id consistently (all three, not just one or two) still passes reuse -- no manifest field is independently authoritative. atomic_write_json(read_only=True) was added as real but partial defense-in-depth (chmod 0o444 after write) -- stops accidental/casual mutation but not a determined tamperer who chmods the file back first.

## Steps

Two reviewer-suggested approaches, pick one:
(a) Write-once audit ledger recorded OUTSIDE the same writable directory the tamperer already has filesystem access to (e.g. a separate append-only log location with stricter permissions, or a location the patch-validation campaign's own execution context controls but the materialized worktree's writer does not).
(b) Deterministic re-materialization comparison on reuse: re-run _add_worktree() + _apply_composition() into a scratch location and diff tree hashes against the live one, instead of trusting any manifest field. Expensive (redoes the whole apply) but fully authoritative -- doesn't rely on ANY value surviving intact from the original write.
Either way: remove the @unittest.expectedFailure decorator from the xfail test once genuinely fixed, and confirm it passes for real.

## Detailed Solution & Technical Design



## Code Samples & Guidance



## Files

tools/bigcherry/patch/source.py, tools/bigcherry/source/identity.py, tools/tests/patch/test_patch_source_isolation.py

## Validation

test_tampered_tree_with_fully_self_consistent_forged_manifest_rejected passes without the expectedFailure decorator; all other PA10 tests continue to pass.

## Effort & Risk

L, same architectural caliber as HI121's own C++-digest-authority gap (HI125) -- this is a genuine unsolved trust-boundary problem, not a quick fix. Option (b) is more tractable to implement correctly; option (a) needs a real answer for where an attacker-inaccessible ledger location would live in this project's actual deployment model.

## Standards



## Acceptance Criteria



## Notes

Current status (2026-08-26): PA14 is completed via deterministic scratch re-materialization in `tools/bigcherry/patch/source.py`. The fully self-consistent forged-manifest regression passes without `expectedFailure`. Source-isolation ran 46 tests with 0 expected failures and 2 Windows capability skips; the full patch domain ran 298 tests with 4 capability skips; compileall passed. Boundary validation has one unrelated Windows Store-alias launcher error in `test_lab_template_runner_requires_copy`.

## Historical Notes

PA10 was reopened to in_progress rather than left completed because of this. Do not close PA10 again until this item is resolved or a documented decision is made to accept the residual risk.

RESOLVED (2026-08-26). Implemented approach (b) deterministic re-materialization, per design guidance from dev-gpt-agent (which correctly reasoned approach (a) has no real trust boundary in this project's deployment model -- a separate ledger location would still be writable by the same execution identity). Added _verify_by_rematerialization() to patch/source.py: after every existing cheap manifest-field check in _verify_reuse() passes, re-runs _add_worktree() + _apply_composition() + variant_transform.apply() into a throwaway scratch worktree and compares its tree OID against the live one -- this does not depend on trusting ANY value previously written to disk. Threaded base_repo/resolved_revision/composition/overlay_root/variant_transform through _materialize_v2()'s call to _verify_reuse(); parameters are optional so existing narrower unit-level tests of _verify_reuse() itself are unaffected. The @unittest.expectedFailure decorator on test_tampered_tree_with_fully_self_consistent_forged_manifest_rejected was removed -- it now passes for real. Full offline suite: 2160 passed, 0 failed, 0 xfailed (down from 2 xfailed).

PA10 was reopened to in_progress rather than left completed because of this. Do not close PA10 again until this item is resolved or a documented decision is made to accept the residual risk.

RESOLVED (2026-08-26). Implemented approach (b) deterministic re-materialization, per design guidance from dev-gpt-agent (which correctly reasoned approach (a) has no real trust boundary in this project's deployment model -- a separate ledger location would still be writable by the same execution identity). Added _verify_by_rematerialization() to patch/source.py: after every existing cheap manifest-field check in _verify_reuse() passes, re-runs _add_worktree() + _apply_composition() + variant_transform.apply() into a throwaway scratch worktree and compares its tree OID against the live one -- this does not depend on trusting ANY value previously written to disk. Threaded base_repo/resolved_revision/composition/overlay_root/variant_transform through _materialize_v2()'s call to _verify_reuse(); parameters are optional so existing narrower unit-level tests of _verify_reuse() itself are unaffected. The @unittest.expectedFailure decorator on test_tampered_tree_with_fully_self_consistent_forged_manifest_rejected was removed -- it now passes for real. Full offline suite: 2160 passed, 0 failed, 0 xfailed (down from 2 xfailed).

Verification update (2026-08-26): live source-isolation suite ran 46 tests with 0 failures, 0 expected failures, and 2 Windows capability skips; the fully self-consistent forged-manifest regression passes as a normal test. Full patch-domain discovery ran 298 tests with 0 failures and 4 capability skips. `compileall -q tools/bigcherry` passed. Boundary validation was 17 passing with one unrelated Windows Store-alias launcher error in `test_lab_template_runner_requires_copy` (the test invokes non-launchable `sys.executable`); no PA14/PA10 source-isolation regression observed.

PA10 was reopened to in_progress rather than left completed because of this. Do not close PA10 again until this item is resolved or a documented decision is made to accept the residual risk.

RESOLVED (2026-08-26). Implemented approach (b) deterministic re-materialization, per design guidance from dev-gpt-agent (which correctly reasoned approach (a) has no real trust boundary in this project's deployment model -- a separate ledger location would still be writable by the same execution identity). Added _verify_by_rematerialization() to patch/source.py: after every existing cheap manifest-field check in _verify_reuse() passes, re-runs _add_worktree() + _apply_composition() + variant_transform.apply() into a throwaway scratch worktree and compares its tree OID against the live one -- this does not depend on trusting ANY value previously written to disk. Threaded base_repo/resolved_revision/composition/overlay_root/variant_transform through _materialize_v2()'s call to _verify_reuse(); parameters are optional so existing narrower unit-level tests of _verify_reuse() itself are unaffected. The @unittest.expectedFailure decorator on test_tampered_tree_with_fully_self_consistent_forged_manifest_rejected was removed -- it now passes for real. Full offline suite: 2160 passed, 0 failed, 0 xfailed (down from 2 xfailed).

Verification update (2026-08-26): live source-isolation suite ran 46 tests with 0 failures, 0 expected failures, and 2 Windows capability skips; the fully self-consistent forged-manifest regression passes as a normal test. Full patch-domain discovery ran 298 tests with 0 failures and 4 capability skips. `compileall -q tools/bigcherry` passed. Boundary validation was 17 passing with one unrelated Windows Store-alias launcher error in `test_lab_template_runner_requires_copy` (the test invokes non-launchable `sys.executable`); no PA14/PA10 source-isolation regression observed.

PA10 was reopened to in_progress rather than left completed because of this. Do not close PA10 again until this item is resolved or a documented decision is made to accept the residual risk.

RESOLVED (2026-08-26). Implemented approach (b) deterministic re-materialization, per design guidance from dev-gpt-agent (which correctly reasoned approach (a) has no real trust boundary in this project's deployment model -- a separate ledger location would still be writable by the same execution identity). Added _verify_by_rematerialization() to patch/source.py: after every existing cheap manifest-field check in _verify_reuse() passes, re-runs _add_worktree() + _apply_composition() + variant_transform.apply() into a throwaway scratch worktree and compares its tree OID against the live one -- this does not depend on trusting ANY value previously written to disk. Threaded base_repo/resolved_revision/composition/overlay_root/variant_transform through _materialize_v2()'s call to _verify_reuse(); parameters are optional so existing narrower unit-level tests of _verify_reuse() itself are unaffected. The @unittest.expectedFailure decorator on test_tampered_tree_with_fully_self_consistent_forged_manifest_rejected was removed -- it now passes for real. Full offline suite: 2160 passed, 0 failed, 0 xfailed (down from 2 xfailed).

PA10 was reopened to in_progress rather than left completed because of this. Do not close PA10 again until this item is resolved or a documented decision is made to accept the residual risk.

RESOLVED (2026-08-26). Implemented approach (b) deterministic re-materialization, per design guidance from dev-gpt-agent (which correctly reasoned approach (a) has no real trust boundary in this project's deployment model -- a separate ledger location would still be writable by the same execution identity). Added _verify_by_rematerialization() to patch/source.py: after every existing cheap manifest-field check in _verify_reuse() passes, re-runs _add_worktree() + _apply_composition() + variant_transform.apply() into a throwaway scratch worktree and compares its tree OID against the live one -- this does not depend on trusting ANY value previously written to disk. Threaded base_repo/resolved_revision/composition/overlay_root/variant_transform through _materialize_v2()'s call to _verify_reuse(); parameters are optional so existing narrower unit-level tests of _verify_reuse() itself are unaffected. The @unittest.expectedFailure decorator on test_tampered_tree_with_fully_self_consistent_forged_manifest_rejected was removed -- it now passes for real. Full offline suite: 2160 passed, 0 failed, 0 xfailed (down from 2 xfailed).

Verification update (2026-08-26): live source-isolation suite ran 46 tests with 0 failures, 0 expected failures, and 2 Windows capability skips; the fully self-consistent forged-manifest regression passes as a normal test. Full patch-domain discovery ran 298 tests with 0 failures and 4 capability skips. `compileall -q tools/bigcherry` passed. Boundary validation was 17 passing with one unrelated Windows Store-alias launcher error in `test_lab_template_runner_requires_copy` (the test invokes non-launchable `sys.executable`); no PA14/PA10 source-isolation regression observed.

Verification update (2026-08-26): live source-isolation suite ran 46 tests with 0 failures, 0 expected failures, and 2 Windows capability skips; the fully self-consistent forged-manifest regression passes as a normal test. Full patch-domain discovery ran 298 tests with 0 failures and 4 capability skips. `compileall -q tools/bigcherry` passed. Boundary validation was 17 passing with one unrelated Windows Store-alias launcher error in `test_lab_template_runner_requires_copy` (the test invokes non-launchable `sys.executable`); no PA14/PA10 source-isolation regression observed.

## Change Log

- 2026-08-26T09:34:23.410507+00:00 (created-by): Created by agent
- 2026-08-26T10:01:57.798679+00:00 (updated-by): Updated: section:notes
- 2026-08-26T10:01:59.942713+00:00 (state-transition): State: pending → completed

## Ledger-events

- chg_20260826_100214_hi82-patch-source-isolation-re_1003
- 2026-08-26T10:02:14.993771+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-26T10:08:24.843394+00:00 (updated-by): Updated (no visible changes)
- 2026-08-26T10:08:42.209510+00:00 (updated-by): Updated: section:notes
- chg_20260826_100849_verified-pa14pa10-authoritati_8960
- 2026-08-26T10:08:49.806737+00:00 (updated-by): Updated: section:ledger-events
- 2026-08-26T10:09:13.865529+00:00 (updated-by): Updated: section:notes
- 2026-08-26T10:09:48.739349+00:00 (updated-by): Updated: section:notes
