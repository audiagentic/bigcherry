---
id: RV81
review-of: PA02
plan: patch-system
state: closed
reviewed-by: dev-gpt-agent (gateway)
reviewed-at: ''
---

# GPT deep repo review of B1/B3/B4/B5 (GitHub @526532f): PASS with 3 corrections — ordered-composition identity, campaign_resolution topological hole, describe() lenient reporting

## Notes

Verification: test_recipes (describe lenient) + test_campaign_resolution (topo fix, live behavior unchanged) + test_patch_registry + test_patch_catalog + test_focal_source_plans + test_patch_source_isolation + test_patch_validation = 151 passed. B6+B2 remain as the next PA02 slice (one coupled PSI/source-identity slice incl. focal CONTROL+SUBJECT resolve_exact + manifest symlink safety), then RV80 acceptance cases + PA02-clean suite. Leave amalgamated RS07/RS08 untouched (RS08 = out-of-scope PA03 known-red, documented in PA02 DoD). Provenance: dev-gpt-agent, session ses_864c3943901a4182, request req_91a8b9c263094d63, reviewed GitHub branch tuning-code-rebase @ 526532f.

## Findings

B1 canonical contract hash: PASS. B3 loader digest re-check: PASS. B5 packaged import AST validation: PASS. B4 topological_order in patchset: PASS. Defect 1 (HIGH): §14.3/RV80 'sorted composition' identity flaw → use ordered resolved composition. Defect 2 (HIGH/systemic): campaign_resolution.resolve_lane() global sort → fixed via topological_order in both merge sites. Defect 3 (LOW/doc): §14.2 version-constant location contradiction → corrected. describe() ruling: (b) narrow lenient reporting path, no general lenient flag.

## Conclusion

PASS. B1, B3, B4, B5 all pass the deep repo review (314 provider events against the live GitHub tree). Three corrections were made and incorporated (all landed): (1) HIGH — the source-identity composition must hash the ORDERED exact composition from resolve_exact(), never a lexicographic sort (runbook §14.3 + RV80 wording corrected); (2) HIGH/systemic — campaign_resolution.resolve_lane() had the same global (order,patch_id) re-sort as B4, reintroducing the bug one layer above patchset (both the multi-set merge and the experiment merge now use patchset.topological_order); (3) LOW — §14.2 said VALIDATION_FRAMEWORK_VERSION lives in patch_validation.py, contradicting A2 (corrected to patch_registry, re-exported). Ruling (b) for describe(): a narrow report-only lenient metadata path (patch_registry.describe_all + validate_state toggle) preserves a malformed lifecycle state (state_valid=False) instead of raising, keeping load_registry/catalog/resolve/materialization strictly fail-closed. B6→B2 design confirmed (with the ordered-composition correction).
