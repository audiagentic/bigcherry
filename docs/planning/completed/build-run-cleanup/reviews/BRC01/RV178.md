---
id: RV178
review-of: BRC01
plan: build-run-cleanup
state: closed
reviewed-by: dev-gpt-agent via gateway
reviewed-at: '2026-09-12T20:54:30.651834Z'
---

# GPT validation of binding-aware backedge follow-up

## Notes



## Findings

Validated c922985d (request req_78d98e448a7a4f7a, same session ses_76206cac3e6b4be0) after the prior BRC01 follow-ups.

Required correction before BRC01 closure:
- check.py::_cli_main_backedge_lines function_bindings currently always starts from module_bindings, so nested functions/lambdas do not inherit enclosing function bindings. This can false-positive when an outer function shadows importlib and false-negative when an outer function imports an alias that an inner function closes over.
- Class bodies currently start from only class-local bindings, so a class body using a module-level importlib binding is missed. Methods must not inherit class locals.
- Propagate enclosing function bindings; resolve global from module scope and nonlocal from the enclosing function scope; use module globals as class-body fallback and a module parent for methods. Add regression tests for nested false-positive/false-negative and class-body module-global lookup.

Previously required apply, pin-bump, tuning, alias, relative-import, static-import, false-positive, and no-regression coverage was otherwise satisfied. No runtime ownership or import-cycle blocker was found. Optional: move apply tests to a dedicated CLI test module.

## Conclusion

PASS WITH REQUIRED FOLLOW-UPS: correct lexical binding propagation and add nested/class regression coverage; no ownership rollback is indicated.
