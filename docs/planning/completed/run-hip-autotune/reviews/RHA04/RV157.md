---
id: RV157
review-of: RHA04
plan: run-hip-autotune
state: closed
reviewed-by: codex
reviewed-at: '2026-09-10T15:02:00+00:00'
---

# Required-attestation capture path lacks physical evidence at production verbosity

## Notes

Evidence: docs/evidence/2026-09-10-rha04-gpu0-required-capture/README.md. Keep RHA04 in_progress; do not rerun the full matrix until the attestation design is resolved.

## Findings

A fresh GPU0 9B native cell loaded and shut down cleanly but produced ATTESTATION_MISSING; no server-bench request or throughput metric ran. The maintained capture path uses ServerRunner directly, while the separate AttestedServerSession forces higher verbosity. Relaxing execution_evidence to observe would violate RHA04's decision-grade gate. A solution must preserve diagnostics-off timing separately from diagnostic/process attestation and must not infer identity from visibility variables alone.

## Conclusion

The first fresh required-attestation GPU0 cell failed closed before measurement because run_server_arm_capture() did not emit parseable physical-device evidence for the production-shaped -sm none/--fit off launch. This confirms an implementation gap, not a performance result.
