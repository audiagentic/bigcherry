# Current Release

## Changes

### audit
- [chg_20260805_012059_validated-hip-autotune-code-ag_8663] Validated HIP autotune code against docs: 11 items confirmed completed, 2 in progress
- [chg_20260805_053447_validated-the-tuning-fixes-on_5741] Validated the tuning fixes on a second GPU architecture (RDNA4), where a full sweep now completes cleanly with no errors. Identified that roughly a third of the generated kernel candidates target datacentre-class AMD hardware and are correctly skipped on consumer cards, but are currently reported in a misleading way.
- [chg_20260805_055250_measured-which-kernel-selectio_7483] Measured which kernel-selection strategies actually pay off. Most of the gain comes from choosing among parameters the upstream library already compiles, rather than from the newly generated kernel variants - a result that redirects where further optimisation effort should go.
- [chg_20260805_055537_measured-kernel-tuning-on-both_3096] Measured kernel tuning on both GPU generations and found the benefit is strongly architecture-dependent: the generated kernel variants deliver a 16% median speed-up on the target hardware while being nearly worthless on the newer generation. A single variant that was previously impossible to select accounts for most of that gain.
- [chg_20260805_064040_tuned-the-real-production-work_7747] Tuned the real production workload for the first time and found that 79% of matrix operations run faster with a tuned kernel variant, with the best gains of 14-20% concentrated exactly where predicted. The results cannot yet be saved and reused, which is now the most valuable remaining work.
- [chg_20260805_115126_found-and-fixed-a-build-settin_8608] Found and fixed a build setting that was halving multi-GPU performance, restoring prefill throughput to match the production configuration. Also established that the long-context workload exercises 51% more distinct operations than the short-context tests everything had been tuned against.
- [chg_20260805_123239_tested-whether-a-compile-time_6071] Tested whether a compile-time flag that prefers quantised matrix-multiply kernels improves generation speed. It makes no difference on this hardware because those kernels are already the default choice for this model format, and the automatic tuning covers the same decision with per-shape measurements.
- [chg_20260805_125347_discovered-that-automatic-tuni_4430] Discovered that automatic tuning had been measuring only a fifth of the work the model actually does, because the test workload did not produce the shapes that dominate real use. After correcting it, tuning cuts matrix-multiplication time by 10 percent, with individual operations up to three times faster than the built-in choice.
- [chg_20260805_131114_measured-the-finished-system-e_8962] Measured the finished system end to end: automatically tuned kernel selection makes prompt processing 1.7 to 4.4 percent faster than the built-in defaults, verified across interleaved runs with no overlap between the two configurations. Generation speed is unchanged.
- [chg_20260805_132816_investigated-an-apparent-flaw_9364] Investigated an apparent flaw in kernel selection and found the selection logic correct; the real issue was that the quick screening pass used too few timing samples to rank candidates reliably. Correcting it confirmed the diagnosis but changed overall performance by a negligible amount.

### code-fix
- [chg_20260805_050646_fixed-a-dispatch-bug-where-the_5680] Fixed a dispatch bug where the autotuner could measure a kernel variant built for one quantisation type against a matrix of a different type, producing either a crash or a recorded winner that misnamed what actually ran. The MMVQ explicit-geometry path is now verified on AMD gfx1100 hardware, where several tuned geometries measurably beat the built-in defaults.
- [chg_20260805_051702_fixed-a-crash-where-the-autotu_9615] Fixed a crash where the autotuner could route a matrix multiplication to a kernel family that does not implement that quantisation format, aborting the run. Tuning sweeps now complete across every quantised format on AMD gfx1100.
- [chg_20260805_052432_the-autotuner-now-completes-a_8459] The autotuner now completes a full matrix-multiplication tuning sweep on AMD gfx1100 hardware. Fixed three further crashes: kernel variants being selected on hardware that cannot run them, an unchecked requirement that activations be 32-bit floats, and the tuner attempting to take timings inside a GPU graph capture.
- [chg_20260805_054935_kernel-variants-that-require-d_7547] Kernel variants that require datacentre-class AMD hardware are now correctly identified as unsupported on consumer cards up front, rather than being re-examined for every matrix multiplication and reported under a misleading reason.
- [chg_20260805_070912_fixed-the-saved-results-format_5368] Fixed the saved-results format so the best-performing kernel selections can actually be reloaded; previously the two settings that matter most to the fastest variants were silently dropped, which would have caused crashes or a quiet fallback to default behaviour.
- [chg_20260805_102857_tuning-results-now-record-enou_6430] Tuning results now record enough information to be reused across rebuilds. Also corrected two earlier verification claims: a slimmed production build silently discards all tuning because the lookup key includes the build's own identity, and an earlier no-gaps result had not actually enabled gap reporting.
- [chg_20260805_103856_tuning-results-now-survive-a-r_4272] Tuning results now survive a rebuild. Previously any change to the kernel catalogue or an upstream update silently discarded every tuned result; they are now reused, and flagged as possibly out of date when the build has changed underneath them.
- [chg_20260805_105855_validated-kernel-dispatch-acro_8076] Validated kernel dispatch across single and multiple GPUs, three GPU generations, tensor splitting and GPU graph capture. Mixing different GPU generations in one tensor split crashes in the upstream library itself, independently of this work.
- [chg_20260806_115425_added-a-safe-opt-in-shutdown-e_5671] Added a safe opt-in shutdown endpoint so Windows benchmark runs can flush tuning data instead of losing measurements on process termination.
- [chg_20260806_234348_fixed-the-missing-gemma-signat_2101] Fixed the missing Gemma signature import path: new tuning runs preserve shapes in measurements, and existing databases can be enriched from diagnostic JSONL sources.

### docs
- [chg_20260805_042913_updated-plans-with-pack_review_7308] Updated plans with PACK_REVIEW action items from reference docs
- [chg_20260805_044652_documented-pack_review-action_7865] Documented PACK_REVIEW action item incorporation in plan items
- [chg_20260805_062819_added-a-generated-reference-do_8675] Added a generated reference document cataloguing every kernel candidate alongside how it actually performed on each GPU tested, so future work can check what exists, what can run where, and what has ever been measured.
- [chg_20260805_063528_established-a-working-pipeline_5467] Established a working pipeline for capturing real workload profiles from a running inference server with speculative decoding enabled, and documented how to build, test and measure the project on every supported configuration.
- [chg_20260805_132027_published-a-detailed-per-opera_3360] Published a detailed per-operation tuning report showing which kernel was selected for each of the busiest operations and how close the alternatives were, along with three concrete opportunities for further improvement.
- [chg_20260805_144103_planned-two-new-optimisation-p_1922] Planned two new optimisation projects for gfx1100: exact-shape WMMA kernel (HI25) and Q8_1 activation reuse with K-tile tuning (HI26). Strategy inverted based on hipEngine's 44% regression — start from proven LDS decomposition, not green-field WMMA.
- [chg_20260806_110150_updated-the-tuning-skills-so-f_2646] Updated the tuning skills so future Windows Gemma runs use the working command and validate generated measurements before accepting results.
- [chg_20260806_110328_made-the-tuning-instructions-r_4570] Made the tuning instructions reusable for any GGUF model family instead of naming a specific model.
- [chg_20260806_111034_added-a-reusable-skill-for-mea_6464] Added a reusable skill for measuring before-versus-after tuning performance without model-family-specific assumptions.
- [chg_20260806_232012_updated-the-handoff-document-w_4137] Updated the handoff document with the latest build, replay evidence, benchmark comparison, and next steps.
- [chg_20260806_234919_expanded-the-hip-autotune-plan_1008] Expanded the HIP autotune plan with implementation-level detail for improving tuning quality: statistical confidence in winner selection, measurement-effort budgeting by signature impact, measurement fidelity, an offline model for what tuning is worth, and generalising winners across similar signatures.
- [chg_20260807_045127_expanded-four-planned-autotune_8630] Expanded four planned autotune work items (adaptive screening, measurement fidelity, impact modelling, winner generalisation) from designs into implementation-ready specifications with verified, paste-ready code.
- [chg_20260807_053047_specified-how-tuned-results-su_2253] Specified how tuned results survive llama.cpp upgrades with per-release history, and added a plan item for recording which workload and machine settings each tune was measured under.

### feature
- [chg_20260805_071838_completed-the-pipeline-that-ca_3350] Completed the pipeline that carries tuning results into a running server: measured kernel choices can now be saved and reloaded, verified with every matrix operation served from the saved selections and no gaps. Overall throughput was unchanged in this test, so the per-operation gains have not yet been shown to translate into faster inference.
- [chg_20260805_095403_added-the-ability-to-build-a-p_8974] Added the ability to build a production binary containing only the kernel variants a tuning run actually selected, cutting the candidate set by 81 percent. Also quantified the benefit of tuning: matrix multiplication time drops by about 7.5 percent, which is too small to observe through the current end-to-end benchmark.
- [chg_20260805_105221_the-coverage-report-now-states_3432] The coverage report now states whether saved tuning results were actually used and whether they are out of date, rather than only reporting that every operation went through the dispatch layer - two things that look identical in the previous output.
- [chg_20260806_130126_added-optional-per-cache-entry_5670] Added optional per-cache-entry replay diagnostics with no production runtime overhead, then used them to verify which tuned candidates actually ran.
- [chg_20260807_000820_the-hip-autotune-engine-now-re_7665] The HIP autotune engine now requires statistical confidence (not just a faster median) before replacing native, ranks candidates on whichever of GPU or host-submission time actually binds, and rejects noisy or non-deterministic candidates instead of silently keeping them as winners.

### tests
- [chg_20260805_105030_added-an-automated-check-that_5333] Added an automated check that catches a class of bug which previously required hours of hardware testing to find, and which had twice caused the fastest kernel variants to be silently unreachable.
