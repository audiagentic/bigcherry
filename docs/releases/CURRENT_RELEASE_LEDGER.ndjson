{"change-class": "audit", "event-id": "chg_20260805_012059_validated-hip-autotune-code-ag_8663", "files": ["docs/planning/completed/hip-autotune/HI01.md", "docs/planning/completed/hip-autotune/HI02.md", "docs/planning/completed/hip-autotune/HI03.md", "docs/planning/completed/hip-autotune/HI04.md", "docs/planning/completed/hip-autotune/HI05.md", "docs/planning/completed/hip-autotune/HI06.md", "docs/planning/completed/hip-autotune/HI07.md", "docs/planning/completed/hip-autotune/HI08.md", "docs/planning/completed/hip-autotune/HI10.md", "docs/planning/completed/hip-autotune/HI11.md", "docs/planning/completed/hip-autotune/HI13.md", "docs/planning/active/hip-autotune/HI09.md", "docs/planning/active/hip-autotune/HI12.md", "docs/reference/OVERVIEW.md", "docs/reference/PACK_REVIEW.md", "docs/reference/FAMILY_MODEL.md", "docs/reference/COVERAGE_AUDIT.md"], "plan-item-ids": ["HI01", "HI02", "HI03", "HI04", "HI05", "HI06", "HI07", "HI08", "HI09", "HI10", "HI11", "HI12", "HI13"], "status": "unreleased", "technical-summary": "Validated all HIP autotune source code against HANDOFF.md and planning documentation claims. Confirmed: HI01 (32/32 invariants, baseline build), HI02 (5 CMake rejection rules, ABI types), HI03 (catalog generator, MMQ config parsing), HI04 (native selection pure function, process cache, mode switching), HI05 (RFC 7693 BLAKE2b, signature/hardware key structs), HI06-HI08 (forced-variant dispatch with same-launcher principle for MMQ/MMVF/MMF), HI10 (JSONL record mode \u2014 major deviation from SQLite plan), HI11 (compact binary cache with BLAKE2b integrity), HI13 (coverage counters, fused collection points). Set in_progress: HI09 (kernels written, routing not yet compiled), HI12 (tuner written and compiles, no completed run yet). Moved free-floating reference docs (PACK_REVIEW, FAMILY_MODEL, COVERAGE_AUDIT, OVERVIEW) from planning/active to docs/reference.", "timestamp-utc": "2026-08-05T01:20:59.210617Z", "user-summary-candidate": "Validated HIP autotune code against docs: 11 items confirmed completed, 2 in progress"}
{"change-class": "docs", "event-id": "chg_20260805_042913_updated-plans-with-pack_review_7308", "files": ["patches/0100_cmake_options.py", "docs/planning/active/hip-autotune/HI12.md", "docs/planning/completed/hip-autotune/HI13.md", "docs/planning/completed/hip-autotune/HI11.md", "docs/planning/completed/hip-autotune/HI05.md", "docs/planning/completed/hip-autotune/HI15.md", "docs/planning/active/hip-autotune/HI09.md"], "plan-item-ids": ["HI05", "HI09", "HI09b", "HI11", "HI12", "HI13", "HI15"], "status": "unreleased", "technical-summary": "Updated plan items to reflect all PACK_REVIEW action items from reference docs. B1 small_k+fusion resolved (HI09). B2 screening retention added to HI12. B3 MAD/host time/winner reason schema prework added to HI12. B4 collection point 5 verified as covered by patch 0700 (HI13). B5 bounded-width array cache noted in HI11. B6 resource blacklist captured in HI09b. B7 export/inspect tools noted in HI15. B8 restart-only reload noted in HI11/HI15. B9 divisibility refinement noted in HI05. B10 variant-set uppercase aliases implemented in CMake patch.", "timestamp-utc": "2026-08-05T04:29:13.817711Z", "user-summary-candidate": "Updated plans with PACK_REVIEW action items from reference docs"}
{"change-class": "docs", "event-id": "chg_20260805_044652_documented-pack_review-action_7865", "files": ["docs/reference/HANDOFF.md"], "status": "unreleased", "technical-summary": "Added PACK_REVIEW action items cross-reference table to HANDOFF.md, mapping all A-series, B-series, and C5 actions to their owning plan items with status (resolved/noted/fixed)", "timestamp-utc": "2026-08-05T04:46:52.629493Z", "user-summary-candidate": "Documented PACK_REVIEW action item incorporation in plan items"}
{"change-class": "code-fix", "event-id": "chg_20260805_050646_fixed-a-dispatch-bug-where-the_5680", "files": ["src/ggml/src/ggml-cuda/hip-autotune-types.h", "src/ggml/src/ggml-cuda/hip-autotune-dispatch.cu", "tools/bigcherry/autotune_catalog.py", "docs/reference/HANDOFF.md", "docs/planning/active/hip-autotune/HI09.md", "docs/planning/active/hip-autotune/HI12.md"], "plan-item-ids": ["HI09", "HI12"], "status": "unreleased", "technical-summary": "Enforce candidate src0 type in dispatch eligibility across all four matmul families, and verify the HI09 MMVQ forced-geometry routing on gfx1100 hardware for the first time.\n\nEvery generated candidate names one src0 type in its stable name (mmvq:q8_0:, mmq:q8_0:, mmf:f32:), but the type never reached ggml_hip_variant_params, so no can_execute predicate could check it. Because every family's launch path dispatches on the runtime tensor type, candidates were ruled eligible for, measured on, and declared winners of signatures of a different type. The per-family _is_eligible helpers did not catch it: they are asked whether the signature's type is servable, never whether it is the type the candidate claims.\n\nThis produced two distinct hardware aborts under GGML_HIP_DISPATCH_MODE=tune: the HI09 part-1 abort at mmvq.cu:972 (no compiled MMVQ instance for type=q4_0, since workload-max from a Q8_0 inventory compiles only q8_0 instances), and NO_DEVICE_CODE at mmq.cuh:98 after an mmq:q8_0:j16:... candidate won an iq1_s signature (MMQ's config table is per type, so a given J names a different kernel configuration per type). MMF/MMVF did not crash only because upstream instantiates every type, which instead silently misattributed winners.\n\nFix: ggml_hip_variant_params gains uint8_t src0_type, taking the last reserved byte so the struct size is unchanged (the precedent small_k set). _variant_initialiser emits it as the enumerator rather than its value so an upstream renumbering cannot silently repoint candidates; BLAS stays 0 as it has no per-type identity. All four can_execute predicates reject a type mismatch. Replay caches need no migration as they validate the manifest hash, which this alters.\n\nAlso corrected HANDOFF.md, which claimed /vault was not mounted on brutus and instructed a tar sync into ~/bigcherry. /mnt/vault is mounted and J:\\development\\llmhosts\\bigcherry is the same directory over SMB; ~/bigcherry is a stale pre-HI09 copy at upstream 6ea215d that the sync instruction would have silently built.\n\nVerified on gfx1100 (ROCm 7.2): builds and links clean; nm confirms ggml_hip_mmvq_forced threaded through the mangled native chain and the geometry instances instantiated; test-backend-ops -o MUL_MAT passes on the native path; under tune mode 198 signatures tune with 0 failures and six forced MMVQ geometries beat native. Audit 32/32, patcher tests 12/12.\n\nOne defect remains open (review RV02 on HI12): ggml_hip_family_can_serve gates MMQ on ggml_is_quantized, which is broader than upstream's supported set, so a full MUL_MAT sweep still aborts on iq1_m. It does not affect the Q8_0 target workload.", "timestamp-utc": "2026-08-05T05:06:46.249217Z", "user-summary-candidate": "Fixed a dispatch bug where the autotuner could measure a kernel variant built for one quantisation type against a matrix of a different type, producing either a crash or a recorded winner that misnamed what actually ran. The MMVQ explicit-geometry path is now verified on AMD gfx1100 hardware, where several tuned geometries measurably beat the built-in defaults."}
{"change-class": "code-fix", "event-id": "chg_20260805_051702_fixed-a-crash-where-the-autotu_9615", "files": ["patches/0300_mmq_forced_j.py", "src/ggml/src/ggml-cuda/hip-autotune-dispatch.cuh", "src/ggml/src/ggml-cuda/hip-autotune-dispatch.cu", "docs/planning/active/hip-autotune/reviews/HI12/RV03.md"], "plan-item-ids": ["HI12"], "status": "unreleased", "technical-summary": "Gate MMQ dispatch eligibility on types MMQ actually implements, derived from upstream's per-architecture config tables (RV02).\n\nggml_hip_family_can_serve gated MMQ on ggml_is_quantized, which is broader than the set MMQ implements: IQ1_M, TQ1_0, TQ2_0, Q8_1 and Q8_K are quantised but unsupported. Because ggml_hip_mmq_can_execute accepts the native wrapper before every other check, such a type reached mmq_get_q8_1_ds_layout and hit its default GGML_ABORT (mmq.cuh:98) during a tune-mode sweep, on the first iq1_m case.\n\nAdded ggml_cuda_mmq_type_is_supported(type, cc, shared_mem_limit) via patches/0300_mmq_forced_j.py, declared in hip-autotune-dispatch.cuh. It sweeps J over the same 8..128 bounds ggml_cuda_mmq_config_is_eligible uses and returns true if any (J, fallback) resolves to a real config row. This derives support from upstream's own tables rather than restating its supported-type switch: no list exists to drift when upstream adds or drops a type, and the answer is architecture-aware, so a type present in the switch but absent from this GPU's table is correctly rejected. ggml_cuda_should_use_mmq is deliberately not reused wholesale because it folds in performance heuristics the tuner exists to override.\n\nScope was narrowed during diagnosis: MMVQ is not affected, since ggml_cuda_should_use_mmvq gates on ggml_is_quantized exactly as our predicate did.\n\nVerified on gfx1100 (ROCm 7.2): iq1_m tunes cleanly to mmvq:native:v1 with MMQ correctly excluded from its eligible set, where it previously aborted. The tune sweep advanced from 198 to 243 signatures with 0 test failures and now clears every quantised type before reaching the float half. Audit 32/32, patcher tests 12/12.\n\nA third defect of the same class is recorded open as RV03: ggml_cuda_mmf_variant_is_eligible discards the type entirely (GGML_UNUSED) and validates only geometry, so MMF launches kernels with no device code for the architecture, producing an HSA hardware exception rather than a clean abort.", "timestamp-utc": "2026-08-05T05:17:02.728117Z", "user-summary-candidate": "Fixed a crash where the autotuner could route a matrix multiplication to a kernel family that does not implement that quantisation format, aborting the run. Tuning sweeps now complete across every quantised format on AMD gfx1100."}
{"change-class": "code-fix", "event-id": "chg_20260805_052432_the-autotuner-now-completes-a_8459", "files": ["src/ggml/src/ggml-cuda/hip-autotune-dispatch.cu", "docs/planning/completed/hip-autotune/reviews/HI12/RV03.md", "docs/planning/completed/hip-autotune/reviews/HI12/RV04.md", "docs/planning/completed/hip-autotune/reviews/HI12/RV05.md"], "plan-item-ids": ["HI12", "HI09"], "status": "unreleased", "technical-summary": "Three further dispatch fixes; with these the full test-backend-ops MUL_MAT tune sweep completes on gfx1100 (1151 signatures, 0 failures, exit 0, 2/2 backends passed), up from an immediate abort at the start of the session.\n\nRV03 - MMF eligibility ignored type and architecture. ggml_cuda_mmf_variant_is_eligible discards the type outright (GGML_UNUSED) and validates only nwarps bounds and shared memory. MMF's F32 path requires ampere_mma_available or amd_mfma_available and RDNA3 has neither, so all 81 mmf:f32 candidates were unrunnable on gfx1100 while appearing eligible. Launching one did not abort cleanly: the kernel body compiled to a NO_DEVICE_CODE stub, so it faulted as HSA_STATUS_ERROR_EXCEPTION, took down the HIP queue and left the device unusable. ggml_hip_mmf_can_execute now calls upstream's ggml_cuda_should_use_mmf before the native-wrapper short-circuit, converting the signature's element-normalised strides back to bytes. Called whole rather than split: its alignment checks are crash-prevention and its ncols bounds track upstream's instantiations. Cost is losing ncols 9..16 exploration on RDNA3.0, which reports as ineligible in coverage rather than silently.\n\nRV04 - all four families assert an F32 activation on entry (mmvf.cu:673 and :816, mmf.cu:35, mmvq.cu:1320, mmq.cu:109) and no eligibility predicate checked it. ggml_hip_family_can_serve reasoned only about src0 and shape. Upstream never trips the assertions because it routes a non-F32 src1 to the dense path first; the dispatch layer selects candidates itself and must encode the same precondition. Added one guard requiring sig.src1_type == GGML_TYPE_F32 for every family except BLAS, which is that dense path.\n\nRV05 - the tuner measured inside HIP graph capture. hipEventRecord, synchronisation and hipEventElapsedTime are recorded into a capture rather than executed, and the first invalidates it; the failure surfaced elsewhere as 'operation failed due to a previous error during capture'. Neither dispatch nor tuner had any notion of capture state. ggml_hip_dispatch_resolve now checks hipStreamIsCapturing in the TUNE branch and keeps the native binding when a capture is active, failing closed on a query error. A call_once warning reports that some launches went unmeasured and points at GGML_CUDA_DISABLE_GRAPHS=1.\n\nAll five defects fixed this session share one shape: eligibility predicates that approximate what the build actually instantiated, each wrong in a different direction, each found only by running on hardware. RV03's conclusion records the recommended structural fix - derive eligibility from the instantiation set the build already knows - and suggests HI16 add a test that launches every registered candidate against a signature it claims to serve.", "timestamp-utc": "2026-08-05T05:24:32.313015Z", "user-summary-candidate": "The autotuner now completes a full matrix-multiplication tuning sweep on AMD gfx1100 hardware. Fixed three further crashes: kernel variants being selected on hardware that cannot run them, an unchecked requirement that activations be 32-bit floats, and the tuner attempting to take timings inside a GPU graph capture."}
{"change-class": "audit", "event-id": "chg_20260805_053447_validated-the-tuning-fixes-on_5741", "files": ["docs/planning/active/hip-autotune/reviews/HI09b/RV06.md", "docs/planning/active/hip-autotune/HI12.md", "docs/reference/HANDOFF.md"], "plan-item-ids": ["HI12", "HI14", "HI09b"], "status": "unreleased", "technical-summary": "Cross-architecture validation of the five dispatch fixes, on gfx1201 (RDNA4) in addition to gfx1100 (RDNA3).\n\nBuilt workload-max for AMDGPU_TARGETS=gfx1100;gfx1201;gfx1030 and ran the full test-backend-ops MUL_MAT sweep under GGML_HIP_DISPATCH_MODE=tune on gfx1201. Result: 1157 signatures tuned, 0 test failures, exit 0, 2/2 backends passed, and no aborts, HSA exceptions or ROCm errors of any kind. This is the first clean tuning sweep on a second architecture and gives the arch-dependent fixes (RV02's per-arch config-table lookup, RV03's should_use_mmf gate) evidence beyond a single GPU.\n\nThe RDNA2 (gfx1030) run remains outstanding because the card was in use. It is the sharpest remaining test: RDNA2 has no WMMA at all, so MMF should be unavailable for every float type there, and it would be the first thing ever to exercise ggml_cuda_mmq_get_config_rdna2.\n\nThe comparison falsified a claim made in RV03 rather than confirming it. RV03 recorded that gating MMF on ggml_cuda_should_use_mmf whole costs exploring ncols 9..16 on RDNA3.0, and predicted RDNA4 would show those candidates becoming eligible. It does not: the eligible-count distributions for float src0 at ncols 9..16 are identical across the two architectures (59/58 cases with 2 eligible, then 6/6, 2/2, 7/7), and every MMF winner on both architectures was mmf:native:v1.\n\nThe reason is recorded as RV06 against HI09b. All 80 generated MMF candidates are mmf:f32, and MMF's F32 path requires amd_mfma_available (GGML_CUDA_CC_IS_CDNA) or ampere_mma_available (NVIDIA >= SM80), so within a HIP overlay they are CDNA-only. They are correctly generated and would be right on an MI300, but are unrunnable on all four GPUs this project has, and bigcherry has no CDNA hardware to validate them on. The defect is that architecture_mask is built from the enumerated architecture list rather than from per-family capability, so these 80 candidates carry a near-universal mask, fall through ggml_hip_candidate_supports_arch, and are rejected as INELIGIBLE by can_execute on every signature instead of being excluded up front as ARCHITECTURE. That is 30% of the candidate space, and it inflates the generated count in every coverage report. Not a correctness bug; a clarity and cost one, and precisely the categorisation ambiguity HI12's coverage report exists to remove.", "timestamp-utc": "2026-08-05T05:34:47.640293Z", "user-summary-candidate": "Validated the tuning fixes on a second GPU architecture (RDNA4), where a full sweep now completes cleanly with no errors. Identified that roughly a third of the generated kernel candidates target datacentre-class AMD hardware and are correctly skipped on consumer cards, but are currently reported in a misleading way."}
{"change-class": "code-fix", "event-id": "chg_20260805_054935_kernel-variants-that-require-d_7547", "files": ["tools/bigcherry/autotune_schema.py", "tools/bigcherry/autotune_catalog.py", "docs/planning/completed/hip-autotune/reviews/HI09b/RV06.md"], "plan-item-ids": ["HI09b", "HI12"], "status": "unreleased", "technical-summary": "Make architecture_mask encode matrix-core capability instead of the catalog's architecture list (RV06).\n\nCandidates were constructed with architectures=list(architectures), the full enumerated set, so the mask meant 'which GPUs does this build know about' rather than 'which GPUs can run this candidate'. All 80 generated MMF candidates are mmf:f32, and MMF's F32 path requires amd_mfma_available (CDNA) or ampere_mma_available (NVIDIA >= SM80), so under a HIP overlay they are CDNA-only and can never run on RDNA. They carried a near-universal mask, passed ggml_hip_candidate_supports_arch, and were rejected by can_execute as INELIGIBLE on every signature - 30 percent of the candidate space, misattributed.\n\nAdded to autotune_schema.py: MFMA_ARCHITECTURES (the four CDNA parts), WMMA_ARCHITECTURES (the ten RDNA3/RDNA3.5/RDNA4 parts - gfx115x included because AMD_WMMA_AVAILABLE is RDNA4 || RDNA3 and RDNA3 is __GFX11__, which covers RDNA3.5), and mmf_architectures(source_type, architectures) mirroring the type switch at the end of ggml_cuda_should_use_mmf. enumerate_mmf passes the narrowed list and skips a candidate whose list is empty.\n\nDeliberately coarse: should_use_mmf also excludes BF16 on CDNA3 and F16/BF16 on CDNA1/CDNA2 and remains the precise authority at dispatch time via RV03's gate. The mask narrows only where a candidate can never run, since too narrow silently drops a workable candidate while slightly too wide defers to a runtime check that already exists.\n\nVerified on the local idle gfx1100 (7900 GRE) using a Windows ROCm 7.1 build: the mask for mmf:f32:w10:nw1:v1 went from UINT64_C(134217726) to UINT64_C(240), exactly bits 4..7; that candidate now reports status 'architecture' in the tuner's measurement JSONL rather than falling through to 'ineligible'; 113,190 architecture rejections were recorded across the run, matching 80 candidates by roughly 1,415 signatures; and the sweep still passes at 1,155 signatures with 0 failures. Winners are unchanged, as expected, since these candidates were already being rejected.\n\nAlso established this session that the Windows HIP build works and is a usable test path: configure with ROCm 7.1's clang/clang++ and a short build directory such as C:\\bcw, since the default scratchpad path exceeds the 250-character Windows object-path limit.\n\nIncidental observation recorded in RV06 for follow-up: candidate status counts across one sweep were ineligible 193,657, architecture 113,190, ok 3,512, tolerance 1,491. The tolerance rejections include blas:hipblas-auto:v1 at nmse 1.5e-06 and max_abs 0.029, which is the tuner correctly refusing a faster but numerically divergent candidate; the volume is worth understanding before a real tuning run because a family rejected on tolerance is silently absent from the winner set.", "timestamp-utc": "2026-08-05T05:49:35.628178Z", "user-summary-candidate": "Kernel variants that require datacentre-class AMD hardware are now correctly identified as unsupported on consumer cards up front, rather than being re-examined for every matrix multiplication and reported under a misleading reason."}
{"change-class": "audit", "event-id": "chg_20260805_055250_measured-which-kernel-selectio_7483", "files": ["docs/planning/active/hip-autotune/reviews/HI09/RV07.md", "docs/reference/HANDOFF.md"], "plan-item-ids": ["HI09", "HI12", "HI17"], "status": "unreleased", "technical-summary": "First tuning measurement taken under conditions that can support a performance claim, recorded as RV07 against HI09.\n\nConditions: gfx1201 (RDNA4), idle GPU, GGML_HIP_TUNE_SCREEN_SAMPLES=3 and GGML_HIP_TUNE_FINAL_SAMPLES=15, full test-backend-ops -o MUL_MAT, 1155 signatures, 0 failures. All earlier runs this session used a single sample on a contended machine and could not support any performance statement.\n\nResult: 1071 native winners against 83 forced (7.2 percent). Forced-winner improvements were min 1.01, median 14.40, p90 38.23, max 70.39 percent. Broken down: mmvf:f32 71 wins, mmvq:q8_0 5, blas:hipblas-auto 4, mmq:q8_0 3.\n\nThe finding that matters for HI09: MMVQ explicit geometry, which accounts for 98 generated instances and the great majority of the new compiled code the item added, wins 5 of 335 MMVQ signatures at margins of 1.01, 1.01, 1.34, 4.64 and 5.15 percent. Three of those are at roughly 1 percent, which 15 final samples can barely separate from noise. By contrast MMVF wins 71 signatures at median 16.13 and max 70.39 percent, and MMVF candidates are existing_runtime - block size and accumulator selection over kernels upstream already instantiates, requiring no new compiled code at all.\n\nThis is explicitly not a verdict on HI09. It is the wrong architecture (RDNA4, not the gfx1100 target), the wrong shapes (a synthetic test matrix, not the Q8_0 27B MTP workload whose draft widths 1..8 are HI09's actual hypothesis - though 4 of the 5 wins are at n=1, the vector regime), and the margins sit near the noise floor. What it does establish is that the record-then-tune cycle on the real configuration should precede any further investment in geometry enumeration, and that on present evidence the families actually winning are BLAS (323 native winners, pointing at HI17) and MMVF parameter selection.\n\nAlso corrected a claim made earlier in this session that the explicit geometry matrix had earned its place; that was based on single-sample runs and should not have been stated.", "timestamp-utc": "2026-08-05T05:52:50.176198Z", "user-summary-candidate": "Measured which kernel-selection strategies actually pay off. Most of the gain comes from choosing among parameters the upstream library already compiles, rather than from the newly generated kernel variants - a result that redirects where further optimisation effort should go."}
{"change-class": "audit", "event-id": "chg_20260805_055537_measured-kernel-tuning-on-both_3096", "files": ["docs/planning/completed/hip-autotune/reviews/HI09/RV07.md", "docs/reference/HANDOFF.md"], "plan-item-ids": ["HI09", "HI17", "HI12"], "status": "unreleased", "technical-summary": "Per-architecture tuning measurement reverses the previous session finding. Recorded as RV07 (closed).\n\nThe same full sweep was run at SCREEN_SAMPLES=3 FINAL_SAMPLES=15 on two idle GPUs: gfx1201 (RDNA4) on brutus, and gfx1100 (RX 7900 GRE, the target architecture) locally via the Windows ROCm 7.1 build. Both 0 failures, 1155 and 1156 signatures.\n\nForced-winner counts, RDNA3 target against RDNA4: mmvf:f32 79 vs 71; mmvq:q8_0 24 vs 5; blas:hipblas-auto 24 vs 4; mmq:q8_0 3 vs 3; 103 forced winners vs 83.\n\nMMVQ explicit geometry on RDNA3 wins 24 signatures at min 1.12, median 16.18, max 25.32 percent, with 20 of 24 above 10 percent. The identical code on RDNA4 wins 5 times at roughly 1 to 5 percent. The earlier conclusion drawn from RDNA4 alone - that nearly all benefit came from parameters upstream already compiles and almost none from the 98 generated MMVQ instances - was therefore wrong on the hardware the project actually targets, and has been superseded in place with the original text retained for the record. The caveat attached to it, wrong architecture, turned out to be the whole explanation rather than a hedge.\n\nMost significant detail: mmvq:q8_0:w1:nw4:rpb4:sk1:v1 alone accounts for 14 of the 24 RDNA3 wins. That is a small_k geometry, the dimension that was present in the stable name and in the generated instances but silently dropped by _variant_initialiser, so the registry could name it and had no way to request it. The single most valuable compiled geometry in the catalog was unreachable until that was fixed.\n\nRevised priorities recorded: HI09 stays and its geometry matrix must not be trimmed on the strength of RDNA4 numbers; MMVF remains strong and cheap at 79 wins with no new compiled code; HI17 gains support but less than the win count suggests, since forced BLAS wins 24 on RDNA3 at median 1.88 percent with a single 18.6 percent outlier. The outstanding question remains record-then-tune on the actual Q8_0 27B MTP configuration, since both runs use synthetic test-backend-ops shapes.\n\nNote the RDNA3 run used the local 80-CU GRE rather than the server's 96-CU XTX. ggml_hip_hardware_key_v1 includes compute_units, so these are distinct hardware keys and the GRE's winners will not be replayed onto an XTX; the comparison is valid as evidence about the architecture but the target configuration's winners must still be tuned on the XTXs.", "timestamp-utc": "2026-08-05T05:55:37.058033Z", "user-summary-candidate": "Measured kernel tuning on both GPU generations and found the benefit is strongly architecture-dependent: the generated kernel variants deliver a 16% median speed-up on the target hardware while being nearly worthless on the newer generation. A single variant that was previously impossible to select accounts for most of that gain."}
{"change-class": "docs", "event-id": "chg_20260805_062819_added-a-generated-reference-do_8675", "files": ["tools/candidate_report.py", "docs/reference/CANDIDATES.md", "artifacts/tuning-logs/gfx1100gre-rdna3-s3f15.log", "artifacts/tuning-logs/gfx1201-rdna4-s3f15.log", "artifacts/tuning-logs/gfx1030-rdna2-s1f1.log", "artifacts/tuning-logs/gfx1030-rdna2-s1f1-nograph.log", "artifacts/tuning-logs/gfx1030-rdna2-native.log"], "plan-item-ids": ["HI09", "HI12", "HI16"], "status": "unreleased", "technical-summary": "Add a regenerable candidate reference joining the catalog to measured outcomes, and preserve the tuning logs it is built from.\n\ntools/candidate_report.py reads the newest manifest under artifacts/ and every log under artifacts/tuning-logs/, then writes docs/reference/CANDIDATES.md. Provenance is parsed from the log filename (architecture, screen/final sample counts, native or nograph variant) so a run cannot be cited without its sampling conditions travelling with it. Runs are classified: only those with at least 10 final samples are marked 'timing' and may be used to argue one candidate is faster than another; single-sample runs are marked 'correctness only' because they resolve nothing below roughly 10 percent.\n\nDeliberately a standalone script rather than a bigcherry subcommand, since it consumes evidence from particular runs rather than build inputs, and nothing in the build depends on it.\n\nThe document covers 270 candidates across five families, of which 62 have won at least one signature somewhere. It states explicitly that generated, runnable-here and measured are three different things, since conflating them is the failure mode behind most of this session's defects.\n\nWhat it makes visible in one place: winners are strongly architecture-specific. mmvq:q8_0:w1:nw4:rpb4:sk1:v1 wins 14 signatures on RDNA3 at 16.2 percent median and nothing at all on RDNA2 or RDNA4. mmq:q8_0:j8:fb1:t256:o2:i128:sram-q8_0:k256:sk0:v1 wins only on RDNA2. mmf:native wins 18 on RDNA3 at 45.1 percent median and 17 on RDNA4, and zero on RDNA2, which is the predicted result since RDNA2 has neither WMMA nor MFMA.\n\nFive logs preserved as artifacts: RDNA3 and RDNA4 at 3/15 samples, and three RDNA2 runs (native baseline, tune with graphs which fails, tune with graphs disabled which passes) that constitute the evidence for RV08.\n\nOne reading caution recorded rather than acted on: the two RDNA2 columns show a large winner-distribution shift between the graph and nograph runs, but both are single-sample so the shift is confounded by timing noise and is not cited as evidence for RV08.", "timestamp-utc": "2026-08-05T06:28:19.873320Z", "user-summary-candidate": "Added a generated reference document cataloguing every kernel candidate alongside how it actually performed on each GPU tested, so future work can check what exists, what can run where, and what has ever been measured."}
{"change-class": "docs", "event-id": "chg_20260805_063528_established-a-working-pipeline_5467", "files": ["docs/reference/HANDOFF.md", "artifacts/mtp-server-inventory.json", "artifacts/mtp-real-inventory.json"], "plan-item-ids": ["HI10", "HI12", "HI16"], "status": "unreleased", "technical-summary": "Complete the record-mode pipeline on the real target workload and document how to run every test in the project.\n\nEstablished that llama-bench cannot record the target workload: it has no speculative decoding, so it yields 17 signatures with widths [1] only, no MMVF and no BLAS. Recording through llama-server with --spec-type draft-mtp --spec-draft-n-max 5, driven by the existing bench harness at llamacpp/bench/run_bench.py in server-bench mode, yields 80 signatures across widths [1,2,4,5,6,8,10,12,16] with MMVF f32 and BLAS observed - matching the character of the pre-existing mtp-inventory.json (92 signatures). MTP was confirmed live by throughput: tg128 at 67.99 t/s through the server against 27.67 t/s from llama-bench on the same model and split, a 2.5x speculative speedup.\n\nThe target model now runs on the dual-XTX tensor split at pp2048 730.90 t/s and tg128 27.67 t/s. An earlier apparent load failure (GGML_ASSERT in ggml_backend_meta_alloc_ctx_tensors_from_buft) was an invocation error, not a defect: llama-bench device lists are slash-separated, so -dev ROCm0,ROCm1 means benchmark each GPU separately and a 28 GB model does not fit one 24.5 GB card. llama-server does use commas.\n\nSaved artifacts/mtp-server-inventory.json. Generating workload-max from it produces 240 candidates against 270 from mtp-inventory.json, narrower because a short run never observed width 3. The tree was deliberately restored to generate from mtp-inventory.json (manifest d64e79c8), since that is what every measurement in docs/reference/CANDIDATES.md was taken against; the server inventory should be adopted together with a rebuild and fresh measurements.\n\nReplaced the four-line verification snippet in HANDOFF.md with a full testing section: offline checks, Linux multi-arch and Windows build recipes, correctness runs with device indices and the measured 568 MiB VRAM requirement, timing runs with the warning never to draw performance conclusions from single-sample runs, coverage, and candidate-report regeneration. Documented the traps found the hard way this session: patch text edits are no-ops without git checkout of the target, Windows needs a short build path and ROCm bin on PATH at run time as well as build time, and run_bench.py appends to the shared results store even with --upload-dry-run.\n\nVerified after all changes: patcher tests 12/12, source audit 32/32.", "timestamp-utc": "2026-08-05T06:35:28.557725Z", "user-summary-candidate": "Established a working pipeline for capturing real workload profiles from a running inference server with speculative decoding enabled, and documented how to build, test and measure the project on every supported configuration."}
{"change-class": "audit", "event-id": "chg_20260805_064040_tuned-the-real-production-work_7747", "files": ["docs/planning/completed/hip-autotune/reviews/HI09/RV10.md", "docs/planning/active/hip-autotune/reviews/HI15/RV09.md", "docs/reference/HANDOFF.md"], "plan-item-ids": ["HI09", "HI15", "HI11", "HI12"], "status": "unreleased", "technical-summary": "Tuned the actual target configuration end to end and confirmed HI09's hypothesis on the workload it was designed for; separately confirmed that the tuning output cannot currently reach production.\n\nRV10 (closed). Ran llama-server with --spec-type draft-mtp --spec-draft-n-max 5 on the Q8_0 27B MTP model across 2x RX 7900 XTX with -sm tensor -ts 1,1 at -c 16384, driven by the bench harness in server-bench mode, GGML_HIP_DISPATCH_MODE=tune at SCREEN_SAMPLES=3 FINAL_SAMPLES=15 with GGML_CUDA_DISABLE_GRAPHS=1 per RV05.\n\nResult: 95 signatures tuned, 75 forced winners against 20 native, so 79 percent of signatures prefer a tuned candidate. The same code on synthetic test-backend-ops shapes gave 103 forced of 1156, or 8.9 percent, on RDNA3. The real workload prefers a tuned candidate roughly nine times as often, so the synthetic sweep understated the value of the dispatch layer by close to an order of magnitude.\n\nThe winners cluster exactly where HI09 predicted. The top MMVQ entries are mmvq:q8_0:w6:nw4:rpb4:sk1:v1 at 19.65 percent median, w4:nw4:rpb4:sk1 at 17.09, w5:nw4:rpb4:sk1 at 19.12 and w3:nw4:rpb4:sk1 at 14.18 - small_k geometries at widths 3 to 6, which is the draft-width regime produced by spec_draft_n_max 5. Candidate status totals were ok 1581, architecture 9212, ineligible 14550, tolerance 136.\n\nsmall_k has now been the standout dimension on two independent workloads, having also taken 14 of 24 MMVQ wins on the RDNA3 synthetic sweep. It is the dimension _variant_initialiser silently dropped, so the registry could name it and had no way to request it; the highest-value geometry in the catalog was unreachable until that was fixed.\n\nRV09 (open, against HI15). Source search confirms the replay cache has a reader and no writer: GGML_HIP_REPLAY_MAGIC is referenced only in the loader's validation branch, and nothing in src/, tools/ or patches/ emits it. Replay mode has therefore never been exercised against real tuned data - existing verification covers the loader's eight rejection paths, not a replayed winner - and the 95 winners just measured exist only as a JSONL. HANDOFF's HI11 row was corrected from 'mostly done' to 'loader only', and HI15 marked the top priority.\n\nAlso recorded: llama-server installs its own log callback, so bigcherry tuning lines never reach its log and a grep of that log misleadingly shows zero tuning; the measurements JSONL is the only record.", "timestamp-utc": "2026-08-05T06:40:40.963265Z", "user-summary-candidate": "Tuned the real production workload for the first time and found that 79% of matrix operations run faster with a tuned kernel variant, with the best gains of 14-20% concentrated exactly where predicted. The results cannot yet be saved and reused, which is now the most valuable remaining work."}
{"change-class": "code-fix", "event-id": "chg_20260805_070912_fixed-the-saved-results-format_5368", "files": ["src/ggml/src/ggml-cuda/hip-autotune-replay.cpp", "docs/planning/active/hip-autotune/reviews/HI15/RV09.md", "docs/reference/HANDOFF.md"], "plan-item-ids": ["HI15", "HI11", "HI09"], "status": "unreleased", "technical-summary": "Fix the replay cache entry format so tuned winners can round-trip, and document the remaining gap between tuning output and production.\n\nThe cache entry carried dispatch digest, signature digest, name offset, primary, secondary, width, acc_f16 and fallback, followed by two reserved bytes named ENT_SEEDED that nothing read. ggml_hip_variant_params has two further members and neither round-tripped.\n\nsmall_k was missing. Every top winner on the real MTP workload is an sk1 geometry (RV10: w6:nw4:rpb4:sk1 at 19.65 percent, w4 at 17.09, w5 at 19.12, w3 at 14.18). Replayed as sk0 they name a different compiled instance, and the (w6,nw4,rpb4,sk0) combination is not generated, so the launch would reach the HI09 missing-instance abort rather than merely losing the improvement.\n\nsrc0_type was missing because RV01 added it to the struct earlier in this same session without extending the serialisation. A replayed candidate would carry type 0, meaning F32, and be rejected by its own family's can_execute on every quantised signature, silently falling back to native. That was a regression introduced by RV01 and is now closed.\n\nSo replay would have failed on precisely the results worth replaying: sk1 geometries aborting, everything else quietly reverting to native. Latent only because nothing has ever written a cache.\n\nENT_SMALL_K and ENT_SRC0_TYPE now occupy the reserved bytes and are read into the winner's variant params. ENT_SIZE is unchanged and no format version bump was required, since nothing has ever written this format and no cache exists to be misread. The entry layout now has no spare bytes, so any future field needs a version bump.\n\nAlso recorded in RV09: the writer itself does not exist, which is now the highest-value remaining work, and two design questions it forces. First, the loader rejects a cache whose manifest hash differs from the build's, and a production build wanting only the winning instances will have a different manifest from the tuning build, so either the cache is re-emitted against the production manifest or the pruned set must preserve the hash. Second, replay-full and replay-slim appear in VARIANT_SETS and nowhere else - the mechanism for slimming a catalog down to the winners is declared but unimplemented.\n\nVerified: patcher tests 12/12, source audit 32/32, ggml-hip builds clean.", "timestamp-utc": "2026-08-05T07:09:12.405505Z", "user-summary-candidate": "Fixed the saved-results format so the best-performing kernel selections can actually be reloaded; previously the two settings that matter most to the fastest variants were silently dropped, which would have caused crashes or a quiet fallback to default behaviour."}
{"change-class": "feature", "event-id": "chg_20260805_071838_completed-the-pipeline-that-ca_3350", "files": ["tools/bigcherry/replay_cache.py", "tools/bigcherry/autotune_catalog.py", "src/ggml/src/ggml-cuda/hip-autotune-replay.cpp", "docs/reference/HANDOFF.md", "docs/planning/completed/hip-autotune/reviews/HI15/RV09.md"], "plan-item-ids": ["HI15", "HI11", "HI12", "HI09"], "status": "unreleased", "technical-summary": "Build the replay cache writer and verify the complete tune-to-production chain on the real workload for the first time.\n\ntools/bigcherry/replay_cache.py mirrors hip-autotune-replay.cpp byte for byte: magic 0x59484342, container and artifact versions, signature and hardware schema versions, the 16-byte manifest hash, a blake2b-16 content checksum personalised llama-dispatch, a string table and fixed-size entries. Written in Python for the same reason record mode is - nothing in the production link path should be able to write this format, and a run killed partway still yields a usable cache from whatever its JSONL flushed, with truncated final lines tolerated by construction. It refuses rather than skips when a winner is absent from the manifest, since skipping would produce a cache that loads cleanly and is quietly worse. ggml_type values are parsed from upstream's ggml.h rather than restated, because the cache stores the type as a byte and a drifted table would repoint every entry at a different type with no error.\n\nRefactored variant_fields() out of _variant_initialiser in autotune_catalog.py so the writer packs the same numbers the registry emits rather than reproducing the mapping; independent reproduction of that mapping is the root cause of four defects found this session.\n\nAcceptance test on the real MTP workload: tuned 95 winners, exported a 6459-byte cache covering 49 distinct candidates against manifest d64e79c8, built a separate GGML_HIP_DISPATCH_REPLAY=ON binary from the same manifest, and replayed the same workload. Coverage read mmvq 12246/12246, mmq 3612/3612, mmvf 52/52, blas 172/172, total 16082 dispatched of 16082 executed, 100 percent, with zero misses. This is the first time tuning output has reached a running inference server; every earlier replay verification covered only the loader's rejection paths.\n\nEnd-to-end throughput did not change and this is recorded prominently rather than glossed. Native measured pp512 553.16 plus or minus 18.70 and tg128 67.99 plus or minus 6.92; replay measured pp512 571.03 plus or minus 18.00 and tg128 65.69 plus or minus 7.74. Both differences sit inside one standard deviation. RV10's 14 to 20 percent per-signature improvements stand as measurements, but a matmul gain only moves tokens per second if the improved signatures are on the critical path and matmul dominates wall time, and neither is established. Recorded next steps: repeat runs, hot-signature weighting per standards 7.4 using the execution counts the coverage output already provides, and a check of what else dominates at this context length.\n\nStill missing: replay-full and replay-slim appear in VARIANT_SETS and nowhere else, so a production binary cannot be slimmed to the winning instances. The acceptance test used a full workload-max build, which sidesteps the manifest-hash coupling that becomes live once the production build stops sharing the tuning build's manifest.\n\nVerified: patcher tests 12/12, source audit 32/32.", "timestamp-utc": "2026-08-05T07:18:38.256502Z", "user-summary-candidate": "Completed the pipeline that carries tuning results into a running server: measured kernel choices can now be saved and reloaded, verified with every matrix operation served from the saved selections and no gaps. Overall throughput was unchanged in this test, so the per-operation gains have not yet been shown to translate into faster inference."}
{"change-class": "feature", "event-id": "chg_20260805_095403_added-the-ability-to-build-a-p_8974", "files": ["tools/bigcherry/autotune_catalog.py", "tools/bigcherry/__main__.py", "docs/planning/active/hip-autotune/reviews/HI15/RV12.md", "docs/planning/active/hip-autotune/reviews/HI12/RV11.md"], "plan-item-ids": ["HI15", "HI12"], "status": "unreleased", "technical-summary": "Implement replay-slim, the last structurally missing piece of the production path, and quantify what the tuning work is actually worth.\n\nreplay-slim and replay-full had been declared in VARIANT_SETS since the schema was written and implemented nowhere; build_manifest handled only inventory, workload-max and full-max. Added read_winners(), which parses the same measurements JSONL the replay-cache writer reads so a slim build and the cache it loads derive from one set of measurements rather than two readings of it, and a winners parameter on build_manifest that filters the enumerated catalog to those names. --winners is wired through both the standalone parser and the bigcherry generate subcommand.\n\nNative wrappers always survive the filter, which is correctness rather than caution: a replay miss falls back to native per standards 9.2, so a build without them cannot serve a signature the cache does not cover, and validate_manifest rejects a family with candidates but no native_wrapper. A winner naming a candidate absent from the generated catalog is a hard error rather than a skip, since it means the measurements came from a different inventory or architecture set.\n\nOn the real MTP winners this takes 270 candidates to 50, an 81 percent reduction: 45 winning variants plus 5 native wrappers.\n\nThe manifest-hash coupling question is answered and needs no format change. A slim build necessarily has a different manifest from the tuning build (3126b3aa against d64e79c8) and the loader rejects a mismatched cache. The working order is to generate slim first, then export the cache against the slim manifest; the writer's existing refusal when a winner is absent from the manifest is exactly the guarantee that slimming dropped nothing the cache needs. Verified: all 95 winners resolved and the cache stamped 3126b3aa.\n\nThe stated rationale for slimming does not survive measurement. libggml-hip.so is 67 MiB slim against 68 MiB full for the same single architecture, about 1.5 percent, because the library is dominated by upstream's own kernel instantiations and the generated MMVQ variants are a small slice of it. If replay-slim is to be justified it must be on compile time or on not shipping unmeasured code, not on binary size. The runtime replay test on the slim build is outstanding, blocked by the machine's own production llama-server reclaiming both XTXs, not by any fault in slimming.\n\nSeparately (RV11), quantified why the tuned build showed no end-to-end throughput change. Aggregate matmul saving across all 95 signatures is 7.6 percent, not the 14 to 20 percent of the headline winners which are a minority: mmvq 9.3 percent over 58 signatures, mmq 5.8 over 11, mmvf 3.0 over 5, blas 0.5 over 21. Weighting by the replay run's real execution counts and each family's mean per-call cost gives about 7.5 percent, essentially unchanged, because mmq's high per-call cost and smaller saving offsets mmvq's larger one. The null end-to-end result follows: matmul time falls about 7.5 percent, matmul is only a fraction of decode wall time, and the benchmark's own spread is plus or minus 10 percent, so the expected effect is roughly a third of one standard deviation. The measurement setup cannot answer the question in either direction.", "timestamp-utc": "2026-08-05T09:54:03.737101Z", "user-summary-candidate": "Added the ability to build a production binary containing only the kernel variants a tuning run actually selected, cutting the candidate set by 81 percent. Also quantified the benefit of tuning: matrix multiplication time drops by about 7.5 percent, which is too small to observe through the current end-to-end benchmark."}
{"change-class": "code-fix", "event-id": "chg_20260805_102857_tuning-results-now-record-enou_6430", "files": ["src/ggml/src/ggml-cuda/hip-autotune-tuner.cu", "docs/planning/active/hip-autotune/HI23.md", "docs/planning/active/hip-autotune/reviews/HI15/RV12.md"], "plan-item-ids": ["HI15", "HI23", "HI14"], "status": "unreleased", "technical-summary": "Record the dispatch key's components in tuning output, and correct two verification claims that did not hold.\n\nThe tuner's result records carried only the composite dispatch digest. That digest mixes in the build's manifest hash and source revision per standards 13.1, so it is meaningful only to the build that produced it, and no tool could compute any other key from a measurements file - not a re-keyed cache for a slimmed catalog, not a migration after an upstream bump, not an offline comparison of which signatures two runs share. Result now carries signature_digest and hardware_digest, computed in ggml_hip_tuner_resolve from the sig and hw structs it already receives, and both are emitted as hex alongside dispatch. Cold path, once per signature. Builds clean; patcher tests 12/12, source audit 32/32.\n\nFirst correction. RV12 previously concluded that the manifest-hash coupling for replay-slim was answered and needed no format change. That was wrong. Because the manifest hash is hashed into the lookup key, slimming the catalog changes every key and orphans the entire cache. Measured on the real MTP workload: the matching-manifest full replay build gives 25244 of 25244 dispatched with zero misses, while the slim build gives 14476 of 14476 dispatched with 81 misses whose digests are entirely disjoint from the 95 tuned digests. It fails silently because the cache header carries its own manifest hash, which was stamped with the slim value, so the header check passes and the loader reports 95 winners loaded while none is reachable. A production deployment would look correct, log nothing unusual, and run entirely untuned.\n\nSecond correction. RV09 reported the full replay chain at 100 percent coverage with zero misses. The coverage figure was real but the zero-misses figure was not evidence of anything, because ggml_hip_replay_record_miss returns early unless GGML_HIP_DISPATCH_MISS is set to native-record and that run did not set it. Re-run with recording enabled, the matching-manifest build does have zero misses across 25244 dispatches, so the conclusion survives, but it had not been tested when it was claimed. Coverage counts dispatches through the layer and a miss is still a dispatch; the two numbers answer different questions.\n\nCreated HI23 for the design the owner asked for: make the lookup key portable over hardware, signature and objective, and carry manifest hash, source revision and a monotonic release generation as per-entry provenance. Lookup prefers an entry matching this build exactly, falls back to the highest generation and reports it as stale, and misses fall back to native as today. This preserves standards 13.1's intent while turning a silent total miss into a labelled, counted condition, and fixes the larger problem the slim work exposed: every upstream bump currently discards all tuning, silently, at roughly 20 minutes of exclusive GPU time per run for winners worth about 7.5 percent of matmul time. Reuse is safe because the loader already drops entries naming candidates this binary does not carry and the resolver already refuses a stored winner that can_execute rejects.\n\nAlso measured: the RV08 intermittent tolerance failure did not recur in 8 further RDNA2 runs at one sample, putting it at roughly 1 in 11 observed runs.", "timestamp-utc": "2026-08-05T10:28:57.386498Z", "user-summary-candidate": "Tuning results now record enough information to be reused across rebuilds. Also corrected two earlier verification claims: a slimmed production build silently discards all tuning because the lookup key includes the build's own identity, and an earlier no-gaps result had not actually enabled gap reporting."}
{"change-class": "code-fix", "event-id": "chg_20260805_103856_tuning-results-now-survive-a-r_4272", "files": ["src/ggml/src/ggml-cuda/hip-autotune-signature.cpp", "src/ggml/src/ggml-cuda/hip-autotune-replay.cpp", "src/ggml/src/ggml-cuda/hip-autotune-replay.h", "src/ggml/src/ggml-cuda/hip-autotune-tuner.cu", "docs/planning/active/hip-autotune/HI23.md"], "plan-item-ids": ["HI23", "HI15", "HI11"], "status": "unreleased", "technical-summary": "Make tuning survive a rebuild: the dispatch key is now portable, and a mismatched candidate set is a reported staleness condition rather than a silent total miss.\n\nggml_hip_dispatch_digest previously hashed GGML_HIP_AUTOTUNE_MANIFEST_HASH_STR and GGML_HIP_AUTOTUNE_SOURCE_REVISION_STR into the lookup key alongside hardware, signature and objective, per standards 13.1. The intent was that a build against a different candidate set must not resolve to winners measured by the previous one. The mechanism was too blunt: every rebuild that touched the catalog or bumped upstream moved every key and silently discarded all tuning. A replay-slim build was the extreme case, since slimming necessarily changes the candidate set - measured previously at 81 missed signatures whose digests were entirely disjoint from the 95 tuned ones, while the cache still reported itself loaded.\n\nThe digest now covers hardware, signature and objective only. The manifest hash check in the loader was downgraded from reject to a flag: the cache loads, its winners are used, and a warning states that they were chosen from a different candidate set and may no longer be best. Added ggml_hip_replay_is_stale() and the g_stale flag so the coverage report can carry it, since a log line in a long-running server is not a usable answer to whether a build is tuned.\n\nLoosening the key is safe because the guards that protect correctness are per entry and both still run: the loader drops any entry naming a candidate this binary does not carry, and the resolver re-runs can_execute before launching a stored winner. An older winner is therefore valid but possibly no longer optimal, which is what the staleness flag communicates.\n\nAlso completed the prerequisite: the tuner's Result now carries signature_digest and hardware_digest, computed in ggml_hip_tuner_resolve from the sig and hw structs it already receives, and emitted alongside dispatch in every result record. Verified 1155 of 1155 records carry both. Without them no tool could compute an alternative key from a measurements file.\n\nVerified on gfx1100 by tuning under workload-max, generating replay-slim (35 candidates, manifest 13fc4790), exporting, rebuilding and replaying. A slim build with a matching cache gives 1188 of 1188 dispatched with 1 miss, against 81 misses before the change. A slim build loaded with a cache stamped for a different manifest also gives 1188 dispatched with 1 miss and emits the staleness warning, confirming both halves of the requirement. Patcher tests 12/12, source audit 32/32.\n\nRecorded as an interim measure in HI23 and in the code comments. The fuller design - per-entry provenance, a monotonic release generation, several retained generations, writer-side merging and fresh/stale/miss coverage counts - remains outstanding; until it lands a cache holds one generation and re-tuning replaces it wholesale. Note that changing the digest invalidated measurements taken before it, including the MTP run behind RV10 and RV11, whose cache needs a re-tune to be usable.\n\nSeparately observed: tools/tests/test_inventory.py has 10 failures from parallel HI20 work (sqlite3.OperationalError near '?' in load_measurements), unrelated to these changes.", "timestamp-utc": "2026-08-05T10:38:56.120487Z", "user-summary-candidate": "Tuning results now survive a rebuild. Previously any change to the kernel catalogue or an upstream update silently discarded every tuned result; they are now reused, and flagged as possibly out of date when the build has changed underneath them."}
{"change-class": "tests", "event-id": "chg_20260805_105030_added-an-automated-check-that_5333", "files": ["tools/tests/test_variant_params.py"], "plan-item-ids": ["HI16", "HI09"], "status": "unreleased", "technical-summary": "Add an offline test for the defect class that produced the two most expensive bugs in this work: a dimension present in a candidate's stable name and absent from its packed variant params.\n\nThat invariant has broken twice. small_k appeared in the stable name and selected a distinct compiled instance, but _variant_initialiser did not pack it, so the registry could name an instance it had no way to request; the dispatch layer requested the sk0 geometry whatever the name said. On the real MTP workload the sk1 geometries are the most valuable variants in the catalog (RV10, w6:nw4:rpb4:sk1 at 22 percent), so a one-line packing omission was hiding the best result the project had. src0_type was the same failure again: every family names itself for one src0 type, every launch path dispatches on the runtime type, and the type never reached the descriptor, so eligibility could not check it and candidates were measured on signatures of other types (RV01) - fatal in one family, silently misattributed winners in two others.\n\nNeither was visible by reading the code, because the name and the packing are written in different functions and each looks correct in isolation. Both were found on hardware, hours into a run. This test finds the class in under a second with no GPU.\n\ntools/tests/test_variant_params.py contains four tests. The general one parses each stable name's numeric tokens and asserts each equals the corresponding slot in variant_fields(), with the token-to-slot mapping declared per family so that a family reading the wrong slot is also caught - MMQ's J and MMVQ's nwarps share primary, and swapping them would launch a plausible but wrong kernel. Two targeted regression tests cover small_k and src0_type by name. A fourth asserts every generated MMVQ candidate appears in the rendered instances, since the launcher aborts rather than falling back on a missing instance.\n\nIt deliberately parses the name rather than reading config twice: checking config against params derived from config would pass even when the name promises a dimension nothing implements, which is exactly the situation that occurred. It builds from full-max rather than a workload profile so every geometry exists regardless of what any inventory happened to observe - small_k is not forced into existence by any workload.\n\nMutation-verified: reintroducing the small_k drop in _variant_initialiser fails the suite immediately with a message naming the consequence. Restored afterwards; 16 of 16 tests pass alongside the patcher suite.", "timestamp-utc": "2026-08-05T10:50:30.781116Z", "user-summary-candidate": "Added an automated check that catches a class of bug which previously required hours of hardware testing to find, and which had twice caused the fastest kernel variants to be silently unreachable."}
{"change-class": "feature", "event-id": "chg_20260805_105221_the-coverage-report-now-states_3432", "files": ["src/ggml/src/ggml-cuda/hip-autotune-coverage.cpp", "docs/reference/HANDOFF.md"], "plan-item-ids": ["HI13", "HI23", "HI15"], "status": "unreleased", "technical-summary": "Put replay provenance in the coverage artifact, closing the loop on the staleness flag added with portable keys.\n\nggml_hip_replay_is_stale() was added when the manifest check moved from rejection to a flag, but nothing consumed it, so the condition existed only as a log line. A warning in a server that has been up for a week is not a usable answer to whether a build is tuned.\n\nReplay builds now emit an extra object beside the totals: entries, misses and stale. The distinction matters because dispatched equals executed does not mean the cache was used - a miss is still a dispatch, to native - so a fully covered run can be entirely untuned. That exact confusion produced a wrong conclusion earlier in this work (RV12), where a slim build reported full coverage while not one of its 95 winners was reachable. The same counts are logged alongside the coverage line for the same reason.\n\nGuarded by GGML_HIP_DISPATCH_REPLAY since the replay module is absent from tune and record builds.\n\nVerified on gfx1100: a workload-max replay build loaded with a cache stamped for the slim manifest reports total_dispatched 1188, replay entries 1155, misses 1, stale true - correctly stale, since the cache's manifest differs from the build's. Patcher and variant-params tests pass, source audit 32/32, both replay and tune configurations build clean.\n\nDocumented in HANDOFF alongside the coverage instructions, including the trap that misses are only meaningful with GGML_HIP_DISPATCH_MISS=native-record: without it ggml_hip_replay_record_miss returns early and the count is always zero, which reads as success. That trap invalidated an earlier zero-misses claim.\n\nAlso recorded: the interleaved native-versus-replay throughput comparison was inconclusive. Three alternating pairs on the real MTP workload gave native 69.72 with standard deviation 4.73, replay 64.81 with 9.51, and native 64.24 with 16.03. A 23 percent spread means the machine was not settled between back-to-back server starts, and replay came out lower here while higher in an isolated run, which is what noise at this level looks like. No direction should be read into it; RV11's estimate that resolving a roughly 3 percent end-to-end effect needs far more repetitions, or direct matmul timing rather than tokens per second, stands.", "timestamp-utc": "2026-08-05T10:52:21.727816Z", "user-summary-candidate": "The coverage report now states whether saved tuning results were actually used and whether they are out of date, rather than only reporting that every operation went through the dispatch layer - two things that look identical in the previous output."}
{"change-class": "code-fix", "event-id": "chg_20260805_105855_validated-kernel-dispatch-acro_8076", "files": ["src/ggml/src/ggml-cuda/hip-autotune-dispatch.cu", "docs/planning/active/hip-autotune/reviews/HI14/RV14.md"], "plan-item-ids": ["HI14"], "status": "unreleased", "technical-summary": "Work through HI14's multi-GPU and graph-capture validation matrix, and fix a mode-parsing defect found doing it.\n\nggml_hip_parse_mode did not accept 'native', the documented name of its own default, so asking for it explicitly produced 'unknown GGML_HIP_DISPATCH_MODE=native; using native' and fell through. Harmless in effect since the fallback is native either way, but it teaches a reader to distrust the mode they requested and makes a deliberate native baseline look like a typo. Baselines are load-bearing in this work: comparing against native is how RV08 was correctly re-attributed after a wrong first diagnosis, and how the segfault below was pinned on upstream. Fixed and verified at zero unknown-mode warnings.\n\nValidation results recorded as RV14. Passing rows: single gfx1100 with a full MUL_MAT sweep at 1155 signatures and zero failures plus the real MTP workload; single gfx1201 at 1157 signatures and zero failures; gfx1030 which was not in the original matrix and had never been run; multiple XTX under tensor split with replay at 17500 of 17500 dispatched and zero misses; MTP widths 1 through 16 observed and tuned; pp512 and tg128 throughout. Notably every replay run had HIP graphs enabled - no GGML_CUDA_DISABLE_GRAPHS was set - and reported 100 percent dispatched, so nothing bypasses the dispatch layer under graph capture.\n\nMixed XTX plus R9700 tensor split segfaults during model load with -dev ROCm0,ROCm2 -sm tensor. Attributed to upstream rather than to this project by the same method used for RV08: native mode segfaults identically, no bigcherry frame appears in either trace, and the crash occurs before any matmul reaches the dispatch layer. The same path also logs that internal AllReduce init failed and fell back to a meta-backend butterfly, and that llama_params_fit is not implemented for SPLIT_MODE_TENSOR.\n\nHardware separation is correct by construction: ggml_hip_make_hardware_key takes the context's device and the key includes architecture_code and compute_units, so heterogeneous devices produce distinct keys and cannot share winners. That property is what would have made the mixed run informative, and it remains unexercised until upstream supports the topology.\n\nRemaining for HI14, in value order: the long-context server workload, which is the largest untested row and closest to production since everything so far ran at 8k or 16k context against a 245,760 target; pp256, pp1024, pp4096 and tg512, which are cheap additions to an existing bench invocation; mixed topology, blocked upstream and needing no work here; and RCCL with the production PCIe topology, which nothing in this session exercised.", "timestamp-utc": "2026-08-05T10:58:55.201792Z", "user-summary-candidate": "Validated kernel dispatch across single and multiple GPUs, three GPU generations, tensor splitting and GPU graph capture. Mixing different GPU generations in one tensor split crashes in the upstream library itself, independently of this work."}
{"change-class": "audit", "event-id": "chg_20260805_115126_found-and-fixed-a-build-settin_8608", "files": ["docs/reference/HANDOFF.md", "docs/planning/active/hip-autotune/reviews/HI14/RV15.md", "docs/planning/active/hip-autotune/reviews/HI14/RV16.md", "artifacts/mtp-longctx-inventory.json"], "plan-item-ids": ["HI14", "HI12"], "status": "unreleased", "technical-summary": "Complete HI14's long-context and multi-GPU rows, and correct a build misconfiguration that invalidated every dual-GPU throughput measurement in this work.\n\nRV15. The owner supplied reference numbers from the production lane that were 1.5 to 1.7 times higher than anything measured here. Cause: GGML_HIP_RCCL was OFF in every build used, so a tensor-split run logged 'internal AllReduce init failed; falling back to meta-backend butterfly' and performed a fallback allreduce on the critical path of every layer. Dispatch-layer overhead was ruled out from data already collected, since native at 69.83, record at 67.99 and replay at 71.45 all sat within noise on that build.\n\nRebuilt with GGML_HIP_RCCL=ON and re-measured at matched settings so RCCL was the only variable. Prefill is now fully explained: pp256 693.7 against the lane's 685.0, pp1024 1031.7 against 1025.6, pp4096 1254.8 against 1248.5, all within noise, from 460, 596 and 666 respectively without RCCL. Generation is mostly explained: tg512 matches at 89.8 against 91.7, but tg128 remains 20 percent short at 89.5 against 112.7 and tg2048 12 percent short. The residual is concentrated at short generation and absent at tg512, pointing at the speculative-decode path; the lane also differs in upstream revision (b10227 against 22dc605) and carries hip-mmq, and ran at ctx 245760 which aborts on this build at ggml-backend-meta.cpp:1520, so that variable could not be eliminated.\n\nTuning results are unaffected because the tuner times matmul launches in isolation and allreduce is not inside that measurement. Dual-GPU end-to-end numbers from earlier in this work are on a crippled baseline and need redoing. Hot-signature ranking is provisional, since a faster allreduce changes how much wall time each shape represents. HANDOFF's build recipe now sets GGML_HIP_RCCL=ON with the measured cost of omitting it stated inline.\n\nRV16. Ran HI14's long-context row at production settings: -c 229376 across both XTXs with q8_0 draft KV, checkpoints and --fit off, driven by the full profile and then compression-172k-token, a 449 KB prompt with 8192 generation. Throughput held its expected shape, prefill 460 to 666 across pp256 to pp4096 and 408 at pp172k, generation flat near 68 through tg2048 and 48.7 at tg8192.\n\nThe substantive result is the recorded inventory: 121 signatures at 200k context against 80 at 16k, a 51 percent increase, with width 3 appearing only at long context. 41 signatures, a third of the long-context workload, never appear at 16k. Every tuning run in this project was recorded at 8k or 16k, so a cache built from them covers two thirds of what production executes and the remainder falls back to native, correctly but untuned. Type sets are identical, so the candidate set does not need widening - this concerns which shapes are measured, not which kernels are compiled. Saved as artifacts/mtp-longctx-inventory.json.\n\nTwo invocation traps recorded: the bench harness resolves prompt_file relative to the working directory, so run_bench.py must be invoked from the llamacpp root or the 172k profile fails; and -c 245760 with f16 KV aborts on this hardware while 229376 fits with cache-ram 24576.", "timestamp-utc": "2026-08-05T11:51:26.602647Z", "user-summary-candidate": "Found and fixed a build setting that was halving multi-GPU performance, restoring prefill throughput to match the production configuration. Also established that the long-context workload exercises 51% more distinct operations than the short-context tests everything had been tuned against."}
{"change-class": "audit", "event-id": "chg_20260805_123239_tested-whether-a-compile-time_6071", "files": ["docs/planning/active/hip-autotune/reviews/HI12/RV17.md"], "plan-item-ids": ["HI12", "HI17"], "status": "unreleased", "technical-summary": "Test whether GGML_CUDA_FORCE_MMQ improves short token generation and whether measured dispatch subsumes it. Recorded as RV17.\n\nThe premise is correct: FORCE_MMQ is a pure policy flag, a bare return true inside ggml_cuda_should_use_mmq at mmq.cu:403. It does not change what is compiled, since MMQ instantiations derive from the config tables, so it only re-prioritises families - which is what measured dispatch does per signature rather than by a global compile-time switch. bigcherry's CMake already rejects the flag alongside measured dispatch under standards 4.3 because forcing it would hide the non-MMQ families and truncate the candidate set.\n\nVerified the tuner is not comparing unequal work: ggml_hip_blas_launch calls ggml_cuda_mul_mat_cublas_dispatch, upstream's full dense path including dequantisation, and ggml_hip_blas_workspace accounts for dequantised src0 plus converted src1.\n\nMeasured a plain RCCL build against plain RCCL plus FORCE_MMQ at identical settings on dual XTX tensor split. Prefill identical within noise at 693.7 against 692.3 for pp256, 1031.7 against 1029.2 for pp1024 and 1254.8 against 1256.5 for pp4096. Short generation identical at 89.5 against 88.5 for tg128 and 90.3 against 90.0 for tg256. Long generation was lower with the flag, 89.8 against 82.6 at tg512 and 91.2 against 81.0 at tg2048, though those points carry standard deviations of 10 to 15 so that is suggestive rather than established.\n\nThe reason it is a no-op is the substantive finding. The AMD branch of ggml_cuda_should_use_mmq caps MMQ only for Q2_K, Q6_K and IQ2_XS/IQ2_S, and every other type hits default return true. The target model is q8_0, so MMQ is already the default choice on this architecture and forcing it changes nothing. The flag matters where the RDNA3 q8_0 path never goes, principally NVIDIA, where fp16 tensor cores make cuBLAS competitive and the function has a dedicated branch.\n\nConsequently FORCE_MMQ does not explain the remaining tg128 gap against the production lane, 89.5 here against 112.7 there. The lane's different upstream revision is the leading remaining candidate.\n\nThis also clarifies that blas:native winning signatures is not a failure of the tuner: where it picks BLAS it has measured BLAS as faster on that shape having timed the full dequantise-and-multiply path, while upstream would have picked MMQ for q8_0 by default. Those are cases of the dispatch layer disagreeing with upstream on evidence, which is its purpose.", "timestamp-utc": "2026-08-05T12:32:39.429837Z", "user-summary-candidate": "Tested whether a compile-time flag that prefers quantised matrix-multiply kernels improves generation speed. It makes no difference on this hardware because those kernels are already the default choice for this model format, and the automatic tuning covers the same decision with per-shape measurements."}
{"change-class": "audit", "event-id": "chg_20260805_125347_discovered-that-automatic-tuni_4430", "files": ["docs/planning/active/hip-autotune/reviews/HI12/RV18.md", "docs/planning/active/hip-autotune/reviews/HI12/RV19.md"], "plan-item-ids": ["HI12", "HI09", "HI17"], "status": "unreleased", "technical-summary": "Establish that the tuning workload, not the tuning engine, was limiting results, then re-tune correctly and measure the achievable gain.\n\nRV18. Joining the long-context record against tuning measurements, using the signature digest the tuner now emits, showed the five hottest signatures had never been measured. 121 signatures recorded across 416,940 matmul calls, 95 tuned, 81 overlapping, 40 recorded-but-untuned - and those 40 included the top five by call count, about 252,000 calls or 60 percent of all matmul work. Upstream chooses mmq for 72.8 percent of calls across 36 signatures, mmvq for 23.5 percent, blas for 3.6 percent and mmvf for 0.2 percent. The top 12 of 121 signatures are 73.7 percent of calls.\n\nThe cause was verified rather than assumed. An initial hypothesis that context-dependent strides produced different digests for the same shape was wrong: the 16k run contains zero signatures with ne1 of 512, and all five hot shapes are absent from it entirely. Tuning is per signature and lazy, measuring every eligible candidate the first time a signature is dispatched, and that worked correctly - the one hot signature that was covered had 24 candidates measured. Coverage is determined solely by which shapes the driving workload executes, and the 16k tune used the default bench profile, which never produces width-512 prefill dispatches, while the recorded workload used full plus a 172k compression test.\n\nRV19. Re-tuned on the RCCL build at the corrected production configuration with the full profile. Call coverage rose from roughly 40 percent to 98.8 percent, 411,860 of 416,940 calls across 121 signatures.\n\nCall-weighted saving is 10.2 percent of matmul time, 91,523 ms down to 82,208 ms. By family: mmq 87,718 to 78,854 ms, a 10.1 percent saving over 298,284 calls; mmvq 3,397 to 2,947 ms, 13.3 percent over 97,912 calls; blas unchanged at 401 ms; mmvf 0.7 percent. MMQ is 96 percent of matmul time, not merely 73 percent of calls, so nothing that leaves MMQ untouched can move the total. This supersedes the earlier 7.5 percent figure, which was unweighted and computed over a set excluding the hot shapes.\n\nUpstream's MMQ J heuristic proves well judged on three of the top five shapes and badly wrong on two. On the single hottest signature, 110,160 calls, J=48 runs 71.3 us against upstream's roughly 128, a 44.4 percent improvement. On a narrow 5120x24 shape, J=16 with fallback runs 38.8 us against 128.3, a 3.3x difference, which is where a large J wastes most of its tile. The wins come from selecting a different row of upstream's own config table per shape on evidence, not from new kernels.\n\nAlso recorded: tuning at full context aborts because the tuner allocates reference-output buffers to validate against native and there is no VRAM headroom at 229376 with cache-ram 0; tune at a smaller context and replay at production context, since the hot shapes are ubatch-driven.", "timestamp-utc": "2026-08-05T12:53:47.175689Z", "user-summary-candidate": "Discovered that automatic tuning had been measuring only a fifth of the work the model actually does, because the test workload did not produce the shapes that dominate real use. After correcting it, tuning cuts matrix-multiplication time by 10 percent, with individual operations up to three times faster than the built-in choice."}
{"change-class": "audit", "event-id": "chg_20260805_131114_measured-the-finished-system-e_8962", "files": ["docs/planning/active/hip-autotune/reviews/HI12/RV20.md", "docs/reference/HANDOFF.md", "artifacts/tuning-logs/mtp-prod-fullprofile.measurements.jsonl"], "plan-item-ids": ["HI12", "HI15", "HI11"], "status": "unreleased", "technical-summary": "First valid end-to-end measurement of the dispatch layer, and the first demonstration of the complete chain on real hardware with a real workload.\n\nDesign: same binary throughout, bc-build-replay-rccl, with only GGML_HIP_DISPATCH_MODE differing, so there is no build variable and no RCCL confound. Three interleaved native and replay rounds so thermal and clock drift affect both arms equally, 20 second settle between arms, context 65536, cache-ram 0, dual XTX tensor split, draft-mtp n_max 5, mtp-dual profile of pp256, pp1024, pp4096, tg512 and tg2048. The cache is the corrected tune measured against the real shape distribution, exported at 121 winners and 7759 bytes.\n\nResult, means across three rounds: pp256 690.8 native against 720.9 replay for plus 4.4 percent; pp1024 1029.5 against 1052.0 for plus 2.2 percent; pp4096 1253.9 against 1275.4 for plus 1.7 percent; tg128 flat at 116.8 against 117.0; tg512 78.7 against 82.7 and tg2048 86.1 against 88.7, both noisy with one reversed round each.\n\nPrefill separates cleanly: every replay run beat every native run at all three prefill points with no overlap between the ranges, which at three rounds is about as strong as the design can give. Generation is flat to noisy and should not be quoted. The cache was fully exercised at 103,168 dispatched of 103,168 executed, 121 entries, zero misses, not stale.\n\nThe split matches prediction. The 10.2 percent call-weighted matmul saving is concentrated in MMQ at width 512, which is the prefill path, while generation runs at narrow widths where the wins are MMVQ's and worth 450 ms against MMQ's 8,864 ms. A prefill-visible, generation-invisible outcome is therefore coherent rather than surprising. That a 10.2 percent matmul saving yields 1.7 to 4.4 percent end to end also gives a first empirical bound on matmul's share of prefill wall time, and the gain scales inversely with prompt length, consistent with per-launch selection benefits mattering more when there is less work to amortise over.\n\nThree confounds had to be removed before any of this was measurable, each of which silently invalidated everything before it: a contended machine, a build with GGML_HIP_RCCL off, and tuning driven by a bench profile that never produced the hot shapes. HANDOFF now leads with this result and states explicitly which figures are established and which are not.", "timestamp-utc": "2026-08-05T13:11:14.704380Z", "user-summary-candidate": "Measured the finished system end to end: automatically tuned kernel selection makes prompt processing 1.7 to 4.4 percent faster than the built-in defaults, verified across interleaved runs with no overlap between the two configurations. Generation speed is unchanged."}
{"change-class": "docs", "event-id": "chg_20260805_132027_published-a-detailed-per-opera_3360", "files": ["docs/reference/TUNING-DETAIL.md", "docs/reference/HANDOFF.md", "docs/planning/active/hip-autotune/reviews/HI12/RV21.md"], "plan-item-ids": ["HI12"], "status": "unreleased", "technical-summary": "Publish a per-signature tuning detail report and record the further-tuning opportunities it exposes (RV21).\n\ndocs/reference/TUNING-DETAIL.md gives, for the top 15 signatures by call count, the full shape, upstream's choice, the tuned winner, the gen/eligible/measured triple and a ranked table of every measured candidate with medians and relative gaps; then winners weighted by calls, and a boundary analysis of which J and width values are actually selected.\n\nThree findings recorded as RV21, open.\n\nFirst, a probable defect. On signature 6608d41c, 5120x6144 by 5120x512 at 55,080 calls or 13.2 percent of the workload, mmq:native wins at 465.80 us and every eligible forced candidate is slower, the best being j64:fb0 at 501.28 us. Both were measured at the full 15 samples so this is not a sampling artefact. The candidate status list shows every fb1 fallback variant marked ineligible - j16, j32, j64 and j128. If upstream's own J search selects a fallback configuration for this shape then ggml_cuda_mmq_variant_is_eligible is rejecting the winner and the tuner can never propose it. That is the mirror image of the earlier eligibility defects, which were too loose; this would be too strict. Fallback variants are demonstrably valuable when admitted, since j16:fb1 won another signature by 69.8 percent. Next step is to instrument which J and fallback upstream actually selects and compare against what the predicate admits.\n\nSecond, the J grid on RDNA3 is fixed by upstream at 16 through 128 in steps of 16; j8, j24 and j40 candidates exist in the catalog but are architecture-rejected because they belong to other tables. On the hottest signature, 110,160 calls or 26.4 percent, the optimum is sharp: j48 at 71.28 us against j64 at 90.24 and j32 at 90.64. A narrow well between sampled points suggests J=40 or J=56 could beat it, which requires adding CASE rows to mmq-config-rdna3.cuh. The catalog parses that table rather than restating it, so new rows are picked up automatically.\n\nThird, MMVQ widths 6 through 8 win 60 calls out of 416,940 and can be trimmed from the enumeration at no measurable cost.\n\nAlso documented a share gotcha that caused the report to appear missing: any file created by a server-side command may be invisible from the Windows view of the SMB share, not only dotfiles and not only in directory listings - opening by exact path also fails. Files written from the Windows side are visible on both. Repo files must therefore be produced from the Windows side, copying back from the server rather than writing in place, since the server-side command reports success while the file is absent for anyone working from J:.", "timestamp-utc": "2026-08-05T13:20:27.474889Z", "user-summary-candidate": "Published a detailed per-operation tuning report showing which kernel was selected for each of the busiest operations and how close the alternatives were, along with three concrete opportunities for further improvement."}
{"change-class": "audit", "event-id": "chg_20260805_132816_investigated-an-apparent-flaw_9364", "files": ["docs/planning/active/hip-autotune/reviews/HI12/RV21.md"], "plan-item-ids": ["HI12"], "status": "unreleased", "technical-summary": "Investigate the apparent MMQ eligibility defect from the tuning detail report. Two hypotheses were wrong; the real mechanism is screening noise, and its aggregate cost is negligible.\n\nFirst hypothesis, that MMQ fallback variants were wrongly ineligible, is incorrect. ggml_hip_mmq_can_execute derives fallback from the shape as (ne0[1] % 128) != 0, mirroring mul_mat_q_case which derives it the same way. For the signature in question ne01 is 6144, divisible by 128, so fb0 is required and every fb1 candidate is correctly rejected. On a different signature with ne01 of 24 the fb1 variant is required and duly won by 69.8 percent. The predicate is correct in both directions.\n\nSecond hypothesis, that the forced-J path carries overhead relative to native, is also incorrect. The patched mul_mat_q_switch_J computes J_best, optionally overwrites it with forced_J, and calls one launcher, so forcing J equal to J_best is not equivalent to native but identical to it.\n\nThe real mechanism: mul_mat_q_compute_J_best selects the J minimising ceil(ncols_max / J), which for ncols_max of 512 is the largest eligible J, 128. So mmq:native and mmq:q8_0:j128:fb0 are the same kernel on that signature. They measured 465.80 and 531.40 us respectively because native received 15 samples while j128 received 3 before screening discarded it. Re-tuning with screening at 15 samples confirmed the prediction exactly: j128 at 462.28 us against native at 464.92 us, converging within 0.6 percent.\n\nAggregate impact is small and is reported as such. Call-weighted over the same 411,860 calls, total saving moves from 10.2 to 10.4 percent, mmq from 10.1 to 10.3, while mmvq moves from 13.3 down to 12.7. The two runs differ by less than their own run-to-run variation, so fixing screening did not measurably improve the outcome on this workload. The candidates being discarded were, in aggregate, ones that did not matter, including the investigated case where the discarded candidate was native's own kernel.\n\nRecommendation recorded: measured screening error is roughly 14 percent at three samples while the documented retention band is everything within 10 percent of best, so the band is narrower than the error of the measurement that selects it. Either widen the band to about 20 percent or raise screening to five to seven samples. Full 15-sample screening is affordable at this signature count but buys nothing measurable.", "timestamp-utc": "2026-08-05T13:28:16.679873Z", "user-summary-candidate": "Investigated an apparent flaw in kernel selection and found the selection logic correct; the real issue was that the quick screening pass used too few timing samples to rank candidates reliably. Correcting it confirmed the diagnosis but changed overall performance by a negligible amount."}
