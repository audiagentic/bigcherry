"""Minimal lab entry point; keep experiments self-contained and non-authoritative."""

if __name__ == "__main__":
    raise SystemExit(
        "Copy this template into a plan-owned lab directory before running it."
    )
