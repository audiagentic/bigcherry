schema = 1

[[check]]
id = "apply"
capability = "apply"
validator = "apply"
required = true

[[check]]
id = "build"
capability = "build"
validator = "build"
required = true

[[check]]
id = "correctness"
capability = "correctness"
validator = "autotune-campaign"
required = true

[[check]]
id = "activation"
capability = "activation"
validator = "trace-marker"
required = true
marker-regex = "BIGCHERRY_PATCH_HIT patch=1204_rd08 path=q6k_mmvq_vdr2"

[[check]]
id = "performance"
capability = "performance"
validator = "autotune-campaign"
required = true
# PVPS02: generic --run-performance-benchmark wiring (docs/planning/
# active/patching-validation-package-standard/PVPS02.md). RD08's real
# lane commands have never carried extra flags on this call path
# (rd08_validation_lane_commands' extra_flags stays empty) -- kept
# empty here to match, not because the field is unsupported.
benchmark-executor = "paired-llama-bench-v1"
benchmark-extra-args = []

[[check]]
id = "controls"
capability = "controls"
validator = "autotune-campaign"
required = true
