# 1250_nro01_allreduce_q8_wire

Plans `PNRO01` (Q8 wire) and `PNRO02` (fused residual). Requires `1252_nro03_allreduce_p2p_provider` (the fork commit sits on top of the P2P commit; its P2P enqueue loop keeps 1252's source-current push).

Generated by `work/forks/make_allreduce_ports.py` with `bigcherry.patch.port_diff`: 3-way merge of e06dcf63 onto b11126 + 1252, verified byte-exact and idempotent. One conflict (P2P enqueue loop wrapped in a profile range) resolved to the fork's structure with the 1252 invariant restored.

Switches (all default off): `GGML_CUDA_AR_WIRE`, `GGML_CUDA_AR_Q8_THRESHOLD`, `GGML_CUDA_AR_FUSED_RESIDUAL`, `GGML_CUDA_AR_PROFILE`, CMake `GGML_HIP_ROCTX` (needs rocprofiler-sdk-roctx).

Activation markers (`BIGCHERRY_PATCH_TRACE`): `patch=1250_nro01 path=allreduce_q8_0_wire`, `patch=1250_nro02 path=allreduce_fused_residual` (logged only when the fused path returned success).

Replaces the earlier kernel-only scaffolds of 1250 and 1251 (1251 removed; its scope is in this commit).
