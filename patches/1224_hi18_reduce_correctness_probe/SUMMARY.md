# 1224_hi18_reduce_correctness_probe: Standalone SPLIT_REDUCE correctness probe, test-hip-reduce (HI18)

**Status:** validated
**Plan item:** HI18

## What it does

Wires a standalone host executable (source in the overlay) into the build that drives the real production META backend through GGML_HIP_REDUCE_PLAN=auto|rccl|meta with a minimal 2-node graph and reports machine-readable execution facts for tools/bigcherry/reduce_correctness.py.

## Why

HI18 needs a native-half correctness-comparison gate that exercises the real META split-state/reduce-plan machinery, not a Python reimplementation that could disagree with it; this is the missing tool HI15/HI16's review update assigned.

## Upstream / provenance

Local design, part of this project's own correctness-evidence work (HI18). D=2 device-count slice only.

**2026-09-19 (PA35 step 4) raw-artifact recheck:** `artifacts/hardware/20260820-qwen38-mtp-dual-xtx/reduction.jsonl`
was searched for across the current checkout and was not found. The raw per-signature
JSONL files referenced in the README are therefore currently non-reinspectable -- honestly
marked as such here rather than silently reconstructed. The recorded aggregate result
(63/63 synthetic executions, 9 real reduction signatures) is preserved as-is; it is a
summary claim, not raw data, and is not itself contradicted by the missing raw artifacts.
If the underlying `reduction.jsonl` is needed for re-inspection in the future, it must be
regenerated by rerunning HI18's real hardware comparison, not reconstructed from this prose.
