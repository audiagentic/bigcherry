schema = 1

[producer.prbe52-adaptive-mtp]
entrypoint = "producer.py"
callable = "run"
trace_probe = "skip"
standard_campaign = "run"
correctness_evidence_cli = "forbid"
performance_benchmark_cli = "forbid"
artifacts = [
  "prbe52-correctness.json",
  "prbe52-performance.json",
  "prbe52-subject-server.log",
  "prbe52-control-server.log",
]
